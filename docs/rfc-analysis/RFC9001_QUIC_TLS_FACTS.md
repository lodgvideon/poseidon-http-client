# RFC 9001 (QUIC-TLS) - normative facts for client conformance

_Generated from local RFC source via FABLE extraction + double independent Opus verification (default-refute). Every quote is verbatim from the fetched RFC 9001 text and confirmed against the source by two verifiers. Rows flagged VERIFY had a verifier disagreement -- correction inline, needs a manual source check._

### 9001 section 1-3 - RFC 9001 — Introduction, Notational Conventions, TLS Overview, Protocol Overview (handshake, key exchange, encryption levels)

| ID | Level | Requirement (scope) | RFC 9001 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `1-3-1` | informative | RFC 2119/8174 key words are normative only when they appear in all capitals. <br>_(all [not-client])_ | are to be interpreted as described in BCP 14 [RFC2119] [RFC8174] when, and only when, they appear in all capitals | N/A — document convention, no code impact. |
| `1-3-2` | informative | In this RFC the acronym TLS means TLS 1.3 (a newer TLS version could be used, per Section 4.2). <br>_(all [CLIENT])_ | the acronym TLS is used to refer to TLS 1.3, though a newer version could be used | quic/handshake.go — verify the tls.Config pins MinVersion/MaxVersion to TLS 1.3 for the QUIC handshake. |
| `1-3-3` | informative | TLS key exchange modes available to QUIC are PSK and (EC)DHE (finite-field or elliptic-curve), which can also be combined. <br>_(all [CLIENT])_ | TLS supports both pre-shared key (PSK) and Diffie-Hellman over either finite fields or elliptic curves ((EC)DHE) key exchanges. | quic/handshake.go — delegated entirely to crypto/tls; no QUIC-layer action, confirm nothing restricts key-exchange modes. |
| `1-3-4` | informative | PSK is the basis for 0-RTT early data; (EC)DHE provides forward secrecy once its keys are destroyed. <br>_(all [CLIENT])_ | PSK is the basis for Early Data (0-RTT); the latter provides forward secrecy (FS) when the (EC)DHE keys are destroyed. | N/A for this client — 0-RTT is explicitly out of scope; PSK/resumption handled inside crypto/tls. |
| `1-3-5` | informative | After the handshake the client has learned and authenticated the server's identity; client authentication is optional for the server. <br>_(client,server [CLIENT])_ | the client will have learned and authenticated an identity for the server, and the server is optionally able to learn and authenticate an identity for the client. | quic/handshake.go — server certificate verification must be on by default (InsecureSkipVerify only via explicit caller opt-in, cf. gosec G402 tuning). |
| `1-3-6` | informative | In the full 1-RTT handshake mode the client can send application data after one round trip. <br>_(client [CLIENT])_ | A full 1-RTT handshake, in which the client is able to send application data after one round trip | quic/handshake.go — handshake state machine implements the 1-RTT flow; this is the only mode the client supports. |
| `1-3-7` | informative | 0-RTT application data is replayable by an attacker and must not carry instructions whose replay could cause unwanted effects. <br>_(client,sender [CLIENT])_ | This application data can be replayed by an attacker, so 0-RTT is not suitable for carrying instructions that might initiate any action that could cause unwanted effects if replayed. | N/A — client does not implement 0-RTT (project non-goal); trivially satisfied, but note it if 0-RTT is ever added. |
| `1-3-8` | informative | The TLS EndOfEarlyData message is not used in QUIC. <br>_(all [CLIENT])_ | Figure 2 omits the EndOfEarlyData message, which is not used in QUIC | quic/handshake.go — confirm the CRYPTO/handshake path never emits or expects EndOfEarlyData (0-RTT unsupported anyway). |
| `1-3-9` | informative | Neither ChangeCipherSpec nor TLS KeyUpdate messages are used by QUIC. <br>_(all [CLIENT])_ | neither ChangeCipherSpec nor KeyUpdate messages are used by QUIC | quic/handshake.go + quic/crypto_keyupdate.go — client must never send TLS KeyUpdate/CCS in CRYPTO frames; later sections make receiving them an error (check handling). |
| `1-3-10` | informative | QUIC uses its own key update mechanism (Section 6) instead of TLS KeyUpdate. <br>_(all [CLIENT])_ | QUIC has its own key update mechanism; see Section 6. | quic/crypto_keyupdate.go — key update implemented per RFC 9001 §6 (Key Phase bit), not via TLS record-layer KeyUpdate. |
| `1-3-11` | informative | Data is protected under four encryption levels: Initial keys, Early data (0-RTT) keys, Handshake keys, and Application data (1-RTT) keys. <br>_(all [CLIENT])_ | Data is protected using a number of encryption levels: | quic/conn_crypto.go + quic/conn_seal.go — per-level sealers/openers must model Initial, Handshake, and 1-RTT (0-RTT level intentionally absent client-side). |
| `1-3-12` | informative | Application data may appear only at the early-data (0-RTT) and application-data (1-RTT) encryption levels. <br>_(sender,receiver [CLIENT])_ | Application data can only appear in the early data and application data levels. | quic/conn_seal.go + quic/conn_recv.go — STREAM/application frames must be sealed only with 1-RTT keys (no 0-RTT), and rejected if seen at Initial/Handshake levels. |
| `1-3-13` | informative | TLS handshake and alert messages may appear at any encryption level. <br>_(all [CLIENT])_ | Handshake and alert messages may appear in any level. | quic/handshake.go + quic/conn_recv.go — CRYPTO-frame handling and CONNECTION_CLOSE(TLS alert) must be accepted at Initial/Handshake/1-RTT levels (per-level refinements come in §4.1.3/§8). |
| `1-3-14` | informative | The 0-RTT handshake can be used only if the client and server have previously communicated. <br>_(client [CLIENT])_ | The 0-RTT handshake can be used if the client and server have previously communicated. | **VERIFY** N/A — 0-RTT out of scope for this client; no resumption-gated early-data path exists. -- Requirement text misstates the quoted sentence's logical form. RFC 9001 §2.1 (line 260-261) says "The 0-RTT handshake can be used if the client and server have previously communicated." — a sufficient condition ("if"), not a necessary one. The fact card renders it as "can be used only if", inserting a necessity that the quoted text does not assert (that restriction is established elsewhere, e.g. the PSK/resumption prerequisite, not by this sentence). Fix: restate the requirement as "The 0-RTT handshake can be used if the client and server have previously communicated" (drop "only"). Quote itself is verbatim, level (informative — no RFC 2119 keyword in the sentence) and applies_to/client_relevant are correct; the reconcile note (0-RTT out of scope) remains valid. |
| `1-3-15` | informative | In the 1-RTT handshake the client cannot send protected application data until it has received all of the server's handshake messages. <br>_(client [CLIENT])_ | the client is unable to send protected application data until it has received all of the handshake messages sent by the server | quic/handshake.go + quic/conn_seal.go — 1-RTT send keys/app writes must be gated on processing the server's Finished (TLS reports 1-RTT keys only then); verify no STREAM frame can be sent before that. |
| `1-3-16` | informative | QUIC itself provides confidentiality and integrity protection of packets, using keys derived from the TLS handshake. <br>_(all [CLIENT])_ | QUIC [QUIC-TRANSPORT] assumes responsibility for the confidentiality and integrity protection of packets. For this it uses keys derived from a TLS handshake [TLS13] | quic/crypto.go + quic/crypto_protect.go — packet protection (AEAD + header protection) keyed from TLS-exported secrets, not TLS record protection. |
| `1-3-17` | informative | TLS handshake and alert messages are carried directly over QUIC (not inside TLS records); QUIC replaces the TLS record layer. <br>_(all [CLIENT])_ | TLS handshake and alert messages are carried directly over the QUIC transport, which takes over the responsibilities of the TLS record layer | quic/handshake.go — CRYPTO frames must carry raw TLS handshake bytes with no TLS record framing (no 5-byte record headers on the wire). |
| `1-3-18` | informative | QUIC relies on TLS for authentication and for negotiation of security- and performance-critical parameters. <br>_(all [CLIENT])_ | QUIC also relies on TLS for authentication and negotiation of parameters that are critical to security and performance. | quic/handshake.go + quic/transport_params.go — ALPN and the quic_transport_parameters extension are negotiated inside the TLS handshake, not out of band. |
| `1-3-19` | informative | QUIC provides a reliable, ordered stream abstraction to TLS for sending and receiving handshake messages. <br>_(all [CLIENT])_ | The TLS component sends and receives messages via the QUIC component, with QUIC providing a reliable stream abstraction to TLS. | **VERIFY** quic/handshake.go — CRYPTO stream buffering must reassemble out-of-order CRYPTO frames per level and deliver contiguous ordered bytes to TLS. -- **VERIFY** re-verified by a fresh pair: pass B refutes the paraphrase -- the quote says only "a reliable stream abstraction" for "messages"; drop "ordered" (or re-quote lines 294-296, which do say "reliability, ordered delivery, and record layer provided by QUIC"). |
| `1-3-20` | informative | TLS supplies updates to QUIC: new packet protection keys to install and state changes such as handshake completion and the server certificate. <br>_(all [CLIENT])_ | including (a) new packet protection keys to install and (b) state changes such as handshake completion, the server certificate, etc. | quic/conn_crypto.go — per-level sealer/opener installation driven by TLS key callbacks; handshake-completion state surfaced to the connection (Figure 4: 0-RTT/Handshake/1-RTT keys, Handshake Done). |
| `1-3-21` | informative | QUIC applications send data as QUIC STREAM frames (or other frame types) in QUIC packets, never as TLS Application Data records. <br>_(sender [CLIENT])_ | QUIC applications that want to send data do not send it using TLS Application Data records. Rather, they send it as QUIC STREAM frames or other frame types | quic/conn_seal.go + http3 request path — application bytes go into STREAM frames sealed at 1-RTT; verify TLS application_data records are never produced or accepted. |

### 9001 section 4.1-4.5 - RFC 9001: Carrying TLS Messages — interface to TLS, handshake complete/confirmed, encryption levels, TLS version, ClientHello size, peer authentication, session resumption

| ID | Level | Requirement (scope) | RFC 9001 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `4.1-4.5-1` | MUST | If QUIC retransmits TLS handshake data, it must use the keys the data was originally associated with, even if TLS has since moved to newer keys. <br>_(endpoint,sender [CLIENT])_ | If QUIC needs to retransmit that data, it MUST use the same keys even if TLS has already updated to newer keys. | quic/handshake.go + loss recovery: lost CRYPTO data must be retransmitted at its original encryption level, never re-encrypted at the current level. |
| `4.1-4.5-2` | informative | Each encryption level corresponds to a packet number space, and the packet number space determines which frames are permitted (per RFC 9000 §12.5). <br>_(endpoint [CLIENT])_ | Each encryption level corresponds to a packet number space. The packet number space that is used determines the semantics of frames. | quic/conn_recv.go + packet.go: verify per-PN-space frame-type validation (frames prohibited in Initial/Handshake spaces rejected). |
| `4.1-4.5-3` | SHOULD | When packets of different types need to be sent at the same time, endpoints should coalesce them into the same UDP datagram. <br>_(endpoint,sender [CLIENT])_ | When packets of different types need to be sent, endpoints SHOULD use coalesced packets to send them in the same UDP datagram. | quic/handshake.go send path: check the client coalesces Initial+Handshake (and Handshake+1-RTT) packets in one datagram during the handshake. |
| `4.1-4.5-4` | informative | Table 1 codepoint mapping: Initial packets use Initial secrets/Initial PN space; 0-RTT uses 0-RTT keys/app-data space; Handshake uses Handshake keys/space; Retry uses Retry keys; Version Negotiation unprotected; short header uses 1-RTT keys/app-data space. <br>_(endpoint [CLIENT])_ | QUIC uses the packet type to indicate which keys were used to protect a given packet, as shown in Table 1. | quic/packet.go + conn_seal.go/conn_crypto.go: sealer/opener selection keyed off packet type must match Table 1 (Retry integrity in retry.go). |
| `4.1-4.5-5` | informative | QUIC and TLS must agree on which layer is responsible for validating peer credentials (certificate validation). <br>_(endpoint [CLIENT])_ | QUIC and TLS need to agree on which is responsible for validation of peer credentials, such as certificate validation | quic/handshake.go: certificate validation is delegated to crypto/tls; confirm the QUIC layer never bypasses it (G402 opt-in only). |
| `4.1-4.5-6` | informative | The TLS handshake is complete when the TLS stack has both sent a Finished message and verified the peer's Finished message; completion is per-endpoint, not simultaneous. <br>_(endpoint [CLIENT])_ | the TLS handshake is considered complete when the TLS stack has reported that the handshake is complete. | quic/handshake.go: handshake-complete state must come from the crypto/tls handshake-done signal, not from packet-level heuristics. |
| `4.1-4.5-7` | MUST | The server must send a HANDSHAKE_DONE frame as soon as the handshake completes; at the server the handshake is confirmed at completion. <br>_(server [not-client])_ | The server MUST send a HANDSHAKE_DONE frame as soon as the handshake is complete. | quic/: server-only send obligation; client side must merely accept HANDSHAKE_DONE (see 4.1-4.5-8) — check conn_recv.go treats it as valid only in 1-RTT. |
| `4.1-4.5-8` | informative | At the client, the handshake is confirmed when a HANDSHAKE_DONE frame is received. <br>_(client [CLIENT])_ | At the client, the handshake is considered confirmed when a HANDSHAKE_DONE frame is received. | quic/handshake.go: confirmed flag set on HANDSHAKE_DONE receipt; this gates key update initiation (crypto_keyupdate.go) and Handshake-key discard. |
| `4.1-4.5-9` | MAY | A client may alternatively treat the handshake as confirmed when a 1-RTT packet it sent is acknowledged (lowest 1-RTT PN sent <= Largest Acknowledged in a 1-RTT ACK). <br>_(client [CLIENT])_ | a client MAY consider the handshake to be confirmed when it receives an acknowledgment for a 1-RTT packet. | quic/handshake.go: optional optimization — verify whether implemented; HANDSHAKE_DONE-only confirmation is conforming. |
| `4.1-4.5-10` | informative | Before starting the handshake, QUIC provides TLS with the transport parameters it wishes to carry (as the quic_transport_parameters extension). <br>_(endpoint,client [CLIENT])_ | Before starting the handshake, QUIC provides TLS with the transport parameters (see Section 8.2) that it wishes to carry. | quic/transport_params.go: client transport parameters must be encoded and installed into the TLS extension before the ClientHello is produced. |
| `4.1-4.5-11` | informative | A QUIC client starts TLS by requesting handshake bytes and acquires those bytes (the ClientHello) before sending its first packet. <br>_(client [CLIENT])_ | The client acquires handshake bytes before sending its first packet. | quic/handshake.go: ClientHello must be pulled from crypto/tls before the first Initial packet is built and sent. |
| `4.1-4.5-12` | informative | Handshake bytes from TLS are appended to the per-encryption-level CRYPTO byte sequence, and that level determines the packet type carrying the resulting CRYPTO frame (Table 1). <br>_(endpoint,sender [CLIENT])_ | When TLS provides handshake bytes to be sent, they are appended to the handshake bytes for the current encryption level. | quic/handshake.go: per-level CRYPTO send offsets; CRYPTO frames must be emitted in the packet type matching their level. |
| `4.1-4.5-13` | informative | Four encryption levels exist (Initial, 0-RTT, Handshake, 1-RTT) but CRYPTO frames are carried in only three — never at the 0-RTT level; 0-RTT and 1-RTT share the application-data PN space. <br>_(endpoint [CLIENT])_ | CRYPTO frames are carried in just three of these levels, omitting the 0-RTT level. | quic/handshake.go + conn_recv.go: reject/never emit CRYPTO at 0-RTT (moot for send since 0-RTT unimplemented); PN-space mapping in packet.go. |
| `4.1-4.5-14` | informative | CRYPTO frame content is the unprotected content of TLS handshake records; TLS record-layer protection is not used — QUIC packet protection protects them instead. <br>_(endpoint [CLIENT])_ | QUIC takes the unprotected content of TLS handshake records as the content of CRYPTO frames. TLS record protection is not used by QUIC. | quic/handshake.go: raw handshake bytes exchanged with the TLS stack, no TLS record framing; protection lives in crypto_protect.go. |
| `4.1-4.5-15` | informative | CRYPTO frames carry only TLS handshake messages; TLS alerts become QUIC CONNECTION_CLOSE error codes, and TLS application data or any other content type from the TLS stack is an error. <br>_(endpoint [CLIENT])_ | TLS application data and other content types cannot be carried by QUIC at any encryption level; it is an error if they are received from the TLS stack. | quic/errors.go: TLS alert -> CRYPTO_ERROR code (0x0100+alert) mapping on CONNECTION_CLOSE; handshake.go must not route app data through CRYPTO. |
| `4.1-4.5-16` | informative | CRYPTO data arriving at the current TLS receiving encryption level is sequenced by offset into the input flow and delivered to TLS in order. <br>_(receiver,endpoint [CLIENT])_ | As with STREAM frames, the offset is used to find the proper location in the data sequence. | quic/handshake.go: per-level CRYPTO reassembly buffer keyed on offset; only contiguous new data is passed to crypto/tls. |
| `4.1-4.5-17` | MUST NOT | A CRYPTO frame in a packet from a previously installed encryption level must not contain data extending past the end of previously received data in that level's flow. <br>_(sender,endpoint [CLIENT])_ | it MUST NOT contain data that extends past the end of previously received data in that flow. | quic/handshake.go: receiver-side check — after a level's handshake data is consumed, new bytes beyond the recorded end at that level are a violation. |
| `4.1-4.5-18` | MUST | Receiving CRYPTO data that extends past previously received data at an old encryption level must be treated as a connection error of type PROTOCOL_VIOLATION. <br>_(receiver,endpoint [CLIENT])_ | Implementations MUST treat any violations of this requirement as a connection error of type PROTOCOL_VIOLATION. | quic/handshake.go + errors.go: verify a typed PROTOCOL_VIOLATION (0x0a) connection close exists for new-data-on-old-level, with a conformance test. |
| `4.1-4.5-19` | informative | CRYPTO data arriving for a not-yet-current (new) encryption level is saved and provided to TLS once TLS moves to receiving at that level. <br>_(receiver,endpoint [CLIENT])_ | If the packet is from a new encryption level, it is saved for later processing by TLS. | quic/conn_recv.go (undecryptable-packet buffering) + handshake.go (per-level CRYPTO buffers): future-level data must be retained, not dropped. |
| `4.1-4.5-20` | MUST | When TLS provides keys for a higher encryption level while unconsumed CRYPTO data remains at a previous level, the endpoint must treat it as a connection error of type PROTOCOL_VIOLATION. <br>_(endpoint,receiver [CLIENT])_ | if there is data from a previous encryption level that TLS has not consumed, this MUST be treated as a connection error of type PROTOCOL_VIOLATION. | quic/handshake.go: on installing keys for a higher level, check for leftover unconsumed CRYPTO at lower levels and close with PROTOCOL_VIOLATION. |
| `4.1-4.5-21` | informative | Each time new CRYPTO data is delivered to TLS, QUIC requests new handshake bytes from TLS (TLS may return nothing if messages are incomplete). <br>_(endpoint [CLIENT])_ | Each time that TLS is provided with new data, new handshake bytes are requested from TLS. | quic/handshake.go: the drive loop must poll TLS output (and new keys) after every CRYPTO delivery, tolerating empty output. |
| `4.1-4.5-22` | informative | TLS buffers in-order incomplete handshake bytes; QUIC is responsible for buffering handshake bytes that arrive out of order or for encryption levels not yet ready. <br>_(endpoint,receiver [CLIENT])_ | QUIC is responsible for buffering handshake bytes that arrive out of order or for encryption levels that are not yet ready. | quic/handshake.go: out-of-order CRYPTO reassembly buffer per level; bound it and close with CRYPTO_BUFFER_EXCEEDED per RFC 9000 §7.5 when exceeded. |
| `4.1-4.5-23` | informative | QUIC provides no flow control for CRYPTO frames — the receiver must impose its own buffering limit (RFC 9000 §7.5, CRYPTO_BUFFER_EXCEEDED). <br>_(endpoint [CLIENT])_ | QUIC does not provide any means of flow control for CRYPTO frames; see Section 7.5 of [QUIC-TRANSPORT]. | quic/handshake.go + errors.go: confirm a hard cap on buffered CRYPTO with CRYPTO_BUFFER_EXCEEDED (0x0d) close — peer-input-policy bug shape. |
| `4.1-4.5-24` | informative | Once the TLS handshake completes, the transport parameters the peer advertised during the handshake are considered authenticated. <br>_(endpoint [CLIENT])_ | At this stage, the transport parameters that the peer advertised during the handshake are authenticated | quic/transport_params.go: peer TPs are only fully trustworthy at handshake completion; check nothing irreversible relies on them earlier. |
| `4.1-4.5-25` | informative | After the handshake completes, QUIC must still deliver arriving CRYPTO-stream data to TLS and request new data afterwards (e.g., session tickets); TLS is otherwise passive. <br>_(endpoint [CLIENT])_ | When the handshake is complete, QUIC only needs to provide TLS with any data that arrives in CRYPTO streams. | quic/handshake.go: post-handshake 1-RTT CRYPTO frames (NewSessionTicket) must be fed to crypto/tls, not rejected or ignored. |
| `4.1-4.5-26` | SHOULD | While waiting for asynchronous TLS processing (e.g., certificate validation), an endpoint should buffer received packets that might be decryptable once pending keys become available. <br>_(endpoint,receiver [CLIENT])_ | an endpoint SHOULD buffer received packets if they might be processed using keys that are not yet available. | quic/conn_recv.go: undecryptable-packet queue replayed on key install (bounded); check Handshake/1-RTT packets arriving before keys are kept. |
| `4.1-4.5-27` | SHOULD | An endpoint should continue responding to packets it can process while waiting for TLS processing to complete. <br>_(endpoint [CLIENT])_ | An endpoint SHOULD continue to respond to packets that can be processed during this time. | quic/conn_recv.go: processing of decryptable packets must not stall behind buffered undecryptable ones. |
| `4.1-4.5-28` | informative | For each new encryption level, TLS provides QUIC three items — a secret, an AEAD function, and a KDF — from which QUIC generates packet and header protection keys (§5, §5.4). <br>_(endpoint [CLIENT])_ | TLS provides QUIC with three items as a new encryption level becomes available | quic/conn_crypto.go + crypto.go: per-level sealer/opener derivation must use the TLS-negotiated secret, AEAD, and hash — not hardcoded AES-128 beyond Initial. |
| `4.1-4.5-29` | informative | QUIC may operate at multiple encryption levels simultaneously (e.g., retransmitting a lost Finished at Handshake level while sending 1-RTT data) and must handle packets above and below TLS's current level. <br>_(endpoint [CLIENT])_ | Although TLS only uses one encryption level at a time, QUIC may use more than one level. | quic/conn_seal.go + conn_recv.go: keep Handshake keys usable alongside 1-RTT until discard conditions; do not tear down a level the moment the next one opens. |
| `4.1-4.5-30` | informative | TLS must hand keys to QUIC before it would produce them for its own use — e.g., a client needs Handshake keys to acknowledge Handshake packets before it sends CRYPTO at that level. <br>_(endpoint,client [CLIENT])_ | TLS therefore needs to provide keys to QUIC before it might produce them for its own use. | quic/handshake.go: install Handshake rx/tx keys as soon as crypto/tls exposes them so the client can ACK server Handshake packets immediately. |
| `4.1-4.5-31` | MUST NOT | Clients must not offer TLS versions older than 1.3 in the handshake. <br>_(client [CLIENT])_ | Clients MUST NOT offer TLS versions older than 1.3. | quic/handshake.go: tls.Config must force MinVersion=tls.VersionTLS13, including when the caller supplies a config with a lower MinVersion. |
| `4.1-4.5-32` | MUST | An endpoint must terminate the connection if a TLS version older than 1.3 is negotiated. <br>_(endpoint [CLIENT])_ | An endpoint MUST terminate the connection if a version of TLS older than 1.3 is negotiated. | quic/handshake.go: with MinVersion pinned, crypto/tls enforces this; verify caller-supplied tls.Config cannot relax it below 1.3. |
| `4.1-4.5-33` | informative | Negotiating a TLS version newer than 1.3 is acceptable provided the TLS 1.3 features QUIC uses are supported by that version. <br>_(endpoint [CLIENT])_ | This is acceptable provided that the features of TLS 1.3 that are used by QUIC are supported by the newer version. | quic/handshake.go: no action — crypto/tls currently tops out at 1.3; nothing must hard-reject a hypothetical newer version. |
| `4.1-4.5-34` | MAY | Servers may use the Retry feature so that they only buffer partial ClientHello messages from clients whose address has been validated. <br>_(server [not-client])_ | servers MAY use the Retry feature (see Section 8.1 of [QUIC-TRANSPORT]) to only buffer partial ClientHello messages from clients with a validated address. | quic/retry.go: server-side choice; the client-side consequence (handling a received Retry, integrity tag validation) is governed by RFC 9000/9001 §5.8, already retry.go's job. |
| `4.1-4.5-35` | informative | QUIC packet and framing overhead on the ClientHello is at least 36 bytes, growing with a non-zero-length Source Connection ID, a token, or a Destination Connection ID longer than 8 bytes. <br>_(client [CLIENT])_ | QUIC packet and framing add at least 36 bytes of overhead to the ClientHello message. | quic/packet.go + handshake.go: sizing note for fitting the ClientHello in the first 1200-byte Initial; relevant if client SCID length or token is large. |
| `4.1-4.5-36` | informative | TLS need not pad the ClientHello to QUIC's minimum Initial datagram size; QUIC itself uses PADDING frames or packet coalescing to satisfy RFC 9000 §14.1 datagram size requirements. <br>_(endpoint,client [CLIENT])_ | QUIC implementations use PADDING frames or packet coalescing to ensure that datagrams are large enough. | quic/handshake.go: client datagrams carrying Initial packets must be padded to >=1200 bytes at the QUIC layer (PADDING or coalescing), not by TLS. |
| `4.1-4.5-37` | MUST | A client must authenticate the identity of the server, typically via certificate verification against a trusted issuer. <br>_(client [CLIENT])_ | A client MUST authenticate the identity of the server. | quic/handshake.go: ServerName must be set on tls.Config and InsecureSkipVerify must remain a deliberate caller opt-in (G402), never a silent default. |
| `4.1-4.5-38` | MAY | A server may request client authentication during the handshake. <br>_(server [CLIENT])_ | A server MAY request that the client authenticate during the handshake. | quic/handshake.go: client must tolerate an in-handshake CertificateRequest — crypto/tls handles it; verify tls.Config.Certificates passes through for mTLS. |
| `4.1-4.5-39` | MAY | A server may refuse a connection if the client cannot authenticate when requested. <br>_(server [not-client])_ | A server MAY refuse a connection if the client is unable to authenticate when requested. | quic/errors.go: client only needs to surface the resulting TLS-alert CONNECTION_CLOSE (0x0100-0x01ff) cleanly, no special handling. |
| `4.1-4.5-40` | MUST NOT | Servers must not use post-handshake client authentication and must not send post-handshake TLS CertificateRequest messages. <br>_(server [not-client])_ | servers MUST NOT send post-handshake TLS CertificateRequest messages | Server-only prohibition; the client-side enforcement half is 4.1-4.5-41 (handshake.go/errors.go). |
| `4.1-4.5-41` | MUST | A client must treat receipt of a post-handshake TLS CertificateRequest message as a connection error of type PROTOCOL_VIOLATION. <br>_(client [CLIENT])_ | clients MUST treat receipt of such messages as a connection error of type PROTOCOL_VIOLATION. | quic/handshake.go + errors.go: check how post-handshake CRYPTO other than NewSessionTicket/KeyUpdate surfaces from crypto/tls; the conn must close with PROTOCOL_VIOLATION (0x0a), not ignore it. |
| `4.1-4.5-42` | informative | TLS 1.3 session resumption is done by carrying NewSessionTicket messages in CRYPTO frames after the handshake completes; resumption is usable with or without 0-RTT. <br>_(endpoint [CLIENT])_ | It does this by carrying NewSessionTicket messages in CRYPTO frames after the handshake is complete. | quic/handshake.go: 1-RTT CRYPTO frames after completion must reach crypto/tls; resumption support hinges on tls.Config.ClientSessionCache passthrough. |
| `4.1-4.5-43` | informative | QUIC itself requires no retained state when resuming a connection unless 0-RTT is also used; only TLS-level state (RFC 8446 §4.6.1) is needed. <br>_(endpoint [CLIENT])_ | QUIC itself does not depend on any state being retained when resuming a connection unless 0-RTT is also used | quic/handshake.go: since 0-RTT is out of scope, resumption (if enabled via session cache) needs no extra QUIC-layer state — confirm none is assumed. |
| `4.1-4.5-44` | SHOULD NOT | Clients should not reuse session tickets, because reuse lets entities other than the server correlate connections. <br>_(client [CLIENT])_ | Clients SHOULD NOT reuse tickets as that allows entities other than the server to correlate connections | quic/handshake.go: if ClientSessionCache is wired up, check crypto/tls single-use ticket behavior; a load-generator reusing one ticket across many conns violates this. |
| `4.1-4.5-45` | informative | All information used to establish 0-RTT (TLS state, QUIC transport parameters, chosen application protocol, application data) must come from the same original connection; endpoints cannot selectively disregard any of it. <br>_(endpoint,client [CLIENT])_ | all information used to establish 0-RTT comes from the same connection. Endpoints cannot selectively disregard information that might alter the sending or processing of 0-RTT. | quic/handshake.go: 0-RTT deliberately unimplemented — verify the client never installs 0-RTT keys or emits 0-RTT packets, making this vacuously satisfied. |
| `4.1-4.5-46` | informative | TLS 1.3 imposes a seven-day limit between the original connection and any attempt to use 0-RTT; replay-exposure constraints (§9.2) also apply. <br>_(endpoint,client [CLIENT])_ | [TLS13] sets a limit of seven days on the time between the original connection and any attempt to use 0-RTT. | quic/handshake.go: not applicable while 0-RTT is unimplemented; record as a constraint if 0-RTT is ever added. |

### 9001 section 4.6-4.9 - RFC 9001: 0-RTT (enabling, accepting/rejecting, validating), HelloRetryRequest, TLS Errors, Discarding Unused Keys (Initial, Handshake, 0-RTT)

| ID | Level | Requirement (scope) | RFC 9001 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `4.6-4.9-1` | informative | QUIC does not use the TLS early-data mechanism itself; early data is carried in QUIC 0-RTT packets instead. <br>_(all [CLIENT])_ | QUIC does not use TLS early data. QUIC uses 0-RTT packets to carry early data. | quic/handshake.go — client is no-0-RTT by design; confirm TLS layer never sends early data records and no 0-RTT packet type is emitted. |
| `4.6-4.9-2` | informative | In NewSessionTicket, max_early_data_size is repurposed as a sentinel 0xffffffff meaning the server is willing to accept QUIC 0-RTT data. <br>_(server,client [CLIENT])_ | the max_early_data_size parameter is repurposed to hold a sentinel value 0xffffffff to indicate that the server is willing to accept QUIC 0-RTT data | quic/handshake.go — if NewSessionTicket messages on the 1-RTT CRYPTO stream are parsed, interpret early_data=0xffffffff as 0-RTT-capable; at minimum they must be consumed without error. |
| `4.6-4.9-3` | informative | A server that does not accept 0-RTT omits the early_data extension from the NewSessionTicket. <br>_(server [CLIENT])_ | To indicate that the server does not accept 0-RTT data, the early_data extension is omitted from the NewSessionTicket. | **VERIFY** quic/handshake.go — absence of early_data in a ticket means no 0-RTT; N/A for sending (0-RTT non-goal) but parsing must tolerate both forms. -- Quote is exact (lines 800-802) and the level is correctly informative (no RFC 2119 keyword in the sentence), but the role list is inconsistent with client_relevant=true. This is the client-observable counterpart of 4.6-4.9-2 (adjacent sentence, same NewSessionTicket early_data extension) and structurally identical to 4.6-4.9-10 (server signals rejection by omitting the extension); both of those list applies_to ["server","client"]. The catalog's own reconcile note ("parsing must tolerate both forms") is a client action. Fix: applies_to should be ["server","client"] with client_relevant true (or, if applies_to is meant to name only the acting role, client_relevant must be false — but that contradicts facts 2/9/10). |
| `4.6-4.9-4` | informative | The amount of data a client can send in QUIC 0-RTT is bounded by the server-supplied initial_max_data transport parameter, not max_early_data_size. <br>_(client [CLIENT])_ | The amount of data that the client can send in QUIC 0-RTT is controlled by the initial_max_data transport parameter supplied by the server. | quic/transport_params.go — N/A while 0-RTT is a non-goal; if resumption is ever added, remembered initial_max_data governs 0-RTT flow-control credit. |
| `4.6-4.9-5` | MUST NOT | Servers must never send an early_data extension whose max_early_data_size is anything other than 0xffffffff. <br>_(server [not-client])_ | Servers MUST NOT send the early_data extension with a max_early_data_size field set to any value other than 0xffffffff. | Server-only emission rule; the client-side check is 4.6-4.9-6. |
| `4.6-4.9-6` | MUST | A client receiving a NewSessionTicket whose early_data extension carries any value other than 0xffffffff must close the connection with PROTOCOL_VIOLATION. <br>_(client [CLIENT])_ | A client MUST treat receipt of a NewSessionTicket that contains an early_data extension with any other value as a connection error of type PROTOCOL_VIOLATION | quic/handshake.go — check post-handshake NewSessionTicket handling: if the ticket is parsed at all, validate max_early_data_size==0xffffffff and raise PROTOCOL_VIOLATION (errors.go) otherwise; if tickets are skipped unparsed, document that this check is unreachable. |
| `4.6-4.9-7` | informative | A client wishing to send 0-RTT offers the early_data extension in the ClientHello of a subsequent (resumed) handshake and then sends application data in 0-RTT packets. <br>_(client [CLIENT])_ | A client that wishes to send 0-RTT packets uses the early_data extension in the ClientHello message of a subsequent handshake | quic/handshake.go — N/A (0-RTT non-goal): confirm ClientHello never includes early_data so the server can never accept 0-RTT. |
| `4.6-4.9-8` | informative | A client attempting 0-RTT may additionally present an address validation token previously received in a NEW_TOKEN frame. <br>_(client [CLIENT])_ | A client that attempts 0-RTT might also provide an address validation token if the server has sent a NEW_TOKEN frame | quic/handshake.go + quic/packet.go — N/A for 0-RTT; independent NEW_TOKEN reuse in Initial packets is a QUIC-TRANSPORT §8.1 matter. |
| `4.6-4.9-9` | informative | Server acceptance of 0-RTT is signaled by an early_data extension in EncryptedExtensions. <br>_(server,client [CLIENT])_ | A server accepts 0-RTT by sending an early_data extension in the EncryptedExtensions | quic/handshake.go — EncryptedExtensions parsing may ignore early_data since the client never offers it, but must not choke on its presence/absence. |
| `4.6-4.9-10` | informative | Server rejection of 0-RTT is signaled by EncryptedExtensions lacking the early_data extension. <br>_(server,client [CLIENT])_ | A server rejects 0-RTT by sending the EncryptedExtensions without an early_data extension. | quic/handshake.go — same as 4.6-4.9-9; rejection detection is N/A without 0-RTT offers. |
| `4.6-4.9-11` | informative | Sending a TLS HelloRetryRequest always implies the server rejects 0-RTT. <br>_(server,client [CLIENT])_ | A server will always reject 0-RTT if it sends a TLS HelloRetryRequest. | quic/handshake.go — if HRR is handled (see 4.6-4.9-17), any future 0-RTT support must treat HRR as 0-RTT rejection. |
| `4.6-4.9-12` | MUST NOT | When rejecting 0-RTT the server must not process any 0-RTT packets, even if it is able to. <br>_(server [not-client])_ | When rejecting 0-RTT, a server MUST NOT process any 0-RTT packets, even if it could. | Server-only; client never sends 0-RTT packets. |
| `4.6-4.9-13` | SHOULD | After 0-RTT rejection, a client should treat an acknowledgment of a 0-RTT packet as a PROTOCOL_VIOLATION connection error if it can detect it. <br>_(client [CLIENT])_ | a client SHOULD treat receipt of an acknowledgment for a 0-RTT packet as a connection error of type PROTOCOL_VIOLATION, if it is able to detect the condition | quic/conn_recv.go — N/A: no 0-RTT packets are ever sent, so no 0-RTT packet numbers exist to be acked; nothing to detect. |
| `4.6-4.9-14` | MUST | On 0-RTT rejection the client must reset the state of all streams, including application state bound to those streams. <br>_(client [CLIENT])_ | The client therefore MUST reset the state of all streams, including application state bound to those streams. | quic/ + http3/ — N/A now; if 0-RTT is ever added, rejection must tear down all early stream and HTTP/3 state (settings, QPACK assumptions) before replay over 1-RTT. |
| `4.6-4.9-15` | MAY | A client may reattempt 0-RTT after receiving a Retry or Version Negotiation packet; those packets do not signify 0-RTT rejection. <br>_(client [CLIENT])_ | A client MAY reattempt 0-RTT if it receives a Retry or Version Negotiation packet. These packets do not signify rejection of 0-RTT. | quic/retry.go — N/A for 0-RTT; note Retry handling must not be conflated with any rejection semantics if 0-RTT is added. |
| `4.6-4.9-16` | informative | QUIC requires additional transport state (e.g., remembered transport parameters) to be associated with a 0-RTT session ticket, used to decide whether 0-RTT must be rejected. <br>_(server [not-client])_ | QUIC requires additional transport state to be associated with a 0-RTT session ticket. | Server-side ticket/state design; client analog (remembering server transport params for resumption) is out of scope while 0-RTT is a non-goal. |
| `4.6-4.9-17` | informative | HelloRetryRequest is not special-cased by QUIC: it is carried like any other handshake message in Initial packets, and the client must handle it at the Initial encryption level. <br>_(all [CLIENT])_ | From the perspective of QUIC, HelloRetryRequest is not differentiated from other cryptographic handshake messages that are carried in Initial packets. | quic/handshake.go — verify the TLS driver accepts an HRR arriving on the Initial CRYPTO stream and sends the second ClientHello still at the Initial encryption level (Initial keys unchanged); check crypto stream buffering handles the extra round trip. |
| `4.6-4.9-18` | SHOULD | QUIC implementations should use the QUIC Retry feature rather than HelloRetryRequest for address verification. <br>_(server [not-client])_ | QUIC implementations SHOULD instead use the Retry feature | Server-side choice; client must support both anyway (retry.go for Retry, handshake.go for HRR). |
| `4.6-4.9-19` | informative | A TLS error produces an appropriate TLS alert as defined in RFC 8446 Section 6. <br>_(endpoint [CLIENT])_ | If TLS experiences an error, it generates an appropriate alert as defined in Section 6 of [TLS13]. | quic/handshake.go — check local TLS failure paths produce a concrete alert code (not a generic Go error) so the mapping in 4.6-4.9-20 can apply. |
| `4.6-4.9-20` | informative | A TLS alert maps to a QUIC error code by adding the AlertDescription value to 0x0100, landing in the CRYPTO_ERROR reserved range (0x0100-0x01ff). <br>_(endpoint [CLIENT])_ | The AlertDescription value is added to 0x0100 to produce a QUIC error code from the range reserved for CRYPTO_ERROR | quic/errors.go — verify the 0x0100-0x01ff CRYPTO_ERROR mapping is exactly alert+0x0100, both when emitting local alerts and when decoding peer CONNECTION_CLOSE codes for error reporting. |
| `4.6-4.9-21` | informative | The alert-derived error code is sent in a QUIC CONNECTION_CLOSE frame of type 0x1c (the transport-level close frame). <br>_(endpoint,sender [CLIENT])_ | The resulting value is sent in a QUIC CONNECTION_CLOSE frame of type 0x1c. | quic/handshake.go + connection close path — TLS alert errors must go out in frame type 0x1c (transport close), never 0x1d (application close). |
| `4.6-4.9-22` | MUST | A QUIC endpoint must treat every TLS alert as fatal regardless of its nominal level, since QUIC can only convey fatal alerts. <br>_(endpoint [CLIENT])_ | a QUIC endpoint MUST treat any alert from TLS as if it were at the "fatal" level | quic/handshake.go — any alert received or generated (including close_notify/user_canceled class warnings) must terminate the connection; verify no warning-alert tolerance path exists. |
| `4.6-4.9-23` | MAY | Endpoints may substitute a generic error code for a specific TLS alert to avoid exposing confidential information. <br>_(endpoint [CLIENT])_ | Endpoints MAY use a generic error code to avoid possibly exposing confidential information. | quic/errors.go + handshake.go — optional: client may coarsen outgoing alert codes; must still accept any 0x0100-0x01ff code from the peer. |
| `4.6-4.9-24` | informative | The generic substitute for any TLS alert is a generic alert such as handshake_failure, which is codepoint 0x0128 in QUIC error-code space. <br>_(endpoint [CLIENT])_ | this includes replacing any alert with a generic alert, such as handshake_failure (0x0128 in QUIC) | quic/errors.go — if a generic crypto-error constant exists, it should be 0x0128 (handshake_failure 40 + 0x0100). |
| `4.6-4.9-25` | MUST | Retransmissions of CRYPTO frame data originally sent at a lower encryption level must be sent at that same encryption level. <br>_(endpoint,sender [CLIENT])_ | If packets from a lower encryption level contain CRYPTO frames, frames that retransmit that data MUST be sent at the same encryption level. | quic/ loss-recovery + handshake.go — verify lost Initial/Handshake CRYPTO data is retransmitted using the original level's sealer (conn_seal.go), never promoted to a higher level. |
| `4.6-4.9-26` | informative | Acknowledgments are generated at the same encryption level as the packet being acknowledged. <br>_(endpoint [CLIENT])_ | an endpoint generates acknowledgments for packets at the same encryption level as the packet being acknowledged | quic/conn_recv.go + send path — per-packet-number-space ACK frames must be sent in packets of the matching level (Initial acks in Initial packets, etc.). |
| `4.6-4.9-27` | informative | Keys for an encryption level cannot be discarded until the endpoint has received all of the peer's handshake messages at that level and the peer has done the same. <br>_(endpoint [CLIENT])_ | An endpoint cannot discard keys for a given encryption level unless it has received all the cryptographic handshake messages from its peer at that encryption level | quic/conn_crypto.go — key-discard triggers must follow the concrete rules of 4.9.1/4.9.2, which encode this condition; do not drop openers while peer CRYPTO data at that level may still arrive. |
| `4.6-4.9-28` | MUST | New data must always be sent at the highest currently available encryption level, even while older keys are retained. <br>_(endpoint,sender [CLIENT])_ | new data MUST be sent at the highest currently available encryption level | quic/conn_seal.go + send path — packet assembly must pick the highest installed sealer for any new frames; only the 4.6-4.9-25/29 exceptions use older levels. |
| `4.6-4.9-29` | informative | Only ACK frames and retransmissions of CRYPTO frame data may be sent at a previous encryption level. <br>_(endpoint,sender [CLIENT])_ | Only ACK frames and retransmissions of data in CRYPTO frames are sent at a previous encryption level. | quic/ send path — verify no other frame types (STREAM, flow control, etc.) can be scheduled into Initial/Handshake packets after 1-RTT keys exist. |
| `4.6-4.9-30` | MAY | Packets sent at a previous encryption level may also include PADDING frames. <br>_(endpoint,sender [CLIENT])_ | These packets MAY also include PADDING frames. | quic/ send path — PADDING is permitted alongside ACK/CRYPTO in lower-level packets (needed for Initial datagram-size minimums). |
| `4.6-4.9-31` | MUST | A client must discard Initial keys the first time it sends a Handshake packet. <br>_(client [CLIENT])_ | a client MUST discard Initial keys when it first sends a Handshake packet | quic/conn_crypto.go + conn_seal.go — verify Initial sealer AND opener are dropped exactly when the first Handshake-level packet is sent (not when Handshake keys install), and conn_recv.go then discards late Initial packets as undecryptable. |
| `4.6-4.9-32` | MUST | A server must discard Initial keys when it first successfully processes a Handshake packet. <br>_(server [not-client])_ | a server MUST discard Initial keys when it first successfully processes a Handshake packet | Server-only trigger; client trigger is 4.6-4.9-31. |
| `4.6-4.9-33` | MUST NOT | After discarding Initial keys, endpoints must not send any further Initial packets. <br>_(endpoint [CLIENT])_ | Endpoints MUST NOT send Initial packets after this point. | quic/ send path + handshake.go — once the first Handshake packet is sent, no path (including loss recovery and PTO probes) may emit an Initial packet. |
| `4.6-4.9-34` | informative | Discarding Initial keys entails abandoning loss-recovery state for the Initial encryption level and ignoring outstanding Initial packets. <br>_(endpoint [CLIENT])_ | This results in abandoning loss recovery state for the Initial encryption level and ignoring any outstanding Initial packets. | quic/ loss recovery — on Initial key discard, drop the Initial PN space's sent-packet state (no spurious retransmission, remove from bytes-in-flight) per RFC 9002 §6.4. |
| `4.6-4.9-35` | MUST | An endpoint must discard its Handshake keys when the TLS handshake is confirmed. <br>_(endpoint [CLIENT])_ | An endpoint MUST discard its Handshake keys when the TLS handshake is confirmed | quic/conn_crypto.go + conn_recv.go — for a client, handshake confirmed = receipt of HANDSHAKE_DONE (RFC 9001 §4.1.2); verify HANDSHAKE_DONE triggers dropping Handshake sealer/opener and that Handshake-level loss state is abandoned. |
| `4.6-4.9-36` | informative | 0-RTT and 1-RTT packets share one packet number space, and clients do not send 0-RTT packets after sending a 1-RTT packet. <br>_(client,endpoint [CLIENT])_ | 0-RTT and 1-RTT packets share the same packet number space, and clients do not send 0-RTT packets after sending a 1-RTT packet | quic/packet.go + conn_recv.go — PN-space accounting must treat 0-RTT and 1-RTT as one application space; trivially satisfied with no 0-RTT, but keep the space model correct. |
| `4.6-4.9-37` | SHOULD | A client should discard 0-RTT keys as soon as it installs 1-RTT keys, since they have no further use. <br>_(client [CLIENT])_ | a client SHOULD discard 0-RTT keys as soon as it installs 1-RTT keys as they have no use after that moment | quic/conn_crypto.go — N/A: no 0-RTT keys are ever derived; if added, tie discard to 1-RTT key installation. |
| `4.6-4.9-38` | MAY | A server may discard 0-RTT keys as soon as it receives a 1-RTT packet. <br>_(server [not-client])_ | a server MAY discard 0-RTT keys as soon as it receives a 1-RTT packet | Server-only key management. |
| `4.6-4.9-39` | MAY | Servers may temporarily retain 0-RTT keys to decrypt reordered 0-RTT packets arriving after a 1-RTT packet. <br>_(server [not-client])_ | Servers MAY temporarily retain 0-RTT keys to allow decrypting reordered packets without requiring their contents to be retransmitted with 1-RTT keys | Server-only key management. |
| `4.6-4.9-40` | MUST | After receiving a 1-RTT packet, a server must discard 0-RTT keys within a short time. <br>_(server [not-client])_ | After receiving a 1-RTT packet, servers MUST discard 0-RTT keys within a short time | Server-only key management. |
| `4.6-4.9-41` | RECOMMENDED | The recommended retention period for server-held 0-RTT keys after the first 1-RTT packet is three times the Probe Timeout (PTO). <br>_(server [not-client])_ | the RECOMMENDED time period is three times the Probe Timeout | Server-only timing constant. |
| `4.6-4.9-42` | MAY | A server may discard 0-RTT keys earlier once it determines it has received all 0-RTT packets, e.g., by tracking missing packet numbers. <br>_(server [not-client])_ | A server MAY discard 0-RTT keys earlier if it determines that it has received all 0-RTT packets | Server-only key management. |

### 9001 section 5.1-5.4 - Packet protection: key derivation, Initial secrets, AEAD usage and nonce, header protection

| ID | Level | Requirement (scope) | RFC 9001 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `5.1-5.4-1` | informative | Version Negotiation packets carry no cryptographic protection at all (no AEAD, no header protection). <br>_(all [CLIENT])_ | Version Negotiation packets have no cryptographic protection. | quic/conn_recv.go + quic/packet.go — Version Negotiation must be parsed without attempting HP removal or AEAD open. |
| `5.1-5.4-2` | informative | Retry packets are protected with AEAD_AES_128_GCM (fixed key) solely as an integrity mechanism limiting who can produce a valid Retry. <br>_(all [CLIENT])_ | Retry packets use AEAD_AES_128_GCM to provide protection against accidental modification and to limit the entities that can produce a valid Retry | quic/retry.go — Retry integrity tag must be verified with AEAD_AES_128_GCM per §5.8 before accepting a Retry. |
| `5.1-5.4-3` | informative | Initial packets are protected with AEAD_AES_128_GCM using keys derived from the Destination Connection ID of the client's first Initial packet. <br>_(all [CLIENT])_ | Initial packets use AEAD_AES_128_GCM with keys derived from the Destination Connection ID field of the first Initial packet sent by the client | quic/initial.go — Initial sealer/opener must be AES-128-GCM keyed from the client's first DCID. |
| `5.1-5.4-4` | informative | All packets other than Version Negotiation, Retry, and Initial use keys and AEAD algorithms negotiated by TLS. <br>_(all [CLIENT])_ | All other packets have strong cryptographic protections for confidentiality and integrity, using keys and algorithms negotiated by TLS. | quic/conn_crypto.go — Handshake/1-RTT sealers/openers must be instantiated from the TLS-negotiated cipher suite. |
| `5.1-5.4-5` | informative | The same packet protection process used for Handshake/0-RTT/1-RTT packets is also applied to Initial packets. <br>_(all [CLIENT])_ | The same packet protection process is applied to Initial packets. | quic/conn_seal.go — Initial level must go through the identical AEAD + header-protection path as other levels. |
| `5.1-5.4-6` | informative | Each encryption level has separate traffic secrets for each direction; all levels except Initial take these secrets from TLS. <br>_(endpoint [CLIENT])_ | Each encryption level has separate secret values for protection of packets sent in each direction. | quic/conn_crypto.go — maintain distinct client-write and server-write secrets per encryption level, sourced from TLS except Initial. |
| `5.1-5.4-7` | informative | Packet protection keys are computed with TLS 1.3 HKDF-Expand-Label using the negotiated cipher suite's hash, and every QUIC use of HKDF-Expand-Label has a zero-length Context. <br>_(endpoint [CLIENT])_ | All uses of HKDF-Expand-Label in QUIC use a zero-length Context. | quic/crypto.go — verify hkdfExpandLabel passes empty context and uses the suite hash for non-Initial levels. |
| `5.1-5.4-8` | informative | HKDF labels are encoded as raw ASCII bytes, without quotes and without a trailing NUL byte. <br>_(endpoint [CLIENT])_ | labels, which are described using strings, are encoded as bytes using ASCII [ASCII] without quotes or any trailing NUL byte. | quic/crypto.go — label constants must be exact ASCII with no NUL terminator (inside the tls13 "tls13 " prefix framing of HKDF-Expand-Label). |
| `5.1-5.4-9` | MUST | TLS versions other than 1.3 must provide an equivalent key-derivation function to be usable with QUIC. <br>_(all [not-client])_ | Other versions of TLS MUST provide a similar function in order to be used with QUIC. | quic/handshake.go — client pins TLS 1.3 only, so this is satisfied vacuously; confirm MinVersion/MaxVersion force 1.3. |
| `5.1-5.4-10` | informative | The AEAD key is derived from the current encryption-level secret with the label "quic key". <br>_(endpoint [CLIENT])_ | The current encryption level secret and the label "quic key" are input to the KDF to produce the AEAD key | quic/crypto.go — check exact label string "quic key" in key derivation. |
| `5.1-5.4-11` | informative | The packet-protection IV is derived from the encryption-level secret with the label "quic iv". <br>_(endpoint [CLIENT])_ | the label "quic iv" is used to derive the Initialization Vector (IV) | quic/crypto.go — check exact label string "quic iv" for IV derivation. |
| `5.1-5.4-12` | informative | The header protection key is derived from the encryption-level secret with the label "quic hp". <br>_(endpoint [CLIENT])_ | The header protection key uses the "quic hp" label | quic/crypto.go — check exact label string "quic hp" for HP key derivation. |
| `5.1-5.4-13` | informative | The HKDF-Expand-Label output Length for "quic key" and "quic hp" equals the key size of the AEAD or header-protection algorithm respectively. <br>_(endpoint [CLIENT])_ | the Length provided to HKDF-Expand-Label along with these labels is determined by the size of keys in the AEAD or header protection algorithm | quic/crypto.go — key lengths: 16 for AES-128 suites, 32 for AES-256-GCM and ChaCha20-Poly1305 (both AEAD and HP keys). |
| `5.1-5.4-14` | informative | The Length for "quic iv" is the minimum AEAD nonce length, or 8 bytes if that is larger (12 for all defined suites). <br>_(endpoint [CLIENT])_ | The Length provided with "quic iv" is the minimum length of the AEAD nonce or 8 bytes if that is larger | quic/crypto.go — IV derivation length must be 12 bytes for all four supported AEADs. |
| `5.1-5.4-15` | informative | The KDF for Initial secrets is always the TLS 1.3 HKDF-Expand-Label function, regardless of negotiated TLS version. <br>_(endpoint [CLIENT])_ | The KDF used for initial secrets is always the HKDF-Expand-Label function from TLS 1.3 | quic/initial.go — Initial derivation hardcodes TLS 1.3 HKDF-Expand-Label with SHA-256. |
| `5.1-5.4-16` | informative | The Initial secret PRK is HKDF-Extract with salt 0x38762cf7f55934b34d179ae6a4c80cadccbb7f0a and IKM equal to the Destination Connection ID. <br>_(endpoint [CLIENT])_ | HKDF-Extract (see Section 2.2 of [HKDF]) with a salt of 0x38762cf7f55934b34d179ae6a4c80cadccbb7f0a and the input keying material (IKM) of the Destination Connection ID field | quic/initial.go — verify the v1 salt constant byte-for-byte and HKDF-Extract(salt, DCID) ordering (salt is the HKDF salt argument, DCID the IKM). |
| `5.1-5.4-17` | informative | The client Initial secret is HKDF-Expand-Label(initial_secret, "client in", "", Hash.length), producing a 32-byte secret. <br>_(client [CLIENT])_ | uses the PRK and the label "client in" as input to the HKDF-Expand-Label function from TLS [TLS13] to produce a 32-byte secret | quic/initial.go — label "client in", output length 32 (SHA-256 Hash.length) for the client's send secret. |
| `5.1-5.4-18` | informative | The server Initial secret uses the same process with the label "server in"; the client derives it to open server Initial packets. <br>_(endpoint [CLIENT])_ | Packets constructed by the server use the same process with the label "server in". | quic/initial.go — label "server in" derives the opener secret used by the client to decrypt server Initials. |
| `5.1-5.4-19` | informative | SHA-256 is the hash function for HKDF when deriving Initial secrets and keys. <br>_(endpoint [CLIENT])_ | The hash function for HKDF when deriving initial secrets and keys is SHA-256 | quic/initial.go — Initial derivation must use SHA-256 irrespective of the eventually negotiated suite hash. |
| `5.1-5.4-20` | informative | The connection ID input to Initial derivation is the DCID in the client's Initial packet — randomly selected by the client, unless a Retry made the server select it. <br>_(client [CLIENT])_ | The connection ID used with HKDF-Expand-Label is the Destination Connection ID in the Initial packet sent by the client. | quic/initial.go + quic/retry.go — the IKM is the DCID the client puts in its Initial (server-chosen after Retry). |
| `5.1-5.4-21` | SHOULD | Future QUIC versions should define a new Initial salt so keys differ per version. <br>_(all [not-client])_ | Future versions of QUIC SHOULD generate a new salt value, thus ensuring that the keys are different for each version of QUIC. | quic/initial.go — v1-only client; salt is fixed. No action unless additional versions are added. |
| `5.1-5.4-22` | MUST | The TLS 1.3 HKDF-Expand-Label function must be used for Initial packets even when TLS 1.3 is not among the offered TLS versions. <br>_(endpoint [CLIENT])_ | The HKDF-Expand-Label function defined in TLS 1.3 MUST be used for Initial packets even where the TLS versions offered do not include TLS 1.3. | quic/initial.go — Initial key schedule must not depend on handshake.go's TLS version negotiation state. |
| `5.1-5.4-23` | informative | After a server Retry, subsequent Initial packets use secrets re-derived from the server-selected connection ID. <br>_(client [CLIENT])_ | The secrets used for constructing subsequent Initial packets change when a server sends a Retry packet to use the connection ID value selected by the server. | quic/retry.go — on accepting Retry, re-derive Initial secrets from the Retry's Source Connection ID before resending the Initial. |
| `5.1-5.4-24` | informative | Initial secrets do NOT change when the client switches its Destination Connection ID in response to the server's first Initial packet. <br>_(client [CLIENT])_ | The secrets do not change when a client changes the Destination Connection ID it uses in response to an Initial packet from the server. | quic/initial.go + quic/conn_crypto.go — adopting the server's SCID as new DCID must NOT trigger Initial key re-derivation (only Retry does). |
| `5.1-5.4-25` | informative | The Destination Connection ID used as IKM can be any length up to 20 bytes, including zero length after a Retry with a zero-length SCID. <br>_(endpoint [CLIENT])_ | The Destination Connection ID field could be any length up to 20 bytes, including zero length if the server sends a Retry packet with a zero-length Source Connection ID field. | quic/initial.go — derivation must handle 0..20-byte DCID IKM without assuming a minimum length. |
| `5.1-5.4-26` | informative | The AEAD for packet protection is exactly the AEAD negotiated by the TLS connection (e.g. TLS_AES_128_GCM_SHA256 implies AEAD_AES_128_GCM). <br>_(endpoint [CLIENT])_ | used for QUIC packet protection is the AEAD that is negotiated for use with the TLS connection | quic/conn_seal.go — cipher-suite-to-AEAD mapping for Handshake/1-RTT sealers must follow the TLS-negotiated suite. |
| `5.1-5.4-27` | informative | QUIC can use any TLS 1.3 cipher suite except TLS_AES_128_CCM_8_SHA256, for which no header protection scheme is defined. <br>_(endpoint [CLIENT])_ | QUIC can use any of the cipher suites defined in [TLS13] with the exception of TLS_AES_128_CCM_8_SHA256. | quic/handshake.go — client must not offer or accept TLS_AES_128_CCM_8_SHA256 in its cipher suite list. |
| `5.1-5.4-28` | MUST NOT | A cipher suite must not be negotiated unless a header protection scheme is defined for it. <br>_(endpoint [CLIENT])_ | A cipher suite MUST NOT be negotiated unless a header protection scheme is defined for the cipher suite. | quic/handshake.go — offered suites restricted to the four with defined HP (AES-128-GCM, AES-128-CCM, AES-256-GCM, ChaCha20-Poly1305). |
| `5.1-5.4-29` | informative | All usable TLS 1.3 cipher suites have a 16-byte authentication tag, expanding output by 16 bytes over input. <br>_(endpoint [CLIENT])_ | These cipher suites have a 16-byte authentication tag and produce an output 16 bytes larger than their input. | quic/crypto_protect.go — AEAD overhead constant must be 16 for payload-length budgeting and minimum-packet-size checks. |
| `5.1-5.4-30` | MUST NOT | An endpoint must not reject a ClientHello merely because it offers a cipher suite the endpoint does not support (including TLS_AES_128_CCM_8_SHA256). <br>_(endpoint,server [not-client])_ | An endpoint MUST NOT reject a ClientHello that offers a cipher suite that it does not support | quic/handshake.go — server-side ClientHello processing; N/A for client-only implementation. |
| `5.1-5.4-31` | informative | When constructing packets, the AEAD is applied before header protection, with the unprotected packet header as associated data. <br>_(sender [CLIENT])_ | When constructing packets, the AEAD function is applied prior to applying header protection | quic/crypto_protect.go — seal path order: AEAD-encrypt payload first, then mask header bits/PN. |
| `5.1-5.4-32` | informative | When processing received packets, an endpoint removes header protection first, then AEAD-decrypts. <br>_(receiver [CLIENT])_ | When processing packets, an endpoint first removes the header protection. | quic/conn_recv.go — decrypt path order: unmask header/PN before AEAD open. |
| `5.1-5.4-33` | informative | The nonce input starts from the 62-bit reconstructed packet number in network byte order, left-padded with zeros to the IV size. <br>_(endpoint [CLIENT])_ | The 62 bits of the reconstructed QUIC packet number in network byte order are left-padded with zeros to the size of the IV. | quic/crypto_protect.go — nonce uses the full decoded (not truncated wire) packet number, big-endian, zero-padded to 12 bytes. |
| `5.1-5.4-34` | informative | The AEAD nonce is the exclusive OR of the zero-padded packet number and the IV. <br>_(endpoint [CLIENT])_ | The exclusive OR of the padded packet number and the IV forms the AEAD nonce. | quic/crypto_protect.go — verify nonce = IV XOR padded-PN, not concatenation. |
| `5.1-5.4-35` | informative | The AEAD associated data is the QUIC header from the first byte of the short or long header through the unprotected packet number inclusive. <br>_(endpoint [CLIENT])_ | the contents of the QUIC header, starting from the first byte of either the short or long header, up to and including the unprotected packet number | quic/crypto_protect.go — AAD must be the header with HP removed (unprotected first byte and PN), ending exactly after the PN field. |
| `5.1-5.4-36` | informative | The AEAD plaintext is the QUIC packet payload; the ciphertext output is transmitted in its place. <br>_(sender [CLIENT])_ | The output ciphertext, C, of the AEAD is transmitted in place of P. | quic/crypto_protect.go + quic/packet.go — payload slot in the wire packet carries C (payload + 16-byte tag). |
| `5.1-5.4-37` | MUST | An endpoint must initiate a key update before exceeding any confidentiality limit set for the AEAD in use (which may be lower than the packet number limit). <br>_(endpoint [CLIENT])_ | An endpoint MUST initiate a key update (Section 6) prior to exceeding any limit set for the AEAD that is in use. | quic/crypto_keyupdate.go + quic/conn_seal.go — track packets encrypted per key and trigger key update before the per-suite confidentiality limit (§6.6). |
| `5.1-5.4-38` | informative | For long headers, the four least significant bits of the first byte are header-protected (mask[0] & 0x0f). <br>_(endpoint [CLIENT])_ | The four least significant bits of the first byte are protected for packets with long headers | quic/crypto_protect.go — long-header first-byte mask constant must be 0x0f. |
| `5.1-5.4-39` | informative | For short headers, the five least significant bits of the first byte are header-protected (mask[0] & 0x1f). <br>_(endpoint [CLIENT])_ | the five least significant bits of the first byte are protected for packets with short headers | quic/crypto_protect.go — short-header first-byte mask constant must be 0x1f. |
| `5.1-5.4-40` | informative | Header protection covers the reserved bits and Packet Number Length field in both forms, plus the Key Phase bit in short headers. <br>_(endpoint [CLIENT])_ | this covers the reserved bits and the Packet Number Length field; the Key Phase bit is also protected for packets with a short header | quic/packet.go + quic/crypto_protect.go — Key Phase must only be read after HP removal; reserved-bit checks likewise post-unmask. |
| `5.1-5.4-41` | informative | The header protection key stays the same for the whole connection and does not change on key update, which lets it protect the Key Phase bit. <br>_(endpoint [CLIENT])_ | The same header protection key is used for the duration of the connection, with the value not changing after a key update | quic/crypto_keyupdate.go — key update must rotate AEAD key and IV only, never the "quic hp" key. |
| `5.1-5.4-42` | informative | Header protection is not applied to Retry or Version Negotiation packets. <br>_(endpoint [CLIENT])_ | This process does not apply to Retry or Version Negotiation packets | quic/conn_recv.go + quic/retry.go — skip HP removal entirely for Retry and Version Negotiation. |
| `5.1-5.4-43` | informative | Header protection samples the packet ciphertext as input to an encryption algorithm chosen by the negotiated AEAD. <br>_(endpoint [CLIENT])_ | The ciphertext of the packet is sampled and used as input to an encryption algorithm. | quic/crypto_protect.go — HP function selection keyed on negotiated AEAD (AES vs ChaCha20). |
| `5.1-5.4-44` | informative | The HP algorithm output is a 5-byte mask XORed onto the header: first-byte LSBs by mask byte 0, packet number by the remaining mask bytes. <br>_(endpoint [CLIENT])_ | The output of this algorithm is a 5-byte mask that is applied to the protected header fields using exclusive OR. | quic/crypto_protect.go — mask[0] on first byte, mask[1..1+pn_length] on the PN bytes. |
| `5.1-5.4-45` | informative | Mask bytes beyond the actual packet number length (for shorter PN encodings) are simply unused. <br>_(endpoint [CLIENT])_ | Any unused bytes of mask that might result from a shorter packet number encoding are unused. | quic/crypto_protect.go — apply only pn_length mask bytes; never XOR trailing mask bytes into payload. |
| `5.1-5.4-46` | informative | The Packet Number Length is encoded in the two least significant bits of the first byte, plus one (1-4 bytes). <br>_(endpoint [CLIENT])_ | pn_length = (packet[0] & 0x03) + 1 | quic/packet.go — PN length decode must be (firstByte & 0x03) + 1, read only after first-byte unmasking on receive. |
| `5.1-5.4-47` | informative | Removing header protection uses the same algorithm as applying it, differing only in when pn_length is determined (after unmasking the first byte). <br>_(receiver [CLIENT])_ | Removing header protection only differs in the order in which the packet number length (pn_length) is determined | quic/conn_recv.go + quic/crypto_protect.go — on receive: unmask first byte, then read pn_length, then unmask PN bytes. |
| `5.1-5.4-48` | MUST | A header protection algorithm must be specified for an AEAD before its cipher suite can be used with QUIC; this document defines them only for AES-128-GCM, AES-128-CCM, AES-256-GCM, and ChaCha20-Poly1305. <br>_(all [CLIENT])_ | Before a TLS cipher suite can be used with QUIC, a header protection algorithm MUST be specified for the AEAD used with that cipher suite. | quic/handshake.go — enabled suite set must be exactly the four with defined HP algorithms. |
| `5.1-5.4-49` | informative | Before TLS selects a cipher suite (i.e. at the Initial level), AES header protection matching AEAD_AES_128_GCM is used. <br>_(endpoint [CLIENT])_ | Prior to TLS selecting a cipher suite, AES header protection is used (Section 5.4.3), matching the AEAD_AES_128_GCM packet protection. | quic/initial.go — Initial level HP must be AES-128-ECB regardless of what will be negotiated. |
| `5.1-5.4-50` | informative | The ciphertext sample starts 4 bytes after the start of the Packet Number field — the PN is assumed to be its maximum 4-byte encoding for sampling. <br>_(endpoint [CLIENT])_ | The sample of ciphertext is taken starting from an offset of 4 bytes after the start of the Packet Number field. | quic/crypto_protect.go — sample_offset = pn_offset + 4 on both seal and open paths, independent of actual pn_length. |
| `5.1-5.4-51` | MUST | An endpoint must discard received packets that are too short to contain a complete header-protection sample. <br>_(endpoint,receiver [CLIENT])_ | An endpoint MUST discard packets that are not long enough to contain a complete sample. | quic/conn_recv.go — length check (pn_offset + 4 + 16 bytes available) before HP removal; drop, don't error the connection. |
| `5.1-5.4-52` | informative | Senders pad packets so that encoded packet number plus protected payload is at least 4 bytes longer than the required sample. <br>_(sender [CLIENT])_ | packets are padded so that the combined lengths of the encoded packet number and protected payload is at least 4 bytes longer than the sample required for header protection | quic/packet.go (packet assembly) — enforce minimum: pn_length + payload+tag >= 4 + 16; pad with PADDING frames if short. |
| `5.1-5.4-53` | informative | With 16-byte expansion and 16-byte samples, a packet needs at least 3 bytes of frames with a 1-byte PN encoding, or 2 bytes with a 2-byte PN. <br>_(sender [CLIENT])_ | This results in needing at least 3 bytes of frames in the unprotected payload if the packet number is encoded on a single byte | quic/packet.go — minimum-frame-bytes padding rule as a function of chosen PN encoding length. |
| `5.1-5.4-54` | informative | For short header packets, the packet number offset is 1 + len(connection_id). <br>_(endpoint [CLIENT])_ | pn_offset = 1 + len(connection_id) | quic/packet.go — 1-RTT pn_offset computation; client must use the length of its own chosen (destination) CID on receive. |
| `5.1-5.4-55` | informative | For long header packets, pn_offset = 7 + len(DCID) + len(SCID) + len(Length field); Initial packets additionally add len(Token Length) + len(Token). <br>_(endpoint [CLIENT])_ | pn_offset = 7 + len(destination_connection_id) + len(source_connection_id) + len(payload_length) | quic/packet.go — long-header pn_offset accounts for varint Length field size and, for Initial, the token length varint plus token bytes. |
| `5.1-5.4-56` | informative | When multiple QUIC packets are coalesced in one UDP datagram, each packet's protection is handled separately. <br>_(receiver [CLIENT])_ | Multiple QUIC packets might be included in the same UDP datagram. Each packet is handled separately. | quic/conn_recv.go — coalesced-datagram loop must sample/unprotect each packet independently with its own level's keys. |
| `5.1-5.4-57` | informative | AES-based header protection uses 128-bit AES-ECB for AEAD_AES_128_GCM and AEAD_AES_128_CCM, and 256-bit AES-ECB for AEAD_AES_256_GCM. <br>_(endpoint [CLIENT])_ | AEAD_AES_128_GCM and AEAD_AES_128_CCM use 128-bit AES in Electronic Codebook (ECB) mode. AEAD_AES_256_GCM uses 256-bit AES in ECB mode. | quic/crypto_protect.go — HP cipher key size follows the suite (AES-128 vs AES-256 ECB, i.e. single-block AES encrypt). |
| `5.1-5.4-58` | informative | The AES header-protection mask is AES-ECB(hp_key, sample) over a 16-byte ciphertext sample (first 5 bytes of the block used as mask). <br>_(endpoint [CLIENT])_ | mask = AES-ECB(hp_key, sample) | quic/crypto_protect.go — single AES block encrypt of the 16-byte sample; take mask[0..4]. |
| `5.1-5.4-59` | informative | ChaCha20-based header protection uses the raw ChaCha20 function with a 256-bit key and a 16-byte sample of the packet protection output. <br>_(endpoint [CLIENT])_ | header protection uses the raw ChaCha20 function as defined in Section 2.4 of [CHACHA]. This uses a 256-bit key and 16 bytes sampled | quic/crypto_protect.go — ChaCha20 HP path: raw ChaCha20 (not Poly1305 AEAD), 32-byte hp key, 16-byte sample. |
| `5.1-5.4-60` | informative | For ChaCha20 HP, the first 4 sampled bytes are the block counter, interpreted as a little-endian 32-bit integer if the implementation takes an integer. <br>_(endpoint [CLIENT])_ | The first 4 bytes of the sampled ciphertext are the block counter. | quic/crypto_protect.go — counter = LE-uint32(sample[0:4]); easy to get endianness wrong. |
| `5.1-5.4-61` | informative | For ChaCha20 HP, the remaining 12 sampled bytes are the nonce, interpreted as three little-endian 32-bit integers if taken as integers. <br>_(endpoint [CLIENT])_ | The remaining 12 bytes are used as the nonce. | quic/crypto_protect.go — nonce = sample[4:16] passed byte-wise (or as three LE uint32s) to ChaCha20. |
| `5.1-5.4-62` | informative | The ChaCha20 header-protection mask is produced by encrypting 5 zero bytes: mask = ChaCha20(hp_key, counter, nonce, {0,0,0,0,0}). <br>_(endpoint [CLIENT])_ | The encryption mask is produced by invoking ChaCha20 to protect 5 zero bytes. | quic/crypto_protect.go — mask is the ChaCha20 keystream over 5 zero bytes with the sampled counter/nonce. |

### 9001 section 5.5-5.8 - Receiving Protected Packets; Use of 0-RTT Keys; Receiving Out-of-Order Protected Packets; Retry Packet Integrity

| ID | Level | Requirement (scope) | RFC 9001 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `5.5-5.8-1` | MUST | After successfully receiving a packet with a given packet number, an endpoint must discard packets in the same packet number space with higher packet numbers that cannot be unprotected with the same key or (after a key update) a subsequent packet protection key. <br>_(endpoint [CLIENT])_ | it MUST discard all packets in the same packet number space with higher packet numbers if they cannot be successfully unprotected with either the same key | quic/conn_recv.go — AEAD-open failure must drop the packet silently (no conn error, no PN-state update); with key phase flip, trial-decrypt via crypto_keyupdate.go next-gen keys, then drop. |
| `5.5-5.8-2` | MUST | A packet that appears to trigger a key update (flipped Key Phase) but fails to unprotect must be discarded without committing the key update. <br>_(endpoint [CLIENT])_ | a packet that appears to trigger a key update but cannot be unprotected successfully MUST be discarded | quic/crypto_keyupdate.go — short-header packet with flipped Key Phase bit failing AEAD must be dropped and must NOT advance the key phase or install next-gen keys. |
| `5.5-5.8-3` | informative | Failure to unprotect a packet is not necessarily a protocol error or attack (truncated packet number encoding can decode incorrectly for significantly delayed packets), so it must not be escalated. <br>_(endpoint [CLIENT])_ | Failure to unprotect a packet does not necessarily indicate the existence of a protocol error in a peer or an attack | quic/conn_recv.go — confirm unprotect failure is not treated as a connection error/close; also packet.go PN decoding tolerance for delayed packets. |
| `5.5-5.8-4` | MUST NOT | A client must not use 0-RTT for application data unless the application in use specifically requests it. <br>_(client [CLIENT])_ | A client therefore MUST NOT use 0-RTT for application data unless specifically requested by the application that is in use | quic/handshake.go — 0-RTT is a project non-goal; trivially satisfied. Confirm no early_data extension is offered in ClientHello. |
| `5.5-5.8-5` | MUST | An application protocol using QUIC must include a profile defining acceptable 0-RTT use; otherwise 0-RTT may only carry QUIC frames that do not carry application data. <br>_(all [CLIENT])_ | An application protocol that uses QUIC MUST include a profile that defines acceptable use of 0-RTT | http3/ — the HTTP/3 0-RTT profile (RFC 9114 §10.9) would govern; moot while 0-RTT is unimplemented. |
| `5.5-5.8-6` | MAY | A client may apply additional restrictions on what data it sends before TLS handshake completion. <br>_(client [CLIENT])_ | A client MAY wish to apply additional restrictions on what data it sends prior to the completion of the TLS handshake | N/A — optional and 0-RTT is unimplemented; client sends no application data pre-handshake. |
| `5.5-5.8-7` | informative | A client otherwise treats 0-RTT keys as equivalent to 1-RTT keys, except certain frames cannot be sent with 0-RTT keys (RFC 9000 §12.5). <br>_(client [CLIENT])_ | A client otherwise treats 0-RTT keys as equivalent to 1-RTT keys, except that it cannot send certain frames with 0-RTT keys | N/A — no 0-RTT keys are derived (quic/conn_crypto.go has no 0-RTT sealer/opener level). |
| `5.5-5.8-8` | informative | A client that gets an indication its 0-RTT data was accepted can send 0-RTT data until it receives all of the server's handshake messages. <br>_(client [CLIENT])_ | A client that receives an indication that its 0-RTT data has been accepted by a server can send 0-RTT data until it receives all of the server's handshake messages | N/A — no 0-RTT send path exists; would live in quic/handshake.go if 0-RTT were added. |
| `5.5-5.8-9` | SHOULD | A client should stop sending 0-RTT data upon an indication that its 0-RTT data has been rejected. <br>_(client [CLIENT])_ | A client SHOULD stop sending 0-RTT data if it receives an indication that 0-RTT data has been rejected | N/A — vacuously satisfied; no 0-RTT data is ever sent. |
| `5.5-5.8-10` | MUST NOT | A server must not protect packets with 0-RTT keys; it uses 1-RTT keys to protect acknowledgments of 0-RTT packets. <br>_(server [not-client])_ | A server MUST NOT use 0-RTT keys to protect packets; it uses 1-RTT keys to protect acknowledgments of 0-RTT packets | quic/conn_crypto.go — server-side rule; client consequence is that no 0-RTT opener is ever needed — confirm none is installed. |
| `5.5-5.8-11` | MUST NOT | A client must not attempt to decrypt 0-RTT packets it receives. <br>_(client [CLIENT])_ | A client MUST NOT attempt to decrypt 0-RTT packets it receives | quic/conn_recv.go + packet.go — a long-header packet with type 0x1 (0-RTT) must never reach a decrypt attempt on the client receive path. |
| `5.5-5.8-12` | MUST | A client must discard any 0-RTT packets it receives instead of decrypting them. <br>_(client [CLIENT])_ | and instead MUST discard them | quic/conn_recv.go — 0-RTT-type packets must be dropped outright, not buffered in the undecryptable-packet queue. |
| `5.5-5.8-13` | MUST NOT | Once a client has installed 1-RTT keys, it must not send any more 0-RTT packets. <br>_(client [CLIENT])_ | Once a client has installed 1-RTT keys, it MUST NOT send any more 0-RTT packets | N/A — vacuously satisfied (no 0-RTT send path); would gate in quic/conn_seal.go level selection if 0-RTT were added. |
| `5.5-5.8-14` | informative | Packets containing acknowledgments of 0-RTT data are 1-RTT protected and cannot have protection removed by the client until the TLS handshake completes and 1-RTT keys are derived. <br>_(client [CLIENT])_ | any packets containing acknowledgments of 0-RTT data cannot have packet protection removed by the client until the TLS handshake is complete | quic/conn_recv.go — moot without 0-RTT; 1-RTT opener availability timing is covered by the §5.7 rules below. |
| `5.5-5.8-15` | MUST NOT | Endpoints in either role must not decrypt 1-RTT packets from their peer before completing the TLS handshake. <br>_(endpoint [CLIENT])_ | Endpoints in either role MUST NOT decrypt 1-RTT packets from their peer prior to completing the handshake | quic/conn_crypto.go + conn_recv.go — ensure the 1-RTT opener is not usable (not installed, or gated) until TLS handshake completion. |
| `5.5-5.8-16` | MUST NOT | A server must not process incoming 1-RTT protected packets before the TLS handshake is complete; its pre-completion use of 1-RTT keys is limited to sending. <br>_(server [not-client])_ | A server MUST NOT process incoming 1-RTT protected packets before the TLS handshake is complete | Server-only; no server role in quic/. |
| `5.5-5.8-17` | informative | Because an acknowledgment indicates all frames in a packet were processed, a server cannot acknowledge 1-RTT packets until the TLS handshake is complete. <br>_(server [not-client])_ | a server cannot send acknowledgments for 1-RTT packets until the TLS handshake is complete | Server-only; explains why client 1-RTT sends may not be ACKed until handshake completion — relevant to loss-recovery expectations only. |
| `5.5-5.8-18` | MAY | Packets received under 1-RTT protection before the handshake completes may be stored and later decrypted and used once the handshake is complete. <br>_(endpoint [CLIENT])_ | Received packets protected with 1-RTT keys MAY be stored and later decrypted and used once the handshake is complete | quic/conn_recv.go — check whether pre-handshake short-header packets are buffered for later decryption or dropped; both conform (MAY), but buffering avoids relying on peer retransmission. |
| `5.5-5.8-19` | informative | Even where a TLS implementation provides 1-RTT read secrets before handshake completion, those keys are not to be used prior to completing the handshake. <br>_(endpoint [CLIENT])_ | Even where QUIC implementations have 1-RTT read keys, those keys are not to be used prior to completing the handshake | quic/handshake.go + conn_crypto.go — if the TLS layer surfaces application secrets early, defer 1-RTT opener installation/use until handshake completion. |
| `5.5-5.8-20` | informative | A client can avoid server-side head-of-line blocking on its Finished by coalescing 1-RTT packets with a Handshake packet carrying a copy of the Finished CRYPTO frame until a Handshake packet is acknowledged. <br>_(client [CLIENT])_ | sending its 1-RTT packets coalesced with a Handshake packet containing a copy of the CRYPTO frame that carries the Finished message, until one of the Handshake packets is acknowledged | quic/handshake.go send path — optional latency optimization; note whether the client coalesces Finished retransmits with early 1-RTT sends (absence is conformant). |
| `5.5-5.8-21` | MAY | A server may retain 0-RTT-protected packets received before a TLS ClientHello for later decryption. <br>_(server [not-client])_ | The server MAY retain these packets for later decryption in anticipation of receiving a ClientHello | Server-only; no server role in quic/. |
| `5.5-5.8-22` | MUST NOT | A client, even if it has 1-RTT secrets, must not process incoming 1-RTT protected packets before the TLS handshake is complete. <br>_(client [CLIENT])_ | Even if it has 1-RTT secrets, a client MUST NOT process incoming 1-RTT protected packets before the TLS handshake is complete | quic/conn_recv.go + handshake.go — client-specific form of -15: verify short-header packets are not decrypted/processed until crypto/tls reports handshake completion (check install ordering in conn_crypto.go). |
| `5.5-5.8-23` | informative | The Retry Integrity Tag exists so corrupted Retry packets can be discarded and so only an entity that observed the Initial packet can produce a valid Retry. <br>_(endpoint [CLIENT])_ | only an entity that observes an Initial packet can send a valid Retry packet | quic/retry.go — the receive path must verify the tag and drop Retry packets whose tag fails verification (discard MUST itself lives in RFC 9000 §17.2.5). |
| `5.5-5.8-24` | informative | The Retry Integrity Tag is a 128-bit field computed as the output of AEAD_AES_128_GCM over the specified key, nonce, empty plaintext, and pseudo-packet associated data. <br>_(endpoint [CLIENT])_ | The Retry Integrity Tag is a 128-bit field that is computed as the output of AEAD_AES_128_GCM [AEAD] used with the following inputs | quic/retry.go — tag must be AES-128-GCM (crypto/aes + cipher.GCM) producing a 16-byte tag; client verifies via constant-time compare against the received tag. |
| `5.5-5.8-25` | informative | The Retry integrity secret key K is the fixed 128-bit value 0xbe0c690b9f66575a1d766b54e368c84e. <br>_(endpoint [CLIENT])_ | The secret key, K, is 128 bits equal to 0xbe0c690b9f66575a1d766b54e368c84e | quic/retry.go — verify the hardcoded key constant matches be0c690b9f66575a1d766b54e368c84e byte-for-byte (this is the QUIC v1 value). |
| `5.5-5.8-26` | informative | The Retry integrity nonce N is the fixed 96-bit value 0x461599d35d632bf2239825bb. <br>_(endpoint [CLIENT])_ | The nonce, N, is 96 bits equal to 0x461599d35d632bf2239825bb | quic/retry.go — verify the hardcoded 12-byte nonce constant matches 461599d35d632bf2239825bb. |
| `5.5-5.8-27` | informative | The plaintext P for the Retry integrity AEAD computation is empty. <br>_(endpoint [CLIENT])_ | The plaintext, P, is empty | quic/retry.go — GCM Seal must be called with nil/empty plaintext; the entire output is the 16-byte tag. |
| `5.5-5.8-28` | informative | The associated data A for the Retry integrity AEAD computation is the contents of the Retry Pseudo-Packet. <br>_(endpoint [CLIENT])_ | The associated data, A, is the contents of the Retry Pseudo-Packet | quic/retry.go — AAD passed to GCM must be exactly the constructed pseudo-packet bytes (facts -30/-31). |
| `5.5-5.8-29` | informative | K and N are derived by HKDF-Expand-Label from the secret 0xd9c9943e6101fd200021506bcc02814c73030f25c79d71ce876eca876e6fca8e with labels "quic key" and "quic iv". <br>_(endpoint [CLIENT])_ | derived by calling HKDF-Expand-Label using 0xd9c9943e6101fd200021506bcc02814c73030f25c79d71ce876eca876e6fca8e as the secret, with labels being "quic key" and "quic iv" | quic/retry.go — hardcoding the derived K/N constants is equivalent and conformant; if derived at runtime, must reuse crypto.go's HKDF-Expand-Label with labels "quic key"/"quic iv". |
| `5.5-5.8-30` | informative | The Retry Pseudo-Packet layout is: ODCID Length (8 bits), Original Destination Connection ID (0..160 bits), then the Retry packet header with Header Form=1, Fixed Bit=1, Long Packet Type=3, Unused (4 bits), Version (32), DCID Len/DCID, SCID Len/SCID, Retry Token. <br>_(endpoint [CLIENT])_ | ODCID Length (8), Original Destination Connection ID (0..160), Header Form (1) = 1, Fixed Bit (1) = 1, Long Packet Type (2) = 3, Unused (4), Version (32) | quic/retry.go — pseudo-packet must be ODCID length prefix + ODCID + the received Retry packet bytes (long header type 3, per packet.go) minus the trailing tag. |
| `5.5-5.8-31` | informative | The Retry Pseudo-Packet is not sent over the wire; it is built by taking the transmitted Retry packet, removing the Retry Integrity Tag, and prepending the ODCID Length and Original Destination Connection ID fields. <br>_(endpoint [CLIENT])_ | It is computed by taking the transmitted Retry packet, removing the Retry Integrity Tag, and prepending the two following fields | quic/retry.go — verify construction strips exactly the last 16 bytes (tag) of the received Retry and prepends 1-byte length + ODCID before AAD use. |
| `5.5-5.8-32` | informative | The ODCID Length field carries the byte length of the following Original Destination Connection ID, encoded as an 8-bit unsigned integer. <br>_(endpoint [CLIENT])_ | contains the length in bytes of the Original Destination Connection ID field that follows it, encoded as an 8-bit unsigned integer | quic/retry.go — ODCID length must be written as a single uint8, not a QUIC varint (internal/bytesx varint would be wrong here). |
| `5.5-5.8-33` | informative | The Original Destination Connection ID in the pseudo-packet is the Destination Connection ID from the client's Initial packet that the Retry responds to. <br>_(endpoint [CLIENT])_ | contains the value of the Destination Connection ID from the Initial packet that this Retry is in response to | quic/retry.go — client must retain the DCID it chose for its first Initial and use it as ODCID when verifying; same value later cross-checked via original_destination_connection_id in transport_params.go. |

### 9001 section 6 - Key Update (RFC 9001, Using TLS to Secure QUIC)

| ID | Level | Requirement (scope) | RFC 9001 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `6-1` | MAY | After the handshake is confirmed, an endpoint is permitted to initiate a key update. <br>_(endpoint [CLIENT])_ | Once the handshake is confirmed (see Section 4.1.2), an endpoint MAY initiate a key update. | quic/crypto_keyupdate.go + handshake.go: verify update initiation is gated on client handshake-confirmed (HANDSHAKE_DONE received). |
| `6-2` | informative | The Key Phase bit starts at 0 for the first 1-RTT packets and is toggled on each subsequent key update. <br>_(endpoint [CLIENT])_ | The Key Phase bit is initially set to 0 for the first set of 1-RTT packets and toggled to signal each subsequent key update. | quic/packet.go + crypto_keyupdate.go: Key Phase bit in short header starts 0 and toggles per update. |
| `6-3` | informative | An endpoint noticing a changed Key Phase bit updates its keys and decrypts that packet with the new keys. <br>_(receiver [CLIENT])_ | An endpoint that notices a changed Key Phase bit updates keys and decrypts the packet that contains the changed value. | quic/conn_recv.go: decrypt path must detect Key Phase mismatch and attempt next-generation keys. |
| `6-4` | informative | A key update initiated by one endpoint causes both endpoints to update keys (unlike TLS independent updates). <br>_(all [CLIENT])_ | Initiating a key update results in both endpoints updating keys. | quic/crypto_keyupdate.go: read and write generations advance together, not independently. |
| `6-5` | MUST NOT | An endpoint is forbidden from sending a TLS KeyUpdate handshake message. <br>_(endpoint,sender [CLIENT])_ | Endpoints MUST NOT send a TLS KeyUpdate message. | quic/handshake.go: confirm the TLS layer never emits post-handshake KeyUpdate onto CRYPTO streams. |
| `6-6` | MUST | Receipt of a TLS KeyUpdate message must be treated as connection error 0x010a (unexpected_message alert). <br>_(endpoint,receiver [CLIENT])_ | Endpoints MUST treat the receipt of a TLS KeyUpdate message as a connection error of type 0x010a | quic/handshake.go + errors.go: post-handshake CRYPTO handling must reject KeyUpdate with CRYPTO_ERROR 0x0100+10 (0x010a). |
| `6-7` | informative | The new write secret is derived from the existing write secret using the TLS KDF with label "quic ku". <br>_(endpoint [CLIENT])_ | This uses the KDF function provided by TLS with a label of "quic ku". | quic/crypto_keyupdate.go: verify exact HKDF-Expand-Label label string "quic ku". |
| `6-8` | informative | Update formula: secret_<n+1> = HKDF-Expand-Label(secret_<n>, "quic ku", "", Hash.length). <br>_(endpoint [CLIENT])_ | secret_<n+1> = HKDF-Expand-Label(secret_<n>, "quic ku", "", Hash.length) | quic/crypto_keyupdate.go + crypto.go: empty context, output length = hash length of negotiated suite. |
| `6-9` | informative | AEAD key and IV are re-derived from the updated secret per Section 5.1; the header protection key is never updated. <br>_(endpoint [CLIENT])_ | The corresponding key and IV are created from that secret as defined in Section 5.1. The header protection key is not updated. | quic/crypto_keyupdate.go: new "quic key"/"quic iv" from updated secret; "quic hp" key MUST stay the original one. |
| `6-10` | informative | After deriving updated keys the endpoint toggles the Key Phase bit and protects all subsequent packets with the new key and IV. <br>_(sender [CLIENT])_ | The endpoint toggles the value of the Key Phase bit and uses the updated key and IV to protect all subsequent packets. | quic/crypto_keyupdate.go + conn_seal.go: seal path switches to new key/IV and flipped phase bit atomically. |
| `6-11` | MUST NOT | An endpoint is forbidden from initiating a key update before the handshake is confirmed. <br>_(endpoint [CLIENT])_ | An endpoint MUST NOT initiate a key update prior to having confirmed the handshake (Section 4.1.2). | quic/crypto_keyupdate.go: initiation refused until client-side handshake-confirmed flag set. |
| `6-12` | MUST NOT | A subsequent key update is forbidden until an acknowledgment is received for a packet protected with current-phase keys. <br>_(endpoint [CLIENT])_ | An endpoint MUST NOT initiate a subsequent key update unless it has received an acknowledgment for a packet that was sent protected with keys from the current key phase | quic/crypto_keyupdate.go + ack_tracker.go: gate next update on ack of a current-phase packet. |
| `6-13` | informative | The ack gate can be implemented by comparing lowest packet number sent in each key phase with highest acknowledged 1-RTT packet number. <br>_(endpoint [CLIENT])_ | tracking the lowest packet number sent with each key phase and the highest acknowledged packet number in the 1-RTT space | quic/crypto_keyupdate.go: check the tracked lowest-sent-PN-per-phase vs highest-acked-PN comparison. |
| `6-14` | informative | Only 1-RTT keys are ever key-updated; Initial and Handshake keys derive solely from TLS handshake state. <br>_(endpoint [CLIENT])_ | Keys of packets other than the 1-RTT packets are never updated; their keys are derived solely from the TLS handshake state. | quic/conn_crypto.go: key update must touch only the 1-RTT (application) sealer/opener level. |
| `6-15` | informative | The initiator of a key update also installs the corresponding new receive keys. <br>_(endpoint [CLIENT])_ | The endpoint that initiates a key update also updates the keys that it uses for receiving packets. | quic/crypto_keyupdate.go: initiating side derives next read keys at initiation time. |
| `6-16` | MUST | Old keys must be retained until a packet protected with the new keys has been successfully unprotected. <br>_(endpoint,receiver [CLIENT])_ | An endpoint MUST retain old keys until it has successfully unprotected a packet sent using the new keys. | quic/crypto_keyupdate.go + conn_recv.go: old read keys not dropped before first successful new-key decrypt. |
| `6-17` | SHOULD | Old keys should be retained for some period even after a new-key packet has been unprotected, to avoid dropping delayed packets. <br>_(endpoint,receiver [CLIENT])_ | An endpoint SHOULD retain old keys for some time after unprotecting a packet sent using the new keys. | quic/crypto_keyupdate.go: old read keys kept for a grace window (see 6-39 for the 3xPTO bound). |
| `6-18` | informative | A key update is detected when a packet's Key Phase differs from the phase used for the last packet sent; the next key and IV decrypt it. <br>_(receiver [CLIENT])_ | An endpoint detects a key update when processing a packet with a key phase that differs from the value used to protect the last packet it sent. | quic/conn_recv.go: detection compares received phase bit against current send phase, then tries next keys. |
| `6-19` | MUST | On successful decryption with next-phase keys, the endpoint must update its own send keys to that phase. <br>_(endpoint [CLIENT])_ | The endpoint MUST update its send keys to the corresponding key phase in response | quic/crypto_keyupdate.go: peer-initiated update forces local write-key rollover to matching phase. |
| `6-20` | MUST | Send keys must be updated before any acknowledgment of the packet received with updated keys is sent. <br>_(sender [CLIENT])_ | Sending keys MUST be updated before sending an acknowledgment for the packet that was received with updated keys. | quic/conn_recv.go + conn_seal.go: ordering — write keys switch before the ACK covering the triggering packet is sealed. |
| `6-21` | MAY | A second key update detected before the endpoint has acknowledged the first with updated keys may be treated as connection error KEY_UPDATE_ERROR. <br>_(endpoint,receiver [CLIENT])_ | An endpoint MAY treat such consecutive key updates as a connection error of type KEY_UPDATE_ERROR. | quic/conn_recv.go: optional check; record whether implemented or deliberately skipped (MAY). |
| `6-22` | MAY | An ack carried in an old-keys packet that acknowledges a newer-keys packet may be treated as connection error KEY_UPDATE_ERROR. <br>_(endpoint,receiver [CLIENT])_ | carried in a packet protected with old keys where any acknowledged packet was protected with newer keys MAY treat that as a connection error of type KEY_UPDATE_ERROR | quic/conn_recv.go + ack_tracker.go: optional cross-check of ack key phase vs acked packets' phase. |
| `6-23` | MUST NOT | When responding to an apparent key update, the endpoint must not produce a timing side channel revealing that the Key Phase bit was invalid. <br>_(endpoint,receiver [CLIENT])_ | Endpoints responding to an apparent key update MUST NOT generate a timing side-channel signal that might indicate that the Key Phase bit was invalid | quic/conn_recv.go: failed-phase decrypt path should be constant-shape; document best-effort stance in Go. |
| `6-24` | informative | Randomized packet protection keys can stand in for discarded keys when key updates are not yet permitted, avoiding timing variation. <br>_(endpoint,receiver [CLIENT])_ | Endpoints can use randomized packet protection keys in place of discarded keys when key updates are not yet permitted. | quic/crypto_keyupdate.go: check whether discarded-key slots use random keys or a distinguishable fast-fail. |
| `6-25` | MAY | Generating next receive keys lazily during packet processing is permitted, but it creates a timing signal leaking the Key Phase bit. <br>_(endpoint,receiver [CLIENT])_ | An endpoint MAY generate new keys as part of packet processing | quic/crypto_keyupdate.go: note whether next read keys are precomputed or derived on demand. |
| `6-26` | MAY | For up to a PTO after a key update completes, the endpoint may defer generating the next set of receive keys. <br>_(endpoint,receiver [CLIENT])_ | For a short period after a key update completes, up to the PTO, endpoints MAY defer generation of the next set of receive packet protection keys. | quic/crypto_keyupdate.go + pto.go: optional PTO-bounded deferral of next-read-key derivation. |
| `6-27` | SHOULD | Once generated, the next set of receive keys should be kept even if the triggering packet is later discarded (forged-update DoS defense). <br>_(endpoint,receiver [CLIENT])_ | Once generated, the next set of packet protection keys SHOULD be retained, even if the packet that was received was subsequently discarded. | quic/crypto_keyupdate.go: next-generation read keys not thrown away on decrypt failure of the trigger packet. |
| `6-28` | MUST | An endpoint must be capable of holding two sets of receive keys simultaneously: current and next. <br>_(endpoint,receiver [CLIENT])_ | endpoints MUST be able to retain two sets of packet protection keys for receiving packets: the current and the next | quic/crypto_keyupdate.go + conn_crypto.go: 1-RTT opener holds at least current+next key sets. |
| `6-29` | informative | Packets are never sent protected with old keys; only current keys are used for sending. <br>_(sender [CLIENT])_ | An endpoint never sends packets that are protected with old keys. Only the current keys are used. | quic/conn_seal.go + packet_write.go: single current write-key set; no path can seal with a stale generation. |
| `6-30` | informative | Old send keys can be discarded immediately upon switching to newer keys. <br>_(sender [CLIENT])_ | Keys used for protecting packets can be discarded immediately after switching to newer keys. | quic/crypto_keyupdate.go: old write key/IV dropped (zeroed) at rollover. |
| `6-31` | MUST | A sender must protect higher-numbered packets with the same or newer keys than lower-numbered packets. <br>_(sender [CLIENT])_ | Packets with higher packet numbers MUST be protected with either the same or newer packet protection keys than packets with lower packet numbers. | quic/packet_write.go + conn_seal.go: key-phase rollover must be ordered with packet-number allocation under one lock. |
| `6-32` | MUST | Successfully unprotecting with old keys a packet whose number is higher than packets seen under newer keys must be treated as KEY_UPDATE_ERROR. <br>_(receiver [CLIENT])_ | An endpoint that successfully removes protection with old keys when newer keys were used for packets with lower packet numbers MUST treat this as a connection error of type KEY_UPDATE_ERROR | quic/conn_recv.go: after old-key decrypt succeeds, compare recovered PN against newer-key PNs and raise KEY_UPDATE_ERROR (0x0e). |
| `6-33` | informative | Old vs next keys sharing the same Key Phase value are distinguished by packet number: lower than current phase uses previous keys, higher uses next. <br>_(receiver [CLIENT])_ | A recovered packet number that is lower than any packet number from the current key phase uses the previous packet protection keys | quic/conn_recv.go + packet.go: PN-decode-then-select logic for previous/current/next opener choice. |
| `6-34` | informative | Key selection among previous/current/next must be careful not to expose a timing side channel revealing which keys were used. <br>_(receiver [CLIENT])_ | any process for selecting between previous, current, and next packet protection keys does not expose a timing side channel | quic/conn_recv.go: selection branch structure — best-effort constant-shape; document limitation. |
| `6-35` | informative | Alternatively an endpoint may keep only two key sets, swapping previous for next after a reordering window, letting the Key Phase bit alone select keys. <br>_(receiver [CLIENT])_ | endpoints can retain only two sets of packet protection keys, swapping previous for next after enough time has passed to allow for reordering | quic/crypto_keyupdate.go: identify which retention strategy (three-set vs two-set swap) is implemented. |
| `6-36` | MAY | An endpoint may wait roughly one PTO after promoting next receive keys to current before creating the subsequent key set. <br>_(endpoint,receiver [CLIENT])_ | An endpoint MAY allow a period of approximately the Probe Timeout (PTO; see [QUIC-RECOVERY]) after promoting the next set of receive keys to be current | quic/crypto_keyupdate.go + pto.go: optional deferred derivation of the subsequent read-key set. |
| `6-37` | MAY | When the subsequent key set is created, it may replace the previous (oldest) keys at that time. <br>_(endpoint,receiver [CLIENT])_ | These updated keys MAY replace the previous keys at that time. | quic/crypto_keyupdate.go: previous-key eviction timing when subsequent set is generated. |
| `6-38` | SHOULD | An endpoint should wait 3x PTO after the ack confirming the previous key update before initiating another update, so the peer can drop old keys safely. <br>_(endpoint,sender [CLIENT])_ | Endpoints SHOULD wait three times the PTO before initiating a key update after receiving an acknowledgment that confirms that the previous key update was received | quic/crypto_keyupdate.go + pto.go: 3xPTO pacing between successive locally initiated updates. |
| `6-39` | SHOULD | Old read keys should be retained no longer than 3x PTO after the first packet protected with new keys is received. <br>_(endpoint,receiver [CLIENT])_ | An endpoint SHOULD retain old read keys for no more than three times the PTO after having received a packet protected using the new keys | quic/crypto_keyupdate.go: old-read-key retention timer bounded at 3xPTO. |
| `6-40` | SHOULD | After that 3x PTO period, old read keys and their corresponding secrets should be discarded. <br>_(endpoint,receiver [CLIENT])_ | After this period, old read keys and their corresponding secrets SHOULD be discarded. | quic/crypto_keyupdate.go: verify secrets (not just derived keys) are dropped/zeroed on expiry. |
| `6-41` | MUST | An endpoint must count the number of packets it encrypts under each key set. <br>_(endpoint,sender [CLIENT])_ | Endpoints MUST count the number of encrypted packets for each set of keys. | quic/conn_seal.go: per-key-generation encryption counter incremented on every seal (aeadlimit_test.go covers). |
| `6-42` | MUST | If encrypted-packet count with one key exceeds the AEAD's confidentiality limit, the endpoint must stop using those keys. <br>_(endpoint,sender [CLIENT])_ | If the total number of encrypted packets with the same key exceeds the confidentiality limit for the selected AEAD, the endpoint MUST stop using those keys | quic/conn_seal.go + crypto_keyupdate.go: hard stop on sealing once confidentiality limit hit. |
| `6-43` | MUST | A key update must be initiated before the confidentiality limit for the selected AEAD is exceeded by sent packets. <br>_(endpoint,sender [CLIENT])_ | Endpoints MUST initiate a key update before sending more protected packets than the confidentiality limit for the selected AEAD permits. | quic/crypto_keyupdate.go: proactive update triggered below the limit (with headroom for the ack-gate in 6-12). |
| `6-44` | MUST | If a key update is impossible or the integrity limit is reached, the endpoint must stop using the connection, responding only with stateless resets. <br>_(endpoint [CLIENT])_ | the endpoint MUST stop using the connection and only send stateless resets in response to receiving packets | quic/conn_seal.go + close.go: client generates no stateless resets by design; must still cease all sends on the connection. |
| `6-45` | RECOMMENDED | It is recommended to immediately close the connection with AEAD_LIMIT_REACHED before reaching a state where key updates are impossible. <br>_(endpoint [CLIENT])_ | It is RECOMMENDED that endpoints immediately close the connection with a connection error of type AEAD_LIMIT_REACHED before reaching a state where key updates are not possible. | quic/close.go + errors.go: close path uses AEAD_LIMIT_REACHED (0x0f) when limit is imminent and update blocked. |
| `6-46` | informative | Confidentiality limit for AEAD_AES_128_GCM and AEAD_AES_256_GCM is 2^23 encrypted packets per key. <br>_(endpoint,sender [CLIENT])_ | For AEAD_AES_128_GCM and AEAD_AES_256_GCM, the confidentiality limit is 2^23 encrypted packets | quic/crypto.go or conn_seal.go: constant 1<<23 for GCM suites. |
| `6-47` | informative | Confidentiality limit for AEAD_CHACHA20_POLY1305 exceeds the maximum possible packets (2^62) and can be disregarded. <br>_(endpoint,sender [CLIENT])_ | For AEAD_CHACHA20_POLY1305, the confidentiality limit is greater than the number of possible packets (2^62) and so can be disregarded. | quic/crypto.go: ChaCha20 confidentiality limit effectively unbounded (or 1<<62 sentinel). |
| `6-48` | informative | Confidentiality limit for AEAD_AES_128_CCM is 2^21.5 encrypted packets. <br>_(endpoint,sender [not-client])_ | For AEAD_AES_128_CCM, the confidentiality limit is 2^21.5 encrypted packets | quic/crypto.go: verify AES-128-CCM is never offered/negotiated; if it is, this constant is required. |
| `6-49` | MUST | An endpoint must count received packets that fail authentication over the connection's lifetime. <br>_(endpoint,receiver [CLIENT])_ | endpoints MUST count the number of received packets that fail authentication during the lifetime of a connection | quic/conn_recv.go: connection-lifetime forgery counter incremented on every AEAD open failure, across all keys and levels. |
| `6-50` | MUST | If authentication failures across all keys exceed the AEAD's integrity limit, the endpoint must immediately close with AEAD_LIMIT_REACHED and process no more packets. <br>_(endpoint,receiver [CLIENT])_ | the endpoint MUST immediately close the connection with a connection error of type AEAD_LIMIT_REACHED and not process any more packets | quic/conn_recv.go + errors.go: integrity-limit breach closes with 0x0f and halts packet processing (aeadlimit_test.go). |
| `6-51` | informative | Integrity limit for AEAD_AES_128_GCM and AEAD_AES_256_GCM is 2^52 invalid packets. <br>_(endpoint,receiver [CLIENT])_ | For AEAD_AES_128_GCM and AEAD_AES_256_GCM, the integrity limit is 2^52 invalid packets | quic/crypto.go or conn_recv.go: constant 1<<52 for GCM suites. |
| `6-52` | informative | Integrity limit for AEAD_CHACHA20_POLY1305 is 2^36 invalid packets. <br>_(endpoint,receiver [CLIENT])_ | For AEAD_CHACHA20_POLY1305, the integrity limit is 2^36 invalid packets | quic/crypto.go: constant 1<<36 for ChaCha20-Poly1305 — differs from GCM; do not share one constant. |
| `6-53` | informative | Integrity limit for AEAD_AES_128_CCM is 2^21.5 invalid packets. <br>_(endpoint,receiver [not-client])_ | For AEAD_AES_128_CCM, the integrity limit is 2^21.5 invalid packets | quic/crypto.go: only relevant if AES-128-CCM ever negotiated; verify it is not. |
| `6-54` | MAY | Endpoints that limit packet size may use higher confidentiality and integrity limits per Appendix B. <br>_(endpoint [CLIENT])_ | Endpoints that limit the size of packets MAY use higher confidentiality and integrity limits | quic/crypto.go: if default limits used, no action; note if larger limits are claimed they need the Appendix B size bound. |
| `6-55` | MAY | Future analyses and specifications may relax the confidentiality or integrity limits for an AEAD. <br>_(all [not-client])_ | Future analyses and specifications MAY relax confidentiality or integrity limits for an AEAD. | n/a — spec-evolution clause; no code obligation. |
| `6-56` | MUST | Any TLS cipher suite specified for QUIC must define AEAD confidentiality and integrity usage limits. <br>_(all [not-client])_ | Any TLS cipher suite that is specified for use with QUIC MUST define limits on the use of the associated AEAD function | n/a — obligation on cipher-suite specifications, not implementations; matters only if a non-RFC-9001 suite is added. |
| `6-57` | informative | KEY_UPDATE_ERROR is transport error code 0x0e, signaling key-update-related errors. <br>_(all [CLIENT])_ | The KEY_UPDATE_ERROR error code (0x0e) is used to signal errors related to key updates. | quic/errors.go: KEY_UPDATE_ERROR defined as 0x0e and used by the checks in 6-21/6-22/6-32. |

### 9001 section 7-8 - RFC 9001 §7-8: Security of Initial Messages; QUIC-Specific Adjustments to the TLS Handshake (ALPN, transport parameters extension, EndOfEarlyData removal, middlebox compatibility mode prohibition)

| ID | Level | Requirement (scope) | RFC 9001 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `7-8-1` | SHOULD | Because Initial packets are not protected with a secret key and can be tampered with, implementations should be cautious about relying on any unauthenticated data carried in Initial packets (e.g., ACK frames, ACK Delay). <br>_(all [CLIENT])_ | Implementations SHOULD use caution in relying on any data that is contained in Initial packets that is not otherwise authenticated. | quic/conn_recv.go + loss recovery: Initial keys derive from a public salt — verify ACK/ACK-Delay data from Initial space is not trusted for anything irreversible (e.g., discarding state) beyond what RFC 9002 requires. |
| `7-8-2` | informative | QUIC requires the cryptographic handshake to provide authenticated application-protocol negotiation. <br>_(endpoint [CLIENT])_ | QUIC requires that the cryptographic handshake provide authenticated protocol negotiation. | quic/handshake.go: negotiation happens inside TLS (ALPN via crypto/tls QUIC mode) so it is authenticated by the handshake; confirm no out-of-band protocol selection path exists. |
| `7-8-3` | MUST | Unless another agreed mechanism exists, endpoints must use ALPN to negotiate the application protocol. <br>_(endpoint [CLIENT])_ | Unless another mechanism is used for agreeing on an application protocol, endpoints MUST use ALPN for this purpose. | quic/handshake.go: verify the client always populates ALPN (NextProtos) in the TLS config — http3.Dial should force "h3"; consider rejecting an empty ALPN list at Dial time. |
| `7-8-4` | MUST | When using ALPN, endpoints must immediately close the connection with a no_application_protocol TLS alert (QUIC error code 0x0178) if no application protocol is negotiated. <br>_(endpoint [CLIENT])_ | endpoints MUST immediately close a connection (see Section 10.2 of [QUIC-TRANSPORT]) with a no_application_protocol TLS alert (QUIC error code 0x0178; see Section 4.8) | quic/handshake.go + quic/errors.go: on handshake completion with no negotiated ALPN, send CONNECTION_CLOSE with error 0x0178 (CRYPTO_ERROR base 0x0100 + alert 120), not a generic error. |
| `7-8-5` | MUST | QUIC clients specifically (not only servers, as in base ALPN) must use error code 0x0178 to terminate the connection when ALPN negotiation fails. <br>_(client [CLIENT])_ | QUIC clients MUST use error 0x0178 to terminate a connection when ALPN negotiation fails. | quic/errors.go: verify the TLS alert→transport-error mapping (0x0100-0x01ff) covers no_application_protocol (alert 120 → 0x0178) and that handshake.go routes client-side ALPN failure through it. |
| `7-8-6` | MAY | An application protocol may restrict which QUIC versions it can operate over. <br>_(all [CLIENT])_ | An application protocol MAY restrict the QUIC versions that it can operate over. | quic/handshake.go + http3: client offers only QUIC v1, so offering "h3" (defined for v1) is consistent; nothing to change unless more versions are added. |
| `7-8-7` | MUST | Servers must select an application protocol compatible with the QUIC version the client selected. <br>_(server [not-client])_ | Servers MUST select an application protocol compatible with the QUIC version that the client has selected. | N/A — server-only; this client has no server role. |
| `7-8-8` | MUST | A server must treat inability to select a compatible application protocol as a connection error of type 0x0178 (no_application_protocol). <br>_(server [not-client])_ | The server MUST treat the inability to select a compatible application protocol as a connection error of type 0x0178 (no_application_protocol). | N/A — server-only; this client has no server role. |
| `7-8-9` | MUST | A client must treat the server's selection of an application protocol incompatible with the QUIC version in use as a connection error of type 0x0178. <br>_(client [CLIENT])_ | a client MUST treat the selection of an incompatible application protocol by a server as a connection error of type 0x0178. | quic/handshake.go: verify the client validates the server-selected ALPN is one it offered (crypto/tls enforces membership) and that a mismatch is surfaced as 0x0178, not a generic handshake error. |
| `7-8-10` | informative | QUIC transport parameters are carried in a TLS extension with ExtensionType codepoint 0x39 (quic_transport_parameters). <br>_(endpoint [CLIENT])_ | quic_transport_parameters(0x39), (65535) | quic/transport_params.go + handshake.go: verify the extension goes on the wire as type 0x39 (crypto/tls QUICConfig handles this) — not the pre-RFC draft codepoint 0xffa5. |
| `7-8-11` | informative | The extension_data of the quic_transport_parameters extension is defined by the QUIC version in use (for v1, the RFC 9000 §18 encoding). <br>_(endpoint [CLIENT])_ | The extension_data field of the quic_transport_parameters extension contains a value that is defined by the version of QUIC that is in use. | quic/transport_params.go: verify marshal/unmarshal uses the QUIC v1 sequence-of-(id,length,value) varint encoding from RFC 9000 §18. |
| `7-8-12` | informative | The quic_transport_parameters extension is carried in the ClientHello (client's parameters) and the EncryptedExtensions message (server's parameters). <br>_(endpoint [CLIENT])_ | The quic_transport_parameters extension is carried in the ClientHello and the EncryptedExtensions messages during the handshake. | quic/handshake.go + transport_params.go: client must place its TPs in ClientHello and parse the server's from the EncryptedExtensions/transport-parameters event of the TLS stack — not from any other message. |
| `7-8-13` | MUST | Endpoints must send the quic_transport_parameters extension; it is not optional. <br>_(endpoint [CLIENT])_ | Endpoints MUST send the quic_transport_parameters extension | quic/handshake.go: verify client transport parameters are always set on the TLS/QUIC config before the ClientHello is generated, on every Dial path. |
| `7-8-14` | MUST | An endpoint receiving a ClientHello or EncryptedExtensions without the quic_transport_parameters extension must close the connection with error 0x016d (equivalent to a fatal TLS missing_extension alert). <br>_(endpoint,receiver [CLIENT])_ | endpoints that receive ClientHello or EncryptedExtensions messages without the quic_transport_parameters extension MUST close the connection with an error of type 0x016d | quic/handshake.go + errors.go: if the server's EncryptedExtensions carries no transport parameters, close with 0x016d (0x0100 + missing_extension alert 109) — verify this exact code, not PROTOCOL_VIOLATION. |
| `7-8-15` | informative | Transport parameter values are available before handshake completion but are not authenticated until it completes, so no use of them may depend on their authenticity; tampering causes handshake failure. <br>_(endpoint [CLIENT])_ | the value of transport parameters is not authenticated until the handshake completes, so any use of these parameters cannot depend on their authenticity | quic/transport_params.go + conn: client applies server TPs when EncryptedExtensions arrives (pre-Finished); acceptable, but verify nothing irreversible (e.g., discarding state keyed on TPs) happens before handshake confirmation. |
| `7-8-16` | MUST NOT | Endpoints must not send the quic_transport_parameters extension in a TLS connection that does not use QUIC (e.g., TLS over TCP). <br>_(endpoint [CLIENT])_ | Endpoints MUST NOT send this extension in a TLS connection that does not use QUIC | quic/transport_params.go: verify the extension is only wired through the QUIC handshake path (crypto/tls QUICConn) and never registered on any plain-TLS config elsewhere in the repo (e.g., client/ H1/H2 TLS dials). |
| `7-8-17` | MUST | An implementation that supports the quic_transport_parameters extension must send a fatal unsupported_extension alert if it receives the extension when the transport is not QUIC. <br>_(endpoint [CLIENT])_ | A fatal unsupported_extension alert MUST be sent by an implementation that supports this extension if the extension is received when the transport is not QUIC. | quic/handshake.go: vacuously satisfied if the extension exists only inside the QUIC handshake (crypto/tls enforces this); confirm no custom TLS-over-TCP code accepts extension 0x39. |
| `7-8-18` | informative | Negotiating the quic_transport_parameters extension removes the EndOfEarlyData message from the TLS handshake. <br>_(endpoint [CLIENT])_ | Negotiating the quic_transport_parameters extension causes the EndOfEarlyData to be removed | quic/handshake.go: transcript handling is owned by crypto/tls QUIC mode, which omits EndOfEarlyData; nothing custom, but confirm no code expects or injects it. |
| `7-8-19` | informative | The TLS EndOfEarlyData message is not used with QUIC; QUIC does not rely on it to end 0-RTT data or signal the switch to Handshake keys. <br>_(endpoint [CLIENT])_ | The TLS EndOfEarlyData message is not used with QUIC. | quic/handshake.go + conn_crypto.go: key-level transitions must be driven by QUIC encryption-level events, never by an EndOfEarlyData marker; verify (moot in practice since 0-RTT is not implemented). |
| `7-8-20` | MUST NOT | Clients must never send the EndOfEarlyData TLS message. <br>_(client [CLIENT])_ | Clients MUST NOT send the EndOfEarlyData message. | quic/handshake.go: no 0-RTT is implemented, and crypto/tls QUIC mode never emits EndOfEarlyData; verify no code path writes it into the CRYPTO stream. |
| `7-8-21` | MUST | A server must treat receipt of a CRYPTO frame in a 0-RTT packet as a connection error of type PROTOCOL_VIOLATION. <br>_(server [not-client])_ | A server MUST treat receipt of a CRYPTO frame in a 0-RTT packet as a connection error of type PROTOCOL_VIOLATION. | N/A — server-side rule; client neither sends 0-RTT (not implemented) nor receives 0-RTT packets. |
| `7-8-22` | informative | As a consequence of its removal, EndOfEarlyData never appears in the TLS handshake transcript for QUIC. <br>_(endpoint [CLIENT])_ | EndOfEarlyData does not appear in the TLS handshake transcript. | quic/handshake.go: transcript hash is computed inside crypto/tls; nothing custom to reconcile beyond not feeding synthetic messages into the handshake byte stream. |
| `7-8-23` | informative | TLS 1.3 middlebox compatibility mode is defined as setting legacy_session_id to a 32-byte value in ClientHello/ServerHello and sending a change_cipher_spec record; both carry no semantics. <br>_(endpoint [CLIENT])_ | setting the legacy_session_id field to a 32-byte value in the ClientHello and ServerHello, then sending a change_cipher_spec record | quic/handshake.go: this defines what 7-8-25 prohibits — the client's ClientHello legacy_session_id must be empty; verify crypto/tls QUIC mode leaves it empty. |
| `7-8-24` | informative | QUIC provides no way to carry a change_cipher_spec record at all. <br>_(endpoint [CLIENT])_ | QUIC also provides no means to carry a change_cipher_spec record. | quic/handshake.go + conn_crypto.go: CRYPTO frames must carry only TLS handshake messages — verify no record-layer bytes (CCS or otherwise) are ever written to or accepted from the CRYPTO stream. |
| `7-8-25` | MUST NOT | A client must not request TLS 1.3 middlebox compatibility mode (i.e., must send an empty legacy_session_id and no change_cipher_spec). <br>_(client [CLIENT])_ | A client MUST NOT request the use of the TLS 1.3 compatibility mode. | quic/handshake.go: verify the TLS stack in QUIC mode (crypto/tls QUICClient) generates a ClientHello with empty legacy_session_id — add a wire-level assertion test on the emitted ClientHello if absent. |
| `7-8-26` | SHOULD | A server should treat a ClientHello with a non-empty legacy_session_id field as a connection error of type PROTOCOL_VIOLATION. <br>_(server [not-client])_ | A server SHOULD treat the receipt of a TLS ClientHello with a non-empty legacy_session_id field as a connection error of type PROTOCOL_VIOLATION. | N/A — server-only; but note interop implication for the client: servers may enforce this, reinforcing 7-8-25 (client's legacy_session_id must be empty or connections will be rejected). |

### 9001 section 9-10 - RFC 9001 Sections 9-10: Security Considerations (session linkability, 0-RTT replay, packet reflection, header protection analysis, timing side channels, key diversity, randomness) and IANA Considerations

| ID | Level | Requirement (scope) | RFC 9001 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `9-10-1` | informative | TLS session tickets let servers and other entities correlate connections made by the same client, a session-linkability privacy exposure. <br>_(client,server [CLIENT])_ | Use of TLS session tickets allows servers and possibly other entities to correlate connections made by the same client | quic/handshake.go — check whether the client enables TLS session resumption/ticket caching; if no session cache is configured, linkability is moot, but document that adding resumption reintroduces it. |
| `9-10-2` | MUST | Endpoints using 0-RTT must implement and use the TLS 1.3 replay protections (anti-replay mechanisms of RFC 8446 Section 8). <br>_(endpoint [CLIENT])_ | Endpoints MUST implement and use the replay protections described in [TLS13] | quic/handshake.go — client is 0-RTT-free by design; verify early data is never offered in ClientHello so this MUST is satisfied vacuously; re-audit if 0-RTT is ever added. |
| `9-10-3` | informative | QUIC frame processing is designed to be idempotent: replayed, reordered, or lost frames cannot produce invalid connection states, and connection effects do not outlive the connection. <br>_(endpoint [CLIENT])_ | Processing of QUIC frames is idempotent and cannot result in invalid connection states if frames are replayed, reordered, or lost. | **VERIFY** quic/conn_recv.go — verify duplicate and reordered frame handling (retransmitted CRYPTO/STREAM data, duplicate ACK ranges) is idempotent and never corrupts connection state. -- Quote (line 2014-2016), level (informative, no 2119 keyword) and roles are all fine, but the requirement's second clause overstates the RFC. Source line 2016-2018 reads: 'QUIC connections do not produce effects that last beyond the lifetime of the connection, except for those produced by the application protocol that QUIC serves.' The fact drops the carve-out and asserts flatly that 'connection effects do not outlive the connection', which is the opposite of what the RFC says about application-protocol effects — and that carve-out is the whole reason §9.2 pushes replay responsibility onto the application protocol (9-10-6). Fix: reword to '...replayed, reordered, or lost frames cannot produce invalid connection states, and QUIC connections produce no effects lasting beyond the connection EXCEPT those produced by the application protocol QUIC serves.' Optionally split the second clause into its own fact with quote 'QUIC connections do not produce effects that last beyond the lifetime of the connection, except for those produced by the application protocol that QUIC serves.' \|\| Quote (line 2014-2016), level (informative, no 2119 keyword) and applies_to (endpoint) are all correct, but the requirement text materially misstates the RFC's second clause. It says "connection effects do not outlive the connection" with no qualifier; RFC 9001 §9.2 lines 2016-2018 say "QUIC connections do not produce effects that last beyond the lifetime of the connection, except for those produced by the application protocol that QUIC serves." The dropped exception is not incidental — it is the premise for the rest of §9.2 (the application protocol MUST describe its 0-RTT replay measures, fact 9-10-6). Fix: restate as "...and QUIC connections produce no effects lasting beyond the connection lifetime EXCEPT those produced by the application protocol QUIC serves," or split the second clause into its own fact quoting lines 2016-2018 verbatim. |
| `9-10-4` | MUST NOT | TLS session tickets and address validation tokens must not be used to communicate application semantics between endpoints. <br>_(endpoint [CLIENT])_ | These MUST NOT be used to communicate application semantics between endpoints | quic/retry.go + quic/handshake.go — confirm the client never encodes or extracts application meaning from Retry/NEW_TOKEN tokens or session tickets. |
| `9-10-5` | MUST | Clients must treat session tickets and address validation tokens as opaque values, never parsing or interpreting their contents. <br>_(client [CLIENT])_ | clients MUST treat them as opaque values | quic/retry.go — verify the Retry token is stored and echoed byte-for-byte into the subsequent Initial without any parsing; same for any NEW_TOKEN handling in handshake.go/conn_recv.go. |
| `9-10-6` | MUST | An application protocol running over QUIC must document how it uses 0-RTT and what replay-attack protections it employs. <br>_(all [not-client])_ | An application protocol that uses QUIC MUST describe how the protocol uses 0-RTT and the measures that are employed to protect against replay attack. | Spec-authorship requirement on application-protocol definitions (HTTP/3 = RFC 9114 satisfies it); no code obligation since http3/ never uses 0-RTT. |
| `9-10-7` | informative | Disabling 0-RTT entirely is the most effective replay-attack defense. <br>_(endpoint [CLIENT])_ | Disabling 0-RTT entirely is the most effective defense against replay attack. | quic/handshake.go — the client's deliberate no-0-RTT stance implements the strongest defense; note this in docs/RFC_COVERAGE.md as intentional. |
| `9-10-8` | MUST | Any QUIC extension must either describe how replay attacks affect its operation or prohibit its use in 0-RTT. <br>_(all [not-client])_ | QUIC extensions MUST either describe how replay attacks affect their operation or prohibit the use of the extension in 0-RTT. | Spec-authorship requirement for extension definitions; relevant only if quic/ ever defines custom transport-parameter extensions (transport_params.go currently sends standard parameters only). |
| `9-10-9` | MUST | Application protocols must either prohibit extensions carrying application semantics in 0-RTT or provide replay-mitigation strategies for them. <br>_(all [not-client])_ | Application protocols MUST either prohibit the use of extensions that carry application semantics in 0-RTT or provide replay mitigation strategies. | Spec-authorship requirement on application protocols; vacuous for this client (no 0-RTT anywhere in http3/ or quic/). |
| `9-10-10` | MUST | The packet containing a ClientHello must be padded to a minimum size, as an anti-amplification / anti-reflection defense. <br>_(client [CLIENT])_ | the packet containing a ClientHello MUST be padded to a minimum size | quic/packet.go + the Initial send path driven by handshake.go — verify client Initial datagrams carrying ClientHello CRYPTO frames are padded to at least 1200 bytes (RFC 9000 Section 14.1). |
| `9-10-11` | informative | A server responding to an unverified source address is limited to sending at most three times the bytes it has received (anti-amplification limit, normative home RFC 9000 Section 8.1). <br>_(server [not-client])_ | the server is forbidden to send more than three times as many bytes as the number of bytes it has received | Server-only anti-amplification restatement; no client obligation (client is never the address-unverified responder). |
| `9-10-12` | informative | Header protection is applied after packet-protection AEAD by sampling bytes from AEAD output and XORing the header field with a PRF of the hp key and sample: protected_field = field XOR PRF(hp_key, sample). <br>_(endpoint [CLIENT])_ | protected_field = field XOR PRF(hp_key, sample) | quic/crypto_protect.go — verify mask = PRF(hp_key, 16-byte sample from ciphertext) is XORed onto the protected first-byte bits and packet-number bytes, applied after AEAD sealing / before AEAD opening. |
| `9-10-13` | MUST | Future header-protection variants based on this construction must use a PRF to preserve the equivalent (AE2/HN1) security guarantees. <br>_(all [not-client])_ | Future header protection variants based on this construction MUST use a PRF to ensure equivalent security guarantees. | Spec-authorship constraint on future cipher-suite/HP definitions; quic/crypto_protect.go implements only the RFC 9001 AES-ECB and ChaCha20 variants, both PRPs (hence PRFs). |
| `9-10-14` | informative | Because hp_key is distinct from the packet-protection key, the header-protection construction achieves AE2 security, guaranteeing privacy of the protected header field. <br>_(endpoint [CLIENT])_ | As "hp_key" is distinct from the packet protection key, it follows that header protection achieves AE2 security | quic/crypto.go — verify hp_key is derived with its own HKDF-Expand-Label ("quic hp") from the traffic secret and is never the AEAD packet key reused. |
| `9-10-15` | informative | Using the same header-protection key and ciphertext sample more than once risks compromising header protection (reveals XOR of the protected fields); sample collisions obey the birthday bound. <br>_(endpoint [CLIENT])_ | Use of the same key and ciphertext sample more than once risks compromising header protection. | quic/crypto_protect.go — verify the sample is always taken from the per-packet AEAD ciphertext (offset pn_offset+4, 16 bytes), never a fixed or reused buffer. |
| `9-10-16` | informative | The entire packet header is part of the AEAD authenticated additional data, so the header is transitively authenticated by packet protection. <br>_(endpoint [CLIENT])_ | the entire packet header is part of the authenticated additional data | quic/crypto_protect.go (seal) + quic/conn_recv.go (open) — verify AAD spans the full header from the first byte through the unprotected packet number, with header-protection removed first on receive. |
| `9-10-17` | informative | Falsified or modified protected header fields can only be detected once packet protection is removed; header fields are untrustworthy before AEAD verification. <br>_(receiver [CLIENT])_ | Protected fields that are falsified or modified can only be detected once the packet protection is removed. | quic/conn_recv.go — verify no state change (PN dedup marking, key-phase commitment, ACK generation) happens based on header fields before the AEAD open succeeds; failed opens are discarded without effect. |
| `9-10-18` | MUST | On receive, header-protection removal, packet-number recovery, and packet-protection removal must be applied together, free of timing and other side channels (e.g., no discarding duplicate packet numbers before attempting decryption). <br>_(receiver [CLIENT])_ | the entire process of header protection removal, packet number recovery, and packet protection removal MUST be applied together without timing and other side channels | quic/conn_recv.go — verify the decrypt path does not short-circuit on duplicate packet number, guessed PN length, or Key Phase before AEAD open; dedup checks must run only after successful decryption. |
| `9-10-19` | MUST | On send, construction and protection of packet payloads and packet numbers must be free from side channels that would reveal the packet number or its encoded size. <br>_(sender [CLIENT])_ | construction and protection of packet payloads and packet numbers MUST be free from side channels that would reveal the packet number or its encoded size | quic/crypto_protect.go + quic/packet.go — verify PN encoding and header-protection application on the send path have no externally observable branching/timing dependent on PN value or length. |
| `9-10-20` | SHOULD | After receiving a key update, an endpoint should generate and save the next set of receive packet-protection keys (per Section 6.3), so later receipt does not create timing signals leaking the Key Phase. <br>_(endpoint [CLIENT])_ | After receiving a key update, an endpoint SHOULD generate and save the next set of receive packet protection keys | quic/crypto_keyupdate.go — verify the next-generation receive keys are precomputed eagerly after a key update completes, not derived lazily inside the per-packet decrypt path. |
| `9-10-21` | informative | The pre-generation approach can require retaining three sets of receive packet-protection keys: previous, current, and next key phase. <br>_(endpoint [CLIENT])_ | endpoints maintain three sets of packet protection keys for receiving: for the previous key phase, for the current key phase, and for the next key phase | quic/crypto_keyupdate.go — check which retention model is implemented (three receive key sets vs two) and that the choice is deliberate and documented. |
| `9-10-22` | informative | As an alternative, endpoints can defer generating the next receive keys until old keys are discarded, so only two sets of receive keys are retained at any time. <br>_(endpoint [CLIENT])_ | Endpoints can instead choose to defer generation of the next receive packet protection keys until they discard old keys | quic/crypto_keyupdate.go — if the deferred model is used, verify next-key generation happens at old-key-discard time (still outside the packet-processing hot path) to keep the timing-side-channel property of 9-10-20. |
| `9-10-23` | informative | QUIC packet-protection keys and IVs are derived using different HKDF labels than the equivalent TLS keys, ensuring key separation from TLS-over-TCP. <br>_(endpoint [CLIENT])_ | The QUIC packet protection keys and IVs are derived using a different label than the equivalent keys in TLS. | quic/crypto.go — verify HKDF-Expand-Label uses "quic key", "quic iv", "quic hp" (and "quic ku" for updates), never the TLS "key"/"iv" labels. |
| `9-10-24` | SHOULD | A new QUIC version should define new key-derivation labels for the packet-protection key, IV, and header-protection keys to preserve cross-version key separation. <br>_(all [not-client])_ | a new version of QUIC SHOULD define new labels for key derivation for packet protection key and IV, plus the header protection keys | Spec-authorship requirement for future QUIC versions; quic/ implements v1 only, so crypto.go correctly hard-codes the v1 labels. |
| `9-10-25` | informative | QUIC version 1 uses the label string "quic" in key derivation; other versions can substitute a version-specific label. <br>_(endpoint [CLIENT])_ | This version of QUIC uses the string "quic". | quic/crypto.go — confirm every HKDF-Expand-Label label constant starts with the literal "quic " prefix (v1 wire constant). |
| `9-10-26` | informative | The Initial secrets are keyed with a value specific to the negotiated QUIC version (the version-specific Initial salt). <br>_(endpoint [CLIENT])_ | The initial secrets use a key that is specific to the negotiated QUIC version. | quic/initial.go — verify the v1 Initial salt constant 0x38762cf7f55934b34d179ae6a4c80cadccbb7f0a is used and is tied to version 0x00000001 (no other version accepted). |
| `9-10-27` | SHOULD | New QUIC versions should define a new salt value for calculating Initial secrets. <br>_(all [not-client])_ | New QUIC versions SHOULD define a new salt value used in calculating initial secrets. | Spec-authorship requirement for future versions; quic/initial.go pins the v1 salt only — correct for a v1-only client. |
| `9-10-28` | informative | Endpoints must be able to generate secure random numbers, both directly for protocol values such as connection IDs and transitively via TLS (guidance in RFC 4086). <br>_(endpoint [CLIENT])_ | QUIC depends on endpoints being able to generate secure random numbers, both directly for protocol values such as the connection ID, and transitively via TLS. | quic/ connection-setup path (callers of packet.go/handshake.go that mint SCID/DCID) — verify crypto/rand is used for connection IDs and TLS random values, never math/rand. |
| `9-10-29` | informative | The quic_transport_parameters TLS extension has IANA-registered codepoint 57 (0x39) in the TLS ExtensionType Values registry. <br>_(endpoint [CLIENT])_ | IANA has registered a codepoint of 57 (or 0x39) for the quic_transport_parameters extension | quic/transport_params.go — verify the extension-type constant used when adding the extension to ClientHello (and when matching it in EncryptedExtensions) equals 57 (0x39). |
| `9-10-30` | informative | The quic_transport_parameters extension is permitted in exactly two TLS 1.3 messages: ClientHello (CH) and EncryptedExtensions (EE); the client sends it in CH and receives the server's in EE. <br>_(client,server [CLIENT])_ | The TLS 1.3 Column includes CH (ClientHello) and EE (EncryptedExtensions). | quic/transport_params.go + quic/handshake.go — verify the client offers the extension in ClientHello and reads the server's transport parameters only from EncryptedExtensions, rejecting their absence. |
| `9-10-31` | informative | The IANA registry entry for quic_transport_parameters is marked Recommended = Yes (registry metadata). <br>_(all [not-client])_ | The Recommended column for this extension is marked Yes. | Registry metadata only; no code obligation in quic/transport_params.go. |

