# RFC 9204 (QPACK) - normative facts for client conformance

_Generated from local RFC source via FABLE extraction + double independent Opus verification (default-refute). Every quote is verbatim from the fetched RFC 9204 text and confirmed against the source by two verifiers. Rows flagged VERIFY had a verifier disagreement -- correction inline, needs a manual source check._

### 9204 section 1-2.1 - RFC 9204 §1-2.1: Introduction, Conventions, Compression Overview, Encoder (insertions, blocked streams, deadlocks, Known Received Count)

| ID | Level | Requirement (scope) | RFC 9204 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `1-2.1-1` | informative | BCP 14 key words are normative only when they appear in all capitals. <br>_(all [CLIENT])_ | are to be interpreted as described in BCP 14 [RFC2119] [RFC8174] when, and only when, they appear in all capitals | Meta-rule for reading this catalog; no code check. |
| `1-2.1-2` | informative | An HTTP field value is the concatenation of all field line values with that name in the section, joined with comma separators; QPACK itself carries individual field lines. <br>_(all [CLIENT])_ | composed from all field line values with that field name in that section, concatenated together with comma separators | http3/validate.go + http3/response.go: ensure decoder surfaces individual field lines (no premature comma-joining) since QPACK operates on field lines, not joined values. |
| `1-2.1-3` | informative | Each dynamic table entry has a unique Absolute Index. <br>_(endpoint [CLIENT])_ | A unique index for each entry in the dynamic table. | qpack/dynamic_table.go: verify entries carry a monotonically assigned absolute index that never repeats within a connection (basis for §3.2.4 arithmetic). |
| `1-2.1-4` | informative | Base is the reference point for relative and post-Base indices; dynamic-table representations are interpreted relative to a Base. <br>_(endpoint [CLIENT])_ | A reference point for relative and post-Base indices. Representations that reference dynamic table entries are relative to a Base. | qpack/encoder.go (emitting) and qpack/decoder.go (interpreting): confirm all dynamic-table representation indices are computed relative to the field-section Base, not raw absolute indices. |
| `1-2.1-5` | informative | Insert Count is the total number of entries ever inserted into the dynamic table (not the current entry count). <br>_(endpoint [CLIENT])_ | The total number of entries inserted in the dynamic table. | qpack/dynamic_table.go: Insert Count counter must be cumulative across evictions, maintained on both encoder (requests) and decoder (responses) sides. |
| `1-2.1-6` | informative | Diagram notation: x (A) means x is A bits long; x (A+) means x uses the Section 4.1.1 prefixed-integer encoding with an A-bit prefix; x ... means variable length to end of region. <br>_(all [CLIENT])_ | x (A+) Indicates that x uses the prefixed integer encoding defined in Section 4.1.1, beginning with an A-bit prefix. | qpack/instructions.go: every field the RFC marks (A+) must go through the shared prefixed-integer codec with the correct prefix width per instruction. |
| `1-2.1-7` | informative | QPACK uses two tables: a predefined static table and a per-connection dynamic table usable by the encoder for both header and trailer field lines. <br>_(endpoint [CLIENT])_ | The static table (Section 3.1) is predefined and contains common header field lines (some of them with an empty value). | qpack/static_table.go (exact predefined entries incl. empty-value ones) and qpack/dynamic_table.go (per-connection state, usable for trailers too — check http3/request.go trailer path if present). |
| `1-2.1-8` | informative | QPACK defines unidirectional streams carrying instructions from encoder to decoder and from decoder to encoder. <br>_(endpoint [CLIENT])_ | QPACK defines unidirectional streams for sending instructions from encoder to decoder and vice versa. | http3/control.go: client must open both its encoder stream (carrying its encoder instructions) and its decoder stream (carrying Section Ack / Stream Cancellation / ICI), and read the peer's. |
| `1-2.1-9` | informative | Static-table references and literal representations require no dynamic state and never risk head-of-line blocking; only dynamic-table references can block. <br>_(sender [CLIENT])_ | References to the static table and literal representations do not require any dynamic state and never risk head-of-line blocking. | qpack/encoder.go: blocked-stream accounting must exclude sections that use only static/literal representations (Required Insert Count 0). |
| `1-2.1-10` | MAY | An encoder MAY insert any entry into the dynamic table, not only field lines it is currently compressing. <br>_(sender [CLIENT])_ | An encoder MAY insert any entry in the dynamic table it chooses; it is not limited to field lines it is compressing. | qpack/encoder.go: permissive — no check needed, but API should not structurally forbid speculative insertions if dynamic-table insertion is implemented. |
| `1-2.1-11` | MUST | An encoder MUST emit field representations in the exact order the field lines appear in the input field section. <br>_(sender [CLIENT])_ | An encoder MUST emit field representations in the order they appear in the input field section. | qpack/encoder.go driven by http3/request.go: verify encoding iterates the request field list in order with no reordering (e.g., no map-based iteration or index-sorting). |
| `1-2.1-12` | informative | A dynamic table entry cannot be evicted immediately after insertion, even if never referenced. <br>_(sender [CLIENT])_ | A dynamic table entry cannot be evicted immediately after insertion, even if it has never been referenced. | qpack/dynamic_table.go: eviction on the encoder side must be gated on acknowledgment state, not just capacity pressure. |
| `1-2.1-13` | informative | An entry becomes evictable only once its insertion has been acknowledged and it has no outstanding references in unacknowledged representations. <br>_(sender [CLIENT])_ | Once the insertion of a dynamic table entry has been acknowledged and there are no outstanding references to the entry in unacknowledged representations, the entry becomes evictable | qpack/encoder.go + qpack/dynamic_table.go: evictability check = (absolute index < Known Received Count) AND per-entry outstanding-reference count == 0. |
| `1-2.1-14` | informative | References made on the encoder stream (e.g., Duplicate, insert-with-name-reference) never preclude eviction, because encoder-stream instructions are processed in order before the evicting instruction. <br>_(sender [CLIENT])_ | references on the encoder stream never preclude the eviction of an entry, because those references are guaranteed to be processed before the instruction evicting the entry | qpack/encoder.go: outstanding-reference tracking must count only field-section references, not encoder-stream name references or Duplicate sources. |
| `1-2.1-15` | MUST NOT | The encoder MUST NOT insert an entry (including a duplicate of an existing entry) if making room would require evicting entries that are not evictable. <br>_(sender [CLIENT])_ | the encoder MUST NOT insert that entry into the dynamic table (including duplicates of existing entries) | qpack/encoder.go: before any Insert/Duplicate, verify all entries that would be evicted are evictable per 1-2.1-13; otherwise fall back to a literal representation. |
| `1-2.1-16` | informative | An encoder using the dynamic table must track every dynamic table entry referenced by each field section until the decoder acknowledges those representations. <br>_(sender [CLIENT])_ | an encoder that uses the dynamic table has to keep track of each dynamic table entry referenced by each field section until those representations are acknowledged by the decoder | qpack/encoder.go: per-stream/per-section reference sets retained until Section Acknowledgment or Stream Cancellation arrives on the peer decoder stream (read in http3/control.go). |
| `1-2.1-17` | informative | To avoid referencing entries close to eviction, the encoder can emit a Duplicate instruction and reference the fresh duplicate instead. <br>_(sender [CLIENT])_ | the encoder can emit a Duplicate instruction (Section 4.3.4) and reference the duplicate instead | qpack/encoder.go + qpack/instructions.go: if dynamic table implemented, check a Duplicate path exists (or that near-eviction entries are simply not referenced). |
| `1-2.1-18` | informative | Recommended heuristic: maintain a draining index — the smallest absolute index the encoder will still reference — and increase it as new entries are inserted so old entries become evictable. <br>_(sender [CLIENT])_ | the encoder can maintain a draining index, which is the smallest absolute index (Section 3.2.4) in the dynamic table that it will emit a reference for | qpack/encoder.go: optional heuristic; check whether some draining/threshold mechanism prevents the MUST NOT in 1-2.1-15 from permanently stalling insertions. |
| `1-2.1-19` | informative | For a field section that references the dynamic table, the Required Insert Count equals one plus the largest absolute index among all referenced dynamic table entries. <br>_(endpoint [CLIENT])_ | the Required Insert Count is one larger than the largest absolute index of all referenced dynamic table entries | qpack/encoder.go: compute RIC as max(referenced absolute indices)+1 when writing the field-section prefix; qpack/decoder.go: use decoded RIC for the blocked check. |
| `1-2.1-20` | informative | A field section with no dynamic table references has a Required Insert Count of zero. <br>_(endpoint [CLIENT])_ | For a field section encoded with no references to the dynamic table, the Required Insert Count is zero. | qpack/encoder.go: static-only/literal-only sections (the current default) must encode RIC=0 in the prefix; qpack/decoder.go must accept RIC=0 sections without any dynamic state. |
| `1-2.1-21` | informative | When a decoder receives an encoded field section whose Required Insert Count exceeds its own Insert Count, that stream is blocked and cannot be processed immediately. <br>_(receiver [CLIENT])_ | When the decoder receives an encoded field section with a Required Insert Count greater than its own Insert Count, the stream cannot be processed immediately and is considered "blocked" | qpack/decoder.go + http3/response.go: on RIC > local Insert Count the client must park the response stream (not error) until encoder-stream insertions catch up — verify blocked handling exists rather than an immediate decode failure. |
| `1-2.1-22` | informative | The decoder advertises the maximum number of streams it allows to be blocked via the SETTINGS_QPACK_BLOCKED_STREAMS setting. <br>_(receiver [CLIENT])_ | The decoder specifies an upper bound on the number of streams that can be blocked using the SETTINGS_QPACK_BLOCKED_STREAMS setting | http3/control.go: check what SETTINGS_QPACK_BLOCKED_STREAMS the client advertises in its SETTINGS frame and that qpack/decoder.go enforces exactly that value. |
| `1-2.1-23` | MUST | An encoder MUST at all times limit the number of streams that could become blocked to the peer's advertised SETTINGS_QPACK_BLOCKED_STREAMS value. <br>_(sender [CLIENT])_ | An encoder MUST limit the number of streams that could become blocked to the value of SETTINGS_QPACK_BLOCKED_STREAMS at all times. | qpack/encoder.go: before referencing an entry above Known Received Count, count streams with potentially-blocking sections against the server's SETTINGS_QPACK_BLOCKED_STREAMS (plumbed from http3/control.go SETTINGS parse); note the count is streams that COULD block, not streams actually blocked. |
| `1-2.1-24` | MUST | A decoder that encounters more blocked streams than it advertised MUST treat this as a connection error of type QPACK_DECOMPRESSION_FAILED. <br>_(receiver [CLIENT])_ | If a decoder encounters more blocked streams than it promised to support, it MUST treat this as a connection error of type QPACK_DECOMPRESSION_FAILED | qpack/decoder.go + qpack/errors.go: blocked-stream counter with hard limit; exceeding it must surface QPACK_DECOMPRESSION_FAILED as a CONNECTION error through http3 (closing the QUIC connection, not just the stream). |
| `1-2.1-25` | informative | The decoder might not actually block on every stream that risks blocking, so the encoder's limit applies to potentially blocked streams regardless of actual delivery order. <br>_(endpoint [CLIENT])_ | the decoder might not become blocked on every stream that risks becoming blocked | qpack/encoder.go: confirm the 1-2.1-23 accounting is based on RIC > Known Received Count at send time, not on any feedback about actual blocking. |
| `1-2.1-26` | SHOULD NOT | An encoder SHOULD NOT write an instruction unless sufficient stream and connection flow-control credit is available for the entire instruction, to avoid flow-control deadlocks. <br>_(sender [CLIENT])_ | an encoder SHOULD NOT write an instruction unless sufficient stream and connection flow-control credit is available for the entire instruction | http3/control.go encoder-stream writes via quic stream: check writes of a whole instruction are gated on available QUIC stream+connection flow-control credit (quic/ send path), or that instructions are buffered atomically; apply the same discipline to the client's decoder-stream instructions. |
| `1-2.1-27` | informative | The Known Received Count is the total number of dynamic table insertions and duplications acknowledged by the decoder. <br>_(endpoint [CLIENT])_ | The Known Received Count is the total number of dynamic table insertions and duplications acknowledged by the decoder. | qpack/encoder.go: maintain a Known Received Count counting insertions AND duplications; qpack/decoder.go maintains its mirror to compute Insert Count Increment values. |
| `1-2.1-28` | informative | The encoder tracks the Known Received Count to identify which dynamic table entries can be referenced without risking blocking a stream. <br>_(sender [CLIENT])_ | The encoder tracks the Known Received Count in order to identify which dynamic table entries can be referenced without potentially blocking a stream. | qpack/encoder.go: 'safe' (non-blocking) references are those with absolute index < Known Received Count; anything above consumes a blocked-stream slot (1-2.1-23). |
| `1-2.1-29` | informative | The decoder tracks the Known Received Count in order to send Insert Count Increment instructions. <br>_(receiver [CLIENT])_ | The decoder tracks the Known Received Count in order to be able to send Insert Count Increment instructions. | qpack/decoder.go: client-as-decoder must track what the server already knows it received, so ICI increments sent on the client's decoder stream (http3/control.go) are correct deltas. |
| `1-2.1-30` | informative | A Section Acknowledgment instruction implies the decoder has received all dynamic table state necessary to decode that field section. <br>_(endpoint [CLIENT])_ | A Section Acknowledgment instruction (Section 4.4.1) implies that the decoder has received all dynamic table state necessary to decode the field section. | qpack/decoder.go: only emit Section Acknowledgment after fully decoding the section; qpack/encoder.go: on receiving one, may treat all state up to that section's RIC as delivered. |
| `1-2.1-31` | informative | On receiving a Section Acknowledgment, if the acknowledged section's Required Insert Count exceeds the current Known Received Count, the Known Received Count is raised to that Required Insert Count. <br>_(sender [CLIENT])_ | If the Required Insert Count of the acknowledged field section is greater than the current Known Received Count, the Known Received Count is updated to that Required Insert Count value | qpack/encoder.go: Section Ack handler must do KRC = max(KRC, RIC of acked section) — requires remembering each in-flight section's RIC keyed by stream ID. |
| `1-2.1-32` | informative | An Insert Count Increment instruction increases the Known Received Count by its Increment parameter. <br>_(sender [CLIENT])_ | An Insert Count Increment instruction (Section 4.4.3) increases the Known Received Count by its Increment parameter. | qpack/encoder.go + qpack/instructions.go: on receiving ICI from the server's decoder stream, add Increment to KRC (bounds-check against Insert Count per Section 4.4.3 when auditing that section). |

### 9204 section 2.2-3 - RFC 9204 QPACK §2.2–§3: Decoder (blocked decoding, state synchronization, invalid references) and Reference Tables (static/dynamic table, capacity, eviction, indexing)

| ID | Level | Requirement (scope) | RFC 9204 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `2.2-3-1` | informative | Encoded field sections and encoder-stream instructions arrive on separate streams (unlike HPACK, where header blocks can carry table-modifying instructions). <br>_(endpoint [CLIENT])_ | encoded field sections and encoder stream instructions arrive on separate streams | http3/control.go — encoder-stream instructions must be consumed on the dedicated uni stream, independent of request-stream field sections; qpack/decoder.go must not expect table instructions inside field sections. |
| `2.2-3-2` | MUST | The decoder must emit field lines in the exact order their representations appear in the encoded field section. <br>_(receiver [CLIENT])_ | The decoder MUST emit field lines in the order their representations appear in the encoded field section. | qpack/decoder.go — verify field-section decode appends fields strictly in wire order, no reordering; http3/response.go preserves that order. |
| `2.2-3-3` | informative | If Required Insert Count <= decoder's Insert Count the field section is processed immediately; otherwise the stream on which it was received becomes blocked. <br>_(receiver [CLIENT])_ | When the Required Insert Count is less than or equal to the decoder's Insert Count, the field section can be processed immediately. | qpack/decoder.go + http3/response.go — check RIC-vs-InsertCount comparison; a client advertising max table capacity 0 must still handle nonzero RIC correctly (block or error, never mis-decode). |
| `2.2-3-4` | SHOULD | While a stream is blocked, its encoded field-section data should remain within the stream's flow-control window (releasing it prematurely enables memory-exhaustion attacks). <br>_(receiver [CLIENT])_ | While blocked, encoded field section data SHOULD remain in the blocked stream's flow-control window. | http3/response.go + quic flow control — if blocked decoding is supported, do not grant flow-control credit for buffered-but-unprocessed field-section bytes. |
| `2.2-3-5` | informative | A stream unblocks only when Insert Count >= Required Insert Count for ALL encoded field sections the decoder has started reading from that stream. <br>_(receiver [CLIENT])_ | A stream becomes unblocked when the Insert Count becomes greater than or equal to the Required Insert Count for all encoded field sections | http3/response.go — unblock condition must cover every section started on the stream (interim responses, headers, trailers), not just the first. |
| `2.2-3-6` | MUST | A Required Insert Count smaller than the lowest value with which the section can be decoded must be treated as a connection error of type QPACK_DECOMPRESSION_FAILED. <br>_(receiver [CLIENT])_ | If it encounters a Required Insert Count smaller than expected, it MUST treat this as a connection error of type QPACK_DECOMPRESSION_FAILED | qpack/decoder.go — recompute expected RIC (max referenced absolute index + 1, per §2.1.2) after decoding and reject smaller declared RIC with the errors.go decompression-failed code. |
| `2.2-3-7` | MAY | A Required Insert Count larger than expected may be treated as a connection error of type QPACK_DECOMPRESSION_FAILED. <br>_(receiver [CLIENT])_ | If it encounters a Required Insert Count larger than expected, it MAY treat this as a connection error of type QPACK_DECOMPRESSION_FAILED. | qpack/decoder.go — decide and document whether over-declared RIC is rejected; either choice is conformant but must be deliberate. |
| `2.2-3-8` | informative | The decoder signals section-completion, stream-abandonment, and insert-count events by emitting decoder instructions on the decoder stream. <br>_(receiver [CLIENT])_ | The decoder signals the following events by emitting decoder instructions (Section 4.4) on the decoder stream. | http3/control.go — client owns its decoder stream; Section Ack / Stream Cancellation / Insert Count Increment must be written there and nowhere else. |
| `2.2-3-9` | MUST | After decoding a field section that contains dynamic-table references, the decoder must emit a Section Acknowledgment instruction. <br>_(receiver [CLIENT])_ | After the decoder finishes decoding a field section encoded using representations containing dynamic table references, it MUST emit a Section Acknowledgment instruction | qpack/decoder.go + http3/response.go — after any section decoded with RIC > 0, emit Section Acknowledgment on the decoder stream; sections with only static/literal refs need no ack. |
| `2.2-3-10` | informative | Each Section Acknowledgment acknowledges the earliest unacknowledged field section containing dynamic-table references sent on that stream (streams may carry multiple sections: interim responses, trailers, pushes). <br>_(sender [CLIENT])_ | The encoder interprets each Section Acknowledgment instruction as acknowledging the earliest unacknowledged field section containing dynamic table references sent on the given stream. | qpack/encoder.go — when client uses dynamic-table encoding, keep a per-stream FIFO of unacked dynamic-referencing sections; each ack from the server pops the earliest. |
| `2.2-3-11` | informative | On receiving a stream reset before end-of-stream or before all field sections are processed, or when abandoning reading of a stream, the endpoint generates a Stream Cancellation instruction. <br>_(endpoint [CLIENT])_ | when it abandons reading of a stream, it generates a Stream Cancellation instruction | http3/request.go + response teardown — on RESET_STREAM/STOP_SENDING or early abandon, client must emit Stream Cancellation on its decoder stream (unless 2.2-3-12 applies). |
| `2.2-3-12` | MAY | A decoder that advertised a maximum dynamic table capacity of zero may omit sending Stream Cancellations, since the peer encoder can hold no dynamic references. <br>_(receiver [CLIENT])_ | A decoder with a maximum dynamic table capacity (Section 3.2.3) equal to zero MAY omit sending Stream Cancellations | http3/control.go — if client advertises SETTINGS_QPACK_MAX_TABLE_CAPACITY=0, skipping cancellations is legal; verify current behavior matches the advertised capacity. |
| `2.2-3-13` | informative | An encoder must not infer from a Stream Cancellation that any dynamic-table updates were received (it only releases references, unlike Section Ack/ICI). <br>_(sender [CLIENT])_ | An encoder cannot infer from this instruction that any updates to the dynamic table have been received. | qpack/encoder.go — Stream Cancellation handling must release references only; it must never advance Known Received Count. |
| `2.2-3-14` | informative | A dynamic table entry becomes evictable only when its absolute index is below the Known Received Count and it has zero outstanding references. <br>_(sender [CLIENT])_ | When an entry with an absolute index lower than the Known Received Count has zero references, then it is considered evictable | qpack/encoder.go + dynamic_table.go — evictability gate on the encoder side: refcount == 0 AND absoluteIndex < KnownReceivedCount; both conditions required. |
| `2.2-3-15` | informative | After receiving new entries on the encoder stream, the decoder chooses when to emit Insert Count Increment instructions (immediate = timeliest, delayed = coalescible or replaceable by Section Acks, too long = compression inefficiency). <br>_(receiver [CLIENT])_ | After receiving new table entries on the encoder stream, the decoder chooses when to emit Insert Count Increment instructions | qpack/decoder.go + http3/control.go — after processing server encoder-stream inserts, emit ICI (timing discretionary); N/A only if client advertises max capacity 0 so server never inserts. |
| `2.2-3-16` | MUST | A field-line representation referencing a dynamic entry that is already evicted, or whose absolute index >= the declared Required Insert Count, must be treated as connection error QPACK_DECOMPRESSION_FAILED. <br>_(receiver [CLIENT])_ | an absolute index greater than or equal to the declared Required Insert Count (Section 4.5.1), it MUST treat this as a connection error of type QPACK_DECOMPRESSION_FAILED | qpack/decoder.go — every dynamic reference in a representation must check both not-evicted AND abs < declared RIC; map to the decompression-failed code in errors.go. |
| `2.2-3-17` | MUST | An encoder instruction (e.g., Insert With Name Reference, Duplicate) referencing an already-evicted dynamic entry must be treated as connection error QPACK_ENCODER_STREAM_ERROR. <br>_(receiver [CLIENT])_ | a reference in an encoder instruction to a dynamic table entry that has already been evicted, it MUST treat this as a connection error of type QPACK_ENCODER_STREAM_ERROR. | qpack/decoder.go encoder-stream handler (instructions.go) — evicted-entry references in encoder instructions raise QPACK_ENCODER_STREAM_ERROR, a distinct errors.go code from the field-section path. |
| `2.2-3-18` | informative | QPACK static and dynamic table entries are addressed separately (no unified HPACK-style address space). <br>_(endpoint [CLIENT])_ | entries in the QPACK static and dynamic tables are addressed separately. | qpack/instructions.go — the T/S bit selects the table; verify no HPACK-style combined index arithmetic leaked in from the hpack package. |
| `2.2-3-19` | informative | The static table is a predefined list of field lines with fixed indices over time, defined in Appendix A. <br>_(endpoint [CLIENT])_ | The static table consists of a predefined list of field lines, each of which has a fixed index over time. | qpack/static_table.go — table must match RFC 9204 Appendix A exactly (entries 0-98), byte-for-byte names and values. |
| `2.2-3-20` | informative | All static table entries have a name and a value, but values can be empty (length 0). <br>_(endpoint [CLIENT])_ | values can be empty (that is, have a length of 0) | qpack/static_table.go — empty-value entries preserved; http3/validate.go must not reject empty field values coming from static-table matches. |
| `2.2-3-21` | informative | The QPACK static table is indexed from 0, unlike the HPACK static table which is indexed from 1. <br>_(endpoint [CLIENT])_ | the QPACK static table is indexed from 0, whereas the HPACK static table is indexed from 1. | qpack/static_table.go — zero-based lookup; audit for any off-by-one reuse of hpack's one-based static table (sibling-divergence risk). |
| `2.2-3-22` | MUST | An invalid static table index in a field line representation must be treated as connection error QPACK_DECOMPRESSION_FAILED. <br>_(receiver [CLIENT])_ | When the decoder encounters an invalid static table index in a field line representation, it MUST treat this as a connection error of type QPACK_DECOMPRESSION_FAILED. | qpack/decoder.go — static index bounds check (> 98) in representation decode raises the decompression-failed code. |
| `2.2-3-23` | MUST | An invalid static table index received on the encoder stream must be treated as connection error QPACK_ENCODER_STREAM_ERROR. <br>_(receiver [CLIENT])_ | If this index is received on the encoder stream, this MUST be treated as a connection error of type QPACK_ENCODER_STREAM_ERROR. | qpack/decoder.go encoder-stream path — same bounds check but must raise QPACK_ENCODER_STREAM_ERROR, not the field-section error code. |
| `2.2-3-24` | informative | The dynamic table is a FIFO list of field lines, initially empty and shared by encoder and decoder; the encoder adds entries and conveys them via encoder-stream instructions. <br>_(endpoint [CLIENT])_ | A QPACK encoder and decoder share a dynamic table that is initially empty. | qpack/dynamic_table.go — FIFO order, starts empty, mutated only via encoder-stream instructions (both directions: client encoder table and client's mirror of server table). |
| `2.2-3-25` | MUST NOT | The dynamic table can contain duplicate entries (same name and value); the decoder must not treat duplicates as an error. <br>_(receiver [CLIENT])_ | duplicate entries MUST NOT be treated as an error by the decoder. | qpack/dynamic_table.go — insert path accepts duplicate name/value pairs without error (Duplicate instruction produces exactly this). |
| `2.2-3-26` | informative | Dynamic table entries can have empty values. <br>_(endpoint [CLIENT])_ | Dynamic table entries can have empty values. | qpack/dynamic_table.go + http3/validate.go — empty-value dynamic entries must insert and decode without rejection. |
| `2.2-3-27` | informative | Dynamic table size is the sum of entry sizes; an entry's size is name length + value length + 32 bytes. <br>_(endpoint [CLIENT])_ | The size of an entry is the sum of its name's length in bytes, its value's length in bytes, and 32 additional bytes. | qpack/dynamic_table.go — size accounting must be nameLen+valueLen+32 per entry, summed for table size. |
| `2.2-3-28` | informative | Entry size is computed from the name and value lengths WITHOUT Huffman encoding applied (decoded byte lengths). <br>_(endpoint [CLIENT])_ | The size of an entry is calculated using the length of its name and value without Huffman encoding applied. | qpack/dynamic_table.go — verify size uses post-Huffman-decode (plain) lengths, never the compressed wire lengths. |
| `2.2-3-29` | informative | The initial capacity of the dynamic table is zero; capacity is set by the encoder as the upper limit on table size. <br>_(endpoint [CLIENT])_ | The initial capacity of the dynamic table is zero. | qpack/dynamic_table.go — capacity must start at 0 on both the client's own table and its mirror of the server's. |
| `2.2-3-30` | informative | To begin using the dynamic table, the encoder sends a Set Dynamic Table Capacity instruction with a non-zero capacity. <br>_(sender [CLIENT])_ | The encoder sends a Set Dynamic Table Capacity instruction (Section 4.3.1) with a non-zero capacity to begin using the dynamic table. | qpack/encoder.go — emit Set Dynamic Table Capacity on the encoder stream before the first insert; never insert while capacity is 0. |
| `2.2-3-31` | informative | Before adding a new entry, entries are evicted from the end of the table until table size <= (capacity - size of new entry); then the entry is added. <br>_(endpoint [CLIENT])_ | entries are evicted from the end of the dynamic table until the size of the dynamic table is less than or equal to (table capacity - size of new entry). | qpack/dynamic_table.go — insert path evicts from the FIFO tail (oldest) until size <= capacity - newEntrySize, before appending. |
| `2.2-3-32` | MUST NOT | The encoder must not cause a dynamic table entry to be evicted unless that entry is evictable (abs index < Known Received Count and zero references). <br>_(sender [CLIENT])_ | The encoder MUST NOT cause a dynamic table entry to be evicted unless that entry is evictable | qpack/encoder.go — before inserting, verify the entries eviction would remove are evictable per 2.2-3-14; otherwise encode the field as a literal instead of inserting. |
| `2.2-3-33` | MUST | It is an error for the encoder to attempt to add an entry larger than the dynamic table capacity; the decoder must treat this as connection error QPACK_ENCODER_STREAM_ERROR. <br>_(sender,receiver [CLIENT])_ | the encoder attempts to add an entry that is larger than the dynamic table capacity; the decoder MUST treat this as a connection error of type QPACK_ENCODER_STREAM_ERROR. | qpack/decoder.go — encoder-stream insert with entrySize > capacity raises QPACK_ENCODER_STREAM_ERROR; qpack/encoder.go must never attempt an oversize insert (fall back to literal). |
| `2.2-3-34` | informative | A new entry may reference an entry that gets evicted by its own insertion; implementations are cautioned not to delete the referenced name/value before completing the insert. <br>_(endpoint [CLIENT])_ | Implementations are cautioned to avoid deleting the referenced name or value if the referenced entry is evicted from the dynamic table prior to inserting the new entry. | qpack/dynamic_table.go — Insert With Name Reference / Duplicate must copy or retain the referenced name before eviction runs (use-after-evict / buffer-reuse hazard). |
| `2.2-3-35` | informative | When the encoder reduces the table capacity, entries are evicted from the end until size <= new capacity; setting capacity 0 completely clears the table (and it can later be restored). <br>_(endpoint [CLIENT])_ | entries are evicted from the end of the dynamic table until the size of the dynamic table is less than or equal to the new table capacity | qpack/dynamic_table.go — SetCapacity(smaller) triggers tail eviction; SetCapacity(0) empties the table; verify both encoder-side and decoder-mirror behavior. |
| `2.2-3-36` | informative | The decoder bounds encoder memory: in HTTP/3 the maximum dynamic table capacity is the value of SETTINGS_QPACK_MAX_TABLE_CAPACITY sent by the decoder. <br>_(endpoint [CLIENT])_ | this limit is determined by the value of SETTINGS_QPACK_MAX_TABLE_CAPACITY sent by the decoder | http3/control.go — the client's advertised SETTINGS_QPACK_MAX_TABLE_CAPACITY bounds the server's Set Capacity instructions; qpack/decoder.go must enforce it (exceeding is a §4.3.1 encoder-stream error). |
| `2.2-3-37` | MUST NOT | The encoder must not set a dynamic table capacity exceeding the decoder-advertised maximum, though it may use a lower capacity. <br>_(sender [CLIENT])_ | The encoder MUST NOT set a dynamic table capacity that exceeds this maximum, but it can choose to use a lower dynamic table capacity | qpack/encoder.go — clamp Set Dynamic Table Capacity to the server's advertised SETTINGS_QPACK_MAX_TABLE_CAPACITY (from http3/control.go SETTINGS processing). |
| `2.2-3-38` | informative | For clients using 0-RTT in HTTP/3, the server's maximum table capacity is the remembered SETTINGS value, or zero if never sent. <br>_(client,server [CLIENT])_ | the server's maximum table capacity is the remembered value of the setting or zero if the value was not previously sent. | N/A today — quic/http3 has no 0-RTT (declared non-goal); confirm no remembered-SETTINGS path exists that would need this rule. |
| `2.2-3-39` | MAY | When the client's remembered 0-RTT value of the setting is zero, the server may raise it to a non-zero value in its SETTINGS frame. <br>_(server [CLIENT])_ | When the client's 0-RTT value of the SETTING is zero, the server MAY set it to a non-zero value in its SETTINGS frame. | http3/control.go — client SETTINGS processing must accept the max-capacity value arriving/growing in the server's SETTINGS frame (0-RTT itself N/A). |
| `2.2-3-40` | MUST | If the remembered 0-RTT value is non-zero, the server must send the same non-zero value in its SETTINGS frame. <br>_(server [not-client])_ | If the remembered value is non-zero, the server MUST send the same non-zero value in its SETTINGS frame. | Server-side obligation; the client-side enforcement of a violation is fact 2.2-3-41. |
| `2.2-3-41` | MUST | If the server sends any other value than the remembered non-zero one, or omits SETTINGS_QPACK_MAX_TABLE_CAPACITY entirely, the (client) encoder must treat this as connection error QPACK_DECODER_STREAM_ERROR. <br>_(client,sender [CLIENT])_ | If it specifies any other value, or omits SETTINGS_QPACK_MAX_TABLE_CAPACITY from SETTINGS, the encoder must treat this as a connection error of type QPACK_DECODER_STREAM_ERROR. | **VERIFY** http3/control.go — only relevant if 0-RTT is ever added: validate remembered vs received SETTINGS_QPACK_MAX_TABLE_CAPACITY and raise QPACK_DECODER_STREAM_ERROR (errors.go) on mismatch/omission. -- Quote is verbatim (RFC 9204 Section 3.2.3, source line 566-569) and the role list is right, but the level is wrong. The RFC text uses a LOWERCASE "must" here: "...the encoder must treat this as a connection error of type QPACK_DECODER_STREAM_ERROR." Per the RFC 8174 boilerplate the key words are normative "when, and only when, they appear in all capitals", so this sentence carries no RFC 2119 MUST. Contrast the surrounding sentences in the same paragraph, which do use capitals ("the server MAY set it to a non-zero value", "the server MUST send the same non-zero value"). Fix: change level from "MUST" to "informative" (or annotate it as a lowercase/non-2119 "must", likely an editorial slip in the RFC), and do not treat it as a hard-MUST row in the conformance matrix. The requirement text and reconcile note are otherwise accurate. \|\| Level is wrong. The quote is verbatim (RFC 9204 Section 3.2.3, source lines 566-569) and the roles are right, but the source reads lowercase 'the encoder must treat this as a connection error of type QPACK_DECODER_STREAM_ERROR' -- not 'MUST'. RFC 9204 Section 1.1 (source lines 145-149) carries the BCP 14 / RFC 8174 boilerplate: the key words are to be interpreted as described in BCP 14 'when, and only when, they appear in all capitals, as shown here.' A lowercase 'must' therefore is NOT a normative RFC 2119 keyword in this document. Fix: change level from 'MUST' to 'informative' (or annotate as 'lowercase must -- non-normative per Section 1.1'), and note in the requirement text that the obligation is stated in lowercase so it cannot be cited as a hard MUST in a conformance gate. Note also that the fact's own requirement wording says 'must treat', matching the source, while the level field over-states it. The reconcile note (0-RTT is a declared non-goal, so this is latent) is otherwise fine. |
| `2.2-3-42` | informative | For clients not using 0-RTT (and all servers), the maximum table capacity is 0 until the encoder processes a SETTINGS frame with a non-zero SETTINGS_QPACK_MAX_TABLE_CAPACITY. <br>_(sender [CLIENT])_ | the maximum table capacity is 0 until the encoder processes a SETTINGS frame with a non-zero value of SETTINGS_QPACK_MAX_TABLE_CAPACITY. | qpack/encoder.go + http3/control.go — client encoder must behave as capacity-0 (literal-only, no instructions) until the server's SETTINGS with a non-zero value is actually processed. |
| `2.2-3-43` | MUST NOT | While the maximum table capacity is zero, the encoder must not insert entries into the dynamic table. <br>_(sender [CLIENT])_ | the encoder MUST NOT insert entries into the dynamic table | qpack/encoder.go — no dynamic inserts while effective max capacity is 0 (before server SETTINGS, or when server advertises 0). |
| `2.2-3-44` | MUST NOT | While the maximum table capacity is zero, the encoder must not send ANY encoder instructions on the encoder stream (not even Set Dynamic Table Capacity 0). <br>_(sender [CLIENT])_ | MUST NOT send any encoder instructions on the encoder stream. | http3/control.go + qpack/encoder.go — the client encoder stream may carry only the unidirectional stream-type byte (stream framing, not an instruction) while server max capacity is 0/unknown; verify nothing else is written. |
| `2.2-3-45` | informative | Each dynamic entry has an absolute index fixed for its lifetime; the first inserted entry has absolute index 0 and indices increase by one per insertion. <br>_(endpoint [CLIENT])_ | The first entry inserted has an absolute index of 0; indices increase by one with each insertion. | qpack/dynamic_table.go — monotonic absolute-index counter starting at 0, never reset or reused across evictions. |
| `2.2-3-46` | informative | Relative indices begin at zero and increase in the opposite direction from absolute indices; which entry is relative 0 depends on the reference context. <br>_(endpoint [CLIENT])_ | Relative indices begin at zero and increase in the opposite direction from the absolute index. | qpack/instructions.go + decoder.go — two distinct relative-index conversions must exist (encoder-stream context vs field-section context); confirm they are not conflated. |
| `2.2-3-47` | informative | In encoder instructions, relative index 0 refers to the most recently inserted dynamic table entry (i.e., absolute = InsertCount - 1 - relative, evaluated at instruction time). <br>_(endpoint [CLIENT])_ | a relative index of 0 refers to the most recently inserted value in the dynamic table. | qpack/decoder.go encoder-stream handler + encoder.go — abs = insertCount - 1 - rel for Insert With Name Reference / Duplicate on the encoder stream. |
| `2.2-3-48` | informative | The entry referenced by a given encoder-stream relative index changes as instructions are interpreted, so each instruction's index must be resolved against the table state at that instruction. <br>_(endpoint [CLIENT])_ | the entry referenced by a given relative index will change while interpreting instructions on the encoder stream. | qpack/decoder.go — resolve each encoder-stream instruction's relative index against the CURRENT insert count when that instruction is processed, not a batch-start snapshot. |
| `2.2-3-49` | informative | Relative indices in field line representations are relative to the Base declared at the start of the encoded field section, keeping references stable under out-of-order processing. <br>_(receiver [CLIENT])_ | relative indices in field line representations are relative to the Base at the beginning of the encoded field section | qpack/decoder.go — representation relative indices use the section-prefix Base, not the live insert count. |
| `2.2-3-50` | informative | In a field line representation, relative index 0 refers to the entry with absolute index Base - 1 (i.e., absolute = Base - 1 - relative). <br>_(endpoint [CLIENT])_ | In a field line representation, a relative index of 0 refers to the entry with absolute index equal to Base - 1. | qpack/decoder.go — abs = Base - 1 - rel for indexed/name-ref representations; qpack/encoder.go emits the inverse. |
| `2.2-3-51` | informative | Post-Base indices address entries with absolute index >= Base, starting at 0 for the entry at absolute index Base and increasing in the same direction as absolute indices (absolute = Base + postBase). <br>_(endpoint [CLIENT])_ | starting at 0 for the entry with absolute index equal to Base and increasing in the same direction as the absolute index | qpack/decoder.go — abs = Base + postBaseIndex for the post-base representation forms; verify the direction is opposite to relative indexing. |
| `2.2-3-52` | informative | Post-Base indices exist so an encoder can process a field section in a single pass while referencing entries added during that (or another) section's processing. <br>_(sender [CLIENT])_ | Post-Base indices allow an encoder to process a field section in a single pass and include references to entries added while processing this (or other) field sections. | qpack/encoder.go — if the client encoder inserts entries mid-section, references to those entries must use post-base representation forms (or the encoder must pre-insert before choosing Base). |

### 9204 section 4.1-4.4 - QPACK Wire Format: Primitives, Encoder/Decoder Streams, Encoder Instructions, Decoder Instructions

| ID | Level | Requirement (scope) | RFC 9204 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `4.1-4.4-1` | informative | QPACK reuses the HPACK prefixed-integer encoding from RFC 7541 Section 5.1 without modification (though QPACK uses prefix sizes HPACK does not). <br>_(all [CLIENT])_ | The prefixed integer from Section 5.1 of [RFC7541] is used heavily throughout this document. The format from [RFC7541] is used unmodified. | qpack/instructions.go + qpack/encoder.go/decoder.go: verify prefix-int codec matches RFC 7541 §5.1 and supports the extra QPACK prefix sizes (3,5,6,7,8 bits). |
| `4.1-4.4-2` | MUST | Implementations must be able to decode prefixed integers up to and including 62 bits long. <br>_(endpoint [CLIENT])_ | QPACK implementations MUST be able to decode integers up to and including 62 bits long. | qpack/decoder.go / instructions.go: check the prefix-int reader accepts 62-bit values (fits uint64) without overflow and bounds continuation bytes; test max value 2^62-1. |
| `4.1-4.4-3` | informative | QPACK reuses the RFC 7541 Section 5.2 string literal, which includes optional Huffman encoding. <br>_(all [CLIENT])_ | The string literal defined by Section 5.2 of [RFC7541] is also used throughout. This string format includes optional Huffman encoding. | qpack/decoder.go: client-as-decoder must handle both H=0 and H=1 strings from servers; verify Huffman decode is implemented (encoder may choose H=0 only). |
| `4.1-4.4-4` | informative | A standard string literal is a 1-bit H flag, the string length as a 7-bit prefix integer, then that many bytes of data. <br>_(all [CLIENT])_ | begin with a single bit flag, denoted as 'H' in this document (indicating whether the string is Huffman encoded), followed by the string length encoded as a 7-bit prefix integer | qpack/instructions.go: verify string-literal read/write uses H flag + 7-bit-prefix length + exact byte count for 8-bit-prefix strings. |
| `4.1-4.4-5` | informative | When Huffman encoding is used, the RFC 7541 Appendix B Huffman table applies unmodified, and the declared length is the encoded (post-Huffman) size. <br>_(all [CLIENT])_ | the Huffman table from Appendix B of [RFC7541] is used without modification and the indicated length is the size of the string after encoding. | qpack/decoder.go: Huffman decode must use the HPACK Appendix B table (can reuse hpack package's table) and treat the length prefix as encoded-byte count, not decoded length. |
| `4.1-4.4-6` | informative | An N-bit prefix string literal begins mid-byte, with the first (8-N) bits of its starting byte belonging to the previous field. <br>_(all [CLIENT])_ | An "N-bit prefix string literal" begins mid-byte, with the first (8-N) bits allocated to a previous field. | qpack/instructions.go: verify mid-byte string parsing for the 6-bit-prefix name string in Insert with Literal Name (0x01 pattern occupies top 2 bits). |
| `4.1-4.4-7` | informative | Within an N-bit prefix string literal, one bit is the Huffman flag and the encoded-string length follows as an (N-1)-bit prefix integer; the rest is unchanged. <br>_(all [CLIENT])_ | The string uses one bit for the Huffman flag, followed by the length of the encoded string as a (N-1)-bit prefix integer. | qpack/instructions.go: for a 6-bit prefix string the H flag is bit 5 and length is a 5-bit prefix integer; check the arithmetic (N vs N-1) is not off by one. |
| `4.1-4.4-8` | informative | The string-literal prefix size N ranges from 2 to 8 inclusive. <br>_(all [CLIENT])_ | The prefix size, N, can have a value between 2 and 8, inclusive. | qpack/instructions.go: string codec helper should support prefix sizes 2..8 (QPACK uses 4, 6, and 8 in practice). |
| `4.1-4.4-9` | informative | A string literal with no stated prefix size is an 8-bit prefix string literal, exactly as in RFC 7541. <br>_(all [CLIENT])_ | A string literal without a prefix length noted is an 8-bit prefix string literal and follows the definitions in [RFC7541] without modification. | qpack/instructions.go: value strings in encoder instructions and field lines default to 8-bit prefix (H + 7-bit length); confirm defaults. |
| `4.1-4.4-10` | informative | The encoder stream is unidirectional stream type 0x02 carrying an unframed sequence of encoder instructions from encoder to decoder. <br>_(all [CLIENT])_ | An encoder stream is a unidirectional stream of type 0x02. It carries an unframed sequence of encoder instructions from encoder to decoder. | http3/control.go: verify the client opens its encoder stream with type varint 0x02 and writes raw instructions with no HTTP/3 framing; and demuxes the server's 0x02 stream to qpack/decoder.go. |
| `4.1-4.4-11` | informative | The decoder stream is unidirectional stream type 0x03 carrying an unframed sequence of decoder instructions from decoder to encoder. <br>_(all [CLIENT])_ | A decoder stream is a unidirectional stream of type 0x03. It carries an unframed sequence of decoder instructions from decoder to encoder. | http3/control.go: verify the client opens its decoder stream with type varint 0x03 (unframed) and routes the server's 0x03 stream to qpack/encoder.go for ack/cancel/ICI processing. |
| `4.1-4.4-12` | MUST | Each endpoint must initiate at most one encoder stream and at most one decoder stream. <br>_(endpoint [CLIENT])_ | Each endpoint MUST initiate, at most, one encoder stream and, at most, one decoder stream. | http3/control.go: ensure the client opens exactly one encoder and one decoder stream per connection (no re-open on error/reconnect paths). |
| `4.1-4.4-13` | MUST | Receiving a second encoder stream or a second decoder stream is a connection error of type H3_STREAM_CREATION_ERROR. <br>_(receiver [CLIENT])_ | Receipt of a second instance of either stream type MUST be treated as a connection error of type H3_STREAM_CREATION_ERROR. | http3/control.go: unidirectional-stream demux must track that a 0x02 and a 0x03 stream were already seen and close the connection with H3_STREAM_CREATION_ERROR on a duplicate. |
| `4.1-4.4-14` | MUST NOT | The sender must never close (FIN) its encoder or decoder stream. <br>_(sender [CLIENT])_ | The sender MUST NOT close either of these streams | http3/control.go: verify the client never FINs or resets its own encoder/decoder streams for the connection lifetime, including on graceful shutdown before connection close. |
| `4.1-4.4-15` | MUST NOT | The receiver must never request that the sender close the encoder or decoder stream (e.g., via STOP_SENDING). <br>_(receiver [CLIENT])_ | the receiver MUST NOT request that the sender close either of these streams. | http3/control.go + quic stream API: ensure the client never sends STOP_SENDING on the server's encoder/decoder streams (check any generic abandon-unidirectional-stream path excludes 0x02/0x03). |
| `4.1-4.4-16` | MUST | Closure of either the encoder or decoder unidirectional stream is a connection error of type H3_CLOSED_CRITICAL_STREAM. <br>_(receiver [CLIENT])_ | Closure of either unidirectional stream type MUST be treated as a connection error of type H3_CLOSED_CRITICAL_STREAM. | http3/control.go: reader loops for the server's encoder/decoder streams must convert FIN or RESET_STREAM into connection error H3_CLOSED_CRITICAL_STREAM (same treatment as the control stream). |
| `4.1-4.4-17` | MAY | An endpoint may skip creating an encoder stream if it will not be used (e.g., no dynamic-table use or peer permits zero capacity). <br>_(endpoint [CLIENT])_ | An endpoint MAY avoid creating an encoder stream if it will not be used | http3/control.go: optional; note whether the client always opens the encoder stream (fine) or conditionally skips it when not using the dynamic table. |
| `4.1-4.4-18` | MAY | An endpoint may skip creating a decoder stream if its decoder sets the dynamic table maximum capacity to zero. <br>_(endpoint [CLIENT])_ | An endpoint MAY avoid creating a decoder stream if its decoder sets the maximum capacity of the dynamic table to zero. | http3/control.go: optional; if the client advertises SETTINGS_QPACK_MAX_TABLE_CAPACITY=0 it may omit the decoder stream — verify current behavior is deliberate either way. |
| `4.1-4.4-19` | MUST | An endpoint must accept the peer's encoder and decoder streams even when the connection's settings prevent their use. <br>_(endpoint [CLIENT])_ | An endpoint MUST allow its peer to create an encoder stream and a decoder stream even if the connection's settings prevent their use. | http3/control.go: even if the client advertises max table capacity 0, incoming 0x02/0x03 streams from the server must be accepted (and their instructions validated), not rejected as unknown. |
| `4.1-4.4-20` | informative | Encoder instructions on the encoder stream set the dynamic table capacity and add entries; names may be sent by static/dynamic reference or literal, and whole entries may be duplicated. <br>_(sender [CLIENT])_ | An encoder sends encoder instructions on the encoder stream to set the capacity of the dynamic table and add dynamic table entries. | qpack/instructions.go + encoder.go: encoder-instruction set is exactly {Set Capacity, Insert w/ Name Ref, Insert w/ Literal Name, Duplicate}; client-as-decoder must parse all four from the server's encoder stream. |
| `4.1-4.4-21` | informative | Set Dynamic Table Capacity is encoded as the 3-bit pattern '001' followed by the new capacity as a 5-bit prefix integer. <br>_(all [CLIENT])_ | an instruction that starts with the '001' 3-bit pattern. This is followed by the new dynamic table capacity represented as an integer with a 5-bit prefix | qpack/instructions.go: verify opcode dispatch on top bits 001 (0x20 mask) and 5-bit prefix integer for capacity on both emit (encoder.go) and parse (decoder.go) paths. |
| `4.1-4.4-22` | MUST | The new dynamic table capacity the encoder sets must not exceed the limit the decoder advertised (Section 3.2.3). <br>_(sender [CLIENT])_ | The new capacity MUST be lower than or equal to the limit described in Section 3.2.3. | qpack/encoder.go: client-as-encoder must clamp any Set Capacity it sends to the server-advertised SETTINGS_QPACK_MAX_TABLE_CAPACITY (plumbed from http3 SETTINGS handling). |
| `4.1-4.4-23` | informative | In HTTP/3 the capacity limit is the SETTINGS_QPACK_MAX_TABLE_CAPACITY value received from the decoder. <br>_(all [CLIENT])_ | In HTTP/3, this limit is the value of the SETTINGS_QPACK_MAX_TABLE_CAPACITY parameter (Section 5) received from the decoder. | http3/control.go: verify SETTINGS_QPACK_MAX_TABLE_CAPACITY is both sent (client's decoder limit) and consumed (server's limit gates client encoder) and wired into qpack. |
| `4.1-4.4-24` | MUST | A decoder receiving a Set Capacity value exceeding its advertised limit must treat it as a connection error of type QPACK_ENCODER_STREAM_ERROR. <br>_(receiver [CLIENT])_ | The decoder MUST treat a new dynamic table capacity value that exceeds this limit as a connection error of type QPACK_ENCODER_STREAM_ERROR. | qpack/decoder.go + errors.go: client-as-decoder must compare incoming Set Capacity against its own advertised SETTINGS_QPACK_MAX_TABLE_CAPACITY and raise QPACK_ENCODER_STREAM_ERROR (0x201) as a connection error. |
| `4.1-4.4-25` | MUST NOT | Capacity reduction may evict entries, but must never evict entries that are not evictable (entries with outstanding references, Section 2.1.1). <br>_(endpoint [CLIENT])_ | This MUST NOT cause the eviction of entries that are not evictable | qpack/dynamic_table.go: on capacity reduction, eviction must stop at entries still referenced by unacknowledged field sections (encoder side) — check the evictable/ref-count gate. |
| `4.1-4.4-26` | informative | A Set Dynamic Table Capacity instruction is not acknowledged, since it inserts no entry. <br>_(all [CLIENT])_ | Changing the capacity of the dynamic table is not acknowledged as this instruction does not insert an entry. | qpack/encoder.go + decoder.go: Set Capacity must not increment the Insert Count or Known Received Count on either side. |
| `4.1-4.4-27` | informative | Insert with Name Reference starts with the 1-bit pattern '1'; the second bit T selects static vs dynamic table, then a 6-bit prefix integer locates the name. <br>_(all [CLIENT])_ | an instruction that starts with the '1' 1-bit pattern. The second ('T') bit indicates whether the reference is to the static or dynamic table. | qpack/instructions.go: verify opcode 1TXXXXXX with 6-bit prefix name index on both emit and parse; client-as-decoder must parse this from the server's encoder stream. |
| `4.1-4.4-28` | informative | In Insert with Name Reference, T=1 means the index is a static table index; T=0 means it is a relative index into the dynamic table. <br>_(all [CLIENT])_ | When T=1, the number represents the static table index; when T=0, the number is the relative index of the entry in the dynamic table. | qpack/instructions.go + dynamic_table.go: on the encoder stream, T=0 relative index is relative to the most recently inserted entry (encoder-stream convention, no Base); verify the index-to-absolute conversion. |
| `4.1-4.4-29` | informative | The name reference in Insert with Name Reference is followed by the field value as a string literal (H flag + 7-bit prefix length). <br>_(all [CLIENT])_ | The field name reference is followed by the field value represented as a string literal | qpack/instructions.go: value is an 8-bit prefix string literal (H + Value Length (7+) + bytes) per Figure 6; check parse and emit. |
| `4.1-4.4-30` | informative | Insert with Literal Name starts with the 2-bit pattern '01' and carries both name and value as string literals. <br>_(all [CLIENT])_ | both the field name and the field value are represented as string literals using an instruction that starts with the '01' 2-bit pattern. | qpack/instructions.go: verify opcode 01HXXXXX dispatch (checked after the '1' pattern) on both emit and parse paths. |
| `4.1-4.4-31` | informative | In Insert with Literal Name, the name is a 6-bit prefix string literal (H + Name Length (5+)) and the value is an 8-bit prefix string literal. <br>_(all [CLIENT])_ | This is followed by the name represented as a 6-bit prefix string literal and the value represented as an 8-bit prefix string literal | qpack/instructions.go: name string uses H at bit 5 with a 5-bit length prefix (mid-byte string), value uses H + 7-bit length; confirm the asymmetric prefixes per Figure 7. |
| `4.1-4.4-32` | informative | Duplicate starts with the 3-bit pattern '000' followed by the relative index of the existing entry as a 5-bit prefix integer. <br>_(all [CLIENT])_ | an instruction that starts with the '000' 3-bit pattern. This is followed by the relative index of the existing entry represented as an integer with a 5-bit prefix | qpack/instructions.go: verify opcode 000XXXXX with 5-bit prefix relative index; client-as-decoder must handle Duplicate arriving on the server's encoder stream. |
| `4.1-4.4-33` | informative | Duplicate reinserts the referenced existing entry into the dynamic table without retransmitting the name or value. <br>_(all [CLIENT])_ | The existing entry is reinserted into the dynamic table without resending either the name or the value. | qpack/dynamic_table.go: Duplicate creates a new entry (new absolute index, increments Insert Count, counts toward capacity/eviction) copying the referenced entry's name and value. |
| `4.1-4.4-34` | informative | Decoder instructions travel on the decoder stream to inform the encoder about field-section processing and table updates, keeping the dynamic table consistent. <br>_(sender [CLIENT])_ | A decoder sends decoder instructions on the decoder stream to inform the encoder about the processing of field sections and table updates to ensure consistency of the dynamic table. | qpack/decoder.go + http3/control.go: decoder-instruction set is exactly {Section Acknowledgment, Stream Cancellation, Insert Count Increment}; client emits these on its decoder stream and its encoder parses the server's. |
| `4.1-4.4-35` | informative | After processing an encoded field section whose declared Required Insert Count is non-zero, the decoder emits a Section Acknowledgment (and only then). <br>_(receiver [CLIENT])_ | After processing an encoded field section whose declared Required Insert Count is not zero, the decoder emits a Section Acknowledgment instruction. | qpack/decoder.go + http3/response.go: client must send Section Acknowledgment after decoding each response field section with RIC != 0, and must NOT ack sections with RIC == 0. |
| `4.1-4.4-36` | informative | Section Acknowledgment is encoded as the 1-bit pattern '1' followed by the field section's stream ID as a 7-bit prefix integer. <br>_(all [CLIENT])_ | The instruction starts with the '1' 1-bit pattern, followed by the field section's associated stream ID encoded as a 7-bit prefix integer | qpack/instructions.go: verify decoder-instruction opcode 1XXXXXXX with 7-bit prefix stream ID on emit (client decoder side) and parse (client encoder side). |
| `4.1-4.4-37` | MUST | An encoder receiving a Section Acknowledgment for a stream with no outstanding unacknowledged non-zero-RIC field sections must treat it as a connection error of type QPACK_DECODER_STREAM_ERROR. <br>_(receiver [CLIENT])_ | every encoded field section with a non-zero Required Insert Count has already been acknowledged, this MUST be treated as a connection error of type QPACK_DECODER_STREAM_ERROR | qpack/encoder.go + errors.go: client-as-encoder must track per-stream outstanding sections it sent with RIC>0 and raise QPACK_DECODER_STREAM_ERROR (0x202) on an unexpected/duplicate Section Ack from the server. |
| `4.1-4.4-38` | informative | A Section Acknowledgment may increase the encoder's Known Received Count (Section 2.1.4). <br>_(receiver [CLIENT])_ | The Section Acknowledgment instruction might increase the Known Received Count | qpack/encoder.go: on Section Ack, advance Known Received Count to at least the acked section's Required Insert Count (frees entries for eviction). |
| `4.1-4.4-39` | informative | When a stream is reset or reading is abandoned, the decoder emits a Stream Cancellation instruction. <br>_(receiver [CLIENT])_ | When a stream is reset or reading is abandoned, the decoder emits a Stream Cancellation instruction. | http3/request.go + qpack/decoder.go: on client-initiated cancel, server RESET_STREAM, or abandoning a response body, the client must send Stream Cancellation on its decoder stream — check every cancel/teardown path. |
| `4.1-4.4-40` | informative | Stream Cancellation is encoded as the 2-bit pattern '01' followed by the affected stream ID as a 6-bit prefix integer. <br>_(all [CLIENT])_ | The instruction starts with the '01' 2-bit pattern, followed by the stream ID of the affected stream encoded as a 6-bit prefix integer. | qpack/instructions.go: verify decoder-instruction opcode 01XXXXXX with 6-bit prefix stream ID; client-as-encoder must also parse this from the server's decoder stream and release tracked references for that stream. |
| `4.1-4.4-41` | informative | Insert Count Increment is encoded as the 2-bit pattern '00' followed by the Increment as a 6-bit prefix integer. <br>_(all [CLIENT])_ | The Insert Count Increment instruction starts with the '00' 2-bit pattern, followed by the Increment encoded as a 6-bit prefix integer. | qpack/instructions.go: verify decoder-instruction opcode 00XXXXXX with 6-bit prefix Increment on emit (client decoder side) and parse (client encoder side). |
| `4.1-4.4-42` | informative | Insert Count Increment increases the encoder's Known Received Count by the Increment value. <br>_(receiver [CLIENT])_ | This instruction increases the Known Received Count (Section 2.1.4) by the value of the Increment parameter. | qpack/encoder.go: on ICI, add Increment to Known Received Count (additive, not absolute) — a common implementation bug is treating the value as an absolute count. |
| `4.1-4.4-43` | informative | The decoder should (lowercase, non-2119 guidance) send an Increment that raises the Known Received Count to the total insertions and duplications it has processed so far. <br>_(receiver [CLIENT])_ | The decoder should send an Increment value that increases the Known Received Count to the total number of dynamic table insertions and duplications processed so far. | qpack/decoder.go: when processing server encoder-stream insertions/duplicates not implied by Section Acks, send ICI covering the delta (KRC target = total inserts + duplicates processed). |
| `4.1-4.4-44` | MUST | An encoder receiving an Insert Count Increment with an Increment field equal to zero must treat it as a connection error of type QPACK_DECODER_STREAM_ERROR. <br>_(receiver [CLIENT])_ | An encoder that receives an Increment field equal to zero, or one that increases the Known Received Count beyond what the encoder has sent, MUST treat this | qpack/encoder.go + errors.go: client-as-encoder must reject Increment == 0 with connection error QPACK_DECODER_STREAM_ERROR (0x202). |
| `4.1-4.4-45` | MUST | An encoder receiving an Insert Count Increment that would push the Known Received Count beyond the number of insertions it has actually sent must treat it as a connection error of type QPACK_DECODER_STREAM_ERROR. <br>_(receiver [CLIENT])_ | one that increases the Known Received Count beyond what the encoder has sent, MUST treat this as a connection error of type QPACK_DECODER_STREAM_ERROR | qpack/encoder.go: validate KnownReceivedCount + Increment <= InsertCount(sent) before applying; overflow (including uint64 wrap) raises QPACK_DECODER_STREAM_ERROR. |

### 9204 section 4.5 - Field Line Representations

| ID | Level | Requirement (scope) | RFC 9204 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `4.5-1` | informative | An encoded field section is structured as a prefix followed by a possibly empty sequence of field line representations, each corresponding to exactly one field line. <br>_(all [CLIENT])_ | An encoded field section consists of a prefix and a possibly empty sequence of representations defined in this section. | qpack/decoder.go: response field-section parse must read the two-integer prefix first, then loop over zero-or-more representations; empty representation list must be accepted. |
| `4.5-2` | informative | Field line representations reference the static or dynamic table in a given state but never modify table state; only encoder-stream instructions mutate the table. <br>_(all [CLIENT])_ | These representations reference the static table or the dynamic table in a particular state, but they do not modify that state. | qpack/decoder.go: verify decoding an indexed/literal field line never inserts, duplicates, or evicts dynamic-table entries (mutation only via encoder-stream path in instructions.go/dynamic_table.go). |
| `4.5-3` | informative | Encoded field sections are carried in frames on streams defined by the enclosing protocol (HTTP/3 HEADERS frames), not on the QPACK unidirectional streams. <br>_(all [CLIENT])_ | Encoded field sections are carried in frames on streams defined by the enclosing protocol. | http3/request.go and http3/response.go: field sections only in HEADERS frames on request streams; http3/control.go keeps encoder/decoder instruction traffic on the dedicated unidirectional streams. |
| `4.5-4` | informative | The first prefix integer is the Required Insert Count, encoded as a prefixed integer with an 8-bit prefix. <br>_(all [CLIENT])_ | The Required Insert Count is encoded as an integer with an 8-bit prefix using the encoding described in Section 4.5.1.1. | qpack/encoder.go must emit and qpack/decoder.go must parse Required Insert Count as an 8-bit-prefix integer as the first byte(s) of every field section. |
| `4.5-5` | informative | The second prefix field is the Base, encoded as a 1-bit Sign ('S') followed by a Delta Base value as a prefixed integer with a 7-bit prefix. <br>_(all [CLIENT])_ | The Base is encoded as a Sign bit ('S') and a Delta Base value with a 7-bit prefix | qpack/encoder.go emits S + 7-bit-prefix Delta Base immediately after Required Insert Count; qpack/decoder.go parses the same layout before any representations. |
| `4.5-6` | informative | Required Insert Count identifies the dynamic-table state needed to process the field section; a blocking decoder uses it to decide when processing the section is safe. <br>_(receiver [CLIENT])_ | Blocking decoders use the Required Insert Count to determine when it is safe to process the rest of the field section. | qpack/decoder.go (client decodes responses): if dynamic table is supported, check whether decoding blocks until insert count >= Required Insert Count, or whether the client only operates non-blocking (RIC always satisfiable). |
| `4.5-7` | informative | Before encoding, the encoder transforms Required Insert Count: 0 stays 0; otherwise EncInsertCount = (ReqInsertCount mod (2 * MaxEntries)) + 1. <br>_(sender [CLIENT])_ | if ReqInsertCount == 0: EncInsertCount = 0 else: EncInsertCount = (ReqInsertCount mod (2 * MaxEntries)) + 1 | qpack/encoder.go (request encoding): if the encoder never references the dynamic table it must emit literal 0; if it uses the dynamic table, verify the modulo-plus-one transform is implemented exactly. |
| `4.5-8` | informative | MaxEntries, the maximum number of dynamic-table entries, is floor(MaxTableCapacity / 32) because the smallest possible entry (empty name and value) has size 32. <br>_(all [CLIENT])_ | MaxEntries = floor( MaxTableCapacity / 32 ) | qpack/dynamic_table.go: MaxEntries derivation must divide by 32 (the fixed per-entry overhead), using MaxTableCapacity, not the current negotiated capacity. |
| `4.5-9` | informative | MaxTableCapacity used in the MaxEntries calculation is the maximum dynamic-table capacity specified by the decoder (via its SETTINGS), not the encoder's chosen capacity. <br>_(all [CLIENT])_ | MaxTableCapacity is the maximum capacity of the dynamic table as specified by the decoder | http3/control.go + qpack/dynamic_table.go: client-as-decoder advertises SETTINGS_QPACK_MAX_TABLE_CAPACITY and must use that value for MaxEntries when decoding responses; client-as-encoder uses the server-advertised value when encoding requests. |
| `4.5-10` | MUST | A decoder that encounters an EncodedInsertCount value that no conformant encoder could have produced must treat it as a connection error QPACK_DECOMPRESSION_FAILED. <br>_(receiver [CLIENT])_ | If the decoder encounters a value of EncodedInsertCount that could not have been produced by a conformant encoder, it MUST treat this as a connection error of type QPACK_DECOMPRESSION_FAILED | qpack/decoder.go + qpack/errors.go: every Error branch of the RIC reconstruction algorithm must map to QPACK_DECOMPRESSION_FAILED and http3 must close the connection with that code (not a stream error). |
| `4.5-11` | informative | In RIC reconstruction, FullRange = 2 * MaxEntries; EncodedInsertCount of 0 decodes to 0; a nonzero EncodedInsertCount greater than FullRange is an error. <br>_(receiver [CLIENT])_ | if EncodedInsertCount > FullRange: Error | qpack/decoder.go: check the FullRange = 2*MaxEntries bound is enforced before wrap arithmetic; with a zero-capacity table (MaxEntries=0) any nonzero EncodedInsertCount must fail with QPACK_DECOMPRESSION_FAILED. |
| `4.5-12` | informative | RIC reconstruction computes MaxValue = TotalNumberOfInserts + MaxEntries, MaxWrapped = floor(MaxValue / FullRange) * FullRange, then candidate ReqInsertCount = MaxWrapped + EncodedInsertCount - 1. <br>_(receiver [CLIENT])_ | MaxWrapped = floor(MaxValue / FullRange) * FullRange ReqInsertCount = MaxWrapped + EncodedInsertCount - 1 | qpack/decoder.go: verify the unwrap arithmetic uses the decoder's TotalNumberOfInserts (total inserts ever, not current entry count) and matches this formula exactly. |
| `4.5-13` | informative | If the candidate ReqInsertCount exceeds MaxValue, it is an error unless ReqInsertCount > FullRange, in which case the value wrapped one fewer time and FullRange is subtracted. <br>_(receiver [CLIENT])_ | if ReqInsertCount > MaxValue: if ReqInsertCount <= FullRange: Error ReqInsertCount -= FullRange | qpack/decoder.go: verify the one-fewer-wrap correction branch exists and that the ReqInsertCount <= FullRange sub-case errors (QPACK_DECOMPRESSION_FAILED) rather than silently underflowing. |
| `4.5-14` | informative | A Required Insert Count of 0 must be encoded as EncodedInsertCount 0; a nonzero encoding that decodes to 0 is an error. <br>_(receiver [CLIENT])_ | # Value of 0 must be encoded as 0. if ReqInsertCount == 0: Error | qpack/decoder.go: after unwrap, a result of 0 reached via nonzero EncodedInsertCount must raise QPACK_DECOMPRESSION_FAILED; qpack/encoder.go must encode RIC 0 only as literal 0. |
| `4.5-15` | informative | Sign bit 0 means Base >= Required Insert Count, and the decoder computes Base = ReqInsertCount + DeltaBase. <br>_(all [CLIENT])_ | if Sign == 0: Base = ReqInsertCount + DeltaBase | qpack/decoder.go Base computation for S=0; qpack/encoder.go must pick S=0 whenever its chosen Base >= RIC. |
| `4.5-16` | informative | Sign bit 1 means Base < Required Insert Count, and the decoder computes Base = ReqInsertCount - DeltaBase - 1 (note the extra minus one). <br>_(all [CLIENT])_ | Base = ReqInsertCount - DeltaBase - 1 | qpack/decoder.go: verify the S=1 branch subtracts DeltaBase AND an additional 1 — an off-by-one here shifts every dynamic-table reference in the section. |
| `4.5-17` | informative | A single-pass encoder that inserted and referenced new entries while encoding has RIC > Base, so it sets Sign to 1; one that did not has Base >= RIC and sets Sign to 0. <br>_(sender [CLIENT])_ | Required Insert Count will be greater than the Base, so the encoded difference is negative and the Sign bit is set to 1. | qpack/encoder.go: if the client encoder ever inserts during request encoding, verify Sign selection follows the Base-vs-RIC comparison; a static-only encoder always emits S=0. |
| `4.5-18` | MUST NOT | The value of Base is never allowed to be negative in a produced field section. <br>_(sender [CLIENT])_ | The value of Base MUST NOT be negative. | qpack/encoder.go: assert the encoder can never emit S=1 with DeltaBase >= ReqInsertCount (which would encode a negative Base). |
| `4.5-19` | MUST | An endpoint receiving a field section prefix with Sign bit 1 where Required Insert Count <= Delta Base (i.e., negative Base) must treat the field block as invalid. <br>_(endpoint [CLIENT])_ | treat a field block with a Sign bit of 1 as invalid if the value of Required Insert Count is less than or equal to the value of Delta Base | qpack/decoder.go: verify an explicit S==1 && ReqInsertCount <= DeltaBase check rejects the field section (QPACK_DECOMPRESSION_FAILED per Section 6 framing of decompression failure). |
| `4.5-20` | informative | An encoder that finishes all table updates before encoding may set Base equal to Required Insert Count, encoding both the Sign bit and Delta Base as zero. <br>_(sender [CLIENT])_ | might set Base to the value of Required Insert Count. In such a case, both the Sign bit and the Delta Base will be set to zero. | qpack/encoder.go: the simplest conformant dynamic strategy is Base = RIC with S=0, Delta=0; verify whichever Base policy exists reduces to this or fact 4.5-17. |
| `4.5-21` | informative | A field section with no dynamic-table references may use any Base value; Delta Base zero is among the most efficient encodings. <br>_(sender [CLIENT])_ | A field section that was encoded without references to the dynamic table can use any value for the Base | qpack/encoder.go: the client's static-only request path should emit prefix bytes 0x00 0x00 (RIC=0, S=0, Delta=0); verify. |
| `4.5-22` | informative | An indexed field line representation may only reference a static-table entry or a dynamic-table entry whose absolute index is strictly less than the Base. <br>_(all [CLIENT])_ | identifies an entry in the static table or an entry in the dynamic table with an absolute index less than the value of the Base | qpack/decoder.go: relative dynamic index resolves to absolute = Base - 1 - relIndex; reject references at or above Base (or below insertion floor) as decompression failure. qpack/encoder.go must never emit relative indices for entries >= Base. |
| `4.5-23` | informative | Indexed field line wire format: 1-bit pattern '1', then the 'T' table bit, then the index as a prefixed integer with a 6-bit prefix. <br>_(all [CLIENT])_ | This representation starts with the '1' 1-bit pattern, followed by the 'T' bit | qpack/instructions.go / decoder.go: opcode dispatch on top bit 1 (mask 0x80), T at 0x40, 6-bit prefix integer for the index; encoder.go emits the same layout for static hits. |
| `4.5-24` | informative | In the indexed field line, T=1 means the index is a static-table index and T=0 means it is a relative index into the dynamic table. <br>_(all [CLIENT])_ | When T=1, the number represents the static table index; when T=0, the number is the relative index of the entry in the dynamic table. | qpack/static_table.go bounds-check for T=1 (index > 98 is invalid); qpack/decoder.go relative-index resolution for T=0 against the dynamic table. |
| `4.5-25` | informative | An indexed field line with post-Base index references a dynamic-table entry whose absolute index is greater than or equal to the Base. <br>_(all [CLIENT])_ | identifies an entry in the dynamic table with an absolute index greater than or equal to the value of the Base | qpack/decoder.go: post-Base resolution is absolute = Base + postBaseIndex; entries resolved must still be < Required Insert Count or the section is malformed — verify both bounds. |
| `4.5-26` | informative | Indexed field line with post-Base index wire format: 4-bit pattern '0001' followed by the post-Base index as a prefixed integer with a 4-bit prefix. <br>_(all [CLIENT])_ | This representation starts with the '0001' 4-bit pattern. | qpack/instructions.go / decoder.go: dispatch on high nibble 0001 (byte & 0xF0 == 0x10), 4-bit prefix integer; if the client encoder never emits post-Base forms, the decoder must still parse them from servers. |
| `4.5-27` | informative | A literal field line with name reference takes its name from a static-table entry or from a dynamic-table entry with absolute index less than the Base. <br>_(all [CLIENT])_ | encodes a field line where the field name matches the field name of an entry in the static table | qpack/decoder.go: name-reference resolution uses the same static/relative-dynamic rules and the same below-Base bound as fact 4.5-22; qpack/encoder.go uses this form for name-hit/value-miss. |
| `4.5-28` | informative | Literal field line with name reference wire format: 2-bit pattern '01', then the 'N' (never-index) bit, then 'T', then a 4-bit-prefix name index, then the value string. <br>_(all [CLIENT])_ | This representation starts with the '01' 2-bit pattern. The following bit, 'N', indicates whether an intermediary is permitted to add this field line to the dynamic table on subsequent hops | qpack/instructions.go / decoder.go: dispatch on top two bits 01 (byte & 0xC0 == 0x40), N at 0x20, T at 0x10, 4-bit prefix integer name index; encoder.go emits this layout with correct bit positions. |
| `4.5-29` | MUST | When the 'N' bit is set on a field line, that field line must always be encoded with a literal representation, never an indexed one. <br>_(all [CLIENT])_ | When the 'N' bit is set, the encoded field line MUST always be encoded with a literal representation. | qpack/encoder.go: verify sensitive request fields (e.g. authorization, cookie — see http3/request.go policy) are emitted as literals with N=1 and are never inserted into the dynamic table or matched to indexed forms. |
| `4.5-30` | MUST | A peer that received a field line as a literal with the 'N' bit set must use a literal representation when forwarding that field line. <br>_(intermediary [not-client])_ | it MUST use a literal representation to forward this field line | Forwarding rule — not applicable to a pure client. Optionally check qpack/decoder.go surfaces a Sensitive/NeverIndexed flag on decoded fields so any future proxy use could honor it; hpack sibling has this concept (sibling-divergence risk). |
| `4.5-31` | informative | The 'N' bit exists to protect field values that would be endangered by compression (e.g. secrets vulnerable to compression side channels, per Section 7.1). <br>_(all [CLIENT])_ | This bit is intended for protecting field values that are not to be put at risk by compressing them | http3/request.go / qpack/encoder.go: confirm there is a sensitivity policy deciding which request headers get N=1 rather than never setting N at all. |
| `4.5-32` | informative | In the literal-with-name-reference form, the fourth ('T') bit selects static (T=1, static index) versus dynamic (T=0, relative index) for the 4-bit-prefix name index. <br>_(all [CLIENT])_ | The fourth ('T') bit indicates whether the reference is to the static or dynamic table. | qpack/decoder.go: same T semantics as fact 4.5-24 but with a 4-bit (not 6-bit) prefix integer — verify the prefix widths are not conflated between the two representations. |
| `4.5-33` | informative | In a literal field line with name reference, only the name comes from the referenced entry; the value is carried as a string literal with an 8-bit prefix (H bit + 7-bit length). <br>_(all [CLIENT])_ | Only the field name is taken from the dynamic table entry; the field value is encoded as an 8-bit prefix string literal | qpack/decoder.go: value parsing uses the Section 4.1.2 string-literal codec with H flag at 0x80 and 7-bit length prefix; Huffman decode when H=1. |
| `4.5-34` | informative | A literal field line with post-Base name reference takes its name from a dynamic-table entry whose absolute index is greater than or equal to the Base. <br>_(all [CLIENT])_ | encodes a field line where the field name matches the field name of a dynamic table entry | qpack/decoder.go: post-Base name resolution absolute = Base + postBaseIndex, same bounds as fact 4.5-25; decoder must accept this form from servers even if the client encoder never emits it. |
| `4.5-35` | informative | Literal field line with post-Base name reference wire format: 4-bit pattern '0000', fifth bit 'N', then the post-Base name index as a prefixed integer with a 3-bit prefix, then the value string. <br>_(all [CLIENT])_ | This representation starts with the '0000' 4-bit pattern. The fifth bit is the 'N' bit as described in Section 4.5.4. | qpack/instructions.go / decoder.go: dispatch on high nibble 0000 (byte & 0xF0 == 0x00), N at 0x08, 3-bit prefix integer — the narrowest prefix in QPACK; verify it is 3 bits, not 4. |
| `4.5-36` | informative | In the post-Base name reference form too, only the name comes from the dynamic-table entry; the value is an 8-bit prefix string literal. <br>_(all [CLIENT])_ | Only the field name is taken from the dynamic table entry; the field value is encoded as an 8-bit prefix string literal | qpack/decoder.go: post-Base literal path shares the value string-literal codec with fact 4.5-33; verify no divergence between the two literal decode paths. |
| `4.5-37` | informative | Literal field line with literal name wire format begins with the 3-bit pattern '001' followed by the 'N' bit as the fourth bit. <br>_(all [CLIENT])_ | This representation starts with the '001' 3-bit pattern. The fourth bit is the 'N' bit as described in Section 4.5.4. | qpack/instructions.go / decoder.go: dispatch on top three bits 001 (byte & 0xE0 == 0x20), N at 0x10; encoder.go uses this form for names absent from both tables. |
| `4.5-38` | informative | In the literal-name form, the name is a string literal with a 4-bit prefix (its own H bit + 3-bit length prefix) and the value is a string literal with an 8-bit prefix. <br>_(all [CLIENT])_ | The name follows, represented as a 4-bit prefix string literal, then the value, represented as an 8-bit prefix string literal | qpack/encoder.go + decoder.go: name uses H at 0x08 with 3-bit length prefix, value uses H at 0x80 with 7-bit length prefix — verify both prefix widths and both independent Huffman flags; http3/validate.go still validates decoded names/values. |

### 9204 section 5-8 - RFC 9204 §5-8: Configuration, Error Handling, Security Considerations, IANA Considerations

| ID | Level | Requirement (scope) | RFC 9204 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `5-8-1` | informative | SETTINGS_QPACK_MAX_TABLE_CAPACITY has HTTP/3 setting identifier 0x01 and a default value of zero (dynamic table disabled until the decoder advertises a nonzero value). <br>_(endpoint [CLIENT])_ | SETTINGS_QPACK_MAX_TABLE_CAPACITY (0x01): The default value is zero. | http3/control.go: client SETTINGS must emit id 0x01 with its decoder-side capacity, and treat an absent server value as 0 so qpack/encoder.go never uses the dynamic table until the server advertises >0. |
| `5-8-2` | informative | SETTINGS_QPACK_MAX_TABLE_CAPACITY is the QPACK equivalent of HTTP/2's SETTINGS_HEADER_TABLE_SIZE. <br>_(endpoint [CLIENT])_ | This is the equivalent of the SETTINGS_HEADER_TABLE_SIZE from HTTP/2. | docs/RFC_COVERAGE.md: note the mapping; semantics should parallel the hpack table-size handling used by conn/ for HTTP/2. |
| `5-8-3` | informative | SETTINGS_QPACK_BLOCKED_STREAMS has HTTP/3 setting identifier 0x07 and a default value of zero (peer may not risk blocking any stream until told otherwise). <br>_(endpoint [CLIENT])_ | SETTINGS_QPACK_BLOCKED_STREAMS (0x07): The default value is zero. | http3/control.go: client emits id 0x07 with its blocked-streams tolerance; qpack/encoder.go must treat an absent server value as 0 and never emit references that could block a server-side decode. |
| `5-8-4` | informative | The three QPACK error codes are defined to indicate QPACK failures that prevent the stream or connection from continuing. <br>_(endpoint [CLIENT])_ | error codes are defined for HTTP/3 to indicate failures of QPACK that prevent the stream or connection from continuing | qpack/errors.go: all three codes exist and are wired to http3 stream-error vs connection-error paths, not swallowed. |
| `5-8-5` | informative | QPACK_DECOMPRESSION_FAILED is code 0x0200, raised when the decoder failed to interpret an encoded field section and cannot continue decoding that section. <br>_(receiver [CLIENT])_ | QPACK_DECOMPRESSION_FAILED (0x0200): The decoder failed to interpret an encoded field section and is not able to continue decoding that field section. | qpack/decoder.go + http3/response.go: a response field-section decode failure must surface as a stream error 0x0200 on the request stream (RESET/STOP_SENDING), not a connection error. |
| `5-8-6` | informative | QPACK_ENCODER_STREAM_ERROR is code 0x0201, raised when the decoder failed to interpret an encoder instruction received on the encoder stream. <br>_(receiver [CLIENT])_ | QPACK_ENCODER_STREAM_ERROR (0x0201): The decoder failed to interpret an encoder instruction received on the encoder stream. | http3/control.go (reader of the server's encoder stream) + qpack/instructions.go: any malformed/invalid encoder instruction must close the connection with 0x0201. |
| `5-8-7` | informative | QPACK_DECODER_STREAM_ERROR is code 0x0202, raised when the encoder failed to interpret a decoder instruction received on the decoder stream. <br>_(sender [CLIENT])_ | QPACK_DECODER_STREAM_ERROR (0x0202): The encoder failed to interpret a decoder instruction received on the decoder stream. | http3/control.go (reader of the server's decoder stream) + qpack/encoder.go: malformed Section Ack / Stream Cancellation / Insert Count Increment must close the connection with 0x0202. |
| `5-8-8` | informative | The compression context can be probed by an attacker who can both choose fields to be encoded and observe the encoded lengths, confirming guesses about dynamic table state. <br>_(all [CLIENT])_ | probed by an attacker who can both define fields to be encoded and transmitted and observe the length of those fields once they are encoded | qpack/encoder.go: document the threat model behind the indexing policy; for a single-tenant load-generator client risk is low but the policy choice should be deliberate. |
| `5-8-9` | informative | QPACK mitigates but does not prevent CRIME-style attacks by forcing a guess to match an entire field line rather than individual characters. <br>_(all [CLIENT])_ | QPACK mitigates, but does not completely prevent, attacks modeled on CRIME ([CRIME]) by forcing a guess to match an entire field line | qpack/encoder.go: no per-character partial matching against the dynamic table — only whole-field-line matches may shorten output. |
| `5-8-10` | informative | Probing attacks are possible whenever two mutually distrustful entities control requests or responses placed on a single HTTP/3 connection. <br>_(all [CLIENT])_ | possible any time that two mutually distrustful entities control requests or responses that are placed onto a single HTTP/3 connection | client/ pool + http3: connections are per-target and driven by one application; document that pooled H3 conns must not be shared across trust domains without an indexing policy. |
| `5-8-11` | informative | Web browsers must assume requests made on the same connection by different web origins are made by mutually distrustful entities. <br>_(user-agent [not-client])_ | Web browsers also need to assume that requests made on the same connection by different web origins ([RFC6454]) are made by mutually distrustful entities. | N/A for a load-generator client; only relevant if the library is ever embedded in a multi-origin user agent. |
| `5-8-12` | informative | A QPACK implementation can constrain how compression is applied in order to limit dynamic-table probing, instead of constraining HTTP users. <br>_(sender [CLIENT])_ | an implementation of QPACK can instead constrain how compression is applied in order to limit the potential for dynamic table probing | qpack/encoder.go: the encoder's decision of what to insert/index is the mitigation point; document the chosen policy. |
| `5-8-13` | informative | The ideal probing mitigation segregates dynamic-table access by the entity constructing the message, so only the creator of a value can extract it. <br>_(sender [CLIENT])_ | An ideal solution segregates access to the dynamic table based on the entity that is constructing the message. | qpack/encoder.go: single-tenant client — document non-applicability rather than implementing attribution. |
| `5-8-14` | informative | An encoder without provenance knowledge may instead penalize many field lines sharing a name with different values, eventually disabling dynamic-table comparison for that name. <br>_(sender [CLIENT])_ | might instead introduce a penalty for many field lines with the same field name and different values | qpack/encoder.go: optional guess-rate heuristic; not required for this client, note as deliberate omission. |
| `5-8-15` | informative | An intermediary re-encoding messages without knowledge of the constructing entity could inadvertently merge compression contexts the original encoder kept separate. <br>_(intermediary [not-client])_ | the intermediary could inadvertently merge compression contexts that the original encoder had specifically kept separate | N/A: poseidon is not an intermediary. |
| `5-8-16` | informative | Merely evicting a sensitive entry from the dynamic table is ineffectual if the attacker can reliably force the value to be reinstalled (e.g., Cookie via forced image loads). <br>_(sender [CLIENT])_ | Simply removing entries corresponding to the field from the dynamic table can be ineffectual if the attacker has a reliable way of causing values to be reinstalled. | qpack/encoder.go: if a sensitive-field mitigation is added, it must be never-index (N bit), not eviction-based. |
| `5-8-17` | informative | Implementations can protect sensitive fields by not compressing them and encoding their values as literals instead. <br>_(sender [CLIENT])_ | Implementations can also choose to protect sensitive fields by not compressing them and instead encoding their value as literals. | qpack/encoder.go: verify a caller-controllable literal (non-indexed) path exists for sensitive request headers. |
| `5-8-18` | informative | The never-indexed literal 'N' bit (Section 4.5.4) signals to intermediaries that a value was intentionally sent as a literal; the protection only works if all hops honor it. <br>_(sender [CLIENT])_ | The never-indexed literal bit (see Section 4.5.4) can be used to signal to intermediaries that a particular value was intentionally sent as a literal. | qpack/encoder.go: check the client can emit the N bit on literal field-line representations (e.g., driven by hpack.HeaderField.Sensitive). |
| `5-8-19` | MUST NOT | An intermediary must not re-encode a value received with the 'N' bit set using any representation that would index it. <br>_(intermediary [not-client])_ | An intermediary MUST NOT re-encode a value that uses a literal representation with the 'N' bit set with another representation that would index it. | qpack/decoder.go: not an intermediary, but the decoder should surface the N bit (map to hpack.HeaderField.Sensitive) so the flag is not lost if fields are ever re-encoded. |
| `5-8-20` | MUST | If QPACK is used to re-encode an 'N'-bit value, a literal representation with the 'N' bit set must be used. <br>_(intermediary [not-client])_ | If QPACK is used for re-encoding, a literal representation with the 'N' bit set MUST be used. | qpack/encoder.go: if it ever encodes a field with Sensitive=true, it must use the N-bit literal form — verify that mapping even though the client is not a proxy. |
| `5-8-21` | MUST | If HPACK is used to re-encode an 'N'-bit value, the HPACK never-indexed literal representation (RFC 7541 Section 6.2.3) must be used. <br>_(intermediary [not-client])_ | If HPACK is used for re-encoding, the never-indexed literal representation (see Section 6.2.3 of [RFC7541]) MUST be used. | hpack/: never-indexed support already exists for HTTP/2; N/A on the H3 client path. |
| `5-8-22` | informative | An encoder might choose not to index short or low-entropy values because QPACK does not protect against guessing an entire field value. <br>_(sender [CLIENT])_ | an encoder might choose not to index values with low entropy | qpack/encoder.go: consider a value-length/entropy heuristic for indexing decisions; at minimum document the policy. |
| `5-8-23` | informative | An encoder might choose not to index values of highly valuable or sensitive fields such as Cookie or Authorization. <br>_(sender [CLIENT])_ | not to index values for fields that are considered to be highly valuable or sensitive to recovery, such as the Cookie or Authorization header fields | qpack/encoder.go: check Authorization/Cookie are treated as sensitive (never-indexed/N-bit) by default or via a caller flag, matching whatever the hpack encoder does for HTTP/2 (probe both — sibling-divergence risk). |
| `5-8-24` | informative | There is no currently known attack against a static Huffman encoding; a studied information leak was concluded unexploitable. <br>_(all [CLIENT])_ | There is no currently known attack against a static Huffman encoding. | qpack/: Huffman coding of literals is safe to use unconditionally; no action needed. |
| `5-8-25` | informative | The decoder controls the encoder-induced memory bound via SETTINGS_QPACK_MAX_TABLE_CAPACITY (table size) and SETTINGS_QPACK_BLOCKED_STREAMS (blocked streams). <br>_(receiver [CLIENT])_ | these values are controlled by the decoder through the settings parameters SETTINGS_QPACK_MAX_TABLE_CAPACITY and SETTINGS_QPACK_BLOCKED_STREAMS, respectively | http3/control.go: both settings the client advertises must be derived from its own decode-side memory budget, not hardcoded to please servers. |
| `5-8-26` | informative | The dynamic-table size limit accounts for the stored data plus a small allowance for overhead. <br>_(endpoint [CLIENT])_ | takes into account the size of the data stored in the dynamic table, plus a small allowance for overhead | qpack/dynamic_table.go: entry size accounting must include the per-entry overhead constant (name len + value len + 32, per Section 3.2.1), not raw byte length. |
| `5-8-27` | informative | The blocked-streams limit is only a proxy for decoder memory; actual memory depends on per-blocked-stream tracking cost. <br>_(receiver [CLIENT])_ | The limit on the number of blocked streams is only a proxy for the maximum amount of memory required by the decoder. | http3/control.go + qpack/decoder.go: choose the advertised SETTINGS_QPACK_BLOCKED_STREAMS with the real per-blocked-stream state cost in mind and bound buffered blocked sections. |
| `5-8-28` | informative | An encoder can limit its own state memory by choosing a smaller dynamic table size than the decoder allows and signaling that to the decoder. <br>_(sender [CLIENT])_ | An encoder can limit the amount of state memory it uses by choosing a smaller dynamic table size than the decoder allows and signaling this to the decoder | qpack/encoder.go: Set Dynamic Table Capacity may legitimately be below the server's SETTINGS_QPACK_MAX_TABLE_CAPACITY; verify the encoder supports choosing a smaller (including zero) capacity. |
| `5-8-29` | informative | Streams that merely risk becoming blocked consume no additional state memory on the encoder. <br>_(sender [CLIENT])_ | Streams that risk becoming blocked consume no additional state memory on the encoder. | qpack/encoder.go: no per-stream blocking bookkeeping needed beyond reference tracking; informational. |
| `5-8-30` | informative | An encoder can directly bound its state memory by limiting how many dynamic-table references it tracks in unacknowledged field sections, with no signaling required, at a compression cost. <br>_(sender [CLIENT])_ | directly limit the amount of state memory by only using as many references to the dynamic table as it wishes to track | qpack/encoder.go: verify unacknowledged-section reference tracking is bounded (or bounded by construction) per the Peer-input policy. |
| `5-8-31` | informative | Temporary memory can be limited by processing field lines sequentially; a decoder need not retain a complete field-line list while decoding a section. <br>_(endpoint [CLIENT])_ | The amount of temporary memory consumed by an encoder or decoder can be limited by processing field lines sequentially. | qpack/decoder.go + http3/response.go: check whether the response section is decoded streaming vs fully buffered; either way the buffer must be bounded by the field-section size limit. |
| `5-8-32` | informative | Implementations should limit the size of unsent (flow-control-stalled) data, especially on the decoder stream, e.g. by limiting peer stream opening, reading only the encoder stream, or closing the connection. <br>_(endpoint [CLIENT])_ | Implementations should limit the size of unsent data, especially on the decoder stream where flexibility to choose what to send is limited | http3/control.go: the client's decoder-stream writer (Section Acks / Stream Cancellations / Insert Count Increments) must have bounded buffering with a defined response when the server withholds flow-control credit. |
| `5-8-33` | informative | An implementation must ensure large integer values, long integer encodings, and long string literals do not create security weaknesses. <br>_(endpoint [CLIENT])_ | needs to ensure that large values for integers, long encoding for integers, or long string literals do not create security weaknesses | qpack/instructions.go + qpack/decoder.go: prefixed-integer reader must cap both value magnitude and continuation-byte count; string reader must cap length — bound by construction per Peer-input policy. |
| `5-8-34` | informative | An implementation has to set a limit on the integer values it accepts and on the encoded (prefixed-integer) length it accepts. <br>_(endpoint [CLIENT])_ | An implementation has to set a limit for the values it accepts for integers, as well as for the encoded length; see Section 4.1.1. | qpack prefixed-int decode (instructions.go / internal/bytesx if shared): verify explicit max-value and max-encoded-length bounds exist, and are tested with over-long zero-padded encodings. |
| `5-8-35` | informative | An implementation has to set a limit on the string-literal lengths it accepts. <br>_(endpoint [CLIENT])_ | it has to set a limit to the length it accepts for string literals; see Section 4.1.2. | qpack/decoder.go: string-literal length cap must also bound post-Huffman expansion, not just the wire length. |
| `5-8-36` | SHOULD | The integer and string-literal limits should be large enough to process the largest individual field the HTTP implementation can be configured to accept. <br>_(endpoint [CLIENT])_ | These limits SHOULD be large enough to process the largest individual field the HTTP implementation can be configured to accept. | qpack limits vs http3/validate.go and any client max-header-size config: QPACK caps must be >= the configured max field size, or valid responses get falsely rejected (interop/false-reject dimension). |
| `5-8-37` | MUST | A value larger than the implementation can decode must be treated as a stream error of type QPACK_DECOMPRESSION_FAILED on a request stream, or a connection error of the appropriate type on the encoder or decoder stream. <br>_(endpoint [CLIENT])_ | this MUST be treated as a stream error of type QPACK_DECOMPRESSION_FAILED if on a request stream or a connection error of the appropriate type | qpack/decoder.go + http3: oversize in a response field section -> stream error 0x0200; on the server's encoder stream -> connection error 0x0201; on the server's decoder stream -> connection error 0x0202 (raised by the encoder side in encoder.go). |
| `5-8-38` | informative | IANA registers setting QPACK_MAX_TABLE_CAPACITY as code 0x01 (default 0) and QPACK_BLOCKED_STREAMS as code 0x07 (default 0) in the HTTP/3 Settings registry. <br>_(all [CLIENT])_ | \| QPACK_MAX_TABLE_CAPACITY \| 0x01 \| Section 5 \| 0 \| | http3/control.go: setting-ID constants must be exactly 0x01 and 0x07 with default-0 semantics when absent from the server's SETTINGS. |
| `5-8-39` | informative | The QPACK Encoder Stream is registered as unidirectional stream type 0x02, sendable by both endpoints. <br>_(all [CLIENT])_ | \| QPACK Encoder Stream \| 0x02 \| Section 4.2 \| Both \| | http3/control.go: client must open its encoder stream with type varint 0x02 and accept the server's incoming 0x02 stream, routing it to qpack decoder instruction processing. |
| `5-8-40` | informative | The QPACK Decoder Stream is registered as unidirectional stream type 0x03, sendable by both endpoints. <br>_(all [CLIENT])_ | \| QPACK Decoder Stream \| 0x03 \| Section 4.2 \| Both \| | http3/control.go: client must open its decoder stream with type varint 0x03 and accept the server's incoming 0x03 stream, routing it to qpack/encoder.go decoder-instruction handling. |
| `5-8-41` | informative | IANA registers three error codes in the HTTP/3 Error Codes registry: QPACK_DECOMPRESSION_FAILED 0x0200, QPACK_ENCODER_STREAM_ERROR 0x0201, QPACK_DECODER_STREAM_ERROR 0x0202. <br>_(all [CLIENT])_ | This document specifies three error codes. | qpack/errors.go: numeric constants must be exactly 0x0200, 0x0201, 0x0202 as sent on the wire in RESET_STREAM/STOP_SENDING/CONNECTION_CLOSE. |

### 9204 section A - Static Table

| ID | Level | Requirement (scope) | RFC 9204 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `A-1` | informative | The QPACK static table ordering is fixed and optimized so the most common header fields encode with the smallest number of bytes; implementations must reproduce this exact order. <br>_(endpoint [CLIENT])_ | The order of the entries is optimized to encode the most common header fields with the smallest number of bytes. | qpack/static_table.go: table must be in the exact RFC order (index = array position), never sorted or reordered. |
| `A-2` | informative | The table was generated from 2018 Internet traffic analysis, so some entries are inconsistent or appear multiple times with similar values; this is expected and must not be 'fixed' or deduplicated. <br>_(endpoint [CLIENT])_ | some of the entries may be inconsistent or appear multiple times with similar but not identical values. | qpack/static_table.go: keep duplicate-looking entries (e.g. 33/34/75 access-control-allow-headers) verbatim; no dedup. |
| `A-3` | informative | Line breaks appearing inside names or values in the RFC text are formatting artifacts only; the real values are continuous strings. <br>_(endpoint [CLIENT])_ | Any line breaks that appear within field names or values are due to formatting. | qpack/static_table.go: verify wrapped values (30,41,44,45,47,52,54,57,58,85) were joined correctly with no stray newline/space. |
| `A-4` | informative | The static table contains exactly 99 entries with absolute indices 0 through 98; index 98 is the highest valid static index. <br>_(endpoint [CLIENT])_ | \| 98 \| x-frame-options \| sameorigin \| | qpack/static_table.go: len==99; qpack/decoder.go: static index >98 in any representation must fail decoding (QPACK_DECOMPRESSION_FAILED per body sections). |
| `A-5` | informative | Static index 0 is name ':authority' with empty value. <br>_(endpoint [CLIENT])_ | \| 0 \| :authority \| \| | qpack/static_table.go entry 0; qpack/encoder.go should hit this for request :authority name-reference. |
| `A-6` | informative | Static index 1 is name ':path' with value '/'. <br>_(endpoint [CLIENT])_ | \| 1 \| :path \| / \| | qpack/static_table.go entry 1; encoder full-match for GET / requests. |
| `A-7` | informative | Static index 2 is name 'age' with value '0'. <br>_(endpoint [CLIENT])_ | \| 2 \| age \| 0 \| | qpack/static_table.go entry 2 (response header; decoder path). |
| `A-8` | informative | Static index 3 is name 'content-disposition' with empty value. <br>_(endpoint [CLIENT])_ | \| 3 \| content-disposition \| \| | qpack/static_table.go entry 3. |
| `A-9` | informative | Static index 4 is name 'content-length' with value '0'. <br>_(endpoint [CLIENT])_ | \| 4 \| content-length \| 0 \| | qpack/static_table.go entry 4; encoder should full-match content-length: 0 on bodyless requests. |
| `A-10` | informative | Static index 5 is name 'cookie' with empty value. <br>_(endpoint [CLIENT])_ | \| 5 \| cookie \| \| | qpack/static_table.go entry 5; cookie is a never-index candidate (body §7.1) — check http3/validate.go / encoder literal flags. |
| `A-11` | informative | Static index 6 is name 'date' with empty value. <br>_(endpoint [CLIENT])_ | \| 6 \| date \| \| | qpack/static_table.go entry 6. |
| `A-12` | informative | Static index 7 is name 'etag' with empty value. <br>_(endpoint [CLIENT])_ | \| 7 \| etag \| \| | qpack/static_table.go entry 7. |
| `A-13` | informative | Static index 8 is name 'if-modified-since' with empty value. <br>_(endpoint [CLIENT])_ | \| 8 \| if-modified-since \| \| | qpack/static_table.go entry 8. |
| `A-14` | informative | Static index 9 is name 'if-none-match' with empty value. <br>_(endpoint [CLIENT])_ | \| 9 \| if-none-match \| \| | qpack/static_table.go entry 9. |
| `A-15` | informative | Static index 10 is name 'last-modified' with empty value. <br>_(endpoint [CLIENT])_ | \| 10 \| last-modified \| \| | qpack/static_table.go entry 10. |
| `A-16` | informative | Static index 11 is name 'link' with empty value. <br>_(endpoint [CLIENT])_ | \| 11 \| link \| \| | qpack/static_table.go entry 11. |
| `A-17` | informative | Static index 12 is name 'location' with empty value. <br>_(endpoint [CLIENT])_ | \| 12 \| location \| \| | qpack/static_table.go entry 12 (redirect responses; decoder path). |
| `A-18` | informative | Static index 13 is name 'referer' with empty value. <br>_(endpoint [CLIENT])_ | \| 13 \| referer \| \| | qpack/static_table.go entry 13 (note RFC spelling 'referer', one r). |
| `A-19` | informative | Static index 14 is name 'set-cookie' with empty value. <br>_(endpoint [CLIENT])_ | \| 14 \| set-cookie \| \| | qpack/static_table.go entry 14 (response header the client decoder must resolve). |
| `A-20` | informative | Static index 15 is name ':method' with value 'CONNECT'. <br>_(endpoint [CLIENT])_ | \| 15 \| :method \| CONNECT \| | qpack/static_table.go entry 15. |
| `A-21` | informative | Static index 16 is name ':method' with value 'DELETE'. <br>_(endpoint [CLIENT])_ | \| 16 \| :method \| DELETE \| | qpack/static_table.go entry 16. |
| `A-22` | informative | Static index 17 is name ':method' with value 'GET'. <br>_(endpoint [CLIENT])_ | \| 17 \| :method \| GET \| | qpack/static_table.go entry 17; encoder must full-match GET (hottest request codepoint). |
| `A-23` | informative | Static index 18 is name ':method' with value 'HEAD'. <br>_(endpoint [CLIENT])_ | \| 18 \| :method \| HEAD \| | qpack/static_table.go entry 18. |
| `A-24` | informative | Static index 19 is name ':method' with value 'OPTIONS'. <br>_(endpoint [CLIENT])_ | \| 19 \| :method \| OPTIONS \| | qpack/static_table.go entry 19. |
| `A-25` | informative | Static index 20 is name ':method' with value 'POST'. <br>_(endpoint [CLIENT])_ | \| 20 \| :method \| POST \| | qpack/static_table.go entry 20. |
| `A-26` | informative | Static index 21 is name ':method' with value 'PUT'. <br>_(endpoint [CLIENT])_ | \| 21 \| :method \| PUT \| | qpack/static_table.go entry 21. |
| `A-27` | informative | Static index 22 is name ':scheme' with value 'http'. <br>_(endpoint [CLIENT])_ | \| 22 \| :scheme \| http \| | qpack/static_table.go entry 22. |
| `A-28` | informative | Static index 23 is name ':scheme' with value 'https'. <br>_(endpoint [CLIENT])_ | \| 23 \| :scheme \| https \| | qpack/static_table.go entry 23; encoder full-match for every h3 request (https). |
| `A-29` | informative | Static index 24 is name ':status' with value '103'. <br>_(endpoint [CLIENT])_ | \| 24 \| :status \| 103 \| | qpack/static_table.go entry 24; decoder + http3/response.go 1xx interim handling. |
| `A-30` | informative | Static index 25 is name ':status' with value '200'. <br>_(endpoint [CLIENT])_ | \| 25 \| :status \| 200 \| | qpack/static_table.go entry 25 (hottest response codepoint; decoder path). |
| `A-31` | informative | Static index 26 is name ':status' with value '304'. <br>_(endpoint [CLIENT])_ | \| 26 \| :status \| 304 \| | qpack/static_table.go entry 26. |
| `A-32` | informative | Static index 27 is name ':status' with value '404'. <br>_(endpoint [CLIENT])_ | \| 27 \| :status \| 404 \| | qpack/static_table.go entry 27. |
| `A-33` | informative | Static index 28 is name ':status' with value '503'. <br>_(endpoint [CLIENT])_ | \| 28 \| :status \| 503 \| | qpack/static_table.go entry 28. |
| `A-34` | informative | Static index 29 is name 'accept' with value '*/*'. <br>_(endpoint [CLIENT])_ | \| 29 \| accept \| */* \| | qpack/static_table.go entry 29. |
| `A-35` | informative | Static index 30 is name 'accept' with value 'application/dns-message' (RFC line wrap; no space or break in the real value). <br>_(endpoint [CLIENT])_ | \| 30 \| accept \| application/dns- message \| | qpack/static_table.go entry 30: value must be exactly application/dns-message (wrap-join check). |
| `A-36` | informative | Static index 31 is name 'accept-encoding' with value 'gzip, deflate, br' (commas and single spaces exactly). <br>_(endpoint [CLIENT])_ | \| 31 \| accept-encoding \| gzip, deflate, br \| | qpack/static_table.go entry 31: exact string incl. spaces after commas. |
| `A-37` | informative | Static index 32 is name 'accept-ranges' with value 'bytes'. <br>_(endpoint [CLIENT])_ | \| 32 \| accept-ranges \| bytes \| | qpack/static_table.go entry 32. |
| `A-38` | informative | Static index 33 is name 'access-control-allow-headers' with value 'cache-control'. <br>_(endpoint [CLIENT])_ | \| 33 \| access-control-allow-headers \| cache-control \| | qpack/static_table.go entry 33. |
| `A-39` | informative | Static index 34 is name 'access-control-allow-headers' with value 'content-type'. <br>_(endpoint [CLIENT])_ | \| 34 \| access-control-allow-headers \| content-type \| | qpack/static_table.go entry 34. |
| `A-40` | informative | Static index 35 is name 'access-control-allow-origin' with value '*'. <br>_(endpoint [CLIENT])_ | \| 35 \| access-control-allow-origin \| * \| | qpack/static_table.go entry 35. |
| `A-41` | informative | Static index 36 is name 'cache-control' with value 'max-age=0'. <br>_(endpoint [CLIENT])_ | \| 36 \| cache-control \| max-age=0 \| | qpack/static_table.go entry 36. |
| `A-42` | informative | Static index 37 is name 'cache-control' with value 'max-age=2592000'. <br>_(endpoint [CLIENT])_ | \| 37 \| cache-control \| max-age=2592000 \| | qpack/static_table.go entry 37. |
| `A-43` | informative | Static index 38 is name 'cache-control' with value 'max-age=604800'. <br>_(endpoint [CLIENT])_ | \| 38 \| cache-control \| max-age=604800 \| | qpack/static_table.go entry 38. |
| `A-44` | informative | Static index 39 is name 'cache-control' with value 'no-cache'. <br>_(endpoint [CLIENT])_ | \| 39 \| cache-control \| no-cache \| | qpack/static_table.go entry 39. |
| `A-45` | informative | Static index 40 is name 'cache-control' with value 'no-store'. <br>_(endpoint [CLIENT])_ | \| 40 \| cache-control \| no-store \| | qpack/static_table.go entry 40. |
| `A-46` | informative | Static index 41 is name 'cache-control' with value 'public, max-age=31536000' (wrapped in RFC text; single space after comma, none around '='). <br>_(endpoint [CLIENT])_ | \| 41 \| cache-control \| public, max- age=31536000 \| | qpack/static_table.go entry 41: exactly 'public, max-age=31536000' (wrap-join check). |
| `A-47` | informative | Static index 42 is name 'content-encoding' with value 'br'. <br>_(endpoint [CLIENT])_ | \| 42 \| content-encoding \| br \| | qpack/static_table.go entry 42. |
| `A-48` | informative | Static index 43 is name 'content-encoding' with value 'gzip'. <br>_(endpoint [CLIENT])_ | \| 43 \| content-encoding \| gzip \| | qpack/static_table.go entry 43. |
| `A-49` | informative | Static index 44 is name 'content-type' with value 'application/dns-message' (wrapped in RFC text; no break in real value). <br>_(endpoint [CLIENT])_ | \| 44 \| content-type \| application/dns- message \| | qpack/static_table.go entry 44: exactly application/dns-message. |
| `A-50` | informative | Static index 45 is name 'content-type' with value 'application/javascript' (wrapped in RFC text). <br>_(endpoint [CLIENT])_ | \| 45 \| content-type \| application/ javascript \| | qpack/static_table.go entry 45: exactly application/javascript (no space at wrap point). |
| `A-51` | informative | Static index 46 is name 'content-type' with value 'application/json'. <br>_(endpoint [CLIENT])_ | \| 46 \| content-type \| application/json \| | qpack/static_table.go entry 46. |
| `A-52` | informative | Static index 47 is name 'content-type' with value 'application/x-www-form-urlencoded' (wrapped in RFC text). <br>_(endpoint [CLIENT])_ | \| 47 \| content-type \| application/x-www- form-urlencoded \| | qpack/static_table.go entry 47: exactly application/x-www-form-urlencoded. |
| `A-53` | informative | Static index 48 is name 'content-type' with value 'image/gif'. <br>_(endpoint [CLIENT])_ | \| 48 \| content-type \| image/gif \| | qpack/static_table.go entry 48. |
| `A-54` | informative | Static index 49 is name 'content-type' with value 'image/jpeg'. <br>_(endpoint [CLIENT])_ | \| 49 \| content-type \| image/jpeg \| | qpack/static_table.go entry 49. |
| `A-55` | informative | Static index 50 is name 'content-type' with value 'image/png'. <br>_(endpoint [CLIENT])_ | \| 50 \| content-type \| image/png \| | qpack/static_table.go entry 50. |
| `A-56` | informative | Static index 51 is name 'content-type' with value 'text/css'. <br>_(endpoint [CLIENT])_ | \| 51 \| content-type \| text/css \| | qpack/static_table.go entry 51. |
| `A-57` | informative | Static index 52 is name 'content-type' with value 'text/html; charset=utf-8' — WITH a space after the semicolon (contrast index 54 which has none). <br>_(endpoint [CLIENT])_ | \| 52 \| content-type \| text/html; charset=utf-8 \| | qpack/static_table.go entry 52: 'text/html; charset=utf-8' with space after ';' — classic divergence trap vs entry 54. |
| `A-58` | informative | Static index 53 is name 'content-type' with value 'text/plain'. <br>_(endpoint [CLIENT])_ | \| 53 \| content-type \| text/plain \| | qpack/static_table.go entry 53. |
| `A-59` | informative | Static index 54 is name 'content-type' with value 'text/plain;charset=utf-8' — NO space after the semicolon (contrast index 52). <br>_(endpoint [CLIENT])_ | \| 54 \| content-type \| text/ plain;charset=utf-8 \| | qpack/static_table.go entry 54: 'text/plain;charset=utf-8' with no space after ';'. |
| `A-60` | informative | Static index 55 is name 'range' with value 'bytes=0-'. <br>_(endpoint [CLIENT])_ | \| 55 \| range \| bytes=0- \| | qpack/static_table.go entry 55 (trailing hyphen is part of the value). |
| `A-61` | informative | Static index 56 is name 'strict-transport-security' with value 'max-age=31536000'. <br>_(endpoint [CLIENT])_ | \| 56 \| strict-transport-security \| max-age=31536000 \| | qpack/static_table.go entry 56. |
| `A-62` | informative | Static index 57 is name 'strict-transport-security' with value 'max-age=31536000; includesubdomains' (space after semicolon; lowercase 'includesubdomains'). <br>_(endpoint [CLIENT])_ | \| 57 \| strict-transport-security \| max-age=31536000; includesubdomains \| | qpack/static_table.go entry 57: lowercase includesubdomains, '; ' separator. |
| `A-63` | informative | Static index 58 is name 'strict-transport-security' with value 'max-age=31536000; includesubdomains; preload' ('; ' separators). <br>_(endpoint [CLIENT])_ | \| 58 \| strict-transport-security \| max-age=31536000; includesubdomains; preload \| | qpack/static_table.go entry 58: three tokens joined by '; '. |
| `A-64` | informative | Static index 59 is name 'vary' with value 'accept-encoding'. <br>_(endpoint [CLIENT])_ | \| 59 \| vary \| accept-encoding \| | qpack/static_table.go entry 59. |
| `A-65` | informative | Static index 60 is name 'vary' with value 'origin'. <br>_(endpoint [CLIENT])_ | \| 60 \| vary \| origin \| | qpack/static_table.go entry 60. |
| `A-66` | informative | Static index 61 is name 'x-content-type-options' with value 'nosniff'. <br>_(endpoint [CLIENT])_ | \| 61 \| x-content-type-options \| nosniff \| | qpack/static_table.go entry 61. |
| `A-67` | informative | Static index 62 is name 'x-xss-protection' with value '1; mode=block' (space after semicolon). <br>_(endpoint [CLIENT])_ | \| 62 \| x-xss-protection \| 1; mode=block \| | qpack/static_table.go entry 62: exact '1; mode=block'. |
| `A-68` | informative | Static index 63 is name ':status' with value '100'. <br>_(endpoint [CLIENT])_ | \| 63 \| :status \| 100 \| | qpack/static_table.go entry 63; decoder + http3/response.go interim-response handling. |
| `A-69` | informative | Static index 64 is name ':status' with value '204'. <br>_(endpoint [CLIENT])_ | \| 64 \| :status \| 204 \| | qpack/static_table.go entry 64. |
| `A-70` | informative | Static index 65 is name ':status' with value '206'. <br>_(endpoint [CLIENT])_ | \| 65 \| :status \| 206 \| | qpack/static_table.go entry 65. |
| `A-71` | informative | Static index 66 is name ':status' with value '302'. <br>_(endpoint [CLIENT])_ | \| 66 \| :status \| 302 \| | qpack/static_table.go entry 66. |
| `A-72` | informative | Static index 67 is name ':status' with value '400'. <br>_(endpoint [CLIENT])_ | \| 67 \| :status \| 400 \| | qpack/static_table.go entry 67. |
| `A-73` | informative | Static index 68 is name ':status' with value '403'. <br>_(endpoint [CLIENT])_ | \| 68 \| :status \| 403 \| | qpack/static_table.go entry 68. |
| `A-74` | informative | Static index 69 is name ':status' with value '421'. <br>_(endpoint [CLIENT])_ | \| 69 \| :status \| 421 \| | qpack/static_table.go entry 69; 421 also feeds client retry logic (client/ retry on misdirected request). |
| `A-75` | informative | Static index 70 is name ':status' with value '425'. <br>_(endpoint [CLIENT])_ | \| 70 \| :status \| 425 \| | qpack/static_table.go entry 70. |
| `A-76` | informative | Static index 71 is name ':status' with value '500'. <br>_(endpoint [CLIENT])_ | \| 71 \| :status \| 500 \| | qpack/static_table.go entry 71. |
| `A-77` | informative | Static index 72 is name 'accept-language' with empty value. <br>_(endpoint [CLIENT])_ | \| 72 \| accept-language \| \| | qpack/static_table.go entry 72. |
| `A-78` | informative | Static index 73 is name 'access-control-allow-credentials' with value 'FALSE' (uppercase as listed). <br>_(endpoint [CLIENT])_ | \| 73 \| access-control-allow-credentials \| FALSE \| | qpack/static_table.go entry 73: value is uppercase FALSE exactly. |
| `A-79` | informative | Static index 74 is name 'access-control-allow-credentials' with value 'TRUE' (uppercase as listed). <br>_(endpoint [CLIENT])_ | \| 74 \| access-control-allow-credentials \| TRUE \| | qpack/static_table.go entry 74: value is uppercase TRUE exactly. |
| `A-80` | informative | Static index 75 is name 'access-control-allow-headers' with value '*'. <br>_(endpoint [CLIENT])_ | \| 75 \| access-control-allow-headers \| * \| | qpack/static_table.go entry 75 (third entry for this name — keep all three). |
| `A-81` | informative | Static index 76 is name 'access-control-allow-methods' with value 'get' (lowercase as listed). <br>_(endpoint [CLIENT])_ | \| 76 \| access-control-allow-methods \| get \| | qpack/static_table.go entry 76: lowercase 'get'. |
| `A-82` | informative | Static index 77 is name 'access-control-allow-methods' with value 'get, post, options' (lowercase, comma-space separated). <br>_(endpoint [CLIENT])_ | \| 77 \| access-control-allow-methods \| get, post, options \| | qpack/static_table.go entry 77: exact lowercase 'get, post, options'. |
| `A-83` | informative | Static index 78 is name 'access-control-allow-methods' with value 'options' (lowercase). <br>_(endpoint [CLIENT])_ | \| 78 \| access-control-allow-methods \| options \| | qpack/static_table.go entry 78. |
| `A-84` | informative | Static index 79 is name 'access-control-expose-headers' with value 'content-length'. <br>_(endpoint [CLIENT])_ | \| 79 \| access-control-expose-headers \| content-length \| | qpack/static_table.go entry 79. |
| `A-85` | informative | Static index 80 is name 'access-control-request-headers' with value 'content-type'. <br>_(endpoint [CLIENT])_ | \| 80 \| access-control-request-headers \| content-type \| | qpack/static_table.go entry 80. |
| `A-86` | informative | Static index 81 is name 'access-control-request-method' with value 'get' (lowercase). <br>_(endpoint [CLIENT])_ | \| 81 \| access-control-request-method \| get \| | qpack/static_table.go entry 81. |
| `A-87` | informative | Static index 82 is name 'access-control-request-method' with value 'post' (lowercase). <br>_(endpoint [CLIENT])_ | \| 82 \| access-control-request-method \| post \| | qpack/static_table.go entry 82. |
| `A-88` | informative | Static index 83 is name 'alt-svc' with value 'clear'. <br>_(endpoint [CLIENT])_ | \| 83 \| alt-svc \| clear \| | qpack/static_table.go entry 83. |
| `A-89` | informative | Static index 84 is name 'authorization' with empty value. <br>_(endpoint [CLIENT])_ | \| 84 \| authorization \| \| | qpack/static_table.go entry 84; authorization values are never-index candidates (§7.1) — check encoder literal flag when name-referencing this entry. |
| `A-90` | informative | Static index 85 is name 'content-security-policy' with value "script-src 'none'; object-src 'none'; base-uri 'none'" (single quotes literal, '; ' separators). <br>_(endpoint [CLIENT])_ | \| 85 \| content-security-policy \| script-src 'none'; object-src 'none'; base-uri 'none' \| | qpack/static_table.go entry 85: verify full wrapped value incl. ASCII single quotes and '; ' separators. |
| `A-91` | informative | Static index 86 is name 'early-data' with value '1'. <br>_(endpoint [CLIENT])_ | \| 86 \| early-data \| 1 \| | qpack/static_table.go entry 86. |
| `A-92` | informative | Static index 87 is name 'expect-ct' with empty value. <br>_(endpoint [CLIENT])_ | \| 87 \| expect-ct \| \| | qpack/static_table.go entry 87. |
| `A-93` | informative | Static index 88 is name 'forwarded' with empty value. <br>_(endpoint [CLIENT])_ | \| 88 \| forwarded \| \| | qpack/static_table.go entry 88. |
| `A-94` | informative | Static index 89 is name 'if-range' with empty value. <br>_(endpoint [CLIENT])_ | \| 89 \| if-range \| \| | qpack/static_table.go entry 89. |
| `A-95` | informative | Static index 90 is name 'origin' with empty value. <br>_(endpoint [CLIENT])_ | \| 90 \| origin \| \| | qpack/static_table.go entry 90. |
| `A-96` | informative | Static index 91 is name 'purpose' with value 'prefetch'. <br>_(endpoint [CLIENT])_ | \| 91 \| purpose \| prefetch \| | qpack/static_table.go entry 91. |
| `A-97` | informative | Static index 92 is name 'server' with empty value. <br>_(endpoint [CLIENT])_ | \| 92 \| server \| \| | qpack/static_table.go entry 92. |
| `A-98` | informative | Static index 93 is name 'timing-allow-origin' with value '*'. <br>_(endpoint [CLIENT])_ | \| 93 \| timing-allow-origin \| * \| | qpack/static_table.go entry 93. |
| `A-99` | informative | Static index 94 is name 'upgrade-insecure-requests' with value '1'. <br>_(endpoint [CLIENT])_ | \| 94 \| upgrade-insecure-requests \| 1 \| | qpack/static_table.go entry 94. |
| `A-100` | informative | Static index 95 is name 'user-agent' with empty value. <br>_(endpoint [CLIENT])_ | \| 95 \| user-agent \| \| | qpack/static_table.go entry 95; encoder should name-reference this for the client's UA header. |
| `A-101` | informative | Static index 96 is name 'x-forwarded-for' with empty value. <br>_(endpoint [CLIENT])_ | \| 96 \| x-forwarded-for \| \| | qpack/static_table.go entry 96. |
| `A-102` | informative | Static index 97 is name 'x-frame-options' with value 'deny' (lowercase). <br>_(endpoint [CLIENT])_ | \| 97 \| x-frame-options \| deny \| | qpack/static_table.go entry 97: lowercase 'deny'. |
| `A-103` | informative | Static index 98 is name 'x-frame-options' with value 'sameorigin' (lowercase, one word) and is the final static-table entry. <br>_(endpoint [CLIENT])_ | \| 98 \| x-frame-options \| sameorigin \| | qpack/static_table.go entry 98: lowercase single-word 'sameorigin'; confirm table ends here (no HPACK 61-entry or 1-based leftovers). |

