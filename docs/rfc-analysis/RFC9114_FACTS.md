# RFC 9114 (HTTP/3) - normative facts for client conformance

_Generated from local RFC source via FABLE extraction + double independent Opus verification (default-refute). Every quote is verbatim from the fetched RFC 9114 text and confirmed against the source by two verifiers. Rows flagged VERIFY had a verifier disagreement -- correction inline, needs a manual source check._

### 9114 section 1-2 - RFC 9114 — Introduction, Delegation to QUIC, HTTP/3 Protocol Overview, Document Organization, Conventions and Terminology

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `1-2-1` | informative | HTTP/3 carries the same HTTP semantics as prior versions, over the QUIC transport protocol instead of TCP. <br>_(all [CLIENT])_ | HTTP/3 supports the same semantics over a new transport protocol: QUIC. | http3/client.go + http3/request.go/response.go — request/response semantics (RFC 9110) must map unchanged onto quic/ streams; no H3-specific semantic deviations. |
| `1-2-2` | informative | HTTP/3 depends on QUIC for confidentiality, integrity, peer authentication, and reliable in-order per-stream delivery; the HTTP layer does not re-implement these. <br>_(endpoint [CLIENT])_ | HTTP/3 relies on QUIC to provide confidentiality and integrity protection of data; peer authentication; and reliable, in-order, per-stream delivery. | quic/ — TLS 1.3 handshake + AEAD packet protection (RFC 9001) and in-order stream reassembly must be complete below http3/; http3/ must assume ordered bytes per stream. |
| `1-2-3` | informative | Stream lifetime and flow control are delegated to QUIC; HTTP/3 applies only a binary framing layer on each stream (no HTTP-layer flow control or stream states like HTTP/2). <br>_(endpoint [CLIENT])_ | While delegating stream lifetime and flow-control issues to QUIC, a binary framing similar to the HTTP/2 framing is used on each stream. | http3/frame.go must implement framing only; verify no WINDOW_UPDATE-style logic exists in http3/ — flow control and stream close/reset live entirely in quic/. |
| `1-2-4` | informative | The client initiates HTTP/3 by opening a QUIC connection to a known HTTP/3 endpoint; QUIC supplies protocol negotiation, multiplexing, and flow control. <br>_(client [CLIENT])_ | Once a client knows that an HTTP/3 server exists at a certain endpoint, it opens a QUIC connection. | http3/dial.go — Dial must establish the quic/ connection (including ALPN protocol negotiation) before any HTTP/3 stream or frame activity. |
| `1-2-5` | informative | The basic unit of HTTP/3 communication within each stream is the frame; each frame type serves a distinct purpose. <br>_(all [CLIENT])_ | Within each stream, the basic unit of HTTP/3 communication is a frame (Section 7.2). | http3/frame.go — codec must parse and emit all traffic on HTTP/3 streams as frames; no raw non-framed bytes on framed stream types. |
| `1-2-6` | informative | Frames that apply to the entire connection (e.g. SETTINGS, GOAWAY) are carried on a dedicated control stream, not on request streams. <br>_(endpoint [CLIENT])_ | Frames that apply to the entire connection are conveyed on a dedicated control stream. | http3/control.go — SETTINGS/GOAWAY/MAX_PUSH_ID must be sent and expected only on the control unidirectional stream; http3/stream.go must reject them on request streams (per Section 7 rules). |
| `1-2-7` | informative | Request multiplexing is performed via the QUIC stream abstraction rather than an HTTP-layer multiplexing scheme. <br>_(all [CLIENT])_ | Multiplexing of requests is performed using the QUIC stream abstraction | http3/client.go + quic/ streams — concurrent requests must each be a distinct QUIC bidirectional stream; no HTTP/2-style stream-ID framing inside http3/frame.go. |
| `1-2-8` | informative | Each request-response exchange occupies exactly one QUIC stream; streams are never reused for another request. <br>_(all [CLIENT])_ | Each request-response pair consumes a single QUIC stream. | http3/stream.go + request.go/response.go — one client-initiated bidirectional stream per Do; verify no stream reuse across requests and the stream ends with the response. |
| `1-2-9` | informative | Streams are mutually independent: a blocked or loss-affected stream must not prevent progress on other streams. <br>_(all [CLIENT])_ | Streams are independent of each other, so one stream that is blocked or suffers packet loss does not prevent progress on other streams. | quic/ — per-stream reassembly/loss recovery must not head-of-line block other streams; http3/ must not serialize request streams behind one another (check any shared locks on the read path). |
| `1-2-10` | informative | Server push exists in HTTP/3 and is managed via the PUSH_PROMISE, MAX_PUSH_ID, and CANCEL_PUSH frames. <br>_(all [CLIENT])_ | Several HTTP/3 frames are used to manage server push, such as PUSH_PROMISE, MAX_PUSH_ID, and CANCEL_PUSH. | http3/frame.go — frame-type codepoints for PUSH_PROMISE/MAX_PUSH_ID/CANCEL_PUSH must exist; since this client disables push (never sends MAX_PUSH_ID), verify it still parses/handles these frames per Sections 4.6/7.2 rather than treating them as unknown. |
| `1-2-11` | informative | HTTP/3 uses QPACK, not HPACK, for field compression, because HPACK requires in-order delivery of compressed field sections which QUIC does not guarantee across streams. <br>_(all [CLIENT])_ | HTTP/3 replaces HPACK with QPACK ([QPACK]). | http3/request.go + response.go must encode/decode field sections via qpack/ only; verify hpack/ is never imported on the H3 path. |
| `1-2-12` | informative | QPACK dynamic-table state is modified only via separate unidirectional (encoder/decoder) streams; encoded field sections reference table state without modifying it. <br>_(all [CLIENT])_ | QPACK uses separate unidirectional streams to modify and track field table state, while encoded field sections refer to the state of the table without modifying it. | qpack/ instructions + http3/control.go/dial.go — verify the client opens QPACK encoder/decoder unidirectional streams (or correctly operates static-table-only) and that field-section decoding in qpack/decoder never mutates table state. |
| `1-2-13` | informative | BCP 14 keywords (MUST, SHOULD, MAY, etc.) are normative only when they appear in all capitals. <br>_(all [not-client])_ | are to be interpreted as described in BCP 14 [RFC2119] [RFC8174] when, and only when, they appear in all capitals | docs/rfc-analysis catalog convention only — lowercase 'must/should' in RFC 9114 prose is not normative; no code check. |
| `1-2-14` | informative | All HTTP/3 integer fields (frame type, length, stream type, IDs, settings) use the QUIC variable-length integer encoding from RFC 9000. <br>_(all [CLIENT])_ | This document uses the variable-length integer encoding from [QUIC-TRANSPORT]. | internal/bytesx varint codec + http3/frame.go — verify every H3 wire integer (frame type, frame length, stream type, push IDs, setting IDs/values) is read/written as a QUIC varint, including non-minimal-encoding acceptance rules from RFC 9000. |
| `1-2-15` | informative | QUIC also incorporates TLS 1.3 at the transport layer, providing the confidentiality/integrity HTTP/3 depends on. <br>_(all [CLIENT])_ | QUIC also incorporates TLS 1.3 ([TLS]) at the transport layer | quic/ handshake — TLS 1.3 only (RFC 9001); verify no fallback to earlier TLS versions is possible in the quic/ TLS configuration used by http3/dial.go. |
| `1-2-16` | informative | The client is defined as the endpoint that initiates the HTTP/3 connection, sends HTTP requests, and receives HTTP responses. <br>_(client [CLIENT])_ | client: The endpoint that initiates an HTTP/3 connection. Clients send HTTP requests and receive HTTP responses. | http3/client.go — role is fixed as connection initiator; verify the client never accepts inbound bidirectional streams as requests (server-initiated bidirectional streams are an error per Section 6.1). |
| `1-2-17` | informative | The server is defined as the endpoint that accepts the HTTP/3 connection, receives requests, and sends responses. <br>_(server [not-client])_ | server: The endpoint that accepts an HTTP/3 connection. Servers receive HTTP requests and send HTTP responses. | Terminology only; no client-side code check beyond treating the peer as the sole response sender (http3/response.go). |
| `1-2-18` | informative | A connection error is one that affects the entire HTTP/3 connection (terminating it), as distinct from a stream error. <br>_(all [CLIENT])_ | connection error: An error that affects the entire HTTP/3 connection. | http3/ error handling — connection-level H3 errors must close the whole QUIC connection via quic/ CONNECTION_CLOSE (application close) with the H3 error code, not merely reset one stream; verify the error-classification split in http3/control.go/frame.go/stream.go. |
| `1-2-19` | informative | A stream error is an application-level error scoped to a single stream; the connection survives it. <br>_(all [CLIENT])_ | stream error: An application-level error on the individual stream. | **VERIFY** http3/stream.go — stream errors must abort only that request stream (quic/ RESET_STREAM + STOP_SENDING with the H3 error code) while the connection and other streams continue. -- Quote is verbatim (line 322: "stream error: An application-level error on the individual stream."), level and roles are fine, but the requirement's added gloss "the connection survives it" is not what RFC 9114 says and is contradicted by Section 8 (line 1858): "An endpoint MAY choose to treat a stream error as a connection error under certain circumstances, closing the entire connection in response to a condition on a single stream." Fix the requirement to: "A stream error is an application-level error on the individual stream (Section 2.2); by itself it aborts only that stream, though an endpoint MAY escalate it to a connection error (Section 8)." The reconcile note should likewise not assert that the connection always continues. \|\| Quote is verbatim (line 322) and the level is right, but the requirement's added clause 'the connection survives it' overstates the definition and conflicts with Section 8, lines 1858-1860: 'An endpoint MAY choose to treat a stream error as a connection error under certain circumstances, closing the entire connection in response to a condition on a single stream.' The RFC 9114 Section 2.2 definition says only that the error is application-level and scoped to the individual stream; it does not guarantee connection survival. This propagates into a wrong reconcile instruction ('stream errors must abort only that request stream'), which would forbid a permitted escalation. Fix: restate as 'A stream error is an application-level error on an individual stream; unlike a connection error it is scoped to that stream, though an endpoint MAY escalate it to a connection error (Section 8).' and soften the reconcile to 'the default handling resets only that request stream (QUIC RESET_STREAM/STOP_SENDING with an H3 error code); escalation to connection close is permitted but must be deliberate.' |
| `1-2-20` | informative | A frame is the smallest unit of stream communication and consists of a header plus a variable-length byte sequence structured according to its frame type. <br>_(all [CLIENT])_ | frame: The smallest unit of communication on a stream in HTTP/3, consisting of a header and a variable-length sequence of bytes structured according to the frame type. | http3/frame.go — frame layout = header (varint type + varint length) then type-structured payload; verify parser enforces payload structure per frame type. |
| `1-2-21` | informative | An HTTP/3 connection is specifically a QUIC connection whose negotiated application protocol is HTTP/3. <br>_(all [CLIENT])_ | HTTP/3 connection: A QUIC connection where the negotiated application protocol is HTTP/3. | http3/dial.go — ALPN must negotiate the HTTP/3 token ("h3") during the quic/ TLS handshake before the connection is treated as HTTP/3; fail dial if a different protocol is negotiated. |
| `1-2-22` | informative | A stream is a bidirectional or unidirectional QUIC bytestream; HTTP/3 defines multiple distinct stream types within one connection. <br>_(all [CLIENT])_ | stream: A bidirectional or unidirectional bytestream provided by the QUIC transport. | quic/ must expose both bidirectional and unidirectional stream APIs; http3/control.go + stream.go must dispatch by stream type (request, control, QPACK encoder/decoder, push) per Section 6. |
| `1-2-23` | informative | An abort is an abrupt termination of a connection or stream, possibly due to an error condition. <br>_(all [CLIENT])_ | abort: An abrupt termination of a connection or stream, possibly due to an error condition. | http3/stream.go + quic/ — 'abort' in later sections maps to RESET_STREAM/STOP_SENDING (stream) or immediate CONNECTION_CLOSE (connection); verify both abrupt paths exist and carry H3 error codes. |
| `1-2-24` | informative | Frame names prefaced with "QUIC" refer to transport frames from RFC 9000; unprefixed frame names refer to HTTP/3 frames defined in Section 7.2 — the two codepoint spaces are distinct. <br>_(all [CLIENT])_ | Where frames from [QUIC-TRANSPORT] are referenced, the frame name will be prefaced with "QUIC". | http3/frame.go vs quic/ frame types — verify HTTP/3 frame-type constants are a separate namespace from QUIC transport frame types and are never conflated in parsing or error reporting. |
| `1-2-25` | informative | Sender and receiver denote the endpoint transmitting or receiving frames; an endpoint is either client or server, and a peer is the remote endpoint. <br>_(all [not-client])_ | receiver: An endpoint that is receiving frames. | Terminology only — later sender/receiver requirements apply to the client in both roles; no direct code check. -- **VERIFY** client_relevant should be true (both re-verification passes: sender/receiver/endpoint/peer are the role vocabulary that binds this RFC's client obligations); quote and level confirmed verbatim. |
| `1-2-26` | informative | The terms "content", "resource", "message", "user agent", "origin server", "gateway", "intermediary", "proxy", and "tunnel" carry their RFC 9110 (HTTP semantics) definitions. <br>_(all [CLIENT])_ | The term "content" is defined in Section 6.4 of [HTTP]. | http3/request.go/response.go — body handling terminology ('content') must align with RFC 9110 §6.4 (e.g. Content-Length vs actual content rules applied in validate.go/response.go). |
| `1-2-27` | informative | Packet diagrams in the RFC use the RFC 9000 Section 1.3 notation for field order and size. <br>_(all [not-client])_ | Packet diagrams in this document use the format defined in Section 1.3 of [QUIC-TRANSPORT] | Documentation-reading convention only — affects how later wire diagrams (frame layouts in Section 7) are interpreted when reconciling http3/frame.go; no direct code check. |

### 9114 section 3 - Connection Setup and Management

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `3-1` | MUST | On receiving the server certificate in the TLS handshake, the client must verify it is an acceptable match for the URI's origin server per RFC 9110 Section 4.3.4. <br>_(client [CLIENT])_ | the client MUST verify that the certificate is an acceptable match for the URI's origin server | http3/dial.go + quic/ TLS handshake: confirm crypto/tls verification is on by default (ServerName set, InsecureSkipVerify only opt-in per gosec G402 tuning). |
| `3-2` | MUST NOT | If the certificate cannot be verified against the URI's origin server, the client must not treat the server as authoritative for that origin. <br>_(client [CLIENT])_ | the client MUST NOT consider the server authoritative for that origin | http3/dial.go: a TLS verification failure in the quic/ handshake must abort Dial with an error, never yield a usable Client. |
| `3-3` | MAY | A client may access an https resource by resolving the host, establishing a QUIC connection to that address/port with certificate validation, and sending HTTP/3 requests over it. <br>_(client [CLIENT])_ | A client MAY attempt access to a resource with an "https" URI by resolving the host identifier to an IP address, establishing a QUIC connection to that address | http3/dial.go: this is the direct-dial path http3.Dial implements; confirm it validates the cert before requests flow (http3/client.go Do). |
| `3-4` | informative | Unless another selection mechanism is used, the ALPN token "h3" selects HTTP/3 during the TLS handshake. <br>_(client,server [CLIENT])_ | Unless some other mechanism is used to select HTTP/3, the token "h3" is used in the Application-Layer Protocol Negotiation | http3/dial.go: tls.Config.NextProtos must contain exactly "h3"; verify negotiated protocol is checked post-handshake in quic/ handshake completion. |
| `3-5` | SHOULD | If QUIC connection establishment fails (e.g., UDP blocked), clients should fall back to TCP-based HTTP versions. <br>_(client [CLIENT])_ | clients SHOULD attempt to use TCP-based versions of HTTP in this case | http3/dial.go: no h2/h1 fallback exists (documented non-goal); confirm Dial surfaces a distinguishable error so the caller/client layer can fall back. |
| `3-6` | MAY | Servers may serve HTTP/3 on any UDP port; alt-svc advertisements always carry an explicit port and URIs carry an explicit or scheme-default port. <br>_(server [CLIENT])_ | Servers MAY serve HTTP/3 on any UDP port; an alternative service advertisement always includes an explicit port | http3/dial.go: addr parsing must accept any host:port, defaulting to 443 only when the port is absent. |
| `3-7` | informative | An origin can advertise an equivalent HTTP/3 endpoint via the Alt-Svc header field or HTTP/2 ALTSVC frame using the "h3" ALPN token. <br>_(origin [CLIENT])_ | An HTTP origin can advertise the availability of an equivalent HTTP/3 endpoint via the Alt-Svc HTTP response header field | http3/client.go + client layer: Alt-Svc discovery is not implemented; acceptable (client action is MAY, see 3-8) — document as non-goal. |
| `3-8` | MAY | On receipt of an Alt-Svc record indicating HTTP/3, a client may attempt a QUIC connection to the indicated host and port. <br>_(client [CLIENT])_ | a client MAY attempt to establish a QUIC connection to the indicated host and port | http3/dial.go: pure MAY — direct dial by caller-supplied addr satisfies it; no Alt-Svc parsing obligation. |
| `3-9` | informative | HTTP/3 cannot be used for direct access to the authoritative server of an "http"-scheme URI, since http-scheme authority is tied to TCP. <br>_(client [CLIENT])_ | HTTP/3 cannot be used for direct access to the authoritative server for a resource identified by an "http" URI | http3/request.go + http3/validate.go: check what :scheme values are emitted/allowed; http-scheme requests must not be sent by direct dial. |
| `3-10` | MUST | Before making requests for an origin whose scheme is not "https", the client must ensure the server is willing to serve that scheme. <br>_(client [CLIENT])_ | the client MUST ensure the server is willing to serve that scheme | http3/validate.go / request.go: simplest conformance is restricting :scheme to https (or requiring explicit caller opt-in); verify current scheme handling. |
| `3-11` | informative | HTTP/3 runs over QUIC version 1; use of other QUIC versions may be defined by future specifications. <br>_(all [CLIENT])_ | HTTP/3 relies on QUIC version 1 as the underlying transport. | quic/: confirm version field is fixed at 0x00000001 in long-header packet emission and that other versions are not negotiated. |
| `3-12` | MUST | HTTP/3 clients must support a mechanism to indicate the target host to the server during the TLS handshake. <br>_(client [CLIENT])_ | HTTP/3 clients MUST support a mechanism to indicate the target host to the server during the TLS handshake | http3/dial.go: tls.Config.ServerName must be populated (from addr host or caller config) before the quic/ TLS 1.3 handshake starts. |
| `3-13` | MUST | When the server is identified by a domain name, clients must send the SNI TLS extension unless an alternative target-host mechanism is used. <br>_(client [CLIENT])_ | clients MUST send the Server Name Indication (SNI; [RFC6066]) TLS extension unless an alternative mechanism to indicate the target host is used | http3/dial.go + quic/ handshake: crypto/tls sends SNI iff ServerName is non-empty and not an IP literal — verify ServerName derivation strips port and is not dropped for domain names. |
| `3-14` | informative | During connection establishment, HTTP/3 support is indicated by selecting ALPN token "h3" in the TLS handshake. <br>_(client,server [CLIENT])_ | HTTP/3 support is indicated by selecting the ALPN token "h3" in the TLS handshake | http3/dial.go: offer "h3" in NextProtos; quic/ handshake should fail the connection if the server selects anything else. |
| `3-15` | MAY | Support for other application-layer protocols may be offered in the same TLS handshake alongside h3. <br>_(client,server [not-client])_ | Support for other application-layer protocols MAY be offered in the same handshake. | **VERIFY** http3/dial.go: offering only "h3" is conformant; no action needed. -- Quote and level are exact (RFC 9114 lines 423-424: "Support for other application-layer protocols MAY be offered in the same handshake.", a genuine MAY). The defect is client_relevant=false while applies_to includes "client": this permission governs exactly what an HTTP/3 client puts in tls.Config.NextProtos during its own handshake, i.e. client-controlled behaviour. The catalog sets client_relevant=true even for pure server/origin-role facts in this same section (3-6, 3-7, 3-26, 3-28), so a client-role MAY about the client's own ALPN offer cannot consistently be false. Fix: set client_relevant=true, keeping the reconcile note as-is (offering only "h3" already satisfies the MAY, so no code change is required — it is relevant-but-satisfied, not irrelevant). |
| `3-16` | MUST | After the QUIC connection is established, each endpoint must send a SETTINGS frame as the initial frame of its HTTP control stream. <br>_(endpoint [CLIENT])_ | a SETTINGS frame MUST be sent by each endpoint as the initial frame of their respective HTTP control stream | http3/control.go: on setup, open the unidirectional control stream (type 0x00 via quic/ uni-stream) and write SETTINGS before any other frame; also enforce peer's first control frame is SETTINGS. |
| `3-17` | informative | HTTP/3 connections are persistent across multiple requests; clients are expected to keep them open until no further communication with the server is needed. <br>_(client [CLIENT])_ | HTTP/3 connections are persistent across multiple requests. | http3/client.go: Client must support sequential requests on one connection without re-dialing; verify request-stream lifecycle in stream.go doesn't tear down the conn. |
| `3-18` | MAY | An existing connection to a server endpoint may be reused for requests with multiple different URI authority components. <br>_(client [CLIENT])_ | this connection MAY be reused for requests with multiple different URI authority components | http3/client.go + client-layer pool: if cross-authority reuse is ever done, it must be gated by 3-19; if the pool keys strictly per-authority, this MAY is unexercised (conformant). |
| `3-19` | MUST | To reuse an existing connection for a new origin, the client must validate the server's certificate for the new origin per RFC 9110 Section 4.3.4, which requires retaining the certificate. <br>_(client [CLIENT])_ | clients MUST validate the certificate presented by the server for the new origin server using the process described in Section 4.3.4 of [HTTP] | http3/client.go: if connection reuse across origins exists, check the quic/ handshake retains the peer cert chain and re-verifies per new hostname; otherwise confirm pool never coalesces origins. |
| `3-20` | MUST NOT | If the certificate is not acceptable for the new origin for any reason, the connection must not be reused for that origin. <br>_(client [CLIENT])_ | the connection MUST NOT be reused | http3/client.go / client pool: on cert-mismatch for a candidate origin, fall through to a fresh dial — never route the request over the mismatched conn. |
| `3-21` | SHOULD | When the certificate is unacceptable for the new origin, a new connection should be established for that origin. <br>_(client [CLIENT])_ | a new connection SHOULD be established for the new origin | client-layer pool over http3: verify the miss path dials a new conn rather than failing the request outright. |
| `3-22` | SHOULD | If the certificate-verification failure reason might apply to other origins already associated with the connection, the client should revalidate the certificate for those origins. <br>_(client [CLIENT])_ | the client SHOULD revalidate the server certificate for those origins | http3/client.go: only applies if multi-origin coalescing is implemented (e.g., expiry/revocation invalidates all coalesced origins); N/A if pool is per-origin. |
| `3-23` | SHOULD NOT | Clients should not open more than one HTTP/3 connection to a given IP address and UDP port (however derived: URI, alt-svc, proxy, or name resolution). <br>_(client [CLIENT])_ | Clients SHOULD NOT open more than one HTTP/3 connection to a given IP address and UDP port | client-layer pool / http3/client.go: check pool keying and dial dedup — concurrent Do calls to the same addr should share one conn, not race-dial several. |
| `3-24` | MAY | A client may open multiple HTTP/3 connections to the same IP and UDP port when using different transport or TLS configurations. <br>_(client [CLIENT])_ | A client MAY open multiple HTTP/3 connections to the same IP address and UDP port using different transport or TLS configurations | client-layer pool: if pool key includes TLS/transport config identity, this is permitted separation; verify key composition. |
| `3-25` | SHOULD | A client should avoid creating multiple connections to the same IP and port with the same configuration. <br>_(client [CLIENT])_ | SHOULD avoid creating multiple connections with the same configuration | client-layer pool over http3: singleflight/dedup on dial for identical (addr, config) keys; check for thundering-herd dials under concurrency. |
| `3-26` | informative | Servers are encouraged to keep HTTP/3 connections open as long as possible but are permitted to terminate idle connections. <br>_(server [CLIENT])_ | Servers are encouraged to maintain open HTTP/3 connections for as long as possible but are permitted to terminate idle connections if necessary | http3/client.go + quic/: client must tolerate server-initiated close of an idle conn — next Do on a dead conn should surface a clean, retryable error, not hang or panic. |
| `3-27` | SHOULD | When either endpoint closes the HTTP/3 connection, the terminating endpoint should first send a GOAWAY frame so both sides can determine which frames were processed. <br>_(endpoint [CLIENT])_ | the terminating endpoint SHOULD first send a GOAWAY frame | http3/control.go + client.go Close path: client-side graceful close should emit GOAWAY on the control stream before quic/ CONNECTION_CLOSE; also handle inbound GOAWAY per Section 5.2. |
| `3-28` | informative | A server unwilling to have a connection reused for a particular origin can respond 421 (Misdirected Request), indicating it is not authoritative for that request. <br>_(server [CLIENT])_ | can indicate that it is not authoritative for a request by sending a 421 (Misdirected Request) status code | http3/response.go + client-layer retry: 421 must pass through unmangled; if origin coalescing exists, a 421 should mark that origin non-reusable on the conn and permit retry on a fresh connection. |

### 9114 section 4.1 - HTTP Message Framing; Request Cancellation and Rejection; Malformed Requests and Responses

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `4.1-1` | MUST | A client must send only one request per request stream (client-initiated bidirectional QUIC stream). <br>_(client [CLIENT])_ | A client MUST send only a single request on a given stream. | http3/client.go + http3/stream.go: every Do/request opens a fresh client-initiated bidi QUIC stream; no second request ever written to an existing stream. |
| `4.1-2` | informative | A request is carried on a request stream, which is a client-initiated bidirectional QUIC stream. <br>_(client [CLIENT])_ | A client sends an HTTP request on a request stream, which is a client-initiated bidirectional QUIC stream | http3/dial.go + quic/ stream open: request stream is opened as client-initiated bidirectional (stream IDs 0,4,8,...). |
| `4.1-3` | informative | The server sends zero or more interim (1xx) responses on the request stream, then exactly one final response. <br>_(server [CLIENT])_ | A server sends zero or more interim HTTP responses on the same stream as the request, followed by a single final HTTP response | http3/response.go: response-read loop must consume any number of 1xx HEADERS before the final response HEADERS. |
| `4.1-4` | informative | Pushed responses arrive on server-initiated unidirectional QUIC streams (push streams), formatted like standard responses. <br>_(server [CLIENT])_ | Pushed responses are sent on a server-initiated unidirectional QUIC stream | http3/control.go unidirectional-stream dispatch: verify push stream type (0x01) is recognized and handled (or cleanly refused if push is not offered via MAX_PUSH_ID). |
| `4.1-5` | MUST | On a given stream, multiple requests, or an additional response after the final response, must be treated as malformed. <br>_(receiver [CLIENT])_ | receipt of multiple requests or receipt of an additional HTTP response following a final HTTP response MUST be treated as malformed | http3/response.go + stream.go: after the final response (and optional trailers), any further response HEADERS => malformed => stream error H3_MESSAGE_ERROR per 4.1.2. |
| `4.1-6` | informative | An HTTP message is: header section as a single HEADERS frame, then optional content as a series of DATA frames, then optional trailer section as a single HEADERS frame. <br>_(all [CLIENT])_ | the header section, including message control data, sent as a single HEADERS frame | http3/request.go (emit order) + http3/response.go (parse order): HEADERS, DATA*, optional trailing HEADERS — nothing else in the message. |
| `4.1-7` | informative | The trailer section, if present, is exactly one HEADERS frame (so at most two HEADERS frames per message). <br>_(all [CLIENT])_ | optionally, the trailer section, if present, sent as a single HEADERS frame | http3/response.go + stream.go: enforce at most one trailer HEADERS; a second HEADERS after trailers is an invalid sequence (H3_FRAME_UNEXPECTED). |
| `4.1-8` | MUST | An invalid sequence of frames on a request/push stream must be treated as a connection error H3_FRAME_UNEXPECTED. <br>_(receiver [CLIENT])_ | Receipt of an invalid sequence of frames MUST be treated as a connection error of type H3_FRAME_UNEXPECTED. | http3/stream.go frame-sequence state machine: violation closes the whole QUIC connection with H3_FRAME_UNEXPECTED (0x105) — connection error, not stream reset. |
| `4.1-9` | informative | DATA before any HEADERS, or HEADERS/DATA after the trailing HEADERS frame, are invalid sequences. <br>_(receiver [CLIENT])_ | a DATA frame before any HEADERS frame, or a HEADERS or DATA frame after the trailing HEADERS frame, is considered invalid | http3/stream.go: state machine must cover both cases — DATA-first and HEADERS/DATA-after-trailers — each => H3_FRAME_UNEXPECTED connection error. |
| `4.1-10` | MAY | A server may send PUSH_PROMISE frames before, after, or interleaved with response-message frames; they are not part of the response. <br>_(server [CLIENT])_ | A server MAY send one or more PUSH_PROMISE frames before, after, or interleaved with the frames of a response message. | http3/stream.go: client must accept PUSH_PROMISE at any position on a request stream without disturbing the HEADERS/DATA/trailers message state. |
| `4.1-11` | MUST | PUSH_PROMISE frames are forbidden on push streams; a pushed response containing one is a connection error H3_FRAME_UNEXPECTED. <br>_(receiver [CLIENT])_ | a pushed response that includes PUSH_PROMISE frames MUST be treated as a connection error of type H3_FRAME_UNEXPECTED | http3/ push-stream reader (control.go/stream.go): PUSH_PROMISE seen on a push stream => connection error 0x105. |
| `4.1-12` | MAY | Frames of unknown types, including reserved (grease) frames, may appear anywhere on a request or push stream and must be tolerated. <br>_(sender [CLIENT])_ | Frames of unknown types (Section 9), including reserved frames (Section 7.2.8) MAY be sent on a request or push stream | **VERIFY** http3/frame.go + stream.go: unknown frame types (incl. reserved 0x21+0x1f*N grease codepoints) are read and their payload discarded, never an error, at any position. -- Quote and level are right (line 531-533 grants a sender-side MAY), but the requirement text asserts a receiver obligation — 'must be tolerated' — that §4.1 never states and that applies_to=["sender"] does not cover. §4.1 only adds the non-normative 'Other frame types, especially unknown frame types, might be permitted subject to their own rules; see Section 9' (lines 520-522). The tolerance MUST lives in §9, line 1952: 'Implementations MUST ignore unknown or unsupported values in all extensible protocol elements.' Fix: either trim the requirement to the sender permission ('unknown/reserved frames MAY be sent anywhere on a request or push stream', applies_to ["sender"], level MAY), or split off a second fact for the receiver duty citing §9 with level MUST and applies_to ["receiver"]. The reconcile (client discards unknown payloads, never errors) belongs on that second fact, not this one. |
| `4.1-13` | informative | QPACK dynamic-table updates referenced by HEADERS/PUSH_PROMISE must be received and processed before the message can be consumed. <br>_(receiver [CLIENT])_ | they must be received and processed before the message can be consumed | qpack/ decoder + http3/stream.go: header blocks whose Required Insert Count exceeds current dynamic-table state must block until encoder-stream inserts arrive (memory notes dynamic QPACK exists but was never conformance-audited). |
| `4.1-14` | MUST NOT | Transfer codings are not defined in HTTP/3; the Transfer-Encoding header field must not be used. <br>_(all [CLIENT])_ | the Transfer-Encoding header field MUST NOT be used | http3/validate.go + request.go: never emit Transfer-Encoding on requests; treat its presence in a received response as a prohibited field => malformed (H3_MESSAGE_ERROR). |
| `4.1-15` | MAY | A response may consist of multiple messages only when interim (1xx) responses precede the final response to the same request. <br>_(server [CLIENT])_ | A response MAY consist of multiple messages when and only when one or more interim responses (1xx; see Section 15.2 of [HTTP]) precede a final response | http3/response.go: only 1xx statuses may precede the final response; a second final-status HEADERS is malformed (see 4.1-5). |
| `4.1-16` | informative | Interim (1xx) responses carry no content and no trailer section. <br>_(server [CLIENT])_ | Interim responses do not contain content or trailer sections. | http3/response.go: DATA (or trailers) between an interim HEADERS and the final HEADERS is invalid — verify the parser rejects it. |
| `4.1-17` | informative | One request/response exchange fully consumes the client-initiated bidirectional stream; streams are never reused. <br>_(all [CLIENT])_ | An HTTP request/response exchange fully consumes a client-initiated bidirectional QUIC stream. | http3/stream.go: stream objects map 1:1 to exchanges; no pooling/reuse of a QUIC stream across requests. |
| `4.1-18` | MUST | After sending its request, the client must close the stream for sending (FIN). <br>_(client [CLIENT])_ | After sending a request, a client MUST close the stream for sending. | http3/stream_body.go: FIN is written when the request body completes, unconditionally, on every code path. |
| `4.1-19` | MUST NOT | Except for CONNECT, the client must not make closing its sending side dependent on receiving a response. <br>_(client [CLIENT])_ | clients MUST NOT make stream closure dependent on receiving a response to their request | http3/stream_body.go: FIN must not wait on response arrival; only the CONNECT path (Section 4.4) may hold the send side open. |
| `4.1-20` | MUST | After sending the final response, the server must close its sending side, fully closing the QUIC stream. <br>_(server [CLIENT])_ | After sending a final response, the server MUST close the stream for sending. | http3/response.go + stream_body.go: client relies on peer FIN to delimit the final response; a response body ending without FIN is truncation, not success. |
| `4.1-21` | informative | Stream closure (FIN) marks the end of the final HTTP message. <br>_(all [CLIENT])_ | When a stream is closed, this indicates the end of the final HTTP message. | http3/stream_body.go: EOF on body = peer FIN after last frame; verify trailers-at-FIN handling and that message-complete is only signalled at FIN. |
| `4.1-22` | SHOULD | Endpoints should process partial HTTP messages incrementally once enough has been received to make progress. <br>_(endpoint [CLIENT])_ | endpoints SHOULD begin processing partial HTTP messages once enough of the message has been received to make progress | http3/client.go + stream_body.go: response headers/body are surfaced to the caller as they arrive (streaming), not buffered until FIN. |
| `4.1-23` | SHOULD | If a client-initiated stream ends without enough of the request to produce a response, the server should abort its response stream with H3_REQUEST_INCOMPLETE. <br>_(server [CLIENT])_ | the server SHOULD abort its response stream with the error code H3_REQUEST_INCOMPLETE | http3/ error-code constants: define H3_REQUEST_INCOMPLETE (0x10D) and map a received RESET_STREAM with it to a distinct typed client error. |
| `4.1-24` | MAY | A server that no longer needs the rest of the request may abort reading it, send a complete response, and cleanly close its sending side. <br>_(server [CLIENT])_ | it MAY abort reading the request stream, send a complete response, and cleanly close the sending part of the stream | http3/stream_body.go: a STOP_SENDING/abort on the request-write side while the response is still arriving must not fail the exchange — keep reading the response. |
| `4.1-25` | SHOULD | H3_NO_ERROR should be the code used when asking the client to stop sending on the request stream. <br>_(server [CLIENT])_ | The error code H3_NO_ERROR SHOULD be used when requesting that the client stop sending on the request stream. | quic/ STOP_SENDING surfacing + http3/stream_body.go: STOP_SENDING with H3_NO_ERROR (0x100) is benign — stop uploading, do not report an error to the caller. |
| `4.1-26` | MUST NOT | Clients must not discard a complete response merely because their request was terminated abruptly. <br>_(client [CLIENT])_ | Clients MUST NOT discard complete responses as a result of having their request terminated abruptly | http3/stream_body.go: reset/STOP_SENDING of the request-send side must not drop a fully received response — deliver it to the caller. |
| `4.1-27` | SHOULD | If the server sends a response but does not abort reading, the client should keep sending the request content and close normally. <br>_(client [CLIENT])_ | clients SHOULD continue sending the content of the request and close the stream normally | http3/stream_body.go: an early/complete response without STOP_SENDING must not cancel the in-progress body upload; finish and FIN normally. |
| `4.1-28` | MAY | Either endpoint may cancel a request once the request stream is open. <br>_(endpoint [CLIENT])_ | the request MAY be cancelled by either endpoint | http3/client.go: context cancellation must map to request cancellation on the QUIC stream. |
| `4.1-29` | RECOMMENDED | Servers are recommended to answer with a status code rather than cancel a request they have begun processing. <br>_(server [not-client])_ | it is RECOMMENDED that servers send an HTTP response with an appropriate status code rather than cancelling a request it has already begun processing | Server-side only; no client code path in http3/. |
| `4.1-30` | SHOULD | Cancellation should abruptly terminate all still-open stream directions: reset the sending parts and abort reading the receiving parts. <br>_(endpoint [CLIENT])_ | Implementations SHOULD cancel requests by abruptly terminating any directions of a stream that are still open | http3/stream.go cancel path + quic/: cancel issues RESET_STREAM on the send side AND STOP_SENDING on the receive side (both, if both still open). |
| `4.1-31` | SHOULD | When rejecting a request without any application processing, the server should abort its response stream with H3_REQUEST_REJECTED. <br>_(server [CLIENT])_ | The server SHOULD abort its response stream with the error code H3_REQUEST_REJECTED. | http3/ error codes + client retry: RESET_STREAM(H3_REQUEST_REJECTED=0x10B) on the response side means the request was never processed. |
| `4.1-32` | informative | The client can treat server-rejected requests as never sent, making them safe to retry. <br>_(client [CLIENT])_ | The client can treat requests rejected by the server as though they had never been sent at all, thereby allowing them to be retried later. | http3/client.go (or client/ retry layer): H3_REQUEST_REJECTED should be classified retry-safe regardless of method idempotency. |
| `4.1-33` | MUST NOT | Servers must never use H3_REQUEST_REJECTED for requests that were partially or fully processed. <br>_(server [not-client])_ | Servers MUST NOT use the H3_REQUEST_REJECTED error code for requests that were partially or fully processed. | Server obligation; it is what justifies the client treating REJECTED as retry-safe (see 4.1-32). |
| `4.1-34` | SHOULD | A server abandoning a response after partial processing should abort the response stream with H3_REQUEST_CANCELLED. <br>_(server [CLIENT])_ | it SHOULD abort its response stream with the error code H3_REQUEST_CANCELLED | http3/ error codes: H3_REQUEST_CANCELLED (0x10C) received on response stream must map to a non-retry-safe (possibly-processed) typed error. |
| `4.1-35` | SHOULD | Clients should use error code H3_REQUEST_CANCELLED when cancelling requests. <br>_(client [CLIENT])_ | Client SHOULD use the error code H3_REQUEST_CANCELLED to cancel requests. | http3/stream.go: the cancel path's RESET_STREAM and STOP_SENDING carry H3_REQUEST_CANCELLED (0x10C), not H3_NO_ERROR or an internal code. |
| `4.1-36` | MAY | On receiving a client's H3_REQUEST_CANCELLED, a server may terminate the response with H3_REQUEST_REJECTED if nothing was processed. <br>_(server [CLIENT])_ | a server MAY abruptly terminate the response using the error code H3_REQUEST_REJECTED if no processing was performed | http3/stream_body.go: after self-cancel, a REJECTED reset from the peer is an expected outcome, not an anomaly. |
| `4.1-37` | MUST NOT | Clients must not use H3_REQUEST_REJECTED except to echo a server that requested request-stream closure with that code. <br>_(client [CLIENT])_ | Clients MUST NOT use the H3_REQUEST_REJECTED error code, except when a server has requested closure of the request stream with this error code | http3/stream.go + quic/ STOP_SENDING handling: RESET_STREAM sent in reply to STOP_SENDING(H3_REQUEST_REJECTED) may echo that code; the client never originates 0x10B itself. |
| `4.1-38` | MAY | If cancellation arrives after a complete response was received, the client may ignore it and use the response. <br>_(client [CLIENT])_ | the client MAY ignore the cancellation and use the response | http3/stream_body.go: a RESET_STREAM arriving after the full body+FIN was consumed must not retroactively error the delivered response. |
| `4.1-39` | SHOULD NOT | A response of which only part was received before cancellation should not be used. <br>_(client [CLIENT])_ | if a stream is cancelled after receiving a partial response, the response SHOULD NOT be used | http3/stream_body.go: reset mid-body surfaces an error to the caller; a truncated body must never be returned as a successful response. |
| `4.1-40` | SHOULD NOT | Clients should not auto-retry non-idempotent requests unless they can establish idempotent semantics or that the original was never applied. <br>_(client [CLIENT])_ | a client SHOULD NOT automatically retry a request with a non-idempotent method unless it has some means to know that the request semantics are idempotent | http3/client.go (and client/ retry policy once TransportH3 parity lands): gate automatic retry on method idempotency, except when H3_REQUEST_REJECTED proves non-processing (4.1-32). |
| `4.1-41` | informative | A message is malformed if it has prohibited fields/pseudo-headers, missing mandatory pseudo-headers, invalid pseudo-header values, pseudo-headers after regular fields, an invalid message sequence, uppercase field names, or invalid characters in names/values. <br>_(all [CLIENT])_ | A malformed request or response is one that is an otherwise valid sequence of frames but is invalid due to: | http3/validate.go: verify one check exists per bullet — prohibited fields, missing/invalid/duplicate pseudo-headers, pseudo-after-regular ordering, uppercase names, invalid characters in names and values. |
| `4.1-42` | informative | A message defined as having content is malformed when Content-Length does not equal the sum of received DATA frame lengths. <br>_(all [CLIENT])_ | malformed if the value of the Content-Length header field does not equal the sum of the DATA frame lengths received | http3/stream_body.go + response.go: track cumulative DATA payload length vs Content-Length; both overrun and short-at-FIN are malformed (H3_MESSAGE_ERROR); also enforce on sent requests. |
| `4.1-43` | informative | Responses defined as never having content (e.g., to HEAD, or 304) may carry a non-zero Content-Length with no DATA content. <br>_(all [CLIENT])_ | A response that is defined as never having content, even when a Content-Length is present, can have a non-zero Content-Length header field | http3/response.go: exempt HEAD responses and status codes without content (1xx/204/304) from the Content-Length-equals-DATA check. |
| `4.1-44` | MUST NOT | Non-tunnel intermediaries must not forward malformed requests or responses. <br>_(intermediary [not-client])_ | MUST NOT forward a malformed request or response | Intermediary-only; this client does not forward messages. |
| `4.1-45` | MUST | A detected malformed request or response must be treated as a stream error of type H3_MESSAGE_ERROR. <br>_(endpoint [CLIENT])_ | Malformed requests or responses that are detected MUST be treated as a stream error of type H3_MESSAGE_ERROR. | http3/validate.go + stream.go: malformed => STOP_SENDING/RESET_STREAM with H3_MESSAGE_ERROR (0x10E) on that stream only — stream error, never a connection close (contrast H3_FRAME_UNEXPECTED in 4.1-8). |
| `4.1-46` | MAY | For malformed requests a server may send an error response before closing or resetting the stream. <br>_(server [CLIENT])_ | a server MAY send an HTTP response indicating the error prior to closing or resetting the stream | http3/stream_body.go: client must tolerate a valid error response followed by a stream reset — surface the response, not just the reset. |
| `4.1-47` | MUST NOT | Clients must not accept (must reject) a malformed response. <br>_(client [CLIENT])_ | Clients MUST NOT accept a malformed response. | http3/validate.go + response.go: every malformed condition in 4.1-41/42 must fail the exchange with H3_MESSAGE_ERROR and never deliver the response to the caller — deliberately strict, no lenient parsing. |

### 9114 section 4.2-4.3 - HTTP Fields, Field Compression (QPACK), Header Size Constraints, HTTP Control Data, Request/Response Pseudo-Header Fields

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `4.2-4.3-1` | MUST | Field names must be converted to lowercase before being encoded. <br>_(sender,endpoint [CLIENT])_ | Characters in field names MUST be converted to lowercase prior to their encoding. | qpack/encoder.go + http3/validate.go: confirm request field names are lowercased (or non-lowercase rejected) before QPACK encoding in http3/request.go. |
| `4.2-4.3-2` | MUST | A request or response whose field names contain uppercase characters must be treated as malformed. <br>_(endpoint,receiver [CLIENT])_ | A request or response containing uppercase characters in field names MUST be treated as malformed. | http3/validate.go + http3/response.go: decoded response with uppercase field name must be treated malformed (H3_MESSAGE_ERROR stream handling per §4.1.2). |
| `4.2-4.3-3` | MUST NOT | An endpoint must never generate a field section containing connection-specific fields (Connection, Keep-Alive, Proxy-Connection, Transfer-Encoding, Upgrade). <br>_(endpoint,sender [CLIENT])_ | An endpoint MUST NOT generate an HTTP/3 field section containing connection-specific fields | http3/validate.go: verify request-emit path rejects/strips connection-specific headers before http3/request.go encodes them. |
| `4.2-4.3-4` | MUST | Any received message containing connection-specific fields must be treated as malformed. <br>_(endpoint,receiver [CLIENT])_ | any message containing connection-specific fields MUST be treated as malformed. | http3/validate.go + http3/response.go: responses carrying Connection/Keep-Alive/etc. must be treated malformed, not passed to the caller silently. |
| `4.2-4.3-5` | MAY | The TE header field is the one connection-specific field permitted, and only in requests. <br>_(client,sender [CLIENT])_ | the TE header field, which MAY be present in an HTTP/3 request header | http3/validate.go: TE allowed on requests only, never accepted in responses (see http3/conformance_te_direction_test.go). |
| `4.2-4.3-6` | MUST NOT | When TE is present in a request it must carry no value other than "trailers". <br>_(sender,client [CLIENT])_ | when it is, it MUST NOT contain any value other than "trailers". | http3/validate.go: enforce TE value == "trailers" exactly on the request-emit path in http3/request.go. |
| `4.2-4.3-7` | MUST | An intermediary translating HTTP/1.x to HTTP/3 must remove connection-specific header fields. <br>_(intermediary [not-client])_ | An intermediary transforming an HTTP/1.x message to HTTP/3 MUST remove connection-specific header fields | Intermediary-only; no client code path. No check needed in http3/. |
| `4.2-4.3-8` | informative | HTTP/3 compresses header and trailer sections, including pseudo-header control data, with QPACK. <br>_(all [CLIENT])_ | HTTP/3 uses QPACK to compress header and trailer sections, including the control data present in the header section. | http3/request.go + http3/response.go: pseudo-headers, regular headers, and trailers all flow through qpack/encoder.go / qpack/decoder.go — no uncompressed side channel. |
| `4.2-4.3-9` | MAY | The Cookie header may be split into separate field lines before compression to improve compression efficiency. <br>_(sender [CLIENT])_ | the Cookie header field ([COOKIES]) MAY be split into separate field lines, each with one or more cookie-pairs, before compression. | http3/request.go + qpack/encoder.go: optional optimization; confirm nothing rejects multiple cookie field lines on either encode or decode. |
| `4.2-4.3-10` | MUST | Multiple decompressed cookie field lines must be concatenated with "; " before being passed to any non-HTTP/2/HTTP/3 context. <br>_(receiver,endpoint [CLIENT])_ | these MUST be concatenated into a single byte string using the two-byte delimiter of "; " (ASCII 0x3b, 0x20) before being passed into a context other than HTTP/2 or HTTP/3 | http3/response.go: decide/document whether decoded Cookie lines surfaced to callers are pre-concatenated with "; "; if the client hands fields to generic app code, concatenation is required. |
| `4.2-4.3-11` | MAY | An implementation may impose a limit on the maximum message-header size it accepts per HTTP message. <br>_(endpoint [CLIENT])_ | An HTTP/3 implementation MAY impose a limit on the maximum size of the message header it will accept on an individual HTTP message. | http3/response.go + http3/client.go: response header-section size cap enforcement (see http3/response_limits_test.go, http3/maxfieldsection_test.go). |
| `4.2-4.3-12` | informative | A server receiving a header section larger than it will handle can respond 431 (Request Header Fields Too Large). <br>_(server [not-client])_ | A server that receives a larger header section than it is willing to handle can send an HTTP 431 (Request Header Fields Too Large) status code | Server-only; client just surfaces a 431 like any status via http3/response.go — nothing special to check. |
| `4.2-4.3-13` | informative | A client may discard responses whose header sections it cannot process. <br>_(client [CLIENT])_ | A client can discard responses that it cannot process. | http3/client.go + http3/stream.go: on exceeding the local field-section limit, discard the response and reset the request stream rather than kill the connection. |
| `4.2-4.3-14` | informative | Field-list size is computed on uncompressed fields: name length + value length in bytes + 32 bytes overhead per field. <br>_(all [CLIENT])_ | the uncompressed size of fields, including the length of the name and value in bytes plus an overhead of 32 bytes for each field. | http3/response.go + http3/request.go: verify size accounting for SETTINGS_MAX_FIELD_SECTION_SIZE uses uncompressed name+value+32 per field, not compressed/wire bytes. |
| `4.2-4.3-15` | informative | The header-size limit is advertised to the peer, in bytes, via the SETTINGS_MAX_FIELD_SECTION_SIZE parameter. <br>_(endpoint [CLIENT])_ | it can be conveyed as a number of bytes in the SETTINGS_MAX_FIELD_SECTION_SIZE parameter. | http3/control.go: client may advertise its own limit in its SETTINGS frame on the control stream; check the setting is emitted when a limit is configured. |
| `4.2-4.3-16` | SHOULD NOT | An implementation that has received SETTINGS_MAX_FIELD_SECTION_SIZE should not send a message header exceeding the advertised size. <br>_(sender,endpoint [CLIENT])_ | An implementation that has received this parameter SHOULD NOT send an HTTP message header that exceeds the indicated size | http3/control.go (store peer setting) + http3/request.go (enforce on encode): request header sections must be checked against the server-advertised limit (http3/maxfieldsection_test.go). |
| `4.2-4.3-17` | informative | Each hop applies the limit separately, so messages below the advertised limit are still not guaranteed acceptance. <br>_(all [CLIENT])_ | Because this limit is applied separately by each implementation that processes the message, messages below this limit are not guaranteed to be accepted. | http3/client.go: no code change — just do not treat under-limit as guaranteed-accepted (retry/error paths must tolerate rejection anyway). |
| `4.2-4.3-18` | informative | Pseudo-header fields are those whose names begin with ':' (ASCII 0x3a) and carry message control data. <br>_(all [CLIENT])_ | HTTP/3 employs a series of pseudo-header fields, where the field name begins with the : character (ASCII 0x3a) | http3/validate.go: pseudo-header classification keyed on ':' name prefix, applied on both encode (request.go) and decode (response.go). |
| `4.2-4.3-19` | MUST NOT | Endpoints must not generate any pseudo-header fields beyond those defined in RFC 9114 (absent a negotiated extension). <br>_(endpoint,sender [CLIENT])_ | Endpoints MUST NOT generate pseudo-header fields other than those defined in this document. | http3/request.go + http3/validate.go: only :method/:scheme/:authority/:path (and :protocol only if extended-CONNECT is negotiated) may be emitted; reject caller-supplied ':' names. |
| `4.2-4.3-20` | MUST NOT | Request-defined pseudo-headers must not appear in responses, and response-defined pseudo-headers must not appear in requests. <br>_(endpoint [CLIENT])_ | Pseudo-header fields defined for requests MUST NOT appear in responses; pseudo-header fields defined for responses MUST NOT appear in requests. | http3/validate.go + http3/response.go: response decode must reject :method/:scheme/:authority/:path; request encode must never emit :status. |
| `4.2-4.3-21` | MUST NOT | Pseudo-header fields must not appear in trailer sections. <br>_(endpoint [CLIENT])_ | Pseudo-header fields MUST NOT appear in trailer sections. | http3/response.go (trailer decode → malformed) + http3/request.go / http3/stream_body.go (trailer emit must refuse ':' names); see http3/interim_trailers_test.go. |
| `4.2-4.3-22` | MUST | A request or response containing undefined or invalid pseudo-header fields must be treated as malformed. <br>_(endpoint,receiver [CLIENT])_ | Endpoints MUST treat a request or response that contains undefined or invalid pseudo-header fields as malformed. | http3/validate.go: unknown/invalid pseudo-header in a decoded response → malformed → H3_MESSAGE_ERROR stream treatment per §4.1.2 (check the error code used in http3/stream.go). |
| `4.2-4.3-23` | MUST | All pseudo-header fields must precede regular header fields in the header section. <br>_(sender,endpoint [CLIENT])_ | All pseudo-header fields MUST appear in the header section before regular header fields. | http3/request.go: pseudo-headers must be encoded first, before all regular fields, in the QPACK field section. |
| `4.2-4.3-24` | MUST | A message with a pseudo-header field appearing after a regular header field must be treated as malformed. <br>_(receiver,endpoint [CLIENT])_ | Any request or response that contains a pseudo-header field that appears in a header section after a regular header field MUST be treated as malformed. | http3/validate.go + http3/response.go: decode must track the pseudo→regular ordering transition and treat a late pseudo-header as malformed. |
| `4.2-4.3-25` | informative | The pseudo-header fields defined for requests are :method, :scheme, :authority, and :path. <br>_(all [CLIENT])_ | The following pseudo-header fields are defined for requests: | http3/request.go: the request mapping must emit exactly this set; http3/validate.go must recognize exactly this set as request-valid. |
| `4.2-4.3-26` | informative | :scheme carries the target URI scheme and is not limited to "http" and "https". <br>_(all [CLIENT])_ | The :scheme pseudo-header is not restricted to URIs with scheme "http" and "https". | http3/validate.go: confirm scheme validation does not hard-reject non-http(s) schemes if the public API is meant to allow them (or document the restriction). |
| `4.2-4.3-27` | MUST NOT | :authority must not include the deprecated userinfo subcomponent for "http" or "https" URIs. <br>_(sender,client [CLIENT])_ | The authority MUST NOT include the deprecated userinfo subcomponent for URIs of scheme "http" or "https". | http3/request.go + http3/validate.go: strip or reject user@ userinfo when building :authority from the target URL. |
| `4.2-4.3-28` | MUST | When translating an HTTP/1.1 request whose request-target is in a method-specific form, :authority must be omitted. <br>_(intermediary [not-client])_ | this pseudo-header field MUST be omitted when translating from an HTTP/1.1 request that has a request target in a method-specific form | Translation/intermediary rule; this client generates HTTP/3 requests directly — no http3/ code path. |
| `4.2-4.3-29` | SHOULD | Clients generating HTTP/3 requests directly should use :authority rather than the Host header field. <br>_(client [CLIENT])_ | Clients that generate HTTP/3 requests directly SHOULD use the :authority pseudo-header field instead of the Host header field. | http3/request.go: emit :authority (not Host) from the target URL; decide policy for caller-supplied Host headers (see fact 33 equality rule). |
| `4.2-4.3-30` | MUST | An intermediary converting HTTP/3 to HTTP/1.1 must create a Host field from :authority when absent. <br>_(intermediary [not-client])_ | An intermediary that converts an HTTP/3 request to HTTP/1.1 MUST create a Host field if one is not present | Intermediary-only; no client code path in http3/. |
| `4.2-4.3-31` | MUST NOT | :path must not be empty for "http" or "https" URIs. <br>_(sender,endpoint [CLIENT])_ | This pseudo-header field MUST NOT be empty for "http" or "https" URIs | http3/validate.go: reject empty :path on request emit (http3/requestvalidate_test.go should pin this). |
| `4.2-4.3-32` | MUST | "http"/"https" URIs lacking a path component must use :path value "/". <br>_(sender,client [CLIENT])_ | "http" or "https" URIs that do not contain a path component MUST include a value of / (ASCII 0x2f). | http3/request.go: default :path to "/" when the target URL has an empty path. |
| `4.2-4.3-33` | informative | An OPTIONS request without a path component uses "*" as the :path value (asterisk-form). <br>_(sender,client [CLIENT])_ | An OPTIONS request that does not include a path component includes the value * (ASCII 0x2a) for the :path pseudo-header field | http3/request.go + http3/validate.go: allow :path == "*" for OPTIONS (server-wide OPTIONS); ensure validation does not reject it. |
| `4.2-4.3-34` | MUST | Every non-CONNECT HTTP/3 request must include exactly one :method, one :scheme, and one :path pseudo-header. <br>_(client,sender [CLIENT])_ | All HTTP/3 requests MUST include exactly one value for the :method, :scheme, and :path pseudo-header fields, unless the request is a CONNECT request | http3/request.go + http3/validate.go: emit each of :method/:scheme/:path exactly once; reject duplicates or omissions before encoding. |
| `4.2-4.3-35` | MUST | If :scheme has a mandatory authority component (including http/https), the request must contain :authority or Host. <br>_(sender,client [CLIENT])_ | the request MUST contain either an :authority pseudo-header field or a Host header field. | http3/request.go: :authority always populated for http/https targets; http3/validate.go rejects requests with neither. |
| `4.2-4.3-36` | MUST NOT | When :authority or Host is present, it must not be empty. <br>_(sender,client [CLIENT])_ | If these fields are present, they MUST NOT be empty. | http3/validate.go: reject empty :authority and empty Host header values on request emit. |
| `4.2-4.3-37` | MUST | When both :authority and Host are present, they must contain the same value. <br>_(sender,client [CLIENT])_ | If both fields are present, they MUST contain the same value. | http3/validate.go: if a caller supplies a Host header alongside the derived :authority, enforce equality (or strip Host) before encoding in http3/request.go. |
| `4.2-4.3-38` | MUST NOT | If the scheme has no mandatory authority component and the request target provides none, the request must contain neither :authority nor Host. <br>_(sender,client [CLIENT])_ | If the scheme does not have a mandatory authority component and none is provided in the request target, the request MUST NOT contain the :authority pseudo-header or Host header fields. | http3/request.go: for non-authority schemes without an authority, omit both :authority and Host (only relevant if non-http(s) schemes are supported — see fact 26). |
| `4.2-4.3-39` | informative | A request omitting mandatory pseudo-header fields or containing invalid pseudo-header values is malformed. <br>_(endpoint [CLIENT])_ | An HTTP request that omits mandatory pseudo-header fields or contains invalid values for those pseudo-header fields is malformed. | http3/validate.go: the client's own emit-side validation must guarantee it never sends such a request (server would reject with H3_MESSAGE_ERROR per §4.1.2). |
| `4.2-4.3-40` | informative | HTTP/3 carries no request version identifier; requests implicitly have protocol version "3.0". <br>_(all [CLIENT])_ | HTTP/3 requests implicitly have a protocol version of "3.0". | http3/request.go + http3/client.go: no version token on the wire; any Proto reporting in the public API should say 3.0. |
| `4.2-4.3-41` | MUST | Every response must include the :status pseudo-header carrying the HTTP status code; a response without it is malformed. <br>_(server,receiver [CLIENT])_ | This pseudo-header field MUST be included in all responses; otherwise, the response is malformed | http3/response.go + http3/validate.go: client must treat a decoded response with missing/duplicate/invalid :status as malformed (§4.1.2 → H3_MESSAGE_ERROR), not default the status. |
| `4.2-4.3-42` | informative | HTTP/3 carries no response version or reason phrase; responses implicitly have protocol version "3.0". <br>_(all [CLIENT])_ | HTTP/3 responses implicitly have a protocol version of "3.0". | http3/response.go: no reason phrase or version to parse; the response struct must not expect one. |

### 9114 section 4.4-4.6 - The CONNECT Method, HTTP Upgrade, Server Push (push IDs, MAX_PUSH_ID, PUSH_PROMISE, CANCEL_PUSH semantics)

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `4.4-4.6-1` | informative | In HTTP/3 (as in HTTP/2), CONNECT establishes a tunnel over a single request stream rather than converting the whole connection. <br>_(all [CLIENT])_ | In HTTP/2 and HTTP/3, the CONNECT method is used to establish a tunnel over a single stream. | http3/request.go:21-23 documents CONNECT as not yet supported; if added, the tunnel must map onto one bidirectional request stream in http3/client.go/stream.go. |
| `4.4-4.6-2` | MUST | A CONNECT request must set the :method pseudo-header field to "CONNECT". <br>_(client,sender [CLIENT])_ | The :method pseudo-header field is set to "CONNECT" | http3/request.go EncodeHeaders has no CONNECT branch; if CONNECT support is added, emit :method=CONNECT and gate the other pseudo-header rules on it. |
| `4.4-4.6-3` | MUST | A CONNECT request must omit the :scheme and :path pseudo-header fields. <br>_(client,sender [CLIENT])_ | The :scheme and :path pseudo-header fields are omitted | http3/request.go:60 rejects empty Scheme/Path unconditionally — correct today (blocks malformed CONNECT), but must be method-gated if CONNECT is implemented. |
| `4.4-4.6-4` | MUST | A CONNECT request's :authority pseudo-header must contain the host and port to connect to (authority-form). <br>_(client,sender [CLIENT])_ | The :authority pseudo-header field contains the host and port to connect to | http3/request.go + http3/validate.go — a CONNECT path must require non-empty :authority including an explicit port. |
| `4.4-4.6-5` | informative | The client leaves the CONNECT request stream open (no FIN after headers) to carry tunneled data. <br>_(client [CLIENT])_ | The request stream remains open at the end of the request to carry the data to be transferred. | http3/stream_body.go sends FIN after the request body — a CONNECT mode needs a keep-open bidirectional streaming write path. |
| `4.4-4.6-6` | informative | A CONNECT request that violates the construction rules (wrong pseudo-headers) is malformed. <br>_(all [CLIENT])_ | A CONNECT request that does not conform to these restrictions is malformed. | http3/request.go ErrH3Message local-refusal pattern already blocks generating malformed requests; extend the same pre-wire check to CONNECT construction. |
| `4.4-4.6-7` | informative | The proxy signals successful tunnel establishment with a HEADERS frame carrying a 2xx status; the client must interpret 2xx as tunnel-open. <br>_(proxy,client [CLIENT])_ | the proxy sends a HEADERS frame containing a 2xx series status code to the client | http3/response.go — if CONNECT is added, a 2xx final response switches the stream to tunnel mode; non-2xx means tunnel refused. |
| `4.4-4.6-8` | informative | After tunnel establishment, every DATA frame on the stream corresponds one-to-one to bytes on the proxied TCP connection. <br>_(all [CLIENT])_ | All DATA frames on the stream correspond to data sent or received on the TCP connection. | http3/stream_body.go — tunnel payload must be carried exclusively in DATA frames; segment/frame boundaries carry no meaning. |
| `4.4-4.6-9` | informative | Once CONNECT has completed, only DATA frames (plus permitted extension frames) may be sent on the stream. <br>_(endpoint,sender [CLIENT])_ | Once the CONNECT method has completed, only DATA frames are permitted to be sent on the stream. | http3/client.go request-stream write path — a CONNECT mode must never emit HEADERS/trailers after the tunnel is up. |
| `4.4-4.6-10` | MAY | Extension frames may appear on a CONNECT stream only if the extension's definition specifically permits it. <br>_(endpoint [CLIENT])_ | Extension frames MAY be used if specifically permitted by the definition of the extension. | http3/client.go:1084/1140 unknown-frame handling — unknown (grease/extension) frame types must remain ignorable on a tunnel stream, not fatal. |
| `4.4-4.6-11` | MUST | Receipt of any other known frame type on a CONNECT stream after tunnel establishment is a connection error of type H3_FRAME_UNEXPECTED. <br>_(endpoint,receiver [CLIENT])_ | Receipt of any other known frame type MUST be treated as a connection error of type H3_FRAME_UNEXPECTED. | http3/client.go frame dispatch — in tunnel mode, known non-DATA frames must trigger connError(H3FrameUnexpected) (0x0105, client.go:60). |
| `4.4-4.6-12` | informative | TCP FIN from the target maps to the proxy closing its send stream toward the client; the client sees stream FIN as tunnel EOF. <br>_(proxy,client [CLIENT])_ | When the proxy receives a packet with the FIN bit set, it will close the send stream that it sends to the client. | http3/stream_body.go — in tunnel mode, peer FIN is normal EOF, not the 'interim without final response' malformed case. |
| `4.4-4.6-13` | SHOULD NOT | Clients should not close the send side of a CONNECT stream while still expecting data from the tunnel target. <br>_(client [CLIENT])_ | clients SHOULD NOT close a stream for sending while they still expect to receive data from the target of the CONNECT. | http3/client.go — a CONNECT API must let the caller hold the send side open independently of the receive side (half-close control). |
| `4.4-4.6-14` | informative | A TCP connection error on the tunnel is signaled by abruptly terminating (resetting) the stream. <br>_(all [CLIENT])_ | A TCP connection error is signaled by abruptly terminating the stream. | http3/client.go stream-reset surfacing (StreamResetError path, cf. client_test.go:575) — a reset on a tunnel stream means the TCP leg failed. |
| `4.4-4.6-15` | informative | A proxy signals any TCP-side error (including TCP RST) as a stream error of type H3_CONNECT_ERROR; the client must recognize this code as a per-stream tunnel failure. <br>_(proxy,client [CLIENT])_ | A proxy treats any error in the TCP connection, which includes receiving a TCP segment with the RST bit set, as a stream error of type H3_CONNECT_ERROR. | http3/client.go error-code constants (lines 60-67) — verify H3_CONNECT_ERROR (0x010f) is defined and surfaced as a stream-level error, never escalated to connection close. |
| `4.4-4.6-16` | MUST | A proxy that detects a stream or QUIC connection error must close the tunneled TCP connection. <br>_(proxy [not-client])_ | if a proxy detects an error with the stream or the QUIC connection, it MUST close the TCP connection. | Proxy-only; no client-side code in http3/. |
| `4.4-4.6-17` | MUST | A proxy that detects the client reset the stream or aborted reading must close the TCP connection. <br>_(proxy [not-client])_ | If the proxy detects that the client has reset the stream or aborted reading from the stream, it MUST close the TCP connection. | Proxy-only; the client-side signals are quic/ stream Reset and StopSending — already the cancellation vocabulary in http3/client.go:885-886. |
| `4.4-4.6-18` | SHOULD | When one direction of a CONNECT stream is reset or aborted, the proxy should cancel the other direction too. <br>_(proxy [not-client])_ | a proxy SHOULD perform the same operation on the other direction in order to ensure that both directions of the stream are cancelled. | Proxy-only; note http3/client.go:885-886 already applies the analogous both-directions (StopSending+Reset) pattern on client cancel. |
| `4.4-4.6-19` | SHOULD | When cancelling a tunnel, the proxy should send a TCP RST segment if the TCP implementation permits. <br>_(proxy [not-client])_ | if the underlying TCP implementation permits it, the proxy SHOULD send a TCP segment with the RST bit set. | Proxy-only; N/A in http3/. |
| `4.4-4.6-20` | SHOULD | Proxies supporting CONNECT should restrict its use to known ports or a list of safe request targets. <br>_(proxy [not-client])_ | proxies that support CONNECT SHOULD restrict its use to a set of known ports or a list of safe request targets | Proxy-only; N/A in http3/. |
| `4.4-4.6-21` | informative | HTTP/3 has no HTTP Upgrade mechanism and no 101 (Switching Protocols) status code; a client must not attempt Upgrade and should treat 101 as invalid. <br>_(all [CLIENT])_ | HTTP/3 does not support the HTTP Upgrade mechanism (Section 7.8 of [HTTP]) or the 101 (Switching Protocols) informational status code | http3/client.go:1111-1114 buffers any 1xx as interim — check whether :status 101 should instead be rejected as malformed; http3/validate.go must keep rejecting the upgrade header as connection-specific (RFC 9114 4.2). |
| `4.4-4.6-22` | informative | Each server push carries a unique push ID (a varint) that identifies it in PUSH_PROMISE, CANCEL_PUSH, and push streams for the connection lifetime. <br>_(server,client [CLIENT])_ | Each server push is assigned a unique push ID by the server. | http3/frame.go (AppendMaxPushID:76, AppendCancelPush:82) and http3/control.go — push-ID varint parsing exists on the receive side; encoders currently unused. |
| `4.4-4.6-23` | informative | The push ID space starts at zero and is bounded above by the value the client sets in MAX_PUSH_ID. <br>_(all [CLIENT])_ | The push ID space begins at zero and ends at a maximum value set by the MAX_PUSH_ID frame. | http3/control.go:331-338 — with MAX_PUSH_ID never sent, the space is empty and every push ID is out of range; correct as implemented. |
| `4.4-4.6-24` | informative | A server cannot push at all until the client has sent a MAX_PUSH_ID frame; never sending it disables push entirely. <br>_(server,client [CLIENT])_ | a server is not able to push until after the client sends a MAX_PUSH_ID frame. | http3/control.go:198-200 — client deliberately never sends MAX_PUSH_ID, disabling push by construction; this is the load-bearing assumption for all H3IDError paths. |
| `4.4-4.6-25` | informative | The client controls how many pushes a server can promise by sending MAX_PUSH_ID frames. <br>_(client [CLIENT])_ | A client sends MAX_PUSH_ID frames to control the number of pushes that a server can promise. | http3/frame.go:76 AppendMaxPushID exists but must have no caller in http3/client.go — confirm no code path ever emits it while push stays disabled. |
| `4.4-4.6-26` | SHOULD | A server should allocate push IDs sequentially starting from zero. <br>_(server [not-client])_ | A server SHOULD use push IDs sequentially, beginning from zero. | Server-only; client need not enforce sequential order (only the maximum bound). |
| `4.4-4.6-27` | MUST | A client must treat receipt of a push stream as a connection error of type H3_ID_ERROR when it has never sent MAX_PUSH_ID. <br>_(client [CLIENT])_ | MUST treat receipt of a push stream as a connection error of type H3_ID_ERROR when no MAX_PUSH_ID frame has been sent | Implemented: http3/control.go:198-200 connError(H3IDError) on push-typed uni stream; pinned by control_test.go:156-164 — verify RFC_COVERAGE.md row exists. |
| `4.4-4.6-28` | MUST | A client must treat a push stream referencing a push ID greater than the advertised maximum as a connection error of type H3_ID_ERROR. <br>_(client [CLIENT])_ | when the stream references a push ID that is greater than the maximum push ID | http3/control.go — currently subsumed (no MAX_PUSH_ID sent means every ID exceeds the max); if push is ever enabled, add explicit max-push-ID tracking and compare before accepting. |
| `4.4-4.6-29` | informative | PUSH_PROMISE frames carrying the promised request's control data and header fields are sent on the request stream that generated the push. <br>_(server,client [CLIENT])_ | These frames are sent on the request stream that generated the push. | http3/client.go:1163-1165 — PUSH_PROMISE on a request stream is connError(H3IDError) because push is disabled (any push ID exceeds the unset max); tested at client_test.go:499-508. |
| `4.4-4.6-30` | MUST | When the same push ID is promised on multiple request streams, the decompressed request field sections must be byte-identical in content and order. <br>_(server,sender [not-client])_ | the decompressed request field sections MUST contain the same fields in the same order, and both the name and the value in each field MUST be identical. | Binds the server; N/A while push is disabled — if enabled, an optional client-side consistency check in http3/client.go could enforce it. |
| `4.4-4.6-31` | informative | A push stream's wire layout is: the push ID of the fulfilled promise first, then a response message as defined in Section 4.1. <br>_(server,client [CLIENT])_ | The push stream identifies the push ID of the promise that it fulfills, then contains a response to the promised request | http3/control.go push-stream branch aborts the connection before parsing the push-ID varint — fine while disabled; this layout governs any future parser. |
| `4.4-4.6-32` | informative | CANCEL_PUSH is bidirectional: a client sends it to decline a promised resource; a server sends it to withdraw a promise. <br>_(all [CLIENT])_ | Clients use this frame to indicate they do not wish to receive a promised resource. Servers use this frame to indicate they will not be fulfilling a previous promise. | http3/control.go:320-338 handles server-sent CANCEL_PUSH (H3IDError since every push ID is out of range, tested control_test.go:125-135); http3/frame.go:82 AppendCancelPush is the unused client-send encoder. |
| `4.4-4.6-33` | MAY | A server may push only requests that are cacheable, safe, and carry no request content or trailer section. <br>_(server [not-client])_ | A server MAY push requests that have the following properties | Server-only; the client-side mirror is the SHOULD-cancel rule in 4.4-4.6-37. |
| `4.4-4.6-34` | MUST | A pushed request must include an :authority value for which the server is authoritative. <br>_(server [not-client])_ | The server MUST include a value in the :authority pseudo-header field for which the server is authoritative. | Server-only; the client-side enforcement is 4.4-4.6-35/36. |
| `4.4-4.6-35` | MUST | Before using a pushed request for an origin not yet validated on the connection, the client must perform the same authority verification as for sending its own request (Section 3.3). <br>_(client [CLIENT])_ | it MUST perform the same verification process it would do before sending a request for that origin on the connection | N/A while push is disabled; if enabled, tie to the origin/certificate validation done at http3/dial.go connection setup. |
| `4.4-4.6-36` | MUST NOT | If that verification fails, the client must not consider the server authoritative for the pushed origin. <br>_(client [CLIENT])_ | If this verification fails, the client MUST NOT consider the server authoritative for that origin. | N/A while push is disabled; pairs with 4.4-4.6-35 in any future push implementation in http3/client.go. |
| `4.4-4.6-37` | SHOULD | A client should send CANCEL_PUSH on receiving a PUSH_PROMISE whose request is not cacheable, not known safe, indicates request content, or is from a non-authoritative server. <br>_(client [CLIENT])_ | Clients SHOULD send a CANCEL_PUSH frame upon receipt of a PUSH_PROMISE frame carrying a request that is not cacheable, is not known to be safe | Moot today (PUSH_PROMISE is a connection error); if push is enabled, wire http3/frame.go:82 AppendCancelPush onto the control-stream send path in http3/control.go. |
| `4.4-4.6-38` | MUST NOT | Responses corresponding to an unacceptable push must not be used or cached by the client. <br>_(client [CLIENT])_ | Any corresponding responses MUST NOT be used or cached. | Trivially satisfied — poseidon rejects all pushes and has no HTTP cache; keep true if push is ever enabled (discard, never surface). |
| `4.4-4.6-39` | MAY | Push-to-request associations do not affect protocol operation but may inform how a user agent uses pushed resources. <br>_(user-agent [CLIENT])_ | These associations do not affect the operation of the protocol, but they MAY be considered by user agents when deciding how to use pushed resources. | N/A — load-generator client, no resource-usage policy; no code impact in http3/. |
| `4.4-4.6-40` | SHOULD | A server should send PUSH_PROMISE frames before the HEADERS or DATA frames that reference the promised responses. <br>_(server [not-client])_ | The server SHOULD send PUSH_PROMISE frames prior to sending HEADERS or DATA frames that reference the promised responses. | Server-only ordering advice; client must not depend on it (see 4.4-4.6-41). |
| `4.4-4.6-41` | informative | Due to reordering, push stream data can arrive before its PUSH_PROMISE; a client may buffer the unknown-push-ID stream awaiting the promise. <br>_(client [CLIENT])_ | Due to reordering, push stream data can arrive before the corresponding PUSH_PROMISE frame. | Moot — http3/control.go:198-200 makes any push stream an immediate connection error, so no buffering path exists; relevant only if push is enabled. |
| `4.4-4.6-42` | informative | The client can bound how much data a server commits to a push stream via QUIC stream flow control. <br>_(client [CLIENT])_ | The client can use stream flow control (Section 4.1 of [QUIC-TRANSPORT]) to limit the amount of data a server may commit to the pushed stream. | quic/ transport parameters — check initial_max_stream_data_uni bounds server-opened uni streams, limiting push-stream bytes buffered before http3/control.go rejects the stream. |
| `4.4-4.6-43` | SHOULD | Clients should abort reading and discard data from push streams whose PUSH_PROMISE is not processed in a reasonable time. <br>_(client [CLIENT])_ | Clients SHOULD abort reading and discard data already read from push streams if no corresponding PUSH_PROMISE frame is processed in a reasonable amount of time. | Trivially satisfied — push streams terminate the connection immediately in http3/control.go; a future push implementation needs an orphan-stream timeout. |
| `4.4-4.6-44` | informative | When push data arrives after the client cancelled the push, the client can abort reading with error code H3_REQUEST_CANCELLED, asking the server to stop and signalling discard. <br>_(client [CLIENT])_ | the client can abort reading the stream with an error code of H3_REQUEST_CANCELLED. | http3/client.go:67 defines H3RequestCancelled (0x010c), already used for request cancellation via StopSending/Reset (client.go:885-886); extend the same code to push streams if push is enabled. |
| `4.4-4.6-45` | informative | Cacheable pushed responses can be stored by a client that implements an HTTP cache and count as successfully validated at receipt time. <br>_(client [CLIENT])_ | Pushed responses that are cacheable (see Section 3 of [HTTP-CACHING]) can be stored by the client, if it implements an HTTP cache. | N/A — poseidon implements no HTTP cache; confirm no caching layer exists in client/ or http3/ that could store pushed responses. |
| `4.4-4.6-46` | MUST NOT | Pushed responses that are not cacheable must not be stored by any HTTP cache. <br>_(client [CLIENT])_ | Pushed responses that are not cacheable MUST NOT be stored by any HTTP cache. | **VERIFY** Trivially satisfied — no cache and no push acceptance anywhere in http3/; re-verify if either is ever added. -- Quote is verbatim (line 1020) and the MUST NOT level is correct, but applies_to ["client"] is too narrow. The RFC binds "any HTTP cache" — including intermediary/proxy caches — not the client alone. The narrowing is demonstrably wrong because the immediately preceding sentence (lines 1013-1014, fact -45) is deliberately client-scoped ("can be stored by the client, if it implements an HTTP cache") while this one widens to "any HTTP cache"; flattening both to ["client"] erases that contrast. Fix: set applies_to to ["all"] (or ["client","proxy"]) and keep client_relevant true; the requirement prose and reconcile note need no change. |
| `4.4-4.6-47` | MAY | Non-cacheable pushed responses may still be made available to the application separately from any cache. <br>_(client [CLIENT])_ | They MAY be made available to the application separately. | N/A while push is disabled; would shape any future push-delivery API surface in http3/client.go. |

### 9114 section 5 - Connection Closure

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `5-1` | informative | A QUIC connection idle for longer than the negotiated idle timeout is assumed closed by the peer; each endpoint declares its idle timeout in the handshake. <br>_(endpoint [CLIENT])_ | If the QUIC connection remains idle (no packets received) for longer than this duration, the peer will assume that the connection has been closed. | quic/ — verify max_idle_timeout transport param is honored and idle expiry tears down the connection and is surfaced to http3/client.go. |
| `5-2` | SHOULD | An HTTP/3 implementation SHOULD open a new connection for new requests when the existing connection is approaching the negotiated idle timeout. <br>_(all [CLIENT])_ | they SHOULD do so if approaching the idle timeout | http3/client.go + client/ pool — check conn-reuse logic consults remaining idle time before dispatching; dial fresh conn when near timeout (likely missing). |
| `5-3` | informative | HTTP clients are expected to ask the transport to keep the connection open while responses to requests or server pushes are outstanding. <br>_(client [CLIENT])_ | HTTP clients are expected to request that the transport keep connections open while there are responses outstanding for requests or server pushes | quic/ — check for a keep-alive (PING) mechanism while http3/stream.go has responses pending, so idle timeout cannot kill an in-flight request. |
| `5-4` | informative | When the client expects no response, letting the idle connection time out is preferred over spending effort keeping it alive. <br>_(client [CLIENT])_ | allowing an idle connection to time out is preferred over expending effort maintaining a connection that might not be needed | http3/client.go — do not send keep-alives on conns with zero outstanding requests; idle conns may lapse. |
| `5-5` | MAY | A gateway MAY keep connections open in anticipation of need to avoid connection-establishment latency to servers. <br>_(intermediary [not-client])_ | A gateway MAY maintain connections in anticipation of need rather than incur the latency cost of connection establishment to servers. | N/A — this client is not a gateway; no code path. |
| `5-6` | SHOULD NOT | Servers SHOULD NOT actively keep connections open. <br>_(server [not-client])_ | Servers SHOULD NOT actively keep connections open. | Server-only; no client obligation. |
| `5-7` | informative | Graceful shutdown of an HTTP/3 connection is initiated by sending a GOAWAY frame. <br>_(endpoint [CLIENT])_ | Endpoints initiate the graceful shutdown of an HTTP/3 connection by sending a GOAWAY frame. | http3/control.go — verify the client can emit GOAWAY on its control stream during graceful Close, and processes an inbound one. |
| `5-8` | informative | GOAWAY identifier type depends on sender: a server sends a client-initiated bidirectional stream ID, a client sends a push ID. <br>_(endpoint [CLIENT])_ | The server sends a client-initiated bidirectional stream ID; the client sends a push ID. | http3/control.go — inbound GOAWAY payload interpreted as request stream ID; any client-sent GOAWAY must carry a push ID, never a stream ID. |
| `5-9` | informative | Requests or pushes with identifier greater than or equal to the GOAWAY identifier are rejected by the GOAWAY sender. <br>_(sender [CLIENT])_ | Requests or pushes with the indicated identifier or greater are rejected (Section 4.1.1) by the sender of the GOAWAY. | http3/client.go — mark requests on stream IDs >= GOAWAY id as rejected (not processed) and surface a retryable error. |
| `5-10` | MAY | The GOAWAY identifier MAY be zero when no requests or pushes were processed. <br>_(sender [CLIENT])_ | This identifier MAY be zero if no requests or pushes were processed. | http3/control.go — GOAWAY parser must accept identifier 0 (rejects everything) without error. |
| `5-11` | SHOULD | Upon sending GOAWAY, the endpoint SHOULD explicitly cancel any requests or pushes with identifiers greater than or equal to the indicated one, to clean up transport state. <br>_(endpoint [CLIENT])_ | the endpoint SHOULD explicitly cancel (see Sections 4.1.1 and 7.2.3) any requests or pushes that have identifiers greater than or equal to the one indicated | http3/control.go — when the client sends GOAWAY, cancel (CANCEL_PUSH / reset via quic/) push streams with push ID >= the value. |
| `5-12` | SHOULD | The GOAWAY sender SHOULD keep cancelling affected requests or pushes as more of them arrive after the GOAWAY. <br>_(endpoint [CLIENT])_ | The endpoint SHOULD continue to do so as more requests or pushes arrive. | http3/control.go — post-GOAWAY, late-arriving push streams >= the sent push ID must still be cancelled, not just those known at send time. |
| `5-13` | MUST NOT | After receiving a GOAWAY from the peer, an endpoint MUST NOT initiate new requests or promise new pushes on that connection. <br>_(endpoint [CLIENT])_ | Endpoints MUST NOT initiate new requests or promise new pushes on the connection after receipt of a GOAWAY frame from the peer. | http3/client.go + control.go — a received-GOAWAY flag must gate Client.Do so no new request stream opens on that conn. |
| `5-14` | MAY | Clients MAY open a new connection to send additional requests after receiving GOAWAY. <br>_(client [CLIENT])_ | Clients MAY establish a new connection to send additional requests. | client/ transport + http3/dial.go — on GOAWAY, pool should dial a replacement conn for subsequent requests instead of failing them. |
| `5-15` | informative | Requests already sent with stream ID >= the GOAWAY identifier will not be processed and can be safely retried on a different connection. <br>_(client [CLIENT])_ | Clients can safely retry unprocessed requests on a different HTTP connection. | client/ retry — requests on stream IDs >= GOAWAY id are provably unprocessed: safe to auto-retry even non-idempotent methods. |
| `5-16` | informative | Requests on stream IDs below the server's GOAWAY identifier may have been processed; status is unknown until a response, individual reset, lower GOAWAY, or connection termination. <br>_(client [CLIENT])_ | Requests on stream IDs less than the stream ID in a GOAWAY frame from the server might have been processed; their status cannot be known until a response is received | client/ retry — streams below the GOAWAY id must NOT be blind-retried; wait for response/reset or apply idempotency rules. |
| `5-17` | MAY | Servers MAY reject individual unprocessed requests even on streams below the GOAWAY identifier. <br>_(server [CLIENT])_ | Servers MAY reject individual requests on streams below the indicated ID if these requests were not processed. | http3/stream.go — client must handle H3_REQUEST_REJECTED stream reset on streams below the GOAWAY id as retryable. |
| `5-18` | informative | A server receiving a client GOAWAY does not get pushes accepted whose push ID is greater than or equal to the identifier. <br>_(server [CLIENT])_ | having promised pushes with a push ID greater than or equal to the identifier contained in the GOAWAY frame, those pushes will not be accepted | **VERIFY** http3/control.go — this defines the client's sent-GOAWAY semantics: push ID 0 rejects all pushes (appropriate since push is unsupported). -- Role list is wrong. The source sentence (lines 1097-1099) is explicitly scoped to one role: "If a server receives a GOAWAY frame after having promised pushes with a push ID greater than or equal to the identifier contained in the GOAWAY frame, those pushes will not be accepted." The acting party is the server (the GOAWAY receiver), not a generic endpoint — this bullet is the server-side mirror of the client-side bullet captured in 5-15/5-16, so it is not symmetric across endpoints. The catalog elsewhere uses fine-grained roles for exactly this distinction (5-9 "sender", 5-22 "receiver", 5-17/5-19 "server"), so "endpoint" is both imprecise and inconsistent. Fix: applies_to should be ["server"] (or ["receiver"]). quote is verbatim and level "informative" is correct (no RFC 2119 keyword); client_relevant:true is defensible since it defines the semantics of a GOAWAY the client sends. |
| `5-19` | SHOULD | Servers SHOULD send a GOAWAY when connection closure is known in advance, even with little notice, so the peer learns what was partially processed. <br>_(server [not-client])_ | Servers SHOULD send a GOAWAY frame when the closing of a connection is known in advance, even if the advance notice is small | Server-side duty; client merely benefits (see 5-16/5-37 for the client consequence). |
| `5-20` | MAY | An endpoint MAY send multiple GOAWAY frames with different identifiers. <br>_(endpoint [CLIENT])_ | An endpoint MAY send multiple GOAWAY frames indicating different identifiers | http3/control.go — inbound handler must accept repeated GOAWAY frames (equal or decreasing id) without treating the second as an error. |
| `5-21` | MUST NOT | In a subsequent GOAWAY the identifier MUST NOT be greater than any previously sent identifier, since the peer may already have retried unprocessed requests elsewhere. <br>_(sender [CLIENT])_ | the identifier in each frame MUST NOT be greater than the identifier in any previous frame | http3/control.go — if the client ever sends multiple GOAWAYs, enforce non-increasing push ID on the send path. |
| `5-22` | MUST | Receiving a GOAWAY whose identifier is larger than a previously received one MUST be treated as a connection error of type H3_ID_ERROR. <br>_(receiver [CLIENT])_ | Receiving a GOAWAY containing a larger identifier than previously received MUST be treated as a connection error of type H3_ID_ERROR. | http3/control.go — track lowest received GOAWAY id; on increase, close the connection via quic/ with H3_ID_ERROR (0x0108). |
| `5-23` | informative | To begin graceful shutdown an endpoint can first send a GOAWAY with the maximum possible identifier: 2^62-4 for servers, 2^62-1 for clients, stopping new requests/pushes. <br>_(endpoint [CLIENT])_ | can send a GOAWAY frame with a value set to the maximum possible value (2^62-4 for servers, 2^62-1 for clients) | http3/control.go + quic/ varint — client's graceful-shutdown GOAWAY may use 2^62-1; parser must accept 2^62-4 from server (max 8-byte varint values). |
| `5-24` | informative | After the max-value GOAWAY and time for in-flight work to arrive, the endpoint can send another GOAWAY with the final accepted identifier for lossless shutdown. <br>_(endpoint [CLIENT])_ | the endpoint can send another GOAWAY frame indicating which requests or pushes it might accept before the end of the connection | http3/control.go — the two-step shutdown implies receiving a lowered id after a max-value GOAWAY is normal; verify handler updates to the lower id. |
| `5-25` | informative | A client GOAWAY push ID of 2^62-1 means the server may continue fulfilling pushes already promised. <br>_(client [CLIENT])_ | A value of 2^62-1 indicates that the server can continue fulfilling pushes that have already been promised. | http3/control.go — choose the client GOAWAY value deliberately: 2^62-1 keeps promised pushes alive; this client (no push support) should not use it. |
| `5-26` | informative | A smaller client GOAWAY value means the client will reject pushes with push IDs greater than or equal to that value. <br>_(client [CLIENT])_ | A smaller value indicates the client will reject pushes with push IDs greater than or equal to this value. | http3/ push-stream handling — after sending a finite push-ID GOAWAY, incoming push streams with id >= value must be rejected/cancelled. |
| `5-27` | MAY | The client MAY send subsequent GOAWAY frames provided each specified push ID is no greater than any previously sent value. <br>_(client [CLIENT])_ | the client MAY send subsequent GOAWAY frames so long as the specified push ID is no greater than any previously sent value | http3/control.go — send path must remember the last sent push ID and refuse to send a larger one. |
| `5-28` | informative | Even after GOAWAY marks a request or push as not-to-be-processed, transport resources persist; the initiating endpoint can cancel them to clean up state. <br>_(endpoint [CLIENT])_ | The endpoint that initiated these requests can cancel them to clean up transport state. | http3/stream.go + quic/ — when GOAWAY rejects a request, client should reset the send side and STOP_SENDING the QUIC stream, not leak it. |
| `5-29` | MAY | Once all accepted requests and pushes are processed, the endpoint may let the connection go idle or MAY immediately close it. <br>_(endpoint [CLIENT])_ | the endpoint can permit the connection to become idle, or it MAY initiate an immediate closure of the connection | http3/client.go Close — after draining in-flight requests, immediate quic close is permitted. |
| `5-30` | SHOULD | An endpoint completing a graceful shutdown SHOULD close the connection with the H3_NO_ERROR error code. <br>_(endpoint [CLIENT])_ | An endpoint that completes a graceful shutdown SHOULD use the H3_NO_ERROR error code when closing the connection. | http3/client.go Close → quic/ CONNECTION_CLOSE — verify graceful close sends application error H3_NO_ERROR (0x0100), not 0 or a transport code. |
| `5-31` | informative | If the client has exhausted all bidirectional stream IDs with requests, the server need not send GOAWAY since no further requests are possible. <br>_(server [not-client])_ | If a client has consumed all available bidirectional stream IDs with requests, the server need not send a GOAWAY frame | No client action; quic/ stream-ID limit handling already prevents opening beyond the space. |
| `5-32` | informative | Immediate application closure sends a QUIC CONNECTION_CLOSE whose application error code (from Section 8) tells the peer why the connection is being closed. <br>_(endpoint [CLIENT])_ | This results in sending a QUIC CONNECTION_CLOSE frame to the peer indicating that the application layer has terminated the connection. | quic/ connection-close API must carry an application error code; check http3 error-code plumbing into it and parsing of the peer's code on receipt. |
| `5-33` | MAY | Before an immediate close, a GOAWAY frame MAY be sent so the client can retry some requests. <br>_(endpoint [CLIENT])_ | Before closing the connection, a GOAWAY frame MAY be sent to allow the client to retry some requests. | http3/control.go + quic/ — client must still process a GOAWAY delivered immediately before (or alongside) CONNECTION_CLOSE and use it for retry decisions. |
| `5-34` | informative | Coalescing the GOAWAY frame into the same packet as the QUIC CONNECTION_CLOSE improves the odds the client receives it. <br>_(sender [CLIENT])_ | Including the GOAWAY frame in the same packet as the QUIC CONNECTION_CLOSE frame improves the chances of the frame being received by clients. | quic/ packet receive path — verify STREAM frames coalesced ahead of CONNECTION_CLOSE in one packet are still delivered to http3 before teardown. |
| `5-35` | informative | Open streams not explicitly closed are implicitly closed when the connection closes. <br>_(endpoint [CLIENT])_ | If there are open streams that have not been explicitly closed, they are implicitly closed when the connection is closed | quic/ — connection close must terminate every stream; http3/stream.go and stream_body.go pending reads/writes must unblock with a connection-closed error. |
| `5-36` | informative | The QUIC transport may signal connection termination to the application layer due to peer closure, a transport error, or connectivity loss. <br>_(endpoint [CLIENT])_ | the QUIC transport could indicate to the application layer that the connection has terminated. | quic/ → http3/client.go — transport-level termination (peer close, error, path loss) must surface as a distinct error to in-flight requests. |
| `5-37` | MUST | If the connection terminates without a GOAWAY, clients MUST assume every request sent, in whole or in part, might have been processed. <br>_(client [CLIENT])_ | If a connection terminates without a GOAWAY frame, clients MUST assume that any request that was sent, whether in whole or in part, might have been processed. | client/ retry policy for H3 — on transport close with no GOAWAY, only idempotent/safe requests may auto-retry; never blind-retry non-idempotent ones. |

### 9114 section 6 - Stream Mapping and Usage (bidirectional streams, unidirectional streams, control streams, push streams, reserved stream types)

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `6-1` | informative | HTTP/3 relies on QUIC's in-order reliable byte stream and does not use QUIC's out-of-order delivery capability within a stream. <br>_(all [CLIENT])_ | Although QUIC permits out-of-order delivery within a stream, HTTP/3 does not make use of this feature. | quic/ stream receive path: verify http3/ frame parsing only ever consumes the ordered byte stream exposed by quic streams, never raw offsets. |
| `6-2` | informative | Data on any QUIC stream maps either to one HTTP transaction or to the whole HTTP/3 connection context; no sub-stream multiplexing exists. <br>_(all [CLIENT])_ | data sent over a QUIC stream always maps to a particular HTTP transaction or to the entire HTTP/3 connection context | http3/stream.go + http3/control.go: confirm one request per bidi stream and connection-wide state only on control/uni streams. |
| `6-3` | informative | All client-initiated bidirectional streams are request streams carrying HTTP requests and responses. <br>_(client,all [CLIENT])_ | All client-initiated bidirectional streams are used for HTTP requests and responses. | http3/client.go + http3/stream.go: every request must open a client-initiated bidirectional quic stream; nothing else may be sent on bidi streams. |
| `6-4` | informative | The client's request streams occupy QUIC stream IDs 0, 4, 8, ... (client-initiated bidirectional numbering). <br>_(client [CLIENT])_ | the client's first request occurs on QUIC stream 0, with subsequent requests on streams 4, 8, and so on | quic/ stream ID allocation: client bidi streams must be ID % 4 == 0 and monotonically increasing. |
| `6-5` | SHOULD | A server should configure non-zero minimums for permitted streams and the initial stream flow-control window so client request streams can open. <br>_(server [not-client])_ | an HTTP/3 server SHOULD configure non-zero minimum values for the number of permitted streams and the initial stream flow-control window | Server-side only; N/A for http3/ client (no server implementation in this repo). |
| `6-6` | SHOULD | A server should permit at least 100 concurrent request streams. <br>_(server [not-client])_ | at least 100 request streams SHOULD be permitted at a time | Server-side only; the client may however rely on this when sizing its pool — see http3/ multiplexing plans, no code check needed. |
| `6-7` | MUST | A client must treat any server-initiated bidirectional stream as a connection error of type H3_STREAM_CREATION_ERROR unless an extension negotiated its use. <br>_(client [CLIENT])_ | Clients MUST treat receipt of a server-initiated bidirectional stream as a connection error of type H3_STREAM_CREATION_ERROR unless such an extension has been negotiated. | http3/client.go or control.go accept loop: any incoming server-initiated bidi stream from quic/ must close the connection with H3_STREAM_CREATION_ERROR (0x0103). |
| `6-8` | informative | Every unidirectional stream begins with a stream type encoded as a variable-length integer; the type determines the format of all following data. <br>_(all [CLIENT])_ | The purpose is indicated by a stream type, which is sent as a variable-length integer at the start of the stream. | http3/control.go incoming-uni-stream handler: first read a QUIC varint (internal/bytesx or quic/ varint codec) as stream type before any frame parsing. |
| `6-9` | informative | This document defines two unidirectional stream types (control, push); QPACK defines two more (encoder, decoder); extensions may define others. <br>_(all [CLIENT])_ | Two stream types are defined in this document: control streams (Section 6.2.1) and push streams (Section 6.2.2). [QPACK] defines two additional stream types. | http3/control.go uni-stream type dispatch: must recognize 0x00 control, 0x01 push, and the qpack/ encoder (0x02) / decoder (0x03) stream types. |
| `6-10` | MUST | Both clients and servers must send transport parameters allowing the peer to create at least three unidirectional streams. <br>_(client,server,endpoint [CLIENT])_ | the transport parameters sent by both clients and servers MUST allow the peer to create at least three unidirectional streams | http3/dial.go + quic/ transport parameters: client's initial_max_streams_uni must be >= 3. |
| `6-11` | SHOULD | Transport parameters should grant at least 1,024 bytes of flow-control credit to each unidirectional stream. <br>_(endpoint [CLIENT])_ | These transport parameters SHOULD also provide at least 1,024 bytes of flow-control credit to each unidirectional stream. | quic/ transport parameters set in http3/dial.go: initial_max_stream_data_uni >= 1024. |
| `6-12` | SHOULD | Endpoints should open the control stream and mandatory-extension streams (QPACK encoder/decoder) before any other unidirectional streams. <br>_(endpoint [CLIENT])_ | Endpoints SHOULD create the HTTP control stream as well as the unidirectional streams required by mandatory extensions (such as the QPACK encoder and decoder streams) first | http3/dial.go setup order: open control (0x00) then qpack encoder/decoder uni streams before issuing any request. |
| `6-13` | MUST | On receiving an unknown stream type, the recipient must either abort reading the stream or discard its data without processing. <br>_(receiver [CLIENT])_ | Recipients of unknown stream types MUST either abort reading of the stream or discard incoming data without further processing. | http3/control.go incoming-uni-stream handler: unknown type must STOP_SENDING (abort) or drain-and-discard; must not attempt to parse frames. |
| `6-14` | SHOULD | If the recipient aborts reading an unknown stream type, it should use H3_STREAM_CREATION_ERROR or a reserved error code. <br>_(receiver [CLIENT])_ | If reading is aborted, the recipient SHOULD use the H3_STREAM_CREATION_ERROR error code or a reserved error code | http3/control.go: when aborting unknown uni streams, the quic/ STOP_SENDING error code should be H3_STREAM_CREATION_ERROR (0x0103) or a reserved 0x1f*N+0x21 code. |
| `6-15` | MUST NOT | An unknown stream type must never be escalated to a connection error. <br>_(receiver [CLIENT])_ | The recipient MUST NOT consider unknown stream types to be a connection error of any kind. | http3/control.go: unknown uni-stream type path must never call quic/ connection close; stream-level handling only. |
| `6-16` | SHOULD NOT | A recipient should not discard incoming unidirectional stream data before it has read the stream type, since some types affect connection state. <br>_(receiver [CLIENT])_ | a recipient SHOULD NOT discard data from incoming unidirectional streams prior to reading the stream type | http3/control.go: always read the type varint before deciding to drop a uni stream (no blanket STOP_SENDING before the type byte). |
| `6-17` | MAY | Implementations may send stream types before knowing whether the peer supports them. <br>_(sender [CLIENT])_ | Implementations MAY send stream types before knowing whether the peer supports them. | http3/dial.go: permits sending control/QPACK streams immediately at connection start without waiting for peer SETTINGS. |
| `6-18` | MUST NOT | Stream types that could modify state or semantics of existing protocol components must not be sent until the peer is known to support them. <br>_(sender [CLIENT])_ | stream types that could modify the state or semantics of existing protocol components, including QPACK or other extensions, MUST NOT be sent until the peer is known to support them | http3/dial.go + control.go: client sends only mandatory types (0x00, QPACK 0x02/0x03) — verify no extension stream types are emitted before peer SETTINGS confirms support. |
| `6-19` | informative | A sender can close or reset a unidirectional stream unless a stream type specifies otherwise. <br>_(sender [CLIENT])_ | A sender can close or reset a unidirectional stream unless otherwise specified. | http3/control.go: closing rules per type — control stream is the 'otherwise specified' exception (see 6-25). |
| `6-20` | MUST | A receiver must tolerate unidirectional streams that are closed or reset before the stream-type header was received. <br>_(receiver [CLIENT])_ | A receiver MUST tolerate unidirectional streams being closed or reset prior to the reception of the unidirectional stream header. | http3/control.go uni-stream reader: EOF or quic/ RESET_STREAM before the type varint must be handled quietly, not treated as a connection error. |
| `6-21` | informative | The control stream has stream type 0x00 and carries HTTP/3 frames as defined in Section 7.2. <br>_(all [CLIENT])_ | A control stream is indicated by a stream type of 0x00. Data on this stream consists of HTTP/3 frames | http3/control.go: control stream type constant must be 0x00; payload parsed with the http3/frame.go codec. |
| `6-22` | MUST | Each endpoint must open exactly one control stream at connection start and send SETTINGS as the first frame on it. <br>_(endpoint [CLIENT])_ | Each side MUST initiate a single control stream at the beginning of the connection and send its SETTINGS frame as the first frame on this stream. | http3/dial.go + control.go: client must open its control stream and write SETTINGS before any request; verify SETTINGS is the very first frame written. |
| `6-23` | MUST | If the first frame on the peer's control stream is anything other than SETTINGS, it is a connection error of type H3_MISSING_SETTINGS. <br>_(receiver [CLIENT])_ | If the first frame of the control stream is any other frame type, this MUST be treated as a connection error of type H3_MISSING_SETTINGS. | http3/control.go peer-control-stream parser: first frame type != SETTINGS must close the connection with H3_MISSING_SETTINGS (0x010a). |
| `6-24` | MUST | Receipt of a second stream claiming to be a control stream is a connection error of type H3_STREAM_CREATION_ERROR. <br>_(receiver [CLIENT])_ | receipt of a second stream claiming to be a control stream MUST be treated as a connection error of type H3_STREAM_CREATION_ERROR | http3/control.go: track whether a peer control stream was already accepted; a second type-0x00 uni stream must trigger connection close H3_STREAM_CREATION_ERROR (0x0103). |
| `6-25` | MUST NOT | The sender must never close its control stream. <br>_(sender [CLIENT])_ | The sender MUST NOT close the control stream | http3/control.go: client must never FIN or reset its own control stream, including during GOAWAY/graceful shutdown — only the QUIC connection close ends it. |
| `6-26` | MUST NOT | The receiver must never request that the sender close the control stream (no STOP_SENDING on it). <br>_(receiver [CLIENT])_ | the receiver MUST NOT request that the sender close the control stream | http3/control.go: never issue quic/ STOP_SENDING against the peer's control stream. |
| `6-27` | MUST | If either control stream is closed at any point, it is a connection error of type H3_CLOSED_CRITICAL_STREAM. <br>_(endpoint [CLIENT])_ | If either control stream is closed at any point, this MUST be treated as a connection error of type H3_CLOSED_CRITICAL_STREAM. | http3/control.go reader: EOF or RESET_STREAM on the peer control stream (and reset of our own) must close the connection with H3_CLOSED_CRITICAL_STREAM (0x0104). |
| `6-28` | SHOULD | Endpoints should grant enough flow-control credit that the peer's control stream never becomes blocked. <br>_(endpoint [CLIENT])_ | endpoints SHOULD provide enough flow-control credit to keep the peer's control stream from becoming blocked | quic/ stream flow control: MAX_STREAM_DATA updates for the peer control stream must be issued promptly (check window refill logic used by http3/control.go). |
| `6-29` | informative | A push stream starts with stream type 0x01 followed by the push ID as a variable-length integer; remaining data consists of HTTP/3 frames. <br>_(all [CLIENT])_ | A push stream is indicated by a stream type of 0x01, followed by the push ID of the promise that it fulfills, encoded as a variable-length integer. | http3/control.go incoming-uni dispatch: type 0x01 must parse a Push ID varint next; since this client doesn't offer push (no MAX_PUSH_ID), verify handling is defined and safe. |
| `6-30` | informative | A push stream fulfills a promise with zero or more interim HTTP responses followed by a single final HTTP response. <br>_(server,client [CLIENT])_ | fulfills a promised server push by zero or more interim HTTP responses followed by a single final HTTP response | http3/response.go: only relevant if push is ever consumed; currently document as N/A while push is unsupported. |
| `6-31` | MUST | Only servers can push; a server receiving a client-initiated push stream treats it as a connection error of type H3_STREAM_CREATION_ERROR — so a client must never open a push stream. <br>_(server,client [CLIENT])_ | if a server receives a client-initiated push stream, this MUST be treated as a connection error of type H3_STREAM_CREATION_ERROR | http3/dial.go + control.go: verify the client never opens a uni stream with type 0x01 (only 0x00 and QPACK types are ever sent). |
| `6-32` | SHOULD NOT | A client should not abort reading a push stream before it has read the push stream header, to avoid disagreement about consumed push IDs. <br>_(client [CLIENT])_ | A client SHOULD NOT abort reading on a push stream prior to reading the push stream header | http3/control.go: if a type-0x01 stream arrives, read the Push ID varint before any STOP_SENDING/cancel. |
| `6-33` | MUST | Each push ID may appear in at most one push stream header. <br>_(server,sender [CLIENT])_ | Each push ID MUST only be used once in a push stream header. | Server-side emission rule, but the client enforces it via 6-34; if push is ever supported, http3/ must track seen push IDs. |
| `6-34` | MUST | If a client detects a push ID reused across two push stream headers, it must treat this as a connection error of type H3_ID_ERROR. <br>_(client [CLIENT])_ | the client MUST treat this as a connection error of type H3_ID_ERROR | http3/control.go: if push streams are parsed at all, keep a set of seen push IDs and close the connection with H3_ID_ERROR (0x0108) on a duplicate. |
| `6-35` | informative | Stream types 0x1f * N + 0x21 (N >= 0) are reserved grease types with no semantics, used to exercise the unknown-type handling requirement. <br>_(all [CLIENT])_ | Stream types of the format 0x1f * N + 0x21 for non-negative integer values of N are reserved to exercise the requirement that unknown types be ignored | http3/control.go: greased types must fall into the unknown-stream-type path (6-13..6-16) with no special-casing; add a conformance test feeding 0x21, 0x40+0x21 etc. |
| `6-36` | MAY | Reserved (grease) streams may also be sent on connections where no data is currently being transferred. <br>_(sender [CLIENT])_ | They MAY also be sent on connections where no data is currently being transferred. | Optional sender behavior; http3/ need not send grease streams — no code required unless grease emission is added. |
| `6-37` | MUST NOT | Endpoints must not ascribe any meaning to received reserved-type streams. <br>_(endpoint,receiver [CLIENT])_ | Endpoints MUST NOT consider these streams to have any meaning upon receipt. | http3/control.go: reserved-type streams must be discarded/aborted like any unknown type without side effects on connection state. |
| `6-38` | MAY | When sending a reserved stream type, the sender may either terminate the stream cleanly or reset it; payload and length are arbitrary. <br>_(sender [CLIENT])_ | the implementation MAY either terminate the stream cleanly or reset it | Optional; only relevant if http3/ ever emits grease streams. |
| `6-39` | SHOULD | When resetting a reserved stream, the sender should use the H3_NO_ERROR error code or a reserved error code. <br>_(sender [CLIENT])_ | When resetting the stream, either the H3_NO_ERROR error code or a reserved error code (Section 8.1) SHOULD be used. | Optional; if grease emission is added, quic/ RESET_STREAM code must be H3_NO_ERROR (0x0100) or a reserved 0x1f*N+0x21 code. |

### 9114 section 7-7.2.3 - HTTP Framing Layer: frame/stream mapping, frame layout, DATA, HEADERS, CANCEL_PUSH

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `7-7.2.3-1` | informative | HTTP/3 defines exactly three stream types on which frames are carried: control stream, request stream, and push stream. <br>_(all [CLIENT])_ | HTTP/3 defines three stream types: control stream, request stream, and push stream. | http3/dial.go + http3/control.go + http3/stream.go: client opens control + request streams; verify inbound uni-stream classifier also recognizes push-stream type. |
| `7-7.2.3-2` | informative | Per Table 1, DATA frames are permitted on request and push streams but not on the control stream. <br>_(all [CLIENT])_ | \| DATA \| No \| Yes \| Yes \| | http3/control.go: control-stream frame dispatch must reject DATA with H3_FRAME_UNEXPECTED; http3/stream.go accepts it on request streams. |
| `7-7.2.3-3` | informative | Per Table 1, HEADERS frames are permitted on request and push streams but not on the control stream. <br>_(all [CLIENT])_ | \| HEADERS \| No \| Yes \| Yes \| | http3/control.go: control-stream dispatch must reject HEADERS; http3/stream.go handles HEADERS on request streams. |
| `7-7.2.3-4` | informative | Per Table 1, CANCEL_PUSH is permitted only on the control stream, never on request or push streams. <br>_(all [CLIENT])_ | \| CANCEL_PUSH \| Yes \| No \| No \| | http3/stream.go: request-stream frame dispatch must treat CANCEL_PUSH as H3_FRAME_UNEXPECTED; http3/control.go must parse it on control stream. |
| `7-7.2.3-5` | informative | Per Table 1, SETTINGS is permitted only on the control stream, and only as its first frame. <br>_(all [CLIENT])_ | \| SETTINGS \| Yes (1) \| No \| No \| | http3/control.go: SETTINGS must be first frame written on our control stream; SETTINGS on request stream (http3/stream.go) must be H3_FRAME_UNEXPECTED. |
| `7-7.2.3-6` | informative | The SETTINGS frame can appear only as the very first frame of a control stream, never later or elsewhere. <br>_(all [CLIENT])_ | The SETTINGS frame can only occur as the first frame of a Control stream | http3/control.go: verify client writes SETTINGS immediately after stream-type varint, and requires peer's first control frame to be SETTINGS (non-SETTINGS first / second SETTINGS -> error per 7.2.4). |
| `7-7.2.3-7` | informative | Per Table 1, PUSH_PROMISE is permitted only on request streams, not on control or push streams. <br>_(all [CLIENT])_ | \| PUSH_PROMISE \| No \| Yes \| No \| | http3/control.go: PUSH_PROMISE on control stream must be H3_FRAME_UNEXPECTED; http3/stream.go must handle (or reject per push policy) PUSH_PROMISE arriving on request streams. |
| `7-7.2.3-8` | informative | Per Table 1, GOAWAY is permitted only on the control stream. <br>_(all [CLIENT])_ | \| GOAWAY \| Yes \| No \| No \| | http3/control.go handles GOAWAY from control stream; http3/stream.go must reject GOAWAY on request streams with H3_FRAME_UNEXPECTED. |
| `7-7.2.3-9` | informative | Per Table 1, MAX_PUSH_ID is permitted only on the control stream. <br>_(all [CLIENT])_ | \| MAX_PUSH_ID \| Yes \| No \| No \| | http3/stream.go: MAX_PUSH_ID on a request stream must be H3_FRAME_UNEXPECTED; http3/control.go: client receiving MAX_PUSH_ID is also an error per Section 7.2.7 — check dispatch. |
| `7-7.2.3-10` | informative | Per Table 1, reserved (grease) frame types are permitted on all three stream types and must be tolerable everywhere. <br>_(all [CLIENT])_ | \| Reserved \| Yes \| Yes \| Yes \| | http3/frame.go + control.go + stream.go: unknown/reserved frame types must be skipped by consuming exactly Length payload bytes on every stream type, not treated as errors. |
| `7-7.2.3-11` | informative | HTTP/3 frames can span multiple QUIC packets, so the frame parser must handle frames delivered incrementally across stream reads. <br>_(all [CLIENT])_ | unlike QUIC frames, HTTP/3 frames can span multiple packets. | http3/frame.go: reader must reassemble a frame across partial reads from the quic/ stream (no assumption that a frame arrives in one read). |
| `7-7.2.3-12` | informative | Every HTTP/3 frame is laid out as a variable-length-integer Type, a variable-length-integer Length (payload byte count), then the Frame Payload. <br>_(all [CLIENT])_ | Type (i), Length (i), Frame Payload (..), | http3/frame.go: frame header codec must read/write Type and Length as QUIC varints (internal/bytesx varint via quic/), Length = payload bytes only. |
| `7-7.2.3-13` | MUST | A frame payload must contain exactly the fields defined for that frame type — no more, no fewer. <br>_(all [CLIENT])_ | Each frame's payload MUST contain exactly the fields identified in its description. | http3/frame.go: per-type parsers (CANCEL_PUSH, GOAWAY, MAX_PUSH_ID, SETTINGS) must consume exactly Length bytes and fail otherwise. |
| `7-7.2.3-14` | MUST | A payload with trailing extra bytes after its defined fields, or one that ends before the fields are complete, is a connection error of type H3_FRAME_ERROR. <br>_(receiver [CLIENT])_ | a frame payload that terminates before the end of the identified fields MUST be treated as a connection error of type H3_FRAME_ERROR | http3/frame.go + control.go: verify both under- and over-length payloads (e.g. CANCEL_PUSH with leftover bytes) close the connection with H3_FRAME_ERROR (0x106), not a stream error. |
| `7-7.2.3-15` | MUST | Where a frame contains internal length encodings redundant with the frame Length, the receiver must verify they are self-consistent. <br>_(receiver [CLIENT])_ | redundant length encodings MUST be verified to be self-consistent | http3/frame.go (and qpack/decoder.go for encoded field sections): inner varint-length fields must be bounds-checked against the outer frame Length; mismatch -> H3_FRAME_ERROR. |
| `7-7.2.3-16` | MUST | If a stream ends cleanly (FIN) in the middle of a frame, the truncated last frame is a connection error of type H3_FRAME_ERROR. <br>_(receiver [CLIENT])_ | When a stream terminates cleanly, if the last frame on the stream was truncated, this MUST be treated as a connection error of type H3_FRAME_ERROR. | http3/stream.go + stream_body.go: FIN arriving mid-frame (header or payload short of Length) must surface a connection error H3_FRAME_ERROR, not plain io.EOF. |
| `7-7.2.3-17` | informative | Streams that terminate abruptly (reset) may be cut off at any point within a frame; this is not a frame error. <br>_(all [CLIENT])_ | Streams that terminate abruptly may be reset at any point in a frame. | http3/stream.go: a quic/ RESET_STREAM mid-frame must be handled as stream abort, not escalated to H3_FRAME_ERROR connection error. |
| `7-7.2.3-18` | informative | DATA frame type codepoint is 0x00 and its payload is the raw Data bytes of message content. <br>_(all [CLIENT])_ | DATA frames (type=0x00) convey arbitrary, variable-length sequences of bytes associated with HTTP request or response content. | http3/frame.go: DATA type constant == 0x00; payload passed through opaque to stream_body.go. |
| `7-7.2.3-19` | MUST | DATA frames must always be associated with an HTTP request or response (i.e., carried on request or push streams only). <br>_(all [CLIENT])_ | DATA frames MUST be associated with an HTTP request or response. | http3/stream_body.go + request.go: client emits DATA only on request streams and maps received DATA only to a response body. |
| `7-7.2.3-20` | MUST | Receiving a DATA frame on the control stream requires responding with a connection error of type H3_FRAME_UNEXPECTED. <br>_(receiver [CLIENT])_ | If a DATA frame is received on a control stream, the recipient MUST respond with a connection error of type H3_FRAME_UNEXPECTED. | http3/control.go: control-stream dispatch must close the connection with H3_FRAME_UNEXPECTED (0x105) on DATA — check via quic/ connection close, not stream reset. |
| `7-7.2.3-21` | informative | HEADERS frame type codepoint is 0x01 and its payload is an Encoded Field Section produced by QPACK. <br>_(all [CLIENT])_ | The HEADERS frame (type=0x01) is used to carry an HTTP field section that is encoded using QPACK. | http3/frame.go: HEADERS type constant == 0x01; payload encoded by qpack/encoder.go on send and decoded by qpack/decoder.go on receive — no other encoding path. |
| `7-7.2.3-22` | informative | HEADERS frames may only be sent on request streams or push streams, never on the control stream. <br>_(sender [CLIENT])_ | HEADERS frames can only be sent on request streams or push streams. | http3/request.go + stream.go: verify the client's HEADERS write path exists only for request streams; http3/control.go must have no HEADERS emit path. |
| `7-7.2.3-23` | MUST | Receiving a HEADERS frame on the control stream requires responding with a connection error of type H3_FRAME_UNEXPECTED. <br>_(receiver [CLIENT])_ | If a HEADERS frame is received on a control stream, the recipient MUST respond with a connection error of type H3_FRAME_UNEXPECTED. | http3/control.go: control-stream dispatch must connection-error with H3_FRAME_UNEXPECTED on HEADERS. |
| `7-7.2.3-24` | informative | CANCEL_PUSH frame type codepoint is 0x03 and its payload is exactly one Push ID encoded as a variable-length integer. <br>_(all [CLIENT])_ | The CANCEL_PUSH frame (type=0x03) is used to request cancellation of a server push prior to the push stream being received. | http3/frame.go: CANCEL_PUSH constant == 0x03; parser reads a single varint Push ID and enforces it fills the frame Length exactly (else H3_FRAME_ERROR). |
| `7-7.2.3-25` | SHOULD | On receiving a client's CANCEL_PUSH, the server should abort sending the promised resource. <br>_(server [not-client])_ | The server SHOULD abort sending the resource | Server-only behavior; no client code path — n/a for http3/. |
| `7-7.2.3-26` | SHOULD | If the corresponding push stream is already open when CANCEL_PUSH is processed, the server should abruptly terminate that stream. <br>_(server [not-client])_ | If the push stream is open, the server SHOULD abruptly terminate that stream. | Server-only; n/a for the client stack. |
| `7-7.2.3-27` | MAY | If the push stream has already ended, the server may still abruptly terminate it or may take no action. <br>_(server [not-client])_ | the server MAY still abruptly terminate the stream or MAY take no action. | Server-only; n/a for the client stack. |
| `7-7.2.3-28` | informative | When the server sends CANCEL_PUSH, the client must treat the corresponding promise as unfulfilled unless the pushed response was already fully received and processed. <br>_(client [CLIENT])_ | The client cannot expect the corresponding promise to be fulfilled, unless it has already received and processed the promised response. | http3/control.go: client must accept CANCEL_PUSH from the server on the control stream and drop any pending push expectation (with no push support, parse-and-tolerate, subject to fact 35's ID check). |
| `7-7.2.3-29` | SHOULD | A server should send CANCEL_PUSH whenever it determines a previously sent promise will not be fulfilled, regardless of push-stream state. <br>_(server [not-client])_ | a server SHOULD send a CANCEL_PUSH frame when it determines that promise will not be fulfilled. | Server-only; n/a for the client stack. |
| `7-7.2.3-30` | informative | A server cancelling an already-opened push stream can abort sending on it with error code H3_REQUEST_CANCELLED. <br>_(server [not-client])_ | the server can abort sending on the stream with an error code of H3_REQUEST_CANCELLED. | Server-only; client side only needs to tolerate a reset push stream (quic/ reset handling). |
| `7-7.2.3-31` | informative | Sending CANCEL_PUSH does not by itself change the state of any existing push stream. <br>_(all [CLIENT])_ | Sending a CANCEL_PUSH frame has no direct effect on the state of existing push streams. | http3/control.go: receiving/sending CANCEL_PUSH must not mutate quic/ stream state directly; stream teardown is separate. |
| `7-7.2.3-32` | SHOULD NOT | A client should not send CANCEL_PUSH for a push whose push stream it has already received. <br>_(client [CLIENT])_ | A client SHOULD NOT send a CANCEL_PUSH frame when it has already received a corresponding push stream. | http3/client.go + control.go: client currently has no push support — verify there is no CANCEL_PUSH send path at all (trivially conformant); if push is added, gate sends on push-stream receipt. |
| `7-7.2.3-33` | SHOULD | If a push stream arrives after the client sent CANCEL_PUSH for it, the client should abort reading that stream with error code H3_REQUEST_CANCELLED. <br>_(client [CLIENT])_ | The client SHOULD abort reading the stream with an error code of H3_REQUEST_CANCELLED. | http3/dial.go / control.go inbound uni-stream path: push-typed streams must be abortable via quic/ STOP_SENDING with H3_REQUEST_CANCELLED (0x10C); with no MAX_PUSH_ID advertised, confirm the H3_ID_ERROR path applies instead. |
| `7-7.2.3-34` | MUST | Receiving CANCEL_PUSH on any stream other than the control stream is a connection error of type H3_FRAME_UNEXPECTED. <br>_(receiver [CLIENT])_ | Receiving a CANCEL_PUSH frame on a stream other than the control stream MUST be treated as a connection error of type H3_FRAME_UNEXPECTED. | http3/stream.go: request-stream frame dispatch must connection-error with H3_FRAME_UNEXPECTED on CANCEL_PUSH (0x03), not skip it as unknown. |
| `7-7.2.3-35` | MUST | A CANCEL_PUSH referencing a push ID beyond what is currently allowed on the connection (by MAX_PUSH_ID) is a connection error of type H3_ID_ERROR. <br>_(receiver [CLIENT])_ | If a CANCEL_PUSH frame is received that references a push ID greater than currently allowed on the connection, this MUST be treated as a connection error of type H3_ID_ERROR. | http3/control.go: validate CANCEL_PUSH Push ID against the MAX_PUSH_ID the client advertised (none advertised => every push ID is disallowed) and close with H3_ID_ERROR (0x108). |
| `7-7.2.3-36` | informative | A client must tolerate a CANCEL_PUSH naming an allowed push ID that no PUSH_PROMISE has mentioned yet, since frames can be reordered across streams. <br>_(client [CLIENT])_ | that frame might identify a push ID that has not yet been mentioned by a PUSH_PROMISE frame due to reordering. | http3/control.go: unknown-but-allowed push IDs in CANCEL_PUSH must NOT be treated as errors on the client side (asymmetric with the server rule in fact 37). |
| `7-7.2.3-37` | MUST | A server receiving CANCEL_PUSH for a push ID it has not yet mentioned in any PUSH_PROMISE must treat it as a connection error of type H3_ID_ERROR. <br>_(server [not-client])_ | If a server receives a CANCEL_PUSH frame for a push ID that has not yet been mentioned by a PUSH_PROMISE frame, this MUST be treated as a connection error | Server-only validation; n/a for the client stack (client-side rule is fact 36). |

### 9114 section 7.2.4-7.2.8 - SETTINGS (format, parameters, initialization/0-RTT), PUSH_PROMISE, GOAWAY, MAX_PUSH_ID, Reserved Frame Types

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `7.2.4-7.2.8-1` | informative | The SETTINGS frame is type 0x04 with a length-prefixed payload of zero or more Setting entries. <br>_(all [CLIENT])_ | SETTINGS Frame { Type (i) = 0x04, Length (i), Setting (..) ..., } | http3/frame.go: verify SETTINGS frame type constant is 0x04 and codec parses/emits varint type + varint length + settings payload. |
| `7.2.4-7.2.8-2` | informative | SETTINGS parameters are connection-scoped, never per-stream. <br>_(all [CLIENT])_ | SETTINGS frames always apply to an entire HTTP/3 connection, never a single stream. | http3/control.go: peer settings must be stored on the connection/Client object, not on individual request streams. |
| `7.2.4-7.2.8-3` | MUST | Each peer must send SETTINGS as the very first frame on its control stream. <br>_(endpoint,sender [CLIENT])_ | A SETTINGS frame MUST be sent as the first frame of each control stream (see Section 6.2.1) by each peer | http3/dial.go + http3/control.go: verify the client opens the control stream, writes the stream-type varint, then writes SETTINGS before any other frame. |
| `7.2.4-7.2.8-4` | MUST NOT | After the first SETTINGS frame, no further SETTINGS frames may be sent. <br>_(endpoint,sender [CLIENT])_ | it MUST NOT be sent subsequently | http3/control.go: verify no code path writes a second SETTINGS frame on the control stream after setup. |
| `7.2.4-7.2.8-5` | MUST | A second SETTINGS frame on the control stream is a connection error H3_FRAME_UNEXPECTED. <br>_(endpoint,receiver [CLIENT])_ | If an endpoint receives a second SETTINGS frame on the control stream, the endpoint MUST respond with a connection error of type H3_FRAME_UNEXPECTED. | http3/control.go: control-stream reader must track that SETTINGS was seen and close the connection (quic CONNECTION_CLOSE, code H3_FRAME_UNEXPECTED 0x0105) on a duplicate. |
| `7.2.4-7.2.8-6` | MUST NOT | SETTINGS may only be sent on the control stream. <br>_(endpoint,sender [CLIENT])_ | SETTINGS frames MUST NOT be sent on any stream other than the control stream. | http3/stream.go + http3/request.go: verify no path emits frame type 0x04 on a request stream. |
| `7.2.4-7.2.8-7` | MUST | Receiving SETTINGS on a non-control stream is a connection error H3_FRAME_UNEXPECTED. <br>_(endpoint,receiver [CLIENT])_ | If an endpoint receives a SETTINGS frame on a different stream, the endpoint MUST respond with a connection error of type H3_FRAME_UNEXPECTED. | http3/stream.go: request-stream frame dispatch must treat type 0x04 as a connection error H3_FRAME_UNEXPECTED, not as an ignorable unknown frame. |
| `7.2.4-7.2.8-8` | MUST NOT | A SETTINGS frame must not contain the same setting identifier twice. <br>_(sender [CLIENT])_ | The same setting identifier MUST NOT occur more than once in the SETTINGS frame. | http3/control.go: verify the SETTINGS encoder emits each identifier at most once. |
| `7.2.4-7.2.8-9` | MAY | A receiver may treat duplicate setting identifiers as connection error H3_SETTINGS_ERROR. <br>_(receiver [CLIENT])_ | A receiver MAY treat the presence of duplicate setting identifiers as a connection error of type H3_SETTINGS_ERROR. | http3/control.go: SETTINGS parser — decide and document whether duplicates trigger H3_SETTINGS_ERROR (0x0109) or last-wins; either is conformant. |
| `7.2.4-7.2.8-10` | informative | Each setting is an identifier/value pair, both encoded as QUIC varints. <br>_(all [CLIENT])_ | Each parameter consists of a setting identifier and a value, both encoded as QUIC variable-length integers. | http3/control.go + internal/bytesx (or quic/ varint): verify settings payload is parsed/emitted as consecutive varint pairs, rejecting truncated pairs. |
| `7.2.4-7.2.8-11` | MUST | Unknown setting identifiers must be ignored. <br>_(all [CLIENT])_ | An implementation MUST ignore any parameter with an identifier it does not understand. | http3/control.go: parseSettings must skip unknown identifiers without error — but see fact 17: reserved HTTP/2 IDs are NOT unknown, they are errors. |
| `7.2.4-7.2.8-12` | informative | SETTINGS_MAX_FIELD_SECTION_SIZE has identifier 0x06 and default value unlimited. <br>_(all [CLIENT])_ | SETTINGS_MAX_FIELD_SECTION_SIZE (0x06): The default value is unlimited. | http3/control.go + http3/request.go: verify constant 0x06, that absent peer setting means no limit on sent request field sections, and that any client-advertised value is enforced on received responses (validate.go/response.go). |
| `7.2.4-7.2.8-13` | informative | Setting identifiers 0x1f*N+0x21 are reserved grease codepoints with no meaning. <br>_(all [CLIENT])_ | Setting identifiers of the format 0x1f * N + 0x21 for non-negative integer values of N are reserved to exercise the requirement that unknown identifiers be ignored. | http3/control.go: verify grease setting IDs (e.g. 0x21, 0x40, 0x5f...) fall through the unknown-identifier ignore path on receipt. |
| `7.2.4-7.2.8-14` | SHOULD | Endpoints should send at least one grease setting in their SETTINGS frame. <br>_(endpoint,sender [CLIENT])_ | Endpoints SHOULD include at least one such setting in their SETTINGS frame. | http3/control.go: check whether the client's SETTINGS writer includes a grease setting (0x1f*N+0x21 with arbitrary value); add if missing. |
| `7.2.4-7.2.8-15` | MUST NOT | Received grease settings must be given no semantic meaning. <br>_(endpoint,receiver [CLIENT])_ | Endpoints MUST NOT consider such settings to have any meaning upon receipt. | http3/control.go: verify no special-casing of grease identifiers — they must be indistinguishable from unknown settings. |
| `7.2.4-7.2.8-16` | MUST NOT | HTTP/2 setting identifiers with no HTTP/3 equivalent must never be sent. <br>_(sender [CLIENT])_ | These reserved settings MUST NOT be sent | http3/control.go: verify the client's SETTINGS encoder never emits reserved IDs 0x00, 0x02, 0x03, 0x04, 0x05 (per §11.2.2). |
| `7.2.4-7.2.8-17` | MUST | Receipt of a reserved HTTP/2 setting identifier is a connection error H3_SETTINGS_ERROR. <br>_(receiver [CLIENT])_ | their receipt MUST be treated as a connection error of type H3_SETTINGS_ERROR | http3/control.go: parseSettings must special-case IDs 0x00, 0x02, 0x03, 0x04, 0x05 → connection error H3_SETTINGS_ERROR (0x0109), NOT the generic unknown-ignore path. |
| `7.2.4-7.2.8-18` | MUST NOT | An endpoint must not send frames or requests that violate its current knowledge of the peer's settings. <br>_(all,sender [CLIENT])_ | An HTTP implementation MUST NOT send frames or requests that would be invalid based on its current understanding of the peer's settings. | http3/request.go + http3/client.go: verify the encoded request field-section size is checked against a received peer SETTINGS_MAX_FIELD_SECTION_SIZE before sending. |
| `7.2.4-7.2.8-19` | SHOULD | Endpoints should assume initial/default setting values for messages sent before the peer's SETTINGS arrives. <br>_(endpoint,sender [CLIENT])_ | Each endpoint SHOULD use these initial values to send messages before the peer's SETTINGS frame has arrived | http3/client.go: verify requests may be sent before server SETTINGS arrive, using defaults (unlimited field section) until updated. |
| `7.2.4-7.2.8-20` | MUST NOT | An endpoint must not wait for any peer data before sending its own SETTINGS. <br>_(endpoint,sender [CLIENT])_ | Endpoints MUST NOT require any data to be received from the peer prior to sending the SETTINGS frame | http3/dial.go: verify the control stream + SETTINGS are written unconditionally during Dial, with no read-before-write dependency on the server. |
| `7.2.4-7.2.8-21` | MUST | SETTINGS must be sent as soon as the transport can send data. <br>_(endpoint,sender [CLIENT])_ | settings MUST be sent as soon as the transport is ready to send data | http3/dial.go + quic/: verify SETTINGS is flushed immediately after the QUIC handshake completes, before returning the Client or sending the first request. |
| `7.2.4-7.2.8-22` | informative | Servers initialize each client setting to its default value. <br>_(server [not-client])_ | For servers, the initial value of each client setting is the default value. | Server-side rule; no client code to check. |
| `7.2.4-7.2.8-23` | informative | On a 1-RTT connection, clients initialize each server setting to its default value. <br>_(client [CLIENT])_ | For clients using a 1-RTT QUIC connection, the initial value of each server setting is the default value. | http3/control.go + http3/client.go: verify peer-settings state initializes to RFC defaults (MAX_FIELD_SECTION_SIZE = unlimited) before the server's SETTINGS is processed. |
| `7.2.4-7.2.8-24` | SHOULD NOT | Clients should not block indefinitely waiting for the server's SETTINGS before sending requests. <br>_(client [CLIENT])_ | Clients SHOULD NOT wait indefinitely for SETTINGS to arrive before sending requests | http3/client.go: verify Do does not gate on receipt of server SETTINGS (no unbounded wait). |
| `7.2.4-7.2.8-25` | SHOULD | Clients should process received datagrams so SETTINGS is likely applied before the first request. <br>_(client [CLIENT])_ | they SHOULD process received datagrams in order to increase the likelihood of processing SETTINGS before sending the first request | quic/ receive loop + http3/dial.go: verify inbound packets (and thus the control stream) are being read/processed during and immediately after Dial. |
| `7.2.4-7.2.8-26` | informative | On a 0-RTT connection, initial server settings are the values from the previous session. <br>_(client [CLIENT])_ | When a 0-RTT QUIC connection is being used, the initial value of each server setting is the value used in the previous session. | quic/: this client does not implement 0-RTT (project non-goal) — verify no 0-RTT send path exists so this rule is vacuously satisfied; document in RFC_COVERAGE. |
| `7.2.4-7.2.8-27` | SHOULD | Clients should persist server-provided settings alongside resumption state. <br>_(client [CLIENT])_ | Clients SHOULD store the settings the server provided in the HTTP/3 connection where resumption information was provided | quic/ + http3/: vacuous without 0-RTT/resumption support; if session tickets are ever stored, settings must be stored with them. |
| `7.2.4-7.2.8-28` | MAY | Clients may skip storing settings in some cases (e.g., ticket arrives before SETTINGS). <br>_(client [CLIENT])_ | they MAY opt not to store settings in certain cases (e.g., if the session ticket is received before the SETTINGS frame) | quic/: vacuous without 0-RTT; note as N/A in coverage matrix. |
| `7.2.4-7.2.8-29` | MUST | A client attempting 0-RTT must comply with the stored settings, or defaults if none stored. <br>_(client [CLIENT])_ | A client MUST comply with stored settings -- or default values if no values are stored -- when attempting 0-RTT. | quic/: no 0-RTT attempt path exists — verify and mark vacuous; this MUST becomes load-bearing if 0-RTT is ever added. |
| `7.2.4-7.2.8-30` | MUST | Once the server's new SETTINGS arrives, the client must comply with those values. <br>_(client [CLIENT])_ | Once a server has provided new settings, clients MUST comply with those values. | http3/control.go: verify that applying the server's SETTINGS updates the live limits used by http3/request.go (e.g. MAX_FIELD_SECTION_SIZE checked at send time, not a stale snapshot). |
| `7.2.4-7.2.8-31` | MUST NOT | A server must reject 0-RTT data if it cannot confirm the client's remembered settings are compatible. <br>_(server [not-client])_ | If the server cannot determine that the settings remembered by a client are compatible with its current settings, it MUST NOT accept 0-RTT data. | Server-side 0-RTT acceptance rule; no client code to check. |
| `7.2.4-7.2.8-32` | MAY | A server may accept 0-RTT and then advertise different settings. <br>_(server [not-client])_ | A server MAY accept 0-RTT and subsequently provide different settings in its SETTINGS frame. | Server-side; client impact covered by facts 35-36. |
| `7.2.4-7.2.8-33` | MUST NOT | After accepting 0-RTT, the server's SETTINGS must not reduce limits the client's 0-RTT data may have relied on. <br>_(server [not-client])_ | its SETTINGS frame MUST NOT reduce any limits or alter any values that might be violated by the client with its 0-RTT data | Server-side send rule; the client-side enforcement is fact 35. |
| `7.2.4-7.2.8-34` | MUST | After accepting 0-RTT the server must include every setting that differs from its default. <br>_(server [not-client])_ | The server MUST include all settings that differ from their default values. | Server-side send rule; client-side enforcement is fact 36. |
| `7.2.4-7.2.8-35` | MUST | Post-0-RTT settings incompatible with previously specified ones are a connection error H3_SETTINGS_ERROR. <br>_(client,receiver [CLIENT])_ | sends settings that are not compatible with the previously specified settings, this MUST be treated as a connection error of type H3_SETTINGS_ERROR | http3/control.go: vacuous without 0-RTT; if added, compare newly received SETTINGS against stored values and close with H3_SETTINGS_ERROR (0x0109) on incompatibility. |
| `7.2.4-7.2.8-36` | MUST | After accepted 0-RTT, omission of a previously non-default understood setting is a connection error H3_SETTINGS_ERROR. <br>_(client,receiver [CLIENT])_ | omits a setting value that the client understands (apart from reserved setting identifiers) that was previously specified to have a non-default value, this MUST be treated as a connection error | http3/control.go: vacuous without 0-RTT; if added, detect omitted previously-non-default settings in the new SETTINGS frame → H3_SETTINGS_ERROR. |
| `7.2.4-7.2.8-37` | informative | PUSH_PROMISE is type 0x05 carrying a varint Push ID followed by a QPACK-encoded field section, sent server-to-client on a request stream. <br>_(all [CLIENT])_ | PUSH_PROMISE Frame { Type (i) = 0x05, Length (i), Push ID (i), Encoded Field Section (..), } | http3/frame.go + http3/stream.go: client must be able to parse type 0x05 on request streams (varint push ID + remainder as field section) even if it then rejects the push. |
| `7.2.4-7.2.8-38` | informative | The PUSH_PROMISE payload after the Push ID is a QPACK-encoded request header field section. <br>_(all [CLIENT])_ | QPACK-encoded request header fields for the promised response. | qpack/: if PUSH_PROMISE is ever consumed (not connection-errored), its field section must go through the QPACK decoder like any other; today the client likely errors first (fact 40). |
| `7.2.4-7.2.8-39` | MUST NOT | A server must not use a push ID beyond the client's advertised MAX_PUSH_ID. <br>_(server [not-client])_ | A server MUST NOT use a push ID that is larger than the client has provided in a MAX_PUSH_ID frame | Server-side send rule; client-side enforcement is fact 40. |
| `7.2.4-7.2.8-40` | MUST | A client must treat a PUSH_PROMISE whose push ID exceeds what it advertised as connection error H3_ID_ERROR. <br>_(client,receiver [CLIENT])_ | A client MUST treat receipt of a PUSH_PROMISE frame that contains a larger push ID than the client has advertised as a connection error of H3_ID_ERROR. | http3/stream.go: since this client never sends MAX_PUSH_ID, ANY PUSH_PROMISE exceeds the (unset) advertisement — verify receipt closes the connection with H3_ID_ERROR (0x0108), not silent ignore. |
| `7.2.4-7.2.8-41` | MAY | A server may promise the same push ID on multiple request streams. <br>_(server [CLIENT])_ | A server MAY use the same push ID in multiple PUSH_PROMISE frames. | http3/stream.go: client must tolerate duplicate push IDs without state corruption — vacuous here because every PUSH_PROMISE already triggers H3_ID_ERROR (fact 40); verify that path fires first. |
| `7.2.4-7.2.8-42` | MUST | Duplicate promises for one push ID must decompress to identical field sections (same fields, same order, exact name/value matches). <br>_(server [not-client])_ | the decompressed request header sets MUST contain the same fields in the same order, and both the name and the value in each field MUST be exact matches | Server-side obligation; the client-side check is fact 44. |
| `7.2.4-7.2.8-43` | SHOULD | Clients should compare header sections of resources promised multiple times. <br>_(client [CLIENT])_ | Clients SHOULD compare the request header sections for resources promised multiple times. | http3/: N/A while push is rejected outright (fact 40); becomes relevant only if push support is added — then a per-push-ID promise cache is needed. |
| `7.2.4-7.2.8-44` | MUST | A mismatched duplicate promise for an already-promised push ID is a connection error H3_GENERAL_PROTOCOL_ERROR. <br>_(client,receiver [CLIENT])_ | If a client receives a push ID that has already been promised and detects a mismatch, it MUST respond with a connection error of type H3_GENERAL_PROTOCOL_ERROR. | http3/: vacuous while all pushes are rejected via H3_ID_ERROR; if push is added, compare against stored promise and close with H3_GENERAL_PROTOCOL_ERROR (0x0101) on mismatch. |
| `7.2.4-7.2.8-45` | SHOULD | If duplicate promised field sections match exactly, the client should associate the push with each promising stream. <br>_(client [CLIENT])_ | the client SHOULD associate the pushed content with each stream on which a PUSH_PROMISE frame was received | http3/: N/A without push support; note in coverage matrix. |
| `7.2.4-7.2.8-46` | SHOULD | Servers should not reuse a push ID over a long period. <br>_(server [not-client])_ | A server SHOULD avoid reusing a push ID over a long period. | Server-side guidance; no client code to check. |
| `7.2.4-7.2.8-47` | MUST | A PUSH_PROMISE on the control stream is a connection error H3_FRAME_UNEXPECTED. <br>_(client,receiver [CLIENT])_ | If a PUSH_PROMISE frame is received on the control stream, the client MUST respond with a connection error of type H3_FRAME_UNEXPECTED. | http3/control.go: control-stream frame dispatch must map type 0x05 to connection error H3_FRAME_UNEXPECTED (0x0105), not unknown-ignore. |
| `7.2.4-7.2.8-48` | MUST NOT | A client must never send a PUSH_PROMISE frame. <br>_(client,sender [CLIENT])_ | A client MUST NOT send a PUSH_PROMISE frame. | http3/frame.go + http3/request.go: verify no writer API can emit frame type 0x05. |
| `7.2.4-7.2.8-49` | MUST | A server treats any received PUSH_PROMISE as connection error H3_FRAME_UNEXPECTED. <br>_(server,receiver [not-client])_ | A server MUST treat the receipt of a PUSH_PROMISE frame as a connection error of type H3_FRAME_UNEXPECTED. | Server-side receive rule; client obligation is fact 48. |
| `7.2.4-7.2.8-50` | informative | GOAWAY is frame type 0x07 whose payload is a single varint Stream ID (server-to-client) or Push ID (client-to-server). <br>_(all [CLIENT])_ | GOAWAY Frame { Type (i) = 0x07, Length (i), Stream ID/Push ID (i), } | http3/frame.go: verify GOAWAY codec is type 0x07 with exactly one varint payload field. |
| `7.2.4-7.2.8-51` | informative | GOAWAY is connection-scoped graceful shutdown: stop accepting new requests/pushes while finishing in-flight ones; it does not itself close the connection. <br>_(all [CLIENT])_ | The GOAWAY frame applies to the entire connection, not a specific stream. | http3/control.go + http3/client.go: on GOAWAY, mark the connection draining (reject new requests) but let in-flight requests with stream ID <= the received ID complete; do not tear down the QUIC connection. |
| `7.2.4-7.2.8-52` | informative | Server-to-client GOAWAY carries a client-initiated bidirectional QUIC stream ID as a varint. <br>_(server,sender [CLIENT])_ | it carries a QUIC stream ID for a client-initiated bidirectional stream encoded as a variable-length integer | http3/control.go: the GOAWAY handler must interpret the payload as a client-initiated bidi stream ID bounding which requests the server will process. |
| `7.2.4-7.2.8-53` | MUST | A client must treat a GOAWAY carrying a stream ID that is not client-initiated bidirectional as connection error H3_ID_ERROR. <br>_(client,receiver [CLIENT])_ | A client MUST treat receipt of a GOAWAY frame containing a stream ID of any other type as a connection error of type H3_ID_ERROR. | http3/control.go: GOAWAY handler must validate id % 4 == 0 (client-initiated bidi) and close with H3_ID_ERROR (0x0108) otherwise. |
| `7.2.4-7.2.8-54` | informative | Client-to-server GOAWAY carries a push ID as a varint. <br>_(client,sender [CLIENT])_ | In the client-to-server direction, the GOAWAY frame carries a push ID encoded as a variable-length integer. | http3/control.go + http3/client.go: if the client ever sends GOAWAY on graceful close, the payload must be a push ID (0 is safe when no pushes were promised) — verify or confirm the client never sends GOAWAY. |
| `7.2.4-7.2.8-55` | MUST | A GOAWAY on any stream other than the control stream is a connection error H3_FRAME_UNEXPECTED. <br>_(client,receiver [CLIENT])_ | A client MUST treat a GOAWAY frame on a stream other than the control stream as a connection error of type H3_FRAME_UNEXPECTED. | http3/stream.go: request-stream dispatch must map type 0x07 to connection error H3_FRAME_UNEXPECTED, not unknown-ignore. |
| `7.2.4-7.2.8-56` | informative | MAX_PUSH_ID is frame type 0x0d whose payload is a single varint Push ID. <br>_(all [CLIENT])_ | MAX_PUSH_ID Frame { Type (i) = 0x0d, Length (i), Push ID (i), } | http3/frame.go: if MAX_PUSH_ID is ever emitted, codec is type 0x0d with one varint; at minimum the type constant must exist for dispatch checks. |
| `7.2.4-7.2.8-57` | informative | MAX_PUSH_ID is a client-only frame that caps the push IDs usable in PUSH_PROMISE and CANCEL_PUSH. <br>_(client [CLIENT])_ | This sets the maximum value for a push ID that the server can use in PUSH_PROMISE and CANCEL_PUSH frames. | http3/client.go: document the push posture — this client sends no MAX_PUSH_ID, so all pushes are disabled by construction. |
| `7.2.4-7.2.8-58` | MUST | MAX_PUSH_ID received on any stream other than the control stream is a connection error H3_FRAME_UNEXPECTED. <br>_(endpoint,receiver [CLIENT])_ | Receipt of a MAX_PUSH_ID frame on any other stream MUST be treated as a connection error of type H3_FRAME_UNEXPECTED. | http3/stream.go: request-stream dispatch must map type 0x0d to connection error H3_FRAME_UNEXPECTED. |
| `7.2.4-7.2.8-59` | MUST NOT | A server must never send MAX_PUSH_ID. <br>_(server,sender [not-client])_ | A server MUST NOT send a MAX_PUSH_ID frame. | Server-side send rule; client-side enforcement is fact 60. |
| `7.2.4-7.2.8-60` | MUST | A client must treat any received MAX_PUSH_ID frame as connection error H3_FRAME_UNEXPECTED. <br>_(client,receiver [CLIENT])_ | A client MUST treat the receipt of a MAX_PUSH_ID frame as a connection error of type H3_FRAME_UNEXPECTED. | http3/control.go: control-stream dispatch must map type 0x0d to connection error H3_FRAME_UNEXPECTED (0x0105) — client role means receiving it is always an error. |
| `7.2.4-7.2.8-61` | informative | The maximum push ID starts unset; the server cannot push until a MAX_PUSH_ID frame arrives. <br>_(all [CLIENT])_ | The maximum push ID is unset when an HTTP/3 connection is created, meaning that a server cannot push until it receives a MAX_PUSH_ID frame. | http3/client.go + http3/stream.go: this is the basis for rejecting all pushes — verify the client never sends MAX_PUSH_ID and that push-stream receipt (stream type 0x01) and PUSH_PROMISE are both treated as H3_ID_ERROR. |
| `7.2.4-7.2.8-62` | MUST | MAX_PUSH_ID must never decrease; a smaller value than previously received is a connection error H3_ID_ERROR. <br>_(server,receiver [CLIENT])_ | receipt of a MAX_PUSH_ID frame that contains a smaller value than previously received MUST be treated as a connection error of type H3_ID_ERROR. | http3/control.go: enforced by the server, but constrains the client's send side — if MAX_PUSH_ID sending is ever added, values must be monotonically non-decreasing. |
| `7.2.4-7.2.8-63` | informative | Frame types 0x1f*N+0x21 are reserved grease codepoints exercising the unknown-type-ignore rule. <br>_(all [CLIENT])_ | Frame types of the format 0x1f * N + 0x21 for non-negative integer values of N are reserved to exercise the requirement that unknown types be ignored | http3/frame.go + http3/stream.go + http3/control.go: verify grease frame types (0x21, 0x40, ...) hit the generic unknown-frame skip path with bounded memory (payload discarded, not buffered). |
| `7.2.4-7.2.8-64` | MAY | Grease frames have no semantics and may be sent on any stream where frames are allowed (usable as padding). <br>_(all,sender [CLIENT])_ | These frames have no semantics, and they MAY be sent on any stream where frames are allowed to be sent. | http3/frame.go: optionally support emitting a grease frame (padding); at minimum verify receiving them on request AND control streams is harmless. |
| `7.2.4-7.2.8-65` | MUST NOT | Received grease frames must be given no meaning. <br>_(endpoint,receiver [CLIENT])_ | Endpoints MUST NOT consider these frames to have any meaning upon receipt. | http3/stream.go + http3/control.go: verify grease frame types are not special-cased anywhere — identical handling to any unknown type. |
| `7.2.4-7.2.8-66` | MUST NOT | HTTP/2 frame types with no HTTP/3 equivalent must never be sent. <br>_(all,sender [CLIENT])_ | These frame types MUST NOT be sent | http3/frame.go: verify no writer emits reserved types 0x02 (PRIORITY), 0x06 (PING), 0x08 (WINDOW_UPDATE), 0x09 (CONTINUATION) per §11.2.1. |
| `7.2.4-7.2.8-67` | MUST | Receipt of a reserved HTTP/2 frame type is a connection error H3_FRAME_UNEXPECTED. <br>_(all,receiver [CLIENT])_ | their receipt MUST be treated as a connection error of type H3_FRAME_UNEXPECTED | http3/frame.go + http3/stream.go + http3/control.go: CRITICAL — types 0x02, 0x06, 0x08, 0x09 must NOT fall into the generic unknown-ignore path; they must trigger connection error H3_FRAME_UNEXPECTED (0x0105) on both request and control streams. |

### 9114 section 8-9 - RFC 9114: Error Handling (§8), HTTP/3 Error Codes (§8.1), Extensions to HTTP/3 (§9)

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `8-9-1` | informative | A stream error is the abrupt termination (reset) of a single QUIC stream with a communicated reason, using an error code encoded per Section 8.1. <br>_(endpoint [CLIENT])_ | QUIC allows the application to abruptly terminate (reset) that stream and communicate a reason | quic/ RESET_STREAM/STOP_SENDING must carry an application error code; http3/stream.go must map H3 codes onto them when aborting a request stream. |
| `8-9-2` | informative | Stream errors are distinct from HTTP status codes: a stream error means the full request/response was not transferred or consumed, whereas a status code is the result of a successfully received request. <br>_(all [CLIENT])_ | Stream errors indicate that the sender did not transfer or consume the full request or response, while HTTP status codes indicate the result of a request that was successfully received. | http3/stream_body.go + response.go: a RESET_STREAM mid-response must surface as a transport-level error to the caller, never be silently treated as a complete response. |
| `8-9-3` | informative | A connection error terminates the whole QUIC connection, communicating the reason with an HTTP/3 error code from Section 8.1 via QUIC's connection close mechanism. <br>_(endpoint [CLIENT])_ | an HTTP/3 implementation can terminate a QUIC connection and communicate the reason using an error code from Section 8.1 | quic/ application CONNECTION_CLOSE must carry the H3 error code chosen by http3/; check http3/client.go and control.go close paths pass 8.1 codes down. |
| `8-9-4` | informative | Closing streams or connections with an 'error' code does not necessarily indicate a fault; e.g., a stream can be reset simply because the resource is no longer needed. <br>_(all [CLIENT])_ | these actions do not necessarily indicate a problem with the connection or either implementation | http3/client.go: cancelling a request must reset only that stream (H3_REQUEST_CANCELLED), never tear down the connection or log it as a protocol failure. |
| `8-9-5` | MAY | An endpoint is permitted to escalate a stream error into a connection error, closing the whole connection over a single-stream condition, but should weigh the impact on outstanding requests. <br>_(endpoint [CLIENT])_ | An endpoint MAY choose to treat a stream error as a connection error under certain circumstances, closing the entire connection in response to a condition on a single stream. | http3/stream.go: audit which per-stream faults escalate to connection close; escalation is allowed but should not be the default for recoverable per-stream conditions. |
| `8-9-6` | MUST | An error code used in an unexpected context, or an unknown error code, must be treated exactly as if H3_NO_ERROR had been received (though closing the stream still has its normal side effects). <br>_(endpoint,receiver [CLIENT])_ | use of an error code in an unexpected context or receipt of an unknown error code MUST be treated as equivalent to H3_NO_ERROR | http3/stream.go + client.go: decoding of RESET_STREAM/STOP_SENDING/CONNECTION_CLOSE codes must not reject or special-case unknown values — treat them as H3_NO_ERROR. |
| `8-9-7` | informative | The Section 8.1 error codes are the codepoint space used in three operations: abruptly terminating streams (reset), aborting reading of streams (stop-sending), and immediately closing HTTP/3 connections. <br>_(endpoint [CLIENT])_ | The following error codes are defined for use when abruptly terminating streams, aborting reading of streams, or immediately closing HTTP/3 connections. | quic/: all three surfaces (RESET_STREAM, STOP_SENDING, app CONNECTION_CLOSE) must accept the H3 codepoints http3/ supplies; verify the plumbing exists for each. |
| `8-9-8` | informative | H3_NO_ERROR has codepoint 0x0100 and is used to close a connection or stream when there is no error to signal. <br>_(endpoint [CLIENT])_ | H3_NO_ERROR (0x0100): No error. This is used when the connection or stream needs to be closed, but there is no error to signal. | http3/client.go: graceful Close must send application close with 0x0100 (or a grease code per 8-9-26); verify the constant value is 0x0100, not 0x0. |
| `8-9-9` | informative | H3_GENERAL_PROTOCOL_ERROR has codepoint 0x0101, for protocol violations with no more specific code or when the endpoint declines to use the specific code. <br>_(endpoint [CLIENT])_ | Peer violated protocol requirements in a way that does not match a more specific error code or endpoint declines to use the more specific error code. | http3/client.go error constants: 0x0101 exists and is the fallback only where no specific 8.1 code applies — grep that specific codes are preferred elsewhere. |
| `8-9-10` | informative | H3_INTERNAL_ERROR has codepoint 0x0102, signalling an internal error in the HTTP stack. <br>_(endpoint [CLIENT])_ | An internal error has occurred in the HTTP stack. | http3/client.go: internal client-side failures (panic recovery, invariant breaks) should close with 0x0102, not a protocol-violation code. |
| `8-9-11` | informative | H3_STREAM_CREATION_ERROR has codepoint 0x0103, sent when the endpoint detects the peer created a stream it will not accept (e.g., a server-initiated bidirectional stream). <br>_(endpoint [CLIENT])_ | The endpoint detected that its peer created a stream that it will not accept. | http3/control.go / dial.go incoming-stream accept loop: a server-initiated bidirectional stream must be rejected with 0x0103 (per §6.1), and duplicate control/QPACK uni streams likewise. |
| `8-9-12` | informative | H3_CLOSED_CRITICAL_STREAM has codepoint 0x0104, used when a stream required by the HTTP/3 connection (control stream, QPACK streams) was closed or reset. <br>_(endpoint [CLIENT])_ | A stream required by the HTTP/3 connection was closed or reset. | http3/control.go: peer closing/resetting the control stream or either QPACK stream must trigger connection close with 0x0104; verify all three critical streams are monitored. |
| `8-9-13` | informative | H3_FRAME_UNEXPECTED has codepoint 0x0105, used when a frame arrives that is not permitted in the current state or on the current stream type. <br>_(endpoint [CLIENT])_ | A frame was received that was not permitted in the current state or on the current stream. | http3/frame.go + stream.go + control.go: per-stream frame whitelists (DATA/HEADERS on request streams only, SETTINGS/GOAWAY etc. on control only, DATA before HEADERS) must all emit 0x0105. |
| `8-9-14` | informative | H3_FRAME_ERROR has codepoint 0x0106, used when a received frame fails layout requirements or has an invalid size. <br>_(endpoint [CLIENT])_ | A frame that fails to satisfy layout requirements or with an invalid size was received. | http3/frame.go: truncated payloads, bad varint layouts, and length/content mismatches must map to 0x0106, distinct from 0x0105 sequencing errors. |
| `8-9-15` | informative | H3_EXCESSIVE_LOAD has codepoint 0x0107, used when the peer exhibits behavior that might be generating excessive load. <br>_(endpoint [CLIENT])_ | The endpoint detected that its peer is exhibiting a behavior that might be generating excessive load. | http3/response.go limit enforcement (e.g., oversized field sections, frame floods): check whether load-shedding paths use 0x0107 where a resource-exhaustion cap trips. |
| `8-9-16` | informative | H3_ID_ERROR has codepoint 0x0108, used when a stream ID or push ID is used incorrectly: exceeding a limit, reducing a limit, or being reused. <br>_(endpoint [CLIENT])_ | A stream ID or push ID was used incorrectly, such as exceeding a limit, reducing a limit, or being reused. | http3/control.go: GOAWAY IDs that increase across frames, push IDs exceeding MAX_PUSH_ID, and reused push IDs must all close the connection with 0x0108. |
| `8-9-17` | informative | H3_SETTINGS_ERROR has codepoint 0x0109, used when an error is detected in the payload of a SETTINGS frame (e.g., duplicate identifiers). <br>_(endpoint [CLIENT])_ | An endpoint detected an error in the payload of a SETTINGS frame. | http3/control.go SETTINGS parsing: duplicate setting identifiers, HTTP/2-reserved setting IDs, and malformed values must close with 0x0109. |
| `8-9-18` | informative | H3_MISSING_SETTINGS has codepoint 0x010a, used when no SETTINGS frame is received at the beginning of the peer's control stream. <br>_(endpoint [CLIENT])_ | No SETTINGS frame was received at the beginning of the control stream. | http3/control.go: the first frame read on the server's control stream must be SETTINGS; anything else (including an unknown frame type, see 8-9-31) closes with 0x010a. |
| `8-9-19` | informative | H3_REQUEST_REJECTED has codepoint 0x010b, sent by a server that rejected a request without performing any application processing; for the client this signals the request is safely retryable. <br>_(server,client [CLIENT])_ | A server rejected a request without performing any application processing. | http3/stream.go + client/ retry logic: a request-stream reset with 0x010b means zero server processing — the client may auto-retry even non-idempotent requests; verify the code is recognized. |
| `8-9-20` | informative | H3_REQUEST_CANCELLED has codepoint 0x010c, used when a request or its response (including a pushed response) is cancelled. <br>_(endpoint [CLIENT])_ | The request or its response (including pushed response) is cancelled. | http3/client.go / stream.go: context cancellation and body-abandon paths must RESET_STREAM + STOP_SENDING with 0x010c, and cancelling a push uses the same code. |
| `8-9-21` | informative | H3_REQUEST_INCOMPLETE has codepoint 0x010d, sent by a server when the client's stream terminated without containing a fully formed request. <br>_(server [CLIENT])_ | The client's stream terminated without containing a fully formed request. | http3/stream.go: client must recognize an inbound 0x010d reset (means its own send was truncated); also ensure the client always FINs a complete request so servers never see this. |
| `8-9-22` | informative | H3_MESSAGE_ERROR has codepoint 0x010e, used when an HTTP message is malformed and cannot be processed. <br>_(endpoint [CLIENT])_ | An HTTP message was malformed and cannot be processed. | http3/validate.go: malformed response field sections (bad pseudo-headers, forbidden characters, content-length mismatch) detected client-side should abort with 0x010e. |
| `8-9-23` | informative | H3_CONNECT_ERROR has codepoint 0x010f, used when the TCP connection established for a CONNECT request was reset or abnormally closed. <br>_(proxy,client [CLIENT])_ | The TCP connection established in response to a CONNECT request was reset or abnormally closed. | http3/stream.go: if CONNECT is supported, an inbound 0x010f reset must be surfaced as tunnel failure; if CONNECT is unsupported, only recognition of the code matters. |
| `8-9-24` | informative | H3_VERSION_FALLBACK has codepoint 0x0110, indicating the requested operation cannot be served over HTTP/3 and the peer should retry over HTTP/1.1. <br>_(server,client [CLIENT])_ | The requested operation cannot be served over HTTP/3. The peer should retry over HTTP/1.1. | http3/client.go + client/ transport selection: receiving 0x0110 should surface a distinguishable error so callers (or the ALPN/managed transport) can retry over HTTP/1.1. |
| `8-9-25` | informative | Error codes of the form 0x1f * N + 0x21 (N a non-negative integer) are reserved as grease codepoints to exercise the unknown-code-equals-H3_NO_ERROR requirement. <br>_(all [CLIENT])_ | Error codes of the format 0x1f * N + 0x21 for non-negative integer values of N are reserved | http3/client.go + stream.go: receiving any 0x1f*N+0x21 code must behave exactly like H3_NO_ERROR on all decode paths (reset, stop-sending, connection close). |
| `8-9-26` | SHOULD | Implementations should, with some probability, send a grease error code from the reserved space instead of H3_NO_ERROR when closing without error. <br>_(endpoint,sender [CLIENT])_ | Implementations SHOULD select an error code from this space with some probability when they would have sent H3_NO_ERROR. | http3/client.go graceful-close path: check whether H3_NO_ERROR is sometimes greased with 0x1f*N+0x21 (the repo greps show grease handling in control.go/client.go — verify send side, not just receive). |
| `8-9-27` | informative | Protocol extensions are effective only within the scope of a single HTTP/3 connection; extension state must not be assumed across connections. <br>_(all [CLIENT])_ | Extensions are effective only within the scope of a single HTTP/3 connection. | http3/client.go + client/ pool: any negotiated per-connection state (settings-derived behavior) must be stored per-conn, never cached across dials. |
| `8-9-28` | informative | Extensions may introduce new frame types, new settings, new error codes, or new unidirectional stream types, each managed by an IANA registry. <br>_(all [CLIENT])_ | Extensions are permitted to use new frame types (Section 7.2), new settings (Section 7.2.4.1), new error codes (Section 8), or new unidirectional stream types (Section 6.2). | http3/frame.go + control.go: the four extension surfaces (frame type, setting, error code, uni stream type) must each have a tolerant unknown-value path — see 8-9-29/30. |
| `8-9-29` | MUST | Implementations must ignore unknown or unsupported values in all extensible protocol elements (frame types, settings, error codes, stream types). <br>_(all [CLIENT])_ | Implementations MUST ignore unknown or unsupported values in all extensible protocol elements. | http3/frame.go: unknown frame types must be skipped (payload consumed) without error on request and control streams; http3/control.go: unknown SETTINGS identifiers ignored. |
| `8-9-30` | MUST | Implementations must discard the data of, or abort reading on, any unidirectional stream whose stream type is unknown or unsupported — never treat it as a connection error. <br>_(all [CLIENT])_ | Implementations MUST discard data or abort reading on unidirectional streams that have unknown or unsupported types. | http3/control.go inbound uni-stream dispatch: unknown stream-type varint must drain-and-discard or send STOP_SENDING (quic/ must support abort-reading), with no connection teardown. |
| `8-9-31` | SHOULD | An unknown frame type does not satisfy a positional requirement for a known frame — e.g., if the first control-stream frame is an unknown type, the SETTINGS-first requirement is unmet and this should be treated as an error. <br>_(all [CLIENT])_ | an unknown frame type does not satisfy that requirement and SHOULD be treated as an error | http3/control.go: the SETTINGS-first check must fire on the literal first frame type — a grease/unknown frame before SETTINGS is an H3_MISSING_SETTINGS error, not skipped. |
| `8-9-32` | MUST | Extensions that could change the semantics of existing protocol components (e.g., altering HEADERS frame layout) must be negotiated with the peer before use. <br>_(all [CLIENT])_ | Extensions that could change the semantics of existing protocol components MUST be negotiated before being used. | http3/: no semantic extensions are implemented today; if any are added, gate activation on an explicit peer SETTINGS signal read in http3/control.go before changing any parse/emit behavior. |
| `8-9-33` | MUST | When a setting is used to negotiate an extension, its default value must be defined so that the extension is disabled if the setting is omitted. <br>_(all [CLIENT])_ | If a setting is used for extension negotiation, the default value MUST be defined in such a fashion that the extension is disabled if the setting is omitted | http3/control.go SETTINGS handling: absence of a setting must map to extension-off defaults (e.g., QPACK dynamic table only enabled by explicit peer SETTINGS_QPACK_MAX_TABLE_CAPACITY — cross-check with qpack/ encoder gating). |

### 9114 section 10 - Security Considerations

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `10-1` | informative | HTTP/3's security considerations are comparable to HTTP/2 over TLS; many HTTP/2 Section 10 considerations apply to QUIC and are handled at the transport layer. <br>_(all [not-client])_ | The security considerations of HTTP/3 should be comparable to those of HTTP/2 with TLS. | Context only; transport-level considerations are covered by quic/ (RFC 9000 §21) — no http3/ action. |
| `10-2` | informative | HTTP/3 relies on the core HTTP definition of authority; establishing server authority follows RFC 9110 Section 17.1. <br>_(all [CLIENT])_ | HTTP/3 relies on the HTTP definition of authority. The security considerations of establishing authority are discussed in Section 17.1 of [HTTP]. | http3/dial.go — TLS certificate verification must cover the :authority host (RFC 9110 §17.1); confirm ServerName set and verification not skipped by default. |
| `10-3` | informative | ALPN in the TLS/QUIC handshake establishes the application protocol before any application-layer bytes are processed, giving assurance both peers speak HTTP/3. <br>_(endpoint [CLIENT])_ | The use of ALPN in the TLS and QUIC handshakes establishes the target application protocol before application-layer bytes are processed. | http3/dial.go — verify ALPN "h3" is offered and confirmed negotiated before any HTTP/3 stream bytes are sent; fail the dial otherwise. |
| `10-4` | informative | ALPN does not prevent all cross-protocol attacks; QUIC plaintext can be used for request forgery against endpoints without authenticated transports (RFC 9000 Section 21.5). <br>_(endpoint [not-client])_ | Section 21.5 of [QUIC-TRANSPORT] describes some ways in which the plaintext of QUIC packets can be used to perform request forgery against endpoints that don't use authenticated transports. | quic/ — transport-level concern (RFC 9000 §21.5); no http3/ client action beyond not connecting to attacker-supplied addresses. |
| `10-5` | MUST | A request or response containing an invalid field name must be treated as malformed, so it can never be translated into an HTTP/1.1 message by an intermediary. <br>_(endpoint,intermediary [CLIENT])_ | Requests or responses containing invalid field names MUST be treated as malformed. | http3/validate.go — after qpack/decoder.go decode, reject responses whose field names contain octets outside token/lowercase rules (§4.1.2, RFC 9110 §5.1); QPACK itself does not validate names. |
| `10-6` | MUST | A request or response containing a field-value character not permitted by the field-content ABNF (notably CR 0x0d, LF 0x0a, NUL 0x00) must be treated as malformed. <br>_(endpoint,intermediary [CLIENT])_ | Any request or response that contains a character not permitted in a field value MUST be treated as malformed. | http3/validate.go — enforce CR/LF/NUL rejection in decoded response field values AND in field values the client encodes into requests (guard at request.go); valid set per RFC 9110 §5.5 field-content. |
| `10-7` | MUST | A server hosting multiple tenants must ensure a tenant cannot push representations of resources the tenant has no authority over (cache-poisoning across tenants). <br>_(server [not-client])_ | that server MUST ensure that tenants are not able to push representations of resources that they do not have authority over | Server-only obligation; no client code path. Not applicable to http3/. |
| `10-8` | informative | Clients must reject pushed responses for which the origin server is not authoritative (restatement — the normative rule lives in Section 4.6). <br>_(client [CLIENT])_ | Clients are required to reject pushed responses for which an origin server is not authoritative; see Section 4.6. | http3/ — client does not enable push (never raises MAX_PUSH_ID per non-goals), so pushed responses should be impossible; verify control.go/stream.go treat any received push stream per §4.6/§7.2.5 (unexpected push → H3_ID_ERROR or discard) rather than consuming it. |
| `10-9` | SHOULD | A client that accepts server push should limit the number of push IDs it has outstanding at a time to bound PUSH_PROMISE resource commitment. <br>_(client [CLIENT])_ | A client that accepts server push SHOULD limit the number of push IDs it issues at a time. | http3/control.go — confirm MAX_PUSH_ID is never sent (push disabled = limit of zero, which satisfies this); if push is ever enabled, cap outstanding push IDs incrementally. |
| `10-10` | SHOULD | Implementations should track use of abuse-prone features (server push, unknown/undefined protocol elements, field compression) and set limits on their use. <br>_(endpoint [CLIENT])_ | Implementations SHOULD track the use of these features and set limits on their use. | http3/control.go + frame.go — check bounded, O(length)-skip handling of unknown SETTINGS identifiers, unknown frame types, and unknown unidirectional stream types (peer-input policy: bound by construction, charge what memory retains); quic/ for stream-count limits. |
| `10-11` | MAY | An endpoint may treat suspicious/excessive peer activity as a connection error of type H3_EXCESSIVE_LOAD, at the cost of possible false positives. <br>_(endpoint [CLIENT])_ | An endpoint MAY treat activity that is suspicious as a connection error of type H3_EXCESSIVE_LOAD | http3/ error definitions — verify H3_EXCESSIVE_LOAD (0x0107) is defined and usable as a connection-close code; actual heuristic use is optional. |
| `10-12` | informative | SETTINGS_MAX_FIELD_SECTION_SIZE advises peers of a field-section size limit; the setting is advisory only and per-hop, so any request/response may hit a lower unknown limit downstream. <br>_(endpoint [CLIENT])_ | An endpoint can use the SETTINGS_MAX_FIELD_SECTION_SIZE (Section 4.2.2) setting to advise peers of limits that might apply on the size of field sections. | http3/control.go — check whether the client advertises SETTINGS_MAX_FIELD_SECTION_SIZE; if it does, http3/response.go / stream.go must actually enforce that limit on received field sections (advisory to peer, binding on self). |
| `10-13` | MAY | Because the field-section size setting is advisory, an endpoint may send field sections exceeding the peer's advertised limit, risking the message being treated as malformed. <br>_(sender,endpoint [CLIENT])_ | endpoints MAY choose to send field sections that exceed this limit and risk having the request or response being treated as malformed | http3/request.go — decide policy: either honor peer's SETTINGS_MAX_FIELD_SECTION_SIZE when encoding request headers or knowingly accept rejection risk; verify the peer setting is at least surfaced from control.go. |
| `10-14` | informative | A server receiving a field section larger than it will handle can respond with HTTP 431 (Request Header Fields Too Large). <br>_(server [not-client])_ | A server that receives a larger field section than it is willing to handle can send an HTTP 431 (Request Header Fields Too Large) status code | Server-side option; client just passes 431 through as a normal status — no special handling in http3/response.go. |
| `10-15` | informative | A client can discard responses whose field sections it cannot process (rather than buffering unboundedly). <br>_(client [CLIENT])_ | A client can discard responses that it cannot process. | http3/response.go + stream.go — verify oversized/unprocessable response field sections are discarded with a stream reset (H3_MESSAGE_ERROR / STOP_SENDING via quic/), never buffered without bound (peer-input policy). |
| `10-16` | informative | CONNECT lets a client create disproportionate proxy load (cheap stream vs. real TCP connection); a CONNECT proxy may be more conservative in accepted concurrent requests and may delay raising QUIC stream limits after TCP teardown (TIME_WAIT). <br>_(proxy [not-client])_ | a proxy that supports CONNECT might be more conservative in the number of simultaneous requests it accepts. | Proxy-side guidance only; client's http3/ CONNECT support (if any) needs no change — but expect lower stream credit from CONNECT proxies (quic/ MAX_STREAMS handling must tolerate it). |
| `10-17` | MUST NOT | On a secure channel, an implementation must not compress content mixing confidential and attacker-controlled data unless each source gets a separate compression context (BREACH-class attacks). <br>_(endpoint [CLIENT])_ | Implementations communicating on a secure channel MUST NOT compress content that includes both confidential and attacker-controlled data unless separate compression contexts are used for each source of data. | qpack/encoder.go — verify sensitive request fields (Authorization, Cookie) are emitted as never-indexed literals / not inserted into any dynamic table shared with attacker-influenced values; static-table-only mode largely satisfies this but re-check since dynamic QPACK was added un-audited. |
| `10-18` | MUST NOT | Compression must not be used at all if the source of the data cannot be reliably determined. <br>_(endpoint [CLIENT])_ | Compression MUST NOT be used if the source of data cannot be reliably determined. | qpack/encoder.go — client controls its own header sources, so this is satisfiable by construction; confirm no code path compresses values of unknown provenance (e.g., headers copied verbatim from a prior response) with indexing. |
| `10-19` | informative | For traffic-analysis resistance HTTP/3 uses transport-layer padding or the reserved (grease) frame and stream types of Sections 7.2.8 and 6.2.3 instead of HTTP/2-style PADDING frames. <br>_(endpoint [CLIENT])_ | HTTP/3 can either rely on transport-layer padding or employ the reserved frame and stream types discussed in Sections 7.2.8 and 6.2.3. | http3/frame.go + control.go — receiving side: reserved frame types (0x1f*N+0x21) and reserved stream types must be ignored/drained without error; quic/ — PADDING frame support on send is optional. |
| `10-20` | informative | Reserved-type padding streams remain subject to receiver flow control; failing to promptly drain such streams and grant flow-control credit throttles the sender's real traffic. <br>_(receiver [CLIENT])_ | a failure to promptly drain such streams and provide additional flow-control credit can limit the sender's ability to send real traffic | http3/ unknown-unidirectional-stream handling — verify the client either continuously drains unknown stream types (returning quic/ flow-control credit) or issues STOP_SENDING, so connection-level credit is never pinned by a grease stream. |
| `10-21` | MUST | An implementation must ensure a frame's declared length exactly matches the combined length of the fields it contains (nested varint length elements are a parsing hazard). <br>_(endpoint,receiver [CLIENT])_ | An implementation MUST ensure that the length of a frame exactly matches the length of the fields it contains. | http3/frame.go — every fixed-structure frame parser (SETTINGS, GOAWAY, MAX_PUSH_ID, CANCEL_PUSH, PUSH_PROMISE push-ID prefix) must reject leftover or missing bytes vs. the declared length with H3_FRAME_ERROR; check internal/bytesx varint reads are bounds-checked against frame length. |
| `10-22` | MUST | When 0-RTT early data is used with HTTP/3, the anti-replay mitigations of RFC 8470 (HTTP-REPLAY) must be applied. <br>_(all [CLIENT])_ | The anti-replay mitigations in [HTTP-REPLAY] MUST be applied when using HTTP/3 with 0-RTT. | quic/ — 0-RTT is a stated non-goal; verify the handshake never sends early data. If 0-RTT is ever added, the http3/ client must restrict early-data requests to safe/idempotent methods and handle 425 (Too Early) per RFC 8470. |
| `10-23` | informative | When applying RFC 8470 to HTTP/3, 'TLS layer' means the handshake performed within QUIC, and 'application data' means the contents of streams. <br>_(all [CLIENT])_ | references to the TLS layer refer to the handshake performed within QUIC, while all references to application data refer to the contents of streams | Mapping note only; relevant to quic/ + http3/ solely if 0-RTT support is added (currently a non-goal). |
| `10-24` | informative | Because a QUIC client's address can change mid-connection (migration), implementations using client addresses for logging/access control must fetch the current address when relevant or accept that it changes. <br>_(server,endpoint [not-client])_ | such implementations will need to either actively retrieve the client's current address or addresses when they are relevant or explicitly accept that the original address might change | Server-side logging/ACL concern; client's quic/ does not perform active migration (non-goal), so no http3/ action. |
| `10-25` | informative | Settings values, reaction timing, and handling of settings-controlled features create observable behavioral differences usable to fingerprint a specific client. <br>_(client,endpoint [CLIENT])_ | These include the value of settings, the timing of reactions to stimulus, and the handling of any features that are controlled by settings. | http3/control.go — awareness note: the exact SETTINGS the client emits are a fingerprint surface; no code change required, but keep emitted settings minimal/stable. |
| `10-26` | informative | Preference for a single QUIC connection lets an observer correlate a user's activity on a site; reusing one connection for different origins allows cross-origin activity correlation. <br>_(client [CLIENT])_ | Reusing connections for different origins allows for correlation of activity across those origins. | client/ pool + http3/ pooling — privacy trade-off note: if H3 connection pooling ever coalesces multiple origins onto one connection, document the correlation implication; currently per-origin dial, so no action. |
| `10-27` | informative | Several QUIC features solicit immediate responses, letting an endpoint measure latency to its peer, which can have privacy implications. <br>_(endpoint [not-client])_ | Several features of QUIC solicit immediate responses and can be used by an endpoint to measure latency to their peer | quic/ — transport-level observation (PING/ACK timing); no client-side mitigation expected. |

### 9114 section 11 - IANA Considerations

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `11-1` | informative | Section 11 registers one new ALPN protocol ID for HTTP/3 and establishes the IANA registries governing HTTP/3 code points (frame types, settings, error codes, stream types). <br>_(all [not-client])_ | This document registers a new ALPN protocol ID (Section 11.1) and creates new registries that manage the assignment of code points in HTTP/3. | Meta/registry scope only; individual codepoints reconciled in facts below (http3/frame.go, http3/control.go, http3/dial.go). |
| `11-2` | informative | HTTP/3 is identified in TLS ALPN by the exact string "h3", wire bytes 0x68 0x33. <br>_(client,server [CLIENT])_ | The "h3" string identifies HTTP/3: Protocol: HTTP/3 Identification Sequence: 0x68 0x33 ("h3") | http3/dial.go — TLS config must offer NextProtos exactly ["h3"] and verify the negotiated ALPN equals "h3" before HTTP/3 setup; check quic/ TLS 1.3 handshake propagates ALPN. |
| `11-3` | informative | All new registries operate under the QUIC registration policy of RFC 9000 Section 22.1 and include its common fields, collected under the HTTP/3 heading. <br>_(all [not-client])_ | New registries created in this document operate under the QUIC registration policy documented in Section 22.1 of [QUIC-TRANSPORT]. | IANA registry procedure only — no code impact; note in docs/rfc-analysis catalog. |
| `11-4` | informative | The initial allocations in the new registries are permanent, change-controlled by the IETF with the HTTP WG as contact. <br>_(all [not-client])_ | The initial allocations in these registries are all assigned permanent status and list a change controller of the IETF | IANA registry procedure only — no code impact. |
| `11-5` | informative | The HTTP/3 Frame Types registry governs a 62-bit code space (frame type is a QUIC varint); values 0x00-0x3f need Standards Action/IESG Approval, others Specification Required. <br>_(endpoint [CLIENT])_ | The "HTTP/3 Frame Types" registry governs a 62-bit space. | http3/frame.go + internal/bytesx varint — frame Type must be parsed/encoded as a full 62-bit QUIC varint; high codepoints must not overflow or be rejected as parse errors. |
| `11-6` | SHOULD | When a frame-type value exists in only one of the HTTP/2/HTTP/3 registries, every effort should be made to avoid assigning that value to an unrelated operation. <br>_(all [not-client])_ | If an entry is present in only one registry, every effort SHOULD be made to avoid assigning the corresponding value to an unrelated operation. | IANA/expert-reviewer guidance only — no code impact. |
| `11-7` | MAY | Expert reviewers may reject frame-type registrations that conflict with the same value in the corresponding HTTP/2 registry. <br>_(all [not-client])_ | Expert reviewers MAY reject unrelated registrations that would conflict with the same value in the corresponding registry. | IANA/expert-reviewer guidance only — no code impact. |
| `11-8` | MUST | Permanent frame-type registrations must include a Frame Type name/label field in addition to the common QUIC registry fields. <br>_(all [not-client])_ | permanent registrations in this registry MUST include the following field: Frame Type: A name or label for the frame type. | Registrant obligation only — no code impact. |
| `11-9` | MUST | Specifications defining frame types must describe the frame layout and semantics, including conditionally present parts. <br>_(all [not-client])_ | Specifications of frame types MUST include a description of the frame layout and its semantics, including any parts of the frame that are conditionally present. | Spec-author obligation only — no code impact. |
| `11-10` | informative | The assigned HTTP/3 frame-type codepoints are DATA=0x00, HEADERS=0x01, CANCEL_PUSH=0x03, SETTINGS=0x04, PUSH_PROMISE=0x05, GOAWAY=0x07, MAX_PUSH_ID=0x0d. <br>_(endpoint [CLIENT])_ | DATA \| 0x00 ... HEADERS \| 0x01 ... CANCEL_PUSH \| 0x03 ... SETTINGS \| 0x04 ... PUSH_PROMISE \| 0x05 ... GOAWAY \| 0x07 ... MAX_PUSH_ID \| 0x0d | http3/frame.go — verify frame-type constants match these exact values, including the non-contiguous MAX_PUSH_ID=0x0d (not 0x08-0x0c). |
| `11-11` | informative | Frame-type codepoints 0x02, 0x06, 0x08, and 0x09 are Reserved by this document (the HTTP/2 PRIORITY/PING/WINDOW_UPDATE/CONTINUATION slots) and are not usable HTTP/3 frames. <br>_(endpoint [CLIENT])_ | Reserved \| 0x02 \| This document ... Reserved \| 0x06 ... Reserved \| 0x08 ... Reserved \| 0x09 \| This document | http3/frame.go + stream/control dispatch — receipt of types 0x02/0x06/0x08/0x09 must trigger H3_FRAME_UNEXPECTED connection error (RFC 9114 §7.2.8), NOT fall into the ignore-unknown-frame path; encoder must never emit them. |
| `11-12` | MUST NOT | Frame-type codes of form 0x1f*N+0x21 (0x21, 0x40, ... 0x3ffffffffffffffe) are permanently unassignable grease values IANA must never allocate or list. <br>_(all [CLIENT])_ | Each code of the format 0x1f * N + 0x21 ... MUST NOT be assigned by IANA and MUST NOT appear in the listing of assigned values. | http3/frame.go — grease frame types (0x1f*N+0x21) must hit the ignore-unknown-frame path on receipt (never mapped to a known type); if the client greases, sent types must use this formula. |
| `11-13` | informative | The HTTP/3 Settings registry governs a 62-bit code space (setting identifier is a QUIC varint); 0x00-0x3f Standards Action/IESG Approval, others Specification Required. <br>_(endpoint [CLIENT])_ | The "HTTP/3 Settings" registry governs a 62-bit space. | http3/control.go — SETTINGS identifier and value must both be parsed as full 62-bit varints; arbitrarily large unknown identifiers must decode without error. |
| `11-14` | SHOULD | When a settings value exists in only one of the HTTP/2/HTTP/3 registries, every effort should be made to avoid assigning it to an unrelated operation. <br>_(all [not-client])_ | If an entry is present in only one registry, every effort SHOULD be made to avoid assigning the corresponding value to an unrelated operation. | IANA/expert-reviewer guidance only — no code impact. |
| `11-15` | MAY | Expert reviewers may reject settings registrations conflicting with the same value in the HTTP/2 Settings registry. <br>_(all [not-client])_ | Expert reviewers MAY reject unrelated registrations that would conflict with the same value in the corresponding registry. | IANA/expert-reviewer guidance only — no code impact. |
| `11-16` | MUST | Permanent settings registrations must include a Setting Name field (optional to specify) and a Default field. <br>_(all [not-client])_ | permanent registrations in this registry MUST include the following fields: Setting Name: A symbolic name for the setting. Specifying a setting name is optional. | Registrant obligation only — no code impact. |
| `11-17` | SHOULD | A registered setting's default should be the most restrictive possible value (the value assumed when the setting is absent). <br>_(all [not-client])_ | A default SHOULD be the most restrictive possible value. | Registry design guidance; cross-check http3/control.go assumes most-restrictive defaults for extension settings before/absent peer SETTINGS (client-side default handling lives in §7.2.4.2). |
| `11-18` | informative | Settings identifiers 0x00, 0x02, 0x03, 0x04, and 0x05 are Reserved by this document with default N/A (the HTTP/2 setting slots) and must not appear in HTTP/3 SETTINGS. <br>_(endpoint [CLIENT])_ | Reserved \| 0x00 \| This document \| N/A ... Reserved \| 0x02 ... Reserved \| 0x03 ... Reserved \| 0x04 ... Reserved \| 0x05 | http3/control.go — SETTINGS decoder must treat identifiers 0x00 and 0x02-0x05 as H3_SETTINGS_ERROR connection error (per §7.2.4.1), and the encoder must never emit them; note 0x01 is deliberately absent from the reserved list (it is QPACK SETTINGS_QPACK_MAX_TABLE_CAPACITY per RFC 9204). |
| `11-19` | informative | SETTINGS_MAX_FIELD_SECTION_SIZE is assigned identifier 0x06 with default Unlimited. <br>_(endpoint [CLIENT])_ | MAX_FIELD_SECTION_SIZE \| 0x06 \| Section 7.2.4.1 \| Unlimited | http3/control.go — the identifier used to advertise the client's field-section limit must be 0x06, and absence of the setting from the peer must be treated as unlimited (no cap on outgoing request field sections); check response.go/validate.go enforcement of our own advertised limit. |
| `11-20` | informative | Setting names may be abbreviated in registry tables by dropping the SETTINGS_ prefix (formatting only). <br>_(all [not-client])_ | For formatting reasons, setting names can be abbreviated by removing the 'SETTINGS_' prefix. | Editorial only — no code impact. |
| `11-21` | MUST NOT | Settings codes of form 0x1f*N+0x21 (0x21, 0x40, ... 0x3ffffffffffffffe) are permanently unassignable grease values IANA must never allocate or list. <br>_(all [CLIENT])_ | Each code of the format 0x1f * N + 0x21 ... MUST NOT be assigned by IANA and MUST NOT appear in the listing of assigned values. | http3/control.go — grease setting identifiers (0x1f*N+0x21) received in peer SETTINGS must be ignored (unknown-setting path), never rejected; if the client greases its own SETTINGS, use this formula. |
| `11-22` | informative | The HTTP/3 Error Codes registry manages a 62-bit code space (error codes are QUIC varints); 0x00-0x3f Standards Action/IESG Approval, others Specification Required. <br>_(endpoint [CLIENT])_ | The "HTTP/3 Error Codes" registry manages a 62-bit space. | http3 + quic/ — application error codes carried in RESET_STREAM/STOP_SENDING/CONNECTION_CLOSE must support the full 62-bit varint range in both directions; receiving any 62-bit value must not crash or misparse. |
| `11-23` | REQUIRED | Error-code registrations are required to include a description; expert reviewers check for duplication, and reuse of existing registrations is encouraged but not mandated. <br>_(all [not-client])_ | Registrations for error codes are required to include a description of the error code. | **VERIFY** Registrant/reviewer obligation only — no code impact. -- Level is wrong. The source sentence (line 2393) reads 'Registrations for error codes are required to include a description of the error code.' — lowercase 'required'. RFC 9114 §2.2 (lines 268-272) carries the RFC 8174 boilerplate: the key words, including REQUIRED, are normative 'when, and only when, they appear in all capitals'. Lowercase 'required' is therefore NOT a BCP 14 keyword. Fix: change level from 'REQUIRED' to 'informative'. (Note the contrast with §11.2.3's genuine keyword usage two paragraphs later, line 2402: 'Permanent registrations in this registry MUST include the following field: Name' — that is the all-caps MUST already captured by fact 11-25.) Quote text and applies_to/client_relevant are correct; only the level field needs changing. \|\| Level is wrong. The source sentence (RFC 9114 line 2393-2394) reads "Registrations for error codes are required to include a description of the error code." — lowercase "required", not the RFC 2119 keyword REQUIRED. RFC 9114 §2.2 (lines 268-272) states the key words are to be interpreted per BCP 14 "when, and only when, they appear in all capitals, as shown here." No uppercase 2119 keyword appears anywhere in this sentence or in the two that follow it ("An expert reviewer is advised to examine...", "Use of existing registrations is to be encouraged, but not mandated."). Fix: change level from "REQUIRED" to "informative". Note the second half of the requirement text is also softer than the source: the RFC only says a reviewer "is advised to examine new registrations for possible duplication" — the only uppercase keyword in this paragraph is the MAY in the separate HTTP/2-collision sentence already captured as 11-24. Quote itself is verbatim and applies_to/client_relevant are correct. |
| `11-24` | MAY | Registering values that collide with the HTTP/2 Error Code registry is discouraged, and expert reviewers may reject such registrations. <br>_(all [not-client])_ | Use of values that are registered in the "HTTP/2 Error Code" registry is discouraged, and expert reviewers MAY reject such registrations. | IANA/expert-reviewer guidance only — no code impact. |
| `11-25` | MUST | Permanent error-code registrations must include a Name field and a Description field. <br>_(all [not-client])_ | Permanent registrations in this registry MUST include the following field: Name: A name for the error code. | Registrant obligation only — no code impact. |
| `11-26` | informative | The initial HTTP/3 error codes were deliberately placed at 0x0100+ (Specification Required range) to avoid numeric collision with HTTP/2 error codes — HTTP/3 must not reuse HTTP/2's 0x00-0x0d values for these semantics. <br>_(endpoint [CLIENT])_ | These error codes were selected from the range that operates on a Specification Required policy to avoid collisions with HTTP/2 error codes. | **VERIFY** http3 error constants (frame.go/control.go/stream.go or errors file) — verify all H3_* constants use the 0x0100-based values, not HTTP/2 numeric values; do not share constants with the conn/ HTTP/2 stack. -- The quote is verbatim (lines 2408-2410) and level 'informative' is right for that sentence, but the requirement text embeds a normative mandate the RFC does not state: 'HTTP/3 must not reuse HTTP/2's 0x00-0x0d values for these semantics'. §11.2.3 contains no such prohibition — the only related normative text is the weaker 'Use of values that are registered in the "HTTP/2 Error Code" registry is discouraged, and expert reviewers MAY reject such registrations' (lines 2397-2398, already captured as fact 11-24), which is guidance to IANA expert reviewers, not to endpoints. Fix: drop the 'must not' clause and restate as fact only — 'The initial HTTP/3 error codes were selected from the Specification Required range (0x0100-0x0110) so they do not numerically collide with HTTP/2 error codes.' The codepoint values themselves and the reconcile note (H3_* constants must use 0x0100-based values, not shared with conn/) are accurate and unaffected. |
| `11-27` | informative | Error codepoints: H3_NO_ERROR=0x0100 (no error), H3_GENERAL_PROTOCOL_ERROR=0x0101, H3_INTERNAL_ERROR=0x0102, H3_STREAM_CREATION_ERROR=0x0103, H3_CLOSED_CRITICAL_STREAM=0x0104, H3_FRAME_UNEXPECTED=0x0105 (frame not permitted in current state). <br>_(endpoint [CLIENT])_ | H3_NO_ERROR \| 0x0100 ... H3_GENERAL_PROTOCOL_ERROR \| 0x0101 ... H3_INTERNAL_ERROR \| 0x0102 ... H3_STREAM_CREATION_ERROR \| 0x0103 ... H3_CLOSED_CRITICAL_STREAM \| 0x0104 ... H3_FRAME_UNEXPECTED \| 0x0105 | http3 error constants — verify these six values exactly; H3_NO_ERROR (0x0100) on graceful close, H3_CLOSED_CRITICAL_STREAM (0x0104) when the control stream closes, H3_FRAME_UNEXPECTED (0x0105) for wrong-stream/wrong-state frames in control.go/stream.go. |
| `11-28` | informative | Error codepoints: H3_FRAME_ERROR=0x0106 (frame violated layout/size rules), H3_EXCESSIVE_LOAD=0x0107, H3_ID_ERROR=0x0108 (identifier used incorrectly), H3_SETTINGS_ERROR=0x0109 (invalid SETTINGS values), H3_MISSING_SETTINGS=0x010a (no SETTINGS frame received). <br>_(endpoint [CLIENT])_ | H3_FRAME_ERROR \| 0x0106 ... H3_EXCESSIVE_LOAD \| 0x0107 ... H3_ID_ERROR \| 0x0108 ... H3_SETTINGS_ERROR \| 0x0109 ... H3_MISSING_SETTINGS \| 0x010a | http3 error constants — verify values; H3_FRAME_ERROR (0x0106) used by frame.go on malformed frame length/layout, H3_SETTINGS_ERROR (0x0109) and H3_MISSING_SETTINGS (0x010a) used by control.go, H3_ID_ERROR (0x0108) for GOAWAY/push-ID monotonicity violations. |
| `11-29` | informative | Error codepoints: H3_REQUEST_REJECTED=0x010b (request not processed), H3_REQUEST_CANCELLED=0x010c (data no longer needed), H3_REQUEST_INCOMPLETE=0x010d (stream terminated early), H3_MESSAGE_ERROR=0x010e (malformed message), H3_CONNECT_ERROR=0x010f, H3_VERSION_FALLBACK=0x0110 (retry over HTTP/1.1). <br>_(endpoint [CLIENT])_ | H3_REQUEST_REJECTED \| 0x010b ... H3_REQUEST_CANCELLED \| 0x010c ... H3_REQUEST_INCOMPLETE \| 0x010d ... H3_MESSAGE_ERROR \| 0x010e ... H3_CONNECT_ERROR \| 0x010f ... H3_VERSION_FALLBACK \| 0x0110 | http3 error constants + stream.go/stream_body.go — client cancelling a request must send H3_REQUEST_CANCELLED (0x010c); receipt of H3_REQUEST_REJECTED (0x010b) means safely retryable (no processing occurred); H3_MESSAGE_ERROR (0x010e) for validate.go malformed-message rejections. |
| `11-30` | MUST NOT | Error codes of form 0x1f*N+0x21 (0x21, 0x40, ... 0x3ffffffffffffffe) are permanently unassignable grease values IANA must never allocate or list. <br>_(all [CLIENT])_ | Each code of the format 0x1f * N + 0x21 ... MUST NOT be assigned by IANA and MUST NOT appear in the listing of assigned values. | http3 + quic/ — unknown/grease error codes received from the peer must be handled gracefully (treated as generic/unknown error, per §8.1 semantics), never as a parse failure; greased error codes an endpoint sends use this formula. |
| `11-31` | informative | The HTTP/3 Stream Types registry governs a 62-bit code space (unidirectional stream type is a QUIC varint); 0x00-0x3f Standards Action/IESG Approval, others Specification Required. <br>_(endpoint [CLIENT])_ | The "HTTP/3 Stream Types" registry governs a 62-bit space. | http3/control.go (or stream.go) unidirectional-stream dispatch — the stream-type prefix must be read as a full 62-bit varint; large unknown types must decode without overflow. |
| `11-32` | MUST | Permanent stream-type registrations must include a Stream Type name field and a Sender field whose value is "Client", "Server", or "Both". <br>_(all [not-client])_ | Sender: Which endpoint on an HTTP/3 connection may initiate a stream of this type. Values are "Client", "Server", or "Both". | Registrant obligation; the Sender constraint for concrete types is reconciled in 11-34/11-35. |
| `11-33` | MUST | Specifications for permanent stream-type registrations must describe the stream type, including the layout and semantics of the stream contents. <br>_(all [not-client])_ | Specifications for permanent registrations MUST include a description of the stream type, including the layout and semantics of the stream contents. | Spec-author obligation only — no code impact. |
| `11-34` | informative | The Control Stream is unidirectional stream type 0x00 and may be initiated by both client and server. <br>_(client,server [CLIENT])_ | Control Stream \| 0x00 \| Section 6.2.1 \| Both | http3/control.go — client's control stream must open a QUIC unidirectional stream and write varint type 0x00 first; incoming server unidirectional stream with type 0x00 must be dispatched as the peer control stream. |
| `11-35` | informative | The Push Stream is unidirectional stream type 0x01 and may be initiated only by the server. <br>_(server,client [CLIENT])_ | Push Stream \| 0x01 \| Section 4.6 \| Server | http3 uni-stream dispatch (control.go/stream.go) — client must NEVER open a stream of type 0x01 (a server would kill the connection with H3_STREAM_CREATION_ERROR); incoming type 0x01 from the server is a push stream and must be handled per the client's MAX_PUSH_ID state, not treated as unknown. |
| `11-36` | MUST NOT | Stream-type codes of form 0x1f*N+0x21 (0x21, 0x40, ... 0x3ffffffffffffffe) are permanently unassignable grease values IANA must never allocate or list. <br>_(all [CLIENT])_ | Each code of the format 0x1f * N + 0x21 ... MUST NOT be assigned by IANA and MUST NOT appear in the listing of assigned values. | http3 uni-stream dispatch — incoming unidirectional streams with grease/unknown types (0x21, 0x40, ...) must be tolerated (abort reading with H3_STREAM_CREATION_ERROR or discard, per §6.2.3) without a connection error; if the client greases, opened stream types use this formula. |

### 9114 section A - Considerations for Transitioning from HTTP/2

| ID | Level | Requirement (scope) | RFC 9114 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `A-1` | informative | HTTP/3 stream identifier space is 2^62-1 streams, far larger than HTTP/2's; exhaustion is bounded by other QUIC limits first. <br>_(endpoint [CLIENT])_ | HTTP/3 permits use of a larger number of streams (2^62-1) than HTTP/2. | quic/ — stream IDs must be full 62-bit varints end to end; no inherited 2^31-1 HTTP/2 cap in stream allocation or GOAWAY comparison (http3/control.go). |
| `A-2` | informative | Stream concurrency is managed by QUIC, which considers a stream closed only when all data is received and sent data is acknowledged (later than HTTP/2's END_STREAM commit). <br>_(endpoint [CLIENT])_ | QUIC considers a stream closed when all data has been received and sent data has been acknowledged by the peer. | quic/ stream state machine — stream slot/credit release must wait for FIN ack, and http3/ must not impose its own MAX_CONCURRENT_STREAMS-style gate. |
| `A-3` | informative | Servers might permit more concurrent client-initiated bidirectional streams than in HTTP/2 to achieve equivalent concurrency. <br>_(server [not-client])_ | HTTP/3 servers might choose to permit a larger number of concurrent client-initiated bidirectional streams to achieve equivalent concurrency to HTTP/2 | n/a for client — quic/ just honours the server's MAX_STREAMS transport parameter as given. |
| `A-4` | informative | All HTTP/3 frames on all streams are subject to QUIC flow control, not just DATA payload as in HTTP/2. <br>_(all [CLIENT])_ | All HTTP/3 frames are sent on QUIC streams, so all frames on all streams are flow controlled in HTTP/3. | quic/ flow-control accounting — control-stream, QPACK-stream, and HEADERS bytes all count against MAX_DATA/MAX_STREAM_DATA; no DATA-only exemption ported from conn/. |
| `A-5` | informative | Clients control the number of in-flight server pushes via the MAX_PUSH_ID frame, not via unidirectional stream concurrency. <br>_(client [CLIENT])_ | HTTP/3 clients use the MAX_PUSH_ID frame to control the number of pushes received from an HTTP/3 server. | http3/control.go — client sends no MAX_PUSH_ID (push is a non-goal), so any incoming push stream / PUSH_PROMISE push ID must be rejected as H3_ID_ERROR consistently. |
| `A-6` | informative | The generic HTTP/3 frame layout has no stream number, no Flags field, and no END_STREAM flag; QUIC supplies stream identity and termination. <br>_(all [CLIENT])_ | This permits the removal of the Flags field from the generic frame layout. | http3/frame.go — frame header must be exactly Type varint + Length varint; end-of-message signalled only by QUIC FIN (quic/ stream close), never an in-band flag. |
| `A-7` | informative | HTTP/2 frame IDs that are no longer used are reserved in HTTP/3 rather than reassigned. <br>_(all [CLIENT])_ | Where an HTTP/2-defined frame is no longer used, the frame ID has been reserved | **VERIFY** http3/frame.go — types 0x02, 0x06, 0x08, 0x09 must be recognized as reserved and trigger connection error H3_FRAME_UNEXPECTED per Section 7.2.8, not skipped as unknown. -- Quote is verbatim (A.2, lines 2708-2710) and the level/roles are right, but the requirement's added clause 'rather than reassigned' is factually wrong and is contradicted by this very appendix. A.2.5 (line 2790-2792) states the RST_STREAM (0x03) code point IS reassigned: 'The same code point is used for the CANCEL_PUSH frame'. Only 0x02, 0x06, 0x08, 0x09 are actually reserved (Section 11.2.1 / 7.2.8). Fix: restate as 'HTTP/2 frame IDs that are no longer used are reserved in HTTP/3 to maximize portability (0x02, 0x06, 0x08, 0x09); note that 0x03 is an exception -- it is reused for CANCEL_PUSH.' Drop the universal 'rather than reassigned'. |
| `A-8` | informative | Frame types that exist in both HTTP/2 and HTTP/3 do not have identical semantics; HTTP/2 parsing assumptions must not be reused. <br>_(all [CLIENT])_ | even frame types that appear in both mappings do not have identical semantics. | http3/frame.go — verify no frame/ (HTTP/2 codec) semantics leaked in: no flags, no pad-length octet, varint lengths, different SETTINGS/GOAWAY payloads. |
| `A-9` | informative | HTTP/3 provides no means of signaling priority (no PRIORITY frame, no HEADERS priority region). <br>_(all [CLIENT])_ | HTTP/3 does not provide a means of signaling priority. | http3/request.go + frame.go — client must never emit priority signals; inbound type 0x02 handled as reserved (H3_FRAME_UNEXPECTED). |
| `A-10` | informative | QPACK makes all dynamic-table modifications on a single unidirectional (encoder) stream so updates are totally ordered. <br>_(all [CLIENT])_ | QPACK uses a single unidirectional stream to make all modifications to the dynamic table, ensuring a total order of updates. | qpack/encoder.go + qpack/instructions.go + http3/stream.go (AppendClientQPACKStream) — dynamic-table insertions only ever travel on the encoder stream, never inside HEADERS. |
| `A-11` | informative | Frames carrying encoded field sections only reference dynamic-table state at a point in time; decoding them must not modify the table. <br>_(all [CLIENT])_ | All frames that contain encoded fields merely reference the table state at a given time without modifying it. | qpack/decoder.go + dynamic_table.go — decoding a HEADERS field section is read-only against the table; only encoder-stream instructions mutate it. |
| `A-12` | informative | All HTTP/3 frame headers and payloads are subject to QUIC flow control (unlike HTTP/2 where only DATA payload was). <br>_(all [CLIENT])_ | Therefore, all frame headers and payload are subject to flow control. | quic/ — frame-header bytes written by http3/ consume send credits like any stream bytes; no header-byte exemption in credit accounting. |
| `A-13` | informative | HTTP/3 frame fields, including stream IDs and push IDs, use QUIC variable-length integer encoding with a larger value range than HTTP/2. <br>_(all [CLIENT])_ | Frame type definitions in HTTP/3 often use the QUIC variable-length integer encoding. | http3/frame.go + internal/bytesx varint codec — type, length, stream IDs, and push IDs all parse/emit as QUIC varints, full 62-bit range. |
| `A-14` | informative | Extension frame types that depend on flags must allocate flag space inside their frame payload since the generic layout has none. <br>_(all [not-client])_ | those frames that depend on the presence of flags need to allocate space for flags as part of their frame payload. | n/a — guidance for extension-frame designers; http3/ defines no extension frames. |
| `A-15` | informative | HTTP/2 extensions port to HTTP/3 by replacing stream 0 with the control stream; HTTP/3 extensions must not assume cross-stream ordering. <br>_(all [not-client])_ | HTTP/3 extensions will not assume ordering, but would not be harmed by ordering, and are expected to be portable to HTTP/2. | n/a — extension-design guidance; no extensions implemented in http3/. |
| `A-16` | informative | DATA (0x00) frames in HTTP/3 have no padding; payload is entirely message body. <br>_(all [CLIENT])_ | DATA (0x00): Padding is not defined in HTTP/3 frames. | http3/frame.go + stream_body.go — DATA payload delivered verbatim as body; confirm the internal/bytesx RFC 7540 pad-strip helper is never applied on the H3 path. |
| `A-17` | informative | HEADERS (0x01) frames in HTTP/3 have no PRIORITY region and no padding; payload is only the encoded field section. <br>_(all [CLIENT])_ | The PRIORITY region of HEADERS is not defined in HTTP/3 frames. | http3/frame.go + response.go — entire HEADERS payload goes to qpack/decoder.go; no prefix octets skipped before decoding. |
| `A-18` | informative | RST_STREAM frames do not exist in HTTP/3; code point 0x03 is reused for the CANCEL_PUSH frame, and stream resets happen at the QUIC layer. <br>_(all [CLIENT])_ | RST_STREAM frames do not exist in HTTP/3, since QUIC provides stream lifecycle management. The same code point is used for the CANCEL_PUSH frame | http3/frame.go — 0x03 must parse as CANCEL_PUSH (control-stream-only), never as reserved or a reset; resets use quic/ RESET_STREAM/STOP_SENDING. |
| `A-19` | informative | SETTINGS frames are sent only at the beginning of the connection (first frame of the control stream). <br>_(endpoint [CLIENT])_ | SETTINGS frames are sent only at the beginning of the connection. | http3/control.go — client emits SETTINGS as the control stream's first frame; a second inbound SETTINGS is connection error H3_FRAME_UNEXPECTED (Section 7.2.4). |
| `A-20` | informative | PUSH_PROMISE (0x05) does not reference a stream; instead the push stream references the PUSH_PROMISE via a push ID. <br>_(all [CLIENT])_ | the push stream references the PUSH_PROMISE frame using a push ID. | http3/frame.go — PUSH_PROMISE payload layout is Push ID varint + encoded field section (no promised-stream-ID field as in HTTP/2). |
| `A-21` | informative | PING frames do not exist in HTTP/3; liveness/keepalive is QUIC's job, and 0x06 is a reserved frame type. <br>_(all [CLIENT])_ | PING frames do not exist in HTTP/3, as QUIC provides equivalent functionality. | http3/frame.go — inbound 0x06 → H3_FRAME_UNEXPECTED; any keepalive uses quic/ PING frames, not an HTTP/3 frame. |
| `A-22` | informative | HTTP/3 GOAWAY (0x07) carries no error code, and in the client-to-server direction it carries a push ID rather than a stream ID. <br>_(all [CLIENT])_ | GOAWAY does not contain an error code. In the client-to-server direction, it carries a push ID instead of a server-initiated stream ID. | http3/control.go — GOAWAY payload is a single varint; server→client value interpreted as request-stream ID, client-sent GOAWAY (if any) must carry a push ID. |
| `A-23` | informative | WINDOW_UPDATE frames do not exist in HTTP/3; 0x08 is reserved and flow-control updates are QUIC MAX_DATA/MAX_STREAM_DATA. <br>_(all [CLIENT])_ | WINDOW_UPDATE frames do not exist in HTTP/3, since QUIC provides flow control. | http3/frame.go — inbound 0x08 → H3_FRAME_UNEXPECTED; window management lives entirely in quic/. |
| `A-24` | informative | CONTINUATION frames do not exist in HTTP/3; HEADERS and PUSH_PROMISE frames may instead be larger than HTTP/2 allowed. <br>_(all [CLIENT])_ | CONTINUATION frames do not exist in HTTP/3; instead, larger HEADERS/PUSH_PROMISE frames than HTTP/2 are permitted. | http3/frame.go + response.go — inbound 0x09 → H3_FRAME_UNEXPECTED; a single HEADERS frame may be arbitrarily large, bounded only by the client's MAX_FIELD_SECTION_SIZE handling (maxfieldsection path). |
| `A-25` | informative | The HTTP/3 frame-type space is 62 bits (vs 8 in HTTP/2); HTTP/2 extension frame types need separate HTTP/3 registration. <br>_(all [CLIENT])_ | the frame type space in HTTP/3 is substantially larger (62 bits versus 8 bits) | http3/frame.go — frame type parsed as full 62-bit varint; unknown non-reserved types skipped (payload discarded) rather than truncated to a byte. |
| `A-26` | informative | Settings are sent exactly once, as the first frame of the control stream, and cannot change afterwards. <br>_(endpoint [CLIENT])_ | settings are sent once, as the first frame of the control stream, and thereafter cannot change. | http3/control.go — no mid-connection SETTINGS re-emit; peer settings stored immutably once received; non-first control-stream SETTINGS rejected. |
| `A-27` | informative | HTTP/2 transport-level settings superseded by QUIC transport parameters are reserved in HTTP/3, and receiving any of them is an error. <br>_(receiver [CLIENT])_ | The superseded settings are reserved, and their receipt is an error. | http3/control.go SETTINGS parsing — identifiers 0x02–0x05 in a received SETTINGS → connection error H3_SETTINGS_ERROR (per Section 7.2.4.1). |
| `A-28` | informative | Setting identifier 0x01 (HTTP/2 SETTINGS_HEADER_TABLE_SIZE) is retained and defined by QPACK (SETTINGS_QPACK_MAX_TABLE_CAPACITY). <br>_(all [CLIENT])_ | SETTINGS_HEADER_TABLE_SIZE (0x01): See [QPACK]. | http3/control.go + qpack/dynamic_table.go — 0x01 is a valid setting (QPACK max table capacity), must not fall into the reserved-ID error path. |
| `A-29` | informative | Specifying setting identifier 0x02 (SETTINGS_ENABLE_PUSH) in an HTTP/3 SETTINGS frame is an error; push is governed by MAX_PUSH_ID instead. <br>_(receiver [CLIENT])_ | Specifying a setting with the identifier 0x02 (corresponding to the SETTINGS_ENABLE_PUSH parameter) in the HTTP/3 SETTINGS frame is an error. | http3/control.go — never send 0x02; on receipt → H3_SETTINGS_ERROR connection error. |
| `A-30` | informative | Specifying setting identifier 0x03 (SETTINGS_MAX_CONCURRENT_STREAMS) in an HTTP/3 SETTINGS frame is an error; QUIC governs stream concurrency. <br>_(receiver [CLIENT])_ | Specifying a setting with the identifier 0x03 (corresponding to the SETTINGS_MAX_CONCURRENT_STREAMS parameter) in the HTTP/3 SETTINGS frame is an error. | http3/control.go — never send 0x03; on receipt → H3_SETTINGS_ERROR; concurrency comes from quic/ MAX_STREAMS. |
| `A-31` | informative | Specifying setting identifier 0x04 (SETTINGS_INITIAL_WINDOW_SIZE) in an HTTP/3 SETTINGS frame is an error; window sizes are QUIC transport parameters. <br>_(receiver [CLIENT])_ | Specifying a setting with the identifier 0x04 (corresponding to the SETTINGS_INITIAL_WINDOW_SIZE parameter) in the HTTP/3 SETTINGS frame is an error. | http3/control.go — never send 0x04; on receipt → H3_SETTINGS_ERROR; initial windows set in quic/ transport-parameter handshake. |
| `A-32` | informative | Specifying setting identifier 0x05 (SETTINGS_MAX_FRAME_SIZE) in an HTTP/3 SETTINGS frame is an error; HTTP/3 has no frame-size setting. <br>_(receiver [CLIENT])_ | Specifying a setting with the identifier 0x05 (corresponding to the SETTINGS_MAX_FRAME_SIZE parameter) in the HTTP/3 SETTINGS frame is an error. | http3/control.go — never send 0x05; on receipt → H3_SETTINGS_ERROR; no max-frame-size chunking logic ported from conn/. |
| `A-33` | informative | Setting identifier 0x06 is retained with the same value but renamed SETTINGS_MAX_FIELD_SECTION_SIZE. <br>_(all [CLIENT])_ | This setting identifier has been renamed SETTINGS_MAX_FIELD_SECTION_SIZE. | http3/control.go — 0x06 parsed as MAX_FIELD_SECTION_SIZE and honoured both directions (client advertises its limit; respects server's when encoding requests). |
| `A-34` | informative | HTTP/3 setting values are QUIC variable-length integers (6/14/30/62 bits), not fixed 32-bit fields as in HTTP/2. <br>_(all [CLIENT])_ | setting values are variable-length integers (6, 14, 30, or 62 bits long) rather than fixed-length 32-bit fields as in HTTP/2. | http3/frame.go SETTINGS codec — payload is (id varint, value varint) pairs; values up to 2^62-1 must round-trip without 32-bit truncation. |
| `A-35` | informative | The HTTP/3 settings identifier space is 62 bits; HTTP/2-defined setting IDs are reserved, and settings must be registered separately per version. <br>_(all [CLIENT])_ | The IDs of settings defined in [HTTP/2] have been reserved for simplicity. | http3/control.go — reserved H2 IDs error per A-27; unknown 62-bit setting IDs (including 0x1f*N+0x21 grease) must be ignored, not rejected. |
| `A-36` | informative | Because QUIC streams may arrive out of order, endpoints should not wait for the peer's SETTINGS before responding on other streams. <br>_(endpoint [CLIENT])_ | endpoints are advised not to wait for the peers' settings to arrive before responding to other streams. | http3/dial.go + client.go — Client.Do must not block on server SETTINGS arrival; operate on defaults until the peer's control stream SETTINGS lands (Section 7.2.4.2). |
| `A-37` | informative | HTTP/2 and HTTP/3 error codes are not directly portable and are defined separately per version. <br>_(all [CLIENT])_ | the differences between HTTP/2 and HTTP/3 mean that error codes are not directly portable between versions. | http3/client.go error variables — H3 codes use the 0x100–0x110 space; never surface raw HTTP/2 numeric codes on the H3 path. |
| `A-38` | informative | HTTP/2 NO_ERROR (0x00) maps to H3_NO_ERROR. <br>_(all [CLIENT])_ | NO_ERROR (0x00): H3_NO_ERROR in Section 8.1. | http3/client.go — H3_NO_ERROR (0x100) defined and used for graceful connection close in control.go teardown. |
| `A-39` | informative | HTTP/2 PROTOCOL_ERROR maps to H3_GENERAL_PROTOCOL_ERROR except where more specific codes (H3_FRAME_UNEXPECTED, H3_MESSAGE_ERROR, H3_CLOSED_CRITICAL_STREAM) apply. <br>_(all [CLIENT])_ | This is mapped to H3_GENERAL_PROTOCOL_ERROR except in cases where more specific error codes have been defined. | http3/client.go + frame.go + control.go — error selection prefers the specific code (frame-context violations → H3_FRAME_UNEXPECTED, malformed messages → H3_MESSAGE_ERROR, closed control/QPACK stream → H3_CLOSED_CRITICAL_STREAM) over the general one. |
| `A-40` | informative | HTTP/2 INTERNAL_ERROR (0x02) maps to H3_INTERNAL_ERROR. <br>_(all [CLIENT])_ | INTERNAL_ERROR (0x02): H3_INTERNAL_ERROR in Section 8.1. | http3/client.go — H3_INTERNAL_ERROR (0x102) defined for internal failures. |
| `A-41` | informative | HTTP/2 FLOW_CONTROL_ERROR has no HTTP/3 equivalent; flow-control violations are QUIC-level errors. <br>_(all [not-client])_ | FLOW_CONTROL_ERROR (0x03): Not applicable, since QUIC handles flow control. | quic/ — flow-control violations raise QUIC FLOW_CONTROL_ERROR; http3/ must not define or expect an H3 flow-control code. |
| `A-42` | informative | HTTP/2 SETTINGS_TIMEOUT has no HTTP/3 equivalent because HTTP/3 SETTINGS are never acknowledged. <br>_(all [not-client])_ | SETTINGS_TIMEOUT (0x04): Not applicable, since no acknowledgment of SETTINGS is defined. | http3/control.go — confirm there is no SETTINGS ACK / timeout machinery ported from conn/. |
| `A-43` | informative | HTTP/2 STREAM_CLOSED has no HTTP/3 equivalent; QUIC handles stream lifecycle and closed-stream data. <br>_(all [not-client])_ | STREAM_CLOSED (0x05): Not applicable, since QUIC handles stream management. | quic/ stream state machine — data on closed streams handled at transport layer, no H3 code emitted. |
| `A-44` | informative | HTTP/2 FRAME_SIZE_ERROR (0x06) maps to H3_FRAME_ERROR for malformed frame sizes/payloads. <br>_(all [CLIENT])_ | FRAME_SIZE_ERROR (0x06): H3_FRAME_ERROR error code defined in Section 8.1. | http3/frame.go + client.go — payload/length mismatches and truncated varints in frames → connection error H3_FRAME_ERROR (0x106). |
| `A-45` | informative | H3_REQUEST_REJECTED indicates a request was not processed at all (the analogue of HTTP/2 REFUSED_STREAM). <br>_(all [CLIENT])_ | H3_REQUEST_REJECTED (in Section 8.1) is used to indicate that a request was not processed. | http3/stream.go — a server reset with H3_REQUEST_REJECTED (0x10B) must surface as request-not-processed, distinct from mid-response failure. |
| `A-46` | informative | HTTP/2 CANCEL (0x08) maps to H3_REQUEST_CANCELLED for request cancellation. <br>_(all [CLIENT])_ | CANCEL (0x08): H3_REQUEST_CANCELLED in Section 8.1. | http3/stream.go + client_ctx path — ctx cancellation aborts the request stream with error code H3_REQUEST_CANCELLED (0x10C) via quic/ RESET_STREAM/STOP_SENDING. |
| `A-47` | informative | HTTP/2 COMPRESSION_ERROR maps to multiple distinct QPACK error codes defined in RFC 9204. <br>_(all [CLIENT])_ | COMPRESSION_ERROR (0x09): Multiple error codes are defined in [QPACK]. | qpack/errors.go + http3/client.go (H3QpackDecompressionFailed, H3QpackEncoderStreamError, H3QpackDecoderStreamError) — three distinct codes used in the correct contexts, never a single blanket compression error. |
| `A-48` | informative | HTTP/2 CONNECT_ERROR (0x0a) maps to H3_CONNECT_ERROR. <br>_(all [CLIENT])_ | CONNECT_ERROR (0x0a): H3_CONNECT_ERROR in Section 8.1. | http3/client.go — H3_CONNECT_ERROR (0x10F) defined; if CONNECT unsupported, a stream reset with this code must still be decodable. |
| `A-49` | informative | HTTP/2 ENHANCE_YOUR_CALM (0x0b) maps to H3_EXCESSIVE_LOAD. <br>_(all [CLIENT])_ | ENHANCE_YOUR_CALM (0x0b): H3_EXCESSIVE_LOAD in Section 8.1. | http3/client.go — H3_EXCESSIVE_LOAD (0x107) recognized when the server closes or resets with it; candidate backoff signal for client/ retry once TransportH3 parity work lands. |
| `A-50` | informative | HTTP/2 INADEQUATE_SECURITY has no HTTP/3 equivalent; QUIC is assumed to provide sufficient security on all connections. <br>_(all [not-client])_ | INADEQUATE_SECURITY (0x0c): Not applicable, since QUIC is assumed to provide sufficient security on all connections. | quic/ TLS 1.3 handshake enforces security properties; no H3-level security error code needed. |
| `A-51` | informative | HTTP/2 HTTP_1_1_REQUIRED (0x0d) maps to H3_VERSION_FALLBACK. <br>_(all [CLIENT])_ | HTTP_1_1_REQUIRED (0x0d): H3_VERSION_FALLBACK in Section 8.1. | http3/client.go — H3_VERSION_FALLBACK (0x110) recognized; since the stack deliberately has no h2/h3 fallback, it must surface as a distinct terminal error to the caller. |
| `A-52` | informative | An intermediary encountering an upstream origin error can signal it downstream with an HTTP status such as 502 rather than propagating error codes. <br>_(intermediary [not-client])_ | An intermediary that encounters an error from an upstream origin can indicate this by sending an HTTP status code such as 502 (Bad Gateway) | n/a — intermediary-only guidance; client just receives the status via http3/response.go. |
| `A-53` | informative | An H3_REQUEST_REJECTED stream error signals the request was not processed and is safe to retry. <br>_(client [CLIENT])_ | a clear signal that the request was not processed and that the request is safe to retry. | **VERIFY** client/ retry policy (once H3 gains retry parity) + http3/stream.go — classify H3_REQUEST_REJECTED resets as safely retryable, unlike other stream errors. -- The quoted words are verbatim (A.4.1, lines 2950-2952) but they are attached to the wrong subject and the wrong role. In the source, the 'clear signal ... safe to retry' describes an intermediary that receives an HTTP/2 stream error of type REFUSED_STREAM from the origin -- not an H3_REQUEST_REJECTED received by a client. The RFC deliberately does NOT assert retry-safety for the H3 code; it only says 'Propagating this error condition to the client as an HTTP/3 stream error of type H3_REQUEST_REJECTED allows the client to take the action it deems most appropriate.' Fix: set applies_to to ['intermediary'] and restate as 'An intermediary receiving an HTTP/2 REFUSED_STREAM from the origin has a clear signal the request was not processed and is safe to retry; propagating it as H3_REQUEST_REJECTED lets the client decide its own action.' The client-side 'not processed' semantics are already covered verbatim by A-45; retry-safety for H3_REQUEST_REJECTED is an inference, not text of this section. \|\| Quote is verbatim (Appendix A.4.1, lines 2950-2952) and the level is right, but it is mis-attributed. In the source the clause's subject is an INTERMEDIARY that receives an HTTP/2 stream error of type REFUSED_STREAM from the origin: 'an intermediary that receives an HTTP/2 stream error of type REFUSED_STREAM from the origin has a clear signal that the request was not processed and that the request is safe to retry.' The RFC does not say in this passage that an H3_REQUEST_REJECTED stream error means 'safe to retry'; it only says propagating that condition downstream as H3_REQUEST_REJECTED 'allows the client to take the action it deems most appropriate.' Fix: set applies_to to ['intermediary','client'] (as A-54 correctly does) and reword the requirement to: 'An intermediary receiving an HTTP/2 REFUSED_STREAM error has a clear signal the request was not processed and is safe to retry; propagating it as an H3_REQUEST_REJECTED stream error lets the client decide the appropriate action.' The plain 'H3_REQUEST_REJECTED indicates a request was not processed' claim is already covered verbatim by A-45. |
| `A-54` | informative | Client request cancellations are indicated by terminating the stream with H3_REQUEST_CANCELLED, which intermediaries may propagate upstream. <br>_(client,intermediary [CLIENT])_ | pass on client request cancellations that are indicated by terminating a stream with H3_REQUEST_CANCELLED | http3/stream.go — the cancel path must use exactly H3_REQUEST_CANCELLED (not H3_NO_ERROR or internal error) so intermediaries can act on the signal (see Section 4.1.1). |
| `A-55` | informative | HTTP/2 and HTTP/3 error codes occupy non-overlapping numeric spaces to prevent accidental cross-version conversion. <br>_(all [CLIENT])_ | The error codes are defined in non-overlapping spaces in order to protect against accidental conversion | http3/client.go — every H3 error constant lives in the 0x1xx space; audit that no code path emits an HTTP/2 value (0x00–0x0d) as an H3 application error. |
| `A-56` | informative | An intermediary is permitted to promote stream errors to connection errors, but should weigh the cost to the HTTP/3 connection for possibly transient errors. <br>_(intermediary [not-client])_ | An intermediary is permitted to promote stream errors to connection errors but they should be aware of the cost | n/a — intermediary-only; the client must nonetheless tolerate receiving a connection close where it expected only a stream error (http3/control.go close handling). |

