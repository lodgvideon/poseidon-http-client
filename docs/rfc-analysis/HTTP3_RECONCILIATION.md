# HTTP/3 Client Conformance Reconciliation

_part 1 of 3: RFC 9114 + RFC 9204 (208 of 666 obligations)_

## Headline

| Verdict | Count |
|---------|-------|
| **PASS** | 121 |
| **FAIL** | 8 |
| **PARTIAL** | 6 |
| **UNTESTED** | 22 |
| **REVIEW** | 11 |
| **NOT_APPLICABLE** | 40 |
| **total** | 208 |

## Confirmed gaps (2/2 verifiers could not overturn) - 8

| ID | Lvl | Pkg | Requirement | Finding |
|----|-----|-----|-------------|---------|
| `9114/5-13` | MUST NOT | http3 | After receiving a GOAWAY from the peer, an endpoint MUST NOT initiate new requests or promise new pushes on that connection. | The verbatim MUST NOT is unconditional: "Endpoints MUST NOT initiate new requests ... after receipt of a GOAWAY frame from the peer." The code implements a *different* rule — the §5.2 retry-classification rule ("requests with id >= the GOAWAY id were not processed") — and reuses it as the shutdown gate. Those coincide only when the server's GOAWAY id is <= the client's next stream id. RFC 9114 §5.2 explicitly blesses [B gap] [A gap] |
| `9114/5-2` | SHOULD | client | An HTTP/3 implementation SHOULD open a new connection for new requests when the existing connection is approaching the negotiated idle timeout. | The SHOULD requires reuse logic to consult remaining idle time and prefer a fresh connection when close to the negotiated timeout. Nothing in the stack can even read that value, so the decision is impossible to make. The only mitigation is after-the-fact: once the QUIC idle timer actually fires the reader exits and Alive() goes false, so the dead conn is evicted (client/h3_pool.go:223, :448-460). That does not cover  [A gap] [B gap] |
| `9114/7.2.4-7.2.8-14` | SHOULD | http3 | Endpoints should send at least one grease setting in their SETTINGS frame. | The SHOULD is simply not implemented: this client never sends a grease setting, so it does not exercise a peer's unknown-identifier-ignore path. No documented justification for the deviation exists in dial.go or docs/HTTP3_DESIGN.md. [A gap] [B gap] |
| `9114/8-9-26` | SHOULD | http3 | Implementations should, with some probability, send a grease error code from the reserved space instead of H3_NO_ERROR when closing without error. | The SHOULD asks that H3_NO_ERROR be replaced, with some probability, by a reserved code from the 0x1f*N+0x21 space so peers cannot ossify on the defined set. The client emits the literal constant 0x0100 on every graceful close, with no probabilistic path. It also advertises no grease SETTINGS (http3/dial.go:124-127 defaultSettings holds only 0x01 and 0x07) and sends no grease frames or stream types, so nothing on the [A gap] [B gap] |
| `9204/2.2-3-4` | SHOULD | http3 | While a stream is blocked, its encoded field-section data should remain within the stream's flow-control window (releasing it prematurely enables memory-exhaustion attacks). | SHOULD-level, and the code does the opposite: receive credit is granted at Recv time regardless of whether the HTTP/3 layer has processed the field section, so a blocked stream's encoded field-section bytes are retained by the client while the server is free to send more. The memory-exhaustion attack the SHOULD guards is bounded but loosely: qpackMaxBlocked=16 (http3/dial.go:112) x FrameReader max frame len = maxResp [B gap] [A gap] |
| `9204/2.2-3-6` | MUST | qpack | A Required Insert Count smaller than the lowest value with which the section can be decoded must be treated as a connection error of type QPACK_DECOMPRESSION_FAILED. | Concrete failure: dynamic table with insertCount=3 (abs 0,1,2 live), maxCapacity 4096 so maxEntries=128. Server sends prefix 0x02 (EncInsertCount=2 -> RIC=1), 0x02 (Sign=0, DeltaBase=2 -> Base = 1+2 = 3), then 0x80 (Indexed Field Line, dynamic, index 0 -> abs = Base-0-1 = 2). Per RFC 9204 §2.1.2 the correct Required Insert Count is 3 (max referenced abs + 1), so the declared RIC of 1 is smaller than expected and MUST [A gap] [B gap] |
| `9204/4.1-4.4-2` | MUST | qpack | Implementations must be able to decode prefixed integers up to and including 62 bits long. | The quote is an unqualified MUST: 'QPACK implementations MUST be able to decode integers up to and including 62 bits long.' The implementation decodes at most 32 bits and returns ErrIntegerOverflow beyond. This is not merely theoretical for a receiver: a Section Acknowledgment (§4.4.1) or Stream Cancellation (§4.4.2) carries a QUIC stream ID, legal up to 2^62-1. A conforming server acknowledging a stream whose ID rea [A gap] [B gap] |
| `9204/4.1-4.4-37` | MUST | qpack | An encoder receiving a Section Acknowledgment for a stream with no outstanding unacknowledged non-zero-RIC field sections must treat it as a connection error of type QPACK_DECODER_STREAM_ERROR. | The MUST requires a connection error of type QPACK_DECODER_STREAM_ERROR (0x0202) when a Section Acknowledgment names a stream with no outstanding non-zero-RIC field section. The client tracks nothing, so every such acknowledgment — including one for a stream it never opened, or a duplicate for an already-acknowledged section — is accepted silently. This is receiver-side robustness against a hostile/buggy server, squa [A gap] [B gap] |

## Partial - met on the default path, gap on an edge/opt-in path - 6

| ID | Lvl | Pkg | Requirement | Finding |
|----|-----|-----|-------------|---------|
| `9114/10-17` | MUST NOT | qpack | On a secure channel, an implementation must not compress content mixing confidential and attacker-controlled data unless each source gets a separate compression context (BREACH-class attacks). | On the static-only default (qpack.NewEncoder / server advertising QPACK_MAX_TABLE_CAPACITY 0) the MUST NOT is satisfied by construction: no dynamic table exists, every section is prefix 0x00 0x00, nothing is retained across requests. The gap is the dynamic encode path, which is NOT client opt-in — http3/control.go:217 applyServerSettings feeds the SERVER's advertised capacity into enableEncoderDynamic (client.go:556) [A gap] [B gap] |
| `9114/6-27` | MUST | http3 | If either control stream is closed at any point, it is a connection error of type H3_CLOSED_CRITICAL_STREAM. | Failure scenario: a hostile or buggy server sends STOP_SENDING on the client-initiated control stream (itself a violation of §6.2.1's 'the receiver MUST NOT request that the sender close the control stream'). quic resets the send side; the HTTP/3 layer never learns, so the connection continues instead of closing with H3_CLOSED_CRITICAL_STREAM (0x0104). Impact is contained today only because the client writes nothing  [A gap] [B gap] |
| `9114/7.2.4-7.2.8-8` | MUST NOT | http3 | A SETTINGS frame must not contain the same setting identifier twice. | Met on the default/Dial path (unique identifiers), gap on the exported NewClient path: a caller passing a duplicated identifier makes this client emit a SETTINGS frame that its own receive-side parser would reject as H3_SETTINGS_ERROR. [A gap] [B gap] |
| `9204/2.2-3-16` | MUST | qpack | A field-line representation referencing a dynamic entry that is already evicted, or whose absolute index >= the declared Required Insert Count, must be treated as connection error QPACK_DECOMPRESSION_ | Half the MUST holds with direct test coverage; the other half (absolute index at or above the declared Required Insert Count) has no check at all, and a hostile server can exercise it with Sign=0 and a DeltaBase that pushes Base past RIC. Same root cause as 2.2-3-6. [A gap] [B gap] |
| `9204/4.1-4.4-44` | MUST | http3 | An encoder receiving an Insert Count Increment with an Increment field equal to zero must treat it as a connection error of type QPACK_DECODER_STREAM_ERROR. | The MUST binds 'an encoder that receives an Increment field equal to zero'. The client is an encoder on every connection, dynamic table or not, so a zero increment from a server that advertised capacity 0 must still be a QPACK_DECODER_STREAM_ERROR connection error — instead it is dropped on the floor. The obligation holds only on the dynamic-encoder path; TestConformance_RFC9204_Sec21_EncodeStaticFallbackServerCapZer [A gap] [B gap] |
| `9204/4.1-4.4-45` | MUST | http3 | An encoder receiving an Insert Count Increment that would push the Known Received Count beyond the number of insertions it has actually sent must treat it as a connection error of type QPACK_DECODER_S | Detection, escalation to 0x0202, and overflow safety are all correct on the dynamic-encoder path and are pinned by named tests, but the static-only path — reached whenever the server advertises no QPACK capacity — swallows the violation instead of closing the connection. Met on the default (server-advertises-capacity) path, gap on the static-only path. [A gap] [B gap] |

## Verifiers split - adjudicate by hand - 11

| ID | Lvl | Pkg | Requirement | Finding |
|----|-----|-----|-------------|---------|
| `9114/11-12` | MUST NOT | http3 | Frame-type codes of form 0x1f*N+0x21 (0x21, 0x40, ... 0x3ffffffffffffffe) are permanently unassignable grease values IANA must never allocate or list. | The obligation's reconcile target — grease frame types must hit the ignore-unknown-frame path and never be mapped to a known type — is met on both frame-carrying streams the client reads (control and request). But no test names it: not one test feeds a 0x1f*N+0x21 frame and asserts the connection survives and the response still decodes. A future edit that added 0x21 to a case list, or turned `default:` into a connect [B overturn] [A gap] |
| `9114/11-21` | MUST NOT | http3 | Settings codes of form 0x1f*N+0x21 (0x21, 0x40, ... 0x3ffffffffffffffe) are permanently unassignable grease values IANA must never allocate or list. | A grease setting identifier from the server is parsed, returned, and then ignored by applyServerSettings — never rejected — which is exactly what the row requires, and the reserved-range check cannot collide with a grease value. No test pins it: every SETTINGS test uses a defined identifier or the reserved 0x02–0x05 range, and the fuzzer throws the result away, so a regression that widened the reject predicate (e.g.  [B overturn] [A gap] |
| `9114/11-30` | MUST NOT | http3 | Error codes of form 0x1f*N+0x21 (0x21, 0x40, ... 0x3ffffffffffffffe) are permanently unassignable grease values IANA must never allocate or list. | The client is purely a receiver here (it sends only H3_NO_ERROR / H3_REQUEST_CANCELLED / H3_EXCESSIVE_LOAD / H3_MESSAGE_ERROR etc., all assigned), and receiver-side robustness is in scope. The implementation is correct by construction: the code is a uint64 carried through RESET_STREAM, STOP_SENDING, and CONNECTION_CLOSE with no validation, so a grease code from a hostile or greasing server is a generic error, never a [B overturn] [A gap] |
| `9114/11-36` | MUST NOT | http3 | Stream-type codes of form 0x1f*N+0x21 (0x21, 0x40, ... 0x3ffffffffffffffe) are permanently unassignable grease values IANA must never allocate or list. | The dispatch is correct: a grease/unknown unidirectional stream type is aborted with STOP_SENDING(H3_STREAM_CREATION_ERROR) and discarded, with the connection left alive — precisely the §6.2.3 behaviour the row asks for, and it can never be mis-mapped to a known type. But nothing tests it, and the neighbouring cases in the same switch all *do* raise connection errors, so the ignore branch is the one arm of routeUni w [A gap] [B overturn] |
| `9114/3-10` | MUST | http3 | Before making requests for an origin whose scheme is not "https", the client must ensure the server is willing to serve that scheme. | On the default path the client sends :scheme=https, which the MUST does not constrain. But a caller setting ClientOptions.DefaultScheme="http" or Request.Scheme="http" gets :scheme=http on an HTTP/3 connection with no mechanism whatsoever ensuring the server is willing to serve that scheme — the RFC 9114 §3.1.1 requirement is simply absent. Gap on an opt-in path, met on the default path. [B overturn] [A gap] |
| `9114/6-26` | MUST NOT | http3 | The receiver must never request that the sender close the control stream (no STOP_SENDING on it). | The MUST NOT holds by construction — the peer control stream is only ever Recv'd and Feed'd — but there is no negative test guarding a future regression (e.g. an abort path that walks all streams). [A overturn] [B gap] |
| `9114/6-37` | MUST NOT | http3 | Endpoints must not ascribe any meaning to received reserved-type streams. | The literal MUST NOT is satisfied — no meaning is ascribed at the HTTP/3 layer — but it is unpinned by any test. Note the related aggregate defect recorded under 6-15: because initial_max_streams_uni is exactly 3 with no MAX_STREAMS_UNI ever sent, a reserved stream can still consume the peer's last uni-stream credit and cause a STREAM_LIMIT_ERROR connection close, which does give a reserved stream a real consequence. [A gap] [B overturn] |
| `9114/6-7` | MUST | quic | A client must treat any server-initiated bidirectional stream as a connection error of type H3_STREAM_CREATION_ERROR unless an extension negotiated its use. | The defensive half of the MUST is met and tested: a server-initiated bidirectional stream is refused and the connection is torn down with a CONNECTION_CLOSE, never accepted. The error *type* is wrong: the peer is told STREAM_LIMIT_ERROR (transport 0x04) rather than H3_STREAM_CREATION_ERROR (0x0103). It is defensible under RFC 9000 §4.6 (the client advertises no initial_max_streams_bidi at all — quic/transport_params. [A gap] [B overturn] |
| `9114/7.2.4-7.2.8-16` | MUST NOT | http3 | HTTP/2 setting identifiers with no HTTP/3 equivalent must never be sent. | Met on the default/Dial path, gap on the exported NewClient path: nothing prevents a caller from emitting a reserved HTTP/2 identifier that a conformant peer must answer with H3_SETTINGS_ERROR. [A gap] [B overturn] |
| `9204/1-2.1-26` | SHOULD NOT | http3 | An encoder SHOULD NOT write an instruction unless sufficient stream and connection flow-control credit is available for the entire instruction, to avoid flow-control deadlocks. | Default path (credit available for the whole buffer) satisfies the SHOULD NOT trivially; the edge path (stream or connection window exhausted mid-buffer) violates its letter by emitting a partial instruction. The deadlock the SHOULD NOT exists to prevent is nevertheless structurally absent here: the sends are best-effort and never park (encMu/decWMu are never held across a wait), and the encoder never references an e [A gap] [B overturn] |
| `9204/5-8-37` | MUST | http3 | A value larger than the implementation can decode must be treated as a stream error of type QPACK_DECOMPRESSION_FAILED on a request stream, or a connection error of the appropriate type on the encoder | Two of the three limbs are conformant: on the server's encoder stream and decoder stream a value the client cannot decode becomes a connection error with the appropriate QPACK code (0x0201 / 0x0202), and the decoder-stream limb is pinned by name. The request-stream limb does not do what the quote requires: the RFC says a value too large to decode on a request stream MUST be a STREAM error of type QPACK_DECOMPRESSION_ [B gap] [A overturn] |

## Implemented, not pinned by a test - 22

| ID | Lvl | Pkg | Requirement | Finding |
|----|-----|-----|-------------|---------|
| `9114/3-2` | MUST NOT | http3 | If the certificate cannot be verified against the URI's origin server, the client must not treat the server as authoritative for that origin. | A verification failure surfaces from crypto/tls as a tls.AlertError, fails Establish, and Dial returns (nil, err) after closing the UDP socket — the server is never treated as authoritative because no usable Client ever exists. The two tests cover the two halves (an alert aborts with CRYPTO_ERROR; an Establish error yields no Client) but both inject the failure synthetically; neither actually presents an unacceptable [A gap] [B gap] |
| `9114/6-11` | SHOULD | http3 | Transport parameters should grant at least 1,024 bytes of flow-control credit to each unidirectional stream. | Implemented well above the SHOULD floor (256 KiB vs 1,024 bytes), but nothing pins it: a future edit of dial.go to a small or zero InitialMaxStreamDataUni would pass the whole suite, and interop would not necessarily catch it (a server's SETTINGS + QPACK instructions are typically far under 1 KiB). [B gap] [A gap] |
| `9114/6-13` | MUST | http3 | On receiving an unknown stream type, the recipient must either abort reading the stream or discard its data without processing. | The MUST is implemented correctly (abort reading via STOP_SENDING, nothing parsed, stream dropped), but no test drives a stream with an unknown type through routeUni/serviceControl, so the whole default branch — including the drop-from-pendingUni behaviour — is unpinned. [B gap] [A gap] |
| `9114/6-14` | SHOULD | http3 | If the recipient aborts reading an unknown stream type, it should use H3_STREAM_CREATION_ERROR or a reserved error code. | The SHOULD is satisfied — the abort uses exactly the recommended H3_STREAM_CREATION_ERROR code — but the code value on this path is not asserted anywhere. [A gap] [B gap] |
| `9114/6-20` | MUST | http3 | A receiver must tolerate unidirectional streams that are closed or reset before the stream-type header was received. | The MUST is satisfied by construction (quiet tolerance, no connection error, bounded memory), but nothing pins it; a future change that promoted a Finished() pending stream to H3_CLOSED_CRITICAL_STREAM would go unnoticed. [B gap] [A gap] |
| `9114/6-24` | MUST | http3 | Receipt of a second stream claiming to be a control stream is a connection error of type H3_STREAM_CREATION_ERROR. | Implemented exactly as required (second control stream → connection error H3_STREAM_CREATION_ERROR, 0x0103), but the control-stream arm of that check has no test; only the sibling QPACK arm is covered. [B gap] [A gap] |
| `9114/6-25` | MUST NOT | http3 | The sender must never close its control stream. | The MUST NOT is satisfied structurally (the stream handle is unreachable after setup, and the one send passes fin=false), including through Close, but no assertion pins it. [B gap] [A gap] |
| `9114/7-7.2.3-20` | MUST | http3 | Receiving a DATA frame on the control stream requires responding with a connection error of type H3_FRAME_UNEXPECTED. | The behaviour is correct and lands in the same switch arm that a sibling test does exercise, but no test feeds a DATA frame on the control stream, so nothing pins this obligation specifically and the coverage map's claim that it does is inaccurate. Per the rubric this is implemented-but-unpinned, not PASS. [B gap] [A gap] |
| `9114/7-7.2.3-34` | MUST | http3 | Receiving CANCEL_PUSH on any stream other than the control stream is a connection error of type H3_FRAME_UNEXPECTED. | Correct code, correct error code, correct scope (connection close via control.go:356), and it shares a case arm with two types that are tested — but the obligation's own frame type is never fed to dispatchFrame, so no test pins it. Given the pattern in this bucket (three separate obligations relying on one untested shared arm), the arm should be enumerated in a table-driven test rather than sampled. [A gap] [B gap] |
| `9114/7.2.4-7.2.8-11` | MUST | http3 | Unknown setting identifiers must be ignored. | Unknown identifiers are ignored exactly as required (parsed, not rejected; no semantic effect), but no test pins the ignore-and-continue behaviour end-to-end. [B gap] [A gap] |
| `9114/7.2.4-7.2.8-15` | MUST NOT | http3 | Received grease settings must be given no semantic meaning. | Grease settings are structurally indistinguishable from unknown settings and are given no meaning, exactly as required, but no test receives a grease setting and asserts it has no effect. [A gap] [B gap] |
| `9114/7.2.4-7.2.8-25` | SHOULD | http3/quic | Clients should process received datagrams so SETTINGS is likely applied before the first request. | The client does process received datagrams from construction onward, which is what the SHOULD asks for; only the ordering guarantee is unpinned. [A gap] [B gap] |
| `9114/7.2.4-7.2.8-47` | MUST | http3 | A PUSH_PROMISE on the control stream is a connection error H3_FRAME_UNEXPECTED. | Implemented with the correct code on the correct arm, but the PUSH_PROMISE member of that case list is never exercised; the coverage matrix claims coverage that does not exist. [B gap] [A gap] |
| `9114/7.2.4-7.2.8-5` | MUST | http3 | A second SETTINGS frame on the control stream is a connection error H3_FRAME_UNEXPECTED. | Implemented exactly as required with the right error code, but no test feeds a second SETTINGS frame on the control stream. [A gap] [B gap] |
| `9114/7.2.4-7.2.8-55` | MUST | http3 | A GOAWAY on any stream other than the control stream is a connection error H3_FRAME_UNEXPECTED. | Implemented on the correct shared case arm with the correct error code, but GOAWAY specifically is never fed to a request stream by any test. [B gap] [A gap] |
| `9114/7.2.4-7.2.8-58` | MUST | http3 | MAX_PUSH_ID received on any stream other than the control stream is a connection error H3_FRAME_UNEXPECTED. | Implemented on the correct case arm with the correct error code; no test feeds MAX_PUSH_ID on a request stream. [B gap] [A gap] |
| `9114/7.2.4-7.2.8-60` | MUST | http3 | A client must treat any received MAX_PUSH_ID frame as connection error H3_FRAME_UNEXPECTED. | The client role is enforced correctly — receipt of MAX_PUSH_ID is unconditionally H3_FRAME_UNEXPECTED with no push-ID parsing — but that arm is unexercised. [A gap] [B gap] |
| `9114/7.2.4-7.2.8-65` | MUST NOT | http3 | Received grease frames must be given no meaning. | Grease frames are given no meaning and take the identical path as unknown types, as required, but no test pins that a well-formed grease frame is silently skipped rather than erroring. [B gap] [A gap] |
| `9114/8-9-29` | MUST | http3 | Implementations must ignore unknown or unsupported values in all extensible protocol elements (frame types, settings, error codes, stream types). | All four extensible element classes named by the requirement are implemented correctly: unknown frame types are consumed and dropped on both control and request streams, unknown SETTINGS identifiers are dropped, unknown stream types are abandoned, unknown error codes are never validated. Nothing pins any of it. The closest hits are TestClient_ControlFrameTooLarge (http3/control_test.go:171), which uses GREASE type 0x [B gap] [A gap] |
| `9114/8-9-30` | MUST | http3 | Implementations must discard the data of, or abort reading on, any unidirectional stream whose stream type is unknown or unsupported — never treat it as a connection error. | Implementation matches the requirement exactly - abort reading via STOP_SENDING with the section-6.2-recommended H3_STREAM_CREATION_ERROR, connection left intact, no unbounded buffering (a partial leading type varint can retain at most 7 bytes before ReadStreamType resolves). What is untested is precisely the discriminating behaviour: that an unknown type is NOT the connection error every neighbouring case is. Those  [B gap] [A gap] |
| `9204/4.1-4.4-14` | MUST NOT | http3 | The sender must never close (FIN) its encoder or decoder stream. | The MUST NOT is satisfied on every path I could find, but nothing pins it: a future refactor that passes fin=true or adds a graceful per-stream shutdown would not fail any test. [A gap] [B gap] |
| `9204/4.1-4.4-15` | MUST NOT | http3 | The receiver must never request that the sender close the encoder or decoder stream (e.g., via STOP_SENDING). | No abandon-unidirectional-stream path reaches the 0x02/0x03 streams, so the MUST NOT holds, but there is no regression guard: adding a future 'unused critical stream' cleanup would silently violate it. [A gap] [B gap] |

## Not applicable - 40

| ID | Pkg | Justification |
|----|-----|---------------|
| `9114/10-22` | quic | The MUST's antecedent ("when using HTTP/3 with 0-RTT") is never met for HTTP data today: there is no code that seals a 0-RTT packet, so no request is ever carried as early data and nothing is replayable — the stated non-goal holds for the data plane. But the row's check line asks |
| `9114/3-19` | client | The checklist's own check line sanctions this: "otherwise confirm pool never coalesces origins". The client never makes a reuse-for-a-new-origin decision — pool keys are (addr, tlsConfig) and the caller picks the Client. CAVEAT worth recording (not a violation of this row, since  |
| `9114/3-20` | client | The MUST NOT presupposes a reuse decision that this client never makes: there is no code path that considers routing a request for origin B over a connection established to origin A, so the prohibition cannot be violated. Not a server/intermediary mis-tag — it is genuinely vacuou |
| `9114/3-21` | client | NOT_APPLICABLE for the same reason as 3-19/3-20: with no origin coalescing there is no 'certificate unacceptable for the new origin' miss path whose behaviour this SHOULD governs. The adjacent behaviour the checklist asks about IS correct — a pool miss dials a new connection (han |
| `9114/3-22` | client | The checklist row itself states 'N/A if pool is per-origin', and the pool is per-origin. A connection is never associated with more than one origin, so there is no set of other origins to revalidate when a verification failure occurs. |
| `9114/3-27` | http3 | The obligation's second half — handling an INBOUND GOAWAY per §5.2 — is fully implemented and tested (http3/control.go:311-329 validates the id is a client-initiated bidi stream id and non-increasing; TestConformance_RFC9114_Sec52_GoAwayGatesRequests, TestConformance_RFC9114_Sec5 |
| `9114/3-5` | http3 | NOT_APPLICABLE under the declared library scope: no ALPN h2<->h3 fallback negotiation is a documented non-goal, and this library is a low-level stack where the caller selects the transport. The SHOULD binds an agent that owns HTTP-version selection for a URI; here that agent is t |
| `9114/4.1-11` | http3 | The obligation binds an endpoint that accepts pushed responses. This client never enables push, so no push stream is ever read; it refuses the push stream itself with a connection error (H3_ID_ERROR), which is strictly stronger than the required H3_FRAME_UNEXPECTED-on-PUSH_PROMIS |
| `9114/4.1-20` | http3 | Server-role obligation, out of scope for a client-only library. The in-scope half — never treating an un-FIN'd or truncated response as a successful complete response — is implemented and pinned by four tests. |
| `9114/4.1-23` | http3 | Server-role SHOULD. No distinct client action is required for 0x10D: an incomplete-request abort means the request may have been partially processed, which is exactly the default non-retryable classification the client already applies to every non-0x10B reset code. |
| `9114/4.2-4.3-10` | http3 | CLIENT ONLY scope: the Cookie field is a request header, so this client is never the recipient of the split Cookie lines the rule governs, and there is no HTTP/3→HTTP/1.1 translation layer (no net/url or H1 bridging on the H3 receive path) that would constitute "a context other t |
| `9114/4.2-4.3-32` | http3 | This obligation binds the step that derives :path from a URI. This client has no such step by design — the caller passes the request target explicitly — and rather than defaulting it silently the library refuses an empty Path, so it can never emit the value the MUST forbids. Not  |
| `9114/4.4-4.6-11` | http3 | Requires a CONNECT stream in tunnel state, which this client never establishes (no CONNECT support, see 4.4-4.6-2). The closest analogue on a normal request stream is already correct and tested: control-only and reserved h2 frame types yield H3_FRAME_UNEXPECTED (RFC_COVERAGE.md § |
| `9114/4.4-4.6-13` | http3 | A SHOULD NOT that binds a client operating a CONNECT tunnel. There is no CONNECT API and no bidirectional/half-close request surface at all in http3/, so the situation the SHOULD NOT guards against cannot arise. It does record a real prerequisite for any future CONNECT support: t |
| `9114/4.4-4.6-2` | http3 | The obligation is conditional on the client generating a CONNECT request. http3.Request has no CONNECT form and EncodeHeaders has no CONNECT branch, so the client never emits a CONNECT over HTTP/3. Note the reachable path: client/client.go:896 buildHeaders builds a regular CONNEC |
| `9114/4.4-4.6-3` | http3 | Vacuously satisfied while CONNECT is unsupported: the encoder can never produce a CONNECT request carrying :scheme/:path, because it refuses to encode any request lacking them and has no CONNECT form. The checklist row's own reading matches — the unconditional reject is correct t |
| `9114/4.4-4.6-35` | http3 | Deliberate scope: no HTTP/3 server push (MAX_PUSH_ID is never raised). A pushed request never reaches a state where origin authority verification could be performed, because the connection is torn down with H3_ID_ERROR first. Connection-setup certificate/authority validation does |
| `9114/4.4-4.6-36` | http3 | Deliberate scope: no HTTP/3 server push. The MUST NOT is unconditionally satisfied — the client never treats the server as authoritative for a pushed origin because it never processes a pushed origin. |
| `9114/4.4-4.6-37` | http3 | This SHOULD applies to a PUSH_PROMISE the client may legitimately receive. Because no MAX_PUSH_ID is ever sent, §7.2.5's MUST (connection error H3_ID_ERROR) fires first and takes precedence over the §4.6 SHOULD; sending CANCEL_PUSH instead would be non-conformant. Moot under the  |
| `9114/4.4-4.6-4` | http3 | Binds only a CONNECT request, which http3/ cannot generate (see 4.4-4.6-2). There is no authority-form code path to validate a host:port against. If CONNECT is implemented, this needs a new explicit-port check — today's :authority validation would let a portless authority through |
| `9114/4.4-4.6-46` | http3 | Deliberate scope: no HTTP caching layer (caching is the caller's responsibility) and no HTTP/3 server push. The MUST NOT binds an HTTP cache, of which this library has none; re-verify only if a cache or push acceptance is ever added. |
| `9114/5-11` | http3 | Deliberate scope: CLIENT ONLY with no HTTP/3 server push. The obligation is conditional on the endpoint sending a GOAWAY, and it is doubly vacuous here — a client-sent GOAWAY carries a *push ID* (RFC 9114 §5.2, catalog fact 5-8 at docs/rfc-analysis/RFC9114_FACTS.md:231), and this |
| `9114/5-12` | http3 | Deliberate scope: no HTTP/3 server push, client only. The obligation ('continue to cancel as more requests or pushes arrive') is predicated on having sent a GOAWAY, which never happens. Even hypothetically there is no late-arriving stream class to cancel: a push stream is refused |
| `9114/5-21` | http3 | The quote binds the GOAWAY *sender* ('the identifier in each frame MUST NOT be greater than the identifier in any previous frame'). This client is never a GOAWAY sender, so the non-increasing-id invariant cannot be violated. This is the sender-side twin of 5-22, which is separate |
| `9114/6-32` | http3 | Out of scope under the declared no-HTTP/3-server-push design, and preempted by a stronger MUST: because no MAX_PUSH_ID was ever sent, RFC 9114 §4.6 requires the client to treat receipt of ANY push stream as a connection error of type H3_ID_ERROR. Once the connection is closed the |
| `9114/6-33` | n/a | Server-role emission rule, and additionally moot under the no-server-push scope: the client rejects every push stream outright at http3/control.go:197-200, so no push stream header is ever produced or consumed. |
| `9114/6-34` | http3 | Out of scope under the declared no-HTTP/3-server-push design. The precondition for the duplicate check can never arise: the FIRST push stream already terminates the connection with H3_ID_ERROR (0x0108) — the very code this obligation demands for a duplicate — so a second push str |
| `9114/6-39` | http3 | Sender-side obligation for GREASE emission, which this client does not do; the checklist row itself marks it 'Optional; if grease emission is added'. Nothing to conform to today. (The receive-side handling of reserved types is judged under 6-13/6-14/6-37; that path uses STOP_SEND |
| `9114/7-7.2.3-32` | http3 | This SHOULD NOT binds a client that sends CANCEL_PUSH. This client has no CANCEL_PUSH send path at all and never enables push, so the antecedent can never hold — the requirement is vacuous rather than met by logic that could regress. The checklist row itself records this ('verify |
| `9114/7-7.2.3-33` | http3 | A SHOULD conditioned on the client having sent CANCEL_PUSH, which it structurally cannot. The H3_ID_ERROR path that governs instead is implemented and covered, so nothing is silently unhandled. |
| `9114/7.2.4-7.2.8-27` | quic | Deliberate scope exclusion: no 0-RTT / early data, and no session-resumption state is retained, so there is no resumption record to store settings alongside. This SHOULD is vacuous. It becomes load-bearing the moment a ClientSessionCache is wired into h3TLSConfig. |
| `9114/7.2.4-7.2.8-29` | quic | Deliberate scope exclusion: no 0-RTT / early data. The client always uses the RFC 9114 §7.2.4.1 default values until the server's SETTINGS arrives (http3/client.go:304), which is precisely the "default values if no values are stored" branch of this MUST. |
| `9114/7.2.4-7.2.8-35` | http3/quic | Deliberate scope exclusion: no 0-RTT. The compatibility comparison this MUST requires has no antecedent because there is never a previously specified settings set. |
| `9114/7.2.4-7.2.8-36` | http3/quic | Deliberate scope exclusion: no 0-RTT / early data, so there is no "after accepted 0-RTT" state in which a previously non-default setting could be omitted. |
| `9114/7.2.4-7.2.8-43` | http3 | Deliberate scope exclusion: no HTTP/3 server push. A resource can never be promised even once, so "promised multiple times" is unreachable and the SHOULD has no subject. |
| `9114/7.2.4-7.2.8-44` | http3 | Deliberate scope exclusion: no HTTP/3 server push. The mismatch comparison this MUST demands is unreachable — the connection is already closed on the first promise. |
| `9114/7.2.4-7.2.8-45` | http3 | Deliberate scope exclusion: no HTTP/3 server push. There is no pushed content to associate with any promising stream. |
| `9114/7.2.4-7.2.8-62` | http3 | Server-role receiver requirement, and the client's send side never emits MAX_PUSH_ID (deliberate scope exclusion: no HTTP/3 server push), so there is no sequence of values whose monotonicity could be violated. It becomes load-bearing only if push is ever enabled. |
| `9114/8-9-32` | http3 | The requirement binds an implementation that defines or uses an extension changing the semantics of an existing protocol component. This client implements no extension at all - no HTTP Datagrams (RFC 9297), no Extended CONNECT (RFC 9220), no WebTransport, no custom frame/setting/ |
| `9204/2.2-3-41` | http3 | Falls squarely under the declared deliberate scope 'No 0-RTT / early data'. With no resumption there is no remembered value, so there is nothing to validate and no mismatch can arise. Note the requirement is also mis-levelled on the row: the verbatim quote reads 'the encoder must |

