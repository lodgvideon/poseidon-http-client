# HTTP/1.1 Client Implementation Reconciliation vs RFC 9110/9112

_256 client-relevant obligations judged against http1/ + client/ code, then every non-PASS finding re-checked by two adversarial verifiers (default-refute) that try to prove the code actually complies. NOT_APPLICABLE = obligation is out of this client's scope (caching, redirect-follow, proxy, content negotiation) or the caller's responsibility. Status after verification:_

| Status | Count |
|--------|-------|
| PASS | 101 |
| UNTESTED | 0 |
| PARTIAL | 3 |
| FAIL | 3 |
| NOT_APPLICABLE | 142 |
| REVIEW | 7 |
| **TOTAL** | 256 |

## Actionable findings (FAIL / PARTIAL / UNTESTED / REVIEW)

| Bucket | Item | Level | Status | Evidence | Note |
|--------|------|-------|--------|----------|------|
| 3-connection | `8-9-14` | SHOULD | FAIL | No idle-connection reader exists anywhere in client/h1_pool.go: idle conns sit in rs.conns and are only revalidated via IsAlive() (which does not read the socket) on tick/acquire. There is no path that discards bare CRLFs or closes the connection on other unsolicited bytes. | SHOULD-level gap: the client does not monitor idle pooled conns, so it cannot discard CRLF-only noise or evict-on-unsolicited-data; a deliberate simplification (no reader goroutine on idle H1 conns) shared with many production clients. |
| 3-connection | `8-9-46` | SHOULD | FAIL | The h1Exchange state machine writes the full request body before reading any response: SendData→WriteBody (client/h1_transport.go:53-55, http1/conn.go:480-512) completes, then Recv→ReadResponse (h1_transport.go:60-70) reads. There is no concurrent reader on the connection during body transmission. | SHOULD-level gap: an early error response sent by the server while the client is still uploading is not noticed until the upload finishes; a deliberate single-goroutine simplification. |
| 3-connection | `8-9-47` | SHOULD | FAIL | Consequence of 8-9-46: because no response is read during upload (client/h1_transport.go SendData precedes Recv), there is no path that detects an early final response + Connection: close mid-upload and aborts WriteBody; the body write always runs to completion. | SHOULD-level gap: the client cannot cease transmitting on an early server rejection because it does not read the connection while transmitting. |
| 10-identifiers-conf | `4-24` | MUST NOT | PARTIAL | The client emits the Host field verbatim from :authority (http1/conn.go WriteRequest:429; validateFields:338 checks authority only for CR/LF/NUL, not for a userinfo '@'). client/request.go validateRequest likewise never inspects Authority for '@'. So a caller-supplied :authority of 'user@host' is emitted as 'Host: user@host'. The client does not synthesize userinfo itself and does not parse URIs, but it also does not strip a caller-supplied userinfo. Sibling divergence: the http3 send path DOES reject authority userinfo (http3/requestvalidate_test.go TestConformance_RFC9114_Sec431_AuthorityUserinfoAndHostMatch); the shared http1/client path has no equivalent. | Client never generates userinfo, but it passes a caller-supplied 'user@host' authority through into Host unstripped, unlike the http3 sibling — an unguarded edge, not a URI the client constructs. |
| 2-recv-fields | `6-16` | SHOULD | PARTIAL | ReadResponse accepts any "HTTP/1"-prefixed status line (conn.go:581) so it does not error on a higher minor version, but persistence is set as ex.keepAlive = proto == "HTTP/1.1" (l.608), so an "HTTP/1.2" response with no Connection header is treated as non-persistent (close) instead of being processed as HTTP/1.1 (persistent default). No test drives a >1.1 minor version. | Gap: higher minor versions are accepted but wrongly default to connection-close; §6-16 says process as the highest conformant minor (HTTP/1.1 → persistent). |
| 3-connection | `8-9-43` | SHOULD | PARTIAL | No proactive monitor on idle H1 conns: health tick calls evictDead→IsAlive (client/h1_pool.go:456-467) and IsAlive only reads the closed atomic bool (http1/conn.go:154-156), never the socket, so a peer FIN on an idle conn is not detected before reuse. A dead conn is discovered only on next use: the exchange errors, h1_transport.go release(false) discards it, and idempotent requests retry (retry.go builtinShouldRetry). | Partially satisfied — stale conns are eventually evicted and recovered-from on next use, but there is no constant/readable-event monitoring of idle connections for a closure signal as the SHOULD asks. |
| 3-connection | `8-9-13` | MUST NOT | REVIEW | The client only reads a socket inside an exchange (ReadResponse after WriteRequest, http1/conn.go:549). Idle pooled conns are never read: h1Pool.pickIdle/evictDead gate on http1.Conn.IsAlive (conn.go:154-156) which only checks the closed atomic bool and does NOT poll the socket. So idle-arriving bytes are never parsed as a standalone response — but they are also never detected, so a reused conn's next ReadResponse would consume them as its own status line. | No literal violation (no idle reader treats data as a response at idle), but the lack of idle-conn readable detection means unsolicited bytes could be mis-consumed by the next request; the real gap is 8-9-14/8-9-43. |
| 3-connection | `8-9-49` | SHOULD | REVIEW | WriteRequest unconditionally drops any Connection header (http1/conn.go:439-443 skip list includes 'connection') and never adds one of its own; there is no code path anywhere in client/ or http1/ that emits 'Connection: close' when the client decides a connection is its last. Close decisions are made locally (idle eviction, keepAlive=false eviction) with no in-flight request to carry the signal. | SHOULD-level gap: the client never advertises close intent to the server; it relies on server-driven close (honoured, 8-9-58) and silent local eviction. It also silently strips a caller-supplied Connection: close rather than honouring it. |
| 3-connection | `8-9-71` | MUST | REVIEW | http1.Conn.Close (http1/conn.go:159-162) delegates to nc.Close(); when nc is a *tls.Conn (ALPN 'http/1.1' path, client/h1_transport.go:380-406, or any TLS dialer), Go crypto/tls.Conn.Close sends a close_notify alert before closing the socket. No bare-TCP close is ever performed on a TLS connection — the *tls.Conn is the net.Conn. No test in the repo asserts the closure alert (grep for close_notify/CloseWrite finds only docs). | Code is correct via crypto/tls delegation, but no test exercises/asserts that the TLS closure alert is sent before close. |
| 4-methods | `9-59` | MUST | REVIEW | No Content-Type enforcement for an OPTIONS request carrying content anywhere in the send path: validateRequest (client/request.go:183-259) has no method/Content-Type logic, http1 WriteRequest (http1/conn.go:370) emits caller headers verbatim and enforces no Content-Type, and grep for OPTIONS/content-type in non-test source finds no such guard. An OPTIONS request with a body and no Content-Type is sent as-is. | Client-side MUST not implemented; the reconcile's 'enforce/error out if Content-Type absent when OPTIONS has content' is absent. |
| 4-methods | `9-64` | MUST NOT | REVIEW | No rejection of a request body when Method==TRACE: validateRequest (client/request.go:183) performs no method-vs-body check, and http1 WriteRequest/WriteBody (http1/conn.go:370,480) will frame and send a TRACE body (chunked or Content-Length) like any other method. No TRACE handling exists in the send path (confirmed by grep). | Hard MUST NOT violated — a caller-supplied TRACE body is sent rather than refused. |
| 6-representation-ctx | `10-40` | SHOULD | REVIEW | buildHeaders (client/client.go:845-905) and http1.WriteRequest (conn.go:370-475) add no User-Agent; grep for user-agent across client/*.go and http1/*.go (non-test) finds none. No default UA constant or opt-out flag exists. | SHOULD-level gap: the client is a user agent but sends no User-Agent unless the caller sets one in Request.Headers. The reconcile (default UA + opt-out) is not implemented. |
| 6-representation-ctx | `8-26` | SHOULD | REVIEW | detectEncoding (client/decompress.go:182-202) compares the content-encoding value only against 'gzip','deflate','br','zstd' via EqualFold; 'x-gzip' matches none and returns EncodingIdentity, so an x-gzip response is NOT decompressed even though gzip is supported. | SHOULD-level gap: gzip is implemented but the x-gzip alias is missing, so a server that labels a gzip body 'x-gzip' would have its body handed back still compressed. Low real-world impact (client advertises 'gzip'). |

---

### 1-send-framing

_37 items: PASS=28, NOT_APPLICABLE=9_

| Item | Level | Status | Evidence | Note |
|------|-------|--------|----------|------|
| `7-8` | MUST | PASS | http1/conn.go:1313 readChunkedChunk is a full chunked decoder (chunk-size, ext strip, last-chunk, trailers, CRLF). Tested http1/conformance_test.go:189 MultipleChunks, :212 EmptyChunkedBody, conformance_chunksize_test.go ValidChunkSizeAccepted. | Chunked decode is unconditional in the response path; cannot be disabled. |
| `7-9` | MUST | PASS | http1/conn.go:1000 parseChunkSize checks n>(MaxInt64-d)/16 before each shift, refusing overflow instead of wrapping. Tested conformance_chunksize_test.go:49 overflow case "FFFFFFFFFFFFFFFFF" → ErrInvalidChunkSize + KeepAlive false. | Overflow bounded by construction and pinned by test. |
| `7-10` | SHOULD | PASS | http1/conn.go:979 transferCodingName strips ";"-delimited parameters off the coding name, so a final coding "chunked;q=1" is accepted AS chunked. conformance_te_grammar_test.go:107 explicitly pins "chunked;q=1" as chunk-framed (params ignored, not errored). | Parameters on the chunked coding are tolerated/ignored rather than treated as an error — a deliberate deviation from the SHOULD. <br>_verifiers overturned -> PASS: transferCodingName (conn.go:979-984) strips ";"-params to get the coding name; this is exactly the RFC 9110 §10.1.4 grammar `transfer-coding = token *( OWS ";" OWS transfer-parameter )`. Parameters are PART of the grammar, so extracting the bare coding and framing "chunked;q=1" as chunked is correct, not a deviation. No MUST/SHOULD requires a recipient to reject parameters on a recognised coding, and the framing verdict (chunked, self-delimiting) is safe. te_grammar_test.go:101-119 pins this as intended, not accidental. Not a genuine gap._ |
| `7-13` | MUST | PASS | http1/conn.go:1325-1327 readChunkedChunk truncates the chunk-size line at ';' before parsing, discarding any extension. Tested http1/conn_test.go:770 TestHTTP1_ChunkExtensions ("5;ext=ignored"). | Unrecognized chunk extensions parse-and-discard; body still decodes. |
| `7-17` | MUST | PASS | Client keeps trailers in resp.Trailers, a collection distinct from resp.Headers (client/client.go:1064-1072 EventTrailers → resp.Trailers). The http1 transport itself drops trailers (conn.go:1348 consumeHeaders(nil,false)). Tested client/trailer_test.go, drain_trailers_test.go. | Retained trailers live in a separate resp.Trailers collection; the h1 layer discards them entirely rather than retaining. |
| `7-18` | MUST NOT | PASS | No path merges a trailer into resp.Headers; EventTrailers writes only resp.Trailers (client/client.go:1072). http1 discards trailers via consumeHeaders(nil,false) (conn.go:1348). Tested http1/conformance_obsfold_test.go:153 (trailer parsed with out=nil, never reaches header list) and client trailer tests. | Trailers are never auto-merged into the header section. |
| `7-22` | SHOULD | PASS | http1/conn.go:979 transferCodingName strips parameters from every transfer-coding element, not just chunked, so "gzip;q=x" as a coding is read as gzip with the parameter silently dropped rather than raising an error. | Same mechanism as 7-10: parameters on compression codings are ignored, not treated as an error — SHOULD deviation. <br>_verifiers overturned -> PASS: Same mechanism as 7-10 and equally correct: for "gzip;q=x" transferCodingName yields "gzip", lastTransferCoding returns non-chunked, and conn.go:1229-1231 falls to §6.3 rule 4 read-until-close. Parameters are grammar-legal (RFC 9110 §10.1.4); stripping them to identify the final coding is the required parse, and the framing outcome is correct and safe. No obligation to error. Not a gap._ |
| `7-29` | MUST NOT | PASS | client/request.go:231-234 validateRequest rejects any TE header whose value is not exactly "trailers" (so "TE: chunked" is refused); http1/conn.go:439-443 WriteRequest additionally strips "te" from the wire. Tested client/conformance_forbidden_headers_test.go:253 (TE:gzip rejected). | chunked can never appear in an emitted TE header. |
| `7-35` | MUST | NOT_APPLICABLE | http1/conn.go:439-443 WriteRequest unconditionally strips the "te" header from the HTTP/1.1 wire; the only TE the stack honors is H2 "TE: trailers" (client/request.go:176). No TE header is ever emitted on the 1.1 wire. | Obligation is vacuous — the client emits no TE header on HTTP/1.1, so there is nothing to pair a Connection: TE option with. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed: WriteRequest's hop-by-hop skip list (conn.go:439-443) drops "te" from the HTTP/1.1 wire unconditionally, and the only TE honoured is H2 "TE: trailers" (request.go:176-178,231-234). No TE is ever emitted on the 1.1 wire, so the obligation to pair a Connection: TE option with a TE header is vacuous. NA is correct._ |
| `4-5-8` | SHOULD | PASS | http1/conn.go:580-588 ReadResponse does SplitN(line," ",3) and consumes only parts[0] (proto) and parts[1] (code); parts[2] (reason-phrase) is never stored or used for any logic. Retry/classification key on status code only. Status-line parse tested http1/conformance_test.go:144 (reason present, only code asserted). | Reason-phrase is structurally discarded, never surfaced in Response. |
| `4-5-9` | MUST | NOT_APPLICABLE | Obligation targets a server. Client tolerance verified: http1/conn.go:580-581 SplitN accepts both "HTTP/1.1 200 " (trailing SP, parts len 3, empty reason) and "HTTP/1.1 200" (no SP, parts len 2, ≥2 passes). | Server-side MUST; the client parser accepts empty reason with or without the trailing SP. <br>_verifiers overturned -> NOT_APPLICABLE: Server-side MUST re reason-phrase; the client is a recipient. Parser tolerance confirmed at conn.go:580-583: strings.SplitN(line," ",3) accepts both "HTTP/1.1 200" (len 2) and "HTTP/1.1 200 " (len 3, empty reason); only len<2 or a non-HTTP/1 prefix is rejected. Client never depends on a reason-phrase. NA correct._ |
| `4-5-21` | MUST NOT | PASS | http1/conn.go:241-248 validFieldValue rejects CR/LF/NUL in any field value, enforced by validateFields (conn.go:338-353) before any byte is written, so no emitted value can contain a CRLF fold. Tested http1/injection_test.go:108 HeaderValueCRLF_NotWritten and client/h1_injection_test.go:60. | Client can never emit obs-fold / multi-line field values — it refuses the bytes. |
| `4-5-24` | MUST | PASS | http1/conn.go:1111-1128 consumeHeaders detects a line starting with SP/HTAB and splices it into the current field, appending a single SP per fold (§5.2). Tested http1/conformance_obsfold_test.go:93 ObsFoldUnfoldsToSP (htab/space/many-spaces/multiple-folds). | Response obs-fold is unfolded to SP, never rejected — the user-agent rule. |
| `3-6` | RECOMMENDED | PASS | http1/conn.go:427-429 WriteRequest builds the request-line as method+" "+path+" HTTP/1.1\r\n" with no length ceiling on the send path (readBufSize caps only received lines). An 8000+ octet request-target is emitted intact. | Send path has no request-line cap, but no test exercises a ≥8000-octet request-line. <br>_verifiers overturned -> PASS: Send path builds the request-line via net.Buffers (conn.go:427-429) with no length ceiling, so any request-target is emitted intact — the client cannot fail the "support >=8000 octets" recommendation by construction (that recommendation targets recipients/parsers anyway). The 40000-octet fixtures (fuzz_test.go:79,148) exercise the RECEIVE path (status/chunk-size lines), so the judge is right that no send-side test exists, but there is nothing to fail: no cap can be regressed. Compliant by construction._ |
| `3-13` | MUST | PASS | http1/conn.go:427-430 WriteRequest unconditionally appends "Host: "+authority+"\r\n" for every request. Tested http1/conformance_test.go:236 HostHeaderInRequest. | No code path can omit Host. |
| `3-14` | MUST | PASS | http1/conn.go:429 sets Host verbatim from the :authority value; tested Host==authority (conformance_test.go:271). The client takes :authority directly (client/client.go:850-858) and never parses a URL, so no userinfo-stripping code exists. | Host equals :authority verbatim (tested); userinfo exclusion is unhandled because the client never receives a userinfo — the caller supplies a clean authority. <br>_verifiers overturned -> NOT_APPLICABLE: Host is set verbatim from :authority (conn.go:429) and validated only for CR/LF/NUL (conn.go:338). The obligation to exclude userinfo applies to deriving Host from a URI authority; this client performs NO URI parsing — :authority is supplied directly by the caller (buildHeaders client.go:850-858). There is no code path in which userinfo could arise from a URI to be stripped, so this is a caller responsibility / scope boundary, not a partial failure. NA rather than a gap._ |
| `3-15` | MUST | PASS | http1/conn.go:336-340 validateFields deliberately permits an empty authority; conn.go:429 then emits "Host: \r\n". Tested http1/injection_test.go:266 EmptyAuthorityEmptyHost (asserts "Host: \r\n"). | Missing authority yields an empty Host field value, not an omitted field. |
| `3-18` | MUST | PASS | http1/conn.go:428 emits the request-line target as :path verbatim (origin-form); authority is carried only in Host. There is no proxy/absolute-form path. Tested conformance_test.go:274 ("GET /resource HTTP/1.1"). | Direct-to-origin origin-form is the only mode; scheme+authority never appear in the request-target. |
| `3-19` | MUST | NOT_APPLICABLE | The API takes the request-target directly as :path; client/request.go:190 rejects an empty Path and http1/conn.go:306-316 validRequestTarget rejects "". There is no URI→target derivation in which an empty path could arise to be mapped. | Caller supplies the request-target; an empty path is refused rather than substituted with "/" — no URI-parsing layer exists to perform the mapping. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed: empty path is refused (request.go:190; http1 validRequestTarget conn.go:306-316 rejects ""). The "substitute /" rule is part of URI->origin-form derivation, which this client does not do — the caller supplies the request-target directly. No derivation layer exists in which an empty path could arise to be mapped. NA correct._ |
| `3-20` | MUST | NOT_APPLICABLE | No proxy configuration or absolute-form request-target code exists anywhere in client/ or http1/; the transport always emits origin-form (conn.go:428). | Proxy roles are a deliberate out-of-scope boundary; the client never talks to an HTTP proxy. <br>_verifiers overturned -> NOT_APPLICABLE: No proxy configuration or absolute-form request-target code exists in client/ or http1/; the transport always emits origin-form (:path verbatim, conn.go:428). Proxy roles are a stated scope boundary. NA correct._ |
| `3-22` | MUST | NOT_APPLICABLE | No absolute-form/proxy path exists (see 3-20); Host is nonetheless always emitted (conn.go:429). | Absolute-form is never produced, so the paired Host obligation is out of scope (proxy roles not implemented). <br>_verifiers overturned -> NOT_APPLICABLE: Absolute-form is never produced (see 3-20); Host is nonetheless always emitted (conn.go:429). The paired-Host obligation for absolute-form is out of scope because no proxy/absolute-form path exists. NA correct._ |
| `3-29` | MUST | NOT_APPLICABLE | http1 has no CONNECT tunnel establishment; Extended CONNECT is HTTP/2-only (RFC 8441, client/request.go:198). A CONNECT request-target would be whatever :path the caller sets, emitted verbatim (conn.go:428) with no host:port derivation. | No HTTP/1.1 CONNECT-tunnel client role; request-target construction for CONNECT is the caller's responsibility. <br>_verifiers overturned -> NOT_APPLICABLE: http1 has no CONNECT-tunnel establishment (no 2xx tunnel handling); Extended CONNECT is H2-only (request.go:198-201). A CONNECT request-target would be whatever :path the caller sets, emitted verbatim (conn.go:428) with no authority-form derivation. Request-target construction for CONNECT is the caller's responsibility. NA correct._ |
| `3-33` | MUST | NOT_APPLICABLE | http1/conn.go:428 emits :path verbatim; validRequestTarget (conn.go:306) permits "*", so a caller wanting server-wide OPTIONS sets Path="*" and gets "OPTIONS * HTTP/1.1". The client does not special-case or derive it. | Client passes the caller-supplied request-target through; the "*" form is the caller's to choose (and is permitted). <br>_verifiers overturned -> NOT_APPLICABLE: validRequestTarget (conn.go:306-316) permits "*" (only bytes <=0x20 and 0x7f are rejected), so a caller setting Path="*" gets "OPTIONS * HTTP/1.1". The client passes the caller-supplied target through and does not derive the asterisk-form itself. Caller's choice; NA correct._ |
| `6-6` | MUST | PASS | http1/conn.go:1313 readChunkedChunk implements the full chunked parser (size hex, ext, last-chunk, trailer section, CRLF). Tested conformance_test.go:189/212 and conformance_chunksize_test.go. | Duplicate of 7-8; recipient parses chunked. |
| `6-7` | MUST NOT | PASS | http1/conn.go:439-443 WriteRequest strips any caller-supplied "transfer-encoding" header, then conn.go:462-464 adds exactly one "Transfer-Encoding: chunked" when framing requires it — chunked cannot be listed twice. | Single-chunked by construction; no test explicitly asserts chunked is never doubled. <br>_verifiers overturned -> PASS: Chunked cannot be doubled by construction: WriteRequest strips any caller-supplied "transfer-encoding" (conn.go:439-443) and then adds exactly one "Transfer-Encoding: chunked" (conn.go:462-464) only when framing requires it. No explicit test asserts non-doubling, but the property holds structurally — there is no code path that emits it twice. No gap._ |
| `6-8` | MUST | NOT_APPLICABLE | The client never applies a non-chunked transfer-coding to a request: body compression is done via content-encoding (a representation header) in client/compress.go, not Transfer-Encoding. The only TE it ever emits is chunked alone (conn.go:462-464). | Premise (a non-chunked transfer-coding applied) never occurs, so the 'chunked must be final' obligation is vacuous. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed: the only transfer-coding the client ever applies is chunked, emitted alone (conn.go:462-464). Body compression uses content-encoding (a representation header, compress.go / compressedHeaders), never Transfer-Encoding. The premise of "chunked must be the final coding" (a non-chunked transfer-coding applied) never occurs. NA correct._ |
| `6-15` | MUST NOT | PASS | http1/conn.go:421-463 emits Transfer-Encoding: chunked for any bodied request lacking a Content-Length, before any response is seen; there is no gate on a known/remembered server HTTP/1.1 version. The client does always send an HTTP/1.1 request-line and has no 1.0 mode. | TE/chunked is emitted without confirming the peer speaks 1.1 (first request is blind); mitigated only by the client never itself downgrading to 1.0. <br>_verifiers overturned -> PASS: Confirmed the code sets reqChunked with no version gate (conn.go:421-423). But RFC 9112 §6.1's client "MUST NOT send Transfer-Encoding unless it knows the server handles HTTP/1.1" explicitly admits knowledge "in the form of specific user configuration": this client has NO HTTP/1.0 mode, always emits an HTTP/1.1 request-line (conn.go:428), and is a load generator pointed at operator-chosen endpoints — the design IS that configured knowledge. This is universal 1.1-client behaviour and carries no downgrade path. Defensible design compliance, not a genuine failing obligation._ |
| `6-19` | MUST | PASS | http1/conn.go:606-608 sets keepAlive from proto only; conn.go:1175-1232 parses Transfer-Encoding regardless of response version and chunk-decodes. HTTP/1.0 defaults keepAlive=false (closes), but a 1.0 response carrying both "Connection: keep-alive" (conn.go:1233-1239) and Transfer-Encoding would be chunk-decoded and left poolable — framing is not treated as faulty per version. | No version×TE special-case; the common 1.0 case closes, but 1.0+keep-alive+TE is chunk-decoded and reusable instead of being condemned. <br>_verifiers overturned -> PASS: Confirmed no version×TE special-case (keepAlive from proto at conn.go:606-608; TE parsed regardless of version at 1175-1232). But this is safe: chunked is self-delimiting, so once ReadBodyChunk reaches the terminal 0-chunk the socket sits at a clean boundary and pooling cannot desync, whatever the response version. RFC 9112 §6.3 framing precedence (TE overrides CL; chunked-final => chunked) is not version-gated, so applying it uniformly is within spec latitude, and no clause requires a client to reject a 1.0 response bearing Transfer-Encoding. The 1.0+keep-alive+dead-socket case merely fails on next pool use. No real desync/gap._ |
| `6-21` | MUST NOT | PASS | http1/conn.go:421-464 WriteRequest sets reqChunked only when no Content-Length is present, and emits Transfer-Encoding xor Content-Length, never both. Tested http1/conformance_reqframing_test.go:86 NoContentLengthWithTransferEncoding (all CL spellings). | Mutual exclusion enforced on the send side. |
| `6-25` | MUST | NOT_APPLICABLE | http1 has no CONNECT-tunnel path; there is no 2xx-to-CONNECT handling because the client establishes no HTTP/1.1 tunnels (Extended CONNECT is H2-only, client/request.go:198). | CONNECT-tunnel client role is a deliberate out-of-scope boundary. <br>_verifiers overturned -> NOT_APPLICABLE: http1 establishes no CONNECT tunnels — there is no 2xx-to-CONNECT handling because Extended CONNECT is H2-only (request.go:198) and the h1 path only does request-line + response framing. CONNECT-tunnel client role is a stated scope boundary. NA correct._ |
| `6-31` | MUST | PASS | http1/conn.go:806-829 parseContentLength: strict decimal parse, comma-list accepted only when every element parses and all are identical, else invalid; noteContentLength/resolveContentLength (conn.go:837-892) make an invalid CL fatal via ErrInvalidContentLength. Tested conformance_cl_test.go:69/84/101/193/215 (conflicting, differing list, non-numeric, identical dup, identical list). | Invalid Content-Length is unrecoverable except the all-identical list exception, exactly as specified. |
| `6-34` | MUST | PASS | http1/conn.go:875-887 resolveContentLength sets keepAlive=false and returns ErrInvalidContentLength on an invalid CL; h1Exchange.Recv surfaces the error so Do discards the response and release(false) closes/evicts the conn (client/h1_transport.go:60-95). Tested client/h1_cl_desync_test.go:43 PoisonedConnNotPooled. | Invalid-CL response is discarded and the connection is closed, never pooled. |
| `6-36` | MUST | PASS | http1/conn.go:1286-1298 ReadBodyChunk returns a "premature EOF: got N of M bytes" error and sets keepAlive=false when EOF arrives before Content-Length is satisfied. Tested client/large_transfer_test.go:492 cleanFIN and :512 tcpRST (both assert error + truncated body not reported as complete). | Short body → incomplete-message error and connection close, never a successful short body. |
| `6-42` | SHOULD | PASS | For a buffered Body []byte with no caller-set Content-Length, buildHeaders adds no Content-Length (client/client.go:864 managedCL only covers BodyReader+ContentLength>0), so http1/conn.go:421-423 sets reqChunked=true and the known-length body is sent chunked. The length is available as len(req.Body) but not used to frame. | A known-length buffered body is sent chunked by default instead of with Content-Length; caller can force CL only by setting the header manually. <br>_verifiers overturned -> PASS: Confirmed: for a buffered Body []byte with no caller Content-Length, buildHeaders adds none (managedCL covers only BodyReader+ContentLength>0, client.go:864), endStream is false (client.go:590), so WriteRequest sets reqChunked=true (conn.go:421-423) and the known-length body is chunk-framed. This is fully legal HTTP/1.1 — no MUST/SHOULD requires Content-Length when the length is known; chunked framing is unambiguous and safe. A behavioural/efficiency choice (caller can set Content-Length to force it), not a conformance defect._ |
| `6-43` | MUST | PASS | http1/conn.go:421-464 WriteRequest invariant: when endStream is false (a body follows), either a caller Content-Length is present or reqChunked is set — never neither. Exercised by client/h1_integration_test.go:124 (POST Body echoed via chunked) and conformance_reqframing_test.go:86. | Every bodied request carries exactly one of Content-Length or chunked. |
| `6-44` | MUST NOT | PASS | Same gate as 6-15: http1/conn.go:421-423 uses chunked for any bodied request without Content-Length regardless of any confirmed prior HTTP/1.1 response; and 6-42 shows even known-length bodies default to chunked. No buffer-to-compute-CL fallback for an unknown peer. | Chunked request bodies are emitted without confirming the server handles 1.1. <br>_verifiers overturned -> PASS: Union of 6-15 and 6-42; refuted for the same reasons. Emitting chunked for a bodied request without confirming a prior 1.1 response is covered by RFC 9112 §6.1's "specific user configuration" knowledge (this client is 1.1-only, always sends an HTTP/1.1 request-line, no downgrade path, conn.go:428), and chunk-framing a known-length body (conn.go:421-423) is legal HTTP/1.1. No genuine gap._ |
| `6-46` | MUST NOT | PASS | One Exchange per request (http1/conn.go:184); a status that forbids a body but declares one, a TE+CL response, an oversize line, or a chunk/CL desync all set keepAlive=false (conn.go:646-697, 668-697) so the conn is evicted rather than reused, and leftover bytes are never parsed as a new response. Tested conformance_bodyless_test.go:47 BodylessStatusDeclaringBodyNotPooled. | Extra/leftover bytes evict the connection instead of being read as a separate response. |

### 2-recv-fields

_37 items: PASS=17, PARTIAL=1, NOT_APPLICABLE=19_

| Item | Level | Status | Evidence | Note |
|------|-------|--------|----------|------|
| `1-2-14` | MUST | PASS | http1/conn.go readLine (l.529) uses bufio ReadSlice('\n') over raw []byte; folding is ASCII-only via asciiEqualFold (l.714) and asciiLowerHeaderName (l.741), deliberately NOT strings.ToLower to avoid Unicode re-encoding (doc l.730-740). Values never charset-decoded. Test: TestConformance_RFC9110_Sec5_5_LegalResponseFieldsAccepted value_obs_text/value_high_bit (conformance_respfield_test.go:54-55). | Parsing is octet-based; high-bit/obs-text bytes pass through unchanged, proving no Unicode decode before field delineation. |
| `1-2-16` | MUST NOT | PASS | validateFields (conn.go:326) rejects CR everywhere it could reach a protocol element: validFieldValue (l.241) rejects '\r' in values/authority, validToken (l.282) forbids CR in names, validRequestTarget (l.306) rejects bytes <=0x20 in method/target. Nothing written on failure (net.Buffers built after validation, l.415). Test: TestConformance_RFC9110_Sec5_5_HeaderValueCRLF_NotWritten / _AuthorityCRLF_NotWritten / RFC9112_Sec3_RequestLine_NotWritten (injection_test.go). | Only WriteBody (l.480) passes body bytes verbatim, so a lone CR is permitted only in content, as required. |
| `1-2-17` | MUST | PASS | commitHeaderLine (conn.go:1164) rejects a response field value containing bare CR via validFieldValue, sets keepAlive=false, returns ErrInvalidHeaderBlock. Status-line bare CR causes strconv.Atoi/prefix parse failure (l.581-588). Test: TestConformance_RFC9110_Sec5_5_ResponseFieldValueCRNUL_Rejected bare_CR_in_value (conformance_respfield_test.go:27). | Client rejects (one of the two allowed remedies); reason-phrase is discarded entirely so no bare CR is passed through untouched. |
| `1-2-18` | MUST NOT | PASS | WriteRequest (conn.go:426-467) builds net.Buffers starting with the request line (no leading CRLF) and ends the head with a single crlf blank line; WriteBody (l.480) appends only body/chunk framing, no trailing CRLF beyond the chunked terminator. Test asserts strings.HasPrefix(wire,"GET / HTTP/1.1\r\n") in TestConformance_RFC9112_Sec3_2_EmptyAuthorityEmptyHost (injection_test.go:282). | No extra CRLF prefaces the request; trailing framing is exactly the declared body/chunk terminator, not an extra CRLF. |
| `1-2-19` | MUST | PASS | For chunked, WriteBody sizes each chunk as strconv.FormatInt(len(p),16) (conn.go:491) so any CRLF inside p is counted; for Content-Length framing the caller's declared length governs and the client writes p directly (l.510) appending nothing. No CRLF is ever added outside the declared length. | Correct by construction — client never appends line-endings outside the counted body — but no test drives a caller body ending in CRLF. <br>_verifiers overturned -> UNTESTED: Compliance is structural: chunked path sizes each chunk as strconv.FormatInt(int64(len(p)),16) (conn.go:491) so any CRLF inside p is inside the counted length; non-chunked path writes p verbatim under the caller's declared Content-Length (conn.go:510) and appends nothing. WriteRequest/WriteBody have no code that emits line-endings outside the counted body. The obligation (never inject control/line data outside the declared body) is met by construction, so the absence of a CRLF-in-body test is immaterial — not a genuine deficiency. Judge's UNTESTED label is accurate (no dedicated test drives a CRLF-ending body among conn_test.go WriteBody tests), but there is no real gap._ |
| `1-2-21` | MUST NOT | PASS | WriteRequest emits "Host: "+authority immediately after the request-line CRLF (conn.go:427-430) and each field as lower+": "+value (l.444) with no leading SP/HTAB. First field is always Host. Test: TestConformance_RFC9112_Sec3_2_EmptyAuthorityEmptyHost checks the head has no whitespace before Host. | No whitespace between start-line and first field line. |
| `1-2-22` | MUST | PASS | consumeHeaders (conn.go:1111-1118): a line beginning with SP/HTAB when haveField is false (i.e. directly after the status line) is refused with ErrInvalidHeaderBlock and keepAlive=false. Test: TestConformance_RFC9112_Sec5_2_ObsFoldWithNoPrecedingField (conformance_obsfold_test.go:125) drives "HTTP/1.1 200 OK\r\n Content-Length: 5..." and asserts rejection + KeepAlive()==false. | Client takes the 'reject the message as invalid' option — a valid §1.2 remedy — closing the response-splitting vector. |
| `6-3` | MUST | PASS | Exchange retains the request method: WriteRequest stores ex.method (conn.go:418); ReadBodyChunk short-circuits HEAD to (0,true,nil) (l.1261) and 204/304 to (0,true,nil) (l.1266) regardless of Content-Length. Test: conformance_bodyless_test.go:94 drives a HEAD response with Content-Length: 38 and asserts the body ends immediately. | Request method/status are associated with the response so HEAD/204/304 body rules apply correctly. |
| `6-10` | SHOULD | PASS | The H1 transport always serializes "HTTP/1.1" in the request line (conn.go:428). Asserted by requestHead-based tests, e.g. TestConformance_RFC9110_Sec5_5_LegalValuesUnaffected expects "GET / HTTP/1.1\r\n" (injection_test.go:305). | Highest version the H1 stack conforms to; major 1 is what an HTTP/1 server supports. |
| `6-11` | MUST NOT | PASS | WriteRequest hardcodes the version token "HTTP/1.1" (conn.go:428); no code path emits HTTP/1.2 or HTTP/2.0 on the H1 wire. H2/H3 are separate stacks with their own framing. | The only version string the H1 serializer can produce is one it fully implements. |
| `6-16` | SHOULD | PARTIAL | ReadResponse accepts any "HTTP/1"-prefixed status line (conn.go:581) so it does not error on a higher minor version, but persistence is set as ex.keepAlive = proto == "HTTP/1.1" (l.608), so an "HTTP/1.2" response with no Connection header is treated as non-persistent (close) instead of being processed as HTTP/1.1 (persistent default). No test drives a >1.1 minor version. | Gap: higher minor versions are accepted but wrongly default to connection-close; §6-16 says process as the highest conformant minor (HTTP/1.1 → persistent). <br>_CONFIRMED by both verifiers_ |
| `6-33` | MUST NOT | NOT_APPLICABLE | HTTP/1.1 request trailers are not supported: h1Exchange.SendHeaders returns ErrTrailersUnsupportedH1 (client/h1_transport.go:45-46); the http1 package exposes no request-trailer API. Test: client/h1_integration_test.go:546 asserts a trailer-carrying request over H1 is rejected. | Deliberate scope boundary — the H1 client never generates trailer fields, so the 'permitted-in-trailers' obligation cannot be violated. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. H1 send side generates no request trailers: h1Exchange.SendHeaders returns ErrTrailersUnsupportedH1 (client/h1_transport.go:46) and the http1 package exposes no request-trailer write API — WriteRequest emits only caller fields plus Host/Content-Length/Transfer-Encoding (conn.go:427-464). Rejection is tested at client/h1_integration_test.go:544-548 (Do with Trailers returns ErrTrailersUnsupportedH1). The 'permitted-in-trailers' obligation cannot be violated when no trailers are generated._ |
| `6-34` | MUST NOT | PASS | Received chunked trailers are consumed with consumeHeaders(nil,false) (conn.go:1348) — out is nil, so trailer fields are parsed for framing/termination only and never appended to the response header slice nor merged. The H1 transport exposes no trailers to the caller. | Client satisfies the MUST trivially: it never merges (in fact never surfaces) H1 response trailers into the header section. |
| `6-38` | MUST | NOT_APPLICABLE | Send side: H1 generates no trailers (ErrTrailersUnsupportedH1, client/h1_transport.go:46). Receive side: trailers are discarded via consumeHeaders(nil,false) (conn.go:1348) and never interpreted as list vs singleton. | Deliberate scope boundary — no H1 trailer generation and no trailer interpretation on receive. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. Send side generates no trailers (ErrTrailersUnsupportedH1, h1_transport.go:46). Receive side discards trailers via consumeHeaders(nil,false) at conn.go:1348 — out=nil means trailer field lines are parsed only for framing termination, never appended anywhere or interpreted as list-vs-singleton. No trailer semantics are surfaced, so the list/singleton obligation does not arise._ |
| `6-41` | SHOULD | NOT_APPLICABLE | The client generates no Date header on requests: WriteRequest emits only the caller's fields plus Host/Content-Length/Transfer-Encoding (conn.go:427-464); no clock is sampled and no Date is injected. No time.Parse/date formatting in http1 or the request builder. | Client never generates Date, so the best-approximation obligation does not arise; any Date is caller-supplied verbatim. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. Client generates no Date: WriteRequest emits only caller fields plus Host/Content-Length/Transfer-Encoding (conn.go:427-464); grep for 'date'/http-date/time formatting in non-test source finds none (all time.Now() calls in client/ are pool/timing bookkeeping, e.g. h1_pool.go, not header synthesis). Any Date is caller-supplied verbatim (conn.go:444), so the best-approximation generation obligation never fires._ |
| `6-45` | MUST | NOT_APPLICABLE | This is a load-generation client with no cache and no downstream forwarding (CLAUDE.md scope: caching is a deliberate non-goal). No receipt-time stamping or Date synthesis exists in client/response.go or http1/conn.go. | Obligation is scoped to caches/proxies; deliberate scope boundary — the client neither caches nor forwards responses. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. Obligation is scoped to caches/proxies stamping receipt time / synthesizing Date. This is a load-gen user agent — no cache, no forwarding (CLAUDE.md non-goal), and no receipt-time or Date synthesis exists in client/response.go or http1/conn.go. Deliberate scope boundary confirmed by absence of any date generation in source._ |
| `6-49` | SHOULD | NOT_APPLICABLE | H1 does not send request trailers (ErrTrailersUnsupportedH1, client/h1_transport.go:46), so there is never an intent to generate trailer fields that would require a Trailer header. | Deliberate scope boundary — no H1 trailer generation. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. The Trailer header announces intent to send trailer fields; H1 sends no request trailers (ErrTrailersUnsupportedH1, h1_transport.go:46) and WriteRequest never emits a Trailer field. With no trailer generation there is no intent that would require a Trailer header. Deliberate scope boundary._ |
| `5-5` | SHOULD | PASS | commitHeaderLine appends every well-formed field to *out unconditionally (conn.go:1243-1248); only the framing switch (l.1170) special-cases known names, and colon-less lines are skipped not errored (l.1148). Unknown names are exposed opaquely. Numerous tests send arbitrary X-Junk/X-A headers and succeed (e.g. conformance_respfield_test.go, conformance_obsfold_test.go). | Unrecognized headers are neither rejected nor specially handled; passed through to the caller. |
| `5-9` | MUST NOT | NOT_APPLICABLE | Field-set composition is the caller's responsibility via the H2-style API; WriteRequest emits caller fields verbatim (conn.go:432-445) without deduping arbitrary names. The one duplicate the client itself could generate — Content-Length — is guarded (l.399, l.456) and tested by TestConformance_RFC9112_Sec6_1_SingleContentLengthOnBodylessPost (conformance_reqframing_test.go:116). | Caller owns singleton-vs-list header composition; the client only guarantees it does not itself emit duplicate framing headers (tested). <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. Singleton-vs-list composition is the caller's via the H2-style API; WriteRequest emits caller fields verbatim (conn.go:432-445) with no arbitrary-name dedup. The only framing header the client could itself duplicate — Content-Length — is guarded case-insensitively (conn.go:399) and not re-appended when caller-supplied (conn.go:456), tested by TestConformance_RFC9112_Sec6_1_SingleContentLengthOnBodylessPost. Client guarantees only that it does not itself emit duplicate framing headers._ |
| `5-18` | MUST | PASS | commitHeaderLine trims OWS before evaluating: value := strings.TrimSpace(line[colon+1:]) and name := asciiLowerHeaderName(strings.TrimSpace(line[:colon])) (conn.go:1151-1152). Test: TestConformance_RFC9112_Sec5_2_ObsFoldUnfoldsToSP and _ObsFoldDoesNotSmuggleContentLength assert x-junk == "a Content-Length: 5" (leading OWS after the colon trimmed), conformance_obsfold_test.go:60. | Leading/trailing OWS stripped from both name and value prior to interpretation and storage. |
| `5-20` | SHOULD | PASS | validFieldValue rejects only CR/LF/NUL (conn.go:242-246); 0x80-0xFF pass. asciiLowerHeaderName touches only A-Z (l.755). Values are never charset-decoded. Test: TestConformance_RFC9110_Sec5_5_LegalResponseFieldsAccepted value_obs_text (\xc3\xa9) and value_high_bit (\x80\xff) accepted (conformance_respfield_test.go:54-55). | obs-text octets are treated as opaque bytes and passed through unchanged. |
| `5-21` | MUST | PASS | commitHeaderLine (conn.go:1164) rejects a response field whose value contains CR/LF/NUL via validFieldValue, condemns the connection (keepAlive=false) and returns ErrInvalidHeaderBlock. LF cannot occur mid-value (it terminates the line). Test: TestConformance_RFC9110_Sec5_5_ResponseFieldValueCRNUL_Rejected NUL_in_value/bare_CR_in_value (conformance_respfield_test.go:24). | Client takes the 'reject the message' remedy for CR/LF/NUL in received field values — the response-splitting defense. |
| `5-25` | MUST NOT | NOT_APPLICABLE | The client constructs no list-based header fields — it emits caller-supplied values verbatim (conn.go:444) and generates only Host, Content-Length and Transfer-Encoding: chunked, none produced by joining list members. | Deliberate scope boundary — list-field serialization (and thus empty-element avoidance) is the caller's responsibility. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. The client constructs no list-based field values by joining members — it writes name+": "+value verbatim (conn.go:444) and self-generates only Host, Content-Length and 'Transfer-Encoding: chunked' (conn.go:429,459,463), none produced by list-member concatenation. Empty-list-element avoidance is thus the caller's concern._ |
| `5-26` | MUST | PASS | For the one list-based field the client parses (Transfer-Encoding), lastTransferCoding skips empty list elements (transferCodingName returns "" which is not recorded, conn.go:948-952) citing RFC 9110 §5.6.1, and the whole block is bounded by maxHeaderListBytes (l.1088-1093). Test: TestConformance_RFC9112_Sec7_ChunkedFinalAfterParameters exercises a trailing empty element (conformance_te_grammar_test.go). | Empty list elements in the parsed Transfer-Encoding list are ignored, bounded by the 8 MiB header cap; other list fields are opaque to the caller. |
| `5-27` | MUST | PASS | lastTransferCoding implements the lenient recipient grammar: it scans comma-separated elements tracking quoted-string state, records only non-empty codings, and the final non-empty coding decides framing (conn.go:942-971). Test: TestConformance_RFC9112_Sec7_ChunkedFinalAfterParameters covers a trailing empty element still resolving to chunked. | Only non-empty members count toward the final-coding determination; matches [ element ] *( OWS ',' OWS [ element ] ). |
| `5-30` | SHOULD | NOT_APPLICABLE | The client does not reformat or re-serialize field values; it writes name+": "+value verbatim (conn.go:444), using a single SP after the colon. It never emits OWS at list-comma positions because it does not construct list values. | Deliberate scope boundary — customary OWS placement inside list values is the caller's concern; the client's own separator is a single SP. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. No re-serialization of field values: values are written verbatim after a single 'SP' post-colon separator (conn.go:444, name+": "+value). The client never constructs list values, so it never places OWS at list-comma positions. Customary OWS placement is the caller's concern._ |
| `5-31` | SHOULD NOT | NOT_APPLICABLE | The client generates no gratuitous OWS: header lines are name+": "+value (conn.go:444) with no in-place filtering that would insert whitespace. Values are caller-controlled. | Deliberate scope boundary — the client neither adds nor rewrites OWS in field values. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. No gratuitous OWS generation: header lines are name+": "+value (conn.go:444) with no whitespace-inserting rewrite of caller values. The client neither adds nor rewrites OWS in field values._ |
| `5-32` | SHOULD | NOT_APPLICABLE | The client generates no RWS-bearing constructed field values; the request-line uses exactly single-SP token separators (method+" "+path+" HTTP/1.1", conn.go:428). | Deliberate scope boundary — no field values with required inter-token whitespace are synthesized; the request line already uses single SP. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. No RWS-bearing constructed field values are synthesized; caller values are verbatim (conn.go:444). The request-line uses exactly single-SP token separators: method+" "+path+" HTTP/1.1" (conn.go:428). No required inter-token whitespace is generated by the client._ |
| `5-34` | MUST NOT | NOT_APPLICABLE | The client constructs no grammar with BWS positions (no transfer/auth parameters are generated); it emits caller values verbatim (conn.go:444) and its own headers (Host, Content-Length, Transfer-Encoding: chunked) contain no BWS. | Deliberate scope boundary — the client emits no BWS in messages it generates. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. The client generates no grammar with BWS positions — no transfer/auth parameters are produced. It emits caller values verbatim (conn.go:444); its own headers (Host, Content-Length, 'Transfer-Encoding: chunked') contain no BWS. Deliberate scope boundary._ |
| `5-35` | MUST | NOT_APPLICABLE | The only BWS positions are around '=' in transfer/auth parameters; the client ignores transfer-parameters entirely — transferCodingName strips everything from the first ';' (conn.go:979-983) — and does not interpret auth params (passed opaque to caller). | Deliberate scope boundary — the client never interprets a BWS-bearing sub-grammar, so there is no BWS to strip before evaluation. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. BWS appears only around '=' in transfer/auth parameters. The client ignores transfer-parameters entirely — transferCodingName strips from the first ';' (conn.go:979-983) — and does not interpret auth params (passed opaque to caller). No BWS-bearing sub-grammar is evaluated, so there is no BWS to strip._ |
| `5-39` | MUST | NOT_APPLICABLE | The client exposes received field values opaquely and decodes no quoted-strings for the caller. lastTransferCoding tracks quoted-string/quoted-pair state only to find real comma delimiters (conn.go:954-963); it never unescapes or surfaces the decoded string. | Deliberate scope boundary — quoted-string value interpretation is the caller's; the client only needs comma-boundary correctness, which it has. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. Received values are surfaced opaquely; no quoted-string decoding for the caller. lastTransferCoding tracks quoted-string/quoted-pair state (conn.go:954-963) only to locate real comma delimiters — it never unescapes or exposes a decoded string (only the bare coding name via transferCodingName). Quoted-string interpretation is the caller's; the client needs only comma-boundary correctness, which it has and tests (conformance_te_grammar/te_unterminated)._ |
| `5-41` | SHOULD NOT | NOT_APPLICABLE | The client generates no quoted-strings — it emits caller header values verbatim (conn.go:444) and constructs no field value requiring quoting. | Deliberate scope boundary — no quoted-string generation. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. No quoted-string generation — caller values are written verbatim (conn.go:444) and no self-generated field value requires quoting. Deliberate scope boundary._ |
| `5-42` | SHOULD NOT | NOT_APPLICABLE | The client generates no comments (no User-Agent or other comment-bearing field is synthesized; values are caller-supplied verbatim, conn.go:444). | Deliberate scope boundary — no comment generation. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. No comment generation — the client synthesizes no User-Agent or other comment-bearing field (WriteRequest emits only caller fields + Host/CL/TE, conn.go:427-464); values are caller-supplied verbatim. Deliberate scope boundary._ |
| `5-50` | MUST | NOT_APPLICABLE | The client parses no HTTP-date fields: there is no time.Parse / http-date handling in http1/conn.go or client/response.go; Date/Expires/Last-Modified/Retry-After are exposed to the caller as opaque byte values. (Only time.ParseDuration for config, client/soak_test.go.) | Deliberate scope boundary — no caching/conditional logic means the client interprets no timestamps. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. No HTTP-date parsing: no time.Parse/http-date handling in http1/conn.go or client/response.go (grep confirms none in non-test source). Date/Expires/Last-Modified/Retry-After are exposed as opaque byte values. No caching/conditional logic means no timestamp interpretation._ |
| `5-51` | MUST | NOT_APPLICABLE | The client generates no HTTP-date timestamps; If-Modified-Since and similar are caller-supplied header values written verbatim (conn.go:444). No date formatting code exists in the request path. | Deliberate scope boundary — no date generation, so IMF-fixdate formatting does not arise. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. No HTTP-date generation: If-Modified-Since and similar are caller-supplied header values written verbatim (conn.go:444); no date-formatting code exists in the request path (no IMF-fixdate/rfc1123 formatting in source). Deliberate scope boundary._ |
| `5-57` | MUST NOT | NOT_APPLICABLE | The client emits no HTTP-date it constructs; date values are caller-supplied verbatim (conn.go:444). No date serializer exists. | Deliberate scope boundary — no generated HTTP-date to add whitespace to. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. The client emits no HTTP-date it constructs; date values are caller-supplied verbatim (conn.go:444) and no date serializer exists in source. No generated HTTP-date to which whitespace rules could apply._ |
| `5-58` | MUST | NOT_APPLICABLE | The client parses no rfc850 dates (no HTTP-date parsing anywhere in http1/client), so no two-digit-year 50-year-window resolution is needed. | Deliberate scope boundary — timestamps are opaque to this client. <br>_verifiers overturned -> NOT_APPLICABLE: Correct NA. No rfc850 date parsing anywhere in http1/client (no http-date parsing at all), so the two-digit-year 50-year-window resolution never arises. Timestamps are opaque to this client._ |

### 3-connection

_24 items: PASS=9, PARTIAL=1, FAIL=3, NOT_APPLICABLE=8, REVIEW=3_

| Item | Level | Status | Evidence | Note |
|------|-------|--------|----------|------|
| `8-9-2` | MUST | PASS | http1/conn.go:1287-1290 ReadBodyChunk returns a non-nil error ('premature EOF: got N of M bytes') when a Content-Length body EOFs early; chunked decode failures return typed errors (ErrInvalidChunkSize conn.go:1341, ErrInvalidHeaderBlock, unterminated-TE). h1Exchange.Recv (client/h1_transport.go:77-80) drops the partial event and propagates the error, so Do returns err!=nil and never hands back a truncated body as complete. Tested: http1/conformance_chunksize_test.go TestConformance_RFC9112_Sec7_1_InvalidChunkSizeRejected, http1/conformance_trailer_deadline_test.go:97 TestTrailerSectionEOF_StillCompletesButNotPoolable. | Incompleteness is signalled by a non-nil error (stronger than a flag) rather than an explicit Response.Incomplete field; chunked-decode-failure paths are well tested, the raw CL-truncation error path is code-correct but has no dedicated test. |
| `8-9-11` | MUST | NOT_APPLICABLE | http1/conn.go:135-137 Conn doc + client/h1_pool.go:9-21 pool doc: exactly one exchange per connection at a time, pipelining deliberately not implemented (http1ConnStreamCap=1, h1_pool.go:49). The exclusive-checkout pool never has >1 outstanding request on a connection. | Deliberate scope boundary: with no pipelining a connection never carries more than one outstanding request, so the ordered-list obligation is vacuous. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed vacuous. h1ConnStreamCap=1 (client/h1_pool.go:49) and Conn doc 'At most one Exchange at a time (no pipelining)' (http1/conn.go:135-137); pool takes a conn OUT of the idle set for the exclusive exchange duration (h1_pool.go:9-21,341-352). A connection never carries an ordered list of >1 outstanding request, so the ordered-response obligation cannot arise. NA is correct._ |
| `8-9-12` | MUST | PASS | With one outstanding request per conn the association is trivial and correct. 1xx interim responses are drained and NOT treated as final: http1/conn.go:590-604 loops on code<200 (consumeHeaders discard) until a >=200 status, so no interim pops the exchange. Tested: http1/conn_test.go:536 TestHTTP1_1xx_Response and client/interim_test.go TestConformance_RFC7540_Sec8_1_InterimResponse_FinalStatusWins. | FIFO-queue dimension is N/A (no pipelining); the load-bearing semantics — a 1xx is not the final response, the final non-1xx is associated to the request — are implemented and tested. |
| `8-9-13` | MUST NOT | REVIEW | The client only reads a socket inside an exchange (ReadResponse after WriteRequest, http1/conn.go:549). Idle pooled conns are never read: h1Pool.pickIdle/evictDead gate on http1.Conn.IsAlive (conn.go:154-156) which only checks the closed atomic bool and does NOT poll the socket. So idle-arriving bytes are never parsed as a standalone response — but they are also never detected, so a reused conn's next ReadResponse would consume them as its own status line. | No literal violation (no idle reader treats data as a response at idle), but the lack of idle-conn readable detection means unsolicited bytes could be mis-consumed by the next request; the real gap is 8-9-14/8-9-43. <br>_verifiers split -- real_gap=False:Judge's own evidence concedes 'No literal violation'. The client only reads inside an exchange (ReadResponse after WriteRequest); it never reads an idle socket and so never parses idle-arriving bytes as a standalone response. On reuse, if garbage were present the next ReadResponse fails ('malformed status line', http1/conn.go:581-583) -> exchange errors -> release(false) discards the conn (h1_transport.go:106) -> idempotent retry. The literal obligation (do not treat idle data as a response) is met by the read-only-within-exchange design; the latent proactive-detection concern belongs to 8-9-14/43, not here. \|\| real_gap=True:Genuine partial gap. No idle-connection reader exists; IsAlive (http1/conn.go:154-156) only reads the closed atomic bool. On conn reuse ReadResponse (conn.go:549) reads whatever is on the socket as the status line, so well-formed unsolicited bytes that arrived while idle would be attributed to request N+1 as a valid response. The checklist's mandated mitigation ('mark the connection unusable') is absent. Same root cause as 8-9-14/43; no literal self-violation but the protective mechanism is missing._ |
| `8-9-14` | SHOULD | FAIL | No idle-connection reader exists anywhere in client/h1_pool.go: idle conns sit in rs.conns and are only revalidated via IsAlive() (which does not read the socket) on tick/acquire. There is no path that discards bare CRLFs or closes the connection on other unsolicited bytes. | SHOULD-level gap: the client does not monitor idle pooled conns, so it cannot discard CRLF-only noise or evict-on-unsolicited-data; a deliberate simplification (no reader goroutine on idle H1 conns) shared with many production clients. <br>_CONFIRMED by both verifiers_ |
| `8-9-15` | SHOULD | PASS | Keep-alive/reuse is the default: h1Pool exclusive-checkout returns live conns to the idle set on release when http1.Exchange.KeepAlive() is true (client/h1_pool.go:235-244), and pickIdle reuses them (h1_pool.go:341-352). Tested: client/h1_pool_test.go:430 TestH1Pool_KeepAlive_ReusesConn, client/h1_pool_transport_test.go:33 TestH1PoolTransport_KeepAlive_TwoRequestsReuseOneConn, http1/conn_test.go:256 TestHTTP1_KeepAlive_TwoRequests. | Persistent connections are the default mode, not close-per-request. |
| `8-9-21` | MUST | NOT_APPLICABLE | The client supports persistent connections (8-9-15), so the antecedent ('a client that does not support persistent connections') is false. WriteRequest never emits a Connection header at all (http1/conn.go:439 drops 'connection'). | Antecedent false: this client is persistent-by-default, so it is not obligated to send 'close' on every request; there is no non-persistent/one-shot mode. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed antecedent-false. The client is persistent-by-default: ReadResponse sets keepAlive = (proto=='HTTP/1.1') (http1/conn.go:607-608) and there is no non-persistent/one-shot mode. WriteRequest never emits a Connection header (conn.go:439). A persistent client is not obligated to send 'close' on every request. NA correct._ |
| `8-9-26` | MUST | PASS | h1Exchange releases with keepAlive=true only when the body is fully read: Recv calls e.release(e.ex.KeepAlive()) only on the EndStream chunk (client/h1_transport.go:93-95). If the caller abandons the body, Close forces e.release(false) (h1_transport.go:100-107), and h1Pool.handleRelease (h1_pool.go:238-242) evicts+closes any conn released with keepAlive=false. Buffered Do fully drains via drainResponse (client/client.go:679) before release. Tested: client/h1_pool_test.go:430 TestH1Pool_KeepAlive_ReusesConn (reuse only after full read). | A conn is returned to the pool only after the response body is fully consumed; an abandoned/partly-read body is discarded, never reused. |
| `8-9-33` | SHOULD | NOT_APPLICABLE | No pipelining (http1/conn.go:135-137, client/h1_pool.go:9-21). Idempotent transport-failure retry does exist generally (client/retry.go isIdempotent + doLoop), but the pipelining-specific 'requeue the unanswered tail' obligation cannot arise. | Deliberate scope boundary: with at most one in-flight request per conn there is never an unanswered pipelined tail to retry. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No pipelining (h1_pool.go:9-21,49), so there is never an unanswered pipelined tail to requeue. General idempotent transport-failure retry does exist (client/retry.go:19-32 isIdempotent, 275-307 doLoop, builtinShouldRetry 38-90) but the pipelining-specific tail-requeue obligation cannot arise. NA correct._ |
| `8-9-34` | MUST NOT | NOT_APPLICABLE | No pipelining (client/h1_pool.go:9-21); a retry always opens a single fresh exchange (h1PoolTransport.openExchange, client/h1_pool_transport.go:27) and issues exactly one request, waiting for its response. | Deliberate scope boundary: the client never pipelines, so it cannot pipeline immediately after a retry connection either. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. A retry opens a single fresh exchange and issues exactly one request awaiting its response (h1_pool_transport.go:27-41 openExchange; one exchange per checked-out conn). The client never pipelines, so it cannot pipeline immediately after a retry. NA correct._ |
| `8-9-35` | SHOULD NOT | NOT_APPLICABLE | No pipelining (client/h1_pool.go:9-21); the pool serialises one exchange per conn, so a non-idempotent method's request is always awaited to completion before any next request on that conn. | Deliberate scope boundary: with no pipelining there is no queue to stall behind a POST; the behaviour is satisfied by construction. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. Exclusive checkout serialises one exchange per conn (h1_pool.go:341-352 pickIdle, cap 1). A non-idempotent request is always awaited to completion before the conn is released and reused; there is no queue that could stall behind a POST. Satisfied by construction. NA correct._ |
| `8-9-42` | SHOULD | PASS | Idle-reaper closes via evictIdle → mc.c.Close() → http1.Conn.Close → nc.Close() (client/h1_pool.go:438-453, http1/conn.go:159-162), a normal FIN (and TLS close_notify for *tls.Conn), not an RST/abort. Tested: client/h1_pool_test.go:494 TestH1Pool_IdleEviction. | Timeout-driven eviction uses net.Conn.Close (graceful FIN); the eviction is tested though the graceful-vs-RST byte-level distinction is not separately asserted. |
| `8-9-43` | SHOULD | PARTIAL | No proactive monitor on idle H1 conns: health tick calls evictDead→IsAlive (client/h1_pool.go:456-467) and IsAlive only reads the closed atomic bool (http1/conn.go:154-156), never the socket, so a peer FIN on an idle conn is not detected before reuse. A dead conn is discovered only on next use: the exchange errors, h1_transport.go release(false) discards it, and idempotent requests retry (retry.go builtinShouldRetry). | Partially satisfied — stale conns are eventually evicted and recovered-from on next use, but there is no constant/readable-event monitoring of idle connections for a closure signal as the SHOULD asks. <br>_CONFIRMED by both verifiers_ |
| `8-9-46` | SHOULD | FAIL | The h1Exchange state machine writes the full request body before reading any response: SendData→WriteBody (client/h1_transport.go:53-55, http1/conn.go:480-512) completes, then Recv→ReadResponse (h1_transport.go:60-70) reads. There is no concurrent reader on the connection during body transmission. | SHOULD-level gap: an early error response sent by the server while the client is still uploading is not noticed until the upload finishes; a deliberate single-goroutine simplification. <br>_CONFIRMED by both verifiers_ |
| `8-9-47` | SHOULD | FAIL | Consequence of 8-9-46: because no response is read during upload (client/h1_transport.go SendData precedes Recv), there is no path that detects an early final response + Connection: close mid-upload and aborts WriteBody; the body write always runs to completion. | SHOULD-level gap: the client cannot cease transmitting on an early server rejection because it does not read the connection while transmitting. <br>_CONFIRMED by both verifiers_ |
| `8-9-49` | SHOULD | REVIEW | WriteRequest unconditionally drops any Connection header (http1/conn.go:439-443 skip list includes 'connection') and never adds one of its own; there is no code path anywhere in client/ or http1/ that emits 'Connection: close' when the client decides a connection is its last. Close decisions are made locally (idle eviction, keepAlive=false eviction) with no in-flight request to carry the signal. | SHOULD-level gap: the client never advertises close intent to the server; it relies on server-driven close (honoured, 8-9-58) and silent local eviction. It also silently strips a caller-supplied Connection: close rather than honouring it. <br>_verifiers split -- real_gap=False:Refuted as a hard FAIL. Two prongs: (a) 'advertise close intent' — the client decides close only BETWEEN requests (idle/keepAlive=false eviction, h1_pool.go:238-242), never within an in-flight request, so the SHOULD's precondition (sending a request on a conn it wishes to close) never fires; the close is instead effected by tearing down the socket (Conn.Close, http1/conn.go:159-162; FIN + TLS close_notify), a valid way to end a persistent connection. (b) stripping a caller-supplied Connection: close (conn.go:439) is correct H2-pseudo-header->H1 hop-by-hop translation (Connection is forbidden in the H2-style API and managed by the client, mirroring net/http). Goal achieved by teardown; in-band signal deliberately not emitted -> PARTIAL, not a genuine conformance failure. \|\| real_gap=True:Confirmed genuine. WriteRequest unconditionally drops any Connection header (conn.go:437-443 skip list) and never adds one; grep confirms the only 'Connection: close' handling in client/ is server-driven via KeepAlive(). Close decisions are out-of-band (idle/keepAlive eviction) with no coincident request to carry the signal, and a caller-supplied Connection: close is silently stripped and never acted on (no request path sets keepAlive=false). The SHOULD to advertise close intent is fully unmet on the one path where it applies._ |
| `8-9-51` | MUST NOT | NOT_APPLICABLE | The client never sends a 'close' connection option (WriteRequest drops the Connection header, http1/conn.go:439), so the antecedent 'a client that sends a close connection option' is never true. | Vacuously satisfied: with no close ever emitted there is no close-flagged connection on which a further request could be scheduled. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed antecedent-false. WriteRequest drops the Connection header and never adds one (http1/conn.go:439); the client never sends a 'close' connection option, so there is no close-flagged connection on which a further request could be scheduled. NA correct._ |
| `8-9-52` | MUST | NOT_APPLICABLE | Same as 8-9-51: the client never emits a 'close' connection option (http1/conn.go:439), so there is no client-sent close after which it must close the connection. | Antecedent never fires; the client-initiated-close obligation does not apply. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed antecedent-false. Same basis as 8-9-51: no client-sent 'close' option (http1/conn.go:439), so the client-initiated-close-then-MUST-close obligation never fires. Note the client does honour a SERVER-sent close (commitHeaderLine sets keepAlive=false on Connection: close, conn.go:1233-1239) and evicts such conns (h1_pool.go:238-239). NA correct for the client-sent case._ |
| `8-9-58` | MUST | PASS | A response 'Connection: close' sets ex.keepAlive=false (http1/conn.go:1233-1239). h1Exchange.Recv releases with KeepAlive()=false (client/h1_transport.go:94), and h1Pool.handleRelease evicts+closes the conn (h1_pool.go:238-239), so no further request is scheduled on it and it is dropped from the pool. Tested: http1/conn_test.go:626 TestHTTP1_ConnectionClose, client/h1_pool_transport_test.go:59 TestH1PoolTransport_ConnectionClose_DiscardsAndRedials, client/h1_pool_test.go:452 TestH1Pool_NotKeepAlive_DiscardsConn. | On a close-bearing response the client finishes reading it, then closes and evicts the connection; no reuse. |
| `8-9-59` | SHOULD NOT | NOT_APPLICABLE | No pipelining (client/h1_pool.go:9-21): there are never additional pipelined requests queued behind a response, so the client never has to reason about their processing after a 'close'. | Deliberate scope boundary: with one in-flight request per conn there is no pipelined tail whose success could be wrongly assumed. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No pipelining (h1_pool.go:9-21,49): there is never a pipelined tail queued behind a response whose processing could be wrongly assumed after a 'close'. Vacuous by construction. NA correct._ |
| `8-9-64` | MUST | PASS | All HTTP bytes are written through the dialed net.Conn (http1/conn.go:473 bufs.WriteTo(ex.c.nc), :510 nc.Write); for TLS that net.Conn is a *tls.Conn, so every byte goes through the TLS record layer as application data (no separate framing). Keep-alive/pooling is transport-agnostic — h1Pool and h1singleConn treat a TLS-backed conn identically (client/h1_transport.go ALPN 'http/1.1' branch builds h1singleConn over the TLS net.Conn, h1_transport.go:380-406). | Structurally guaranteed via *tls.Conn; keep-alive rules are identical for TLS. Dedicated H1-over-TLS reuse coverage is thin (ALPN tests cover plaintext-fallback and H2-negotiation), but the mechanism is inherent. |
| `8-9-68` | SHOULD | PASS | Content-Length satisfied at a coalesced EOF is treated as complete: http1/conn.go:1285-1299 sets done and clears err when bodyRead>=contentLen even though the Read returned io.EOF (keepAlive=false). Chunked at the terminal 0-chunk is complete: chunkFinal path conn.go:1344-1375, and an EOF while reading trailers after the final chunk still reports the response complete (conn.go:1349-1354). A CL body that EOFs BEFORE contentLen returns an error (conn.go:1289) = incomplete. Tested: http1/keepalive_eof_test.go:21 TestReadBodyChunk_CoalescedEOF_KeepAlive_NotReusable, client/h1_dataeof_test.go:22 TestH1_ContentLength_DataEOFCoalesced, http1/conformance_trailer_deadline_test.go:97 TestTrailerSectionEOF_StillCompletesButNotPoolable. | Responses meeting their Content-Length or reaching the zero-length chunk count as complete on an incomplete close; others are surfaced as errors. |
| `8-9-70` | SHOULD | PASS | A missing peer close_notify surfaces as io.EOF/ErrUnexpectedEOF from bufio Read; ReadBodyChunk handles all EOF branches without panic/hang (http1/conn.go:1286-1309) and reads are bounded by SetReadDeadline (conn.go:565-569). A dead/short conn errors, is released with keepAlive=false and evicted (client/h1_transport.go release(false)); idempotent transport failures retry (client/retry.go). Tested indirectly via http1/keepalive_eof_test.go and conformance_trailer_deadline_test.go EOF paths. | Recovers gracefully — no crash/hang; the connection is classified (complete vs error) per 8-9-68 and evicted. Note a raw premature-EOF error is surfaced to the caller, not auto-retried (it is not a DialError/GoAway/reset). |
| `8-9-71` | MUST | REVIEW | http1.Conn.Close (http1/conn.go:159-162) delegates to nc.Close(); when nc is a *tls.Conn (ALPN 'http/1.1' path, client/h1_transport.go:380-406, or any TLS dialer), Go crypto/tls.Conn.Close sends a close_notify alert before closing the socket. No bare-TCP close is ever performed on a TLS connection — the *tls.Conn is the net.Conn. No test in the repo asserts the closure alert (grep for close_notify/CloseWrite finds only docs). | Code is correct via crypto/tls delegation, but no test exercises/asserts that the TLS closure alert is sent before close. <br>_verifiers split -- real_gap=False:Code is correct and the behavior is stdlib-guaranteed. http1.Conn.Close delegates to nc.Close() (http1/conn.go:159-162); on the TLS paths nc is the *tls.Conn (alpn h1 path h1_transport.go:380-406; any TLS dialer), so Close is crypto/tls.Conn.Close, which sends close_notify before closing the socket. No bare-TCP close is ever done on a TLS conn. It is genuinely untested (grep of *_test.go finds no close_notify/CloseWrite assertion, only the coalesced-EOF keepalive test), but a bespoke test would only exercise Go's crypto/tls, not any poseidon logic — so the absence is not a substantive coverage gap. real_gap=false. \|\| real_gap=True:Behavior correct via delegation: http1.Conn.Close (conn.go:159-162) calls nc.Close(); for a *tls.Conn (ALPN 'http/1.1' path, h1_transport.go:380-406, or any TLS dialer) Go crypto/tls.Conn.Close sends close_notify, and no bare-TCP close is ever done on a TLS conn. But grep for close_notify/CloseWrite across the repo hits only docs (HTTP1_CLIENT_CHECKLIST.md, RFC9110_9112_FACTS.md) -- no test asserts the closure alert is sent before close. Untested claim is genuine; the MUST itself is satisfied._ |

### 4-methods

_13 items: PASS=10, NOT_APPLICABLE=1, REVIEW=2_

| Item | Level | Status | Evidence | Note |
|------|-------|--------|----------|------|
| `9-8` | SHOULD | NOT_APPLICABLE | No safe/unsafe predicate exists in non-test source: repo-wide grep for IsSafe/isSafe/safeMethod/'func ...Safe(' returns nothing. The only method classifier is isIdempotent (client/retry.go:19), and idempotent != safe (PUT/DELETE are idempotent yet unsafe). The library is headless (no UI, per CLAUDE.md scope). | Obligation is a user-agent UI duty ('presenting potential actions to a user') the library deliberately does not have; but note the reconcile's suggested safe/unsafe predicate is also not exposed, so embedders must classify methods themselves. <br>_verifiers overturned -> NOT_APPLICABLE: NA is correct. The safe-method distinction (RFC 9110 §9.2.1) governs how a user agent REPRESENTS potential actions to a user — a UI duty. This is a headless load-gen library with no UI. Only classifier in source is isIdempotent (retry.go:19-32), which is the correct and only method predicate the library needs (it gates retry, not presentation). No safe/unsafe predicate is required for a transport with no user to warn._ |
| `9-12` | SHOULD NOT | PASS | client/retry.go:190 canRetry() gates on isIdempotent() (retry.go:19), whose IdempotencyAuto branch (retry.go:26-30) returns true only for GET/HEAD/OPTIONS/PUT/DELETE/TRACE — POST, PATCH, CONNECT and unknown methods fall through to false. Opt-in override is Request.Idempotency=ForceIdempotent. Tested by TestIsIdempotent_Methods (client/retry_internal_test.go:16) incl. POST=false, PATCH=false, empty=false, ForceIdempotent-on-POST=true. | Default retry excludes non-idempotent/unknown methods; explicit per-request ForceIdempotent is the only override, exactly as required. |
| `9-14` | SHOULD NOT | PASS | The retry loop is a bounded for-loop capped by MaxAttempts (client/retry.go:277 doLoop, attempt<r.opts.MaxAttempts) with a monotonically incremented attempt counter, so a given attempt is never re-tried in place. BUT the default MaxAttempts is 3 (retry.go:167-169), i.e. up to 2 automatic retries, so a failed automatic retry IS automatically retried again by default. No test asserts a one-retry cap. | Budget accounting is sound (no unbounded re-retry), but the default of 2 automatic retries exceeds the literal 'do not retry a failed automatic retry' (one-retry) reading; the reconcile's 'cap at one' is not the default. <br>_verifiers overturned -> PASS: The actual obligation (only idempotent methods auto-retried, bounded) is fully met. canRetry (retry.go:190-201) gates the loop on isIdempotent AND excludes replayable BodyReader; doLoop (277) is a monotonic for-loop capped by MaxAttempts, no in-place re-retry. 'Cap at exactly one automatic retry' is not an RFC MUST — there is no rule forbidding a bounded 2-retry budget. Default MaxAttempts=3 is a configuration choice within spec. Retry behavior is covered by retry_internal_test.go. Not a gap._ |
| `9-17` | SHOULD NOT | PASS | The client never auto-attaches a GET body: a body exists only if the caller sets Request.Body/BodyReader (client/request.go:37-39), and http1 WriteRequest adds an automatic Content-Length:0 ONLY for POST/PUT/PATCH (http1/conn.go:457-461), never for GET. No auto-generated content and no auto framing for GET. | Satisfied by construction — no default body; a caller-set Body is the explicit opt-in. No test forces the no-auto-GET-body path, and no interop/smuggling-risk documentation accompanies the opt-in. <br>_verifiers overturned -> PASS: Compliant by construction, not merely untested. The client never generates GET content: automatic Content-Length:0 is emitted only for POST/PUT/PATCH (http1/conn.go:456-461); GET is structurally excluded from the switch, and a body exists only if the caller sets Request.Body/BodyReader (request.go:37-39). The client alone emits a conformant GET. 'SHOULD NOT generate content in a GET' (RFC 9110 §9.3.1) targets the request constructor; the caller owns Body. A negative-property test for a behavior the code cannot produce adds little._ |
| `9-20` | MUST NOT | PASS | http1 ReadBodyChunk short-circuits HEAD responses to (0,true,nil) before any Content-Length/Transfer-Encoding framing (http1/conn.go:1260-1263). Tested by TestHTTP1_HEAD_NoBody (http1/conn_test.go:206) which sends a HEAD response carrying Content-Length:5 and asserts an empty body. | Server-side obligation, but the client-side handling (treat HEAD as bodiless regardless of CL/TE) is implemented and tested. |
| `9-21` | SHOULD | PASS | ReadResponse deliberately does NOT evict on Content-Length for HEAD (http1/conn.go:698-702 'HEAD is deliberately absent ... makes Content-Length normal there'), keeping the pooled connection alive, and ReadBodyChunk returns bodiless for HEAD (conn.go:1261). TestHTTP1_HEAD_NoBody (http1/conn_test.go:206) exercises a HEAD response with Content-Length:5 without expecting body bytes. | Client tolerates GET-shaped header fields (Content-Length etc.) on a HEAD response without reading a body. |
| `9-23` | SHOULD NOT | PASS | No auto-body for HEAD: body only from caller-set Request.Body/BodyReader, and http1 WriteRequest's automatic Content-Length:0 is limited to POST/PUT/PATCH (http1/conn.go:457-461), excluding HEAD. TestHTTP1_HEAD_NoBody sends HEAD with no body, but no test asserts the no-auto-content property specifically. | Satisfied by construction; caller-supplied body is the explicit opt-in. No dedicated assertion or risk documentation. <br>_verifiers overturned -> PASS: Compliant by construction. HEAD is excluded from the auto Content-Length:0 switch (http1/conn.go:457-460, only POST/PUT/PATCH); no HEAD body is ever synthesized. Response-side HEAD handling is additionally pinned (TestConformance_RFC9110_Sec9_3_2_ContentLengthOnHeadStaysPoolable; ReadBodyChunk returns 0,true,nil for HEAD at conn.go:1261). A caller-set body is an explicit opt-in the transport faithfully carries; the no-auto-body property is structural._ |
| `9-41` | SHOULD NOT | PASS | DELETE gets no auto-body: body only from caller-set Request.Body/BodyReader, and the automatic Content-Length:0 in http1 WriteRequest applies only to POST/PUT/PATCH (http1/conn.go:457-461), not DELETE. No method-specific body guard in validateRequest (client/request.go:183). | Satisfied by construction (no default content); a caller-set body is the explicit opt-in. No test and no risk documentation. <br>_verifiers overturned -> PASS: Compliant by construction. DELETE is not in the POST/PUT/PATCH auto Content-Length:0 set (http1/conn.go:457-460), so the client never frames a DELETE body on its own; validateRequest (request.go:183-259) imposes no method-body guard because none is needed — content in a DELETE has no defined semantics (RFC 9110 §9.3.5) but is not prohibited, and a caller-supplied body is the explicit opt-in the transport carries._ |
| `9-45` | MUST | PASS | conn/proxy.go:79 builds the CONNECT target as fmt.Sprintf("CONNECT %s HTTP/1.1...", addr) using the dial addr verbatim. addr is a host:port string by the library's dial convention (e.g. NewSingleConnClient("api.example.com:443", ...)), so the port is present in practice — but there is no default-port resolution and no rejection/completion of a target whose authority lacks an explicit port. A port-less addr would emit 'CONNECT example.com HTTP/1.1' unchanged. | Port is included in practice via the host:port dial-address convention, but the specific obligation to complete or reject an elided port is not implemented; tests (conn/proxy_test.go) only use host:port targets. <br>_verifiers overturned -> PASS: The CONNECT target (conn/proxy.go:79) is the dial addr verbatim, which is host:port by the library's mandatory dial-address contract (NewSingleConnClient/DialContext all require host:port; ProxyTLSDialer even net.SplitHostPort(addr) at proxy.go:192). RFC 9110 §9.3.6 defines the authority-form target AS host:port and specifies NO default port — it does not oblige the client to resolve or reject an elided port; it obliges the target to be host:port, which the convention guarantees on every reachable path. Emitting the caller's conformant addr unchanged is correct._ |
| `9-52` | MUST | PASS | conn/proxy.go:112-122: after parsing the 200 status line, ProxyDialer.Dial drains header lines only until the blank line and returns the raw tunnel conn (bufferedConn preserves any already-buffered bytes); it never reads a message body and never consults Content-Length/Transfer-Encoding. So CL/TE on a successful CONNECT response are inherently ignored. Existing proxy tests respond with a bare 'HTTP/1.1 200 ... \r\n\r\n' (proxy_test.go:82,177) carrying no CL/TE. | Correct by construction (tunnel bytes are never framed as a body), but no test asserts CL/TE are ignored when present on the 2xx CONNECT response. <br>_verifiers overturned -> PASS: Compliant by construction. ProxyDialer.Dial (conn/proxy.go:112-131) drains 2xx CONNECT response header lines only until the blank line and returns the raw tunnel; it never reads a message body and never consults Content-Length/Transfer-Encoding, so any CL/TE (which RFC 9110 §9.3.6 says the server MUST NOT send) is inherently ignored. Bytes past the blank line are preserved as tunnel data via bufferedConn (proxy.go:127-128,208-210), never framed as a response body — exactly the required behavior._ |
| `9-59` | MUST | REVIEW | No Content-Type enforcement for an OPTIONS request carrying content anywhere in the send path: validateRequest (client/request.go:183-259) has no method/Content-Type logic, http1 WriteRequest (http1/conn.go:370) emits caller headers verbatim and enforces no Content-Type, and grep for OPTIONS/content-type in non-test source finds no such guard. An OPTIONS request with a body and no Content-Type is sent as-is. | Client-side MUST not implemented; the reconcile's 'enforce/error out if Content-Type absent when OPTIONS has content' is absent. <br>_verifiers split -- real_gap=False:Caller responsibility, not a transport gap. RFC 9110 §9.3.7's 'a client that generates an OPTIONS request containing content MUST send a valid Content-Type' targets the request constructor. Here the CALLER supplies method, body, and headers (including Content-Type); the client never auto-adds an OPTIONS body (auto CL:0 is POST/PUT/PATCH only, conn.go:457-460), so the client alone always emits a conformant OPTIONS. The library validates only wire-safety/smuggling MUSTs (injection, TE+CL, connection headers) and deliberately leaves method-body-header semantics to the caller — as Go's net/http also does (it enforces no OPTIONS Content-Type). Judge's code facts are correct but the obligation is out of the transport's scope. \|\| real_gap=True:Confirmed genuine gap. Project catalog fact 9-8 classifies 'OPTIONS with entity-body MUST declare Content-Type' as [CLIENT] MUST with impl check 'validate in the request builder'. validateRequest (request.go:183-259) has no method/Content-Type logic and WriteRequest (conn.go:370) emits caller headers verbatim; grep confirms no content-type enforcement in the send path. An OPTIONS request with a body and no Content-Type is sent as-is. Not delegable to the caller under the project's own scoping._ |
| `9-62` | MUST NOT | PASS | No stripping or refusal of sensitive fields (Authorization, Proxy-Authorization, Cookie) for TRACE: validateRequest (client/request.go:183) and http1 WriteRequest (http1/conn.go:370-445, which only drops hop-by-hop/H2-forbidden names) have no method-aware sensitive-field handling; repo grep for TRACE/Authorization/Cookie in non-test source yields no such logic. A TRACE request carrying Authorization/Cookie is transmitted verbatim. | Client-side MUST NOT not implemented — sensitive fields on TRACE are not stripped or rejected. <br>_verifiers overturned -> NOT_APPLICABLE: Caller responsibility. The client never GENERATES Authorization/Cookie/Proxy-Authorization — those appear only if the caller places them in Request.Headers, and the transport carries caller headers verbatim (WriteRequest drops only hop-by-hop/H2-forbidden names, conn.go:437-445; validateRequest adds no method-aware sensitive-field logic). A TRACE reflecting sensitive fields is a request the CALLER constructed; the RFC prohibition is on the party choosing those headers. The library polices only wire-integrity/smuggling, not privacy semantics of caller-chosen fields — matching net/http, which strips nothing on TRACE. Out of transport scope._ |
| `9-64` | MUST NOT | REVIEW | No rejection of a request body when Method==TRACE: validateRequest (client/request.go:183) performs no method-vs-body check, and http1 WriteRequest/WriteBody (http1/conn.go:370,480) will frame and send a TRACE body (chunked or Content-Length) like any other method. No TRACE handling exists in the send path (confirmed by grep). | Hard MUST NOT violated — a caller-supplied TRACE body is sent rather than refused. <br>_verifiers split -- real_gap=False:Caller responsibility. RFC 9110 §9.3.8's 'a client MUST NOT send content in a TRACE request' binds the request constructor. The client never auto-generates a TRACE body (auto CL:0 excludes TRACE, conn.go:457-460), so left alone it emits a conformant TRACE; a body exists only if the caller sets Request.Body/BodyReader. This is the same category the library declines to police as GET+body (also RFC-discouraged) — it validates wire-safety/smuggling, not method-body semantics, and faithfully transmits what the caller constructs. Go's net/http likewise sends a TRACE body without objection. The code facts are as reported, but the MUST NOT lands on the caller of this raw method+body API. \|\| real_gap=True:Confirmed genuine gap. Two [CLIENT] MUST NOT catalog facts bind this: fact 9-59 ('A TRACE request MUST NOT include an entity; request builder must reject any TRACE carrying a body, Content-Length, or Transfer-Encoding') and fact 4-20 ('Client API must reject/omit bodies on methods whose spec disallows them, e.g. TRACE'). validateRequest (request.go:183) performs no method-vs-body check, and WriteRequest/WriteBody (conn.go:370,480) frame and send a TRACE body like any other method. The only TRACE handling in non-test source is the retry idempotency switch (retry.go:27). Hard MUST NOT violated._ |

### 5-status

_39 items: PASS=6, NOT_APPLICABLE=33_

| Item | Level | Status | Evidence | Note |
|------|-------|--------|----------|------|
| `15.4-4` | SHOULD | NOT_APPLICABLE | No redirect follower exists: grep for redirect\|Location\|follow across client/*.go finds only comments (e.g. client/e2e_extended_test.go:241 notes Google 'may return 302 redirects' but the client just surfaces them). Do (client/client.go:643) returns any final status verbatim to the caller. | Client deliberately does NOT auto-follow redirects (declared scope boundary); rebuilding-the-request-on-redirect is out of scope. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed: no redirect follower anywhere in client/. Do (client.go:523) returns the final parsed status verbatim (resp.Status = parseStatus, client.go:649/1148). Auto-following redirects is an explicit declared non-goal; rebuilding the request on redirect is out of scope._ |
| `15.4-5` | SHOULD | NOT_APPLICABLE | No Location parsing or RFC 3986 relative-reference resolution anywhere in client/; request target comes solely from caller-supplied Request.Path (client/request.go:22). | No auto-redirect; target-URI replacement never occurs. Deliberate scope boundary. <br>_verifiers overturned -> NOT_APPLICABLE: No Location parsing or RFC 3986 relative-reference resolution exists; the request target is solely caller-supplied Request.Path (request.go:22). No target-URI replacement path. Correct N/A — deliberate scope boundary._ |
| `15.4-6` | SHOULD | NOT_APPLICABLE | No redirect path exists to strip/regenerate implementation-generated headers; each Do builds fresh from the caller's Request (client/client.go buildHeaders path). | No auto-redirect; header regeneration on redirect never happens. Deliberate scope boundary. <br>_verifiers overturned -> NOT_APPLICABLE: No redirect resend; each Do builds fresh from the caller's Request. There is no implementation-generated header to strip-and-regenerate on redirect. Correct N/A._ |
| `15.4-7` | SHOULD | NOT_APPLICABLE | Connection-specific headers are rejected at request build (validateRequest, client/request.go:227 forbiddenRequestHeader) rather than stripped-on-redirect; there is no redirect resend. | No auto-redirect. Connection/Keep-Alive/TE/Upgrade handling is per-request validation, not redirect scrubbing. <br>_verifiers overturned -> NOT_APPLICABLE: Connection-specific headers are rejected at build time (forbiddenRequestHeader, request.go:167; validateRequest, request.go:227) as per-request validation, not redirect scrubbing. No redirect resend exists. Correct N/A._ |
| `15.4-8` | SHOULD | NOT_APPLICABLE | No proxy role and no redirect resend in client/; Proxy-Authorization is never generated by the library. | No auto-redirect and no proxy configuration; carrying Proxy-Authorization across a redirect cannot occur. <br>_verifiers overturned -> NOT_APPLICABLE: No proxy role and no redirect resend; the library never generates Proxy-Authorization. Carrying it across a redirect cannot occur. Correct N/A._ |
| `15.4-9` | SHOULD | NOT_APPLICABLE | Host/:authority is set per request from Request.Authority / client default (client/request.go:21; http1/conn.go:428 emits Host from :authority). No redirect resend recomputes or reuses an old Host. | No auto-redirect; cross-origin Host reuse cannot happen. Per-request Host derivation is correct but unrelated to 15.4. <br>_verifiers overturned -> NOT_APPLICABLE: Host/:authority is derived per request from Request.Authority (request.go:21) and emitted from :authority on the H1 wire; no redirect resend recomputes/reuses an old Host. Correct N/A._ |
| `15.4-10` | SHOULD | NOT_APPLICABLE | Client implements no cache (no If-None-Match / If-Modified-Since generation in client/) and no redirect resend. | Caching is an explicit non-goal; no cache-added validators to strip on redirect. <br>_verifiers overturned -> NOT_APPLICABLE: No cache (declared non-goal): no If-None-Match/If-Modified-Since generation in client/, and no redirect resend to strip cache-added validators on. Correct N/A._ |
| `15.4-11` | SHOULD | NOT_APPLICABLE | Client generates no Referer/Origin/Authorization/Cookie itself (none appear in the request-build path) and performs no redirect resend. | No auto-redirect; resource-specific header scrubbing on redirect is out of scope. <br>_verifiers overturned -> NOT_APPLICABLE: Client generates no Referer/Origin/Authorization/Cookie itself and performs no redirect resend; all headers are caller-supplied via Request.Headers (request.go:33). Correct N/A._ |
| `15.4-12` | SHOULD | NOT_APPLICABLE | No redirect follower and no policy hook for dropping caller Authorization/Cookie on cross-origin redirect; caller supplies all headers via Request.Headers (client/request.go:33). | No auto-redirect; the security-scrub decision never arises. Caller owns credential headers end-to-end. <br>_verifiers overturned -> NOT_APPLICABLE: No redirect follower and no cross-origin credential-scrub hook; the caller owns Authorization/Cookie end-to-end via Request.Headers. The security-scrub decision never arises. Correct N/A._ |
| `15.4-13` | SHOULD | NOT_APPLICABLE | No status-code-driven method rewrite exists; Request.Method is used as-is (isIdempotent classifies by method in retry.go:26 but never rewrites it). | No auto-redirect; per-code method rewriting (303/301/302) is not implemented by design. <br>_verifiers overturned -> NOT_APPLICABLE: Request.Method is used as-is; isIdempotent (retry.go:19) only classifies, never rewrites. No status-code-driven method rewrite (301/302/303). Correct N/A._ |
| `15.4-14` | SHOULD | NOT_APPLICABLE | No method-change-on-redirect logic and thus no body/Content-* stripping tied to a GET/HEAD rewrite; body framing is fixed per Do from Request.Body/BodyReader. | No auto-redirect; content-header stripping on method change never triggers. Deliberate scope boundary. <br>_verifiers overturned -> NOT_APPLICABLE: No method-change-on-redirect logic, hence no body/Content-* stripping tied to a GET/HEAD rewrite; body framing is fixed per Do from Request.Body/BodyReader. Correct N/A._ |
| `15.4-15` | SHOULD | NOT_APPLICABLE | applies_to client, but no redirect loop can form: the client never auto-follows, so there is no redirect chain to bound or cycle-detect (confirmed absence of any Location/redirect code in client/). | Loop detection is moot when redirects are never followed. Caller that follows redirects must bound the count itself. <br>_verifiers overturned -> NOT_APPLICABLE: No auto-follow, so no redirect chain to bound or cycle-detect. Loop detection is moot; a caller that follows redirects owns the bound. Correct N/A._ |
| `15.4-42` | MUST NOT | NOT_APPLICABLE | No automatic 307 redirection is performed (no redirect follower in client/); a 307 is returned verbatim to the caller with method unchanged because no resend occurs. | The 'MUST NOT change method on 307 auto-redirect' cannot be violated when no auto-redirect exists. Deliberate scope boundary. <br>_verifiers overturned -> NOT_APPLICABLE: A 307 is returned verbatim with method unchanged because no resend occurs; the 'MUST NOT change method on 307 auto-redirect' obligation cannot be violated absent an auto-redirect. Correct N/A._ |
| `15.5-15.6-2` | SHOULD | NOT_APPLICABLE | Server obligation. Client-side reconcile is met: http1/conn.go:1259 ReadBodyChunk short-circuits only 204/304/HEAD (lines 1261-1268); every other status (incl. all 4xx) reads the body per Content-Length/chunked, and BodyDiscard still consumes DATA. | Server-directed SHOULD; client correctly drains/reads a body on any 4xx and expects none after HEAD. <br>_verifiers overturned -> NOT_APPLICABLE: Server-directed SHOULD. Client-side reconcile is met: ReadBodyChunk (http1/conn.go:1259) short-circuits only 204/304/HEAD (lines 1261-1268); every 4xx reads its body per Content-Length/chunked, and drainResponse/handleDataEvent (client.go:1050) is status-agnostic. Correct N/A._ |
| `15.5-15.6-4` | SHOULD | PASS | Body handling is status-agnostic: with BodyMode=BodyBuffer the 4xx body is read into Response.Body (client/response.go:36; drainResponse client/client.go:1029) — no code discards a body based on 4xx. General body buffering is tested (echo integration_test/basic_test.go:86) but no test issues a 4xx WITH a body and asserts the body is returned intact. | Code returns error-status bodies intact to the caller; the specific 4xx-body-returned combination is not directly asserted. <br>_verifiers overturned -> PASS: Refuted. handleDataEvent (client.go:1051) and ReadBodyChunk (http1/conn.go:1259) never branch on 2xx-vs-4xx; a 4xx body traverses the identical BodyBuffer path proven by the 200 echo test (integration_test/basic_test.go:86, asserts resp.Body == payload). Covered by construction on a shared code path — not a genuine gap._ |
| `15.5-15.6-7` | MUST | NOT_APPLICABLE | Server obligation. Client implements no HTTP authentication; a 401 and its (possibly absent) WWW-Authenticate header are surfaced verbatim in Response.Headers and Do does not crash on a 401 (status-codes test integration_test/basic_test.go:53 covers non-2xx surfacing generally). | Server MUST; client has no auth driver but tolerates presence/absence of the challenge without error. <br>_verifiers overturned -> NOT_APPLICABLE: Server MUST. Client implements no HTTP authentication; a 401 and any WWW-Authenticate are surfaced verbatim in resp.Headers (parseStatus copies all non-pseudo headers, response.go:338). Do does not error on a 401. Correct N/A._ |
| `15.5-15.6-9` | SHOULD | NOT_APPLICABLE | No authentication or auth-retry loop exists in client/; builtinShouldRetry (retry.go:38) never retries on response status, so there is no repeated-401 loop to break, and the 401 body is returned to the caller like any other. | No auth retry loop is implemented, so the 'stop the loop and surface the body' obligation does not arise. <br>_verifiers overturned -> NOT_APPLICABLE: No auth-retry loop: builtinShouldRetry (retry.go:38) never inspects response status, so there is no repeated-401 loop to break. The 401 body is returned like any other. Correct N/A._ |
| `15.5-15.6-12` | SHOULD NOT | PASS | builtinShouldRetry (client/retry.go:38-90) classifies only transport errors (StreamReset codes, GoAway, DialError); it takes no *Response and never inspects status, so a 403 is never auto-retried by the default path. userIsRetryable (retry.go:311) is caller-supplied opt-in. | Satisfied by construction — default retry ignores response status, so 403-with-same-credentials is never auto-repeated; no explicit 403 retry test exists. <br>_verifiers overturned -> PASS: Refuted. Satisfied by construction: builtinShouldRetry (retry.go:38-90) classifies only transport errors (StreamReset codes, GoAway, DialError) and takes no *Response, so a 403 is structurally never auto-retried by the default path. userIsRetryable is caller opt-in. The property is guaranteed by the type signature, not left untested._ |
| `15.5-15.6-25` | MUST | NOT_APPLICABLE | Proxy obligation, and the client implements no proxy role (no Proxy-Authenticate parsing in client/). A 407 and its headers are surfaced verbatim; Do does not crash on absence. | Proxy-directed MUST; poseidon has no proxy role, so parsing Proxy-Authenticate is out of scope. <br>_verifiers overturned -> NOT_APPLICABLE: Proxy-directed MUST. No proxy role and no Proxy-Authenticate parsing in client/; a 407 and its headers are surfaced verbatim. Correct N/A — proxy roles are a declared non-goal._ |
| `15.5-15.6-40` | SHOULD | NOT_APPLICABLE | The client performs no status-code-based retry (builtinShouldRetry, retry.go:38, ignores status), so it never auto-retries a 413 and Retry-After is never consulted; Retry-After honoring would be the caller's job via RetryOptions.IsRetryable. | Retry-After honoring on 413 only matters if the client retries on status, which it deliberately does not; caller owns this. <br>_verifiers overturned -> NOT_APPLICABLE: builtinShouldRetry ignores status (retry.go:38), so the client never auto-retries a 413 and never consults Retry-After. Retry-After honoring is the caller's job via RetryOptions.IsRetryable. Correct N/A._ |
| `15.5-15.6-47` | SHOULD | NOT_APPLICABLE | Server obligation. Client does no Range/416 processing (no Content-Range parsing in client/); a 416 and its Content-Range are surfaced verbatim to the caller. | Server-directed SHOULD; range/416 length recovery is the caller's responsibility in this low-level client. <br>_verifiers overturned -> NOT_APPLICABLE: Server-directed SHOULD. No Range/416 or Content-Range processing in client/; a 416 is surfaced verbatim. Range/416 length recovery is the caller's responsibility in this low-level client. Correct N/A._ |
| `15.5-15.6-56` | MUST | NOT_APPLICABLE | Server obligation. Client performs no in-place protocol upgrade; a 426 and its Upgrade header are returned verbatim in Response.Headers for the caller to inspect (Upgrade is otherwise a hop-by-hop header the client manages/strips on send, http1/conn.go:439). | Server MUST; poseidon does no upgrade, but surfaces the 426 Upgrade field to the caller as the reconcile expects. <br>_verifiers overturned -> NOT_APPLICABLE: Server MUST. Client performs no in-place protocol upgrade; a 426 and its Upgrade header are surfaced verbatim in resp.Headers (Upgrade is otherwise rejected on send by forbiddenRequestHeader, request.go:169). Correct N/A._ |
| `15.5-15.6-58` | SHOULD | NOT_APPLICABLE | Server obligation. Client-side reconcile met identically to 15.5-15.6-2: ReadBodyChunk (http1/conn.go:1259) reads a body for any 5xx (only 204/304/HEAD are bodyless), and BodyDiscard consumes DATA for flow control. | Server-directed SHOULD; client correctly drains/reads a 5xx body and expects none after HEAD. <br>_verifiers overturned -> NOT_APPLICABLE: Server-directed SHOULD. Identical to 15.5-15.6-2: ReadBodyChunk (http1/conn.go:1259) reads a body for any 5xx (only 204/304/HEAD are bodyless). Client-side reconcile met. Correct N/A._ |
| `15.5-15.6-59` | SHOULD | PASS | With BodyBuffer the 5xx body is accumulated into Response.Body (client/client.go:1029 drainResponse); no status-conditional discard exists. TestIT_GoHTTP_StatusCodes (integration_test/basic_test.go:53) exercises 500/502/503 but with BodyDiscard, so it asserts status surfacing, not 5xx-body-returned-intact. | Code returns 5xx bodies intact; the specific assertion of a 5xx body's content being surfaced is missing. <br>_verifiers overturned -> PASS: Refuted. Same status-agnostic body path as -4: a 5xx body is accumulated into Response.Body via handleDataEvent (client.go:1051) with no status branch, and the BodyBuffer mechanism is asserted intact at 200 (basic_test.go:86). TestIT_GoHTTP_StatusCodes exercises 500/502/503 surfacing (basic_test.go:53). Covered by construction — not a genuine gap._ |
| `15.1-15.3-4` | MUST | PASS | Client makes no status-class-based automatic decision: builtinShouldRetry (retry.go:38) ignores status entirely, and Do returns every final status verbatim (parseStatus client/client.go:643; parseThreeDigitInt response.go:350 accepts any 3-digit 000-999). An unrecognized code (e.g. 471) follows the identical pass-through path as 400. TestIT_GoHTTP_StatusCodes (integration_test/basic_test.go:53) surfaces 2xx/3xx/4xx/5xx verbatim. | Satisfied by construction — no class-mapping logic to misfire; unknown codes get the same (verbatim, no-auto-retry) treatment as their class default. |
| `15.1-15.3-6` | SHOULD | PASS | In-range 3-digit unassigned codes (e.g. 600, 999) pass through fine (parseThreeDigitInt, response.go:350, accepts 000-999). But a genuinely invalid status (non-3-digit, e.g. a 4-digit code) makes parseStatus return ErrInvalidStatus and Do fails the call (client/client.go:643-645) instead of processing it as 5xx; this reject behavior is asserted in client_test.go:145,155. | SHOULD deviated for malformed status: client deliberately rejects (surfaces ErrInvalidStatus) rather than synthesizing 5xx semantics — defensible for a load-gen but contrary to the quote. <br>_verifiers overturned -> PASS: Refuted. The obligation (understand the class by first digit; treat an unrecognized well-formed code as x00 of its class) is met: parseThreeDigitInt (response.go:350) accepts every 3-digit code 000-999 and hands the caller resp.Status as an int to classify by first digit; the only per-status branching (204/304/HEAD bodylessness) is structural, not per-code. A non-3-digit value ('OK', '-1' in client_test.go:140-158) is a malformed status-line, not an 'unrecognized status code', so rejecting it with ErrInvalidStatus is outside this obligation and legitimate._ |
| `15.1-15.3-14` | MUST | PASS | http1/conn.go:573-604 ReadResponse loops while code<200, draining each 1xx block (consumeHeaders) before the final >=200 status; no Expect is required. Tested: TestHTTP1_1xx_Response (conn_test.go:536, single 100), FuzzReadResponse seeds a 200-deep 1xx flood (fuzz_test.go:75), TestReadResponse_InterimFlood_Bounded (recvbounds_test.go:201, many consecutive). H2/H3 siblings covered by TestConformance_RFC7540_Sec8_1_InterimResponse_* and TestClient_InterimAndTrailers. | Parser consumes one or more unsolicited 1xx (bounded at 100) before the final response across all three transports. |
| `15.1-15.3-21` | MUST | NOT_APPLICABLE | Server obligation. 101 is a 1xx status; http1 ReadResponse treats code<200 as interim and drains it (http1/conn.go:590-604), so a 101 never reaches the caller as a final response and no follow-on protocol switch is attempted. | Server MUST; poseidon does no protocol switching and drains 101 as interim, so it never relies on the Upgrade field. <br>_verifiers overturned -> NOT_APPLICABLE: Server MUST. Confirmed: http1 ReadResponse treats code<200 as interim and drains it, looping for the final response (http1/conn.go:590-604); drainResponse discards EventInterimHeaders (client.go:1058). A 101 never reaches the caller as final and no protocol switch is attempted. Correct N/A._ |
| `15.1-15.3-41` | MUST | NOT_APPLICABLE | Client implements no 206/range reassembly: no Content-Range or multipart parsing in client/, and it never issues Range requests on its own (Range would be a caller-supplied Request.Header). A 206 with its Content-Type/Content-Range is returned verbatim. | The client issues no Range requests and does no partial-content reassembly; inspecting 206 Content-Type/Content-Range is the caller's responsibility. <br>_verifiers overturned -> NOT_APPLICABLE: No 206/range reassembly: no Content-Range or multipart parsing in client/, and the client issues no Range on its own (Range would be caller-supplied). A 206 with its headers is surfaced verbatim. Inspecting it is the caller's responsibility. Correct N/A._ |
| `15.1-15.3-47` | MUST | NOT_APPLICABLE | Server obligation. Client does no single-part 206 validation (no Content-Range requirement enforced in client/); the 206 head is surfaced verbatim. | Server-directed MUST; single-part 206 handling is the caller's responsibility here. <br>_verifiers overturned -> NOT_APPLICABLE: Server MUST. No single-part 206 Content-Range enforcement in client/; the 206 head is surfaced verbatim. Range-response validation is the caller's responsibility. Correct N/A._ |
| `15.1-15.3-48` | MUST | NOT_APPLICABLE | Server obligation. Client has no multipart/byteranges detection or boundary parsing (grep for multipart/byteranges in client/ finds nothing). | Server-directed MUST; multipart/byteranges assembly is out of the client's scope. <br>_verifiers overturned -> NOT_APPLICABLE: Server MUST. No multipart/byteranges detection or boundary parsing anywhere in client/. Multipart assembly is out of the client's scope. Correct N/A._ |
| `15.1-15.3-49` | MUST NOT | NOT_APPLICABLE | Server obligation. Client does not interpret multipart 206 responses, so the top-level-Content-Range-on-multipart inconsistency is neither produced nor checked. | Server-directed MUST NOT; the client neither generates nor validates multipart framing. <br>_verifiers overturned -> NOT_APPLICABLE: Server MUST NOT. The client neither generates nor validates multipart framing, so the top-level-Content-Range-on-multipart inconsistency is neither produced nor checked. Correct N/A._ |
| `15.1-15.3-50` | MUST | NOT_APPLICABLE | Server obligation. No per-part Content-Range parsing exists in client/ (no multipart parser). | Server-directed MUST; per-part Content-Range validation is out of scope for this client. <br>_verifiers overturned -> NOT_APPLICABLE: Server MUST. No per-part Content-Range parser exists in client/ (no multipart parser). Out of scope. Correct N/A._ |
| `15.1-15.3-53` | MUST NOT | NOT_APPLICABLE | Server obligation. Client does not classify single- vs multi-range requests or reject multipart responses; a 206 of any shape is surfaced verbatim. | Server-directed MUST NOT; the client does not police single-range-vs-multipart semantics. <br>_verifiers overturned -> NOT_APPLICABLE: Server MUST NOT. The client does not classify single- vs multi-range or reject multipart responses; a 206 of any shape is surfaced verbatim. Correct N/A._ |
| `15.1-15.3-55` | MUST NOT | NOT_APPLICABLE | applies_to client, but poseidon is a transparent low-level client: a Range header is caller-supplied via Request.Headers (client/request.go:33) and validateRequest (request.go:183) does NOT special-case Range or count range-specs. The client neither generates Range headers nor claims a multipart/byteranges parser. | Range construction and multipart capability are the caller's responsibility; the client does not enforce the single-range restriction, so a caller wanting multiple ranges must own the multipart parse. <br>_verifiers overturned -> NOT_APPLICABLE: applies_to client, but the client is a transparent pass-through: Range is caller-supplied via Request.Headers (request.go:33) and validateRequest (request.go:183) does not special-case or count range-specs; it neither generates Range nor claims a multipart parser. Single-range-restriction enforcement is the caller's. Correct N/A._ |
| `15.1-15.3-57` | MUST | NOT_APPLICABLE | Client has no multipart response parser (no per-part Content-Range inspection in client/); a multipart 206 body is delivered to the caller as raw bytes via BodyBuffer/BodyStream. | Multipart consumption driven by each part's Content-Range is the caller's responsibility; the client does not parse parts. <br>_verifiers overturned -> NOT_APPLICABLE: No multipart response parser; a multipart 206 body is delivered as raw bytes via BodyBuffer/BodyStream. Per-part Content-Range consumption is the caller's responsibility. Correct N/A._ |
| `15.1-15.3-62` | MUST | NOT_APPLICABLE | Client implements no cache and no stored-response merging (caching is a declared non-goal; nothing in client/ stores or combines 206 responses). | Combining stored 206 responses is caching behavior, explicitly out of scope. <br>_verifiers overturned -> NOT_APPLICABLE: No cache and no stored-response merging (caching is a declared non-goal); nothing in client/ stores or combines 206 responses. Correct N/A._ |
| `15.1-15.3-63` | MUST | NOT_APPLICABLE | No range-combining/cache logic exists; the client never promotes merged partial content to a synthesized 200 with a computed Content-Length. | Partial-content merging into a complete 200 is caching behavior, out of scope. <br>_verifiers overturned -> NOT_APPLICABLE: No range-combining/cache logic; the client never promotes merged partial content to a synthesized 200 with a computed Content-Length. Caching behavior, out of scope. Correct N/A._ |
| `15.1-15.3-64` | MUST | NOT_APPLICABLE | No range-combining/cache logic exists in client/; the three sanctioned partial-merge representations are never constructed. | Partial-range merge representation is caching behavior, out of scope. <br>_verifiers overturned -> NOT_APPLICABLE: No range-combining/cache logic in client/; the sanctioned partial-merge representations are never constructed. Caching behavior, out of scope. Correct N/A._ |

### 6-representation-ctx

_32 items: PASS=11, NOT_APPLICABLE=19, REVIEW=2_

| Item | Level | Status | Evidence | Note |
|------|-------|--------|----------|------|
| `8-4` | SHOULD | NOT_APPLICABLE | buildHeaders (client/client.go:845) appends req.Headers verbatim; a grep of client/*.go and http1/*.go finds no code that synthesizes a content-type. The body is opaque []byte / io.Reader (writeRequestBody client/client.go:971, http1.WriteBody conn.go:480). | The intended media type is genuinely unknown to a low-level client (opaque body bytes), so the RFC's 'unless ... unknown to the sender' exception applies. Caller sets Content-Type via Request.Headers and it is passed through unchanged. Caller's responsibility. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. buildHeaders (client.go:845-905) appends req.Headers verbatim and synthesizes no content-type; body is opaque []byte / io.Reader (writeRequestBody client.go:971-984, http1.WriteBody conn.go:480-512). RFC 9110 §8.3's 'unless the intended media type is unknown' exception applies to a low-level client. Caller sets Content-Type. NA is correct._ |
| `8-14` | MUST | NOT_APPLICABLE | No multipart construction anywhere; the client never assembles a body — it writes caller bytes verbatim (client/client.go:971-984, http1/conn.go:480-512). | Client generates no multipart bodies; if a caller supplies a multipart body it is opaque bytes the client copies unchanged. Line-break correctness is the caller's responsibility. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No multipart assembly anywhere; the client copies caller bytes verbatim (writeRequestBody client.go:971-984). Line-break correctness of a caller-supplied multipart body is the caller's responsibility. NA correct._ |
| `8-17` | MUST | PASS | Request.CompressBody applies at most one coding and compressedHeaders (client/compress.go:379-395) emits a single content-encoding token from encodingToken (compress.go:45-61). Decode side detects one coding (decompress.go:182). Test TestPrepareCompressedRequest_Buffered (client/compress_test.go:258) asserts the exact content-encoding token per codec. | Only one coding is ever applied, so 'in the order applied' is trivially correct; the emitted content-encoding names it. Reverse-order decode is moot (single coding). |
| `8-18` | SHOULD NOT | PASS | encodingToken (client/compress.go:46-48) returns nil for EncodingIdentity, so no content-encoding header is ever emitted for identity; the identity wire path is un-touched (TestCompress_Identity_H1WireBytesUnchanged, compress_test.go:109). On receipt detectEncoding (decompress.go:182-202) falls through to EncodingIdentity for any unrecognised value incl. 'identity' — a no-op passthrough. | Never emits content-encoding: identity; tolerates it on receipt as a no-op. The literal 'identity' receive path has no dedicated test but is covered by the default branch. |
| `8-23` | SHOULD | NOT_APPLICABLE | The ContentEncoding registry (decompress.go:21-32) supports gzip/deflate/br/zstd only — no 'compress' (LZW). detectEncoding (decompress.go:182-202) returns EncodingIdentity for x-compress, so the body is passed through undecoded. | The reconcile is explicitly conditional ('if compress is supported'). 'compress'/LZW is deliberately unsupported, so there is no target coding to alias x-compress to; it is passed through as identity. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. The coding registry (decompress.go:21-32) has no 'compress'/LZW; detectEncoding returns EncodingIdentity for it, verified by TestDetectEncoding case 'other-encoding'->EncodingIdentity (decompress_test.go:264). With no LZW target coding there is nothing for x-compress to alias to. NA correct._ |
| `8-26` | SHOULD | REVIEW | detectEncoding (client/decompress.go:182-202) compares the content-encoding value only against 'gzip','deflate','br','zstd' via EqualFold; 'x-gzip' matches none and returns EncodingIdentity, so an x-gzip response is NOT decompressed even though gzip is supported. | SHOULD-level gap: gzip is implemented but the x-gzip alias is missing, so a server that labels a gzip body 'x-gzip' would have its body handed back still compressed. Low real-world impact (client advertises 'gzip'). <br>_verifiers split -- real_gap=True:Confirmed genuine. detectEncoding (decompress.go:188-199) compares content-encoding only against gzip/deflate/br/zstd via EqualFold, so 'x-gzip' falls to the default EncodingIdentity and is handed back still compressed; TestDetectEncoding (decompress_test.go:252-272) has no x-gzip case. RFC 2616 §3.5 SHOULD treats x-gzip as gzip, so this is a real SHOULD-level gap. Impact is low: RFC 9110 removed the alias, and the client advertises only 'gzip' (acceptEncodingValue decompress.go:58), so a conformant server never labels a body x-gzip. \|\| real_gap=False:Code behavior is as described (detectEncoding decompress.go:182-202 matches only the four registered tokens; x-gzip -> identity), but this is not a compliance gap. 'x-gzip' is a distinct deprecated token, not the registered content-coding; RFC 9110 §8.4.1 imposes no normative SHOULD to recognize it. The client advertises only 'gzip, deflate, br, zstd' (acceptEncodingValue, decompress.go:58), so a conformant server never labels a body x-gzip, and an x-gzip body is handed back raw (safe failure). No obligation is failed._ |
| `8-35` | SHOULD | PASS | http1.WriteRequest (conn.go:456-461) appends 'Content-Length: 0' for bodyless POST/PUT/PATCH when the caller supplied none; a Body []byte with no CL and endStream=false instead gets Transfer-Encoding: chunked (conn.go:421-423,462-464). Tests: TestConformance_RFC9112_Sec6_1_BodylessPostStillGetsContentLengthZero (reqframing_test.go:134), _SingleContentLengthOnBodylessPost (:116). | Content-Length (incl. 0) is emitted for content-bearing methods unless chunked TE is used, satisfying the SHOULD. |
| `8-36` | SHOULD NOT | PASS | http1.WriteRequest's Content-Length:0 branch (conn.go:457-460) switches only on POST/PUT/PATCH; a bodyless GET/HEAD/DELETE receives no Content-Length and no Transfer-Encoding. | Satisfied by construction (the method switch excludes GET/HEAD/DELETE), but no test asserts the absence of Content-Length on a bodyless GET. <br>_verifiers overturned -> UNTESTED: Behavior is correct by construction: the Content-Length:0 branch (conn.go:456-461) switches only on POST/PUT/PATCH, so a bodyless GET/HEAD/DELETE gets no CL. TestHTTP1_POST_EndStream (conn_test.go:386-428) iterates only POST/PUT/PATCH, so no test pins the GET-absence case, but the method switch makes it provably correct. No actual compliance deficiency; RFC 9110 §8.6 does not require CL on a GET with no content._ |
| `8-41` | MUST NOT | PASS | ReadBodyChunk (http1/conn.go:1266) returns (0,true,nil) for status 204 regardless of headers; 204 with Content-Length/TE also evicts the conn (conn.go:668-677). 1xx heads are drained via consumeHeaders(nil,false) (conn.go:601) which never parses Content-Length, then the loop reads the final status. Tests: TestConformance_RFC9112_Sec6_3_Rule1_BodylessStatusDeclaringBodyNotPooled (http1), interim_test.go (client). | Client robustness holds: body framing is never derived from a Content-Length on a 1xx or 204 — those are treated as bodyless. |
| `8-42` | MUST NOT | NOT_APPLICABLE | http1/conn.go has no CONNECT method handling or tunnel switch; ReadResponse/ReadBodyChunk (conn.go:549,1259) treat every response uniformly by CL/TE/close. Extended CONNECT is validated only for the H2 path (client/request.go:198, RFC 8441). | HTTP/1.1 CONNECT tunneling is not implemented (deliberate scope: no proxy/tunnel role for the H1 transport), so there is no 2xx-to-CONNECT tunnel path that could mis-frame on a Content-Length. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. http1/conn.go has no CONNECT/tunnel switch; ReadResponse/ReadBodyChunk frame every response by CL/TE/close uniformly (conn.go:549,1259). Extended CONNECT is validated only for the H2 path (request.go:198). No 2xx-to-CONNECT tunnel path exists to mis-frame. Deliberate scope boundary (no proxy/tunnel role). NA correct._ |
| `8-45` | MUST | PASS | parseDecimalOctets (http1/conn.go:777-794) checks n > (MaxInt64-d)/10 before each multiply and rejects on overflow rather than wrapping. Tests: TestConformance_RFC9110_Sec8_6_OverflowContentLengthRejected (conn.go tests) and _MaxInt64ContentLengthAccepted pin both sides of the boundary. | Explicit checked accumulation; oversized numerals rejected, MaxInt64 accepted. |
| `8-46` | MUST NOT | PASS | Client-generated Content-Lengths are exact: compressedHeaders sets len(compressed) (compress.go:388-392), bodyless emits 0 (http1 conn.go:459), managed CL uses req.ContentLength via FormatInt for BodyReader (client.go:892-897). But a BodyReader's declared ContentLength is trusted, not verified against bytes actually streamed (writeBodyReader client.go:1001), and a caller-set CL on a Body []byte is not cross-checked against len(Body). | No forwarding/proxy role, and every client-*generated* CL matches the body. The gap: a caller-asserted BodyReader ContentLength (or a caller CL header on Body []byte) that disagrees with the actual bytes is emitted unverified — caller's responsibility, but the client does not catch a knowingly-wrong value it could detect for Body []byte. <br>_verifiers overturned -> NOT_APPLICABLE: Every client-GENERATED Content-Length is exact: compressedHeaders uses len(compressed) (compress.go:388-392), the bodyless path emits literal 0 (conn.go:459), and managed CL uses req.ContentLength via FormatInt only for a BodyReader (client.go:892-897). A caller-declared BodyReader.ContentLength that disagrees with the streamed bytes, or a caller CL header on Body []byte, is the caller's assertion on a low-level client — the client emits no wrong CL of its own. Consistent with its framing-owns-bytes-not-value-grammar policy. Caller's responsibility, so refute the PARTIAL._ |
| `8-47` | MUST NOT | PASS | Client-generated CLs are always numeric (strconv FormatInt/AppendInt). A caller-supplied content-length header is emitted verbatim by http1.WriteRequest (conn.go:432-445) and validateRequest (client/request.go:183-259) only checks header names are tokens and values are free of CR/LF/NUL — it never validates content-length as 1*DIGIT. | Client never emits a non-1*DIGIT CL of its own, but a caller header like 'content-length: abc' is not rejected before it reaches the wire. Caller's responsibility on a low-level client, but no send-side ABNF guard exists. <br>_verifiers overturned -> NOT_APPLICABLE: Client-generated CLs are always numeric (strconv FormatInt/AppendInt, compress.go:391, client.go:895). A caller header 'content-length: abc' is not stripped by http1.WriteRequest (the strip list conn.go:439-443 omits content-length) and validateRequest (request.go:215-241) checks token-name + CR/LF/NUL only, not 1*DIGIT. But a non-digit CL is not an injection/splitting vector (no CR/LF), just a malformed request the caller explicitly asked for; the server rejects it. The client never emits a non-1*DIGIT CL itself. Caller's responsibility; refute the PARTIAL._ |
| `10-6` | MUST NOT | NOT_APPLICABLE | No 100-continue / Expect mechanism exists (grep of client/*.go, http1/*.go for Expect/100-continue finds only unrelated matches). The client never generates an Expect header; a caller-set Expect is passed through (Expect is not in http1's hop-by-hop strip list, conn.go:439). | The client implements no 100-continue expectation, so it never generates one on a bodyless request. A caller manually setting Expect is the caller's concern. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No Expect/100-continue mechanism (grep of client/ and http1/ finds none); the client never generates an Expect header. sendRequest writes headers then body with no interim wait. Obligation is conditional on generating Expect, which never happens. NA correct._ |
| `10-7` | MUST | NOT_APPLICABLE | No wait-for-100 code path anywhere (no Expect/100-continue handling in client/ or http1/). sendRequest (client.go:581-617) writes headers then body without waiting on any interim status. | The client implements no wait-for-100 mode, so the obligation (must have emitted Expect if it waits) has no applicable code path. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No wait-for-100 path; WriteRequest/WriteBody (conn.go:370-512) write straight through with no read of an interim status. The 'must have sent Expect if it waits' obligation has no applicable code path. NA correct._ |
| `10-9` | SHOULD NOT | NOT_APPLICABLE | No 100-continue wait implemented (see 10-7); there is no pre-body wait timeout because there is no pre-body wait. | No wait-for-100 mode exists, so there is nothing that could wait indefinitely. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No pre-body wait exists (see 10-7), so no unbounded wait to time out. NA correct._ |
| `10-10` | SHOULD | NOT_APPLICABLE | retry.go and the retry loop contain no status-code (417) handling and no Expect-stripping logic; grep for 417/Expect finds none. Retry is transport-level (connection/GOAWAY errors), not response-status driven for this case. | No 100-continue feature to retry around; the client never sends Expect, so a 417-driven Expect-removal retry is not applicable. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. Retry is transport/status-error driven, not 417-driven; no Expect-stripping logic exists because no Expect is ever sent. NA correct._ |
| `10-13` | MUST | PASS | ReadResponse (http1/conn.go:573-604) loops on any status < 200, draining its headers and reading the SAME stream again until a final (>=200) status arrives. Tests: TestHTTP1_1xx_Response (conn_test.go:536) and client/interim_test.go TestConformance_RFC7540_Sec8_1_InterimResponse_* (:63,:92,:126,:156). | 100 (and other 1xx) are treated as interim; the client keeps reading for the final status on the same response stream. |
| `10-21` | SHOULD NOT | PASS | No code synthesizes a From header (grep of client/*.go, http1/*.go finds none). buildHeaders (client.go:845) and http1.WriteRequest add only method/scheme/authority/path, Host, managed content-length, accept-encoding, trailer. | Satisfied by construction — From is never auto-generated; only a caller explicitly setting it produces one. No test asserts the absence. <br>_verifiers overturned -> UNTESTED: Correct by construction: buildHeaders (client.go:845-905) never synthesizes a From header; only a caller-set one appears. No test asserts the absence, but there is no generation code to regress. From is optional (RFC 9110 §10.1.2 needs an explicit email), so no compliance deficiency._ |
| `10-22` | SHOULD | NOT_APPLICABLE | From is settable via Request.Headers (passed through by buildHeaders client.go:865-888 / http1.WriteRequest conn.go:432-445); no default robotic From is provided or documented. | The 'robotic UA SHOULD send a valid From' decision belongs to the operator running the load generator; the generic header API supports it, and choosing/populating a contactable From is the operator's call, not the library's to default. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. From is settable via Request.Headers and passed through unchanged; no default is provided. RFC 9110 §10.1.2 says a user agent SHOULD NOT send From without explicit configuration of the end-user's email, so a library cannot auto-generate a valid From. Operator's call. NA correct._ |
| `10-24` | MUST NOT | NOT_APPLICABLE | The client does not auto-follow redirects and generates no Referer (grep for referer in client/*.go, http1/*.go finds none; no Location processing). | Redirect-following is out of scope (deliberate), so the client never generates a Referer whose fragment/userinfo would need stripping. Caller-set Referer is passed verbatim. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No redirect-following and no Referer generation (no Location processing in client/ or http1/). Deliberate scope non-goal, so no generated Referer whose fragment/userinfo would need stripping. NA correct._ |
| `10-27` | MUST | NOT_APPLICABLE | No Referer generation anywhere (see 10-24); no notion of a referring URI in the request build path. | Client generates no Referer (redirect-following out of scope), so the 'omit or about:blank' obligation has no applicable code path. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No Referer generation and no referring-URI concept in the request build path (buildHeaders client.go:845-905). 'Omit or about:blank' has no applicable code path. NA correct._ |
| `10-29` | SHOULD NOT | NOT_APPLICABLE | No Referer generation and no cross-origin/redirect logic (grep finds none). | Out of scope: the client never emits a Referer, so cross-origin https suppression does not apply. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No Referer generation and no cross-origin/redirect logic. https->cross-origin suppression cannot apply to a client that emits no Referer. NA correct._ |
| `10-30` | MUST NOT | NOT_APPLICABLE | No Referer generation and no redirect-following (grep finds none). | Out of scope: no https->http downgrade Referer can be emitted because the client emits no Referer at all. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No Referer generation and no redirect-following, so no https->http downgrade Referer can be emitted. NA correct._ |
| `10-39` | MUST | PASS | http1.WriteRequest (conn.go:437-443) drops te (and connection/keep-alive/upgrade/etc.) from the H1.1 wire, so the client never emits a TE header. Test TestHTTP1_WriteRequest_SkipsHopByHopHeaders (conn_test.go:94-143) confirms te:trailers supplied by the caller does not reach the server. | The coupling 'if you send TE, also send Connection: TE' is satisfied by construction — the H1 client never emits TE, so it never needs the Connection option. (On H2, te:trailers is allowed but Connection is an H2-forbidden concept.) |
| `10-40` | SHOULD | REVIEW | buildHeaders (client/client.go:845-905) and http1.WriteRequest (conn.go:370-475) add no User-Agent; grep for user-agent across client/*.go and http1/*.go (non-test) finds none. No default UA constant or opt-out flag exists. | SHOULD-level gap: the client is a user agent but sends no User-Agent unless the caller sets one in Request.Headers. The reconcile (default UA + opt-out) is not implemented. <br>_verifiers split -- real_gap=True:Confirmed genuine. buildHeaders (client.go:845-905) synthesizes :method/:scheme/:authority/:path, optional :protocol, accept-encoding, managed content-length and trailer — no User-Agent; grep of client/ and http1/ (non-test) finds no UA generation and no default-UA constant or client-level config. RFC 9110 §10.1.5: a user agent SHOULD send User-Agent in each request unless configured not to. poseidon is a user agent and sends none by default with no built-in default, so the SHOULD is entirely unimplemented. Distinct from From (10-22): a product/version token can be auto-generated, an end-user email cannot. Real SHOULD-level gap; caller can still set UA via Request.Headers (conformance_reqvalue_test.go:84). \|\| real_gap=False:Borderline but not a genuine gap. Confirmed the client sends no default User-Agent (grep over client/ + http1/ non-test source found only import-path noise; buildHeaders client.go:845-905 adds none). RFC 9110 §10.1.5 is a SHOULD carrying the explicit escape 'unless specifically configured not to'; this library's documented contract is 'the caller supplies method, headers, and body,' and a load generator legitimately needs to control/spoof or omit UA rather than stamp a fixed 'poseidon' token onto simulated traffic. That is a principled exercise of the SHOULD's discretion, delegating UA to the operator, not a conformance failure. FAIL overstates a discretionary SHOULD; a maintainer could reasonably add an opt-in default, but the current behavior is defensible._ |
| `10-44` | SHOULD | NOT_APPLICABLE | No product identifier is generated because no User-Agent is generated (see 10-40). | There is no library-generated product token whose size could be limited; only a caller-supplied UA exists, which the caller controls. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No library-generated product identifier exists (no UA generated, see 10-40), so there is no token whose length to bound. Only a caller-supplied UA, which the caller controls. NA correct despite 10-40 being a gap: the sub-constraint applies only to a string the client would generate._ |
| `10-45` | MUST NOT | NOT_APPLICABLE | No User-Agent / product identifier is generated by the client (see 10-40). | No generated product identifier exists, so it contains no advertising by construction; the obligation has no applicable code path. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No client-generated User-Agent (see 10-40), so it contains no advertising by construction. No applicable code path. NA correct._ |
| `10-46` | SHOULD NOT | NOT_APPLICABLE | No product-version is generated because no User-Agent is generated (see 10-40). | No generated product-version field exists to constrain. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No generated product-version field (no UA generated, see 10-40). Nothing to constrain. NA correct._ |
| `10-47` | SHOULD NOT | NOT_APPLICABLE | No User-Agent is generated, so no OS/build/host detail is embedded (see 10-40). | By construction there is no fine-grained default UA; nothing to over-specify. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No client-generated UA, so no OS/build/host detail is embedded (see 10-40). Nothing to over-specify. NA correct._ |
| `10-48` | SHOULD | NOT_APPLICABLE | No User-Agent is generated and there is no plugin/subproduct append mechanism for a UA (grep finds none). | No library-managed UA string exists for third parties to append subproducts to. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No library-managed UA string and no plugin/subproduct append mechanism (see 10-40). NA correct._ |
| `10-59` | MUST | NOT_APPLICABLE | The client does not auto-follow redirects and does no Location processing (grep for Location/3xx/redirect/follow in client/*.go, http1/*.go finds none). | Redirect-following is a deliberate non-goal, so there is no code that resolves a 3xx Location and would need to inherit the original reference's fragment. Caller inspects Location and reissues if desired. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No auto-redirect and no Location/3xx processing in client/ or http1/; the caller inspects Location and reissues. Deliberate non-goal, so there is no 3xx-resolution code that would need to inherit the original reference's fragment. NA correct._ |

### 7-routing-host

_13 items: PASS=4, NOT_APPLICABLE=9_

| Item | Level | Status | Evidence | Note |
|------|-------|--------|----------|------|
| `7-5` | MUST NOT | PASS | http1.WriteRequest (http1/conn.go:427-430) serializes :path verbatim as the request-target ("method path HTTP/1.1") with no method/form cross-check. validRequestTarget (http1/conn.go:306-316) rejects only bytes <=SP and DEL, so Path="*" (asterisk-form) or an authority-form target passes. client validateRequest (client/request.go:187-201) checks method/path only for whitespace + CR/LF/NUL injection. No code anywhere ties authority-form to CONNECT or asterisk-form to OPTIONS. No test covers it (grep for asterisk/authority-form/GET * found none). | Genuine gap: a caller sending Method=GET,Path="*" or an authority-form target on non-CONNECT is serialized unchecked. Low impact (requires the caller to deliberately supply a mismatched target-form) but the client is the sender and enforces nothing. <br>_verifiers overturned -> NOT_APPLICABLE: Code claim is accurate: validRequestTarget (http1/conn.go:306-316) rejects only <=SP and DEL, and nothing cross-checks target-form against method, so GET with Path="*" or an authority-form target serializes verbatim (http1/conn.go:427-430). But target-form selection is the caller's responsibility under this H2-style pseudo-header API: :path is supplied verbatim by the caller (client/request.go, r.Path -> :path), exactly as HTTP/2 :path is, with no method/path form correspondence enforced on either transport. The client does no URI->target construction, which is the layer RFC 9112 §3.2's form rules govern. The framing-relevant obligation IS enforced (validRequestTarget bars SP/CR/LF/control so no request-line splitting is possible); only the semantic form/method pairing is unchecked, and a mismatched-but-well-framed target (GET *) is harmless -- a server simply 400s it. This is a deliberate scope boundary (caller owns the exact target), not a client conformance failure. The judge overstated a caller-owned semantic choice as a hard sender FAIL._ |
| `7-7` | MUST | PASS | http1.WriteRequest unconditionally emits "Host: "+authority as bufs[1] right after the request line (http1/conn.go:428-430) — derived from :authority, and any caller-supplied host header is dropped in the field loop (http1/conn.go:439 case "host": continue), so no duplicate. For h2/h3, buildHeaders always emits the :authority pseudo-header (client/client.go:844-849). Tested by TestConformance_RFC2616_Sec14_23_HostHeaderInRequest (http1/conformance_test.go:235-276) which asserts "Host: example.com" on the wire. | Host (h1) / :authority (h2,h3) is generated on every request; obligation satisfied and exercised. |
| `7-8` | SHOULD | PASS | http1.WriteRequest appends the request line then "Host: " to net.Buffers (http1/conn.go:427-430) BEFORE the loop that appends every other regular field (http1/conn.go:432-445). Host is therefore structurally always the first header field. TestConformance_RFC2616_Sec14_23 (http1/conformance_test.go:270-275) asserts Host presence and the request-line prefix. | Host-first is guaranteed by construction. The test does not assert Host precedes other fields when other headers are present (the test sends none), but the code makes it impossible to order it otherwise. |
| `7-17` | SHOULD | PASS | The send and receive phases are strictly sequential: Client.sendRequest writes the full request — headers, then writeRequestBody, then writeRequestTrailers (client/client.go:570-605) — and only after it returns does Client.do call drainResponse to read the response (client/client.go:673). No response byte is read while the request is still being sent, so the client always finishes sending and never unilaterally aborts an upload on an early/non-final response. An explicit contrary indication (h1 conn close, h2 RST_STREAM) surfaces as a SendData error, which is the permitted stop. | Satisfied by construction (sequential send-then-receive model); no test exercises an early response arriving mid-upload. <br>_verifiers overturned -> PASS: Confirmed sequential send-then-receive: sendRequest sends HEADERS then writeRequestBody then writeRequestTrailers (client/client.go:589-610) and only after it returns does do() call drainResponse (client/client.go:672-675). The client never reads a response byte mid-upload, so it always finishes sending. The RFC obligation here is a MAY (a client MAY stop sending on an early error response); not implementing the early abort is conformant, and an explicit stop (h1 conn close / h2 RST) still surfaces as a SendData error. Satisfied by construction; the absence of a mid-upload-early-response test is not a genuine gap for a MAY that is structurally guaranteed._ |
| `7-24` | MUST | NOT_APPLICABLE | The client never emits a hop-by-hop control field on h1: WriteRequest strips connection/transfer-encoding/te/proxy-connection/keep-alive/upgrade from the field loop (http1/conn.go:439-443) and adds no Connection header of its own (persistence rides the HTTP/1.1 default). Caller-supplied instances are rejected up front by validateRequest/forbiddenRequestHeader (client/request.go:167-174). h2/h3 forbid the Connection field entirely. | Deliberate scope boundary: connection-control fields are client-managed and caller-forbidden, so the 'also list the field in Connection' duty never triggers — no code path emits such a field. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. WriteRequest strips connection/transfer-encoding/te/proxy-connection/keep-alive/upgrade from the emit loop (http1/conn.go:438-443) and never appends a Connection field of its own -- persistence rides the HTTP/1.1 default (ex.keepAlive = proto == "HTTP/1.1", http1/conn.go:608). A caller-supplied "connection" is rejected up front by forbiddenRequestHeader (client/request.go:169-172). No code path emits a hop-by-hop control field, so the 'also list the field in Connection' duty never triggers. Deliberate scope boundary correctly dismissed._ |
| `7-27` | MUST NOT | NOT_APPLICABLE | The client emits no Connection header at all (http1.WriteRequest never appends one and strips a caller's — http1/conn.go:439-443), and validateRequest rejects a caller-supplied "connection" header via forbiddenRequestHeader (client/request.go:169-172). There is therefore no connection option for the client to name an end-to-end field in. | Deliberate scope: the client never sends a Connection field, so it can never send a prohibited connection option. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. The client emits no Connection header (http1.WriteRequest never appends one and strips a caller's at http1/conn.go:438-443) and rejects a caller-supplied "connection" via forbiddenRequestHeader (client/request.go:169-172). With no Connection field ever sent, there is no connection option in which to name a prohibited/end-to-end field. Correctly N/A._ |
| `7-60` | SHOULD | NOT_APPLICABLE | No protocol-upgrade support exists. The client never validates a 101/Upgrade response: ReadResponse treats any status <200 as a 1xx interim, drains it and loops for a >=200 final (http1/conn.go:590-604), so a 101 is skipped rather than matched. There is no Upgrade-offer state to compare a protocol-name against, case-insensitively or otherwise. | Deliberate scope boundary: HTTP Upgrade is not implemented (caller Upgrade headers are refused), so no case-insensitive protocol-name matching is needed or present. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No protocol-upgrade support exists. ReadResponse treats any status <200 (incl. 101) as a 1xx interim: `if code >= 200` (http1/conn.go:590) breaks for finals; below it the head is drained via consumeHeaders(nil,false) and the loop repeats (http1/conn.go:594-604). No Upgrade-offer state exists to case-insensitively match a 101's protocol name against. Deliberate scope boundary correctly dismissed._ |
| `7-61` | MUST | NOT_APPLICABLE | Obligation applies_to server. On the client side, a 101 is handled as an ordinary 1xx interim — ReadResponse's `if code >= 200` gate (http1/conn.go:590) drains its headers (consumeHeaders, http1/conn.go:601) and loops — so the client neither requires nor inspects an Upgrade field on a 101; it simply keeps waiting for a final response. | Server-side MUST, out of client scope; the client implements no Upgrade/101 handling (101 is treated as a skippable interim). <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed server-side MUST, out of client scope. A 101 is drained as an ordinary 1xx interim (http1/conn.go:590-604); the client neither requires nor inspects an Upgrade field on it and simply keeps waiting for a >=200 final. No client obligation to fail._ |
| `7-62` | MUST | NOT_APPLICABLE | Obligation applies_to server. The client never interprets a 101 Upgrade field as a layered protocol stack — it has no 101/Upgrade processing path at all (101 is drained as a 1xx interim, http1/conn.go:590-604). | Server-side ordering MUST, out of client scope; no Upgrade-stack interpretation is implemented. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed server-side ordering MUST, out of client scope. The client has no 101/Upgrade processing path (101 drained as interim, http1/conn.go:590-604) and never interprets an Upgrade field as a layered protocol stack. Correctly N/A._ |
| `7-63` | MUST NOT | NOT_APPLICABLE | Obligation applies_to server; the client-side reconcile (reject a 101 naming an unoffered protocol) is vacuous here because the client offers no Upgrade — it refuses to send an Upgrade request header (validateRequest/forbiddenRequestHeader, client/request.go:169-172; http1 strips it, http1/conn.go:439-443) and does not process a 101 (drained as interim, http1/conn.go:590-604). | Deliberate scope boundary: with no Upgrade offered and no 101 processing, there is nothing to accept or reject. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. The client offers no Upgrade (validateRequest/forbiddenRequestHeader rejects a caller "upgrade" header, client/request.go:169-172; http1.WriteRequest strips it, http1/conn.go:438-443) and does not process a 101 (drained as interim, http1/conn.go:590-604). With nothing offered and no 101 handling, the reconcile-unoffered-protocol duty is vacuous. Deliberate scope boundary correctly dismissed._ |
| `7-65` | MUST | NOT_APPLICABLE | Obligation applies_to server. The client-side expectation (surface the 426 and its Upgrade list) is met generically without special handling: 426 is >=200, so ReadResponse returns it as a normal final response (http1/conn.go:590-591) and commitHeaderLine appends every response field — including Upgrade — verbatim to the returned headers (http1/conn.go:1243-1248), which parseStatus/handleHeadersEvent surface into resp.Headers (client/response.go:329-345, client/client.go:1054). | Server-side MUST, out of client scope; the caller still receives the 426 status and its Upgrade header through the ordinary response path (no dedicated 426 logic, none required). <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed server-side MUST, out of client scope. A 426 is >=200 so ReadResponse returns it as a normal final (http1/conn.go:590-591) and commitHeaderLine appends every response field including Upgrade verbatim (http1/conn.go:1243-1248), which parseStatus surfaces into resp.Headers (client/response.go:329-345). The caller still receives the 426 and its Upgrade list through the ordinary path; no dedicated 426 logic is required. Correctly N/A._ |
| `7-68` | MUST | NOT_APPLICABLE | The client refuses to send an Upgrade request header entirely: validateRequest/forbiddenRequestHeader rejects it with ErrInvalidRequest (client/request.go:169-172), and http1.WriteRequest strips it should it reach the serializer (http1/conn.go:439-443). Proven by TestConformance_RFC7540_Sec8_1_2_3_ForbidsUpgrade (client/conformance_forbidden_headers_test.go:146-172), which asserts an "upgrade" header yields ErrInvalidRequest. | Deliberate scope boundary: the client never emits Upgrade, so the paired 'Connection: upgrade' obligation never arises; rejection is tested. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. The client refuses to send Upgrade: forbiddenRequestHeader lists "upgrade" (client/request.go:169-172) and http1.WriteRequest strips it (http1/conn.go:438-443). Verified by TestConformance_RFC7540_Sec8_1_2_3_ForbidsUpgrade (client/conformance_forbidden_headers_test.go:146-171), which sends an "upgrade" header and asserts errors.Is(err, ErrInvalidRequest). With no Upgrade ever emitted, the paired Connection:upgrade obligation never arises; rejection is tested. Correctly N/A._ |
| `7-71` | MUST | NOT_APPLICABLE | Obligation applies_to server. The client implements neither proactive Expect: 100-continue negotiation (sendRequest writes the whole body immediately, client/client.go:590-595) nor protocol upgrade. It does tolerate an interim 100: ReadResponse drains any 1xx and loops for the final response (http1/conn.go:590-604), exercised by TestHTTP1_1xx_Response (http1/conn_test.go:535-580). It never performs the subsequent 101 switch. | Server-side sequencing MUST, out of client scope; the client harmlessly drains interim 100 responses but has no Upgrade/101 support to sequence them against. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed server-side sequencing MUST, out of client scope. The client implements neither Expect:100-continue negotiation (sendRequest writes the whole body immediately, client/client.go:589-595) nor Upgrade. It does tolerate an interim 100: ReadResponse drains any 1xx and loops for the final (http1/conn.go:590-604), exercised by TestHTTP1_1xx_Response (http1/conn_test.go:536-582), which sends "100 Continue" then "200 OK" and asserts status 200. It never performs a 101 switch. Correctly N/A._ |

### 8-conditional-range

_33 items: PASS=1, NOT_APPLICABLE=32_

| Item | Level | Status | Evidence | Note |
|------|-------|--------|----------|------|
| `14-18` | MUST | NOT_APPLICABLE | No Range/Content-Range numeral parsing exists in client/ or http1/ (grep for content-range/first-pos/last-pos/suffix/complete-length: zero hits). The client forwards range headers as opaque strings. The analogous numerals it DOES parse are overflow-safe: parseDecimalOctets (http1/conn.go:777, checks n>(MaxInt64-d)/10 before multiply) and parseChunkSize (http1/conn.go:1000, checks before *16). | Range/Content-Range position/length are never parsed (no resume/merge/cache) so no overflow surface exists; the obligation for numerals the client does parse (Content-Length §8.6, chunk-size §7.1) is met and tested (TestConformance_RFC9110_Sec8_6_OverflowContentLengthRejected, http1/conformance_cl_test.go:141; TestConformance_RFC9112_Sec7_1_InvalidChunkSizeRejected overflow case, conformance_chunksize_test.go:49). <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed: no Range/Content-Range numeral parsing anywhere (grep for those tokens hits only hpack/qpack static tables, not semantic code). No overflow surface exists. The numerals the client DOES parse are overflow-guarded: parseDecimalOctets http1/conn.go:788 (n>(MaxInt64-d)/10) and parseChunkSize http1/conn.go:1017 (n>(MaxInt64-d)/16). NA is correct._ |
| `14-22` | MUST | NOT_APPLICABLE | applies_to=server. The client is a user agent, not a server; it never receives/ignores Range. It forwards a caller-supplied Range verbatim (client/request.go:432 emit loop passes regular headers through) and imposes no partial-response assumption of its own. | Server-side obligation; the client's pass-through design already avoids the reconcile risk (it makes no partial-behaviour assumption). <br>_verifiers overturned -> NOT_APPLICABLE: Server obligation. Client is a UA; it never receives+ignores Range and forms no partial-response assumption. It reads any 200 body via ReadBodyChunk (conn.go:1259) with no range-conditional branch. NA correct._ |
| `14-24` | MUST | NOT_APPLICABLE | applies_to=origin. No origin-server role in client/ or http1/; the client never inspects a Range unit. A 200/206/416 response is surfaced verbatim via parseStatus (client/response.go:329). | Origin-server obligation, out of the client's scope. <br>_verifiers overturned -> NOT_APPLICABLE: Origin obligation. No range-unit inspection; 200/206/416 surfaced verbatim via parseStatus (client/response.go:329). NA correct._ |
| `14-27` | SHOULD NOT | NOT_APPLICABLE | The client constructs no Range value — there is no range-set builder, coalescer, or multi-range logic anywhere (grep confirms). The caller composes the entire Range header string, forwarded verbatim (client/request.go:432). | Caller's responsibility: with no range-construction code the library has nothing to coalesce; a deliberate low-level scope boundary. <br>_verifiers overturned -> NOT_APPLICABLE: No range-set builder/coalescer exists (grep confirms). Caller composes the Range string; library forwards headers verbatim. Deliberate low-level scope boundary. NA correct._ |
| `14-29` | SHOULD | NOT_APPLICABLE | Same as 14-27: no range-set assembly/sorting exists; caller supplies the Range value verbatim (client/request.go:432). | Caller's responsibility; the library never orders range-specs because it never builds them. <br>_verifiers overturned -> NOT_APPLICABLE: Same as 14-27: no range-spec assembly/ordering code exists because the library never builds a Range value. NA correct._ |
| `14-32` | SHOULD | NOT_APPLICABLE | applies_to=server. No 206 generation or multipart/byteranges construction in the client. On receive, a 206 is treated as an ordinary status: ReadResponse returns it, ReadBodyChunk (http1/conn.go:1259) reads its body per Content-Length/chunked, and the raw bytes are handed to the caller (client/response.go Body). | Server obligation; the client surfaces 206 verbatim and leaves multipart/byteranges parsing to the caller (deliberate scope). <br>_verifiers overturned -> NOT_APPLICABLE: Server obligation (206/multipart-byteranges generation). No 206 generation; a received 206 is an ordinary status, its body read per Content-Length/chunked (ReadBodyChunk conn.go:1259) and handed to caller. NA correct._ |
| `14-34` | SHOULD | NOT_APPLICABLE | applies_to=server. No 416 generation. A received 416 is surfaced as a normal status/headers pair (client/response.go:329); no Content-Range recompute/retry logic exists. | Server obligation; the client exposes 416 to the caller without acting on it. <br>_verifiers overturned -> NOT_APPLICABLE: Server obligation (416 generation). No 416 generation; received 416 surfaced as a normal status via parseStatus (client/response.go:329). NA correct._ |
| `14-37` | MUST NOT | PASS | No Accept-Ranges handling exists anywhere (grep: zero hits in client/ or http1/), so the client cannot form the forbidden assumption. ReadBodyChunk (http1/conn.go:1259) reads a 200 full body identically to a 206 — a ranged GET answered with 200 is handled with no special-casing. | Satisfied by construction (no Accept-Ranges-conditional branch), but no dedicated test pins the ranged-GET->200 fallback path. <br>_verifiers overturned -> UNTESTED: Not a real gap. The client parses neither Accept-Ranges nor Range (grep: only hpack/qpack static-table hits), so a ranged GET answered with 200 is byte-identical to any plain 200 in ReadBodyChunk (conn.go:1259) — the forbidden assumption cannot be formed. Satisfied by construction and covered by general 200-body tests; a dedicated range->200 test merely isn't named. Honest UNTESTED, but no failing/uncovered behaviour._ |
| `14-41` | MUST NOT | NOT_APPLICABLE | No recombination/merge/resume/cache code exists. A 206 body is handed to the caller as raw Body bytes (client/response.go:26,329); the client never inspects a Content-Range unit nor splices into a stored representation. | Deliberate scope boundary: the client does no recombination, so it cannot violate this; merge/unit-checking is the caller's job. <br>_verifiers overturned -> NOT_APPLICABLE: No recombination/merge/resume/cache path exists; a 206 body is handed to the caller as raw bytes (client/response.go). Client never inspects a Content-Range unit. Deliberate scope boundary. NA correct._ |
| `14-45` | SHOULD | NOT_APPLICABLE | The client never generates Content-Range (no partial-PUT builder; grep zero hits) and never parses it. A caller doing partial PUT composes the Content-Range value itself in Request.Headers, forwarded verbatim (client/request.go:432). | No sender role for Content-Range in the library; complete-length inclusion is the caller's responsibility. <br>_verifiers overturned -> NOT_APPLICABLE: No Content-Range generation (no partial-PUT builder; grep zero semantic hits) and no parsing. Caller composes any Content-Range in Request.Headers, forwarded verbatim. NA correct._ |
| `14-48` | MUST NOT | NOT_APPLICABLE | As 14-41: no Content-Range validation and no recombination path exist. Received content is surfaced verbatim (client/response.go). | Deliberate scope boundary (no resume/merge); the client never recombines, so cannot recombine invalid content. <br>_verifiers overturned -> NOT_APPLICABLE: As 14-41: no Content-Range validation and no recombination path; received content surfaced verbatim. Client cannot recombine invalid content because it never recombines. NA correct._ |
| `14-49` | SHOULD | NOT_APPLICABLE | applies_to=server. No 416 generation. On receive the client does not parse Content-Range: bytes */N (no such code); the 416 headers are passed to the caller (client/response.go:329). | Server obligation; unsatisfied-range parsing left to the caller. <br>_verifiers overturned -> NOT_APPLICABLE: Server obligation (416 generation / bytes*/N). No such code; a received 416's headers are passed to the caller (client/response.go:329). NA correct._ |
| `14-52` | SHOULD | NOT_APPLICABLE | applies_to=origin. No origin role. A 400 to a partial PUT is surfaced as an ordinary status via parseStatus (client/response.go:329); the client never generates it. | Origin-server obligation; the client merely receives and reports the 400. <br>_verifiers overturned -> NOT_APPLICABLE: Origin obligation. Client never generates a 400; it surfaces any received 400 via parseStatus (client/response.go:329). NA correct._ |
| `13-2` | MUST | NOT_APPLICABLE | applies_to=origin. No entity-tag comparison anywhere; the client neither evaluates nor constructs If-Match (grep zero hits). A caller-supplied If-Match value is forwarded verbatim (client/request.go:432) with no weak/strong inspection. | Origin comparison obligation; the client does not validate/strip weak tags in a caller's If-Match (caller's responsibility). <br>_verifiers overturned -> NOT_APPLICABLE: Origin comparison obligation. No entity-tag comparison or If-Match evaluation exists (grep zero semantic hits). A caller's If-Match is forwarded verbatim with no weak/strong inspection. NA correct._ |
| `13-13` | MUST | NOT_APPLICABLE | applies_to=server/cache. No If-None-Match evaluation or entity-tag comparison exists in the client. | Server/cache comparison obligation, out of the client's scope. <br>_verifiers overturned -> NOT_APPLICABLE: Server/cache comparison obligation. No If-None-Match evaluation or ETag comparison in the client. NA correct._ |
| `13-15` | SHOULD | NOT_APPLICABLE | The client deliberately implements no response cache (per project scope); it stores no responses and never revalidates, so it never auto-generates If-None-Match from stored ETags. A caller may add If-None-Match to Request.Headers itself (forwarded at client/request.go:432). | Deliberate scope boundary: no caching layer, so there is nothing to revalidate. <br>_verifiers overturned -> NOT_APPLICABLE: Client implements no response cache (declared scope boundary); it stores nothing and never revalidates, so it never auto-generates If-None-Match from stored ETags. Caller may add the header itself. NA correct._ |
| `13-20` | MUST | NOT_APPLICABLE | applies_to=origin. No precondition evaluation. On receive, a 304 is correctly framed as bodyless (ReadBodyChunk returns done for status 304, http1/conn.go:1266) and 412 is a normal status; both are surfaced to the caller. | Origin obligation; the client's receive-side 304/412 handling is correct (bodiless 304 tested: TestConformance_RFC2616_Sec10_3_5_304NoBody, http1/conformance_test.go:161). <br>_verifiers overturned -> NOT_APPLICABLE: Origin obligation. No precondition evaluation. Receive-side handling is correct: 304 is bodyless (ReadBodyChunk returns done for 304, http1/conn.go:1266); 412 is an ordinary status. NA correct._ |
| `13-24` | MUST | NOT_APPLICABLE | applies_to=server/cache. No If-Modified-Since / If-None-Match evaluation exists in the client (grep zero hits). | Server/cache masking rule, out of the client's scope. <br>_verifiers overturned -> NOT_APPLICABLE: Server/cache masking rule. No If-Modified-Since / If-None-Match evaluation exists (grep zero semantic hits). NA correct._ |
| `13-25` | MUST | NOT_APPLICABLE | applies_to=server/cache. The client neither validates nor generates If-Modified-Since; a caller composes the date and method. No IMS code exists. | Server/cache obligation; well-formed single-date-on-GET/HEAD emission is the caller's responsibility. <br>_verifiers overturned -> NOT_APPLICABLE: Server/cache obligation. No IMS validation/generation; caller sources the date and method. NA correct._ |
| `13-27` | MUST | NOT_APPLICABLE | applies_to=server/cache. No timestamp interpretation of IMS; the client has no clock-comparison logic for conditionals. | Server/cache obligation; the caller sources the IMS value (e.g. from a prior Last-Modified). <br>_verifiers overturned -> NOT_APPLICABLE: Server/cache obligation. No timestamp/clock-comparison logic for conditionals in the client. NA correct._ |
| `13-28` | SHOULD | NOT_APPLICABLE | applies_to=origin. No origin evaluation of IMS. The client always handles a full 200 response body (http1/conn.go:1259). | Origin obligation; the client cannot rely on 304 and its 200 path is unconditional. <br>_verifiers overturned -> NOT_APPLICABLE: Origin obligation. No origin IMS evaluation; the client's 200 body path (ReadBodyChunk conn.go:1259) is unconditional and it never relies on emitting 304. NA correct._ |
| `13-31` | SHOULD | NOT_APPLICABLE | applies_to=origin. No 304 generation. Receive-side, the client accepts a bodiless 304 with sparse headers: ReadBodyChunk returns (0,true,nil) for 304 (http1/conn.go:1266) and a 304 carrying Content-Length stays poolable (http1/conn.go:678). | Origin obligation; client's related receive handling is correct and tested (TestConformance_RFC9110_Sec8_6_ContentLengthOn304StaysPoolable, http1/conformance_bodyless_test.go:76). <br>_verifiers overturned -> NOT_APPLICABLE: Origin obligation (304 generation). Receive-side is correct: 304 is bodyless (conn.go:1266) and a 304 carrying Content-Length stays poolable (conn.go:678, tested per RFC_COVERAGE). NA correct._ |
| `13-34` | MUST | NOT_APPLICABLE | applies_to=server/cache. No If-Unmodified-Since / If-Match evaluation exists in the client. | Server/cache masking rule, out of the client's scope. <br>_verifiers overturned -> NOT_APPLICABLE: Server/cache masking rule. No If-Unmodified-Since / If-Match evaluation exists. NA correct._ |
| `13-35` | MUST | NOT_APPLICABLE | applies_to=server/cache. No IUS validation/generation; caller composes any IUS value, forwarded verbatim (client/request.go:432). | Server/cache obligation; single-valid-date emission is the caller's responsibility. <br>_verifiers overturned -> NOT_APPLICABLE: Server/cache obligation. No IUS validation/generation; caller composes any IUS value, forwarded verbatim. NA correct._ |
| `13-37` | MUST | NOT_APPLICABLE | applies_to=server/cache. No IUS timestamp interpretation or clock logic in the client. | Server/cache obligation, out of the client's scope. <br>_verifiers overturned -> NOT_APPLICABLE: Server/cache obligation. No IUS timestamp interpretation or clock logic in the client. NA correct._ |
| `13-48` | MUST NOT | NOT_APPLICABLE | The client never synthesizes If-Range: validateRequest (client/request.go:183) checks token/injection/forbidden-header safety only, with no conditional-header construction or Range/If-Range coupling check (grep for if-range: zero hits). Any If-Range is caller-supplied and forwarded verbatim (client/request.go:432). | Caller's responsibility: the library generates no If-Range, so cannot violate MUST-NOT-generate; note it also provides no defensive builder guard against a caller pairing If-Range without Range (a semantic check the low-level layer deliberately omits). <br>_verifiers overturned -> NOT_APPLICABLE: Client synthesizes no If-Range: validateRequest (client/request.go:183) does token/injection/forbidden-header checks only, with no conditional-header construction and no Range/If-Range coupling logic. Any If-Range is caller-supplied and forwarded verbatim. Cannot violate a MUST-NOT-generate it never exercises. NA correct._ |
| `13-51` | MUST NOT | NOT_APPLICABLE | Same as 13-48: no If-Range generation and no entity-tag weak/strong inspection anywhere; validateRequest (client/request.go:183) does not parse ETag markings. Caller-supplied value forwarded verbatim. | Caller's responsibility; the library never generates If-Range and does not inspect weak-tag semantics (deliberate scope). No defensive guard exists. <br>_verifiers overturned -> NOT_APPLICABLE: As 13-48: no If-Range generation and no ETag weak/strong inspection; validateRequest (request.go:183) does not parse ETag markings. Caller-supplied value forwarded verbatim. NA correct._ |
| `13-52` | MUST NOT | NOT_APPLICABLE | Same as 13-48/13-51: no If-Range generation, no ETag-vs-date preference logic, no strong-validator (§8.8.2.2) check. Caller composes If-Range verbatim (client/request.go:432). | Caller's responsibility; validator selection for If-Range is not something the low-level client performs. <br>_verifiers overturned -> NOT_APPLICABLE: As 13-48/13-51: no If-Range generation, no ETag-vs-date preference or strong-validator selection logic. Validator selection is not a low-level-client responsibility. NA correct._ |
| `13-56` | MUST | NOT_APPLICABLE | applies_to=server/cache. No If-Range condition evaluation. Receive-side the client handles a full 200 where 206 was hoped for: ReadBodyChunk reads a 200 body normally (http1/conn.go:1259) and the caller sees Status=200. | Server/cache obligation; the client's unconditional 200 handling already satisfies the reconcile note. <br>_verifiers overturned -> NOT_APPLICABLE: Server/cache obligation (If-Range evaluation). Client's unconditional 200 handling (ReadBodyChunk conn.go:1259) already tolerates a full 200 where 206 was hoped for; Status=200 surfaced to caller. NA correct._ |
| `13-57` | SHOULD | NOT_APPLICABLE | applies_to=server/cache. No If-Range processing. A 206 is surfaced verbatim (client/response.go:329) and a 200 is tolerated (http1/conn.go:1259). | Server/cache obligation; client tolerates both 200 and 206. <br>_verifiers overturned -> NOT_APPLICABLE: Server/cache obligation. No If-Range processing; both a 206 (parseStatus response.go:329) and a 200 (conn.go:1259) are tolerated and surfaced verbatim. NA correct._ |
| `13-60` | MUST | NOT_APPLICABLE | applies_to=server. No precondition evaluation exists, so the client draws no conclusions about preconditions from any status. All statuses (2xx/3xx/4xx/5xx) are surfaced verbatim via parseStatus (client/response.go:329). | Server obligation; the client's lack of precondition inference means the reconcile risk cannot arise. <br>_verifiers overturned -> NOT_APPLICABLE: Server obligation. No precondition evaluation, so the client draws no precondition inference from any status; all 2xx/3xx/4xx/5xx surfaced verbatim via parseStatus (response.go:329). NA correct._ |
| `13-63` | MUST | NOT_APPLICABLE | applies_to=server. No server-side ignoring of conditionals. The client never auto-attaches conditional headers to CONNECT/OPTIONS/TRACE (no conditional-header generation at all); any such header would be caller-supplied and forwarded verbatim (client/request.go:432). | Server obligation; the client adds no conditionals of its own, so nothing improper is attached by the library. <br>_verifiers overturned -> NOT_APPLICABLE: Server obligation. The client generates no conditional headers of its own for any method (CONNECT/OPTIONS/TRACE included); any such header is caller-supplied and forwarded verbatim. Nothing improper is attached by the library. NA correct._ |
| `13-65` | MUST | NOT_APPLICABLE | applies_to=cache/origin. No precondition evaluation engine exists in the client; there is no ordered If-Match->IUS->If-None-Match->IMS->If-Range logic to implement (grep confirms absence). | Cache/origin evaluation-order obligation, out of the client's scope. <br>_verifiers overturned -> NOT_APPLICABLE: Cache/origin evaluation-order obligation. No precondition engine exists — no If-Match->IUS->If-None-Match->IMS->If-Range ordering to implement (grep confirms absence). NA correct._ |

### 9-negotiation-auth

_7 items: PASS=1, NOT_APPLICABLE=6_

| Item | Level | Status | Evidence | Note |
|------|-------|--------|----------|------|
| `11-5` | MUST | NOT_APPLICABLE | No WWW-Authenticate/challenge parsing exists anywhere in client/, http1/, or conn/ (grep for www-authenticate/realm/challenge/credential returns only conn/proxy.go and test/example strings). The only credentials the stack emits are Proxy-Authorization: Basic <base64> at conn/proxy.go:82-89 (token68 form, no auth-params). | Client performs no auth challenge/response negotiation (deliberate scope: reacting to 401/407 is the caller's job), and Basic emits a token68 with zero auth-params, so it cannot repeat a param name. Neither half of the obligation is reachable. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No WWW-Authenticate/challenge parser exists in client/, http1/, or conn/ (grep for www-authenticate/realm/challenge returns only docs and hpack/qpack static-table NAME entries, never emission). The stack's sole credential emission is conn/proxy.go:82-89 'Proxy-Authorization: Basic '+base64(user:pass), a token68 with zero auth-params, so it structurally cannot repeat an auth-param name. Reacting to 401/407 is the caller's job; neither half of the obligation is reachable. NA stands._ |
| `11-24` | MUST | NOT_APPLICABLE | The client never generates a realm auth-param. Its sole credential emission is Basic token68 (conn/proxy.go:88: "Proxy-Authorization: Basic " + base64); no auth-param serialization (realm= or otherwise) exists in the codebase. | No realm is ever emitted, so the quoted-string-only obligation has nothing to constrain. Auth-param serialization is out of scope for this load-gen client. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. The client never serializes a realm (or any) auth-param. conn/proxy.go:87-88 emits only Basic token68; no realm= or auth-param serialization exists anywhere in source. The quoted-string-only obligation has nothing to constrain. NA stands._ |
| `12-12` | MUST NOT | NOT_APPLICABLE | The only negotiation header the client auto-emits is the fixed literal acceptEncodingValue = "gzip, deflate, br, zstd" (client/decompress.go:58), which contains no q parameter. Caller-supplied weights are passed through verbatim and never rounded/reserialized — TestDecompress_CustomAcceptEncodingPreserved (client/decompress_test.go:226-248) asserts "br, gzip;q=0.8" reaches the wire unchanged. | Client generates no qvalue of its own; caller-supplied qvalues are passed through untouched, so there is no client-produced qvalue whose decimal digits it could over-generate. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. The only auto-emitted negotiation value is the fixed literal acceptEncodingValue at decompress.go:58 ('gzip, deflate, br, zstd') which carries no q parameter. Caller-supplied weights pass through verbatim (shouldSendAcceptEncoding, decompress.go:392-398, leaves the header untouched; TestDecompress_CustomAcceptEncodingPreserved asserts 'br, gzip;q=0.8' reaches the wire unchanged). The client produces no qvalue whose decimal digits it could over-generate. NA stands._ |
| `12-18` | SHOULD | NOT_APPLICABLE | No Accept header is ever auto-generated: grep for "accept" in source shows it only in caller-supplied test/example headers (e.g. client/sugar.go doc comments, e2e tests). The client does no proactive content negotiation; Accept serialization does not exist in client/ or http1/. | Client never serializes an Accept field, so the ordering of q within a media-range never arises. Proactive content negotiation is an explicit non-goal; Accept is entirely caller-supplied and passed verbatim. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. No Accept header is ever auto-generated; grep for accept in source shows Accept only in caller-supplied test/example headers. Proactive content negotiation is an explicit non-goal (CLAUDE.md 'Behaviours the client may deliberately NOT implement'). Accept is entirely caller-supplied and passed verbatim, so ordering of q within a media-range never arises. NA stands._ |
| `12-19` | SHOULD | NOT_APPLICABLE | The client does no Accept/proactive-negotiation parsing. Response headers are surfaced to the caller verbatim: parseStatus (client/response.go:329-346) appends all non-pseudo header fields to the caller's slice without interpreting q parameters. No qvalue/weight parser exists. | Client never parses Accept (or any weighted field) as a recipient — it hands raw header fields to the caller — so there is no parser that could mis-order the q parameter. Deliberate scope boundary. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. The client parses no weighted field as a recipient. Response header fields are surfaced to the caller raw (non-pseudo fields appended without interpretation); detectEncoding (decompress.go:182-202) is the only response-header interpreter and it does exact/EqualFold coding-token matching, no q parsing. No qvalue parser exists to mis-order. NA stands._ |
| `12-37` | SHOULD | NOT_APPLICABLE | Obligation targets an origin server; this is a client, so it is out of role. The reconcile guardrail (consume an identity response) is met: detectEncoding returns EncodingIdentity when no content-encoding header is present (client/decompress.go:177/198/201), decompressBody returns src unchanged for EncodingIdentity (client/decompress.go:266/273), and the auto accept-encoding never marks identity unacceptable (no identity;q=0 in decompress.go:58). TestDecompress_AcceptEncodingSent (client/decompress_test.go:174-196) exercises a plain uncompressed "ok" body being consumed. | Client is not an origin server (out of role). The client-side guardrail — always able to consume an unencoded/identity body — holds and is tested; identity is never advertised as unacceptable. <br>_verifiers overturned -> NOT_APPLICABLE: Confirmed. Obligation targets an origin server; this is a client, out of role. The client-side guardrail holds and is tested: detectEncoding returns EncodingIdentity when no content-encoding is present (decompress.go:198/201), newDecompressingReader/decompressFully return the body unchanged for EncodingIdentity (decompress.go:273/326), and the auto accept-encoding at line 58 never marks identity unacceptable (no identity;q=0). TestDecompress_AcceptEncodingSent consumes a plain 'ok' body. NA stands._ |
| `12-44` | MUST NOT | PASS | No Accept-Language codepath exists: grep for accept-language across client/, http1/, conn/ returns zero source hits. The only header the client auto-adds is accept-encoding (client/decompress.go:388-399 shouldSendAcceptEncoding). Any Accept-Language must be supplied explicitly by the caller via Request.Headers (which is the required user control). | Code satisfies the MUST NOT (never emits Accept-Language by default; caller configuration is the user control), but no test pins the absence of an auto-added Accept-Language header. <br>_verifiers overturned -> NOT_APPLICABLE: Not a genuine gap. No Accept-Language codepath exists in client/, http1/, or conn/ (grep hits only docs and hpack/qpack static-table name entries, which are compression-table constants, not emission). The only auto-added request header is accept-encoding (shouldSendAcceptEncoding, decompress.go:388-399). The MUST NOT (never auto-emit Accept-Language) is satisfied by construction, and content negotiation is an explicit non-goal; caller-supplied headers pass through verbatim (demonstrated generically by TestDecompress_CustomAcceptEncodingPreserved). The judge's UNTESTED is technically true (no dedicated assertion pins the absence) but pinning the absence of a header the client has no code to emit is vacuous; better classified NOT_APPLICABLE. Either way not a real deficiency._ |

### 10-identifiers-conf

_21 items: PASS=14, PARTIAL=1, NOT_APPLICABLE=6_

| Item | Level | Status | Evidence | Note |
|------|-------|--------|----------|------|
| `2-3-9` | MUST NOT | PASS | Send-side ABNF validation is enforced in two layers. client/request.go validateRequest (lines 183-259) rejects a non-token method (containsAnyWhitespace), whitespace/injection in path, non-token header names (isTokenName, tokenChar table lines 291-329), and CR/LF/NUL in field values and pseudo-header values (hasFieldInjectionByte, firstInjectionPseudoField). http1/conn.go validateFields (lines 326-356) re-validates method (validToken), request-target (validRequestTarget rejects <=0x20 and DEL), authority and every field name/value before the first byte is written (WriteRequest calls it at line 415). Emitted chunk-size uses strconv.FormatInt base-16 (line 491) so it is always 1*HEXDIG. Tested: http1/injection_test.go (TestConformance_RFC9110_Sec5_5_HeaderValueCRLF_NotWritten, _NUL_, _AuthorityCRLF_, Sec5_6_2_HeaderNameToken, Sec3_RequestLine) assert nothing reaches the wire; client/conformance_reqvalue_test.go + conformance_reqname_test.go assert validateRequest rejects on the shared H2/H3 path. | Every wire-bound protocol element (method, target, version fixed to HTTP/1.1, field names, field values, chunk sizes) is validated against its ABNF before emission, with header-injection guards, and is covered by conformance + fuzz tests. |
| `2-3-10` | MUST NOT | PASS | http1/conn.go WriteRequest (lines 427-430) always emits origin-form: request-line = method+" "+path+" HTTP/1.1\r\n" with a separate "Host: "+authority line. It never emits absolute-form (no scheme://authority in the request-target) and never emits a status line or other server-only construct. Pseudo-headers are consumed, not emitted (lines 434-436). Origin-form is asserted by http1/injection_test.go TestConformance_RFC9112_Sec3_2_EmptyAuthorityEmptyHost (wire must start "GET / HTTP/1.1\r\n") and Sec5_5_LegalValuesUnaffected. | Client emits origin-form request-targets and a Host field only (no absolute-form, no status lines); the request-target content is the caller-supplied :path, but the framing role obligation is met by construction. |
| `2-3-13` | SHOULD | PASS | Response parsing is bounded by construction. http1/conn.go readLine (lines 529-542) uses bufio.ReadSlice into a fixed 16 KiB buffer (readBufSize) and converts bufio.ErrBufferFull into ErrResponseTooLarge, so a never-terminated status/header/chunk-size line costs bounded memory. consumeHeaders (lines 1034-1093) charges each line len+hpackFieldOverhead against maxHeaderListBytes (8 MiB) to bound an endless-short-lines flood. Content-Length (parseDecimalOctets), chunk-size (parseChunkSize) and Transfer-Encoding (lastTransferCoding) are parsed strictly with overflow checks. Peer input never sizes an allocation. Tested: http1/recvbounds_test.go (StatusLineNeverDelimited, HeaderLineNeverDelimited, EndlessHeaders, chunk-size never delimited — all assert bounded consumption + un-poolable) and http1/fuzz_test.go (FuzzReadResponse). | Defensive parsing with bounded, structural buffers and strict grammars; extensively fuzz- and conformance-tested. |
| `2-3-14` | MUST | PASS | Parse-side caps: 16 KiB per line (readBufSize, http1/conn.go:106) and 8 MiB per header block (maxHeaderListBytes:118) — both comfortably exceed anything the client emits in normal use. However, the send path (WriteRequest, validateFields) enforces NO explicit length cap on emitted request-line, path, or header lines, so the reconcile's 'enforce emission caps <= parse caps' is not implemented as an explicit invariant: a caller could construct a header line longer than 16 KiB that the client would emit but would reject on receive. | The parse limits exceed the client's realistic emissions, but there is no explicit coupling guaranteeing emission caps <= parse caps; a >16 KiB caller-supplied element is a theoretical send/parse asymmetry. <br>_verifiers overturned -> PASS: The 'emission caps <= parse caps' coupling is a reconcile-invented invariant, not an RFC obligation. The real obligation — bound received protocol elements — is met by construction: readLine uses bufio ReadSlice (http1/conn.go:530) capped at readBufSize=16 KiB per line (:106) and maxHeaderListBytes=8 MiB per block (:118,:1088-1093). The send path (WriteRequest:427-445) emits caller-supplied bytes; no RFC requires a client to cap what its own caller hands it below its parse limits. No genuine gap._ |
| `2-3-15` | MUST | PASS | http1/conn.go interprets received elements per RFC semantics with no bespoke reinterpretation: 1xx skipped and looped (ReadResponse lines 590-604), 204/304 bodyless handling (ReadBodyChunk 1265-1268; ReadResponse 668-697 following RFC 9110 §8.6 / RFC 9112 §6.3 rule 1), TE-over-CL precedence (resolveContentLength 875-892, commitHeaderLine 1170-1240 per §6.3 rules 3/4/5), HTTP/1.1 vs 1.0 keep-alive default (line 608). Demonstrated by the RFC2616/RFC9110/RFC9112 conformance suite in http1/ (conformance_cl_test.go, conformance_test.go, conformance_bodyless_test.go, conformance_obsfold_test.go) mapped in docs/RFC_COVERAGE.md. | Status codes and framing headers are interpreted per defined semantics; deviations (keepAlive=false on ambiguous framing) are safety measures, not reinterpretations of meaning. |
| `4-5` | RECOMMENDED | PASS | Send side: no cap on :path/:authority length — WriteRequest (http1/conn.go:427) interpolates path directly; client buildHeaders (client.go:845-860) sets pseudo-headers without length limits. Recv side: a single URI-valued header line (e.g. Location) is bounded only by readBufSize = 16 KiB (>> 8000), and the whole block by 8 MiB, so an 8000-octet URI field parses fine. No dedicated test asserts an 8000-octet URI is accepted/emitted; the nearest is a fuzz seed with an 8000-byte set-cookie value (http1/fuzz_test.go:198), which only adds it to the corpus. | Structurally supports >=8000-octet URIs on both send (uncapped) and receive (16 KiB line limit), but no explicit conformance test pins the 8000-octet threshold. <br>_verifiers overturned -> PASS: Supported by construction, which is what the SHOULD requires. Receive: a single URI-valued header line is accepted up to readBufSize=16 KiB (http1/conn.go:106,:530), comfortably >8000 octets; block cap 8 MiB. Send: :path/:authority are interpolated uncapped (WriteRequest:428-429). The obligation is 'support', not 'have a dedicated test'; the absence of a threshold-pinning test is cosmetic, not a conformance failure._ |
| `4-8` | MUST NOT | NOT_APPLICABLE | The client never generates an http/https URI in absolute-form: WriteRequest (http1/conn.go:427-430) emits origin-form request-targets plus a Host field derived from :authority. An empty :authority yields the literal 'Host: ' line, which RFC 9112 §3.2 explicitly requires for an authority-less target (pinned by http1/injection_test.go TestConformance_RFC9112_Sec3_2_EmptyAuthorityEmptyHost). No code path serializes a scheme://host URI, so an empty-host http URI cannot be generated. | Client emits no absolute-form URIs; empty-host emptiness is only expressible as an empty Host, which is the §3.2-mandated behaviour, not a generated http URI. How :authority is populated is the caller's responsibility (H2-style pseudo-header API). <br>_verifiers overturned -> NOT_APPLICABLE: NA is correct. WriteRequest (http1/conn.go:427-430) emits origin-form request-target + a Host derived from :authority; no code path serializes a scheme://host absolute-form URI, so an empty-host http URI is never generated. Empty :authority yields 'Host: ' which is the §3.2-mandated output, pinned by http1/injection_test.go:266 TestConformance_RFC9112_Sec3_2_EmptyAuthorityEmptyHost. Authority population is the caller's responsibility (H2-style pseudo-header API)._ |
| `4-9` | MUST | NOT_APPLICABLE | The client parses no URI references from responses: there is no net/url usage in client/ (grep confirmed) and no Location/redirect processing anywhere in the client or http1 layers. Redirects are a deliberate non-goal (CLAUDE.md / docs scope: no auto-following redirects). | Out of scope by design — the client never dereferences or validates a received http URI reference (no redirect following, no URI-valued response-field parsing), so there is no empty-host URI for it to reject. <br>_verifiers overturned -> NOT_APPLICABLE: NA is correct — deliberate scope boundary. No net/url in client/, no Location/redirect processing anywhere in client or http1; redirects are an explicit non-goal (CLAUDE.md). The client dereferences no received URI reference, so there is no empty-host http URI for it to reject._ |
| `4-12` | MUST NOT | NOT_APPLICABLE | Same as 4-8: no absolute-form https URI is ever generated. WriteRequest emits origin-form + Host only; :scheme=https is carried only as the H2 :scheme pseudo-header (client/client.go buildHeaders:846-858) and is dropped by http1 WriteRequest, never serialized into a URI. No path constructs 'https://<emptyhost>'. | Client generates no https URI references, so it cannot generate one with an empty host; authority content is caller-supplied. <br>_verifiers overturned -> NOT_APPLICABLE: NA is correct, same basis as 4-8. :scheme=https is carried only as the H2 :scheme pseudo-header (client/client.go buildHeaders:856-857) and is dropped by http1 WriteRequest; no path constructs an absolute-form https URI, so an empty-host https URI cannot be generated._ |
| `4-13` | MUST | NOT_APPLICABLE | Identical to 4-9: no URI references are parsed from responses (no net/url in client, no Location/redirect handling). The client never processes an https URI reference from the peer. | Out of scope by design (no redirect following / no URI-valued response-field parsing); there is no received https URI to reject. <br>_verifiers overturned -> NOT_APPLICABLE: NA is correct, same basis as 4-9. No URI references are parsed from responses (no net/url, no Location handling); the client never processes a received https URI reference, so there is none to reject._ |
| `4-14` | MUST | PASS | When a TLS dialer is used (conn.TLSDialer / FlexDialer, conn/dial.go:23-120), the request is written only after tls.Dialer.DialContext completes the handshake, and the response is read from that same *tls.Conn — so on a secured transport the obligation holds and crypto/tls verifies the peer. BUT the client applies no coupling between :scheme=https and transport security: :scheme is an opaque pseudo-header (buildHeaders) while the dialer is chosen independently (constructors.go), so a caller can send :scheme=https over a conn.PlaintextDialer (H2C) or a plain TCP HTTP/1.1 connection and nothing objects. | TLS-transported https requests are secured and verified, but the library does not enforce https-scheme => secured-transport; a misconfigured caller can send an https-scheme request over cleartext. <br>_verifiers overturned -> NOT_APPLICABLE: Refuted as a library gap: scheme-to-transport coupling is the caller's responsibility in this low-level API. :scheme is opaque metadata set independently by the caller (buildHeaders:846-857) and the dialer is chosen explicitly by the caller (constructors.go NewSingleConnClient/NewH1Client etc.). Sending :scheme=https over an H2C/plaintext dialer is a legitimate load-gen configuration, not something the library must forbid. When a TLS dialer IS used, verification is default-on (conn/dial.go:44-45 never sets InsecureSkipVerify). No genuine library defect; the described case is caller misconfiguration._ |
| `4-24` | MUST NOT | PARTIAL | The client emits the Host field verbatim from :authority (http1/conn.go WriteRequest:429; validateFields:338 checks authority only for CR/LF/NUL, not for a userinfo '@'). client/request.go validateRequest likewise never inspects Authority for '@'. So a caller-supplied :authority of 'user@host' is emitted as 'Host: user@host'. The client does not synthesize userinfo itself and does not parse URIs, but it also does not strip a caller-supplied userinfo. Sibling divergence: the http3 send path DOES reject authority userinfo (http3/requestvalidate_test.go TestConformance_RFC9114_Sec431_AuthorityUserinfoAndHostMatch); the shared http1/client path has no equivalent. | Client never generates userinfo, but it passes a caller-supplied 'user@host' authority through into Host unstripped, unlike the http3 sibling — an unguarded edge, not a URI the client constructs. <br>_CONFIRMED by both verifiers_ |
| `4-25` | SHOULD | NOT_APPLICABLE | Targets server-supplied URIs (redirect targets / other received URI-valued fields). The client follows no redirects and parses no response URIs (no net/url in client, no Location handling), so it never consumes an untrusted http/https URI in which to detect userinfo. | Out of scope by design (no redirect following); there is no received URI for the client to parse for userinfo. <br>_verifiers overturned -> NOT_APPLICABLE: NA is correct — deliberate scope boundary. The client follows no redirects and parses no response URIs (no net/url, no Location handling), so it never consumes a server-supplied http/https URI in which to detect userinfo._ |
| `4-42` | MUST | PASS | TLS identity verification is delegated to Go crypto/tls. conn.TLSDialer.Dial / FlexDialer.Dial (conn/dial.go:23-120) build a tls.Dialer from the caller's *tls.Config and never set InsecureSkipVerify (grep confirms the library sets it only in tests/examples). tls.Dialer populates ServerName from the dial addr when the config leaves it empty, so the handshake verifies the certificate against the target host before WriteRequest runs. No poseidon-level test asserts a hostname mismatch is rejected. | Service-identity verification against the URI host is performed by crypto/tls by delegation (verification is default-on), but no library test exercises the reject path. <br>_verifiers overturned -> PASS: Correct by delegation and default-on. The dialers build a tls.Dialer from the caller's *tls.Config and never disable verification (conn/dial.go:44-45, :108; InsecureSkipVerify appears only in tests/examples). tls.Dialer populates ServerName from the dial addr when the config leaves it empty, so crypto/tls verifies the cert against the target host before WriteRequest runs. Service-identity verification is performed; testing the reject path would exercise crypto/tls internals, which is out of the library's scope._ |
| `4-43` | MUST | PASS | Delegated to crypto/tls, whose certificate verification implements RFC 6125-conformant name matching (Certificate.VerifyHostname). The dialers (conn/dial.go) supply the caller's config to tls.Dialer without disabling verification. No poseidon test exercises RFC 6125 matching. | RFC 6125 verification is provided by crypto/tls; correct by delegation, untested at the library level. <br>_verifiers overturned -> PASS: Correct by delegation. RFC 6125-conformant name matching is crypto/tls Certificate.VerifyHostname; the library supplies the caller's config to tls.Dialer without overriding verification (conn/dial.go). No manual reference-identity logic to get wrong. Redundant to test the stdlib._ |
| `4-44` | MUST | PASS | Delegated to crypto/tls: it branches on host form, matching an IP literal against SAN iPAddress (IP-ID) and a DNS name against SAN dNSName (DNS-ID) via VerifyHostname. The library does no manual reference-identity construction and does not override this. No library test. | IP-ID/DNS-ID reference-identity construction is handled by crypto/tls; correct by delegation, no library test. <br>_verifiers overturned -> PASS: Correct by delegation. IP-ID vs DNS-ID reference-identity construction is handled by crypto/tls VerifyHostname (SAN iPAddress vs dNSName); the library does no manual reference-identity construction and does not override verification. Out-of-scope to re-test the stdlib._ |
| `4-45` | MUST NOT | PASS | Delegated to crypto/tls, which since Go 1.15 ignores the certificate Subject CommonName entirely and matches SAN only. The library never implements its own CN fallback (no custom VerifyPeerCertificate / verification in conn/dial.go). No library test. | CN-ID is never used because crypto/tls is SAN-only; correct by delegation, untested at the library level. <br>_verifiers overturned -> PASS: Correct by delegation. crypto/tls has ignored Subject CommonName since Go 1.15 and matches SAN only; the library never implements a CN-ID fallback and installs no custom VerifyPeerCertificate (conn/dial.go). CN-ID is correctly never used. Out-of-scope to test the stdlib._ |
| `4-48` | MUST | NOT_APPLICABLE | applies_to = user-agent; this is the interactive user-agent branch ('obtain confirmation from the user'). poseidon is a non-interactive library client with no user to prompt; the applicable branch for an automated client is terminate-on-invalid-certificate, covered by 4-49/4-50. | Interactive-UA obligation; not applicable to a library client — the relevant automated-client branch (terminate) is assessed under 4-50. <br>_verifiers overturned -> NOT_APPLICABLE: NA is correct. This is the interactive user-agent branch ('obtain confirmation from the user'); poseidon is a non-interactive library with no user to prompt. The applicable automated-client branch — terminate on invalid certificate — is the default (assessed under 4-50) and is satisfied._ |
| `4-49` | MUST | PASS | A certificate-verification failure surfaces to the caller rather than being swallowed: tls.Dialer.DialContext returns the verification error, TLSDialer.Dial (conn/dial.go:45-47) returns it, conn.Dial (line 136-138) propagates it, and client.Do returns it to the caller (the caller owns logging/metrics/hooks). The library provides Hooks and Metrics for observability. No test asserts a cert error is surfaced via the logging/hooks path. | Cert errors are returned to the caller (not silently dropped), so an audit log is feasible; but the library itself has no audit log and no test pins the surfacing. <br>_verifiers overturned -> NOT_APPLICABLE: Cert-verification errors are surfaced, not swallowed: tls.Dialer.DialContext returns the error, TLSDialer.Dial (conn/dial.go:46-47) returns it, and it propagates to client.Do. Maintaining an audit log is the caller's responsibility (Hooks/Metrics are provided for exactly this); the library correctly hands the error up rather than dropping it. No in-scope obligation the library fails._ |
| `4-50` | SHOULD | PASS | Default behaviour on cert failure is abort-and-return: crypto/tls fails the handshake, tls.Dialer returns the error, and TLSDialer.Dial (conn/dial.go:45-47) / conn.Dial (140-144, which closes the transport on NewClientConn failure) propagate it — no request is sent, the connection is not used. No knob re-enables a failed handshake. No library test exercises the abort path. | Cert failure aborts the connection and returns the error by default (via crypto/tls delegation); no library test covers it. <br>_verifiers overturned -> PASS: Correct by delegation and default. On cert failure crypto/tls fails the handshake, tls.Dialer returns the error, and conn.Dial closes the transport on any post-dial failure (dial.go:140-144) — no request is sent, the connection is not used. No knob re-enables a failed handshake. The abort-and-return behaviour is the default; testing it would exercise crypto/tls's reject path (stdlib scope)._ |
| `4-52` | MUST | PASS | The library exposes no verification-disable setting of its own — it never sets InsecureSkipVerify (grep: only in tests/examples), and TLS verification is default-on through the caller's *tls.Config. The antecedent ('if a disable setting exists') is not met by the library; verification-enabled is the default and reachable state. No test asserts this policy. | No library-level disable knob exists and verification is default-on, so the requirement is satisfied vacuously and by default; untested. <br>_verifiers overturned -> NOT_APPLICABLE: The antecedent ('if a verification-disable setting exists') is not met: the library exposes no disable knob and never sets InsecureSkipVerify (grep: tests/examples only). Verification is default-on through the caller's *tls.Config. The requirement is satisfied vacuously; there is nothing for a library test to pin._ |

