# x

_Generated from local RFC source via FABLE extraction + double independent Opus verification (default-refute). Every quote is verbatim from the fetched RFC 9113 text and confirmed against the source by two verifiers. Rows flagged VERIFY had a verifier disagreement -- correction inline, needs a manual source check._

### 9113 section 1-3 - Introduction, Protocol Overview, Terminology, Starting HTTP/2 (version id, https, prior knowledge, connection preface)

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `1-3-1` | informative | RFC 9113 obsoletes RFC 7540 and RFC 8740 and is the current HTTP/2 specification. <br>_(all [CLIENT])_ | This document obsoletes RFCs 7540 and 8740. Appendix B lists notable changes. | docs/RFC_COVERAGE.md + conn/ — conformance tests are named TestConformance_RFC7540_*; verify each pinned behavior still matches the 9113 text, not the obsoleted 7540 wording. |
| `1-3-2` | informative | HTTP/2 is a connection-oriented application-layer protocol over TCP, and the client is the TCP connection initiator. <br>_(client [CLIENT])_ | HTTP/2 is a connection-oriented application-layer protocol that runs over a TCP connection ([TCP]). The client is the TCP connection initiator. | client/ — transports dial TCP; conn/ assumes client role throughout (odd stream IDs, no accept path). |
| `1-3-3` | informative | Each HTTP request/response exchange is associated with its own stream, and streams are largely independent so one blocked exchange does not stall others. <br>_(all [CLIENT])_ | Multiplexing of requests is achieved by having each HTTP request/response exchange associated with its own stream (Section 5). | conn/ — each Do/DoStream gets its own Stream; verify a stalled stream (flow-control blocked) never blocks progress on sibling streams except via shared wmu write serialization. |
| `1-3-4` | informative | Flow control restricts transmitted data to what the receiver is able to handle, enabling efficient multiplexing. <br>_(receiver,sender [CLIENT])_ | Flow control (Section 5.2) ensures that it is possible to efficiently use multiplexed streams by restricting data that is transmitted to what the receiver is able to handle. | conn/ — acquireSendCredits must respect both per-stream and connection send windows; recv side refunds via WINDOW_UPDATE (recvWindowRefundThreshold batching). |
| `1-3-5` | informative | RFC 9113 deprecates the RFC 7540 priority signaling scheme (PRIORITY frame / HEADERS priority fields). <br>_(all [CLIENT])_ | This revision of HTTP/2 deprecates the priority signaling scheme from [RFC7540]. | conn/ + frame/ — client should not emit PRIORITY frames or set PRIORITY flag on HEADERS; frame/ must still parse/tolerate inbound PRIORITY frames per later sections (deprecated, not forbidden to receive). |
| `1-3-6` | informative | Frames that carry HTTP fields are compressed (field compression, Section 4.3). <br>_(all [CLIENT])_ | Because HTTP fields used in a connection can contain large amounts of redundant data, frames that contain them are compressed (Section 4.3). | hpack/ — HEADERS/CONTINUATION/PUSH_PROMISE field blocks go through the RFC 7541 Encoder/Decoder owned by each Conn. |
| `1-3-7` | informative | Server push is an optional interaction mode where the server synthesizes a request via PUSH_PROMISE and sends the response on a separate stream. <br>_(server,client [CLIENT])_ | HTTP/2 adds a new, optional interaction mode whereby a server can push responses to a client (Section 8.4). | conn/ — verify PUSH_PROMISE handling: client either supports pushed streams or advertises SETTINGS_ENABLE_PUSH=0 and treats subsequent PUSH_PROMISE as a connection error. |
| `1-3-8` | informative | All numeric values in the wire format are network byte order (big-endian) and unsigned unless otherwise indicated. <br>_(all [CLIENT])_ | All numeric values are in network byte order. Values are unsigned unless otherwise indicated. | frame/ — parser/writer must be big-endian throughout (internal/bytesx uint24/uint31 helpers); no signed interpretation of wire fields. |
| `1-3-9` | informative | The client is the endpoint that initiates the HTTP/2 connection, sends requests, and receives responses. <br>_(client [CLIENT])_ | client: The endpoint that initiates an HTTP/2 connection. Clients send HTTP requests and receive HTTP responses. | conn/ — client role fixed: initiates handshake, allocates odd stream IDs, never accepts. |
| `1-3-10` | informative | The server is the endpoint that accepts the HTTP/2 connection, receives requests, and sends responses. <br>_(server [not-client])_ | server: The endpoint that accepts an HTTP/2 connection. Servers receive HTTP requests and send HTTP responses. | conn/ — peer-role assumption only: even stream IDs belong to server-initiated (pushed) streams. |
| `1-3-11` | informative | A connection error is an error that affects the entire HTTP/2 connection. <br>_(all [CLIENT])_ | connection error: An error that affects the entire HTTP/2 connection. | conn/ — typed ConnError must tear down the whole connection (GOAWAY + close), never just one stream. |
| `1-3-12` | informative | A stream error is an error scoped to an individual HTTP/2 stream. <br>_(all [CLIENT])_ | stream error: An error on the individual HTTP/2 stream. | conn/ — StreamError maps to RST_STREAM on that stream only; connection and sibling streams must survive. |
| `1-3-13` | informative | A frame is the smallest unit of communication: a header plus a variable-length payload structured per frame type. <br>_(all [CLIENT])_ | frame: The smallest unit of communication within an HTTP/2 connection, consisting of a header and a variable-length sequence of octets structured according to the frame type. | frame/ — Framer parses the fixed 9-octet header then dispatches per-type payload validation. |
| `1-3-14` | informative | A stream is a bidirectional flow of frames within the HTTP/2 connection. <br>_(all [CLIENT])_ | stream: A bidirectional flow of frames within the HTTP/2 connection. | conn/ — Stream supports independent send (localEnded) and receive (remoteEnded) directions; half-closed states must be modeled. |
| `1-3-15` | informative | Intermediaries (gateway, proxy, tunnel per RFC 9110 §3.7) act as both client and server at different times. <br>_(intermediary [not-client])_ | Intermediaries act as both client and server at different times. | n/a — poseidon is client-only; no proxy role implemented. |
| `1-3-16` | informative | HTTP/2 uses the http and https URI schemes with the same default ports as HTTP/1.1; the URI itself carries no version indication. <br>_(all [CLIENT])_ | HTTP/2 uses the "http" and "https" URI schemes defined in Section 4.2 of [HTTP], with the same default port numbers as HTTP/1.1 [HTTP/1.1]. | client/ — URL parsing must default to port 443 for https and 80 for http when the authority omits a port. |
| `1-3-17` | informative | HTTP/2 support for http (cleartext) URIs is discoverable only out of band and requires prior knowledge; there is no in-band upgrade anymore. <br>_(client [CLIENT])_ | HTTP/2 support for "http" URIs can only be discovered by out-of-band means and requires prior knowledge of the support as described in Section 3.3. | client/ — cleartext h2 must be an explicit prior-knowledge configuration; verify no Upgrade-based negotiation path exists or is attempted. |
| `1-3-18` | informative | Creating a connection with either protocol identifier commits the endpoint to the full transport, framing, and message semantics of RFC 9113. <br>_(all [CLIENT])_ | Creating a connection based on either implies the use of the transport, framing, and message semantics described in this document. | client/ — negotiating "h2" (or dialing prior-knowledge) commits the whole stack; no partial/feature-subset mode may be exposed. |
| `1-3-19` | informative | The string "h2" identifies HTTP/2 over TLS and is the identifier used in the TLS ALPN extension. <br>_(client,server [CLIENT])_ | The string "h2" identifies the protocol where HTTP/2 uses Transport Layer Security (TLS); see Section 9.2. | client/ — ALPN transport must set tls.Config.NextProtos to include exactly "h2" for HTTP/2-over-TLS. |
| `1-3-20` | informative | The "h2" ALPN protocol identifier is serialized as the two-octet sequence 0x68, 0x32. <br>_(client [CLIENT])_ | The "h2" string is serialized into an ALPN protocol identifier as the two-octet sequence: 0x68, 0x32. | client/ — crypto/tls serializes ALPN; only check the NextProtos string is the exact two-byte token "h2" (no "h2-16" style drafts). |
| `1-3-21` | informative | The "h2c" Upgrade token and the HTTP2-Settings header field are deprecated by RFC 9113 (change vs 7540). <br>_(client [CLIENT])_ | is deprecated by this document. The same applies to the HTTP2-Settings header field, which was used with the upgrade to "h2c". | http1/ + client/ — verify the client never emits Upgrade: h2c or an HTTP2-Settings header on HTTP/1.1 requests. |
| `1-3-22` | informative | A client making a request to an https URI uses TLS (TLS 1.3 reference) with the ALPN extension. <br>_(client [CLIENT])_ | A client that makes a request to an "https" URI uses TLS [TLS13] with the ALPN extension [TLS-ALPN]. | client/ — https targets must go through the TLS+ALPN transport path; ALPN must be offered, not optional. |
| `1-3-23` | MUST NOT | The "h2c" protocol identifier must never be sent by a client in ALPN or selected by a server. <br>_(client,server [CLIENT])_ | The "h2c" protocol identifier MUST NOT be sent by a client or selected by a server | client/ — "h2c" must never appear in tls.Config.NextProtos; if a server somehow negotiates "h2c", the connection must be rejected. |
| `1-3-24` | MUST | After TLS negotiation completes, both client and server must send a connection preface. <br>_(client,server [CLIENT])_ | Once TLS negotiation is complete, both the client and the server MUST send a connection preface (Section 3.4). | conn/ — handshake must write the client preface immediately after the TLS conn is handed over, before any request frames. |
| `1-3-25` | informative | With prior knowledge, a client may open a cleartext TCP connection and send the connection preface followed directly by HTTP/2 frames. <br>_(client [CLIENT])_ | A client that knows that a server supports HTTP/2 can establish a TCP connection and send the connection preface (Section 3.4) followed by HTTP/2 frames. | client/ + conn/ — prior-knowledge cleartext mode: same preface+SETTINGS handshake as TLS path, no negotiation exchange first. |
| `1-3-26` | MUST | HTTP/2 connections over TLS must use TLS protocol negotiation (ALPN); prior knowledge applies only to cleartext TCP. <br>_(endpoint [CLIENT])_ | HTTP/2 connections over TLS MUST use protocol negotiation in TLS [TLS-ALPN]. | client/ — after TLS handshake, verify ConnectionState().NegotiatedProtocol == "h2" before speaking HTTP/2; never assume h2 on TLS without ALPN result. |
| `1-3-27` | MUST | In the prior-knowledge case the server must also send a connection preface. <br>_(server [CLIENT])_ | Likewise, the server MUST send a connection preface (Section 3.4). | conn/ — client side must expect and validate the server preface (a SETTINGS frame) on cleartext prior-knowledge conns exactly as on TLS conns. |
| `1-3-28` | informative | Prior HTTP/2 support is not a strong signal that the same server will support HTTP/2 on future connections. <br>_(client [CLIENT])_ | Without additional information, prior support for HTTP/2 is not a strong signal that a given server will support HTTP/2 for future connections. | client/ — pool/retry must not permanently cache h2 capability for prior-knowledge targets; a failed preface should surface an error rather than assume protocol. |
| `1-3-29` | informative | Each endpoint is required to send a connection preface as final confirmation of the protocol and to establish initial settings; client and server prefaces differ. <br>_(endpoint [CLIENT])_ | each endpoint is required to send a connection preface as a final confirmation of the protocol in use and to establish the initial settings for the HTTP/2 connection | conn/ — handshake sends preface bytes plus initial SETTINGS carrying local settings (e.g. INITIAL_WINDOW_SIZE, MAX_CONCURRENT_STREAMS advertisements). |
| `1-3-30` | informative | The client connection preface begins with the exact 24-octet magic string PRI * HTTP/2.0 CRLF CRLF SM CRLF CRLF (0x505249202a20485454502f322e300d0a0d0a534d0d0a0d0a). <br>_(client [CLIENT])_ | the connection preface starts with the string "PRI * HTTP/2.0\r\n\r\nSM\r\n\r\n" | conn/ — verify the preface constant is byte-for-byte 0x505249202a20485454502f322e300d0a0d0a534d0d0a0d0a (24 octets). |
| `1-3-31` | MUST | The 24-octet magic sequence must be immediately followed by a SETTINGS frame. <br>_(client [CLIENT])_ | This sequence MUST be followed by a SETTINGS frame (Section 6.5), which MAY be empty. | conn/ — handshake must write SETTINGS as the very first frame after the magic bytes, before HEADERS or anything else. |
| `1-3-32` | MAY | The client's preface SETTINGS frame is allowed to be empty (zero settings). <br>_(client [CLIENT])_ | a SETTINGS frame (Section 6.5), which MAY be empty | conn/ + frame/ — writer must support a zero-length SETTINGS payload; an empty initial SETTINGS is conformant if no local settings differ from defaults. |
| `1-3-33` | informative | The client sends the connection preface as the very first application-data octets of the connection. <br>_(client [CLIENT])_ | The client sends the client connection preface as the first application data octets of a connection. | conn/ — nothing may be written to the transport before the preface bytes (no debug frames, no early WINDOW_UPDATE). |
| `1-3-34` | MUST | The server connection preface is a potentially empty SETTINGS frame that must be the first frame the server sends; the client should verify this. <br>_(server [CLIENT])_ | The server connection preface consists of a potentially empty SETTINGS frame (Section 6.5) that MUST be the first frame the server sends in the HTTP/2 connection. | conn/ — reader must enforce that the first inbound frame is SETTINGS (non-ACK); anything else is an invalid preface → ConnError(PROTOCOL_ERROR) per 1-3-38. |
| `1-3-35` | MUST | The peer's preface SETTINGS frame must be acknowledged (SETTINGS ACK) after the endpoint has sent its own connection preface. <br>_(endpoint [CLIENT])_ | The SETTINGS frames received from a peer as part of the connection preface MUST be acknowledged (see Section 6.5.3) after sending the connection preface. | conn/ — connHandler.OnSettings must send an ACK for the server's initial SETTINGS; ordering after own preface is guaranteed since the preface is written at dial time. |
| `1-3-36` | informative | Clients are permitted to send additional frames immediately after their preface without waiting for the server's preface, but risk racing settings that change expectations. <br>_(client [CLIENT])_ | clients are permitted to send additional frames to the server immediately after sending the client connection preface, without waiting to receive the server connection preface. | conn/ — handshake need not block on the server SETTINGS before the first request; if it does block, that is also conformant — just verify whichever choice is deliberate. |
| `1-3-37` | informative | Upon receiving the server's preface SETTINGS frame, the client is expected to honor every setting it establishes. <br>_(client [CLIENT])_ | Upon receiving the SETTINGS frame, the client is expected to honor any settings established. | conn/ — applyPeerSettings must apply all entries, including retroactive INITIAL_WINDOW_SIZE resize of already-open streams and MAX_FRAME_SIZE/MAX_CONCURRENT_STREAMS gates. |
| `1-3-38` | MUST | An invalid connection preface must be treated as a connection error of type PROTOCOL_ERROR. <br>_(client,server [CLIENT])_ | Clients and servers MUST treat an invalid connection preface as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/ — a first inbound frame that is not SETTINGS (or garbage bytes) must produce ConnError(PROTOCOL_ERROR) and tear down the conn; needs a conformance test if absent. |
| `1-3-39` | MAY | When failing a connection for an invalid preface, the GOAWAY frame may be omitted since the peer is evidently not speaking HTTP/2. <br>_(client,server [CLIENT])_ | A GOAWAY frame (Section 6.8) MAY be omitted in this case, since an invalid preface indicates that the peer is not using HTTP/2. | conn/ — invalid-preface teardown may close the transport directly without GOAWAY; verify the error path doesn't require a GOAWAY write to succeed. |

### 9113 section 4 - HTTP Frames (Frame Format, Frame Size, Field Section Compression/Decompression, Compression State) — RFC 9113

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `4-1` | informative | Every frame consists of a fixed 9-octet header followed by a variable-length frame payload. <br>_(all [CLIENT])_ | All frames begin with a fixed 9-octet header followed by a variable-length frame payload. | frame/ — Framer must read/write exactly 9 header octets before every payload. |
| `4-2` | informative | Frame header layout is Length(24 bits), Type(8), Flags(8), Reserved(1), Stream Identifier(31), then payload. <br>_(all [CLIENT])_ | HTTP Frame { Length (24), Type (8), Flags (8), Reserved (1), Stream Identifier (31), Frame Payload (..), } | frame/ — verify field order and bit widths in header parse and serialize paths. |
| `4-3` | informative | Length is the payload length as an unsigned 24-bit integer counted in octets. <br>_(all [CLIENT])_ | The length of the frame payload expressed as an unsigned 24-bit integer in units of octets. | frame/ (via internal/bytesx uint24) — 24-bit big-endian length codec. |
| `4-4` | MUST NOT | A sender must not emit a frame with payload length greater than 16,384 octets unless the receiver advertised a larger SETTINGS_MAX_FRAME_SIZE. <br>_(sender [CLIENT])_ | Values greater than 2^14 (16,384) MUST NOT be sent unless the receiver has set a larger value for SETTINGS_MAX_FRAME_SIZE. | conn/ writeData + writeHeaders — chunking must respect min(peer MAX_FRAME_SIZE, default 16384); frame/ writer should never exceed it. |
| `4-5` | informative | The 9 octets of the frame header are excluded from the Length value. <br>_(all [CLIENT])_ | The 9 octets of the frame header are not included in this value. | frame/ — Length counts payload only; check no off-by-9 in parser or writer. |
| `4-6` | MUST | Implementations must ignore and discard frames whose Type is unknown. <br>_(all [CLIENT])_ | Implementations MUST ignore and discard frames of unknown types. | frame/ parser or conn/ dispatch — unknown frame type skips payload silently, never an error (but still counts against read; note interaction with 4-25: unknown frames still forbidden inside a field-block sequence). |
| `4-7` | MUST | Flag bits with no defined semantics for a frame type must be ignored on receipt and sent as zero. <br>_(sender,receiver [CLIENT])_ | Unused flags MUST be ignored on receipt and MUST be left unset (0x00) when sending. | frame/ — writer emits only defined flag bits per type; parser must not reject or misinterpret set-but-unused bits. |
| `4-8` | MUST | The reserved bit before the stream identifier must be sent as zero and ignored on receipt. <br>_(sender,receiver [CLIENT])_ | the bit MUST remain unset (0x00) when sending and MUST be ignored when receiving. | frame/ — mask off the high bit of the 32-bit stream-id word when reading; always write 0 (bytesx uint31). |
| `4-9` | informative | Stream Identifier is an unsigned 31-bit integer; stream 0 is reserved for connection-scoped frames. <br>_(all [CLIENT])_ | The value 0x00 is reserved for frames that are associated with the connection as a whole as opposed to an individual stream. | conn/ — stream-0 frames (SETTINGS, PING, GOAWAY, conn WINDOW_UPDATE) routed to connection handlers, never to a Stream. |
| `4-10` | informative | Payload structure and content are determined entirely by the frame type. <br>_(all [CLIENT])_ | The structure and content of the frame payload are dependent entirely on the frame type. | frame/ — per-type payload parsers/writers; no shared payload assumptions across types. |
| `4-11` | informative | SETTINGS_MAX_FRAME_SIZE may take any value between 2^14 (16,384) and 2^24-1 (16,777,215) octets inclusive. <br>_(all [CLIENT])_ | This setting can have any value between 2^14 (16,384) and 2^24-1 (16,777,215) octets, inclusive. | conn/ — validate received SETTINGS_MAX_FRAME_SIZE within [16384, 16777215] (out-of-range handling is §6.5.2); clamp own advertisement too. |
| `4-12` | MUST | All implementations must be able to receive and minimally process frames up to 16,384 payload octets plus the 9-octet header. <br>_(all [CLIENT])_ | All implementations MUST be capable of receiving and minimally processing frames up to 2^14 octets in length, plus the 9-octet frame header | frame/ — reader buffers must accept 16384-byte payloads even before any SETTINGS exchange and regardless of a lower local advertisement. |
| `4-13` | informative | Individual frame types impose additional payload-size limits beyond SETTINGS_MAX_FRAME_SIZE (e.g. PING). <br>_(all [CLIENT])_ | Certain frame types, such as PING (Section 6.7), impose additional limits on the amount of frame payload data allowed. | frame/ — fixed-size checks per type: PING=8, RST_STREAM=4, WINDOW_UPDATE=4, SETTINGS multiple of 6, PRIORITY=5. |
| `4-14` | MUST | An endpoint must respond with FRAME_SIZE_ERROR when a frame exceeds SETTINGS_MAX_FRAME_SIZE, exceeds a frame-type-specific limit, or is too small to hold mandatory fields. <br>_(endpoint [CLIENT])_ | An endpoint MUST send an error code of FRAME_SIZE_ERROR if a frame exceeds the size defined in SETTINGS_MAX_FRAME_SIZE, exceeds any limit defined for the frame type | frame/ size validation + conn/ error emission — all three triggers: oversize vs our advertised max, per-type limit, and undersize (too small for mandatory fields). |
| `4-15` | MUST | A frame-size error that could alter whole-connection state must be a connection error — this covers HEADERS, PUSH_PROMISE, CONTINUATION, SETTINGS, and any stream-0 frame. <br>_(endpoint,receiver [CLIENT])_ | A frame size error in a frame that could alter the state of the entire connection MUST be treated as a connection error (Section 5.4.1) | conn/ — escalation matrix: FRAME_SIZE_ERROR on HEADERS/PUSH_PROMISE/CONTINUATION/SETTINGS/stream-id-0 frames tears down the connection (GOAWAY), not just the stream. |
| `4-16` | informative | Endpoints need not fill frames to the maximum; smaller frames reduce blocking of time-sensitive frames like RST_STREAM, WINDOW_UPDATE, PRIORITY. <br>_(sender [CLIENT])_ | Endpoints are not obligated to use all available space in a frame. | conn/ writeData — chunk-size choice is free; advisory only, no check needed. |
| `4-17` | informative | Field section compression/decompression is defined by HPACK ([COMPRESSION], RFC 7541), historically called header compression. <br>_(all [CLIENT])_ | Details of HTTP/2 field section compression and decompression are defined in [COMPRESSION] | hpack/ — the package is the [COMPRESSION] implementation; RFC 7541 conformance is the governing spec for block contents. |
| `4-18` | informative | A field block carries all compressed field lines of exactly one field section. <br>_(all [CLIENT])_ | Each field block carries all of the compressed field lines of a single field section. | conn/ — one SendHeaders call encodes one complete section into one block; never split a section across two blocks or merge two sections. |
| `4-19` | informative | Pseudo-header fields (control data, §8.3) are carried inside the field block using the same wire format as ordinary field lines. <br>_(all [CLIENT])_ | Header sections also include control data associated with the message in the form of pseudo-header fields (Section 8.3) that use the same format as a field line. | conn/ + hpack/ — :method/:scheme/:authority/:path encoded as ordinary hpack.HeaderField entries; no special framing. |
| `4-20` | informative | Requests, responses, and pushed responses may optionally include a trailer field block — except interim (1xx) responses and requests inside PUSH_PROMISE, which cannot carry trailers. <br>_(all [CLIENT])_ | All these messages, except for interim responses and requests contained in PUSH_PROMISE (Section 6.6) frames, can optionally include a field block that carries a trailer section. | conn/ — a second HEADERS on a stream after data is a trailer section; verify client accepts response trailers and that 1xx responses are never treated as trailer-bearing (cf. sibling-divergence memory: HTTP/2 1xx handling). |
| `4-21` | informative | The first field block fragment travels in a HEADERS or PUSH_PROMISE payload; subsequent fragments travel in CONTINUATION frames. <br>_(all [CLIENT])_ | The first field block fragment is transmitted within the frame payload of HEADERS (Section 6.2) or PUSH_PROMISE (Section 6.6), each of which could be followed by CONTINUATION | conn/ — CONTINUATION reassembly must work after both HEADERS and PUSH_PROMISE; frame/ — CONTINUATION carries raw fragment only. |
| `4-22` | informative | The Cookie header field receives special treatment in the HTTP mapping (splitting/reassembly per §8.2.3). <br>_(all [CLIENT])_ | The Cookie header field [COOKIE] is treated specially by the HTTP mapping (see Section 8.2.3). | conn/ or client/ — check §8.2.3 crumb handling: client may split Cookie into multiple field lines for compression; any received split cookies must be concatenated with '; '. |
| `4-23` | informative | A receiver reconstructs the field section by concatenating all fragments of the block and then decompressing the whole. <br>_(receiver [CLIENT])_ | A receiving endpoint reassembles the field block by concatenating its fragments and then decompresses the block to reconstruct the field section. | conn/ — HEADERS+CONTINUATION fragments concatenated (or fed in order) into one hpack.Decoder pass; never decode fragments as independent blocks. |
| `4-24` | informative | A field section is complete either as a single HEADERS/PUSH_PROMISE with END_HEADERS set, or HEADERS/PUSH_PROMISE without it plus CONTINUATIONs where the last carries END_HEADERS. <br>_(all [CLIENT])_ | a HEADERS or PUSH_PROMISE frame with the END_HEADERS flag unset and one or more CONTINUATION frames, where the last CONTINUATION frame has the END_HEADERS flag set. | conn/ — END_HEADERS completion state machine: block open until END_HEADERS seen; header events must not fire before completion. |
| `4-25` | MUST | A field block must be sent as a contiguous frame sequence with no interleaved frames of any other type or from any other stream. <br>_(sender [CLIENT])_ | Field blocks MUST be transmitted as a contiguous sequence of frames, with no interleaved frames of any other type or from any other stream. | conn/ — send side: hold wmu across HEADERS+all CONTINUATIONs so no other stream's frame interleaves; receive side: any non-CONTINUATION or wrong-stream frame while a block is open is a connection error (PROTOCOL_ERROR per §6.2/§6.10). |
| `4-26` | informative | Field block fragments may appear only in HEADERS, PUSH_PROMISE, or CONTINUATION frame payloads, because these mutate the receiver's compression context. <br>_(sender [CLIENT])_ | Field block fragments can only be sent as the frame payload of HEADERS, PUSH_PROMISE, or CONTINUATION frames | frame/ — only these three frame types expose fragment payloads; no other type may feed the hpack decoder. |
| `4-27` | informative | An endpoint receiving HEADERS, PUSH_PROMISE, or CONTINUATION must reassemble and decompress the block even when the frames themselves will be discarded. <br>_(receiver,endpoint [CLIENT])_ | An endpoint receiving HEADERS, PUSH_PROMISE, or CONTINUATION frames needs to reassemble field blocks and perform decompression even if the frames are to be discarded. | conn/ — critical: headers on streams being reset, refused, unknown, post-GOAWAY-evicted, or rejected pushes must still pass through hpack.Decoder to keep dynamic-table state in sync; check the GOAWAY-eviction and REFUSED_STREAM paths do not skip decode. |
| `4-28` | MUST | A receiver that does not decompress a field block must terminate the connection with a COMPRESSION_ERROR connection error. <br>_(receiver [CLIENT])_ | A receiver MUST terminate the connection with a connection error (Section 5.4.1) of type COMPRESSION_ERROR if it does not decompress a field block. | conn/ — if any path chooses to skip decoding (e.g. limits exceeded mid-block), it must escalate to connection teardown with COMPRESSION_ERROR, never silently continue. |
| `4-29` | MUST | Any decoding error inside a field block must be treated as a connection error of type COMPRESSION_ERROR, not a stream error. <br>_(receiver [CLIENT])_ | A decoding error in a field block MUST be treated as a connection error (Section 5.4.1) of type COMPRESSION_ERROR. | conn/ — hpack.Decoder error maps to ConnError(COMPRESSION_ERROR) + GOAWAY + connection close; verify it is never downgraded to RST_STREAM. |
| `4-30` | informative | Compression is stateful: each endpoint keeps one HPACK encoder context and one HPACK decoder context shared by all field blocks on the connection. <br>_(endpoint [CLIENT])_ | Each endpoint has an HPACK encoder context and an HPACK decoder context that are used for encoding and decoding all field blocks on a connection. | conn/ — exactly one *hpack.Encoder + one *hpack.Decoder per Conn (matches stated architecture); never per-stream contexts, never reset mid-connection. |
| `4-31` | informative | The dynamic table maximum is chosen by the HPACK decoder and advertised via SETTINGS_HEADER_TABLE_SIZE; at connection start both encoder and decoder tables begin at 4,096 bytes. <br>_(endpoint [CLIENT])_ | the dynamic table size for the HPACK decoder and encoder at both endpoints starts at 4,096 bytes, the initial value of the SETTINGS_HEADER_TABLE_SIZE setting. | **VERIFY** hpack/ — default table size 4096 on both encoder and decoder; conn/ — SETTINGS_HEADER_TABLE_SIZE advertisement reflects the decoder's cap. -- Quote is NOT verbatim. RFC 9113 §4.3.1 reads '...the dynamic table size for the HPACK decoder and encoder at both endpoints starts at 4,096 octets, the initial value of the SETTINGS_HEADER_TABLE_SIZE setting.' The fact substitutes 'bytes' for 'octets' — a semantic word change, not a wrap/whitespace diff. (The requirement's substance and 4,096 default are otherwise correct.) |
| `4-32` | informative | A change to SETTINGS_HEADER_TABLE_SIZE takes effect only when the receiving endpoint acknowledges the SETTINGS frame. <br>_(endpoint [CLIENT])_ | Any change to the maximum value set using SETTINGS_HEADER_TABLE_SIZE takes effect when the endpoint acknowledges settings (Section 6.5.3). | conn/ — apply peer's HEADER_TABLE_SIZE to our encoder at the moment we send the SETTINGS ACK (not on receipt, not on some later frame); ordering vs in-flight SendHeaders under wmu matters. |
| `4-33` | informative | The encoder may use any dynamic-table size up to the decoder's advertised maximum, and declares its chosen size with a Dynamic Table Size Update instruction. <br>_(sender,endpoint [CLIENT])_ | The HPACK encoder at that endpoint can set the dynamic table to any size up to the maximum value set by the decoder. | hpack/ — Encoder must support choosing size <= peer max and emitting the RFC 7541 §6.3 Dynamic Table Size Update at block start. |
| `4-34` | MUST | After acknowledging a SETTINGS_HEADER_TABLE_SIZE reduction below the current table size, the encoder must begin its next field block with a Dynamic Table Size Update setting a size at or below the reduced maximum. <br>_(sender,endpoint [CLIENT])_ | its HPACK encoder MUST start the next field block with a Dynamic Table Size Update instruction that sets the dynamic table to a size that is less than or equal | hpack/ + conn/ — when peer lowers HEADER_TABLE_SIZE and we ACK: encoder must carry a pending DTSU emitted at the very start of the next encoded block (cf. sibling-divergence memory: verify hpack ring actually implements this, independent of qpack). |
| `4-35` | MUST | An endpoint must treat a field block received after its acknowledged table-size reduction as a COMPRESSION_ERROR connection error if the block does not begin with a conformant Dynamic Table Size Update. <br>_(receiver,endpoint [CLIENT])_ | An endpoint MUST treat a field block that follows an acknowledgment of the reduction to the maximum dynamic table size as a connection error (Section 5.4.1) of type COMPRESSION_ERROR | hpack/ Decoder + conn/ — enforcement path only triggers if the client ever lowers its advertised SETTINGS_HEADER_TABLE_SIZE; if the client can lower it (config option), the decoder must require DTSU-first on the next block, else document the setting as immutable. |
| `4-36` | informative | Reducing SETTINGS_HEADER_TABLE_SIZE mid-connection is not widely interoperable; reducing below 4,096 in the connection preface works somewhat better but can still fail. <br>_(all [CLIENT])_ | Implementers are advised that reducing the value of SETTINGS_HEADER_TABLE_SIZE is not widely interoperable. | conn/ — prefer keeping decoder advertisement at 4096 and never shrinking mid-connection; if a knob exists, note the interop risk in its doc comment. |

### 9113 section 5-5.1 - Streams and Multiplexing: Stream States, Stream Identifiers, Stream Concurrency (RFC 9113)

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `5-5.1-1` | informative | Frame ordering is significant; recipients must process frames in the order received, particularly HEADERS and DATA. <br>_(receiver [CLIENT])_ | Recipients process frames in the order they are received. | conn/: single reader goroutine must dispatch frames strictly in arrival order; no reordering between HEADERS and DATA events delivered to a stream. |
| `5-5.1-2` | informative | Stream identifiers are assigned by the endpoint that initiates the stream. <br>_(endpoint [CLIENT])_ | Stream identifiers are assigned to streams by the endpoint initiating the stream. | conn/: client allocates odd ids in SendHeaders (deferred B.2.1 allocation under wmu); never accept a server-assigned id for a client-initiated stream. |
| `5-5.1-3` | informative | CONTINUATION frames cause no stream-state transitions; they are part of the HEADERS or PUSH_PROMISE they follow. <br>_(all [CLIENT])_ | CONTINUATION frames do not result in state transitions; they are effectively part of the HEADERS or PUSH_PROMISE that they follow. | conn/: header-block assembly must merge CONTINUATION into the preceding HEADERS/PUSH_PROMISE before applying any state transition; no per-CONTINUATION state change. |
| `5-5.1-4` | informative | END_STREAM is processed as a separate event from its carrier frame; HEADERS with END_STREAM can cause two state transitions at once. <br>_(all [CLIENT])_ | the END_STREAM flag is processed as a separate event to the frame that bears it; a HEADERS frame with the END_STREAM flag set can cause two state transitions. | conn/: receiving HEADERS+END_STREAM must go idle→open→half-closed(remote) atomically; sending request HEADERS+END_STREAM goes idle→open→half-closed(local). |
| `5-5.1-5` | informative | Every stream begins in the idle state. <br>_(all [CLIENT])_ | All streams start in the "idle" state. | conn/: NewStream returns a stream logically idle (id==0 until SendHeaders per B.2.1); verify no frames are emitted before HEADERS. |
| `5-5.1-6` | informative | Sending HEADERS as client (or receiving as server) moves an idle stream to open; the same HEADERS can immediately half-close it. <br>_(client,server [CLIENT])_ | Sending a HEADERS frame as a client, or receiving a HEADERS frame as a server, causes the stream to become "open". | conn/: SendHeaders transitions idle→open, and with END_STREAM set directly to half-closed(local). |
| `5-5.1-7` | informative | Only a server sends PUSH_PROMISE; doing so transitions the promised idle stream to reserved (local) on the server side. <br>_(server [CLIENT])_ | Only a server may send PUSH_PROMISE frames. | conn/: verify the client code path never writes PUSH_PROMISE even though frame/ may expose a writer for it. |
| `5-5.1-8` | informative | Only a client receives PUSH_PROMISE; receipt reserves the promised idle stream as reserved (remote). <br>_(client [CLIENT])_ | Only a client may receive PUSH_PROMISE frames. | conn/: reader must handle inbound PUSH_PROMISE — either track reserved(remote) state or reject per SETTINGS_ENABLE_PUSH=0 policy (§6.5.2/8.4); check which the conn advertises. |
| `5-5.1-9` | informative | PUSH_PROMISE is carried on an existing stream and names the reserved stream in its Promised Stream ID payload field. <br>_(all [CLIENT])_ | the PUSH_PROMISE frame is not sent on the idle stream but references the newly reserved stream in the Promised Stream ID field. | frame/: PUSH_PROMISE parser must read the 31-bit Promised Stream ID (R bit masked) from the payload, distinct from the frame header's stream id. |
| `5-5.1-10` | informative | Opening a stream with a higher-valued identifier immediately transitions lower-valued idle streams to closed (transition absent from the diagram). <br>_(all [CLIENT])_ | Opening a stream with a higher-valued stream identifier causes the stream to transition immediately to a "closed" state | conn/: skipped/lower ids must be treated as closed, not idle — affects which error (STREAM_CLOSED-ish tolerance vs PROTOCOL_ERROR) applies to frames arriving on them. |
| `5-5.1-11` | MUST | Receiving any frame other than HEADERS or PRIORITY on an idle stream is a connection error of type PROTOCOL_ERROR. <br>_(endpoint,receiver [CLIENT])_ | Receiving any frame other than HEADERS or PRIORITY on a stream in this state MUST be treated as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: DATA/RST_STREAM/WINDOW_UPDATE/CONTINUATION on an id the client never opened (odd, above high-water) must tear down the connection with PROTOCOL_ERROR, not a stream error. |
| `5-5.1-12` | MUST | Receiving a HEADERS frame on an idle server-initiated stream is also a connection error of type PROTOCOL_ERROR. <br>_(endpoint,receiver [CLIENT])_ | If this stream is initiated by the server, as described in Section 5.1.1, then receiving a HEADERS frame MUST also be treated as a connection error | conn/: client must reject HEADERS on an even-numbered stream that was never reserved via PUSH_PROMISE → connection error PROTOCOL_ERROR. |
| `5-5.1-13` | informative | In reserved (local), the promising endpoint sending HEADERS opens the stream in half-closed (remote). <br>_(server [not-client])_ | The endpoint can send a HEADERS frame. This causes the stream to open in a "half-closed (remote)" state. | Server-only (client never enters reserved(local)); no conn/ client action. |
| `5-5.1-14` | informative | Either endpoint may send RST_STREAM on a reserved stream, closing it and releasing the reservation. <br>_(endpoint [CLIENT])_ | Either endpoint can send a RST_STREAM frame to cause the stream to become "closed". This releases the stream reservation. | conn/: client must be able to RST_STREAM a reserved(remote) pushed stream to cancel it (e.g. push refused). |
| `5-5.1-15` | MUST NOT | In reserved (local), an endpoint must not send any frame type other than HEADERS, RST_STREAM, or PRIORITY. <br>_(endpoint,sender [not-client])_ | An endpoint MUST NOT send any type of frame other than HEADERS, RST_STREAM, or PRIORITY in this state. | Server-only sender rule (reserved(local) is the pusher's state); no client action. |
| `5-5.1-16` | MAY | In reserved (local), PRIORITY or WINDOW_UPDATE frames may be received. <br>_(endpoint,receiver [not-client])_ | A PRIORITY or WINDOW_UPDATE frame MAY be received in this state. | Server-side receive rule; the client-side sending counterpart is bounded by fact 5-5.1-19. |
| `5-5.1-17` | MUST | In reserved (local), receiving any frame other than RST_STREAM, PRIORITY, or WINDOW_UPDATE is a connection error PROTOCOL_ERROR. <br>_(endpoint,receiver [not-client])_ | Receiving any type of frame other than RST_STREAM, PRIORITY, or WINDOW_UPDATE on a stream in this state MUST be treated as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | Server-only (client never holds reserved(local)); no client action. |
| `5-5.1-18` | informative | In reserved (remote), receiving HEADERS transitions the stream to half-closed (local). <br>_(endpoint,receiver [CLIENT])_ | Receiving a HEADERS frame causes the stream to transition to "half-closed (local)". | conn/: pushed-stream state machine — response HEADERS on a reserved(remote) stream must land it in half-closed(local) (client can only send WU/PRIORITY/RST afterward). |
| `5-5.1-19` | MUST NOT | In reserved (remote), an endpoint must not send any frame type other than RST_STREAM, WINDOW_UPDATE, or PRIORITY. <br>_(endpoint,sender [CLIENT])_ | An endpoint MUST NOT send any type of frame other than RST_STREAM, WINDOW_UPDATE, or PRIORITY in this state. | conn/: block DATA/HEADERS/CONTINUATION sends on reserved(remote) pushed streams; only RST_STREAM, WINDOW_UPDATE, PRIORITY permitted. |
| `5-5.1-20` | MUST | In reserved (remote), receiving any frame other than HEADERS, RST_STREAM, or PRIORITY is a connection error PROTOCOL_ERROR (note: WINDOW_UPDATE receipt is NOT permitted here, asymmetric with reserved (local)). <br>_(endpoint,receiver [CLIENT])_ | Receiving any type of frame other than HEADERS, RST_STREAM, or PRIORITY on a stream in this state MUST be treated as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: DATA or WINDOW_UPDATE received on a reserved(remote) stream must be a connection error PROTOCOL_ERROR — check the state table excludes WINDOW_UPDATE for this state specifically. |
| `5-5.1-21` | informative | In open, both peers may send any frame type but senders must observe advertised stream-level flow-control limits. <br>_(sender [CLIENT])_ | In this state, sending peers observe advertised stream-level flow-control limits (Section 5.2). | conn/: acquireSendCredits must gate DATA on the per-stream send window in open state (B.2.3). |
| `5-5.1-22` | informative | From open, sending END_STREAM yields half-closed (local); receiving END_STREAM yields half-closed (remote). <br>_(endpoint [CLIENT])_ | An endpoint sending an END_STREAM flag causes the stream state to become "half-closed (local)"; an endpoint receiving an END_STREAM flag causes the stream state to become "half-closed (remote)". | conn/: verify localEnded/remoteEnded flags map exactly to these two transitions from open. |
| `5-5.1-23` | informative | From open, either endpoint sending RST_STREAM transitions the stream immediately to closed. <br>_(endpoint [CLIENT])_ | Either endpoint can send a RST_STREAM frame from this state, causing it to transition immediately to "closed". | conn/: RST_STREAM (sent or received) must immediately close and evict the stream — with the s.mu guard from the recycle-teardown race note (recycleStream vs concurrent reader). |
| `5-5.1-24` | informative | In half-closed (local), a stream can only be used to send WINDOW_UPDATE, PRIORITY, and RST_STREAM. <br>_(sender [CLIENT])_ | cannot be used for sending frames other than WINDOW_UPDATE, PRIORITY, and RST_STREAM. | conn/: after client sends END_STREAM on the request, writeData/writeHeaders must fail; WINDOW_UPDATE refunds and RST_STREAM must remain allowed. |
| `5-5.1-25` | informative | Half-closed (local) transitions to closed when END_STREAM is received or either peer sends RST_STREAM. <br>_(all [CLIENT])_ | A stream transitions from this state to "closed" when a frame is received with the END_STREAM flag set or when either peer sends a RST_STREAM frame. | conn/: bothEnded computation — remote END_STREAM or any RST closes a half-closed(local) stream. |
| `5-5.1-26` | informative | In half-closed (local) an endpoint can receive any frame type, and must keep granting WINDOW_UPDATE credit to continue receiving flow-controlled frames. <br>_(receiver [CLIENT])_ | Providing flow-control credit using WINDOW_UPDATE frames is necessary to continue receiving flow-controlled frames. | conn/: recvWindowRefundThreshold refunds must keep running in half-closed(local) — the normal client state while streaming a response body. |
| `5-5.1-27` | informative | A receiver may ignore WINDOW_UPDATE frames that arrive shortly after it sent END_STREAM. <br>_(receiver [CLIENT])_ | a receiver can ignore WINDOW_UPDATE frames, which might arrive for a short period after a frame with the END_STREAM flag set is sent. | conn/: parseWindowUpdates must tolerate (not error on) WINDOW_UPDATE received after we sent END_STREAM. |
| `5-5.1-28` | informative | In half-closed (remote), an endpoint is no longer obligated to maintain a receiver flow-control window. <br>_(endpoint,receiver [CLIENT])_ | In this state, an endpoint is no longer obligated to maintain a receiver flow-control window. | conn/: after remoteEnded, refund WINDOW_UPDATEs may stop; verify no refunds are emitted for a peer that already sent END_STREAM. |
| `5-5.1-29` | MUST | In half-closed (remote), receiving frames other than WINDOW_UPDATE, PRIORITY, or RST_STREAM must be answered with a stream error of type STREAM_CLOSED. <br>_(endpoint,receiver [CLIENT])_ | receives additional frames, other than WINDOW_UPDATE, PRIORITY, or RST_STREAM, for a stream that is in this state, it MUST respond with a stream error (Section 5.4.2) of type STREAM_CLOSED | conn/: DATA or HEADERS after the peer's END_STREAM must trigger RST_STREAM(STREAM_CLOSED) — a stream error, not connection teardown. |
| `5-5.1-30` | informative | In half-closed (remote) the endpoint can still send any frame type and continues to observe stream-level flow-control limits. <br>_(sender [CLIENT])_ | A stream that is "half-closed (remote)" can be used by the endpoint to send frames of any type. | conn/: client must be able to keep sending request body after receiving a complete early response; send-side flow control still enforced. |
| `5-5.1-31` | informative | Half-closed (remote) transitions to closed by sending END_STREAM or when either peer sends RST_STREAM. <br>_(all [CLIENT])_ | A stream can transition from this state to "closed" by sending a frame with the END_STREAM flag set or when either peer sends a RST_STREAM frame. | conn/: local END_STREAM or any RST closes a half-closed(remote) stream; verify bothEnded logic covers this direction too. |
| `5-5.1-32` | informative | Closed is the terminal state, entered after both sending and receiving END_STREAM, or after sending or receiving RST_STREAM. <br>_(all [CLIENT])_ | A stream enters the "closed" state after an endpoint both sends and receives a frame with an END_STREAM flag set. | conn/: closed-entry conditions feed recycleStream — pooled Stream fields must reset on this transition (pooled-stream-state-reset memory note). |
| `5-5.1-33` | MUST NOT | An endpoint must not send any frames other than PRIORITY on a closed stream. <br>_(endpoint,sender [CLIENT])_ | An endpoint MUST NOT send frames other than PRIORITY on a closed stream. | conn/: verify no late WINDOW_UPDATE refunds or RST_STREAMs are written for streams already fully closed/evicted. |
| `5-5.1-34` | MAY | An endpoint may treat receipt of any other frame type on a closed stream as a connection error of type STREAM_CLOSED, subject to the exceptions below. <br>_(endpoint,receiver [CLIENT])_ | An endpoint MAY treat receipt of any other type of frame on a closed stream as a connection error (Section 5.4.1) of type STREAM_CLOSED, except as noted below. | conn/: optional escalation — if implemented, error code is STREAM_CLOSED and the exceptions in facts 35-39 must be honored first. |
| `5-5.1-35` | informative | After sending END_STREAM or RST_STREAM, an endpoint may still legitimately receive WINDOW_UPDATE or RST_STREAM from the peer before the peer processes the closure. <br>_(endpoint,receiver [CLIENT])_ | might receive a WINDOW_UPDATE or RST_STREAM frame from its peer in the time before the peer receives and processes the frame that closes the stream. | conn/: WINDOW_UPDATE/RST_STREAM on recently closed streams must be silently tolerated, never a connection error. |
| `5-5.1-36` | MUST | An endpoint that reset a stream from open or half-closed (local) must minimally process, then discard, all frames subsequently received on it. <br>_(endpoint,receiver [CLIENT])_ | An endpoint MUST minimally process and then discard any frames it receives in this state. | conn/: after client sends RST_STREAM (e.g. events-channel overflow → RST REFUSED_STREAM), the reader must keep parsing and discarding that stream's frames rather than erroring. |
| `5-5.1-37` | informative | Minimal processing of frames on a reset stream includes updating header compression state for HEADERS and PUSH_PROMISE. <br>_(endpoint,receiver [CLIENT])_ | This means updating header compression state for HEADERS and PUSH_PROMISE frames. | hpack/ + conn/: header blocks for discarded/reset streams MUST still pass through the hpack.Decoder to keep the dynamic table in sync — verify conn never skips decode when dropping a stream. |
| `5-5.1-38` | informative | A PUSH_PROMISE received even on a closed stream still makes the promised stream reserved (remote). <br>_(endpoint,receiver [CLIENT])_ | Receiving a PUSH_PROMISE frame also causes the promised stream to become "reserved (remote)", even when the PUSH_PROMISE frame is received on a closed stream. | conn/: PUSH_PROMISE handling must not be gated on the carrier stream being live; the promised-stream reservation (or its cancellation) still happens. |
| `5-5.1-39` | informative | DATA received on a closed stream still counts against the connection flow-control window. <br>_(endpoint,receiver [CLIENT])_ | the content of DATA frames counts toward the connection flow-control window. | conn/: connection-level recv window (connRecvWindow=65535) must be debited and refunded for DATA on closed/reset streams, or the connection window leaks and stalls. |
| `5-5.1-40` | MAY | Endpoints may use signals proving the peer saw the closure (e.g. SETTINGS ACK) to then treat any frame except PRIORITY on that closed stream as connection error PROTOCOL_ERROR. <br>_(endpoint [CLIENT])_ | Endpoints MAY use other signals to detect that a peer has received the frames that caused the stream to enter the "closed" state | conn/: optional hardening; if ever added, gate it on a signal like a SETTINGS acknowledgment, and exempt PRIORITY. |
| `5-5.1-41` | SHOULD NOT | Endpoints should not use timers to decide when frames on a closed stream become errors. <br>_(endpoint [CLIENT])_ | Endpoints SHOULD NOT use timers for this purpose. | conn/: verify there is no timer-based escalation of closed-stream frame receipt to errors. |
| `5-5.1-42` | SHOULD | Absent a more specific rule, receipt of a frame not expressly permitted in a state's description should be treated as connection error PROTOCOL_ERROR. <br>_(all [CLIENT])_ | implementations SHOULD treat the receipt of a frame that is not expressly permitted in the description of a state as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: default arm of the per-state frame-acceptance table should produce ConnError(PROTOCOL_ERROR). |
| `5-5.1-43` | informative | PRIORITY frames can be sent and received in any stream state. <br>_(all [CLIENT])_ | PRIORITY can be sent and received in any stream state. | conn/: never reject an inbound PRIORITY frame regardless of stream state (RFC 9113 deprecates the scheme but the frame stays legal); parse-and-ignore is conformant. |
| `5-5.1-44` | informative | Frames of unknown types cannot be treated as stream-state errors, since their send/receive conditions are unknown. <br>_(receiver [CLIENT])_ | Receipt of frames for which the semantics are unknown cannot be treated as an error | frame/ + conn/: unknown frame types must be skipped/ignored, and must bypass the stream-state validity table entirely. |
| `5-5.1-45` | informative | Stream identifiers are unsigned 31-bit integers. <br>_(all [CLIENT])_ | Streams are identified by an unsigned 31-bit integer. | frame/: stream id field parsed with the reserved high bit masked (uint31 helpers in internal/bytesx); ids never exceed 2^31-1. |
| `5-5.1-46` | MUST | Client-initiated streams must use odd stream identifiers; server-initiated streams must use even identifiers. <br>_(client,server [CLIENT])_ | Streams initiated by a client MUST use odd-numbered stream identifiers; those initiated by the server MUST use even-numbered stream identifiers. | conn/: id allocator emits odd ids starting at 1; validate inbound PUSH_PROMISE promised ids are even, else connection error. |
| `5-5.1-47` | informative | Stream identifier zero is reserved for connection control messages and cannot establish a stream. <br>_(all [CLIENT])_ | A stream identifier of zero (0x00) is used for connection control messages; the stream identifier of zero cannot be used to establish a new stream. | conn/ + frame/: HEADERS/DATA/RST_STREAM on stream 0 must be rejected (per-frame §6 rules pin the error codes); id allocator must never produce 0. |
| `5-5.1-48` | MUST | Each newly established stream's identifier must be numerically greater than all streams the initiator has opened or reserved (covers HEADERS-opened and PUSH_PROMISE-reserved streams). <br>_(endpoint,sender [CLIENT])_ | The identifier of a newly established stream MUST be numerically greater than all streams that the initiating endpoint has opened or reserved. | conn/: the B.2.1 deferred-allocation-under-wmu design exists exactly for this — verify concurrent SendHeaders callers cannot emit out-of-order ids. |
| `5-5.1-49` | MUST | An endpoint receiving an unexpected stream identifier must respond with a connection error of type PROTOCOL_ERROR. <br>_(endpoint,receiver [CLIENT])_ | An endpoint that receives an unexpected stream identifier MUST respond with a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: validate PUSH_PROMISE promised ids are strictly increasing even numbers, and reject non-monotonic/odd/reused server ids with ConnError(PROTOCOL_ERROR). |
| `5-5.1-50` | informative | When a stream leaves idle, all lower-valued idle streams the peer could have opened transition immediately to closed (skipped ids are closed). <br>_(all [CLIENT])_ | all streams in the "idle" state that might have been opened by the peer with a lower-valued stream identifier immediately transition to "closed". | conn/: registry must classify frames on skipped lower even ids as closed-stream receipt (tolerated/discarded), not idle-stream receipt (connection error). |
| `5-5.1-51` | informative | Stream identifiers are never reused; a client that exhausts identifiers establishes a new connection for new streams. <br>_(client [CLIENT])_ | A client that is unable to establish a new stream identifier can establish a new connection for new streams. | conn/ + client/: on approaching 2^31-1 odd ids, NewStream must fail permanently and the pool/managed-pool must dial a fresh connection; ids must never wrap. |
| `5-5.1-52` | informative | A server that exhausts stream identifiers can send GOAWAY, forcing the client to open a new connection. <br>_(server [CLIENT])_ | A server that is unable to establish a new stream identifier can send a GOAWAY frame so that the client is forced to open a new connection for new streams | conn/: covered by B.2.6 GOAWAY drain — NewStream returns ErrGoAway and client/ pool must route new requests to a new connection. |
| `5-5.1-53` | informative | SETTINGS_MAX_CONCURRENT_STREAMS is directional: it applies only to the peer receiving the setting — the server's value limits client-initiated streams, the client's value limits server-initiated (pushed) streams. <br>_(all [CLIENT])_ | clients specify the maximum number of concurrent streams the server can initiate, and servers specify the maximum number of concurrent streams the client can initiate. | conn/: NewStream currently gates on min(local advertised, peer advertised) per B.2.5 — the local-advertised half is stricter than RFC requires (locally advertised value only limits server pushes); confirm this over-restriction is intentional. |
| `5-5.1-54` | informative | Streams in open or either half-closed state count toward the SETTINGS_MAX_CONCURRENT_STREAMS limit. <br>_(all [CLIENT])_ | Streams that are in the "open" state or in either of the "half-closed" states count toward the maximum number of streams that an endpoint is permitted to open. | conn/: the inflight counter must decrement only when a stream fully closes — a stream that sent END_STREAM but hasn't received it (half-closed) still counts. |
| `5-5.1-55` | informative | Streams in either reserved state do not count toward the concurrent-stream limit. <br>_(all [CLIENT])_ | Streams in either of the "reserved" states do not count toward the stream limit. | conn/: reserved(remote) pushed streams must be excluded from the client's outbound MAX_CONCURRENT_STREAMS accounting until they open. |
| `5-5.1-56` | MUST NOT | Endpoints must not exceed the concurrent-stream limit set by their peer. <br>_(endpoint,sender [CLIENT])_ | Endpoints MUST NOT exceed the limit set by their peer. | conn/: the ErrTooManyStreams gate in NewStream; verify explicit peer value 0 refuses all new streams (lookupPeerSetting absent-vs-zero distinction, B.2.5). |
| `5-5.1-57` | MUST | An endpoint receiving a HEADERS frame that exceeds its advertised concurrent-stream limit must treat it as a stream error of type PROTOCOL_ERROR or REFUSED_STREAM. <br>_(endpoint,receiver [CLIENT])_ | receives a HEADERS frame that causes its advertised concurrent stream limit to be exceeded MUST treat this as a stream error (Section 5.4.2) of type PROTOCOL_ERROR or REFUSED_STREAM | conn/: if a pushed stream's HEADERS would exceed the client's advertised limit, respond with RST_STREAM (REFUSED_STREAM or PROTOCOL_ERROR) — a stream error, not connection teardown. |
| `5-5.1-58` | informative | The choice between PROTOCOL_ERROR and REFUSED_STREAM signals whether automatic retry (Section 8.7) is desired. <br>_(all [CLIENT])_ | The choice of error code determines whether the endpoint wishes to enable automatic retry (see Section 8.7 for details). | client/: retry logic should treat a received RST_STREAM(REFUSED_STREAM) as safely retryable (request not processed) and PROTOCOL_ERROR as not automatically retryable. |
| `5-5.1-59` | informative | An endpoint reducing SETTINGS_MAX_CONCURRENT_STREAMS below the current open-stream count can either close excess streams or let them complete. <br>_(endpoint [CLIENT])_ | can either close streams that exceed the new value or allow streams to complete. | conn/: if the client ever lowers its advertised limit mid-connection, existing pushed streams need not be reset; also tolerate the server lowering its limit without resetting our in-flight streams. |

### 9113 section 5.2-5.3 - RFC 9113 Flow Control (5.2) and Prioritization (5.3)

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `5.2-5.3-1` | informative | Flow control operates at two levels simultaneously: per individual stream and for the connection as a whole, implemented via the WINDOW_UPDATE frame. <br>_(all [CLIENT])_ | Flow control is used for both individual streams and the connection as a whole. | conn/: verify dual accounting — independent per-stream and connection-level send/recv windows, both consulted in writeData/acquireSendCredits and both refunded via WINDOW_UPDATE. |
| `5.2-5.3-2` | informative | Flow control is hop-by-hop: it applies only between the two endpoints of a single connection, never end-to-end across intermediaries. <br>_(all [CLIENT])_ | HTTP/2 flow control operates between the endpoints of a single hop and not over the entire end-to-end path. | conn/: window state must be per-Conn only; verify pool/managed-pool in client/ never shares or aggregates flow-control state across connections. |
| `5.2-5.3-3` | informative | Flow control is a credit-based scheme: receivers advertise via WINDOW_UPDATE how many octets they are prepared to receive per stream and for the whole connection. <br>_(receiver [CLIENT])_ | Receivers advertise how many octets they are prepared to receive on a stream and for the entire connection. This is a credit-based scheme. | conn/: refund path must emit WINDOW_UPDATE both on stream id>0 and stream 0 (connection); credits are additive, never absolute-set. |
| `5.2-5.3-4` | MAY | A receiver MAY set any window size it desires for each stream and for the connection. <br>_(receiver [CLIENT])_ | A receiver MAY choose to set any window size that it desires for each stream and for the entire connection. | conn/: fixed connRecvWindow=65535 plus SETTINGS_INITIAL_WINDOW_SIZE for streams is a conformant local-policy choice; no action needed, confirm sizes stay <= 2^31-1. |
| `5.2-5.3-5` | MUST | A sender MUST respect the flow-control limits imposed by the receiver and never send DATA beyond the advertised window. <br>_(sender [CLIENT])_ | A sender MUST respect flow-control limits imposed by a receiver. | conn/: acquireSendCredits (B.2.3) must block until BOTH per-stream and connection send windows have credit; verify writeData chunking can never overshoot either window, including after mid-conn SETTINGS_INITIAL_WINDOW_SIZE deltas. |
| `5.2-5.3-6` | informative | Every role (client, server, intermediary) independently advertises its own receive window and independently abides by the peer's limits when sending; the two directions are decoupled. <br>_(client,server,intermediary [CLIENT])_ | Clients, servers, and intermediaries all independently advertise their flow-control window as a receiver and abide by the flow-control limits set by their peer when sending. | conn/: receive-side accounting (recvWindowRefundThreshold refunds) and send-side accounting (fcOut windows) must have zero cross-coupling; verify our own advertised window never influences send-credit math. |
| `5.2-5.3-7` | informative | The initial flow-control window value is 65,535 octets for both new streams and the overall connection. <br>_(all [CLIENT])_ | The initial value for the flow-control window is 65,535 octets for both new streams and the overall connection. | conn/: send windows must start at 65535 (stream windows change only via peer SETTINGS_INITIAL_WINDOW_SIZE; connection window changes ONLY via WINDOW_UPDATE, never via SETTINGS); matches connRecvWindow=65535 gotcha. |
| `5.2-5.3-8` | informative | Only DATA frames are subject to flow control; no other frame type consumes space in the advertised window, so control frames are never blocked by flow control. <br>_(all [CLIENT])_ | only DATA frames are subject to flow control; all other frame types do not consume space in the advertised flow-control window. | conn/: verify window accounting charges DATA only (entire frame payload including padding); HEADERS/CONTINUATION/WINDOW_UPDATE/PING/SETTINGS must never be charged nor blocked in acquireSendCredits. |
| `5.2-5.3-9` | informative | An endpoint may disable its own flow control (as receiver) but cannot ignore flow-control signals from its peer (as sender). <br>_(endpoint [CLIENT])_ | An endpoint can choose to disable its own flow control, but an endpoint cannot ignore flow-control signals from its peer. | conn/: no configuration (large local recv windows, refund tuning) may create a send path that bypasses peer WINDOW_UPDATE/SETTINGS accounting; acquireSendCredits must be unconditional. |
| `5.2-5.3-10` | informative | The spec defines only WINDOW_UPDATE format and semantics; when a receiver sends updates, what values it sends, and how a sender schedules packets are free implementation choices. <br>_(all [CLIENT])_ | This document does not stipulate how a receiver decides when to send this frame or the value that it sends, nor does it specify how a sender chooses to send packets. | conn/: recvWindowRefundThreshold=32KiB batched refunds (B.2.2) are an explicitly permitted algorithm choice; no conformance issue, keep documented as tunable. |
| `5.2-5.3-11` | informative | Implementations are responsible for prioritizing sending of requests/responses, avoiding head-of-line blocking for requests, and managing creation of new streams; these choices interact with flow control. <br>_(all [CLIENT])_ | Implementations are also responsible for prioritizing the sending of requests and responses, choosing how to avoid head-of-line blocking for requests, and managing the creation of new streams. | client/ + conn/: NewStream gating (MAX_CONCURRENT_STREAMS, B.2.5) and wmu write serialization are local policy; verify a stream blocked in acquireSendCredits cannot starve control frames or other streams' writes indefinitely. |
| `5.2-5.3-12` | informative | A deployment not needing flow control can advertise the maximum window (2^31-1) and maintain it by sending WINDOW_UPDATE whenever data is received, effectively disabling receiver-side flow control. <br>_(receiver [CLIENT])_ | advertise a flow-control window of the maximum size (2^31-1) and can maintain this window by sending a WINDOW_UPDATE frame when any data is received. | conn/: optional pattern for a load generator; if adopted, verify refund arithmetic can never grow a window past 2^31-1 (typed ConnError FLOW_CONTROL_ERROR per §6.9.1). |
| `5.2-5.3-13` | informative | A sender is always subject to the receiver's advertised flow-control window, even when that receiver has effectively disabled its own flow control. <br>_(sender [CLIENT])_ | Conversely, a sender is always subject to the flow-control window advertised by the receiver. | conn/: confirm there is no fast-path skipping acquireSendCredits even when peer advertises huge windows — credit check must run on every DATA write. |
| `5.2-5.3-14` | MUST | Endpoints MUST read and process HTTP/2 frames from the TCP receive buffer as soon as data is available. <br>_(endpoint [CLIENT])_ | Endpoints MUST read and process HTTP/2 frames from the TCP receive buffer as soon as data is available. | conn/: reader goroutine must never block on application consumption — verify events-channel-full path (drop + RST REFUSED_STREAM) keeps the read loop draining, and that no lock ordering lets a blocked writer stall the reader. |
| `5.2-5.3-15` | informative | Failure to read frames promptly can deadlock the connection when critical frames such as WINDOW_UPDATE go unread and unacted upon; prompt reading does not risk resource exhaustion because flow control bounds commitments. <br>_(endpoint [CLIENT])_ | Failure to read promptly could lead to a deadlock when critical frames, such as WINDOW_UPDATE, are not read and acted upon. | conn/: test the deadlock shape — client blocked in acquireSendCredits must still have the reader processing inbound WINDOW_UPDATE that replenishes credit (fcOutCond broadcast on OnWindowUpdate). |
| `5.2-5.3-16` | informative | If the peer's available window is not kept larger than the peer's bandwidth-delay product, receive throughput is limited by flow control and performance degrades. <br>_(endpoint [CLIENT])_ | its receive throughput will be limited by HTTP/2 flow control. This will result in degraded performance. | conn/ + client/: perf note for a load generator — default 65535 windows cap throughput on high-BDP links; expose SETTINGS_INITIAL_WINDOW_SIZE and connection WINDOW_UPDATE sizing as client options. |
| `5.2-5.3-17` | informative | Sending timely WINDOW_UPDATE frames improves performance; endpoints should balance receive throughput against resource-exhaustion risk per Section 10.5 when defining window-management strategy. <br>_(endpoint [CLIENT])_ | Sending timely WINDOW_UPDATE frames can improve performance. | conn/: recvWindowRefundThreshold=32KiB is exactly this tradeoff (chatter vs stall tolerance); cross-check against §10.5 limits guidance when auditing that section. |
| `5.2-5.3-18` | informative | Prioritizing allocation of bandwidth and computation among streams is critical to performance; a poor scheme can make HTTP/2 perform worse than HTTP/1.1 since there is no TCP-level parallelism. <br>_(all [CLIENT])_ | A poor prioritization scheme can result in HTTP/2 providing poor performance. | client/: no priority scheme is a valid choice for a load generator; document the deliberate absence rather than implementing RFC 7540 trees. |
| `5.2-5.3-19` | informative | Clients can possess request-priority knowledge relevant to server scheduling, and having clients provide priority information can improve performance. <br>_(client [CLIENT])_ | having clients provide priority information can improve performance. | client/: optional capability only — nothing to implement; if ever added, prefer the RFC 9218 Priority header field, not RFC 7540 signals (see 5.2-5.3-23). |
| `5.2-5.3-20` | informative | Background: the RFC 7540 priority-signaling system was complex, unevenly implemented in clients and servers, widely ignored by server deployments, and ultimately not successful. <br>_(all [CLIENT])_ | In short, the prioritization signaling in RFC 7540 [RFC7540] was not successful. | client/ + conn/: justifies never emitting PRIORITY frames or HEADERS priority fields; no reconcile action beyond confirming the client does not send them. |
| `5.2-5.3-21` | informative | RFC 9113 deprecates the RFC 7540 priority signaling; the bulk of the priority text is removed from this document. <br>_(all [CLIENT])_ | This update to HTTP/2 deprecates the priority signaling defined in RFC 7540 [RFC7540]. | client/ + conn/: confirm the client sends no PRIORITY frames and never sets the PRIORITY flag on HEADERS it emits; deprecated-but-parseable is the target posture. |
| `5.2-5.3-22` | informative | The priority frame-field descriptions and some mandatory handling are retained so RFC 9113 implementations stay interoperable with RFC 7540 implementations that still use priority signaling. <br>_(all [CLIENT])_ | The description of frame fields and some of the mandatory handling is retained to ensure that implementations of this document remain interoperable | frame/: must still parse the PRIORITY frame layout (exclusive bit + 31-bit stream dependency + weight) and the HEADERS PRIORITY-flag field block; conn/ must accept inbound PRIORITY frames and priority-flagged HEADERS without connection error, then ignore the signal. |
| `5.2-5.3-23` | informative | Where priority signaling matters, endpoints are encouraged to use an alternative scheme such as HTTP-PRIORITY (Extensible Priorities, RFC 9218) instead of RFC 7540 signals. <br>_(endpoint [CLIENT])_ | endpoints are encouraged to use an alternative scheme, such as the scheme described in [HTTP-PRIORITY]. | client/: RFC 9218 'priority' is an ordinary header field — already sendable through the generic header API with zero protocol work; nothing to implement in conn/ or frame/. |
| `5.2-5.3-24` | informative | Endpoints that receive RFC 7540 priority signals in HEADERS or PRIORITY frames can still benefit from applying them and should not discard them when no better information exists. <br>_(endpoint [CLIENT])_ | Endpoints that receive priority signals in HEADERS or PRIORITY frames can benefit from applying that information. | conn/: for a client the only obligation is graceful tolerance — receiving a PRIORITY frame or priority-flagged HEADERS must not error or alter stream state; applying the signal is server-side scheduling and out of scope. |
| `5.2-5.3-25` | SHOULD | Servers SHOULD use other contextual information to determine request priority when no priority signals are present. <br>_(server [not-client])_ | Servers SHOULD use other contextual information in determining priority of requests in the absence of any priority signals. | Server-only scheduling guidance; no client package affected. |
| `5.2-5.3-26` | MAY | Servers MAY interpret the complete absence of priority signals as the client not implementing the feature. <br>_(server [not-client])_ | Servers MAY interpret the complete absence of signals as an indication that the client has not implemented the feature. | Server-only; indirectly validates the client strategy of sending no signals at all — total silence is a recognized, well-defined state. |
| `5.2-5.3-27` | informative | The RFC 7540 Section 5.3.5 priority defaults are known to perform poorly under most conditions, and their use is unlikely to be deliberate — clients should send nothing rather than 7540 defaults. <br>_(all [CLIENT])_ | The defaults described in Section 5.3.5 of [RFC7540] are known to have poor performance under most conditions, and their use is unlikely to be deliberate. | client/ + conn/: confirm HEADERS emission never populates the RFC 7540 default priority fields (stream dependency 0, weight 16) — omit the PRIORITY flag entirely. |

### 9113 section 5.4-5.5 - RFC 9113 — Error Handling (5.4) and Extending HTTP/2 (5.5)

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `5.4-5.5-1` | informative | HTTP/2 framing defines exactly two error classes: connection errors (render the whole connection unusable) and stream errors (affect one stream only). <br>_(endpoint [CLIENT])_ | An error condition that renders the entire connection unusable is a connection error. | conn/: verify error taxonomy is split into typed ConnError vs stream-level error that maps to RST_STREAM, not one blended path. |
| `5.4-5.5-2` | SHOULD | An implementation may discover multiple errors while processing, but should report at most one stream error per stream and one connection error overall. <br>_(endpoint [CLIENT])_ | Implementations MAY discover multiple errors during processing, but they SHOULD report at most one stream and one connection error as a result. | conn/: reader loop must stop dispatching after the first connection error (single GOAWAY) and dedupe per-stream error reporting. |
| `5.4-5.5-3` | informative | The first stream error reported for a stream suppresses reporting of any subsequent errors on that same stream. <br>_(endpoint [CLIENT])_ | The first stream error reported for a given stream prevents any other errors on that stream from being reported. | conn/: once a stream is reset (RST sent or received), later error conditions on that stream must not emit additional RST_STREAM or surface duplicate events. |
| `5.4-5.5-4` | SHOULD | Although multiple GOAWAY frames are permitted, an endpoint should report only one type of connection error, except when an error occurs during graceful shutdown. <br>_(endpoint [CLIENT])_ | an endpoint SHOULD report just one type of connection error unless an error is encountered during graceful shutdown | conn/: if Conn ever sends GOAWAY (Close or fatal error), ensure at most one error-code GOAWAY; graceful NO_ERROR GOAWAY is the only case that may be followed by another. |
| `5.4-5.5-5` | MAY | If an error occurs during graceful shutdown, the endpoint may send an additional GOAWAY carrying the new error code after a prior NO_ERROR GOAWAY. <br>_(endpoint [CLIENT])_ | an endpoint MAY send an additional GOAWAY frame with the new error code, in addition to any prior GOAWAY that contained NO_ERROR | conn/: permissive — client must also tolerate RECEIVING a second GOAWAY with a different code/lower lastStreamID after a NO_ERROR one (B.2.6 drain path). |
| `5.4-5.5-6` | MAY | When multiple different errors are detected, the endpoint may pick any one of them to report. <br>_(endpoint [CLIENT])_ | If an endpoint detects multiple different errors, it MAY choose to report any one of those errors. | conn/: permissive — no requirement to rank error codes; first-detected wins is conformant. |
| `5.4-5.5-7` | MUST | If a frame causes a connection error, that error must be reported — it cannot be silently swallowed. <br>_(endpoint [CLIENT])_ | If a frame causes a connection error, that error MUST be reported. | conn/: every conn-error condition raised by frame/ parsing or conn/ validation must reach the GOAWAY+close path; audit for any code path that drops a typed ConnError without emitting GOAWAY. |
| `5.4-5.5-8` | MAY | An endpoint may use any applicable error code; generic codes like PROTOCOL_ERROR or INTERNAL_ERROR are always acceptable substitutes for more specific ones. <br>_(endpoint [CLIENT])_ | an endpoint MAY use any applicable error code when it detects an error condition | conn/ + frame/: permissive — using PROTOCOL_ERROR where a more specific code exists is conformant; also means peer may send generic codes, so client must not require specific codes. |
| `5.4-5.5-9` | informative | A connection error is defined as any error preventing further frame-layer processing or corrupting connection state. <br>_(endpoint [CLIENT])_ | A connection error is any error that prevents further processing of the frame layer or corrupts any connection state. | conn/ + hpack/: classification check — framing errors and HPACK decode failures corrupt shared state and must be treated as connection errors, never stream errors. |
| `5.4-5.5-10` | SHOULD | On a connection error, the endpoint should first send a GOAWAY carrying the stream identifier of the last stream it successfully received from the peer. <br>_(endpoint [CLIENT])_ | An endpoint that encounters a connection error SHOULD first send a GOAWAY frame (Section 6.8) with the stream identifier of the last stream that it successfully received from its peer. | conn/: verify the fatal-error path writes GOAWAY before closing; for a client, lastStreamID is the highest server-initiated (even) stream received — 0 when push is disabled. |
| `5.4-5.5-11` | MUST | After sending a GOAWAY for an error condition, the endpoint must close the TCP connection. <br>_(endpoint [CLIENT])_ | After sending the GOAWAY frame for an error condition, the endpoint MUST close the TCP connection. | conn/: error-path GOAWAY must be followed by net.Conn close, not a lingering half-alive connection; also frees blocked acquireSendCredits writers via broadcast. |
| `5.4-5.5-12` | informative | GOAWAY delivery on a connection error is best-effort only; it may never reach the peer. <br>_(endpoint [CLIENT])_ | GOAWAY only provides a best-effort attempt to communicate with the peer about why the connection is being terminated. | conn/: do not block teardown on GOAWAY write success — write with short deadline / ignore write error, then close. |
| `5.4-5.5-13` | MAY | An endpoint may end the connection at any time and may escalate a stream error into a connection error. <br>_(endpoint [CLIENT])_ | an endpoint MAY choose to treat a stream error as a connection error. | conn/: permissive — escalation is allowed; also means the client must tolerate a server tearing down the connection over what looks like a per-stream issue. |
| `5.4-5.5-14` | SHOULD | Endpoints should send a GOAWAY frame when ending a connection, whenever circumstances permit. <br>_(endpoint [CLIENT])_ | Endpoints SHOULD send a GOAWAY frame when ending a connection, providing that circumstances permit it. | conn/: check Conn.Close() writes GOAWAY(NO_ERROR, lastStreamID) before closing TCP — clients are endpoints too; a bare TCP close on graceful shutdown misses this SHOULD. |
| `5.4-5.5-15` | informative | On detecting a stream error, an endpoint sends a RST_STREAM frame carrying the affected stream's identifier and an error code indicating the error type. <br>_(endpoint [CLIENT])_ | An endpoint that detects a stream error sends a RST_STREAM frame (Section 6.4) that contains the stream identifier of the stream where the error occurred. | conn/: stream-error path must emit RST_STREAM with the correct stream id and code (e.g. event-buffer overflow → RST(REFUSED_STREAM), malformed response → RST(PROTOCOL_ERROR)). |
| `5.4-5.5-16` | informative | RST_STREAM is the last frame an endpoint can send on a stream; nothing may follow it on that stream. <br>_(sender [CLIENT])_ | A RST_STREAM is the last frame that an endpoint can send on a stream. | conn/: after the client sends RST on a stream, writeData/writeHeaders/trailers for that stream must be refused (ErrStreamClosed), and no further RST or stream frames emitted. |
| `5.4-5.5-17` | MUST | After sending RST_STREAM, the sender must be prepared to receive frames the remote peer already sent or enqueued for that stream. <br>_(sender [CLIENT])_ | The peer that sends the RST_STREAM frame MUST be prepared to receive any frames that were sent or enqueued for sending by the remote peer. | conn/: reader must tolerate HEADERS/DATA/WINDOW_UPDATE arriving on a stream the client just reset — drop them without raising a connection error or panicking on a missing registry entry. |
| `5.4-5.5-18` | informative | Frames received after sending RST_STREAM can be ignored, except those that modify connection state such as field-section compression state or flow control. <br>_(sender [CLIENT])_ | These frames can be ignored, except where they modify connection state (such as the state maintained for field section compression (Section 4.3) or flow control). | conn/ + hpack/: critical — HEADERS/CONTINUATION on a reset stream must still run through the HPACK decoder to keep the dynamic table synchronized, and DATA on a reset stream must still be charged to (and refunded from) the CONNECTION recv window. |
| `5.4-5.5-19` | SHOULD NOT | An endpoint should not normally send more than one RST_STREAM frame for any given stream. <br>_(endpoint [CLIENT])_ | Normally, an endpoint SHOULD NOT send more than one RST_STREAM frame for any stream. | conn/: dedupe RST emission per stream — a second error condition on an already-reset stream must not write another RST_STREAM. |
| `5.4-5.5-20` | MAY | An endpoint may send additional RST_STREAM frames if it keeps receiving frames on a closed stream more than a round-trip time after closing it. <br>_(endpoint [CLIENT])_ | an endpoint MAY send additional RST_STREAM frames if it receives frames on a closed stream after more than a round-trip time | conn/: permissive escape hatch for misbehaving peers — not required; if implemented, must still respect fact 21 (never RST in response to RST). |
| `5.4-5.5-21` | MUST NOT | An endpoint must never send a RST_STREAM in response to a received RST_STREAM frame, to prevent RST loops. <br>_(endpoint [CLIENT])_ | To avoid looping, an endpoint MUST NOT send a RST_STREAM in response to a RST_STREAM frame. | conn/: audit OnRSTStream handler — receiving RST (even on unknown/closed streams or with bogus codes) must never trigger an outgoing RST_STREAM. |
| `5.4-5.5-22` | informative | If the TCP connection closes or resets while streams are open or half-closed, those streams cannot be automatically retried (see 8.7 for retry conditions). <br>_(endpoint [CLIENT])_ | If the TCP connection is closed or reset while streams remain in the "open" or "half-closed" states, then the affected streams cannot be automatically retried | client/: retry layer — on connection teardown with in-flight streams, auto-retry only under §8.7 conditions (idempotent, or GOAWAY proves unprocessed); blind replay of in-flight non-idempotent requests violates this. |
| `5.4-5.5-23` | informative | Protocol extensions are effective only within the scope of a single HTTP/2 connection; they do not span connections. <br>_(all [CLIENT])_ | Extensions are effective only within the scope of a single HTTP/2 connection. | conn/: no extension state (settings, frame-type support) may be carried across pooled connections in client/ — each Conn negotiates independently. |
| `5.4-5.5-24` | MAY | Extensions are permitted to introduce new frame types, new settings, or new error codes via the RFC 7540 registries. <br>_(all [CLIENT])_ | Extensions are permitted to use new frame types (Section 4.1), new settings (Section 6.5), or new error codes (Section 7). | frame/: implication for the codec — the peer may legitimately send frame types, settings ids, and error codes outside the RFC-defined ranges; parser must have a defined non-error path for each. |
| `5.4-5.5-25` | MUST | Implementations must ignore unknown or unsupported values in all extensible protocol elements (settings identifiers, error codes, etc.). <br>_(all [CLIENT])_ | Implementations MUST ignore unknown or unsupported values in all extensible protocol elements. | conn/: applyPeerSettings must skip unknown SETTINGS identifiers (while still ACKing the frame), and unknown error codes in RST_STREAM/GOAWAY must be accepted without raising a connection error. |
| `5.4-5.5-26` | MUST | Implementations must discard frames whose type is unknown or unsupported rather than treating them as an error. <br>_(all [CLIENT])_ | Implementations MUST discard frames that have unknown or unsupported types. | frame/ + conn/: parser must consume and drop unknown frame types (respecting MAX_FRAME_SIZE) without error; conn/ reader must not conn-error on them; unknown frames also carry no flow-control or state semantics. |
| `5.4-5.5-27` | MUST | Extension (unknown-type) frames appearing in the middle of a field block are forbidden and must be treated as a connection error of type PROTOCOL_ERROR. <br>_(all [CLIENT])_ | extension frames that appear in the middle of a field block (Section 4.3) are not permitted; these MUST be treated as a connection error (Section 5.4.1) of type PROTOCOL_ERROR | conn/ + frame/: while a field block is open (HEADERS without END_HEADERS awaiting CONTINUATION), ANY interleaved frame — including unknown types — must be ConnError(PROTOCOL_ERROR); verify the unknown-frame discard path (fact 26) does not bypass this contiguity check. |
| `5.4-5.5-28` | SHOULD | Extensions should avoid changing protocol elements defined in RFC 9113 or elements with no defined extension mechanism (frame layouts, pseudo-header definitions, message composition). <br>_(all [not-client])_ | Extensions SHOULD avoid changing protocol elements defined in this document or elements for which no extension mechanism is defined. | N/A for this client (extension-designer guidance); relevant only if poseidon ever ships a custom extension — document in docs/ if so. |
| `5.4-5.5-29` | MUST | An extension that changes existing protocol elements or state must be negotiated with the peer before being used. <br>_(all [CLIENT])_ | An extension that changes existing protocol elements or state MUST be negotiated before being used. | conn/: verify the client never emits non-RFC frame types, settings, or altered frame layouts without prior negotiation — currently it defines no extensions, so conformance is by absence; keep it that way or add negotiation. |
| `5.4-5.5-30` | MUST | If a setting is used to negotiate an extension, its initial value must be defined such that the extension starts disabled. <br>_(all [not-client])_ | If a setting is used for extension negotiation, the initial value MUST be defined in such a fashion that the extension is initially disabled. | N/A (extension-designer rule); only applies if poseidon defines its own extension setting — default-off would then be mandatory. |

### 9113 section 6.1-6.4 - RFC 9113 Frame Definitions: DATA, HEADERS, PRIORITY, RST_STREAM

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `6.1-6.4-1` | informative | Each frame type is identified by a unique 8-bit type code; endpoints dispatch on it. <br>_(all [CLIENT])_ | This specification defines a number of frame types, each identified by a unique 8-bit type code. | frame/: parser dispatches on the 8-bit Type field (0x00 DATA, 0x01 HEADERS, 0x02 PRIORITY, 0x03 RST_STREAM). |
| `6.1-6.4-2` | informative | DATA frames (type 0x00) carry arbitrary variable-length octet sequences associated with a stream, e.g. HTTP message contents. <br>_(all [CLIENT])_ | DATA frames (type=0x00) convey arbitrary, variable-length sequences of octets associated with a stream. | frame/: DATA type code = 0x00; payload is opaque bytes handed to conn/ stream. |
| `6.1-6.4-3` | MAY | A sender is permitted to add padding to DATA frames to obscure message size. <br>_(sender [CLIENT])_ | DATA frames MAY also contain padding. | frame/: writer need not emit padding (optional), but parser MUST handle PADDED DATA from servers. |
| `6.1-6.4-4` | informative | DATA payload layout: optional 8-bit Pad Length, then Data, then up to 2040 bits (255 octets) of Padding. <br>_(all [CLIENT])_ | [Pad Length (8)], Data (..), Padding (..2040), | frame/ (padding strip helper in internal/bytesx): verify parse order Pad Length -> Data -> Padding and 255-octet max padding. |
| `6.1-6.4-5` | informative | The DATA Pad Length field exists only when the PADDED flag is set. <br>_(receiver [CLIENT])_ | This field is conditional and is only present if the PADDED flag is set. | frame/: consume the 1-byte Pad Length only when flag 0x08 is set; never on unpadded DATA. |
| `6.1-6.4-6` | informative | Data length is computed as frame payload length minus the lengths of Pad Length and Padding fields present. <br>_(receiver [CLIENT])_ | The amount of data is the remainder of the frame payload after subtracting the length of the other fields that are present. | frame/: data = Length - 1 (Pad Length byte, if PADDED) - PadLength; check underflow handling. |
| `6.1-6.4-7` | MUST | When sending padded DATA, all padding octets must be zero. <br>_(sender [CLIENT])_ | Padding octets MUST be set to zero when sending. | frame/: if writer ever emits PADDED DATA, padding bytes must be zeroed (watch buffer-pool reuse leaking stale bytes). |
| `6.1-6.4-8` | MAY | A receiver need not verify padding contents but may treat non-zero padding as connection error PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | A receiver is not obligated to verify padding but MAY treat non-zero padding as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/: decide policy — skipping padding without verification is conformant; if verifying, error must be conn-level PROTOCOL_ERROR. |
| `6.1-6.4-9` | informative | DATA PADDED flag (0x08) signals presence of Pad Length field and padding. <br>_(all [CLIENT])_ | When set, the PADDED flag indicates that the Pad Length field and any padding that it describes are present. | frame/: PADDED bit value is 0x08 on DATA; verify flag constant. |
| `6.1-6.4-10` | informative | DATA END_STREAM flag (0x01) marks the last frame the endpoint sends on the stream and transitions it to half-closed or closed. <br>_(endpoint [CLIENT])_ | Setting this flag causes the stream to enter one of the "half-closed" states or the "closed" state (Section 5.1). | conn/: END_STREAM on sent DATA -> localEnded; on received DATA -> remoteEnded; both -> closed and recycle-eligible (see recycleStream race note). |
| `6.1-6.4-11` | informative | An endpoint that has sent all data can close the stream with a zero-length DATA frame carrying END_STREAM (RFC text says 'STREAM frame' — a known erratum, it means DATA), unless it sends trailers. <br>_(endpoint [CLIENT])_ | An endpoint that learns of stream closure after sending all data can close a stream by sending a STREAM frame with a zero-length Data field and the END_STREAM flag set. | conn/: verify client can emit empty DATA+END_STREAM to half-close (e.g. GET with body already flushed); quote contains RFC 9113 erratum 'STREAM frame'. |
| `6.1-6.4-12` | MUST | A DATA frame on stream 0 is a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If a DATA frame is received whose Stream Identifier field is 0x00, the recipient MUST respond with a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ or conn/: check where stream-id-0 rejection for DATA lives; must yield conn-level ConnError(PROTOCOL_ERROR), not stream RST. |
| `6.1-6.4-13` | informative | DATA frames are flow controlled and may only be sent while the stream is open or half-closed (remote). <br>_(sender [CLIENT])_ | DATA frames are subject to flow control and can only be sent when a stream is in the "open" or "half-closed (remote)" state. | conn/: writeData must refuse after localEnded (END_STREAM sent) or stream reset; verify state gate before acquireSendCredits. |
| `6.1-6.4-14` | informative | Flow-control accounting charges the entire DATA frame payload including Pad Length and Padding fields. <br>_(all [CLIENT])_ | The entire DATA frame payload is included in flow control, including the Pad Length and Padding fields if present. | conn/: recv-window debit must use the raw frame Length (pre-padding-strip), not the stripped Data length; check parseDataFrames vs bytesx strip ordering. |
| `6.1-6.4-15` | MUST | A DATA frame received on a stream not in open or half-closed (local) state gets a stream error STREAM_CLOSED. <br>_(receiver [CLIENT])_ | If a DATA frame is received whose stream is not in the "open" or "half-closed (local)" state, the recipient MUST respond with a stream error (Section 5.4.2) of type STREAM_CLOSED. | conn/: DATA after remoteEnded or on closed stream -> RST_STREAM(STREAM_CLOSED), a STREAM error not conn error; check unknown/evicted-stream path too. |
| `6.1-6.4-16` | MUST | DATA padding length equal to or exceeding the frame payload length is a connection error PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If the length of the padding is the length of the frame payload or greater, the recipient MUST treat this as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ (internal/bytesx padding strip): PadLength >= Length-1 must produce conn-level PROTOCOL_ERROR; boundary is 'or greater' (PadLength == payload-1 is legal only if Data empty... verify exact >= check against payload length). |
| `6.1-6.4-17` | informative | HEADERS (type 0x01) opens a stream and carries a field block fragment. <br>_(all [CLIENT])_ | The HEADERS frame (type=0x01) is used to open a stream (Section 5.1), and additionally carries a field block fragment. | conn/: SendHeaders is the idle->open transition; ties to deferred stream-id allocation gotcha (id==0 until first SendHeaders). |
| `6.1-6.4-18` | informative | A HEADERS frame may carry either a header section or a trailer section. <br>_(all [CLIENT])_ | Despite the name, a HEADERS frame can carry a header section or a trailer section. | conn/: client must accept a second HEADERS on a stream (response trailers) without treating it as malformed; probe trailer support explicitly (sibling-divergence risk). |
| `6.1-6.4-19` | informative | HEADERS may be sent in idle, reserved (local), open, or half-closed (remote) stream states. <br>_(sender [CLIENT])_ | HEADERS frames can be sent on a stream in the "idle", "reserved (local)", "open", or "half-closed (remote)" state. | conn/: client sends HEADERS only from idle (request) or open/half-closed(remote) (trailers); reserved(local) is server-push-side only. |
| `6.1-6.4-20` | informative | HEADERS payload layout: optional Pad Length, optional Exclusive/Stream Dependency/Weight (PRIORITY flag), Field Block Fragment, then Padding. <br>_(all [CLIENT])_ | [Pad Length (8)], [Exclusive (1)], [Stream Dependency (31)], [Weight (8)], Field Block Fragment (..), Padding (..2040), | frame/: parser must skip 5 priority octets when PRIORITY flag (0x20) set BEFORE handing fragment to hpack; verify field order and conditional presence. |
| `6.1-6.4-21` | informative | Priority signals carried in HEADERS frames are deprecated in RFC 9113 (change vs RFC 7540). <br>_(sender [CLIENT])_ | Priority signals in HEADERS frames are deprecated; see Section 5.3.2. | conn/: client should never set the PRIORITY flag on outgoing HEADERS; frame/ must still parse it when received. |
| `6.1-6.4-22` | informative | The HEADERS Pad Length field exists only when the PADDED flag is set. <br>_(receiver [CLIENT])_ | This field is only present if the PADDED flag is set. | frame/: same conditional Pad Length parse as DATA, but combined with optional 5-byte priority block ordering. |
| `6.1-6.4-23` | MUST | When sending padded HEADERS, padding octets must be zero (receiver MAY treat non-zero as connection error PROTOCOL_ERROR, same clause as DATA). <br>_(sender [CLIENT])_ | Padding octets MUST be set to zero when sending. | frame/: HEADERS writer padding (if ever emitted) zeroed; identical MUST/MAY pair as §6.1 repeated verbatim in §6.2. |
| `6.1-6.4-24` | informative | HEADERS PRIORITY flag (0x20) signals presence of Exclusive, Stream Dependency, and Weight fields. <br>_(receiver [CLIENT])_ | When set, the PRIORITY flag indicates that the Exclusive, Stream Dependency, and Weight fields are present. | frame/: on PRIORITY flag, consume 4-byte Exclusive+Dependency and 1-byte Weight before the field block fragment. |
| `6.1-6.4-25` | informative | END_HEADERS flag (0x04) means the frame holds the entire field block with no CONTINUATION following. <br>_(all [CLIENT])_ | When set, the END_HEADERS flag indicates that this frame contains an entire field block (Section 4.3) and is not followed by any CONTINUATION frames. | conn/: only pass a complete reassembled field block to hpack.Decoder once END_HEADERS observed. |
| `6.1-6.4-26` | MUST | A HEADERS frame without END_HEADERS must be followed by a CONTINUATION frame on the same stream. <br>_(sender [CLIENT])_ | A HEADERS frame without the END_HEADERS flag set MUST be followed by a CONTINUATION frame for the same stream. | conn//frame/: if writeHeaders splits large field blocks, subsequent frames must be CONTINUATION on the same stream with END_HEADERS only on the last. |
| `6.1-6.4-27` | MUST | While a field block is incomplete, any frame of another type or on another stream is a connection error PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | A receiver MUST treat the receipt of any other type of frame or a frame on a different stream as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: reader must track expect-CONTINUATION state (stream id + type) after HEADERS w/o END_HEADERS and fail whole conn with PROTOCOL_ERROR on any interleaved frame, including PRIORITY. |
| `6.1-6.4-28` | informative | A HEADERS frame with END_STREAM may still be followed by CONTINUATION frames, which are logically part of it. <br>_(receiver [CLIENT])_ | a HEADERS frame with the END_STREAM flag set can be followed by CONTINUATION frames on the same stream. | conn/: do not finalize remoteEnded/stream-close until END_HEADERS arrives even when END_STREAM was set on the HEADERS frame. |
| `6.1-6.4-29` | informative | A field block too large for one HEADERS frame is continued in CONTINUATION frames. <br>_(sender [CLIENT])_ | A field block that does not fit within a HEADERS frame is continued in a CONTINUATION frame (Section 6.10). | conn/: client sending headers larger than peer MAX_FRAME_SIZE must split into HEADERS+CONTINUATION, not oversize the HEADERS frame. |
| `6.1-6.4-30` | MUST | A HEADERS frame on stream 0 is a connection error PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If a HEADERS frame is received whose Stream Identifier field is 0x00, the recipient MUST respond with a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ or conn/: HEADERS with stream id 0 -> conn-level PROTOCOL_ERROR; verify parseFrameHeaders path. |
| `6.1-6.4-31` | informative | HEADERS frames mutate connection-wide state (the HPACK dynamic table) per Section 4.3, so every field block must be decoded even for streams being discarded. <br>_(all [CLIENT])_ | The HEADERS frame changes the connection state as described in Section 4.3. | hpack/ + conn/: decoder must process field blocks for RST/refused/evicted streams too, or dynamic-table state desyncs for the whole connection. |
| `6.1-6.4-32` | MUST | HEADERS padding length equal to or exceeding the frame payload length is a connection error PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If the length of the padding is the length of the frame payload or greater, the recipient MUST treat this as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ (internal/bytesx): same pad-overflow check as DATA but payload also shrinks by 5 when PRIORITY flag set — verify arithmetic accounts for the priority block. |
| `6.1-6.4-33` | informative | The PRIORITY frame type (0x02) is deprecated in RFC 9113 (change vs RFC 7540). <br>_(all [CLIENT])_ | The PRIORITY frame (type=0x02) is deprecated; see Section 5.3.2. | frame//conn/: client should not send PRIORITY frames; must still parse and validate received ones (deprecated != removed). |
| `6.1-6.4-34` | informative | PRIORITY frames are valid on a stream in any state, including idle and closed. <br>_(receiver [CLIENT])_ | The PRIORITY frame can be sent on a stream in any state, including "idle" or "closed". | **VERIFY** conn/: receiving PRIORITY for idle, closed, or unknown stream ids must NOT trigger any error or state change — verify no default 'unknown stream' rejection catches it. -- Quote is not verbatim. Published RFC 9113 §6.3 reads: 'A PRIORITY frame can be sent in any stream state, including idle or closed streams.' The fact misquotes it as 'The PRIORITY frame can be sent on a stream in any state, including "idle" or "closed".' — wrong opening word (The vs A), altered phrasing (on a stream in any state vs in any stream state), added quotation marks around idle/closed, and dropped trailing word 'streams'. The underlying requirement (PRIORITY valid in any state including idle/closed) is accurate. |
| `6.1-6.4-35` | informative | PRIORITY payload is exactly Exclusive (1 bit), Stream Dependency (31 bits), Weight (8 bits) — 5 octets, fixed. <br>_(all [CLIENT])_ | Exclusive (1), Stream Dependency (31), Weight (8), | frame/: PRIORITY payload layout is fixed 5 octets; parser may decode-and-discard but must consume exactly 5. |
| `6.1-6.4-36` | informative | The PRIORITY frame defines no flags; flag bits are unused. <br>_(receiver [CLIENT])_ | The PRIORITY frame does not define any flags. | frame/: ignore (do not error on) any set flag bits in a received PRIORITY frame. |
| `6.1-6.4-37` | MUST | A PRIORITY frame on stream 0 is a connection error PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If a PRIORITY frame is received with a stream identifier of 0x00, the recipient MUST respond with a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ or conn/: even though PRIORITY is otherwise ignorable, stream-id-0 must still be rejected as conn PROTOCOL_ERROR. |
| `6.1-6.4-38` | informative | Sending or receiving PRIORITY never changes any stream's state. <br>_(all [CLIENT])_ | Sending or receiving a PRIORITY frame does not affect the state of any stream (Section 5.1). | conn/: PRIORITY handling must be a no-op on the stream state machine (no open, no close, no idle-tracking side effects). |
| `6.1-6.4-39` | informative | A PRIORITY frame is not permitted between the frames of a single field block (HEADERS/CONTINUATION sequence). <br>_(sender [CLIENT])_ | A PRIORITY frame cannot be sent between consecutive frames that comprise a single field block (Section 4.3). | conn/: receiver side is enforced by the §6.2 continuation rule (fact 27) — PRIORITY mid-field-block is conn PROTOCOL_ERROR, no PRIORITY exception. |
| `6.1-6.4-40` | MUST | A PRIORITY frame whose length is not 5 octets is a STREAM error (not connection error) of type FRAME_SIZE_ERROR. <br>_(receiver [CLIENT])_ | A PRIORITY frame with a length other than 5 octets MUST be treated as a stream error (Section 5.4.2) of type FRAME_SIZE_ERROR. | frame/ + conn/: length!=5 -> RST_STREAM(FRAME_SIZE_ERROR) on that stream, NOT conn teardown — easy to get wrong since RST_STREAM's analogous check IS a conn error. |
| `6.1-6.4-41` | informative | RST_STREAM (type 0x03) immediately terminates a stream, for cancellation or error signaling. <br>_(all [CLIENT])_ | The RST_STREAM frame (type=0x03) allows for immediate termination of a stream. | conn/: client uses RST_STREAM for request cancellation (ctx cancel) and error paths (e.g. event-buffer overflow REFUSED_STREAM per CLAUDE.md). |
| `6.1-6.4-42` | informative | RST_STREAM payload is a single unsigned 32-bit error code explaining the termination. <br>_(all [CLIENT])_ | the RST_STREAM frame contains a single unsigned, 32-bit integer identifying the error code (Section 7). | frame/: RST_STREAM payload = one big-endian uint32 error code; surface it typed to conn/ (StreamError code). |
| `6.1-6.4-43` | informative | The RST_STREAM frame defines no flags. <br>_(receiver [CLIENT])_ | The RST_STREAM frame does not define any flags. | frame/: ignore any flag bits set on received RST_STREAM. |
| `6.1-6.4-44` | informative | RST_STREAM fully terminates the referenced stream, moving it to the closed state. <br>_(all [CLIENT])_ | The RST_STREAM frame fully terminates the referenced stream and causes it to enter the "closed" state. | conn/: OnRSTStream must transition to closed and make the stream recycle-eligible — see the recycleStream/teardown race memory (guard under one s.mu section). |
| `6.1-6.4-45` | MUST NOT | After receiving RST_STREAM on a stream, an endpoint must not send any further frames on it except PRIORITY. <br>_(endpoint [CLIENT])_ | After receiving a RST_STREAM on a stream, the receiver MUST NOT send additional frames for that stream, except for PRIORITY. | conn/: on peer RST, blocked writeData/acquireSendCredits and pending WINDOW_UPDATE refunds for that stream must be suppressed — no DATA, HEADERS, or WINDOW_UPDATE after RST received. |
| `6.1-6.4-46` | MUST | After sending RST_STREAM, the sender must still accept and process in-flight frames the peer sent before the RST arrived. <br>_(sender [CLIENT])_ | the sending endpoint MUST be prepared to receive and process additional frames sent on the stream | conn/: after client-sent RST (cancel), late DATA/HEADERS on that stream must not be treated as protocol errors, and their DATA must still be charged/refunded against connection flow control. |
| `6.1-6.4-47` | MUST | An RST_STREAM frame on stream 0 is a connection error PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If a RST_STREAM frame is received with a stream identifier of 0x00, the recipient MUST treat this as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ or conn/: RST_STREAM stream id 0 -> conn-level PROTOCOL_ERROR. |
| `6.1-6.4-48` | MUST NOT | An endpoint must never send RST_STREAM for a stream in the idle state. <br>_(sender [CLIENT])_ | RST_STREAM frames MUST NOT be sent for a stream in the "idle" state. | conn/: with deferred id allocation (Stream.id==0 until SendHeaders), verify cancel-before-SendHeaders never emits RST for an unopened/idle id. |
| `6.1-6.4-49` | MUST | Receiving RST_STREAM that identifies an idle stream is a connection error PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If a RST_STREAM frame identifying an idle stream is received, the recipient MUST treat this as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: RST for a client-side id never opened (or a server-side even id never promised) -> conn PROTOCOL_ERROR; requires tracking highest opened id, distinct from closed/evicted streams which must be tolerated. |
| `6.1-6.4-50` | MUST | An RST_STREAM frame whose length is not 4 octets is a CONNECTION error of type FRAME_SIZE_ERROR. <br>_(receiver [CLIENT])_ | A RST_STREAM frame with a length other than 4 octets MUST be treated as a connection error (Section 5.4.1) of type FRAME_SIZE_ERROR. | frame/: length!=4 -> conn-level FRAME_SIZE_ERROR (contrast with PRIORITY where wrong length is only a stream error). |

### 9113 section 6.5-6.6 - RFC 9113 Frame Definitions: SETTINGS (format, defined settings, synchronization) and PUSH_PROMISE

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `6.5-6.6-1` | MUST | Both endpoints must send a SETTINGS frame at the start of a connection and may send one at any later time. <br>_(endpoint [CLIENT])_ | A SETTINGS frame MUST be sent by both endpoints at the start of a connection and MAY be sent at any other time by either endpoint | conn/: handshake must write client SETTINGS immediately after the connection preface; verify mid-connection SETTINGS from server is also accepted at any time. |
| `6.5-6.6-2` | MUST | Implementations must support all six settings defined by the specification (0x01-0x06). <br>_(all [CLIENT])_ | Implementations MUST support all of the settings defined by this specification. | conn/: applyPeerSettings must handle HEADER_TABLE_SIZE, ENABLE_PUSH, MAX_CONCURRENT_STREAMS, INITIAL_WINDOW_SIZE, MAX_FRAME_SIZE, MAX_HEADER_LIST_SIZE — none may be silently unimplemented. |
| `6.5-6.6-3` | informative | Each setting replaces the prior value; settings are processed in order and the last value seen wins, with no other state required. <br>_(receiver [CLIENT])_ | the value of a SETTINGS parameter is the last value that is seen by a receiver. | conn/: applyPeerSettings must iterate entries in wire order so a duplicate identifier in one frame ends with the last value. |
| `6.5-6.6-4` | MUST | A SETTINGS frame with the ACK flag (0x01) set must carry an empty payload. <br>_(sender [CLIENT])_ | When this bit is set, the frame payload of the SETTINGS frame MUST be empty. | conn/ + frame/: the SETTINGS ACK the client emits after applying server settings must have zero-length payload. |
| `6.5-6.6-5` | MUST | Receiving a SETTINGS frame with ACK set and a non-zero length is a connection error of type FRAME_SIZE_ERROR. <br>_(receiver [CLIENT])_ | Receipt of a SETTINGS frame with the ACK flag set and a length field value other than 0 MUST be treated as a connection error (Section 5.4.1) of type FRAME_SIZE_ERROR. | frame/: parser must reject ACK+len>0 before payload decode; conn/ maps it to ConnError(FRAME_SIZE_ERROR) — client receives server ACKs of its own SETTINGS. |
| `6.5-6.6-6` | MUST | SETTINGS frames apply to the connection; the stream identifier must be zero. <br>_(sender [CLIENT])_ | The stream identifier for a SETTINGS frame MUST be zero (0x00). | frame/: writer must emit stream id 0 on all SETTINGS (including ACK). |
| `6.5-6.6-7` | MUST | A received SETTINGS frame with a non-zero Stream Identifier is a connection error of type PROTOCOL_ERROR. <br>_(endpoint,receiver [CLIENT])_ | If an endpoint receives a SETTINGS frame whose Stream Identifier field is anything other than 0x00, the endpoint MUST respond with a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ or conn/: verify inbound SETTINGS with sid!=0 produces ConnError(PROTOCOL_ERROR), not a stream error or silent drop. |
| `6.5-6.6-8` | MUST | A badly formed or incomplete SETTINGS frame is a connection error of type PROTOCOL_ERROR (SETTINGS affects connection state). <br>_(receiver [CLIENT])_ | A badly formed or incomplete SETTINGS frame MUST be treated as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: any SETTINGS parse failure must tear down the connection with PROTOCOL_ERROR — never skip-and-continue. |
| `6.5-6.6-9` | MUST | A SETTINGS frame whose length is not a multiple of 6 octets is a connection error of type FRAME_SIZE_ERROR. <br>_(receiver [CLIENT])_ | A SETTINGS frame with a length other than a multiple of 6 octets MUST be treated as a connection error (Section 5.4.1) of type FRAME_SIZE_ERROR. | frame/: parser must check len%6==0 (non-ACK) and map violation to FRAME_SIZE_ERROR. |
| `6.5-6.6-10` | informative | SETTINGS payload layout: zero or more 6-octet entries, each an unsigned 16-bit identifier followed by an unsigned 32-bit value; frame type is 0x04. <br>_(all [CLIENT])_ | zero or more settings, each consisting of an unsigned 16-bit setting identifier and an unsigned 32-bit value. | frame/: codec must read/write big-endian uint16 id + uint32 value pairs; empty payload (zero settings) is legal. |
| `6.5-6.6-11` | informative | SETTINGS_HEADER_TABLE_SIZE (0x01) advertises the maximum HPACK table size the sender will use to decode field blocks; the peer's encoder may pick any size at or below it via a dynamic table size update. <br>_(sender,receiver [CLIENT])_ | This setting allows the sender to inform the remote endpoint of the maximum size of the compression table used to decode field blocks, in units of octets. | conn/ + hpack/: peer's HEADER_TABLE_SIZE must be wired to the client's hpack.Encoder max table size (emit dynamic table size update <= peer value); client's own advertised value bounds its hpack.Decoder table. |
| `6.5-6.6-12` | informative | The initial value of SETTINGS_HEADER_TABLE_SIZE is 4,096 octets. <br>_(all [CLIENT])_ | The initial value is 4,096 octets. | hpack/: encoder and decoder must default to 4096-octet dynamic table until a SETTINGS changes it. |
| `6.5-6.6-13` | MUST NOT | A server that receives SETTINGS_ENABLE_PUSH=0 must not send PUSH_PROMISE frames. <br>_(server [not-client])_ | A server MUST NOT send a PUSH_PROMISE frame if it receives this parameter set to a value of 0 | Server-side duty the client relies on; client enforcement is 6.5-6.6-14. conn/: no client action beyond sending ENABLE_PUSH=0. |
| `6.5-6.6-14` | MUST | A client that set ENABLE_PUSH=0 and had that SETTINGS acknowledged must treat any subsequent PUSH_PROMISE as a connection error PROTOCOL_ERROR. <br>_(client [CLIENT])_ | A client that has both set this parameter to 0 and had it acknowledged MUST treat the receipt of a PUSH_PROMISE frame as a connection error | conn/: check whether handshake SETTINGS includes ENABLE_PUSH=0 and whether an acked flag gates the PUSH_PROMISE-received path to ConnError(PROTOCOL_ERROR); before the ack, a PP is not automatically an error. |
| `6.5-6.6-15` | informative | The initial value of SETTINGS_ENABLE_PUSH is 1; for a client that means willing to receive PUSH_PROMISE, for a server the initial value has no effect (equivalent to 0). <br>_(client,server [CLIENT])_ | The initial value of SETTINGS_ENABLE_PUSH is 1. | conn/: if the client omits ENABLE_PUSH from its SETTINGS it has implicitly agreed to receive pushes — verify the client explicitly sends 0 (or handles reserved streams). |
| `6.5-6.6-16` | MUST | An ENABLE_PUSH value other than 0 or 1 is a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | Any value other than 0 or 1 MUST be treated as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: applyPeerSettings must validate ENABLE_PUSH in {0,1} and raise ConnError(PROTOCOL_ERROR) otherwise. |
| `6.5-6.6-17` | MUST NOT | A server must never explicitly set ENABLE_PUSH to 1. <br>_(server [not-client])_ | A server MUST NOT explicitly set this value to 1. | Server-side emit rule; the client-side enforcement is 6.5-6.6-19. |
| `6.5-6.6-18` | MUST | A server may omit ENABLE_PUSH entirely, but if it includes a value it must be 0. <br>_(server [not-client])_ | A server MAY choose to omit this setting when it sends a SETTINGS frame, but if a server does include a value, it MUST be 0. | Server-side emit rule; client-side enforcement is 6.5-6.6-19. |
| `6.5-6.6-19` | MUST | A client must treat receipt of SETTINGS_ENABLE_PUSH=1 from the server as a connection error PROTOCOL_ERROR (new tightening in RFC 9113 vs 7540). <br>_(client [CLIENT])_ | A client MUST treat receipt of a SETTINGS frame with SETTINGS_ENABLE_PUSH set to 1 as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: verify applyPeerSettings rejects ENABLE_PUSH=1 from the peer with ConnError(PROTOCOL_ERROR) — a 7540-era implementation typically accepts it; this is a 9113 delta to audit explicitly. |
| `6.5-6.6-20` | informative | SETTINGS_MAX_CONCURRENT_STREAMS is directional: it limits the number of streams the receiver of the setting may create toward the sender. <br>_(all [CLIENT])_ | This limit is directional: it applies to the number of streams that the sender permits the receiver to create. | conn/: peer's MAX_CONCURRENT_STREAMS caps client-initiated streams; client's own advertised value caps server pushes — verify NewStream gates on the peer-advertised value in that direction. |
| `6.5-6.6-21` | informative | MAX_CONCURRENT_STREAMS is initially unlimited (absent means no limit). <br>_(all [CLIENT])_ | Initially, there is no limit to this value. | conn/: lookupPeerSetting must distinguish absent (unlimited / fall through to local cap) from explicit zero — CLAUDE.md says implemented; verify with a test. |
| `6.5-6.6-22` | informative | The advertised MAX_CONCURRENT_STREAMS value is recommended (lowercase, non-BCP14) to be no smaller than 100. <br>_(sender [CLIENT])_ | It is recommended that this value be no smaller than 100, so as to not unnecessarily limit parallelism. | conn/: check the client's own advertised MAX_CONCURRENT_STREAMS default is >=100 or deliberately chosen. |
| `6.5-6.6-23` | SHOULD NOT | Endpoints should not treat MAX_CONCURRENT_STREAMS=0 as special; it simply prevents creating new streams like any exhausted limit. <br>_(endpoint [CLIENT])_ | A value of 0 for SETTINGS_MAX_CONCURRENT_STREAMS SHOULD NOT be treated as special by endpoints. | conn/: explicit zero from peer must make NewStream fail (ErrTooManyStreams) without being a protocol error or connection teardown. |
| `6.5-6.6-24` | SHOULD | Servers should only advertise MAX_CONCURRENT_STREAMS=0 for short durations; closing the connection is more appropriate for refusing requests. <br>_(server [not-client])_ | Servers SHOULD only set a zero value for short durations | Server-side guidance; client just needs to tolerate a temporary zero (6.5-6.6-23). |
| `6.5-6.6-25` | informative | SETTINGS_INITIAL_WINDOW_SIZE (0x04) sets the sender's initial stream-level flow-control window; the initial value is 65,535 octets. <br>_(all [CLIENT])_ | This setting indicates the sender's initial window size (in units of octets) for stream-level flow control. The initial value is 2^16-1 (65,535) octets. | conn/: default per-stream send window 65535 until peer SETTINGS arrives; note connection-level window is fixed 65535 at handshake and only WINDOW_UPDATE changes it (per CLAUDE.md, already modeled). |
| `6.5-6.6-26` | informative | A changed INITIAL_WINDOW_SIZE affects the window size of all streams, per the Section 6.9.2 delta rule. <br>_(all [CLIENT])_ | This setting affects the window size of all streams (see Section 6.9.2). | conn/: mid-connection INITIAL_WINDOW_SIZE change must retroactively adjust every open stream's send window by the delta — CLAUDE.md says implemented; keep the conformance test pinned. |
| `6.5-6.6-27` | MUST | An INITIAL_WINDOW_SIZE value above 2^31-1 is a connection error of type FLOW_CONTROL_ERROR. <br>_(receiver [CLIENT])_ | Values above the maximum flow-control window size of 2^31-1 MUST be treated as a connection error (Section 5.4.1) of type FLOW_CONTROL_ERROR. | conn/: validate the raw setting value > 2^31-1 at receipt (distinct from the 6.9.2 delta-overflow case) and raise ConnError(FLOW_CONTROL_ERROR). |
| `6.5-6.6-28` | informative | The initial value of SETTINGS_MAX_FRAME_SIZE is 2^14 (16,384) octets. <br>_(all [CLIENT])_ | The initial value is 2^14 (16,384) octets. | frame/ + conn/: outbound frame payload cap must default to 16384 until the peer advertises higher; writeData already chunks at min(peer, own) MAX_FRAME_SIZE per CLAUDE.md. |
| `6.5-6.6-29` | MUST | An advertised MAX_FRAME_SIZE must lie between 16,384 and 16,777,215 (2^24-1) octets inclusive. <br>_(sender [CLIENT])_ | The value advertised by an endpoint MUST be between this initial value and the maximum allowed frame size (2^24-1 or 16,777,215 octets), inclusive. | conn/: clamp/validate the client's own advertised MAX_FRAME_SIZE into [16384, 16777215] before sending. |
| `6.5-6.6-30` | MUST | A received MAX_FRAME_SIZE outside [2^14, 2^24-1] is a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | Values outside this range MUST be treated as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: applyPeerSettings must range-check MAX_FRAME_SIZE and raise ConnError(PROTOCOL_ERROR) on violation. |
| `6.5-6.6-31` | informative | SETTINGS_MAX_HEADER_LIST_SIZE (0x06) is advisory and counts the uncompressed size of field lines: name length + value length + 32 octets overhead per field line. <br>_(all [CLIENT])_ | the uncompressed size of field lines, including the length of the name and value in units of octets plus an overhead of 32 octets for each field line | conn/ + hpack/: client should bound its emitted request header list against the peer's advertised value using the name+value+32 accounting; verify the decoder's response-side limit uses the same formula. |
| `6.5-6.6-32` | MAY | An endpoint may enforce a lower header-list limit than it advertised for any given request. <br>_(endpoint [CLIENT])_ | For any given request, a lower limit than what is advertised MAY be enforced. | client/ + conn/: do not assume a request within the server's advertised MAX_HEADER_LIST_SIZE is guaranteed acceptance; treat oversized-header rejections as non-protocol failures. |
| `6.5-6.6-33` | informative | The initial value of SETTINGS_MAX_HEADER_LIST_SIZE is unlimited. <br>_(all [CLIENT])_ | The initial value of this setting is unlimited. | conn/: absent MAX_HEADER_LIST_SIZE from the peer means no advertised bound on client-emitted headers (client may still self-limit for its own decoder). |
| `6.5-6.6-34` | MUST | A setting with an unknown or unsupported identifier must be ignored. <br>_(endpoint,receiver [CLIENT])_ | An endpoint that receives a SETTINGS frame with any unknown or unsupported identifier MUST ignore that setting. | conn/: unknown identifiers (e.g. 0x08 ENABLE_CONNECT_PROTOCOL, GREASE values) must be skipped without error and without aborting processing of later entries in the frame. |
| `6.5-6.6-35` | MUST | The recipient of a non-ACK SETTINGS frame must apply the updated settings as soon as possible upon receipt. <br>_(receiver [CLIENT])_ | the recipient of a SETTINGS frame in which the ACK flag is not set MUST apply the updated settings as soon as possible upon receipt. | conn/: settings must be applied (windows resized, caps updated) before the ACK is written and before subsequent frames are processed. |
| `6.5-6.6-36` | MUST | Values within a SETTINGS frame must be processed in order with no other frame processing interleaved between values. <br>_(receiver [CLIENT])_ | The values in the SETTINGS frame MUST be processed in the order they appear, with no other frame processing between values. | conn/: the applyPeerSettings loop must run to completion on the reader goroutine before the next frame is parsed — verify nothing yields mid-frame. |
| `6.5-6.6-37` | MUST | Unsupported settings must be ignored during synchronization processing (restates 6.5.2's ignore rule). <br>_(receiver [CLIENT])_ | Unsupported settings MUST be ignored. | conn/: same code path as 6.5-6.6-34 — ensure ignoring an unknown id does not suppress the mandatory ACK. |
| `6.5-6.6-38` | MUST | After processing all values in a non-ACK SETTINGS frame, the recipient must immediately emit a SETTINGS frame with the ACK flag set. <br>_(receiver [CLIENT])_ | Once all values have been processed, the recipient MUST immediately emit a SETTINGS frame with the ACK flag set. | conn/: verify the ACK write happens under wmu immediately after apply, once per received non-ACK SETTINGS frame (including empty ones), and in receipt order. |
| `6.5-6.6-39` | informative | SETTINGS frames are acknowledged in the order received; on receiving an ACK the sender may rely on the values of its oldest unacknowledged SETTINGS frame being applied. <br>_(sender [CLIENT])_ | the sender of the altered settings can rely on the values from the oldest unacknowledged SETTINGS frame having been applied. | conn/: if the client tracks its own settings acknowledgment (needed for the ENABLE_PUSH=0 'acknowledged' precondition in 6.5-6.6-14), match ACKs FIFO to sent SETTINGS frames. |
| `6.5-6.6-40` | MAY | A SETTINGS sender that receives no acknowledgment within a reasonable time may raise a connection error of type SETTINGS_TIMEOUT. <br>_(sender [CLIENT])_ | If the sender of a SETTINGS frame does not receive an acknowledgment within a reasonable amount of time, it MAY issue a connection error (Section 5.4.1) of type SETTINGS_TIMEOUT. | conn/: optional — check whether a handshake/settings-ack timeout exists; omitting is conformant but leaves a hang risk if the server never ACKs. |
| `6.5-6.6-41` | informative | PUSH_PROMISE (type=0x05) carries an unsigned 31-bit Promised Stream ID plus a field block fragment; payload layout is [Pad Length(8) if PADDED], Reserved(1), Promised Stream ID(31), Field Block Fragment, Padding. <br>_(all [CLIENT])_ | The PUSH_PROMISE frame includes the unsigned 31-bit identifier of the stream the endpoint plans to create along with a field section that provides additional context for the stream. | frame/: parser must at minimum decode the PUSH_PROMISE header (flags, promised id, padding) far enough to validate and reject it cleanly even though this client refuses pushes. |
| `6.5-6.6-42` | informative | The PADDED flag (0x08) indicates the Pad Length field and padding are present; Pad Length is absent otherwise. END_HEADERS is 0x04. <br>_(all [CLIENT])_ | When set, the PADDED flag indicates that the Pad Length field and any padding that it describes are present. | frame/: PUSH_PROMISE parse must gate the leading Pad Length octet on flag 0x08 (same padding-strip helper as DATA/HEADERS in internal/bytesx). |
| `6.5-6.6-43` | MUST | The promised stream identifier must be a valid choice for the next stream the sender opens (per Section 5.1.1 new-stream rules). <br>_(sender [not-client])_ | The promised stream identifier MUST be a valid choice for the next stream sent by the sender | Server-side emit rule; the client-side validation counterpart is 6.5-6.6-59 (illegal promised id -> PROTOCOL_ERROR). |
| `6.5-6.6-44` | MUST | Padding octets in PUSH_PROMISE must be zero when sending. <br>_(sender [not-client])_ | Padding octets MUST be set to zero when sending. | Client never sends PUSH_PROMISE; no emit path to check. |
| `6.5-6.6-45` | MAY | A receiver is not obligated to verify padding but may treat non-zero padding as a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | A receiver is not obligated to verify padding but MAY treat non-zero padding as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/: optional check; skipping padding-content verification is conformant — document the choice either way. |
| `6.5-6.6-46` | MUST | A PUSH_PROMISE without END_HEADERS must be followed by a CONTINUATION frame for the same stream. <br>_(sender [not-client])_ | A PUSH_PROMISE frame without the END_HEADERS flag set MUST be followed by a CONTINUATION frame for the same stream. | Server-side emit rule; client enforcement is 6.5-6.6-47. |
| `6.5-6.6-47` | MUST | While a PUSH_PROMISE field block is unterminated, receipt of any other frame type or a frame on a different stream is a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | A receiver MUST treat the receipt of any other type of frame or a frame on a different stream as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: the continuation-expected state machine must cover PUSH_PROMISE-initiated field blocks, not just HEADERS — even a push-refusing client must enforce this before erroring, or error on the PP itself first. |
| `6.5-6.6-48` | MUST | PUSH_PROMISE may only be sent on a peer-initiated stream in the open or half-closed (remote) state. <br>_(sender [not-client])_ | PUSH_PROMISE frames MUST only be sent on a peer-initiated stream that is in either the "open" or "half-closed (remote)" state. | Server-side emit rule; client-side state validation is 6.5-6.6-57. |
| `6.5-6.6-49` | MUST | A PUSH_PROMISE with Stream Identifier 0x00 is a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If the Stream Identifier field specifies the value 0x00, a recipient MUST respond with a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ or conn/: PUSH_PROMISE on stream 0 must yield ConnError(PROTOCOL_ERROR), distinct from the ENABLE_PUSH-based rejection. |
| `6.5-6.6-50` | informative | Promised streams need not be used in the order promised; PUSH_PROMISE only reserves identifiers for later use. <br>_(all [CLIENT])_ | Promised streams are not required to be used in the order they are promised. | conn/: only relevant if pushes are ever accepted — make no ordering assumption between promised id reservation and the pushed response HEADERS. |
| `6.5-6.6-51` | MUST NOT | PUSH_PROMISE must not be sent when the peer's SETTINGS_ENABLE_PUSH is 0. <br>_(sender [not-client])_ | PUSH_PROMISE MUST NOT be sent if the SETTINGS_ENABLE_PUSH setting of the peer endpoint is set to 0. | Server-side duty; client enforcement is 6.5-6.6-14/52. |
| `6.5-6.6-52` | MUST | An endpoint that set ENABLE_PUSH=0 and received acknowledgment must treat receipt of a PUSH_PROMISE frame as a connection error of type PROTOCOL_ERROR. <br>_(endpoint,receiver [CLIENT])_ | An endpoint that has set this setting and has received acknowledgment MUST treat the receipt of a PUSH_PROMISE frame as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: same enforcement as 6.5-6.6-14 with the ack precondition — a PP arriving before the client's SETTINGS is acked must be tolerated (handled per state rules), not auto-errored under this clause. |
| `6.5-6.6-53` | informative | Recipients can reject a promised stream by sending RST_STREAM referencing the promised stream identifier. <br>_(receiver [CLIENT])_ | Recipients of PUSH_PROMISE frames can choose to reject promised streams by returning a RST_STREAM referencing the promised stream identifier | conn/: if any window exists where a PP must be tolerated (pre-ack), the clean rejection is RST_STREAM(CANCEL or REFUSED_STREAM) on the promised id — check such a path exists or that the conn errors instead. |
| `6.5-6.6-54` | informative | A PUSH_PROMISE's field block modifies connection-level field-compression state, so its field block must be decoded even if the push is unwanted. <br>_(all [CLIENT])_ | the inclusion of a field block (Section 4.3) potentially modifies the state maintained for field section compression. | conn/ + hpack/: any PUSH_PROMISE the client tolerates (rather than conn-erroring) must have its field block run through the shared hpack.Decoder or the decoder desyncs for all later responses. |
| `6.5-6.6-55` | informative | PUSH_PROMISE reserves the promised stream, moving it into reserved (local) at the sender and reserved (remote) at the receiver. <br>_(all [CLIENT])_ | PUSH_PROMISE also reserves a stream for later use, causing the promised stream to enter the "reserved (local)" or "reserved (remote)" state. | conn/: stream state machine — if pushes were ever accepted, promised even ids enter reserved(remote); with push refused, verify the conn error fires before any registry mutation. |
| `6.5-6.6-56` | MUST | The sender must ensure the promised stream is a valid new stream identifier, i.e. currently in the idle state. <br>_(sender [not-client])_ | the sender MUST ensure that the promised stream is a valid choice for a new stream identifier (Section 5.1.1) (that is, the promised stream MUST be in the "idle" state). | Server-side emit rule; client validation counterpart is 6.5-6.6-59. |
| `6.5-6.6-57` | MUST | A receiver must treat a PUSH_PROMISE on a stream that is neither open nor half-closed (local) as a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | A receiver MUST treat the receipt of a PUSH_PROMISE on a stream that is neither "open" nor "half-closed (local)" as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: from the client's view a PP is only state-valid on its own request stream while open or half-closed(local); PP on closed/idle/unknown streams must be ConnError(PROTOCOL_ERROR) — check ordering vs the ENABLE_PUSH rejection. |
| `6.5-6.6-58` | MUST | An endpoint that sent RST_STREAM on a stream must still handle PUSH_PROMISE frames the peer created before processing the RST_STREAM (in-flight race). <br>_(endpoint [CLIENT])_ | an endpoint that has sent RST_STREAM on the associated stream MUST handle PUSH_PROMISE frames that might have been created before the RST_STREAM frame is received and processed. | conn/: after the client RSTs a request stream, a racing PP on that stream must not be treated as a state violation and its field block must still keep HPACK state consistent (see 6.5-6.6-54); test the RST-then-PP race. |
| `6.5-6.6-59` | MUST | A receiver must treat a PUSH_PROMISE promising an illegal stream identifier (one not currently idle) as a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | A receiver MUST treat the receipt of a PUSH_PROMISE that promises an illegal stream identifier (Section 5.1.1) as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: promised ids must be server-even, monotonically increasing, and idle; duplicates or client-odd ids -> ConnError(PROTOCOL_ERROR) — only reachable if any PP path avoids the immediate ENABLE_PUSH rejection. |
| `6.5-6.6-60` | MUST | If Pad Length is greater than or equal to the frame payload length, the recipient must treat it as a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If the length of the padding is the length of the frame payload or greater, the recipient MUST treat this as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ (internal/bytesx padding strip): verify the shared RFC padding-strip helper covers PUSH_PROMISE and maps pad>=payload to PROTOCOL_ERROR. |
| `6.5-6.6-61` | informative | A Pad Length field with value zero is legal and simply increases the frame size by one octet. <br>_(all [CLIENT])_ | A frame can be increased in size by one octet by including a Pad Length field with a value of zero. | frame/: PADDED flag with Pad Length 0 must parse successfully (not treated as malformed). |

### 9113 section 6.7-7 - HTTP/2 Frame Definitions: PING, GOAWAY, WINDOW_UPDATE (flow-control window, initial size, reducing), CONTINUATION; Error Codes

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `6.7-7-1` | informative | PING frame layout: type 0x06, fixed 8-octet payload of opaque data, 7 unused flag bits plus ACK flag, stream identifier 0. <br>_(all [CLIENT])_ | Length (24) = 0x08, Type (8) = 0x06, Unused Flags (7), ACK Flag (1), Reserved (1), Stream Identifier (31) = 0, Opaque Data (64), | frame/: PING codec must emit/parse exactly this shape — 8-byte payload, flag bit 0x01, stream id 0. |
| `6.7-7-2` | MUST | A PING frame must carry exactly 8 octets of opaque data in its payload; the sender may choose any value. <br>_(sender [CLIENT])_ | PING frames MUST contain 8 octets of opaque data in the frame payload. | frame/: writer emits exactly 8 opaque octets for PING; no variable-length path. |
| `6.7-7-3` | MUST | On receiving a PING without the ACK flag, the receiver must reply with a PING carrying ACK set and an identical payload. <br>_(receiver [CLIENT])_ | Receivers of a PING frame that does not include an ACK flag MUST send a PING frame with the ACK flag set in response, with an identical frame payload. | conn/: reader auto-echoes non-ACK PING with ACK=1 + same 8 bytes (CLAUDE.md says done) — verify payload is byte-identical in test. |
| `6.7-7-4` | SHOULD | PING responses should be prioritized above all other frames. <br>_(endpoint [CLIENT])_ | PING responses SHOULD be given higher priority than any other frame. | conn/: check the PING-ACK write is not queued behind bulk DATA under wmu; document if best-effort only. |
| `6.7-7-5` | MUST | An endpoint must set the ACK flag on every PING response it sends. <br>_(endpoint [CLIENT])_ | An endpoint MUST set this flag in PING responses. | conn/: echo path must write flag 0x01 — assert wire bytes in unit test (bytes.Buffer-wired framer). |
| `6.7-7-6` | MUST NOT | An endpoint must never respond to a PING frame that already has the ACK flag set (no ping-pong loops). <br>_(endpoint [CLIENT])_ | An endpoint MUST NOT respond to PING frames containing this flag. | conn/: inbound PING with ACK must be dropped, never re-echoed (CLAUDE.md: ACK frames dropped) — verify. |
| `6.7-7-7` | MUST | A PING frame with a nonzero Stream Identifier is a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If a PING frame is received with a Stream Identifier field value other than 0x00, the recipient MUST respond with a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ or conn/: nonzero stream id on PING → typed ConnError(PROTOCOL_ERROR) + GOAWAY; check which layer validates. |
| `6.7-7-8` | MUST | A PING frame whose length field is not 8 is a connection error of type FRAME_SIZE_ERROR. <br>_(receiver [CLIENT])_ | Receipt of a PING frame with a length field value other than 8 MUST be treated as a connection error (Section 5.4.1) of type FRAME_SIZE_ERROR. | frame/: parser rejects PING length != 8 with FRAME_SIZE_ERROR at the codec layer. |
| `6.7-7-9` | informative | GOAWAY frame layout: type 0x07, no defined flags, stream identifier 0, then 31-bit Last-Stream-ID, 32-bit Error Code, and optional debug data. <br>_(all [CLIENT])_ | Length (24), Type (8) = 0x07, Unused Flags (8), Reserved (1), Stream Identifier (31) = 0, Reserved (1), Last-Stream-ID (31), Error Code (32), Additional Debug Data (..), | frame/: GOAWAY codec masks the reserved bit off Last-Stream-ID, reads 32-bit code, captures trailing debug data, ignores all flag bits. |
| `6.7-7-10` | informative | GOAWAY carries the identifier of the last peer-initiated stream that was or might be processed by the GOAWAY sender. <br>_(all [CLIENT])_ | the GOAWAY contains the stream identifier of the last peer-initiated stream that was or might be processed on the sending endpoint in this connection. | conn/: OnGoAway must interpret Last-Stream-ID as the highest client-initiated stream the server may have processed. |
| `6.7-7-11` | informative | After sending GOAWAY, the sender ignores frames on receiver-initiated streams with identifiers above the advertised last stream identifier. <br>_(sender [CLIENT])_ | Once the GOAWAY is sent, the sender will ignore frames sent on streams initiated by the receiver if the stream has an identifier higher than the included last stream identifier. | conn/: if client sends GOAWAY on Close/drain, inbound frames on server-initiated streams above its last-stream-id are ignorable (push disabled makes this mostly moot). |
| `6.7-7-12` | MUST NOT | An endpoint that has received GOAWAY must not open new streams on that connection; it may open a new connection instead. <br>_(receiver [CLIENT])_ | Receivers of a GOAWAY frame MUST NOT open additional streams on the connection, although a new connection can be established for new streams. | conn/: NewStream after peer GOAWAY returns ErrGoAway (done per CLAUDE.md B.2.6) — verify no race window lets a stream slip through between GOAWAY receipt and gate. |
| `6.7-7-13` | informative | Streams above the last stream identifier may be treated by the GOAWAY receiver as never created, making them safe to retry on a new connection. <br>_(receiver [CLIENT])_ | can treat the streams as though they had never been created at all, thereby allowing those streams to be retried later on a new connection | client/: retry layer treats EventReset(REFUSED_STREAM) from GOAWAY eviction as never-processed → unconditionally retryable on a fresh conn. |
| `6.7-7-14` | SHOULD | Endpoints should always send a GOAWAY frame before closing a connection so the peer knows which streams were partially processed. <br>_(endpoint [CLIENT])_ | Endpoints SHOULD always send a GOAWAY frame before closing a connection so that the remote peer can know whether a stream has been partially processed or not. | conn/: check Conn.Close emits GOAWAY(NO_ERROR) before tearing down the TCP/TLS connection. |
| `6.7-7-15` | SHOULD | A GOAWAY receiver that is done with the connection should itself send a GOAWAY before terminating. <br>_(receiver [CLIENT])_ | a receiver of a GOAWAY that has no more use for the connection SHOULD still send a GOAWAY frame before terminating the connection. | conn/: GOAWAY-drain teardown path should also write the client's own GOAWAY before close. |
| `6.7-7-16` | informative | An endpoint may close a connection without sending GOAWAY when the peer is misbehaving. <br>_(endpoint [CLIENT])_ | An endpoint might choose to close a connection without sending a GOAWAY for misbehaving peers. | conn/: abrupt close on fatal peer violation is permitted — no change needed; note in error-path docs. |
| `6.7-7-17` | MUST | A GOAWAY frame with a nonzero stream identifier is a connection error of type PROTOCOL_ERROR. <br>_(endpoint [CLIENT])_ | An endpoint MUST treat a GOAWAY frame with a stream identifier other than 0x00 as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ or conn/: validate GOAWAY stream id == 0 → ConnError(PROTOCOL_ERROR) otherwise; check which layer owns it. |
| `6.7-7-18` | informative | Last-Stream-ID is the highest-numbered stream the GOAWAY sender might have acted on or might yet act on; 0 means no streams were processed. <br>_(all [CLIENT])_ | contains the highest-numbered stream identifier for which the sender of the GOAWAY frame might have taken some action on or might yet take action on | conn/: handle Last-Stream-ID = 0 correctly — every in-flight stream gets REFUSED_STREAM/never-processed treatment. |
| `6.7-7-19` | informative | If the connection dies without a GOAWAY, the last stream identifier is effectively the highest possible, i.e. everything may have been processed. <br>_(all [CLIENT])_ | If a connection terminates without a GOAWAY frame, the last stream identifier is effectively the highest possible stream identifier. | client/: on conn loss without GOAWAY, retry policy must assume all in-flight requests were possibly processed (retry only idempotent). |
| `6.7-7-20` | informative | Unfinished streams at or below the last stream identifier must not be blindly reattempted; only idempotent actions (GET, PUT, DELETE) are retry-safe. <br>_(all [CLIENT])_ | reattempting requests, transactions, or any protocol activity is not possible, except for idempotent actions like HTTP GET, PUT, or DELETE. | client/: retry/backoff layer must gate replay of streams <= lastStreamID on method idempotency. |
| `6.7-7-21` | informative | Activity on streams numbered above the last stream identifier can always be safely retried on a new connection. <br>_(all [CLIENT])_ | Any protocol activity that uses higher-numbered streams can be safely retried using a new connection. | client/: streams > lastStreamID are retry-safe regardless of method — verify retry path distinguishes the two classes. |
| `6.7-7-22` | MAY | An endpoint may send more than one GOAWAY frame as circumstances change (e.g. graceful shutdown then fatal error). <br>_(endpoint [CLIENT])_ | An endpoint MAY send multiple GOAWAY frames if circumstances change. | conn/: OnGoAway must handle repeated GOAWAYs; the latest received last-stream-id governs stream disposition. |
| `6.7-7-23` | MUST NOT | Across successive GOAWAY frames an endpoint must never advertise a larger last stream identifier than it sent before. <br>_(endpoint [CLIENT])_ | Endpoints MUST NOT increase the value they send in the last stream identifier, since the peers might already have retried unprocessed requests on another connection. | conn/: if client ever sends multiple GOAWAYs, clamp last-stream-id monotonically non-increasing; on receive, tolerate but only shrink the tracked bound. |
| `6.7-7-24` | SHOULD | A gracefully shutting-down server should first send GOAWAY with last stream identifier 2^31-1 and NO_ERROR as an advance warning. <br>_(server [CLIENT])_ | SHOULD send an initial GOAWAY frame with the last stream identifier set to 2^31-1 and a NO_ERROR code | conn/: client must handle GOAWAY(2^31-1, NO_ERROR) — stop NewStream but keep all in-flight streams alive; add conformance test. |
| `6.7-7-25` | MAY | After at least one round trip the server may send a second GOAWAY with the real, lower last stream identifier. <br>_(server [CLIENT])_ | After allowing time for any in-flight stream creation (at least one round-trip time), the server MAY send another GOAWAY frame with an updated last stream identifier. | conn/: on a second GOAWAY with lower id, re-evaluate open streams — those above the new id get EventReset(REFUSED_STREAM) eviction. |
| `6.7-7-26` | MUST | Even after sending GOAWAY, HEADERS, PUSH_PROMISE, and CONTINUATION frames on discarded streams must still be minimally processed to keep HPACK state consistent. <br>_(sender [CLIENT])_ | HEADERS, PUSH_PROMISE, and CONTINUATION frames MUST be minimally processed to ensure that the state maintained for field section compression is consistent | conn/ + hpack/: after client sends GOAWAY (drain), reader must still run header blocks through the HPACK decoder even for ignored streams. |
| `6.7-7-27` | MUST | Even after sending GOAWAY, DATA frames on discarded streams must still be counted against the connection flow-control window. <br>_(sender [CLIENT])_ | DATA frames MUST be counted toward the connection flow-control window. | conn/: post-GOAWAY-sent DATA on ignored streams must still debit connRecvWindow and feed WINDOW_UPDATE refund accounting. |
| `6.7-7-28` | MAY | An endpoint may append opaque debug data to any GOAWAY frame; it is diagnostic only and carries no semantics. <br>_(endpoint [CLIENT])_ | Endpoints MAY append opaque data to the frame payload of any GOAWAY frame. | frame/: GOAWAY parser must accept arbitrary-length (bounded) trailing debug data without error; emit side optional. |
| `6.7-7-29` | MUST | GOAWAY debug data that is logged or persistently stored must be safeguarded against unauthorized access. <br>_(endpoint [CLIENT])_ | Logged or otherwise persistently stored debug data MUST have adequate safeguards to prevent unauthorized access. | client/: if hooks/metrics surface GOAWAY debug data, document that callers must treat it as potentially sensitive. |
| `6.7-7-30` | informative | Flow control is hop by hop; intermediaries do not forward WINDOW_UPDATE frames between connections. <br>_(intermediary [not-client])_ | Intermediaries do not forward WINDOW_UPDATE frames between dependent connections. | n/a — client is not an intermediary; no forwarding logic exists in conn/. |
| `6.7-7-31` | informative | Of the frames defined by RFC 9113, only DATA frames are subject to flow control. <br>_(all [CLIENT])_ | Of the frame types defined in this document, this includes only DATA frames. | conn/: verify only DATA debits/credits windows — HEADERS/CONTINUATION/etc. must never touch flow-control accounting. |
| `6.7-7-32` | MUST | Frames exempt from flow control must be accepted and processed unless the receiver cannot assign resources to them. <br>_(receiver [CLIENT])_ | Frames that are exempt from flow control MUST be accepted and processed, unless the receiver is unable to assign resources to handling the frame. | conn/: non-flow-controlled frames (HEADERS, PING, SETTINGS, WINDOW_UPDATE...) must be processed even when recv windows are exhausted. |
| `6.7-7-33` | MAY | A receiver unable to accept a frame may respond with a stream or connection error of type FLOW_CONTROL_ERROR. <br>_(receiver [CLIENT])_ | A receiver MAY respond with a stream error (Section 5.4.2) or connection error (Section 5.4.1) of type FLOW_CONTROL_ERROR if it is unable to accept a frame. | conn/: optional resource-exhaustion escape hatch; note events-buffer overflow currently uses RST(REFUSED_STREAM) — this MAY permits FLOW_CONTROL_ERROR too. |
| `6.7-7-34` | informative | WINDOW_UPDATE frame layout: type 0x08, length exactly 4, no defined flags, payload is one reserved bit plus a 31-bit unsigned Window Size Increment. <br>_(all [CLIENT])_ | Length (24) = 0x04, Type (8) = 0x08, Unused Flags (8), Reserved (1), Stream Identifier (31), Reserved (1), Window Size Increment (31), | frame/: WINDOW_UPDATE codec — mask reserved bit, extract 31-bit increment, ignore flag bits on receive. |
| `6.7-7-35` | informative | The legal window increment range is 1 through 2^31-1 octets. <br>_(all [CLIENT])_ | The legal range for the increment to the flow-control window is 1 to 2^31-1 (2,147,483,647) octets. | frame/ + conn/: parser yields 31-bit value (0..2^31-1 representable); the increment==0 illegality is enforced in conn/ (facts 37-38). |
| `6.7-7-36` | informative | A WINDOW_UPDATE with a nonzero stream identifier targets that stream; stream identifier 0 targets the entire connection. <br>_(all [CLIENT])_ | in the latter, the value "0" indicates that the entire connection is the subject of the frame. | conn/: parseWindowUpdates must route stream id 0 to the connection send window, all other ids to the per-stream window. |
| `6.7-7-37` | MUST | A WINDOW_UPDATE with increment 0 on a stream is a stream error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | A receiver MUST treat the receipt of a WINDOW_UPDATE frame with a flow-control window increment of 0 as a stream error (Section 5.4.2) of type PROTOCOL_ERROR | conn/: increment==0 on stream id > 0 → RST_STREAM(PROTOCOL_ERROR), stream-level not connection-level. |
| `6.7-7-38` | MUST | A WINDOW_UPDATE with increment 0 on the connection window (stream 0) is a connection error. <br>_(receiver [CLIENT])_ | errors on the connection flow-control window MUST be treated as a connection error (Section 5.4.1). | conn/: increment==0 on stream id 0 → ConnError(PROTOCOL_ERROR) + GOAWAY; separate test from the stream-level case. |
| `6.7-7-39` | MUST NOT | A WINDOW_UPDATE received on a stream in half-closed (remote) or closed state must not be treated as an error. <br>_(receiver [CLIENT])_ | a receiver could receive a WINDOW_UPDATE frame on a stream in a "half-closed (remote)" or "closed" state. A receiver MUST NOT treat this as an error | conn/: tolerate WINDOW_UPDATE for ended/recycled streams silently — beware the pooled recycleStream path (stale-id WU must not error or credit the wrong stream). |
| `6.7-7-40` | MUST | Every received flow-controlled frame must be counted against the connection flow-control window, even if the frame itself is in error, unless the receiver escalates to a connection error. <br>_(receiver [CLIENT])_ | A receiver that receives a flow-controlled frame MUST always account for its contribution against the connection flow-control window, unless the receiver treats this as a connection error | conn/: DATA on unknown/RST'd/erroring streams must still debit connRecvWindow (B.2.2) — audit every DATA error path for the accounting. |
| `6.7-7-41` | MUST | A WINDOW_UPDATE frame whose length is not 4 octets is a connection error of type FRAME_SIZE_ERROR. <br>_(receiver [CLIENT])_ | A WINDOW_UPDATE frame with a length other than 4 octets MUST be treated as a connection error (Section 5.4.1) of type FRAME_SIZE_ERROR. | frame/: parser rejects WINDOW_UPDATE length != 4 with FRAME_SIZE_ERROR at the codec layer. |
| `6.7-7-42` | MUST NOT | A sender must not send a flow-controlled frame longer than the space available in both the stream and connection windows advertised by the receiver. <br>_(sender [CLIENT])_ | The sender MUST NOT send a flow-controlled frame with a length that exceeds the space available in either of the flow-control windows advertised by the receiver. | conn/: acquireSendCredits must gate on min(stream window, conn window) before each DATA chunk (B.2.3) — verify no oversend under races. |
| `6.7-7-43` | MAY | A zero-length frame with END_STREAM set (empty DATA) may be sent even with no window space available. <br>_(sender [CLIENT])_ | Frames with zero length with the END_STREAM flag set (that is, an empty DATA frame) MAY be sent if there is no available space in either flow-control window. | conn/: writeData end-of-body path must not block on credits for a zero-length END_STREAM frame — test at exhausted window. |
| `6.7-7-44` | informative | The 9-octet frame header is excluded from flow-control arithmetic; only payload length counts. <br>_(all [CLIENT])_ | For flow-control calculations, the 9-octet frame header is not counted. | conn/: window debit/credit uses payload length only, never length+9. |
| `6.7-7-45` | informative | After sending a flow-controlled frame the sender debits both the stream and connection windows by the frame's length. <br>_(sender [CLIENT])_ | After sending a flow-controlled frame, the sender reduces the space available in both windows by the length of the transmitted frame. | conn/: writeData must debit stream and connection send windows by each chunk's length atomically with the write. |
| `6.7-7-46` | informative | Receivers send separate WINDOW_UPDATE frames for stream- and connection-level windows as data is consumed, and should avoid very small increments. <br>_(receiver [CLIENT])_ | Separate WINDOW_UPDATE frames are sent for the stream- and connection-level flow-control windows. | conn/: refund path emits both stream and conn WINDOW_UPDATEs; recvWindowRefundThreshold=32KiB batching satisfies the small-increment advice. |
| `6.7-7-47` | informative | On receiving a WINDOW_UPDATE, the sender credits the corresponding window by the increment specified. <br>_(sender [CLIENT])_ | A sender that receives a WINDOW_UPDATE frame updates the corresponding window by the amount specified in the frame. | conn/: WINDOW_UPDATE receipt credits the matching send window and broadcasts fcOutCond so blocked writers re-check. |
| `6.7-7-48` | MUST NOT | A sender must never allow a flow-control window to exceed 2^31-1 octets. <br>_(sender [CLIENT])_ | A sender MUST NOT allow a flow-control window to exceed 2^31-1 octets. | conn/: overflow check on every WINDOW_UPDATE credit — signed/64-bit arithmetic so the 2^31-1 comparison cannot itself overflow. |
| `6.7-7-49` | MUST | If a WINDOW_UPDATE would push a window past 2^31-1, the sender must terminate the stream (RST_STREAM FLOW_CONTROL_ERROR) or the connection (GOAWAY FLOW_CONTROL_ERROR). <br>_(sender [CLIENT])_ | If a sender receives a WINDOW_UPDATE that causes a flow-control window to exceed this maximum, it MUST terminate either the stream or the connection, as appropriate. | conn/: stream-window overflow → RST_STREAM(FLOW_CONTROL_ERROR); conn-window overflow → GOAWAY(FLOW_CONTROL_ERROR) — verify both emission paths exist and are tested. |
| `6.7-7-50` | informative | Initial flow-control windows are 65,535 octets for both new streams and the connection. <br>_(all [CLIENT])_ | new streams are created with an initial flow-control window size of 65,535 octets. The connection flow-control window is also 65,535 octets. | conn/: matches connRecvWindow=65535 gotcha; verify send-side also initializes stream and conn windows to 65535 before any peer SETTINGS/WINDOW_UPDATE. |
| `6.7-7-51` | informative | The connection flow-control window can only be changed by WINDOW_UPDATE frames; SETTINGS (including INITIAL_WINDOW_SIZE) never alters it. <br>_(all [CLIENT])_ | A SETTINGS frame cannot alter the connection flow-control window. | conn/: SETTINGS_INITIAL_WINDOW_SIZE resize must touch only per-stream windows, never the connection window (matches CLAUDE.md gotcha) — keep a negative test. |
| `6.7-7-52` | informative | Until a SETTINGS frame carrying SETTINGS_INITIAL_WINDOW_SIZE arrives, the sender can only assume the default initial window size. <br>_(endpoint [CLIENT])_ | Prior to receiving a SETTINGS frame that sets a value for SETTINGS_INITIAL_WINDOW_SIZE, an endpoint can only use the default initial window size when sending flow-controlled frames. | conn/: before the peer's first SETTINGS is processed, stream send windows default to 65535 — verify handshake ordering. |
| `6.7-7-53` | MUST | When SETTINGS_INITIAL_WINDOW_SIZE changes, all maintained stream flow-control windows (including active ones) must be adjusted by the new-minus-old delta. <br>_(receiver [CLIENT])_ | a receiver MUST adjust the size of all stream flow-control windows that it maintains by the difference between the new value and the old value | conn/: retroactive delta resize of all open-stream send windows on peer SETTINGS change (done per CLAUDE.md) — confirm it covers open and half-closed(remote) streams. |
| `6.7-7-54` | MUST NOT | A window driven negative by a SETTINGS_INITIAL_WINDOW_SIZE decrease must be tracked, and no new flow-controlled frames may be sent until WINDOW_UPDATEs make it positive. <br>_(sender [CLIENT])_ | A sender MUST track the negative flow-control window and MUST NOT send new flow-controlled frames until it receives WINDOW_UPDATE frames that cause the flow-control window to become positive. | conn/: send-window arithmetic must be signed and acquireSendCredits must block while window <= 0 — test the -44KB style scenario. |
| `6.7-7-55` | MUST | A SETTINGS_INITIAL_WINDOW_SIZE change that pushes any flow-control window above the maximum is a connection error of type FLOW_CONTROL_ERROR. <br>_(endpoint [CLIENT])_ | An endpoint MUST treat a change to SETTINGS_INITIAL_WINDOW_SIZE that causes any flow-control window to exceed the maximum size as a connection error (Section 5.4.1) of type FLOW_CONTROL_ERROR. | conn/: delta pushing any stream window past 2^31-1 → typed ConnError(FLOW_CONTROL_ERROR) (done per CLAUDE.md gotcha) — verify test pins the error code. |
| `6.7-7-56` | MUST | After shrinking its window via SETTINGS, a receiver must be prepared for in-flight data that exceeds the new, lower limit. <br>_(receiver [CLIENT])_ | the receiver MUST be prepared to receive data that exceeds this window size, since the sender might send data that exceeds the lower limit prior to processing the SETTINGS frame | conn/: if client lowers its advertised SETTINGS_INITIAL_WINDOW_SIZE, inbound DATA already in flight above the new limit must not be treated as a violation. |
| `6.7-7-57` | MAY | After reducing the initial window, a receiver may keep processing streams that exceed the flow-control limit. <br>_(receiver [CLIENT])_ | a receiver MAY continue to process streams that exceed flow-control limits | conn/: tolerate-and-continue is the simplest conforming choice; document which option the code takes. |
| `6.7-7-58` | MAY | Alternatively the receiver may reset affected over-limit streams with RST_STREAM FLOW_CONTROL_ERROR. <br>_(receiver [CLIENT])_ | The receiver MAY instead send a RST_STREAM with an error code of FLOW_CONTROL_ERROR for the affected streams. | conn/: optional alternative to fact 57 — no obligation; only relevant if window-reduction support is added. |
| `6.7-7-59` | informative | CONTINUATION frame layout: type 0x09, END_HEADERS flag at 0x04, stream identifier of the associated stream, payload is a field block fragment. <br>_(all [CLIENT])_ | Length (24), Type (8) = 0x09, Unused Flags (5), END_HEADERS Flag (1), Unused Flags (2), Reserved (1), Stream Identifier (31), Field Block Fragment (..), | frame/: CONTINUATION codec — flag 0x04 only, opaque fragment payload, all other flag bits ignored on receive. |
| `6.7-7-60` | informative | The END_HEADERS flag on CONTINUATION marks the end of a field block. <br>_(all [CLIENT])_ | When set, the END_HEADERS flag indicates that this frame ends a field block (Section 4.3). | frame/ + conn/: END_HEADERS terminates field-block assembly; only then is the header block handed to hpack/ as complete. |
| `6.7-7-61` | MUST | A CONTINUATION without END_HEADERS must be followed by another CONTINUATION frame on the same stream. <br>_(sender [CLIENT])_ | If the END_HEADERS flag is not set, this frame MUST be followed by another CONTINUATION frame. | conn/: when emitting header blocks larger than MAX_FRAME_SIZE, HEADERS+CONTINUATIONs must be written contiguously under wmu with no interleaved frames. |
| `6.7-7-62` | MUST | While a field block is open, receiving any frame type other than CONTINUATION, or a frame on a different stream, is a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | A receiver MUST treat the receipt of any other type of frame or a frame on a different stream as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: reader must track open-field-block state and reject interleaved frames (including PING/SETTINGS mid-block) with ConnError(PROTOCOL_ERROR). |
| `6.7-7-63` | informative | CONTINUATION frames mutate connection-level compression state as defined in Section 4.3, even before END_HEADERS arrives. <br>_(all [CLIENT])_ | The CONTINUATION frame changes the connection state as defined in Section 4.3. | hpack/ + conn/: fragments must feed the shared decoder state in order; dropping a fragment desynchronizes the dynamic table. |
| `6.7-7-64` | MUST | A CONTINUATION frame with stream identifier 0 is a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | If a CONTINUATION frame is received with a Stream Identifier field of 0x00, the recipient MUST respond with a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | frame/ or conn/: CONTINUATION on stream 0 → ConnError(PROTOCOL_ERROR); check which layer validates. |
| `6.7-7-65` | MUST | A CONTINUATION frame must be preceded by a HEADERS, PUSH_PROMISE, or CONTINUATION frame without END_HEADERS on the same stream. <br>_(sender [CLIENT])_ | A CONTINUATION frame MUST be preceded by a HEADERS, PUSH_PROMISE or CONTINUATION frame without the END_HEADERS flag set. | conn/: emit side never sends bare CONTINUATION; receive side pairs with fact 66 enforcement. |
| `6.7-7-66` | MUST | A recipient observing a CONTINUATION that violates the sequencing rule must respond with a connection error of type PROTOCOL_ERROR. <br>_(receiver [CLIENT])_ | A recipient that observes violation of this rule MUST respond with a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: unsolicited CONTINUATION (no open field block) → ConnError(PROTOCOL_ERROR) — add negative test for CONTINUATION-out-of-nowhere. |
| `6.7-7-67` | informative | Error codes are 32-bit fields carried in RST_STREAM and GOAWAY frames to convey stream or connection error reasons. <br>_(all [CLIENT])_ | Error codes are 32-bit fields that are used in RST_STREAM and GOAWAY frames to convey the reasons for the stream or connection error. | frame/: RST_STREAM and GOAWAY codecs read/write a full 32-bit code; do not truncate to the 14 defined values. |
| `6.7-7-68` | informative | Error codes share one code space, but some codes are only meaningful for streams or only for connections. <br>_(all [CLIENT])_ | Error codes share a common code space. Some error codes apply only to either streams or the entire connection and have no defined semantics in the other context. | frame/: single ErrCode type shared by RST_STREAM and GOAWAY; conn/ decides context-appropriate use. |
| `6.7-7-69` | informative | NO_ERROR (0x00) signals a non-error condition, e.g. graceful GOAWAY shutdown. <br>_(all [CLIENT])_ | NO_ERROR (0x00): The associated condition is not a result of an error. | conn/: GOAWAY(NO_ERROR) must trigger graceful drain, not an error surfaced to callers of in-flight streams <= lastStreamID. |
| `6.7-7-70` | informative | PROTOCOL_ERROR (0x01) is the generic protocol violation code, used when nothing more specific applies. <br>_(all [CLIENT])_ | PROTOCOL_ERROR (0x01): The endpoint detected an unspecific protocol error. | frame/: constant 0x01; conn/ uses it for the many MUST-PROTOCOL_ERROR cases in this section. |
| `6.7-7-71` | informative | INTERNAL_ERROR (0x02) indicates an unexpected internal error at the endpoint. <br>_(all [CLIENT])_ | INTERNAL_ERROR (0x02): The endpoint encountered an unexpected internal error. | frame/: constant 0x02; also the suggested mapping target for unknown codes (fact 84). |
| `6.7-7-72` | informative | FLOW_CONTROL_ERROR (0x03) indicates the peer violated the flow-control protocol. <br>_(all [CLIENT])_ | FLOW_CONTROL_ERROR (0x03): The endpoint detected that its peer violated the flow-control protocol. | conn/: used for window overflow (facts 49, 55) and optional resource refusal (fact 33) — verify emitted code values. |
| `6.7-7-73` | informative | SETTINGS_TIMEOUT (0x04) is used when a SETTINGS frame was not acknowledged in a timely manner. <br>_(all [CLIENT])_ | SETTINGS_TIMEOUT (0x04): The endpoint sent a SETTINGS frame but did not receive a response in a timely manner. | conn/: if a SETTINGS-ACK timeout is implemented, close with GOAWAY(SETTINGS_TIMEOUT); frame/ needs constant 0x04 regardless. |
| `6.7-7-74` | informative | STREAM_CLOSED (0x05) indicates a frame was received after a stream was half-closed. <br>_(all [CLIENT])_ | STREAM_CLOSED (0x05): The endpoint received a frame after a stream was half-closed. | conn/: emit STREAM_CLOSED for frames arriving on half-closed(remote) streams per §5.1 state machine; frame/ constant 0x05. |
| `6.7-7-75` | informative | FRAME_SIZE_ERROR (0x06) indicates a frame with an invalid size was received. <br>_(all [CLIENT])_ | FRAME_SIZE_ERROR (0x06): The endpoint received a frame with an invalid size. | frame/: the code used by PING!=8 and WINDOW_UPDATE!=4 rejections (facts 8, 41). |
| `6.7-7-76` | informative | REFUSED_STREAM (0x07) means the stream was refused before any application processing, making it safe to retry. <br>_(all [CLIENT])_ | REFUSED_STREAM (0x07): The endpoint refused the stream prior to performing any application processing | client/: RST(REFUSED_STREAM) or GOAWAY eviction implies zero processing — retry layer may replay even non-idempotent requests (see §8.7). |
| `6.7-7-77` | informative | CANCEL (0x08) indicates the sender no longer needs the stream. <br>_(all [CLIENT])_ | CANCEL (0x08): The endpoint uses this error code to indicate that the stream is no longer needed. | conn/: client-side stream abandonment (ctx cancel, body discard) should RST_STREAM with CANCEL, not PROTOCOL_ERROR. |
| `6.7-7-78` | informative | COMPRESSION_ERROR (0x09) means the endpoint cannot maintain the field section compression context. <br>_(all [CLIENT])_ | COMPRESSION_ERROR (0x09): The endpoint is unable to maintain the field section compression context for the connection. | hpack/ + conn/: HPACK decode failures must surface as connection error COMPRESSION_ERROR (connection-level, never stream-level). |
| `6.7-7-79` | informative | CONNECT_ERROR (0x0a) means the TCP connection behind a CONNECT request was reset or abnormally closed. <br>_(all [CLIENT])_ | CONNECT_ERROR (0x0a): The connection established in response to a CONNECT request (Section 8.5) was reset or abnormally closed. | frame/: constant 0x0a needed; conn/ has no CONNECT support, so receive-side just surfaces the code verbatim. |
| `6.7-7-80` | informative | ENHANCE_YOUR_CALM (0x0b) signals the peer is generating excessive load. <br>_(all [CLIENT])_ | ENHANCE_YOUR_CALM (0x0b): The endpoint detected that its peer is exhibiting a behavior that might be generating excessive load. | client/: load-gen users will see this from defensive servers — surface distinctly in metrics/hooks so callers can back off; frame/ constant 0x0b. |
| `6.7-7-81` | informative | INADEQUATE_SECURITY (0x0c) means the transport does not meet minimum security requirements. <br>_(all [CLIENT])_ | INADEQUATE_SECURITY (0x0c): The underlying transport has properties that do not meet minimum security requirements (see Section 9.2). | conn/: servers send this for bad TLS config (relevant given the opt-in G402 TLS settings) — surface clearly to the caller; frame/ constant 0x0c. |
| `6.7-7-82` | informative | HTTP_1_1_REQUIRED (0x0d) means the peer requires HTTP/1.1 instead of HTTP/2. <br>_(all [CLIENT])_ | HTTP_1_1_REQUIRED (0x0d): The endpoint requires that HTTP/1.1 be used instead of HTTP/2. | client/: on RST/GOAWAY with HTTP_1_1_REQUIRED the client SHOULD retry over HTTP/1.1 — check whether the ALPN/H1 transport fallback honors this signal. |
| `6.7-7-83` | MUST NOT | Unknown or unsupported error codes must not trigger any special behavior. <br>_(endpoint [CLIENT])_ | Unknown or unsupported error codes MUST NOT trigger any special behavior. | frame/ + conn/: codes > 0x0d must parse cleanly and flow through error surfacing unchanged — never rejected as malformed. |
| `6.7-7-84` | MAY | An implementation may treat unknown error codes as equivalent to INTERNAL_ERROR. <br>_(endpoint [CLIENT])_ | These MAY be treated by an implementation as being equivalent to INTERNAL_ERROR. | conn/: optional normalization of unknown codes to INTERNAL_ERROR for caller-facing errors; keep the raw 32-bit value available in the typed error. |

### 9113 section 8.1-8.3 - RFC 9113 §8.1–8.3: HTTP Message Framing, Malformed Messages, HTTP Fields, HTTP Control Data (pseudo-headers)

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `8.1-8.3-1` | informative | A client sends each HTTP request on a new stream with a previously unused stream identifier; the server sends the response on the same stream. <br>_(client,server [CLIENT])_ | A client sends an HTTP request on a new stream, using a previously unused stream identifier | conn/: NewStream/SendHeaders deferred-id allocation must guarantee monotonically increasing, never-reused odd ids; responses demuxed by stream id. |
| `8.1-8.3-2` | informative | An HTTP message is exactly: one HEADERS frame (+zero or more CONTINUATIONs) with the header section, zero or more DATA frames with content, and optionally one trailer HEADERS (+CONTINUATIONs). <br>_(all [CLIENT])_ | one HEADERS frame (followed by zero or more CONTINUATION frames) containing the header section | conn/: response assembly must accept HEADERS(+CONT) -> DATA* -> optional trailer HEADERS(+CONT) and reject any other frame sequence on the stream. |
| `8.1-8.3-3` | MAY | For responses only, a server may send any number of interim (1xx) responses before the final-response HEADERS. <br>_(server [CLIENT])_ | a server MAY send any number of interim responses before the HEADERS frame containing a final response | conn/: client must surface unlimited 1xx HEADERS blocks before the final response without treating them as the final response (memory notes H2 once had zero 1xx support — verify). |
| `8.1-8.3-4` | informative | A HEADERS frame with END_STREAM set that carries an informational (1xx) status code is malformed. <br>_(all [CLIENT])_ | A HEADERS frame with the END_STREAM flag set that carries an informational status code is malformed | conn/: response validation must RST_STREAM(PROTOCOL_ERROR) on 1xx :status combined with END_STREAM. |
| `8.1-8.3-5` | informative | The last frame of a message bears END_STREAM; a HEADERS frame with END_STREAM may still be followed by CONTINUATION frames completing the field block. <br>_(all [CLIENT])_ | a HEADERS frame with the END_STREAM flag set can be followed by CONTINUATION frames that carry any remaining fragments of the field block | conn/: stream-end processing must be deferred until END_HEADERS arrives on a trailing CONTINUATION, not fired at HEADERS+END_STREAM. |
| `8.1-8.3-6` | MUST NOT | No frame from any stream may be interleaved between a HEADERS frame and its following CONTINUATION frames. <br>_(sender [CLIENT])_ | Other frames (from any stream) MUST NOT occur between the HEADERS frame and any CONTINUATION frames that might follow | conn/: send side — header block writes (HEADERS+CONTINUATIONs) must be atomic under wmu; recv side — reader must error on interleaved frames mid-field-block (frame/ or conn/ contiguity check). |
| `8.1-8.3-7` | informative | Chunked transfer encoding does not exist in HTTP/2; DATA frames carry content. <br>_(all [CLIENT])_ | The chunked transfer encoding defined in Section 7.1 of [HTTP/1.1] cannot be used in HTTP/2 | client/: request builder must never emit Transfer-Encoding on HTTP/2 (ties to 8.2.2 prohibition); no chunked decoding on receive. |
| `8.1-8.3-8` | MUST NOT | Trailer field blocks must not include pseudo-header fields (restated in 8.3: pseudo-header fields must not appear in a trailer section). <br>_(sender [CLIENT])_ | Trailers MUST NOT include pseudo-header fields | conn/: if request-trailer sending is supported, reject ':'-prefixed names in trailer send path. |
| `8.1-8.3-9` | MUST | An endpoint receiving pseudo-header fields in trailers must treat the message as malformed. <br>_(endpoint [CLIENT])_ | An endpoint that receives pseudo-header fields in trailers MUST treat the request or response as malformed | conn/: trailer HEADERS decode must reject any pseudo-header -> RST_STREAM(PROTOCOL_ERROR). |
| `8.1-8.3-10` | MUST | A HEADERS frame without END_STREAM arriving after the request-opening HEADERS or after a final (non-1xx) status code makes the message malformed (trailers must end the stream). <br>_(endpoint [CLIENT])_ | after receiving a final (non-informational) status code MUST treat the corresponding request or response as malformed | conn/: a second non-interim HEADERS on a response stream is only valid as trailers, i.e. only with END_STREAM; otherwise RST(PROTOCOL_ERROR). |
| `8.1-8.3-11` | informative | A request/response exchange fully consumes one stream: request HEADERS opens the stream; the END_STREAM frame moves it to half-closed (local) for the client and half-closed (remote) for the server. <br>_(all [CLIENT])_ | causes the stream to become "half-closed (local)" for the client and "half-closed (remote)" for the server | conn/: stream state machine — sending END_STREAM sets localEnded/half-closed(local); no stream reuse after close. |
| `8.1-8.3-12` | informative | An HTTP response is complete once a frame with END_STREAM (plus any CONTINUATIONs completing its field block) is received. <br>_(all [CLIENT])_ | An HTTP response is complete after the server sends -- or the client receives -- a frame with the END_STREAM flag set | conn/: response-complete event fires on END_STREAM (deferred past CONTINUATIONs per fact 5); remoteEnded transition. |
| `8.1-8.3-13` | MAY | After sending a complete response, a server may send RST_STREAM with NO_ERROR to ask the client to abort sending the rest of the request. <br>_(server [CLIENT])_ | a server MAY request that the client abort transmission of a request without error by sending a RST_STREAM with an error code of NO_ERROR after sending a complete response | conn/: OnRSTStream(NO_ERROR) arriving after remoteEnded must stop the request-body writer without erroring the exchange. |
| `8.1-8.3-14` | MUST NOT | A client must not discard an already-complete response merely because a RST_STREAM(NO_ERROR) follows it. <br>_(client [CLIENT])_ | Clients MUST NOT discard responses as a result of receiving such a RST_STREAM | conn/: verify RST(NO_ERROR) after END_STREAM does not convert the delivered response into a stream error (check OnRSTStream vs bothEnded/recycle path; see conn-recycle-teardown-race memory). |
| `8.1-8.3-15` | informative | Malformed = otherwise-valid frame sequence invalid due to extraneous frames, prohibited fields/pseudo-headers, missing mandatory pseudo-headers, uppercase field names, or invalid names/values. <br>_(all [CLIENT])_ | the presence of extraneous frames, prohibited fields or pseudo-header fields, the absence of mandatory pseudo-header fields, the inclusion of uppercase field names | conn/: this is the checklist for the response-validation path — confirm each category is detected and routed to stream-error handling. |
| `8.1-8.3-16` | informative | A message is malformed if its content-length value does not equal the sum of DATA frame payload lengths forming the content, unless the message is defined to have no content. <br>_(all [CLIENT])_ | malformed if the value of a content-length header field does not equal the sum of the DATA frame payload lengths that form the content | conn/: verify received DATA byte total against response content-length at END_STREAM (and that sent request DATA matches any content-length the client set). |
| `8.1-8.3-17` | MAY | Responses defined to have no content (204, 304, response to HEAD) may carry a non-zero content-length with no DATA frames. <br>_(server [CLIENT])_ | A response that is defined to have no content, as described in Section 6.4.1 of [HTTP], MAY have a non-zero content-length header field | conn/: content-length-vs-DATA check must be skipped for 204/304 and HEAD responses — client must know the request method per stream. |
| `8.1-8.3-18` | MUST NOT | Non-tunnel intermediaries must not forward a malformed request or response. <br>_(intermediary [not-client])_ | MUST NOT forward a malformed request or response | n/a for pure client — intermediary-only forwarding rule. |
| `8.1-8.3-19` | MUST | Detected malformed requests or responses must be treated as a stream error of type PROTOCOL_ERROR. <br>_(all [CLIENT])_ | Malformed requests or responses that are detected MUST be treated as a stream error (Section 5.4.2) of type PROTOCOL_ERROR | conn/: malformed handling must send RST_STREAM(PROTOCOL_ERROR) — a stream error, not a connection error/GOAWAY. |
| `8.1-8.3-20` | MAY | For a malformed request, a server may still send an HTTP response before closing or resetting the stream. <br>_(server [not-client])_ | For malformed requests, a server MAY send an HTTP response prior to closing or resetting the stream | n/a — server-side option; client just needs the fact-22 tolerance for early responses. |
| `8.1-8.3-21` | MUST NOT | Clients must not accept (deliver to the application) a malformed response. <br>_(client [CLIENT])_ | Clients MUST NOT accept a malformed response | conn/+client/: a malformed response must surface as an error to the Do/DoStream caller and be RST — never returned as a usable response. |
| `8.1-8.3-22` | MAY | A server may generate a final response before receiving the entire request. <br>_(server [CLIENT])_ | A server MAY generate a final response before receiving an entire request when the response does not depend on the remainder of the request being correct | conn/: client body writer (writeData/acquireSendCredits) must tolerate a complete response + END_STREAM arriving while the request body is still being sent. |
| `8.1-8.3-23` | informative | HTTP fields are carried only in HEADERS, CONTINUATION, and PUSH_PROMISE frames, HPACK-compressed. <br>_(all [CLIENT])_ | conveyed by HTTP/2 in the HEADERS, CONTINUATION, and PUSH_PROMISE frames, compressed with HPACK | conn/+hpack/: field blocks from these three frame types all flow through the single per-conn hpack.Decoder in receive order. |
| `8.1-8.3-24` | MUST | Field names must be converted to lowercase when constructing an HTTP/2 message. <br>_(sender [CLIENT])_ | Field names MUST be converted to lowercase when constructing an HTTP/2 message | client/+conn/: request builder must lowercase (or reject uppercase) header names before hpack encoding — hpack/ itself will happily encode uppercase. |
| `8.1-8.3-25` | SHOULD | Implementations should validate field names/values against HTTP field-name and field-value grammar and treat prohibited characters as malformed. <br>_(all [CLIENT])_ | implementations SHOULD validate field names and values according to their definitions in Sections 5.1 and 5.5 of [HTTP] | conn/: check whether full RFC 9110 §5.1/§5.5 grammar validation exists on decoded fields, or only the minimal checks of facts 27-30. |
| `8.1-8.3-26` | MUST | The four character-level checks below are mandatory minimal validation of field names and values. <br>_(all [CLIENT])_ | Implementations MUST perform the following minimal validation of field names and values | conn/: validation must sit above hpack decode (hpack/ is a pure codec) and run on every received field block — headers, interim, trailers, and PUSH_PROMISE. |
| `8.1-8.3-27` | MUST NOT | A field name must not contain octets 0x00-0x20, 0x41-0x5a (uppercase A-Z), or 0x7f-0xff. <br>_(all [CLIENT])_ | A field name MUST NOT contain characters in the ranges 0x00-0x20, 0x41-0x5a, or 0x7f-0xff (all ranges inclusive) | conn/: per-octet name check on receive; client/ mirror on send. Verify exact range boundaries (0x20 and 0x7f included). |
| `8.1-8.3-28` | MUST NOT | Except for pseudo-header fields (single leading colon), field names must not include a colon. <br>_(all [CLIENT])_ | field names MUST NOT include a colon (ASCII COLON, 0x3a) | conn/: reject ':' at any non-leading position, and leading-colon names that are not a defined pseudo-header (ties to fact 44). |
| `8.1-8.3-29` | MUST NOT | A field value must not contain NUL (0x00), LF (0x0a), or CR (0x0d) at any position. <br>_(all [CLIENT])_ | A field value MUST NOT contain the zero value (ASCII NUL, 0x00), line feed (ASCII LF, 0x0a), or carriage return (ASCII CR, 0x0d) at any position | conn/: per-octet value scan on receive (request-smuggling defense); client/ must also refuse to send such values. |
| `8.1-8.3-30` | MUST NOT | A field value must not start or end with SP (0x20) or HTAB (0x09). <br>_(all [CLIENT])_ | A field value MUST NOT start or end with an ASCII whitespace character (ASCII SP or HTAB, 0x20 or 0x09) | conn/: check first/last octet of every decoded value; empty values are fine. |
| `8.1-8.3-31` | MUST | A message containing any field violating the minimal-validation conditions must be treated as malformed. <br>_(all [CLIENT])_ | A request or response that contains a field that violates any of these conditions MUST be treated as malformed | conn/: violation routes to malformed handling = RST_STREAM(PROTOCOL_ERROR) per fact 19, and response withheld from caller per fact 21. |
| `8.1-8.3-32` | MUST NOT | An intermediary that does not process fields must still not forward fields containing the prohibited octets. <br>_(intermediary [not-client])_ | an intermediary that does not process fields when forwarding messages MUST NOT forward fields that contain any of the values that are listed as prohibited above | n/a — forwarding-intermediary rule. |
| `8.1-8.3-33` | SHOULD | When a request violates these field requirements, the implementation should generate a 400 (Bad Request) unless a better status applies or none can be sent. <br>_(server [not-client])_ | an implementation SHOULD generate a 400 (Bad Request) status code | n/a — server response-generation rule. |
| `8.1-8.3-34` | informative | Values invalid per a specific field's own definition do NOT make the message malformed — only the generic-syntax rules above do. <br>_(all [CLIENT])_ | Field values that are not valid according to the definition of the corresponding field do not cause a request to be malformed | conn/: validation must be limited to generic syntax — do not reject responses for semantically bad values of individual fields (e.g. unparseable date). |
| `8.1-8.3-35` | MUST NOT | An endpoint must not generate an HTTP/2 message containing connection-specific header fields: Connection, Proxy-Connection, Keep-Alive, Transfer-Encoding, Upgrade. <br>_(endpoint [CLIENT])_ | An endpoint MUST NOT generate an HTTP/2 message containing connection-specific header fields | client/: request builder must reject or strip Connection, Proxy-Connection, Keep-Alive, Transfer-Encoding, Upgrade before encoding. |
| `8.1-8.3-36` | MUST | Any received message containing connection-specific header fields must be treated as malformed. <br>_(all [CLIENT])_ | Any message containing connection-specific header fields MUST be treated as malformed | conn/: response (and trailer/PUSH_PROMISE) validation must reject Connection/Proxy-Connection/Keep-Alive/Transfer-Encoding/Upgrade -> RST(PROTOCOL_ERROR). |
| `8.1-8.3-37` | MUST NOT | Sole exception: TE may appear in an HTTP/2 request but must not carry any value other than "trailers". <br>_(endpoint [CLIENT])_ | the TE header field, which MAY be present in an HTTP/2 request; when it is, it MUST NOT contain any value other than "trailers" | client/+conn/: TE allowed on requests only with exact value "trailers"; TE in a received response is connection-specific -> malformed. Sibling check: HTTP/3 had this backwards until fix #290 — probe the H2 path independently. |
| `8.1-8.3-38` | MUST | An intermediary translating HTTP/1.x to HTTP/2 must remove connection-specific header fields first. <br>_(intermediary [not-client])_ | An intermediary transforming an HTTP/1.x message to HTTP/2 MUST remove connection-specific header fields | n/a — translation-intermediary rule. |
| `8.1-8.3-39` | MAY | The Cookie header may be split into multiple field lines (one or more cookie-pairs each) for HPACK compression efficiency. <br>_(sender [CLIENT])_ | the Cookie header field MAY be split into separate header fields, each with one or more cookie-pairs | client/+hpack/: optional send-side optimization — check whether the encoder splits cookie crumbs; receive side must accept split cookies either way. |
| `8.1-8.3-40` | MUST | Multiple Cookie fields after decompression must be concatenated into one string with the two-octet delimiter 0x3b 0x20 ("; ") before leaving an HTTP/2 context. <br>_(endpoint,intermediary [CLIENT])_ | these MUST be concatenated into a single octet string using the two-octet delimiter of 0x3b, 0x20 (the ASCII string "; ") before being passed into a non-HTTP/2 context | client/: if decoded fields are handed to HTTP/1.1 contexts or generic applications, cookie crumbs must be joined with "; " — check how Do exposes repeated cookie field lines. |
| `8.1-8.3-41` | informative | Pseudo-header fields begin with ':' (0x3a) and carry message control data; they are not HTTP header fields. <br>_(all [CLIENT])_ | HTTP/2 uses special pseudo-header fields beginning with a ':' character (ASCII 0x3a) to convey message control data | conn/: pseudo-headers must be separated from regular fields in the decoded representation, not exposed as ordinary headers to callers. |
| `8.1-8.3-42` | MUST NOT | Endpoints must not generate pseudo-header fields other than those defined in RFC 9113 (absent a negotiated extension). <br>_(endpoint [CLIENT])_ | Endpoints MUST NOT generate pseudo-header fields other than those defined in this document | client/: request builder must refuse caller-supplied ':'-prefixed names outside :method/:scheme/:authority/:path. |
| `8.1-8.3-43` | MUST NOT | Request-defined pseudo-headers must not appear in responses and response-defined pseudo-headers must not appear in requests. <br>_(endpoint [CLIENT])_ | Pseudo-header fields defined for requests MUST NOT appear in responses; pseudo-header fields defined for responses MUST NOT appear in requests | conn/: response validation rejects :method/:scheme/:authority/:path; PUSH_PROMISE (a request) rejects :status; client never sends :status. |
| `8.1-8.3-44` | MUST | A message containing undefined or invalid pseudo-header fields must be treated as malformed. <br>_(endpoint [CLIENT])_ | Endpoints MUST treat a request or response that contains undefined or invalid pseudo-header fields as malformed | conn/: unknown ':'-prefixed name, or wrong-context pseudo-header, in any decoded field block -> RST(PROTOCOL_ERROR). |
| `8.1-8.3-45` | MUST | All pseudo-header fields must precede all regular field lines within a field block. <br>_(sender [CLIENT])_ | All pseudo-header fields MUST appear in a field block before all regular field lines | client/+conn/: encoder must emit pseudo-headers first when building request field blocks. |
| `8.1-8.3-46` | MUST | A message with a pseudo-header field appearing after a regular field line must be treated as malformed. <br>_(receiver [CLIENT])_ | contains a pseudo-header field that appears in a field block after a regular field line MUST be treated as malformed | conn/: decoder-consumer must track the pseudo->regular boundary per field block and RST(PROTOCOL_ERROR) on a late pseudo-header. |
| `8.1-8.3-47` | MUST NOT | The same pseudo-header field name must not appear more than once in a field block. <br>_(sender [CLIENT])_ | The same pseudo-header field name MUST NOT appear more than once in a field block | client/: request builder must emit each of :method/:scheme/:authority/:path at most once. |
| `8.1-8.3-48` | MUST | A field block containing a repeated pseudo-header field name must be treated as malformed. <br>_(receiver [CLIENT])_ | A field block for an HTTP request or response that contains a repeated pseudo-header field name MUST be treated as malformed | conn/: duplicate :status (or any pseudo) in a response field block -> RST(PROTOCOL_ERROR). |
| `8.1-8.3-49` | informative | The :method pseudo-header field carries the HTTP method. <br>_(client [CLIENT])_ | The ":method" pseudo-header field includes the HTTP method | client/: request builder maps method to :method verbatim (method names are case-sensitive tokens). |
| `8.1-8.3-50` | informative | :scheme carries the scheme of the target URI; it is omitted for CONNECT and is not restricted to "http"/"https". <br>_(client [CLIENT])_ | The ":scheme" pseudo-header field includes the scheme portion of the request target | client/: :scheme from target URI; omit for CONNECT; do not hard-reject non-http(s) schemes. |
| `8.1-8.3-51` | MUST NOT | A request recipient must not use the Host header to determine the target URI when :authority is present. <br>_(server [not-client])_ | The recipient of an HTTP/2 request MUST NOT use the Host header field to determine the target URI if ":authority" is present | n/a — request-recipient (server) routing rule. |
| `8.1-8.3-52` | MUST | Clients generating requests directly must convey authority via :authority, and must omit :authority entirely when there is no authority information. <br>_(client [CLIENT])_ | Clients that generate HTTP/2 requests directly MUST use the ":authority" pseudo-header field to convey authority information, unless there is no authority information to convey | client/: :authority always built from the target URI authority; verify the no-authority case omits the pseudo-header rather than sending it empty. |
| `8.1-8.3-53` | MUST NOT | Clients must not generate a request whose Host header differs from the :authority pseudo-header. <br>_(client [CLIENT])_ | Clients MUST NOT generate a request with a Host header field that differs from the ":authority" pseudo-header field | client/: if a caller supplies an explicit Host header, verify it matches :authority (or reject/replace it). |
| `8.1-8.3-54` | SHOULD | A server should treat a request as malformed when Host identifies a different entity than :authority (after normalization). <br>_(server [not-client])_ | A server SHOULD treat a request as malformed if it contains a Host header field that identifies an entity that differs | n/a — server-side comparison rule (but motivates client-side fact 53 enforcement). |
| `8.1-8.3-55` | MUST | Non-origin servers comparing Host and :authority must use scheme-based normalization of the two values. <br>_(server,intermediary [not-client])_ | other servers MUST perform scheme-based normalization (see Section 6.2.3 of [RFC3986]) of the two fields | n/a — server/intermediary comparison rule. |
| `8.1-8.3-56` | MUST | A forwarding intermediary must construct :authority from the original request's control-data authority, omitting it when the target URI has none. <br>_(intermediary [not-client])_ | An intermediary that forwards a request over HTTP/2 MUST construct an ":authority" pseudo-header field using the authority information from the control data of the original request | n/a — forwarding-intermediary rule. |
| `8.1-8.3-57` | MUST | An intermediary generating a Host header (e.g. for HTTP/1.1) must take its value from :authority unless it also changes the request target. <br>_(intermediary [not-client])_ | MUST use the value from the ":authority" pseudo-header field as the value of the Host field, unless the intermediary also changes the request target | n/a — HTTP/2-to-HTTP/1.1 translation rule. |
| `8.1-8.3-58` | MAY | An intermediary forwarding over HTTP/2 may retain any Host header field. <br>_(intermediary [not-client])_ | An intermediary that forwards a request over HTTP/2 MAY retain any Host header field | n/a — intermediary option. |
| `8.1-8.3-59` | MUST NOT | :authority must not include the deprecated userinfo subcomponent for http/https URIs. <br>_(client [CLIENT])_ | ":authority" MUST NOT include the deprecated userinfo subcomponent for "http" or "https" schemed URIs | client/: strip user:pass@ from the target URL before building :authority. |
| `8.1-8.3-60` | informative | :path carries the absolute-path plus optional '?query' of the target URI; asterisk-form OPTIONS uses the value '*'. <br>_(client [CLIENT])_ | The ":path" pseudo-header field includes the path and query parts of the target URI | client/: :path = absolute-path [+ '?' query]; fragment excluded. |
| `8.1-8.3-61` | MUST | :path must not be empty for http/https URIs; a URI with no path component must produce a :path of '/'. <br>_(client [CLIENT])_ | This pseudo-header field MUST NOT be empty for "http" or "https" URIs; "http" or "https" URIs that do not contain a path component MUST include a value of '/' | client/: request builder normalizes empty path to "/" for http/https targets. |
| `8.1-8.3-62` | MUST | An OPTIONS request for an http/https URI without a path component must use :path '*'. <br>_(client [CLIENT])_ | these MUST include a ":path" pseudo-header field with a value of '*' | client/: special-case OPTIONS with no path -> :path "*" instead of "/". |
| `8.1-8.3-63` | informative | CONNECT requests omit the :path pseudo-header field entirely. <br>_(client [CLIENT])_ | CONNECT requests (Section 8.5), where the ":path" pseudo-header field is omitted | client/: if CONNECT is supported, omit :path and :scheme; if unsupported, document it. |
| `8.1-8.3-64` | MUST | Every non-CONNECT HTTP/2 request must include exactly one valid value each for :method, :scheme, and :path. <br>_(client [CLIENT])_ | All HTTP/2 requests MUST include exactly one valid value for the ":method", ":scheme", and ":path" pseudo-header fields, unless they are CONNECT requests | client/: request builder always emits all three exactly once for non-CONNECT; conn/ validates PUSH_PROMISE request blocks the same way. |
| `8.1-8.3-65` | informative | A request omitting mandatory pseudo-header fields is malformed. <br>_(all [CLIENT])_ | An HTTP request that omits mandatory pseudo-header fields is malformed | conn/: applies to received PUSH_PROMISE field blocks — missing :method/:scheme/:path -> malformed -> RST(PROTOCOL_ERROR). |
| `8.1-8.3-66` | informative | HTTP/2 requests carry no explicit version indicator; they implicitly have protocol version "2.0". <br>_(all [CLIENT])_ | All HTTP/2 requests implicitly have a protocol version of "2.0" | client/: never emit a version token in :path/:method; report proto "2.0"/"HTTP/2" in the response object. |
| `8.1-8.3-67` | MUST | A single :status pseudo-header carrying the status code must be included in every response, including each interim response; a response without it is malformed. <br>_(server [CLIENT])_ | This pseudo-header field MUST be included in all responses, including interim responses; otherwise, the response is malformed | conn/: every response HEADERS block (interim and final) must contain exactly one valid :status; missing/duplicate/invalid -> malformed -> RST(PROTOCOL_ERROR). |
| `8.1-8.3-68` | informative | HTTP/2 responses implicitly have protocol version "2.0". <br>_(all [CLIENT])_ | HTTP/2 responses implicitly have a protocol version of "2.0" | client/: response object reports HTTP/2 without expecting any wire version field. |

### 9113 section 8.4-8.8 - RFC 9113 — Server Push, CONNECT, Upgrade, Request Reliability, Examples

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `8.4-8.8-1` | informative | A client may disable server push by setting SETTINGS_ENABLE_PUSH to 0; this is negotiated per hop. <br>_(client [CLIENT])_ | The SETTINGS_ENABLE_PUSH setting can be set to 0 to indicate that server push is disabled. | conn/: verify handshake SETTINGS sends ENABLE_PUSH=0 (client has no push support); this decides whether the rest of the 8.4 client duties are vacuous. |
| `8.4-8.8-2` | MUST | Promised requests must be safe and cacheable methods. <br>_(server [CLIENT])_ | Promised requests MUST be safe (see Section 9.2.1 of [HTTP]) and cacheable (see Section 9.2.3 of [HTTP]). | conn/: only relevant if PUSH_PROMISE is ever accepted; with ENABLE_PUSH=0 + connection-error-on-receipt, vacuous — confirm which path exists. |
| `8.4-8.8-3` | informative | Promised requests cannot carry request content or a trailer section; it is not possible to push a response to a request that includes message content. <br>_(server [CLIENT])_ | Promised requests cannot include any content or a trailer section. | conn/: if push were accepted, a promise implying a body (e.g. content-length) must be rejected; vacuous when PUSH_PROMISE is a connection error. |
| `8.4-8.8-4` | MUST | A client receiving a promised request that is not cacheable, not known safe, or that indicates request content must reset the promised stream with stream error PROTOCOL_ERROR. <br>_(client [CLIENT])_ | Clients that receive a promised request that is not cacheable, that is not known to be safe, or that indicates the presence of request content MUST reset the promised stream | conn/: if push accepted, add RST_STREAM(PROTOCOL_ERROR) validation on decoded promise; N/A if PUSH_PROMISE receipt is already ConnError under ENABLE_PUSH=0. |
| `8.4-8.8-5` | informative | Cacheable pushed responses can be stored by a client that implements an HTTP cache, and count as validated on the origin while the promised stream is still open. <br>_(client [CLIENT])_ | Pushed responses that are cacheable (see Section 3 of [CACHING]) can be stored by the client, if it implements an HTTP cache. | client/: this client implements no HTTP cache — vacuously satisfied; note in docs/RFC_COVERAGE.md as N/A. |
| `8.4-8.8-6` | MUST NOT | Non-cacheable pushed responses must not be stored by any HTTP cache. <br>_(all [CLIENT])_ | Pushed responses that are not cacheable MUST NOT be stored by any HTTP cache. | **VERIFY** client/: no cache exists — vacuously satisfied; document as N/A. -- Quote is not verbatim: it splices two non-adjacent fragments with an ellipsis, eliding the parenthetical "(e.g., if the \"no-cache\" cache response directive is present; see Section 5.2.2.4 of [CACHING])". Actual source (lines 2767-2771): "Pushed responses are considered successfully validated on the origin server (e.g., if the \"no-cache\" cache response directive is present; see Section 5.2.2.4 of [CACHING]) while the stream identified by the promised stream identifier is still open." Substance/level/applies are correct; only the quote fails verbatim. \|\| Quote uses an internal '...' that elides a substantive parenthetical, so it is not a contiguous verbatim string (an internal elision is not a wrap/whitespace diff). Source (lines 2767-2771): 'Pushed responses are considered successfully validated on the origin server (e.g., if the "no-cache" cache response directive is present; see Section 5.2.2.4 of [CACHING]) while the stream identified by the promised stream identifier is still open.' The requirement's substance and level (informative) and applies_to (client) are otherwise accurate; only quote fidelity fails. |
| `8.4-8.8-7` | MAY | Non-cacheable pushed responses may still be made available to the application separately from any cache. <br>_(client [CLIENT])_ | They MAY be made available to the application separately. | client/: only relevant if pushed responses are ever surfaced through the public API; currently N/A. |
| `8.4-8.8-8` | MUST | The server must set :authority in a promised request to a value it is authoritative for. <br>_(server [CLIENT])_ | The server MUST include a value in the ":authority" pseudo-header field for which the server is authoritative | conn/: client-side enforcement is fact 9; server rule itself needs no client code. |
| `8.4-8.8-9` | MUST | A client must treat a PUSH_PROMISE whose :authority the server is not authoritative for as a stream error of type PROTOCOL_ERROR. <br>_(client [CLIENT])_ | A client MUST treat a PUSH_PROMISE for which the server is not authoritative as a stream error (Section 5.4.2) of type PROTOCOL_ERROR. | conn/: if push accepted, compare promise :authority against connection authority (TLS SNI/cert) before surfacing; vacuous under push-disabled ConnError path. |
| `8.4-8.8-10` | informative | An intermediary may decline to forward server pushes and may originate its own pushes toward the client. <br>_(intermediary [not-client])_ | An intermediary can receive pushes from the server and choose not to forward them on to the client. | No package action — intermediary behavior only. |
| `8.4-8.8-11` | MUST | Clients cannot push, so a server must treat receipt of a PUSH_PROMISE frame as a connection error of type PROTOCOL_ERROR. <br>_(server [not-client])_ | servers MUST treat the receipt of a PUSH_PROMISE frame as a connection error (Section 5.4.1) of type PROTOCOL_ERROR. | conn/: server-side enforcement; client-side prohibition is fact 19 — ensure conn/ has no PUSH_PROMISE emit path. |
| `8.4-8.8-12` | informative | A server cannot set SETTINGS_ENABLE_PUSH to any value other than 0. <br>_(server [CLIENT])_ | A server cannot set the SETTINGS_ENABLE_PUSH setting to a value other than 0 (see Section 6.5.2). | conn/: check OnSettings/applyPeerSettings rejects a server-sent ENABLE_PUSH value other than 0 as ConnError(PROTOCOL_ERROR) per the §6.5.2 rule this references. |
| `8.4-8.8-13` | informative | Promised requests are always associated with an explicit client request, and PUSH_PROMISE frames are sent on that request's stream. <br>_(server [CLIENT])_ | The PUSH_PROMISE frames sent by the server are sent on that explicit request's stream. | conn/: PUSH_PROMISE arriving on a stream the client never opened is invalid — verify the receive path checks the carrying stream exists in the registry. |
| `8.4-8.8-14` | informative | PUSH_PROMISE carries a promised stream identifier chosen from the server's stream-identifier space (even IDs, monotonic per §5.1.1). <br>_(server [CLIENT])_ | The PUSH_PROMISE frame also includes a promised stream identifier, chosen from the stream identifiers available to the server (see Section 5.1.1). | frame/: PUSH_PROMISE parser must extract the 31-bit promised-stream-ID field (R bit ignored); conn/ validates even + increasing if push accepted. |
| `8.4-8.8-15` | MUST | The header fields in PUSH_PROMISE plus its CONTINUATIONs must form a valid, complete request header set per §8.3.1. <br>_(server [CLIENT])_ | The header fields in PUSH_PROMISE and any subsequent CONTINUATION frames MUST be a valid and complete set of request header fields (Section 8.3.1). | conn/: if push accepted, run the same request pseudo-header validation (§8.3.1) against decoded promises; client-side enforcement is fact 17. |
| `8.4-8.8-16` | MUST | The server must set a :method in the promise that is both safe and cacheable. <br>_(server [CLIENT])_ | The server MUST include a method in the ":method" pseudo-header field that is safe and cacheable. | conn/: pairs with facts 4 and 17 — client validates safety/cacheability of the promised :method if push is accepted. |
| `8.4-8.8-17` | MUST | A client receiving a PUSH_PROMISE with an incomplete/invalid header set or a non-safe :method must respond on the promised stream with stream error PROTOCOL_ERROR. <br>_(client [CLIENT])_ | the ":method" pseudo-header field identifies a method that is not safe, it MUST respond on the promised stream with a stream error (Section 5.4.2) of type PROTOCOL_ERROR | conn/: stream-error (not connection-error) path targeting the PROMISED stream id, not the carrying stream — check which id an RST would be sent on if push were supported. |
| `8.4-8.8-18` | SHOULD | The server should send PUSH_PROMISE frames before any frames that reference the promised responses, to avoid a request race. <br>_(server [not-client])_ | The server SHOULD send PUSH_PROMISE (Section 6.6) frames prior to sending any frames that reference the promised responses. | No client action — server ordering guidance. |
| `8.4-8.8-19` | MUST NOT | A client must never send PUSH_PROMISE frames. <br>_(client [CLIENT])_ | PUSH_PROMISE frames MUST NOT be sent by the client. | conn/ + frame/: confirm conn/ has no code path that writes PUSH_PROMISE; if frame/ Framer exposes a PUSH_PROMISE writer (for tests), it must be unreachable from conn/. |
| `8.4-8.8-20` | MUST | PUSH_PROMISE may only be sent on a client-initiated stream in "open" or "half-closed (remote)" state from the server's perspective. <br>_(server [CLIENT])_ | the stream MUST be in either the "open" or "half-closed (remote)" state with respect to the server | conn/: mirror check on receive — PUSH_PROMISE on a stream that is closed or half-closed(local) from the client's view is a protocol violation; verify stream-state gate. |
| `8.4-8.8-21` | informative | PUSH_PROMISE frames may be interspersed with response frames but never inside a HEADERS/CONTINUATION sequence forming a single field block. <br>_(sender [CLIENT])_ | they cannot be interspersed with HEADERS and CONTINUATION frames that comprise a single field block | frame/ + conn/: CONTINUATION-contiguity enforcement (only CONTINUATION allowed after un-END_HEADERS frame) must also cover blocks started by PUSH_PROMISE. |
| `8.4-8.8-22` | informative | Sending PUSH_PROMISE creates a new stream in "reserved (local)" state for the server and "reserved (remote)" state for the client. <br>_(all [CLIENT])_ | Sending a PUSH_PROMISE frame creates a new stream and puts the stream into the "reserved (local)" state for the server and the "reserved (remote)" state for the client. | conn/: stream state machine needs a reserved(remote) state only if push is accepted; confirm the chosen posture (reject vs support) is explicit in code and docs. |
| `8.4-8.8-23` | informative | The pushed response is delivered as a standard §8.1 response sequence on a server-initiated stream using the promised stream identifier. <br>_(server [CLIENT])_ | the server can begin delivering the pushed response as a response (Section 8.3.2) on a server-initiated stream that uses the promised stream identifier | conn/: if push accepted, the promised stream must reuse the normal response-parsing path (HEADERS + DATA + trailers). |
| `8.4-8.8-24` | SHOULD NOT | A client that accepts a pushed response should not issue its own request for the promised resource until the promised stream has closed. <br>_(client [CLIENT])_ | the client SHOULD NOT issue any requests for the promised response until after the promised stream has closed. | client/: request-dedup against in-flight promises — only relevant if push is ever surfaced; currently N/A. |
| `8.4-8.8-25` | informative | A client that does not want a pushed response, or finds the server too slow, can send RST_STREAM with CANCEL or REFUSED_STREAM on the promised stream. <br>_(client [CLIENT])_ | the client can send a RST_STREAM frame, using either the CANCEL or REFUSED_STREAM code and referencing the pushed stream's identifier. | conn/: cancel-push mechanism — if push is rejected wholesale this is moot; otherwise RST(CANCEL) on the promised id is the disposal path. |
| `8.4-8.8-26` | informative | Advertising MAX_CONCURRENT_STREAMS=0 prevents the server opening push streams, but reserved streams do not count toward the concurrent stream limit, so PUSH_PROMISE frames can still arrive. <br>_(client,server [CLIENT])_ | this does not prevent the server from reserving streams using PUSH_PROMISE frames, because reserved streams do not count toward the concurrent stream limit. | conn/: MAX_CONCURRENT_STREAMS accounting (B.2.5 gate) must exclude reserved-state streams from the count in both directions. |
| `8.4-8.8-27` | informative | Clients that do not want pushed resources need to reset unwanted reserved streams or set SETTINGS_ENABLE_PUSH to 0. <br>_(client [CLIENT])_ | Clients that do not wish to receive pushed resources need to reset any unwanted reserved streams or set SETTINGS_ENABLE_PUSH to 0. | conn/: ENABLE_PUSH=0 in the client's handshake SETTINGS is the clean posture for this load-gen client — same check as fact 1. |
| `8.4-8.8-28` | MUST | A client receiving a pushed response must validate the server is authoritative for it, or that the providing proxy is configured for the corresponding request. <br>_(client [CLIENT])_ | Clients receiving a pushed response MUST validate that either the server is authoritative (see Section 10.1) or the proxy that provided the pushed response is configured for the corresponding request | client/ + conn/: authority-vs-certificate validation for pushed responses — vacuous while push is disabled; must exist before any future push support. |
| `8.4-8.8-29` | informative | The pushed-response stream starts with HEADERS, putting it in half-closed(remote) for the server and half-closed(local) for the client, and closes on the END_STREAM-flagged frame. <br>_(all [CLIENT])_ | begins with a HEADERS frame, which immediately puts the stream into the "half-closed (remote)" state for the server and "half-closed (local)" state for the client | conn/: state-machine transitions for pushed streams (reserved(remote) -> half-closed(local) -> closed) if push is ever supported. |
| `8.4-8.8-30` | informative | The client never sends an END_STREAM-flagged frame on a server-push stream. <br>_(client [CLIENT])_ | The client never sends a frame with the END_STREAM flag set for a server push. | conn/: on a pushed stream the client only sends RST_STREAM/WINDOW_UPDATE/PRIORITY — no DATA/HEADERS emit path should exist for it. |
| `8.4-8.8-31` | informative | A CONNECT request sets the :method pseudo-header field to CONNECT. <br>_(client [CLIENT])_ | The ":method" pseudo-header field is set to CONNECT. | conn/: if CONNECT is supported, the pseudo-header writer needs a CONNECT mode; if unsupported, document as an explicit non-goal. |
| `8.4-8.8-32` | MUST | In a CONNECT request the :scheme and :path pseudo-header fields must be omitted. <br>_(client [CLIENT])_ | The ":scheme" and ":path" pseudo-header fields MUST be omitted. | conn/: header emission for CONNECT must skip :scheme/:path — check writeHeaders pseudo-header construction has (or deliberately lacks) a CONNECT branch. |
| `8.4-8.8-33` | informative | In a CONNECT request the :authority pseudo-header field carries the host and port to connect to (authority-form request-target). <br>_(client [CLIENT])_ | The ":authority" pseudo-header field contains the host and port to connect to | conn/: CONNECT :authority must include the port — different validation from normal requests. |
| `8.4-8.8-34` | informative | A CONNECT request that does not follow the :method/:scheme/:path/:authority restrictions is malformed per §8.1.1. <br>_(all [CLIENT])_ | A CONNECT request that does not conform to these restrictions is malformed (Section 8.1.1). | conn/: malformed-request classification — the client must not emit a non-conforming CONNECT; add to pseudo-header validation if CONNECT is exposed. |
| `8.4-8.8-35` | informative | A proxy supporting CONNECT establishes the TCP connection and then signals success with a HEADERS frame carrying a 2xx status code. <br>_(proxy [CLIENT])_ | Once this connection is successfully established, the proxy sends a HEADERS frame containing a 2xx-series status code to the client | client/: tunnel-established detection = 2xx response HEADERS on the CONNECT stream; only relevant if CONNECT support is added. |
| `8.4-8.8-36` | MUST NOT | On a connected (tunnel) stream, frame types other than DATA, RST_STREAM, WINDOW_UPDATE, and PRIORITY must not be sent and must be treated as a stream error if received. <br>_(endpoint [CLIENT])_ | Frame types other than DATA or stream management frames (RST_STREAM, WINDOW_UPDATE, and PRIORITY) MUST NOT be sent on a connected stream and MUST be treated as a stream error | conn/: per-stream tunnel mode would need a frame-type allowlist (e.g. HEADERS after tunnel establishment = stream error); N/A until CONNECT exists. |
| `8.4-8.8-37` | informative | The END_STREAM flag on a DATA frame in a CONNECT tunnel is equivalent to the TCP FIN bit; either peer can close. <br>_(all [CLIENT])_ | The END_STREAM flag on a DATA frame is treated as being equivalent to the TCP FIN bit. | conn/: tunnel half-close semantics — END_STREAM maps to FIN, and the final DATA frame or segment may be empty. |
| `8.4-8.8-38` | informative | A client is expected to send an END_STREAM-flagged DATA frame after receiving an END_STREAM-flagged frame on a CONNECT stream. <br>_(client [CLIENT])_ | A client is expected to send a DATA frame with the END_STREAM flag set after receiving a frame with the END_STREAM flag set. | conn/: tunnel teardown ordering for a future CONNECT implementation — respond to remote half-close with local half-close. |
| `8.4-8.8-39` | informative | A proxy maps a received END_STREAM DATA frame to FIN on the last TCP segment, and a received FIN to an END_STREAM DATA frame. <br>_(proxy [not-client])_ | A proxy that receives a TCP segment with the FIN bit set sends a DATA frame with the END_STREAM flag set. | No client action — proxy translation behavior. |
| `8.4-8.8-40` | informative | A proxy signals any TCP connection error (including received RST) as a stream error of type CONNECT_ERROR. <br>_(proxy [CLIENT])_ | A proxy treats any error in the TCP connection, which includes receiving a TCP segment with the RST bit set, as a stream error (Section 5.4.2) of type CONNECT_ERROR. | frame/: error-code enum must include CONNECT_ERROR (0xa); conn/ must surface RST_STREAM(CONNECT_ERROR) to the caller rather than choke on an unknown code. |
| `8.4-8.8-41` | MUST | A proxy must send a TCP segment with the RST bit set if it detects an error with the stream or the HTTP/2 connection. <br>_(proxy [not-client])_ | Correspondingly, a proxy MUST send a TCP segment with the RST bit set if it detects an error with the stream or the HTTP/2 connection. | No client action — proxy behavior. |
| `8.4-8.8-42` | informative | HTTP/2 does not support the 101 (Switching Protocols) status code; similar functionality goes through extended CONNECT (RFC 8441). <br>_(all [CLIENT])_ | HTTP/2 does not support the 101 (Switching Protocols) informational status code (Section 15.2.2 of [HTTP]). | conn/: response validation should reject :status=101 (malformed response) — check 1xx handling explicitly excludes 101. |
| `8.4-8.8-43` | informative | GOAWAY's last-stream-id marks the highest stream that might have been processed; requests on higher-numbered streams are guaranteed safe to retry. <br>_(client [CLIENT])_ | The GOAWAY frame indicates the highest stream number that might have been processed. Requests on streams with higher numbers are therefore guaranteed to be safe to retry. | conn/ (B.2.6) + client/: streams > lastStreamID get EventReset(REFUSED_STREAM) — verify client/ retry layer treats those as safe-to-retry, including non-idempotent methods. |
| `8.4-8.8-44` | informative | RST_STREAM with REFUSED_STREAM means the stream closed before any processing, so the request on it can be safely retried. <br>_(client,server [CLIENT])_ | The REFUSED_STREAM error code can be included in a RST_STREAM frame to indicate that the stream is being closed prior to any processing having occurred. | client/: retry policy should classify RST(REFUSED_STREAM) as a no-processing-occurred signal, distinct from other RST codes. |
| `8.4-8.8-45` | MAY | Clients may automatically retry requests that are guaranteed unprocessed, even non-idempotent ones. <br>_(client [CLIENT])_ | Requests that have not been processed have not failed; clients MAY automatically retry them, even those with non-idempotent methods. | client/: check the retry/backoff layer gates automatic retry of non-idempotent methods on exactly these two signals (GOAWAY id, REFUSED_STREAM) and nothing weaker. |
| `8.4-8.8-46` | MUST NOT | A server must not claim a stream was unprocessed unless it can guarantee that fact. <br>_(server [not-client])_ | A server MUST NOT indicate that a stream has not been processed unless it can guarantee that fact. | No client action — the client may rely on the guarantee (facts 43-45). |
| `8.4-8.8-47` | MUST NOT | If any stream frames reached the application layer, the server must not use REFUSED_STREAM for it and any GOAWAY must carry a last-stream-id >= that stream's id. <br>_(server [not-client])_ | REFUSED_STREAM MUST NOT be used for that stream, and a GOAWAY frame MUST include a stream identifier that is greater than or equal to the given stream identifier | No client action — server-side consistency rule backing the retry guarantee. |
| `8.4-8.8-48` | informative | The PING frame lets a client test whether an idle connection is still alive without sending a request (middleboxes silently drop idle bindings). <br>_(client [CLIENT])_ | The PING frame allows a client to safely test whether a connection is still active without sending a request. | conn/: client currently initiates no active PINGs (only echoes ACKs) — MAY-level capability gap worth noting for pool health-checking of idle conns. |
| `8.4-8.8-49` | informative | A GET request with no content is sent as a single HEADERS frame (plus zero or more CONTINUATIONs), with END_STREAM and END_HEADERS set when it fits. <br>_(client [CLIENT])_ | is therefore transmitted as a single HEADERS frame, followed by zero or more CONTINUATION frames containing the serialized block of request header fields. | conn/: bodyless requests must set END_STREAM on the HEADERS frame (no empty DATA); verify SendHeaders flag handling. |
| `8.4-8.8-50` | informative | A response with only control data and headers is a HEADERS frame plus zero or more CONTINUATION frames, with no DATA. <br>_(server [CLIENT])_ | a response that includes only control data and a response header is transmitted as a HEADERS frame (again, followed by zero or more CONTINUATION frames) containing the serialized block | conn/: response path must complete a response on HEADERS+END_STREAM alone (e.g. 304) without expecting DATA. |
| `8.4-8.8-51` | informative | A request with content is HEADERS + zero or more CONTINUATIONs + one or more DATA frames; END_HEADERS on the last header-block frame, END_STREAM on the final DATA frame. <br>_(client [CLIENT])_ | with the last CONTINUATION (or HEADERS) frame having the END_HEADERS flag set and the final DATA frame having the END_STREAM flag set | conn/ + frame/: verify writeHeaders/writeData flag placement — END_HEADERS terminates the block, END_STREAM only on the last DATA chunk. |
| `8.4-8.8-52` | informative | Data contributing to a single field line can be split across field block fragments (HEADERS/CONTINUATION boundaries are arbitrary). <br>_(all [CLIENT])_ | data contributing to any given field line could be spread between field block fragments | hpack/ + conn/: decoder must run only on the fully reassembled block — a Huffman/integer/literal may straddle a CONTINUATION boundary; check reassembly happens before hpack.Decode. |
| `8.4-8.8-53` | informative | A response with content is HEADERS + zero or more CONTINUATIONs + one or more DATA frames, with END_STREAM on the last DATA frame. <br>_(server [CLIENT])_ | followed by zero or more CONTINUATION frames, followed by one or more DATA frames, with the last DATA frame in the sequence having the END_STREAM flag set | conn/: response-complete detection = END_STREAM on final DATA (or trailer HEADERS, fact 55) — not connection close. |
| `8.4-8.8-54` | informative | An informational 1xx response (other than 101) is transmitted as a HEADERS frame plus zero or more CONTINUATIONs, before the final response HEADERS. <br>_(server [CLIENT])_ | An informational response using a 1xx status code other than 101 is transmitted as a HEADERS frame, followed by zero or more CONTINUATION frames. | conn/: multiple HEADERS blocks per stream — interim 1xx must be surfaced (or skipped) without treating the stream's response as complete; this was a historical gap shape in this repo, re-verify. |
| `8.4-8.8-55` | informative | A trailer section is sent as a field block after the request/response field block and all DATA frames have been sent. <br>_(all [CLIENT])_ | A trailer section is sent as a field block after both the request or response field block and all the DATA frames have been sent. | conn/: receive path must accept a second HEADERS block after DATA as trailers (and per §8.1 rules, reject pseudo-headers inside it). |
| `8.4-8.8-56` | informative | The HEADERS frame that starts the trailer field block carries the END_STREAM flag. <br>_(all [CLIENT])_ | The HEADERS frame starting the field block that comprises the trailer section has the END_STREAM flag set. | conn/: a trailer HEADERS without END_STREAM is malformed — verify the trailer path enforces END_STREAM and closes the stream on it. |

### 9113 section 9 - HTTP/2 Connections: Connection Management, Connection Reuse, Use of TLS Features (RFC 9113)

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `9-1` | informative | HTTP/2 connections are persistent; clients are expected to keep a connection open until no further communication with the server is needed or the server closes it. <br>_(client [CLIENT])_ | HTTP/2 connections are persistent. | client/ pool + managed-pool: verify conns are kept open and reused across requests rather than closed per-request. |
| `9-2` | SHOULD NOT | A client should not open more than one HTTP/2 connection to the same host:port pair (host from URI, alt-svc, or configured proxy). <br>_(client [CLIENT])_ | Clients SHOULD NOT open more than one HTTP/2 connection to a given host and port pair | client/ pool: check pool keys on host:port and coalesces concurrent Do calls onto one conn instead of dialing duplicates (note: load-generator use case may deliberately violate — document if so). |
| `9-3` | informative | A client may create replacement connections when stream-ID space is nearly exhausted, to refresh TLS keying material, or after connection errors. <br>_(client [CLIENT])_ | A client can create additional connections as replacements, either to replace connections that are near to exhausting the available stream identifier space | client/ managed-pool: verify a conn nearing stream-ID exhaustion (§5.1.1) or hit by a connection error is replaced, not reused until failure. |
| `9-4` | MAY | A client may open multiple connections to the same IP:port when using different SNI values or different TLS client certificates. <br>_(client [CLIENT])_ | A client MAY open multiple connections to the same IP address and TCP port using different Server Name Indication [TLS-EXT] values | client/ pool key: verify TLS config identity (ServerName, client certs) participates in the pool key so different-SNI conns are not wrongly coalesced. |
| `9-5` | SHOULD | A client should avoid creating multiple connections with the same configuration. <br>_(client [CLIENT])_ | SHOULD avoid creating multiple connections with the same configuration | client/ pool: same as 9-2 — singleflight/dedupe on identical (host,port,tls-config) dials. |
| `9-6` | informative | Servers are encouraged to keep connections open as long as possible but are permitted to terminate idle connections. <br>_(server [CLIENT])_ | Servers are encouraged to maintain open connections for as long as possible but are permitted to terminate idle connections if necessary | client/ pool must tolerate server-initiated idle close (GOAWAY or bare TCP FIN) by evicting the conn and dialing fresh, not surfacing an error to the next caller. |
| `9-7` | SHOULD | Whichever endpoint closes the TCP connection should first send a GOAWAY frame so both sides can determine which frames were processed. <br>_(endpoint [CLIENT])_ | the terminating endpoint SHOULD first send a GOAWAY (Section 6.8) frame | conn/ Close(): verify the client sends GOAWAY before closing the transport; also verify received-GOAWAY drain path (B.2.6) determines which in-flight streams were processed. |
| `9-8` | MAY | A connection to an origin server (direct or via CONNECT tunnel) may be reused for requests with different URI authority components. <br>_(client [CLIENT])_ | MAY be reused for requests with multiple different URI authority components | client/: if cross-authority coalescing is implemented, gate it on the authoritative checks in 9-9/9-10; if pool is strictly per-authority, this MAY is unexercised — safe. |
| `9-9` | informative | Connection reuse is only permitted while the origin server is authoritative for the target URI (per §10.1 / RFC 9110 §4.3). <br>_(client [CLIENT])_ | A connection can be reused as long as the origin server is authoritative | client/ pool: verify no code path routes a request for authority X onto a conn established for authority Y without an authority check. |
| `9-10` | informative | For cleartext TCP (no TLS), cross-authority reuse requires the new host to have resolved to the same IP address. <br>_(client [CLIENT])_ | For TCP connections without TLS, this depends on the host having resolved to the same IP address | client/ resolver + pool: for h2c conns, cross-host reuse (if any) must compare resolved IPs; simplest conformant answer is never coalesce h2c across hosts. |
| `9-11` | MUST | To reuse an https connection for another host, the server's certificate must pass every check the client would perform on a fresh TLS connection for that host. <br>_(client [CLIENT])_ | The certificate presented by the server MUST satisfy any checks that the client would perform when forming a new TLS connection for the host in the URI | client/: if cross-authority coalescing exists, it must re-verify the stored peer certificate (SAN match, validity) against the new host; if coalescing is absent, MUST is vacuously satisfied — confirm and document. |
| `9-12` | informative | A server can reject misdirected reuse by responding 421 (Misdirected Request); the client should stop reusing that connection for that origin and retry elsewhere. <br>_(server [CLIENT])_ | indicate that it is not authoritative for a request by sending a 421 (Misdirected Request) status code | client/ retry policy: check whether 421 triggers retry on a fresh connection (and marks the conn non-authoritative for that origin) rather than being surfaced as a terminal response. |
| `9-13` | informative | A client configured with an HTTP/2 proxy sends all requests to that proxy over a single connection. <br>_(client,proxy [CLIENT])_ | all requests sent via a proxy reuse the connection to the proxy | client/: no HTTP/2 proxy support exists today — N/A; if proxy support is added, the proxy becomes the single pool key. |
| `9-14` | MUST | HTTP/2 over TLS requires TLS 1.2 or higher. <br>_(all [CLIENT])_ | Implementations of HTTP/2 MUST use TLS version 1.2 [TLS12] or higher for HTTP/2 over TLS | client/ ALPN transport: TLS config is caller-supplied (gosec G402 opt-in) — verify the dial path enforces/defaults MinVersion >= tls.VersionTLS12 even when the caller passes a permissive config, or documents the caller obligation. |
| `9-15` | SHOULD | General TLS usage guidance in the TLS BCP (RFC 7525 family) should be followed, plus the HTTP/2-specific restrictions. <br>_(all [CLIENT])_ | The general TLS usage guidance in [TLSBCP] SHOULD be followed | client/: Go crypto/tls defaults largely satisfy the BCP; verify no config knob downgrades below BCP (e.g. caller-supplied CipherSuites) without documentation. |
| `9-16` | MUST | The TLS implementation must support the Server Name Indication (SNI) extension. <br>_(all [CLIENT])_ | The TLS implementation MUST support the Server Name Indication (SNI) [TLS-EXT] extension to TLS | client/: crypto/tls supports SNI natively — satisfied by construction; no action beyond 9-17. |
| `9-17` | MUST | When the server is identified by a domain name, the client must send the server_name TLS extension unless another target-host mechanism is used. <br>_(client [CLIENT])_ | clients MUST send the server_name TLS extension unless an alternative mechanism to indicate the target host is used | client/ dial: verify tls.Config.ServerName is populated from the request authority/host before handshake (crypto/tls only sends SNI when ServerName is a non-IP, non-empty name). |
| `9-18` | MAY | An endpoint may immediately terminate an HTTP/2 connection that fails these TLS requirements with a connection error of type INADEQUATE_SECURITY. <br>_(endpoint [CLIENT])_ | An endpoint MAY immediately terminate an HTTP/2 connection that does not meet these TLS requirements with a connection error (Section 5.4.1) of type INADEQUATE_SECURITY | frame/: verify INADEQUATE_SECURITY (0xc) error code constant exists; conn/: verify receiving GOAWAY(INADEQUATE_SECURITY) is surfaced as a typed, non-retryable-on-same-config error. |
| `9-19` | MUST | HTTP/2 over TLS 1.2 deployments must disable TLS-level compression. <br>_(all [CLIENT])_ | A deployment of HTTP/2 over TLS 1.2 MUST disable compression | client/: Go crypto/tls never implements TLS compression — satisfied by construction; no check possible or needed. |
| `9-20` | MUST | HTTP/2 over TLS 1.2 deployments must disable TLS renegotiation. <br>_(all [CLIENT])_ | A deployment of HTTP/2 over TLS 1.2 MUST disable renegotiation | client/: crypto/tls default Renegotiation=RenegotiateNever — verify no code path sets RenegotiateOnceAsClient/RenegotiateFreelyAsClient on the h2 dial config. |
| `9-21` | MUST | An endpoint must treat a TLS renegotiation (after the preface) as a connection error of type PROTOCOL_ERROR. <br>_(endpoint [CLIENT])_ | An endpoint MUST treat a TLS renegotiation as a connection error (Section 5.4.1) of type PROTOCOL_ERROR | conn/: crypto/tls with RenegotiateNever fails the TLS record layer on a HelloRequest, which conn/ sees as a read error — verify that maps to connection teardown (PROTOCOL_ERROR semantics), not a per-stream retryable error. |
| `9-22` | MUST | Renegotiation may be used only to protect client credentials in the handshake, and any renegotiation must complete before the connection preface is sent. <br>_(endpoint [CLIENT])_ | but any renegotiation MUST occur prior to sending the connection preface | conn/ handshake: preface is written only after tls handshake returns and no renegotiation is possible afterwards (9-20) — satisfied by construction; confirm preface write strictly follows completed handshake. |
| `9-23` | SHOULD | A server should request a client certificate if it sees a renegotiation request immediately after connection establishment. <br>_(server [not-client])_ | A server SHOULD request a client certificate if it sees a renegotiation request immediately after establishing a connection | Server-only; no client action. |
| `9-24` | informative | A server may send an error of type HTTP_1_1_REQUIRED to ask the client to retry over a protocol that supports renegotiation; the client must recognize this error code. <br>_(server [CLIENT])_ | a server might use an error (Section 5.4) of type HTTP_1_1_REQUIRED to request that the client use a protocol that supports renegotiation | frame/: verify HTTP_1_1_REQUIRED (0xd) constant exists; client/: repo has no h2-to-h1 fallback by design — verify the error is at least surfaced distinctly (typed) so callers can retry via TransportH1, and document the non-fallback. |
| `9-25` | MUST | Implementations must support ephemeral key-exchange sizes of at least 2048 bits for DHE and 224 bits for ECDHE cipher suites. <br>_(all [CLIENT])_ | Implementations MUST support ephemeral key exchange sizes of at least 2048 bits for cipher suites that use ephemeral finite field Diffie-Hellman (DHE) | client/: Go crypto/tls offers no FFDHE suites (DHE unnegotiable) and its ECDHE curves (P-256+, X25519) all exceed 224 bits — satisfied by construction. |
| `9-26` | MUST | Clients must accept DHE key sizes up to 4096 bits. <br>_(client [CLIENT])_ | Clients MUST accept DHE sizes of up to 4096 bits | client/: N/A by construction — crypto/tls never negotiates DHE suites, so the client can never receive a DHE key; document as vacuous. |
| `9-27` | MAY | Endpoints may treat negotiation of key sizes below the 2048/224-bit floors as a connection error of type INADEQUATE_SECURITY. <br>_(endpoint [CLIENT])_ | Endpoints MAY treat negotiation of key sizes smaller than the lower limits as a connection error (Section 5.4.1) of type INADEQUATE_SECURITY | conn/: optional; unreachable with crypto/tls curve set. Only relevant as receiver of GOAWAY(INADEQUATE_SECURITY) — covered by 9-18. |
| `9-28` | SHOULD NOT | HTTP/2 over TLS 1.2 deployments should not use any of the cipher suites prohibited in Appendix A. <br>_(all [CLIENT])_ | A deployment of HTTP/2 over TLS 1.2 SHOULD NOT use any of the prohibited cipher suites listed in Appendix A | client/: Go's default TLS 1.2 suite list for h2-capable configs is AEAD/ECDHE-based (not on the Appendix A blocklist); verify caller-supplied CipherSuites are either passed through documented or filtered. |
| `9-29` | MAY | Endpoints may generate a connection error of type INADEQUATE_SECURITY if a prohibited cipher suite is negotiated. <br>_(endpoint [CLIENT])_ | Endpoints MAY choose to generate a connection error (Section 5.4.1) of type INADEQUATE_SECURITY if one of the prohibited cipher suites is negotiated | conn/: optional enforcement; if not implemented, must still handle receiving GOAWAY(INADEQUATE_SECURITY) from a stricter server (9-18). |
| `9-30` | MUST NOT | Implementations must not raise INADEQUATE_SECURITY for a cipher suite that is not prohibited; a client offering a non-prohibited suite must be prepared to use it with HTTP/2. <br>_(all [CLIENT])_ | Implementations MUST NOT generate this error in reaction to the negotiation of a cipher suite that is not prohibited | conn/: verify no cipher-suite policing beyond Appendix A exists (currently none — trivially satisfied); any future suite check must be limited to the prohibited list. |
| `9-31` | MUST | HTTP/2 deployments using TLS 1.2 must support TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256 with the P-256 curve to guarantee an intersecting suite. <br>_(all [CLIENT])_ | deployments of HTTP/2 that use TLS 1.2 MUST support TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256 [TLS-ECDHE] with the P-256 elliptic curve | client/: crypto/tls includes this suite and P-256 by default — verify the dial path does not let a caller-supplied CipherSuites/CurvePreferences list strip it for h2 conns, or document the caller obligation. |
| `9-32` | informative | Clients may advertise prohibited cipher suites to permit HTTP/1.1 fallback to non-h2 servers, accepting the risk of h2 being negotiated with a prohibited suite. <br>_(client [CLIENT])_ | clients might advertise support of cipher suites that are prohibited in order to allow for connection to servers that do not support HTTP/2 | client/ ALPN transport: if the same tls.Config serves both h1 and h2 ALPN outcomes, note the offered-suite set is shared; acceptable per this paragraph. |
| `9-33` | MUST NOT | HTTP/2 servers must not send post-handshake TLS 1.3 CertificateRequest messages. <br>_(server [not-client])_ | HTTP/2 servers MUST NOT send post-handshake TLS 1.3 CertificateRequest messages | Server-only; client-side counterpart is 9-34. |
| `9-34` | MUST | An HTTP/2 client must treat a TLS 1.3 post-handshake CertificateRequest as a connection error of type PROTOCOL_ERROR. <br>_(client [CLIENT])_ | HTTP/2 clients MUST treat a TLS post-handshake CertificateRequest message as a connection error (Section 5.4.1) of type PROTOCOL_ERROR | conn/: Go crypto/tls never offers post_handshake_auth, so an unsolicited post-handshake CertificateRequest is rejected at the TLS layer as an unexpected message — verify the resulting read error tears down the whole Conn (connection-error semantics), not just one stream. |
| `9-35` | informative | The post-handshake-authentication prohibition applies even when the client offered the post_handshake_auth TLS extension. <br>_(client,server [CLIENT])_ | The prohibition on post-handshake authentication applies even if the client offered the "post_handshake_auth" TLS extension | client/: crypto/tls does not offer post_handshake_auth — vacuous; if a custom TLS stack is ever pluggable, 9-34 must still hold. |
| `9-36` | informative | TLS 1.3 NewSessionTicket and KeyUpdate post-handshake messages are permitted and must be tolerated, as they do not interact with HTTP/2. <br>_(endpoint [CLIENT])_ | NewSessionTicket and KeyUpdate, which can be used as they have no direct interaction with HTTP/2 | conn/: crypto/tls processes both transparently inside Read — verify long-lived-conn tests (soak) exercise a server sending session tickets mid-connection without disturbing the frame reader. |
| `9-37` | MAY | TLS early data (0-RTT) may carry requests only if the RFC 8470 guidance (safe methods, Early-Data handling, 425 retry) is observed. <br>_(client [CLIENT])_ | TLS early data MAY be used to send requests, provided that the guidance in [RFC8470] is observed | client/: verify no 0-RTT is used on the h2 path (crypto/tls client early data would need explicit enabling) — if never sent, MAY is unexercised and RFC 8470 obligations do not attach. |
| `9-38` | informative | Requests sent in TLS early data must assume the initial (default) values for all server SETTINGS, since no SETTINGS have been received yet. <br>_(client [CLIENT])_ | Clients send requests in early data assuming initial values for all server settings | conn/: only relevant if 0-RTT is ever added — the handshake path must then use RFC 9113 default SETTINGS (e.g. MAX_FRAME_SIZE 16384, no peer MAX_CONCURRENT_STREAMS) until the server's SETTINGS arrive; today N/A. |
| `9-39` | informative | TLS 1.3 NewSessionTicket and KeyUpdate post-handshake messages are permitted since they do not interact with HTTP/2. <br>_(all [CLIENT])_ | [TLS13] defines other post-handshake messages, NewSessionTicket and KeyUpdate, which can be used as they have no direct interaction with HTTP/2. | client/ — crypto/tls consumes NewSessionTicket/KeyUpdate transparently; conn/ read path only sees application data; verify no error on mid-connection KeyUpdate (long-lived load-gen conns will hit it). |
| `9-40` | MAY | TLS early data may be used to send requests if the RFC 8470 guidance is observed. <br>_(client [CLIENT])_ | TLS early data MAY be used to send requests, provided that the guidance in [RFC8470] is observed. | client/ — no TLS-over-TCP early data in crypto/tls; unexercised MAY (0-RTT is also an explicit H3 non-goal); conformant. |
| `9-41` | informative | Requests sent in early data must assume the initial (default) values for all server settings. <br>_(client [CLIENT])_ | Clients send requests in early data assuming initial values for all server settings. | conn/ — only if early data were implemented: use RFC-default SETTINGS until the server's SETTINGS frame arrives; N/A today. |

### 9113 section 10 - Security Considerations

| ID | Level | Requirement (scope) | RFC 9113 verbatim | Impl check / status |
|----|-------|---------------------|-------------------|---------------------|
| `10-1` | informative | The security properties claimed for HTTP/2 hold only when TLS is used as described in Section 9.2. <br>_(all [CLIENT])_ | Many of the claims in this section do not hold unless TLS is used as described in Section 9.2. | conn/: verify h2-over-TLS handshake path enforces §9.2 profile (TLS 1.2+, ALPN); InsecureSkipVerify must stay caller opt-in (gosec G402 tuning). |
| `10-2` | informative | Server authoritativeness follows the HTTP definition of authority: local name resolution for http URIs, authenticated server identity for https URIs. <br>_(client,server [CLIENT])_ | This relies on local name resolution for the "http" URI scheme and the authenticated server identity for the "https" scheme. | conn/: confirm TLS ServerName is set from the request authority and certificate verification is on by default in the handshake config. |
| `10-3` | informative | Completing a TLS handshake with the HTTP/2 ALPN identifier is considered sufficient protection against cross-protocol attacks. <br>_(client [CLIENT])_ | Completing a TLS handshake with an ALPN identifier for HTTP/2 can be considered sufficient protection against cross-protocol attacks. | conn/: verify tls.Config.NextProtos includes "h2" and the negotiated protocol is checked to equal "h2" before sending the connection preface. |
| `10-4` | informative | Cleartext HTTP/2 has only minimal cross-protocol protection: the connection preface string is designed to confuse HTTP/1.1 servers but protects against nothing else. <br>_(client [CLIENT])_ | The connection preface (Section 3.4) contains a string that is designed to confuse HTTP/1.1 servers, but no special protection is offered for other protocols. | conn/: confirm the exact 24-byte preface "PRI * HTTP/2.0\r\n\r\nSM\r\n\r\n" is sent verbatim before any frame on both TLS and cleartext paths. |
| `10-5` | MUST | An intermediary translating an HTTP/2 message to another HTTP version must validate fields per Section 8.2 before translating. <br>_(intermediary [not-client])_ | An intermediary that translates an HTTP/2 request or response MUST validate fields according to the rules in Section 8.2 before translating a message to another HTTP version. | N/A for client (not a translator); conn/ already applies §8.2 field validation to received field blocks for its own consumption. |
| `10-6` | informative | Pseudo-header field values need validation beyond Section 8.2 before being combined into a URI or request line; simple concatenation of :scheme/:authority/:path/:method is insecure without full input validation. <br>_(intermediary,endpoint [CLIENT])_ | Simple concatenation is not secure unless the input values are fully validated. | conn/: check pseudo-header emission rejects CTLs, spaces, and empty values in :method/:scheme/:path/:authority built from the caller's URL before HPACK-encoding the request. |
| `10-7` | MUST | An intermediary receiving fields that require removal before forwarding (connection-specific fields, RFC 9110 §7.6.1) must remove or replace them when forwarding. <br>_(intermediary [not-client])_ | An intermediary that receives any fields that require removal before forwarding (see Section 7.6.1 of [HTTP]) MUST remove or replace those header fields when forwarding messages. | N/A as forwarder; conn/ must still reject connection-specific headers on send/receive per §8.2.2 (separate 8.2.2 facts cover that). |
| `10-8` | informative | Intermediaries forwarding messages containing Content-Length should ensure the message is well-formed per Section 8.1.1 so HTTP/1.1 translation framing stays correct. <br>_(intermediary [not-client])_ | intermediaries should take care when forwarding messages containing Content-Length fields to ensure that the message is well-formed (Section 8.1.1). | N/A as forwarder; conn/ already enforces content-length vs. summed DATA length (§8.1.1 malformed check) on received responses. |
| `10-9` | informative | Pushed responses carry no explicit client request; the request is supplied by the server inside the PUSH_PROMISE frame. <br>_(client,server [CLIENT])_ | Pushed responses do not have an explicit request from the client; the request is provided by the server in the PUSH_PROMISE frame. | conn/: confirm the client advertises SETTINGS_ENABLE_PUSH=0 in its initial SETTINGS; then any PUSH_PROMISE received must be a connection error PROTOCOL_ERROR (§6.6). |
| `10-10` | MUST | A server hosting multiple tenants must ensure a tenant cannot push representations of resources outside that tenant's authority. <br>_(server,origin [not-client])_ | that server MUST ensure that tenants are not able to push representations of resources that they do not have authority over. | N/A — server-side multi-tenant push policy; no client obligation. |
| `10-11` | MUST NOT | A client must not use or cache pushed responses for which the origin server is not authoritative. <br>_(client [CLIENT])_ | Pushed responses for which an origin server is not authoritative (see Section 10.1) MUST NOT be used or cached. | conn/: with ENABLE_PUSH=0 this is moot by construction — verify push is disabled and PUSH_PROMISE is fatal; if push is ever accepted, an authority check against the connection's TLS identity is mandatory before use. |
| `10-12` | informative | SETTINGS values are the mechanism that strictly bounds the memory commitment of field-section compression and flow control. <br>_(endpoint [CLIENT])_ | Settings for these features ensure that memory commitments for these features are strictly bounded. | hpack/: decoder dynamic table must be hard-capped by our advertised SETTINGS_HEADER_TABLE_SIZE; conn/: receive windows bounded by advertised INITIAL_WINDOW_SIZE — verify both are enforced, not just advertised. |
| `10-13` | SHOULD | A client that accepts server push should limit the number of streams it allows in the reserved (remote) state, since PUSH_PROMISE count is not bounded by settings. <br>_(client [CLIENT])_ | A client that accepts server push SHOULD limit the number of streams it allows to be in the "reserved (remote)" state. | conn/: N/A while ENABLE_PUSH=0 is sent; if push support is added, a reserved(remote) stream-count cap is required alongside MAX_CONCURRENT_STREAMS. |
| `10-14` | informative | An excessive number of server-push streams can be treated as a stream error of type ENHANCE_YOUR_CALM. <br>_(client [CLIENT])_ | An excessive number of server push streams can be treated as a stream error (Section 5.4.2) of type ENHANCE_YOUR_CALM. | frame/: verify ENHANCE_YOUR_CALM (0xb) is defined in the error-code table; conn/: RST_STREAM emission path can carry it if push handling is ever added. |
| `10-15` | informative | Tiny WINDOW_UPDATE increments from a peer can force the sender to generate a large number of small DATA frames — an outbound-frame amplification DoS vector. <br>_(endpoint,sender [CLIENT])_ | Providing tiny increments to flow control in WINDOW_UPDATE frames can cause a sender to generate a large number of DATA frames. | conn/: check acquireSendCredits/writeData does not emit unboundedly small DATA frames under 1-byte credit drip from a malicious server; receiver side, recvWindowRefundThreshold=32KiB already avoids us being the dripper. |
| `10-16` | informative | The mandatory PING response obligation is an outbound-frame amplification vector: a peer can force frame generation by sending PINGs. <br>_(endpoint [CLIENT])_ | An endpoint is required to respond to a PING frame. | conn/: PING auto-echo (ACK=1) — verify echoes are written with backpressure under wmu, not queued unboundedly, if the server floods PINGs. |
| `10-17` | informative | The mandatory SETTINGS acknowledgment is an outbound-frame amplification vector: every peer SETTINGS frame forces an ACK. <br>_(endpoint [CLIENT])_ | Each SETTINGS frame requires acknowledgment. | conn/: verify the SETTINGS-ACK write in connHandler.OnSettings is synchronous/bounded so a server SETTINGS flood cannot grow an unbounded ACK queue. |
| `10-18` | informative | Invalid requests or pushes provoke RST_STREAM frames in response — another way an attacker induces frame generation. <br>_(endpoint [CLIENT])_ | An invalid request (or server push) can cause a peer to send RST_STREAM frames in response. | conn/: check inbound RST_STREAM floods (and our own RST-generating paths, e.g. event-buffer-overflow REFUSED_STREAM) do bounded per-frame work with no amplification loop. |
| `10-19` | informative | An endpoint that builds and retains frames for sending on HTTP/2 flow-control credit without regard to TCP-level backpressure is subject to resource exhaustion. <br>_(endpoint,sender [CLIENT])_ | An endpoint that constructs and remembers frames for sending without considering TCP limits might be subject to resource exhaustion. | conn/: writeData writes through the socket under wmu (blocking on TCP) rather than queueing — verify the v0.8.0 group-commit batching buffer is bounded and blocks, never grows, when TCP stalls. |
| `10-20` | informative | Floods of small or empty frames waste receiver processing time on frame headers; some small frames (empty end-of-stream DATA/CONTINUATION) are legitimate. <br>_(endpoint,receiver [CLIENT])_ | Large numbers of small or empty frames can be abused to cause a peer to expend time processing frame headers. | frame/+conn/: check the reader loop's cost per empty DATA/CONTINUATION from a server, and whether any counter/limit exists (must still permit legitimate empty END_STREAM DATA). |
| `10-21` | informative | SETTINGS frames can be abused to burn processing time via pointless changes, multiple undefined settings, or the same setting repeated within one frame. <br>_(endpoint [CLIENT])_ | This might be done by pointlessly changing settings, sending multiple undefined settings, or changing the same setting multiple times in the same frame. | conn/: applyPeerSettings must skip unknown identifiers in O(1) and apply last-value-wins for duplicates in one frame; confirm repeated INITIAL_WINDOW_SIZE changes don't do per-stream work beyond the required delta walk. |
| `10-22` | informative | Reprioritization via PRIORITY frames can require significant processing and cause overload when sent in volume. <br>_(endpoint [CLIENT])_ | Handling reprioritization with PRIORITY frames can require significant processing time and can lead to overload if many PRIORITY frames are sent. | frame/+conn/: RFC 9113 deprecates the 7540 priority scheme — verify inbound PRIORITY frames are parsed for length-validity only and discarded in O(1), with no priority-tree state. |
| `10-23` | informative | Field-section compression is a processing-resource attack surface; the abuse catalogue is Section 7 of RFC 7541. <br>_(endpoint [CLIENT])_ | Field section compression also provides opportunities for an attacker to waste processing resources; see Section 7 of [COMPRESSION] for more details on potential abuses. | hpack/: decoder must bound dynamic-table size to advertised HEADER_TABLE_SIZE, bound total decoded header-list bytes, and resist Huffman/length bombs (fuzz corpus closed in #198 — confirm h2 decoder shares that coverage, cf. sibling-divergence risk). |
| `10-24` | informative | SETTINGS limit reductions take effect only after a round trip, so an endpoint is exposed to peer behavior exceeding new limits; right after connection setup, server limits are unknown to clients and can be exceeded innocently. <br>_(endpoint [CLIENT])_ | Limits in SETTINGS cannot be reduced instantaneously, which leaves an endpoint exposed to behavior from a peer that could exceed the new limits. | conn/: verify we tolerate (no hard error) server traffic that exceeds a limit we just lowered before our SETTINGS is ACKed, and that our own sends respect server limits only once learned (initial-flight requests vs. unknown MAX_CONCURRENT_STREAMS). |
| `10-25` | SHOULD | Implementations should track use of DoS-capable features (SETTINGS churn, small frames, compression) and set limits on their use. <br>_(endpoint [CLIENT])_ | Implementations SHOULD track the use of these features and set limits on their use. | conn/: audit for any accounting/limits on inbound PING, SETTINGS, RST_STREAM, and empty-frame rates from the server — likely a deliberate gap for a load-gen client; document if so. |
| `10-26` | MAY | An endpoint may treat suspicious peer activity as a connection error of type ENHANCE_YOUR_CALM. <br>_(endpoint [CLIENT])_ | An endpoint MAY treat activity that is suspicious as a connection error (Section 5.4.1) of type ENHANCE_YOUR_CALM. | frame/: ENHANCE_YOUR_CALM (0xb) present in error-code enum; conn/: GOAWAY path must be able to carry an arbitrary error code so this option is available. |
| `10-27` | informative | There is no protocol-level hard limit on field-block size, so a receiver can be forced to buffer an entire arbitrarily large field block; endpoints need their own memory cap. <br>_(endpoint,receiver [CLIENT])_ | Since there is no hard limit to the size of a field block, some endpoints could be forced to commit a large amount of available memory for field blocks. | conn/: verify a hard cap on accumulated HEADERS+CONTINUATION bytes before/while HPACK-decoding a response field block (CONTINUATION-flood defense), with connection teardown on breach. |
| `10-28` | MAY | SETTINGS_MAX_HEADER_LIST_SIZE is advisory only; endpoints may send field blocks exceeding the peer's advertised limit, risking the message being treated as malformed. <br>_(endpoint,sender [CLIENT])_ | This setting is only advisory, so endpoints MAY choose to send field blocks that exceed this limit and risk the request or response being treated as malformed. | conn/+hpack/: encoder need not hard-fail requests over the server's advertised MAX_HEADER_LIST_SIZE (surface as caller choice); receiver side, exceeding OUR advertised limit may be treated as malformed (stream error) — check both directions. |
| `10-29` | informative | A client is permitted to discard responses it cannot process (e.g. field blocks larger than it will handle). <br>_(client [CLIENT])_ | A client can discard responses that it cannot process. | conn/: oversized-response handling may discard + RST_STREAM, but only subject to 10-30's decode obligation. |
| `10-30` | MUST | Even when a message is discarded, the received field block must still be fully processed (HPACK-decoded) to keep connection state consistent — the only alternative is closing the connection. <br>_(endpoint,receiver [CLIENT])_ | The field block MUST be processed to ensure a consistent connection state, unless the connection is closed. | hpack/+conn/: critical — verify every discard path (over-limit headers, refused/reset stream, unknown stream) still runs the shared Decoder over the complete HEADERS+CONTINUATION sequence so dynamic-table state stays synchronized, or tears down the connection. |
| `10-31` | informative | A proxy cannot rely on SETTINGS_MAX_CONCURRENT_STREAMS alone to bound CONNECT resource consumption, because TCP connection state (e.g. TIME_WAIT) outlives the stream. <br>_(proxy [not-client])_ | a proxy cannot rely on SETTINGS_MAX_CONCURRENT_STREAMS alone to limit the resources consumed by CONNECT requests. | N/A — proxy-side resource management; client CONNECT support unaffected. |
| `10-32` | MUST NOT | On a secure channel, content mixing confidential and attacker-controlled data must not be compressed in one context unless separate dictionaries per data source are used. <br>_(all [CLIENT])_ | Implementations communicating on a secure channel MUST NOT compress content that includes both confidential and attacker-controlled data unless separate compression dictionaries are used for each source of data. | hpack/: verify the encoder supports never-indexed literals (RFC 7541 §6.2.3/§7.1.3) and that sensitive request headers (authorization, cookie) are sent never-indexed / unindexed rather than entered into the dynamic table. |
| `10-33` | MUST NOT | Compression must not be used at all when the source of the data cannot be reliably determined. <br>_(all [CLIENT])_ | Compression MUST NOT be used if the source of data cannot be reliably determined. | hpack/: same check as 10-32 — a sensitive/never-index escape hatch must exist and be exposed to the client layer for caller-supplied secrets. |
| `10-34` | MUST NOT | Generic stream compression such as TLS-level compression must not be used with HTTP/2. <br>_(all [CLIENT])_ | Generic stream compression, such as that provided by TLS, MUST NOT be used with HTTP/2 (see Section 9.2). | conn/: Go crypto/tls never negotiates TLS compression, so satisfied by construction — confirm no custom wrapping io layer adds stream compression. |
| `10-35` | informative | HTTP/2 padding is a narrow countermeasure, not general-purpose padding: at best it increases the frames an attacker must observe, and flawed schemes (predictable randomized padding, fixed-size padding at boundaries) are easily defeated. <br>_(all [CLIENT])_ | Incorrectly implemented padding schemes can be easily defeated. | frame/: client emits no padding (verify writer sends PADDED=0 by default); inbound padding handled by internal/bytesx strip — no security padding claimed, which is the honest position. |
| `10-36` | SHOULD | Intermediaries should retain padding on DATA frames but may drop padding from HEADERS and PUSH_PROMISE frames; changing padding is valid only to improve its protection. <br>_(intermediary [not-client])_ | Intermediaries SHOULD retain padding for DATA frames but MAY drop padding for HEADERS and PUSH_PROMISE frames. | N/A — client is not a forwarding intermediary; local padding strip on receive is end-consumption, not forwarding. |
| `10-37` | informative | Observable behavioral differences — settings values, flow-control window management, priority allocation, reaction timing, settings-controlled feature handling — enable fingerprinting of a specific client. <br>_(client,server [CLIENT])_ | As far as these create observable differences in behavior, they could be used as a basis for fingerprinting a specific client | conn/: no action required for a load generator; note that the fixed initial SETTINGS values and 32KiB WINDOW_UPDATE cadence fingerprint the tool to servers. |
| `10-38` | informative | Single-connection preference allows correlating a user's activity on a site, and reusing one connection for different origins allows cross-origin tracking. <br>_(client [CLIENT])_ | Reusing connections for different origins allows tracking across those origins. | Pool lives in client/ (outside the three audited packages): conn/ itself never coalesces origins — verify the pool keys connections per-origin and never reuses a conn across different authorities. |
| `10-39` | informative | PING and SETTINGS solicit immediate responses and thus let a peer measure latency, with possible privacy implications. <br>_(endpoint [CLIENT])_ | Because the PING and SETTINGS frames solicit immediate responses, they can be used by an endpoint to measure latency to their peer. | conn/: auto PING-ACK and SETTINGS-ACK make the client measurable by servers — inherent to conformance, acceptable for a load-gen client; no code change. |
| `10-40` | informative | HTTP/2's concurrent request creation makes remote timing attacks on servers more efficient; the defense is making processing time independent of secret values. <br>_(server [not-client])_ | Ensuring that processing time is not dependent on the value of a secret is the best defense against any form of timing attack. | N/A — server-side defense; client-side there is no secret-dependent processing in frame/, conn/, or hpack/ decode paths worth constant-timing. |
| `10-41` | informative | Preferring a single TCP connection allows correlating one user's activity on a site, and reusing a connection across different origins allows cross-origin tracking. <br>_(client [CLIENT])_ | HTTP/2's preference for using a single TCP connection allows correlation of a user's activity on a site. Reusing connections for different origins allows tracking across those origins. | client/ pool (conn reuse layer above conn/): check whether pooled conns are keyed strictly per-origin — if any cross-origin coalescing exists, document the tracking implication. |
| `10-42` | informative | Because PING and SETTINGS solicit immediate responses, a peer can use them to measure latency, with possible privacy implications. <br>_(endpoint [CLIENT])_ | Because the PING and SETTINGS frames solicit immediate responses, they can be used by an endpoint to measure latency to their peer. | conn/: the mandatory PING-ACK echo makes the client measurable by servers — inherent to conformance, no action; note only. |
| `10-43` | informative | HTTP/2's concurrency and packet/record coalescing make remote timing attacks more efficient; the best defense is making processing time independent of secret values. <br>_(server [not-client])_ | Ensuring that processing time is not dependent on the value of a secret is the best defense against any form of timing attack. | Server-side defense; no client package action. |

