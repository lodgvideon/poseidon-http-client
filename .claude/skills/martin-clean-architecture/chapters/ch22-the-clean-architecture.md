# Chapter 22: The Clean Architecture

*Part V: Architecture — **the central chapter of the book***

## Core Idea
> **The Dependency Rule: "Source code dependencies must point only inward, toward higher-level policies."**

Concentric circles, outer = mechanisms, inner = policies. Everything else in this chapter is a consequence of that one rule.

## Framework: The Predecessors and What They Share

| Architecture | Origin |
|---|---|
| **Hexagonal Architecture** (Ports and Adapters) | Alistair Cockburn; adopted by Steve Freeman and Nat Pryce in *Growing Object Oriented Software with Tests* |
| **DCI** | James Coplien and Trygve Reenskaug |
| **BCE** | Ivar Jacobson, *Object Oriented Software Engineering: A Use-Case Driven Approach* |

*"Although these architectures all vary somewhat in their details, they are very similar. **They all have the same objective, which is the separation of concerns.** They all achieve this separation by dividing the software into layers. Each has at least one layer for business rules, and another layer for user and system interfaces."*

## Reference Table: The Five Characteristics

| Characteristic | What it means |
|---|---|
| **Independent of frameworks** | *"The architecture does not depend on the existence of some library of feature-laden software. This allows you to **use such frameworks as tools**, rather than forcing you to cram your system into their limited constraints"* |
| **Testable** | *"The business rules can be tested **without the UI, database, web server, or any other external element**"* |
| **Independent of the UI** | *"A web UI could be replaced with a console UI, for example, without changing the business rules"* |
| **Independent of the database** | *"You can swap out Oracle or SQL Server for Mongo, BigTable, CouchDB, or something else"* |
| **Independent of any external agency** | *"Your business rules **don't know anything at all** about the interfaces to the outside world"* |

## Framework: The Dependency Rule

> *"The concentric circles represent different areas of software. In general, **the further in you go, the higher level the software becomes. The outer circles are mechanisms. The inner circles are policies.**"*

> **"Source code dependencies must point only inward, toward higher-level policies."**

**The rule stated as prohibitions — this is the part people violate:**
- *"**Nothing in an inner circle can know anything at all about something in an outer circle.**"*
- *"In particular, **the name of something declared in an outer circle must not be mentioned by the code in an inner circle**. That includes functions, classes, variables, or any other named software entity."*
- *"By the same token, **data formats declared in an outer circle should not be used by an inner circle**, especially if those formats are generated by a framework in an outer circle."*

## Reference Table: The Four Circles (from inside out)

| Circle | Contains | Changes when… | Does NOT change when… |
|---|---|---|---|
| **Entities** | *"Enterprise-wide Critical Business Rules."* An object with methods, or *"a set of data structures and functions"* — *"it doesn't matter so long as the entities can be used by many different applications in the enterprise"* | Almost never — *"the **least likely** to change when something external changes"* | *"You would not expect these objects to be affected by a change to **page navigation or security**. **No operational change to any particular application should affect the entity layer.**"* |
| **Use Cases** | *"Application-specific business rules… These use cases **orchestrate the flow of data to and from the entities**, and direct those entities to use their Critical Business Rules"* | *"Changes to the **operation of the application**"* — *"if the details of a use case change, then some code in this layer will certainly be affected"* | Database, UI, or common framework changes |
| **Interface Adapters** | *"Adapters that convert data from the format most convenient for the use cases and entities, to the format most convenient for some external agency."* **Wholly contains the MVC architecture of a GUI** — presenters, views, and controllers all live here | External formats change | — |
| **Frameworks and Drivers** | *"Frameworks and tools such as the database and the web framework"* | Constantly | — |

**On the Entities circle when you have no "enterprise"**: *"If you don't have an enterprise and are writing just a single application, then these entities are the business objects of the application."*

**On the Interface Adapters circle — the SQL rule**: *"No code inward of this circle should know anything at all about the database. **If the database is a SQL database, then all SQL should be restricted to this layer** — and in particular to the parts of this layer that have to do with the database."* The MVC "models" are *"likely just data structures that are passed from the controllers to the use cases, and then back from the use cases to the presenters and views."*

**On the outermost circle**: *"Generally you don't write much code in this layer, other than **glue code** that communicates to the next circle inward… **The frameworks and drivers layer is where all the details go. The web is a detail. The database is a detail. We keep these things on the outside where they can do little harm.**"*

**Only four circles?**
> *"The circles are intended to be **schematic**: You may find that you need more than just these four. There's **no rule** that says you must always have just these four. **However, the Dependency Rule always applies.**"*

## Framework: Crossing Boundaries

**The apparent contradiction**: control flows *outward* (controller → use case → presenter) while all source dependencies point *inward*.

**The resolution:**
> *"We usually resolve this apparent contradiction by using the **Dependency Inversion Principle**… we would arrange interfaces and inheritance relationships such that the source code dependencies **oppose the flow of control at just the right points** across the boundary."*

**The concrete mechanism — the output port:**
> *"Suppose the use case needs to call the presenter. **This call must not be direct** because that would violate the Dependency Rule: No name in an outer circle can be mentioned by an inner circle. So we have the use case call an interface (shown as **'use case output port'**) **in the inner circle**, and have the presenter in the outer circle **implement** it."*

> *"The same technique is used to cross **all** the boundaries… We take advantage of **dynamic polymorphism** to create source code dependencies that oppose the flow of control so that we can conform to the Dependency Rule, **no matter which direction the flow of control travels**."*

## Framework: Which Data Crosses the Boundaries

> *"Typically the data that crosses the boundaries consists of **simple data structures**. You can use basic structs or simple data transfer objects… Or the data can simply be arguments in function calls. Or you can pack it into a hashmap, or construct it into an object. **The important thing is that isolated, simple data structures are passed across the boundaries.**"*

**The two named prohibitions:**
> *"**We don't want to cheat and pass Entity objects or database rows.** We don't want the data structures to have any kind of dependency that violates the Dependency Rule."*

**The row-structure example, spelled out**: many database frameworks return a convenient format — a "row structure." *"We don't want to pass that row structure **inward** across a boundary. Doing so would violate the Dependency Rule because it would **force an inner circle to know something about an outer circle**."*

> *"Thus, when we pass data across a boundary, **it is always in the form that is most convenient for the inner circle**."*

## Worked Example: A Typical Web/Java/Database Scenario (Fig 22.2)

Follow one request all the way through — note that *every* arrow crossing a boundary points inward:

1. **The web server** gathers input data from the user and hands it to the **`Controller`** (upper left).
2. The `Controller` **packages that data into a plain old Java object** and passes it through the **`InputBoundary`** to the **`UseCaseInteractor`**.
3. The `UseCaseInteractor` interprets the data and *"uses it to **control the dance of the Entities**."*
4. It also uses the **`DataAccessInterface`** to bring the Entities' data into memory from the **`Database`**.
5. On completion, it gathers data from the `Entities` and constructs **`OutputData`** — *"another plain old Java object."*
6. `OutputData` passes through the **`OutputBoundary`** interface to the **`Presenter`**.
7. The **`Presenter`** repackages `OutputData` into the **`ViewModel`** — *"yet another plain old Java object."*
8. The **`View`** moves data from the `ViewModel` into the HTML page.

**What the Presenter/ViewModel split actually buys** — the detail that makes this concrete:

| Object | Contents |
|---|---|
| **`OutputData`** | *"May contain **`Date` objects"*, `Currency` objects, or other business-related data |
| **`ViewModel`** | *"Mostly **Strings and flags**."* The Presenter *"will load the ViewModel with corresponding **Strings already formatted properly for the user**."* Button and MenuItem **names** go here, *"as are **flags that tell the View whether those Buttons and MenuItems should be gray**"* |

> *"This leaves the **View with almost nothing to do** other than to move the data from the ViewModel into the HTML page."*

*(That "almost nothing to do" is deliberate — it is the Humble Object pattern of Ch 23, and it is what makes the View the only untestable part.)*

## Mental Models
- **The Dependency Rule is a name-mention rule.** The practical check is textual: does any inner-circle file mention an outer-circle identifier? That includes framework-generated data formats.
- **Control flow and dependency direction are independent** — output ports exist precisely to let them diverge.
- **Formatting is an outward concern.** A `Date` inside, a formatted `String` outside. The moment inner code formats for display, the boundary has leaked.
- **Passing an ORM row inward is the most common violation**, because it's convenient and looks harmless.
- **Four circles is a diagram, not a law.** The invariant is the rule, not the count.

## Key Takeaways
1. **Source code dependencies point only inward.** No inner circle may name anything in an outer circle — including data formats.
2. Outer = mechanisms, inner = policies; abstraction and policy level increase as you move inward.
3. Entities (enterprise rules) → Use Cases (application rules) → Interface Adapters (MVC, SQL, conversions) → Frameworks & Drivers (glue only).
4. All SQL is confined to the interface adapters layer; the web and the database are details kept outside *"where they can do little harm."*
5. Cross boundaries with DIP and output ports so dependencies oppose the flow of control.
6. Only simple, isolated data structures cross boundaries — never Entity objects, never database rows.
7. Always convert data into the form most convenient for the **inner** circle.
8. The Presenter converts business types into display-ready Strings and flags, leaving the View trivial.
9. The count of circles is negotiable; the Dependency Rule is not.

## Connects To
- **Ch 11 (DIP)**: the curved line separating abstract from concrete becomes the Dependency Rule.
- **Ch 20 (Business Rules)**: Entities and Use Cases are the two inner circles, defined there.
- **Ch 23 (Presenters and Humble Objects)**: why the ViewModel is stuffed with formatted Strings and flags.
- **Ch 17–18 (Boundaries)**: where the lines go and what crossing them costs.
- **Ch 28 (The Test Boundary)**: testability as a structural consequence of this diagram.
- **Ch 30–32**: the database, the web, and frameworks as outermost-circle details.
- **Cockburn** (Hexagonal/Ports and Adapters), **Coplien & Reenskaug** (DCI), **Jacobson** (BCE).
